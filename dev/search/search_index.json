{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"MLClient","text":"<p>A Python client and CLI for MarkLogic Server. Start with a query or a document operation, then choose how much control your application needs.</p>"},{"location":"#start-here","title":"Start here","text":"<pre><code>pip install mlclient\n</code></pre> <pre><code>from mlclient import MLClient\n\nwith MLClient() as ml:\n    print(ml.eval.xquery('\"Hello World!\"'))  # Hello World!\n</code></pre> <p>This uses the local REST server at <code>http://localhost:8000</code> with Digest auth and <code>admin</code> / <code>admin</code> credentials. The quickstart explains prerequisites, different hosts, async usage and CLI setup.</p>"},{"location":"#one-library-several-ways-to-work","title":"One library, several ways to work","text":"Interface Start with Go further Python Parsed documents and query results Sync/async clients, transactions, raw API wrappers and custom endpoints CLI <code>ml env init</code>, <code>ml eval</code>, <code>ml logs</code> Import Gradle settings, discover App Servers and select multiple connections <p>The Python guide helps choose between <code>ml.documents</code>, <code>ml.rest.documents</code> and <code>ml.http</code>. The CLI guide gets a project connected without writing Python. Recipes show metadata cleanup, concurrent evaluations and a custom application API.</p>"},{"location":"#from-simple-usage-to-application-specific-control","title":"From simple usage to application-specific control","text":"<p>Use environments to share connection settings across scripts and commands. Configure authentication and TLS, then add HTTP retry, timeout and pool limits where the application needs them. Manage, Admin and Health can have independent connection settings.</p> <p>For your own application routes, extend the client with a Call, an API wrapper and a higher-level method. The API reference is generated from the implementation and describes all available modules and public signatures.</p>"},{"location":"#an-independent-alternative","title":"An independent alternative","text":"<p>MLClient began roughly eight months before the MarkLogic Python Client and continued as an independent project after a development break. Its focus includes async usage, composable client layers and command-line workflows. The background page explains the motivation and links to the MarkLogic client as another option.</p> <p>To help improve the project, read the contribution guide.</p>"},{"location":"about/","title":"Background and direction","text":"<p>MLClient is an independent Python library and CLI for MarkLogic. It offers synchronous and asynchronous clients, document models and services, REST/Manage/ Admin wrappers, and direct HTTP access. Its client layers compose HTTPX transport rather than subclassing a third-party HTTP session API.</p>"},{"location":"about/#why-another-client","title":"Why another client?","text":"<p>I began MLClient roughly eight months before the MarkLogic Python Client project began, and developed it without knowing that client had appeared. After putting my project aside for a few years, I returned to it and discovered the other library. I chose to continue because I enjoy Python and wanted to build on the work already started.</p> <p>The purpose is to offer another useful choice. If a requests-based client covers your needs and you do not need MLClient's async interfaces or CLI, the MarkLogic Python Client is worth considering. Its own documentation describes its supported operations and authentication; this is not a claim that the two libraries have identical scope or that every feature differs.</p> <p>MLClient focuses on async alongside sync, explicit connection/authentication configuration, several API levels, custom application extensions, and a CLI that can use multiple configured connections. The guides show those capabilities with working examples rather than requiring every application to use every layer.</p>"},{"location":"about/#what-comes-next","title":"What comes next","text":"<p>Broader REST endpoint coverage is a direction, not a claim of completeness today. The CLI may also grow project-management workflows as another option alongside tools such as ml-gradle. It does not currently replace ml-gradle's deployment functionality.</p> <p>More useful tools and approaches can make MarkLogic easier to adopt; users should be able to choose the workflow that fits their application.</p>"},{"location":"built-with/","title":"Built with","text":"<p>MLClient uses these open-source tools for its library, CLI and development workflow. Thanks to their maintainers and contributors.</p> <ul> <li>Project management -- Poetry. Dependency   resolution, virtual environments, packaging and the build backend.</li> <li>HTTP -- HTTPX. The HTTP client behind   every request, giving MLClient both synchronous and <code>asyncio</code> transports from   one API.</li> <li>CLI -- Cleo. The framework behind   the <code>ml</code> command-line interface: commands, arguments, prompts and styled   output.</li> <li>Format and lint -- Ruff. Linting and code   formatting in a single fast pass, shared between local development and CI.</li> <li>Docs -- MkDocs with   Material for MkDocs. This   documentation site; the API reference is generated from docstrings by   mkdocstrings.</li> </ul>"},{"location":"contributing/","title":"Contributing to MLClient","text":"<p>Bug reports, documentation improvements and code contributions are welcome. You do not need a running MarkLogic server to work on the documentation or run unit tests. For a substantial feature or a public API change, open an issue first so we can agree on its scope before you invest in an implementation. Small fixes can go straight to a pull request; draft PRs are welcome for early feedback.</p>"},{"location":"contributing/#reporting-a-problem","title":"Reporting a problem","text":"<p>Check existing issues first. Include a minimal example, expected and actual behavior, the MLClient and Python versions, and your operating system. For a server issue, include the MarkLogic version, authentication method, endpoint and relevant error code. Remove credentials, tokens, document contents and private environment details from examples and logs.</p> <p>Keep discussion respectful and focused on the problem. Explain disagreements with examples or evidence. Maintainers decide whether a change fits the library's scope; a useful idea may still belong in an application rather than in MLClient.</p> <p>Do not report an exploitable security issue in a public issue. Contact the maintainer privately at the email listed in <code>pyproject.toml</code>, initially without secrets or sensitive attachments, to arrange a disclosure channel.</p>"},{"location":"contributing/#set-up-a-development-checkout","title":"Set up a development checkout","text":"<p>Fork the repository, clone your fork and create a branch from the upstream <code>main</code>. If you have write access, use a feature branch in the main repository. Use a descriptive name such as <code>feature/262-documentation</code> or <code>fix/eval-timeout</code>; an issue number is useful when one exists.</p> <p>Use Python 3.11 and Poetry 2.2.0 to match the documentation build. Unit-test CI also covers Python 3.10, 3.12, 3.13 and 3.14; keep library code compatible with 3.10.</p> <pre><code>poetry env use python3.11\npoetry install --with docs\npoetry run ml --help\npoetry run pytest tests/unit -q\n</code></pre> <p>Install Poetry separately if it is not already available. Run commands with <code>poetry run</code> so they use this checkout's environment. Keep <code>poetry.lock</code> in sync when deliberately changing dependencies; avoid unrelated dependency upgrades.</p>"},{"location":"contributing/#how-the-library-fits-together","title":"How the library fits together","text":"<p>Trace a nearby implementation from its public entry point to the HTTP request before choosing where to put a change.</p> Area Responsibility <code>mlclient/calls/</code> Represent one request: method, path, parameters, headers and body. Calls perform no I/O. <code>mlclient/api/</code> Thin resource wrappers that send Calls and return raw responses, with sync and async counterparts. <code>mlclient/services/</code> Higher-level operations: parsed results, orchestration and resource lifecycles. <code>mlclient/_client.py</code>, <code>mlclient/_manager.py</code> Main client composition and environment-based client creation. <code>mlclient/clients/</code> HTTP transport, API call execution and session ownership. <code>mlclient/http.py</code> Resolved HTTP settings, cloning and session-sharing compatibility. <code>mlclient/env.py</code>, <code>mlclient/connection.py</code>, <code>mlclient/auth.py</code> Declarative connection settings, inheritance, validation and authentication. <code>mlclient/cli/</code> Cleo commands: parse input, call the library and format output. <code>tests/unit/</code>, <code>tests/integration/</code> Mocked behavior checks and real MarkLogic scenarios. <p>A new endpoint normally needs a Call and an API wrapper. Add a service when it provides a useful higher-level operation. Keep domain logic out of CLI commands. API wrappers execute requests through sync/async transport clients; Calls themselves are synchronous request descriptions.</p>"},{"location":"contributing/#public-imports-and-module-dependencies","title":"Public imports and module dependencies","text":"<p>Export a public symbol from one canonical namespace using <code>__all__</code>. Main clients and the manager belong to <code>mlclient</code>; transport clients to <code>mlclient.clients</code>; settings to <code>mlclient.http</code>, <code>mlclient.auth</code>, <code>mlclient.connection</code>, or <code>mlclient.env</code>; domain models to <code>mlclient.models</code>. The API reference reads these exports, so update the import contract tests and examples whenever the public surface changes. Internal helpers use <code>_</code> names and retain docstrings explaining their parameters, behavior, and exceptions.</p> <p>Implementation modules import definitions from their owning modules rather than through the root facade. Models, Calls, authentication, and transport must not import the main client, manager, services, or CLI. The main client composes those lower layers; jobs and CLI may use it. Type-only imports describe real dependencies and should follow the same direction.</p>"},{"location":"contributing/#design-and-code-quality","title":"Design and code quality","text":"<p>Prefer small, direct changes and existing helpers over new abstractions. Keep validation at the boundary that owns the value, and preserve useful server errors. Add a dependency only when its value justifies its maintenance and license obligations.</p> <p>Use public methods and properties rather than another object's private state. Prefer observable behavior in tests; test private mechanisms directly when that is the purpose of the test. Keep transport options out of MarkLogic query parameters. Do not hide errors with broad exception handling or fall back after unrelated failures. Diagnostic logs should explain attempts and failures without exposing credentials or sensitive payloads.</p> <p>Use type annotations and NumPy-style docstrings. Public methods and nontrivial private helpers should document parameters, return values, relevant exceptions and surprising behavior. Each sync and async method needs its own usable documentation; a reference to the other implementation is not enough.</p> <p>Follow neighboring code and the Ruff configuration in <code>pyproject.toml</code>. Fix lint findings rather than adding suppressions; discuss any necessary exception in the PR. Keep formatting-only changes out of unrelated files.</p>"},{"location":"contributing/#testing-a-change","title":"Testing a change","text":"<p>For a bug, start with a regression test that fails for the reported behavior. Test observable results and failure paths, rather than mirroring implementation steps. Keep new tests beside the relevant operation and parameter group, and use distinct test-module basenames to avoid collection collisions.</p> Test kind What it should establish Call unit tests Correct request construction, defaults and invalid input. API unit tests Parameters, headers and bodies reach the expected HTTP route; sync and async agree. Service unit tests Parsing, orchestration, routing, cleanup and fallback boundaries. CLI unit tests Parsing, defaults, short/long options, errors, output and exit status through Cleo <code>CommandTester</code>. Live API tests HTTP status/error contracts that mocks cannot establish. Live service tests Value added by the service, such as transaction isolation or a setting round-trip. <p>Reuse <code>MLRespXMocker</code> and existing document mock helpers. Match the request's parameters or body at the mock route to prove forwarding. For HTTP options such as timeout, inspect the resulting request extensions. Mirror affected sync and async behavior. Coverage is line-based; aim to retain 100% line coverage, but also test distinct requirements that happen to share a line.</p> <pre><code># During development, select the affected module or test.\npoetry run pytest tests/unit/mlclient/cli/commands/test_log_level_command.py -q\n\n# Before submitting a code change.\npoetry run pytest tests/unit --cov=mlclient --cov-report=term-missing\npoetry run ruff check .\ngit diff --check\n</code></pre> <p>Documentation-only changes need a documentation build and example checks, not new unit tests for prose.</p>"},{"location":"contributing/#running-against-marklogic","title":"Running against MarkLogic","text":"<p>The integration setup provisions a disposable MarkLogic server and a Kerberos KDC through Docker Compose. It needs Docker Compose, access to the image named in <code>tests/integration/Dockerfile.marklogic</code>, and free host ports 88, 7997 and 8000\u20138012. Use a development machine; the setup uses test credentials and tests can change server state. Do not point these tests at production.</p> <p>On Ubuntu, the full authentication matrix also needs <code>libkrb5-dev</code> and <code>krb5-user</code> installed before installing the <code>kerberos</code> extra.</p> <pre><code>poetry install --with docs --extras kerberos\ndocker compose -f tests/integration/docker-compose.yaml up -d --build\n</code></pre> <p>Wait until <code>mlclient_integration</code> reports <code>healthy</code>. The health check confirms provisioning, not just an open port:</p> <pre><code>docker inspect --format '{{.State.Health.Status}}' mlclient_integration\nMLCLIENT_IT_CERTS_DIR=tests/integration/.certs poetry run pytest tests/integration -ra\n</code></pre> <p>Review skips: missing certificates, OAuth configuration or Kerberos tooling can skip authentication scenarios. Record what ran and what was unavailable in the PR. See <code>.github/workflows/integration-test.yml</code> for the CI provisioning flow. When finished, stop the test containers:</p> <pre><code>docker compose -f tests/integration/docker-compose.yaml down\n</code></pre> <p>Live tests must restore changed settings and delete test data in <code>finally</code> blocks. Use generic names and dedicated test resources. Add live scenarios when they prove a server contract or service behavior; a separate integration suite for every Call is unnecessary. For CLI changes, also run representative commands as actual processes and check errors and output flags where relevant.</p>"},{"location":"contributing/#common-changes","title":"Common changes","text":""},{"location":"contributing/#supporting-a-rest-endpoint","title":"Supporting a REST endpoint","text":"<ol> <li>Check the official MarkLogic endpoint reference and the nearest Call/API pair.    Probe enumerated parameter values on a test server before defining accepted    values; record the server version and observed behavior in the PR.</li> <li>Add request construction and validation in <code>calls/</code>, with unit tests. Register    public imports and exports in <code>calls/__init__.py</code>.</li> <li>Add sync/async resource methods in <code>api/</code>, the appropriate REST/Manage/Admin    group accessors, and package exports. Forward HTTP request options separately    from endpoint parameters.</li> <li>Test the wire contract in both API variants. Add live coverage where a real    status code, server error or behavior needs verification.</li> <li>Update public docstrings and the Python guide. Canonical public exports enter the    generated API reference automatically; no reference stub is needed.</li> </ol>"},{"location":"contributing/#adding-or-changing-a-cli-command","title":"Adding or changing a CLI command","text":"<ol> <li>Reuse or implement the library operation first. Define positional arguments,    defaults and the difference between connection selection and operation targets.</li> <li>Register the command in <code>mlclient/cli/commands/__init__.py</code> and <code>mlclient/cli/app.py</code>.    Follow neighboring Cleo commands and test through <code>CommandTester</code>.</li> <li>Keep selectors consistent: <code>-e/--environment</code> selects an environment;    <code>-c/--connection</code> selects a connection identifier or port where applicable.    <code>logs -s/--server</code> accepts an identifier or port; <code>log-level -s/--server</code>    takes an actual MarkLogic App Server name. Short options are command-specific.</li> <li>Cover defaults, short/long forms, invalid values, output and exit codes.    Escape external text used in Cleo markup; preserve raw response bodies.</li> <li>Update the command page, CLI overview and navigation. Update README and Home    examples when applicable, and compare documented help with <code>ml COMMAND --help</code>.</li> </ol>"},{"location":"contributing/#adding-a-service","title":"Adding a service","text":"<p>Use existing API wrappers where possible. Decide which results to parse, which errors permit fallback, and how resources are closed or restored. Match sync and async support to the public surface being extended. Stateful transaction handles have a lifecycle; they are not a template for every service. Update exports, client accessors where appropriate, narrative examples and service tables.</p>"},{"location":"contributing/#changing-configuration-environments-or-client-factories","title":"Changing configuration, environments or client factories","text":"<p>Define omitted, explicit and <code>None</code> behavior before implementing overrides. Connection settings belong in environment models; retry, timeout and pool limits are HTTP runtime settings exposed through HTTP configuration, clients and the manager, not YAML environment fields.</p> <p>Trace defaults and overrides through the environment, manager and both client variants. Preserve explicit health settings and health-specific defaults. Transport-affecting changes need session-sharing checks: matching ports alone are insufficient. Auxiliary connections open lazily, and closing a client must close every unique session once.</p> <p>Test inheritance, invalid input, cloning, override precedence and mutation isolation. A per-request override must not affect the next request. Environment changes also need serialization, templates, <code>env init</code> and <code>env show</code> review. Inspect-only commands read raw configuration so injected defaults do not appear as if they were written in the user's file. Update setup examples and the Python guide with basic usage before advanced overrides.</p>"},{"location":"contributing/#documentation","title":"Documentation","text":"<p>Authored documentation is Markdown in <code>docs/</code>. The main entry points are <code>docs/index.md</code>, <code>docs/quickstart.md</code>, <code>docs/user/environments.md</code>, the Python task guides under <code>docs/user/python/</code>, and the command pages under <code>docs/user/cli/</code>. Keep README examples aligned with Home. Register new guide pages in <code>mkdocs.yml</code>; API pages are generated from canonical public exports and NumPy docstrings, including Pydantic fields and schemas.</p> <pre><code>make docs-serve\nmake docs-build\n</code></pre> <p><code>make docs-build</code> runs a strict MkDocs build and checks generated pages, links, anchors and representative API rendering. Do not commit <code>site/</code> or generated reference pages. See the documentation contribution guide for build details and versioned publication.</p>"},{"location":"contributing/#pull-requests-branches-and-releases","title":"Pull requests, branches and releases","text":"<p>Target <code>main</code> and keep one coherent purpose per PR. Explain the problem, the resulting behavior and the checks you ran. Include a small before/after example for user-facing changes, and identify compatibility changes and unavailable checks. Draft PRs are useful when the API design needs discussion.</p> <p>Rebase your feature branch when upstream changes affect it, especially after a PR it depends on has merged. Coordinate before rewriting a branch shared with others. Do not include unrelated branch work or generated files. Maintainers review scope, API consistency, tests and documentation before merging.</p> <p>MLClient is currently pre-1.0. Call out breaking changes explicitly; do not assume that every existing interface is frozen, or that a breaking change needs no explanation. CI currently tests Python 3.10\u20133.14; this does not establish support for every Python or MarkLogic version.</p> <p>Releases are maintainer work. Version preparation opens a PR; tagging happens only after it is squash-merged into <code>main</code>. Numeric tags trigger reusable test, coverage and lint gates, artifact checks, OIDC PyPI publication and release notes. See the release process for the commands, publication checks and recovery procedures.</p> <p>From 1.0.0, stable APIs and CLI commands follow SemVer. The entire jobs package is experimental and can change in minor releases. Stable feature removal needs a major release, a prior working deprecation with <code>DeprecationWarning</code>, and migration guidance. See the stability policy.</p>"},{"location":"contributing/#license-and-third-party-code","title":"License and third-party code","text":"<p>MLClient is currently distributed under the MIT license in <code>LICENSE</code>. Submit code you have the right to contribute under those terms, retain required attributions, and identify any third-party code or new dependency in the PR. A dependency has its own license; the project's MIT declaration does not replace its obligations.</p>"},{"location":"documentation/","title":"Contributing to the documentation","text":"<p>Documentation changes follow the same pull-request process as code changes. You can build the complete site locally without a MarkLogic server.</p>"},{"location":"documentation/#build-and-preview","title":"Build and preview","text":"<p>Use Python 3.11 and Poetry 2.2.0, matching CI:</p> <pre><code>make docs-install\nmake docs-serve\nmake docs-build\n</code></pre> <p>The optional <code>docs</code> dependency group is resolved in <code>poetry.lock</code>. Both local and CI installs use <code>poetry install --only main,docs</code>. To run library tests too, install the development group with <code>poetry install --with docs</code>.</p> <p>The local MkDocs preview renders a single version. The version catalog is provided by mike only in a versioned deployment (or <code>mike serve</code>); it is not available in the plain preview.</p> <p>MkDocs runs in strict mode. Broken internal links, unresolved API references and build warnings fail the build. The site is written to ignored <code>site/</code>; never commit it. Material supplies light/dark themes, search and the version selector.</p>"},{"location":"documentation/#writing-guides","title":"Writing guides","text":"<p>The repository-level <code>CONTRIBUTING.md</code> is the source of the website contribution guide. The existing generation script publishes it as <code>contributing.md</code>; edit the root file rather than creating a second copy under <code>docs/</code>.</p> <p>Write guides in Markdown under <code>docs/</code> and register them in <code>mkdocs.yml</code>. Start with a working example; introduce configuration only when the reader needs it. Split advanced topics into focused pages when their detail would overwhelm the quickstart.</p> <p>CLI pages follow: a short description and examples, then Arguments and Options. Use a third-level heading for each argument or option, including its short alias, default and meaningful constraints. Describe global options once in <code>user/cli.md</code>. Do not paste terminal help output into authored pages. <code>make docs-build</code> checks that each command argument, option and short alias has its own documented heading. Keep command families nested (<code>env</code> contains <code>init</code> and <code>show</code>). Keep <code>user/environments.md</code> focused on a basic environment and its Python usage; put inheritance, authentication overrides and advanced examples in <code>user/python/more-on-environments.md</code>.</p> <p>Use one top-level heading per authored page, with nested headings for its sections. Link to the shared configuration guides instead of repeating them in individual command or Python task pages.</p> <p>Keep README examples in sync with <code>docs/index.md</code>. Command help and examples must match the CLI on the branch being documented. Use fenced code blocks, Markdown tables and Material admonitions (<code>!!! note</code>). The example environment is included directly from <code>docs/user/environments/mlclient-local.yaml</code>.</p>"},{"location":"documentation/#updating-the-api-reference","title":"Updating the API reference","text":"<p><code>docs/gen_ref_pages.py</code> lists the canonical public namespaces and reads each namespace's <code>__all__</code>. It generates one page per exported class/function and namespace indexes for values and type aliases. Every symbol page shows the public import; headings and anchors use that same identifier. Implementation modules do not get their own pages. Do not add hand-maintained reference stubs.</p> <p>When adding an export, update its owning namespace and <code>tests/unit/mlclient/test_public_api.py</code>. Add a new namespace to the generator only when the public structure needs one. <code>scripts/check_docs.py</code> verifies the canonical pages and anchors, duplicate exports, links and representative API rendering. NumPy-style docstrings stay in Python source; source links may point to private modules without making those modules public imports.</p> <p>Experimental job classes and report types use the internal <code>@experimental</code> decorator. Its metadata supplies the warning notice on each reference page; <code>log_on_init=True</code> additionally warns when a job is constructed. Report models carry the notice without logging for every document.</p> <p><code>griffe-pydantic</code> renders model fields, validator relationships and JSON schema.</p>"},{"location":"documentation/#versioned-publishing","title":"Versioned publishing","text":"<p>Only <code>.github/workflows/docs.yml</code> writes the production <code>gh-pages</code> branch. PRs run a strict build; <code>main</code> publishes <code>dev</code>. The release workflow calls this workflow after successful package publication: stable tags publish their version and update <code>latest</code>, while prerelease tags publish a numbered entry only. See Releasing MLClient for release checks and tag preparation.</p> <p><code>make docs-deploy-dev</code> requests a CI run from <code>main</code> using authenticated GitHub CLI. It does not push local docs. A failed release documentation job can be retried through the release workflow. Republishing an older stable tag also moves <code>latest</code>; do this only when intentionally changing the default release.</p> <p>When changing documentation dependencies or deployment configuration, also check a local versioned preview with <code>mike serve</code> to verify the version selector.</p>"},{"location":"quickstart/","title":"Quickstart","text":"<p>Install MLClient:</p> <pre><code>pip install mlclient\n</code></pre>"},{"location":"quickstart/#run-a-query","title":"Run a query","text":"<pre><code>from mlclient import MLClient\n\nwith MLClient() as ml:\n    print(ml.eval.xquery('\"Hello World!\"'))  # Hello World!\n</code></pre> <p>The defaults connect to a local MarkLogic REST App Server at <code>http://localhost:8000</code> with Digest authentication and <code>admin</code> / <code>admin</code> credentials. You need a running server with those settings. The context manager closes connections on exit.</p>"},{"location":"quickstart/#connect-to-another-server","title":"Connect to another server","text":"<p>Pass only the settings you want to change:</p> <pre><code>with MLClient(\n    host=\"ml.example.com\", username=\"my-user\", password=\"my-password\"\n) as ml:\n    print(ml.eval.xquery('\"Hello World!\"'))\n</code></pre> <p>Add <code>port=8100</code> for a different REST port or <code>protocol=\"https\"</code> for HTTPS. Connection settings cover authentication and TLS; environments let you share configuration between Python and CLI.</p>"},{"location":"quickstart/#work-with-documents","title":"Work with documents","text":"<p>Create a document, write it and read its content back:</p> <pre><code>from mlclient import MLClient\nfrom mlclient.models import Document\n\nwith MLClient() as ml:\n    document = Document.create(\"/hello.json\", {\"message\": \"Hello, MarkLogic!\"})\n    ml.documents.write(document)\n    document = ml.documents.read(\"/hello.json\")\n    print(document.content)  # {'message': 'Hello, MarkLogic!'}\n    ml.documents.delete(\"/hello.json\")\n</code></pre> <p>The documents guide covers XML, JSON, text, binary content and metadata. Use transactions to group related changes into one commit or rollback.</p>"},{"location":"quickstart/#use-rest-manage-admin-and-raw-http","title":"Use REST, Manage, Admin and raw HTTP","text":"<p>The same client exposes MarkLogic's API tiers alongside higher-level services such as <code>ml.documents</code> and <code>ml.eval</code>:</p> <pre><code>with MLClient() as ml:\n    query_response = ml.rest.eval.post(xquery='\"Hello World!\"')\n    databases_response = ml.manage.databases.get_list()\n    timestamp_response = ml.admin.get_timestamp()\n    raw_response = ml.http.post(\"/v1/eval\", {\"xquery\": '\"Hello World!\"'})\n</code></pre> <p>These calls return HTTPX responses, giving you access to status codes, headers and response bodies. Use <code>ml.rest</code>, <code>ml.manage</code> and <code>ml.admin</code> for named endpoint methods, or <code>ml.http</code> to control the method, path, parameters, headers and body yourself.</p> <p>The API wrappers use their corresponding connections (REST on port 8000, Manage on 8002 and Admin on 8001 by default). <code>ml.http</code> uses the primary connection; the URL path does not switch ports. Your account needs permission for each operation. See Clients for details.</p>"},{"location":"quickstart/#use-async","title":"Use async","text":"<p>The asynchronous client exposes the same operations:</p> <pre><code>import asyncio\nfrom mlclient import AsyncMLClient\n\n\nasync def main():\n    async with AsyncMLClient() as ml:\n        print(await ml.eval.xquery('\"Hello World!\"'))\n\n\nasyncio.run(main())\n</code></pre> <p>See async support for client reuse and concurrent requests.</p>"},{"location":"quickstart/#use-the-command-line","title":"Use the command line","text":"<pre><code>ml env init\nml eval -x '\"Hello World!\"'\nml logs\nml logs --all-hosts --regex 'Forest M.*'\nml version\n</code></pre> <p>Review the environment generated by <code>ml env init</code> before sending requests. The CLI guide covers importing Gradle settings, discovering servers and running commands. To choose between parsed results, REST wrappers and raw HTTP in Python, continue with clients and API layers.</p>"},{"location":"recipes/","title":"Recipes","text":"<p>These examples use stable document and client APIs. Experimental jobs are not required. Start with a test database and credentials allowed to perform the operations shown.</p>"},{"location":"recipes/#replace-a-collection-without-rewriting-content","title":"Replace a collection without rewriting content","text":"<p>A data cleanup often changes classification rather than document content. Read the complete metadata, modify its collection membership, and write a metadata-only document. Keeping the read and write in a transaction avoids a separate uncoordinated read/modify/write sequence.</p> <pre><code>\"\"\"Replace a collection on one document without rewriting its content.\"\"\"\n\nfrom mlclient import MLClient\nfrom mlclient.models import Document\n\n\ndef replace_collection(ml: MLClient, uri: str, old: str, new: str) -&gt; bool:\n    \"\"\"Read and update metadata in one transaction; return whether it changed.\"\"\"\n    if not new or not new.strip():\n        raise ValueError(\"New collection must not be blank\")\n    with ml.transaction() as transaction:\n        document = ml.documents.read(uri, category=\"metadata\", **transaction)\n        metadata = document.metadata\n        if old == new or not metadata.remove_collection(old):\n            return False\n        metadata.add_collection(new)\n        ml.documents.write(Document.metadata_update(uri, metadata), **transaction)\n        return True\n</code></pre> <p>Use it with a configured client:</p> <pre><code>from mlclient import MLClient\nfrom collections_recipe import replace_collection\n\nwith MLClient() as ml:\n    changed = replace_collection(ml, \"/invoices/42.json\", \"pending\", \"processed\")\n    print(changed)\n</code></pre> <p>Save the first block as <code>collections_recipe.py</code>. The operation preserves content and other metadata by reading all metadata categories before updating. It returns <code>False</code> when the old collection is absent or the names are equal. A blank new collection name is rejected before opening a transaction.</p> <p>A successful context-manager exit commits; an exception rolls back. For a large migration, use a deliberate batch size and checkpoint strategy rather than one unbounded transaction. See document models and transactions.</p>"},{"location":"recipes/#evaluate-queries-across-databases-concurrently","title":"Evaluate queries across databases concurrently","text":"<p>Build a small report of document counts across databases. Each evaluation runs in a different database context, so these are independent requests that can overlap. Reuse one async client and allow at most four evaluations at a time:</p> <pre><code>\"\"\"Count documents in a small set of databases using independent eval requests.\"\"\"\n\nimport asyncio\n\nimport httpx\n\nfrom mlclient import AsyncMLClient\n\n\nasync def count_documents(databases: list[str]) -&gt; dict[str, int]:\n    \"\"\"Return document counts, allowing up to four evaluations at a time.\"\"\"\n    limits = httpx.Limits(max_connections=4)\n    async with AsyncMLClient(limits=limits) as ml:\n\n        async def count_one(database):\n            count = await ml.eval.xquery(\n                \"fn:count(fn:collection())\",\n                database=database,\n            )\n            return database, count\n\n        return dict(await asyncio.gather(*(count_one(name) for name in databases)))\n</code></pre> <p>Save the block as <code>database_counts.py</code>, then run it for databases accessible to your account:</p> <pre><code>import asyncio\nfrom database_counts import count_documents\n\ncounts = asyncio.run(count_documents([\"Documents\", \"Modules\"]))\nprint(counts)\n</code></pre> <p>Each query counts the documents visible to the connecting user in that database. The requests do not share a transaction or a cross-database snapshot. Use this for independent reporting, not operations that need an atomic result.</p> <p>One client means one connection pool, so <code>max_connections</code> is the concurrency cap: <code>gather</code> schedules every task, but only four requests reach the network at once and the rest await a free connection. That is why no semaphore is needed here. For a large list, process bounded batches - pool limits bound in-flight requests, not the number of scheduled tasks.</p> <p>To read several documents from the same database, prefer a single bulk read with <code>ml.documents.read(uris)</code>.</p>"},{"location":"recipes/#cap-concurrency-across-several-hosts-of-one-cluster","title":"Cap concurrency across several hosts of one cluster","text":"<p>Pool limits bound a single client. When the work spreads over several hosts, each host needs its own client and therefore its own pool, so <code>max_connections</code> can no longer bound the total. A shared <code>asyncio.Semaphore</code> is the aggregate cap. Derive each host's client from one resolved configuration with <code>HTTPConfig.clone(host=...)</code>, so credentials, TLS and timeouts stay identical:</p> <pre><code>\"\"\"Run one query on every host of a cluster under a shared concurrency cap.\"\"\"\n\nimport asyncio\n\nfrom mlclient import AsyncMLClient\nfrom mlclient.http import HTTPConfig\n\n\nasync def eval_on_each_host(\n    hosts: list[str],\n    xquery: str,\n    *,\n    max_in_flight: int = 4,\n) -&gt; dict[str, str]:\n    \"\"\"Evaluate a query against each host, capping how many run at once.\n\n    Each host needs its own client, so each has its own connection pool -\n    pool limits bound a single client, not the aggregate across clients. One\n    shared semaphore caps how many hosts are queried concurrently.\n    \"\"\"\n    base = HTTPConfig.resolve(port=8000)\n    semaphore = asyncio.Semaphore(max_in_flight)\n\n    async def eval_on(host):\n        async with semaphore, AsyncMLClient(config=base.clone(host=host)) as ml:\n            return host, await ml.eval.xquery(xquery)\n\n    return dict(await asyncio.gather(*(eval_on(host) for host in hosts)))\n</code></pre> <p>Run one read-only probe on every host:</p> <pre><code>import asyncio\nfrom cluster_hosts import eval_on_each_host\n\nnames = asyncio.run(eval_on_each_host(\n    [\"node-1.example\", \"node-2.example\", \"node-3.example\"],\n    \"xdmp:host-name()\",\n))\nprint(names)\n</code></pre> <p><code>AsyncExitStack</code> closes every client on exit, including on error. The semaphore caps evaluations in flight across all hosts at once; raise <code>max_in_flight</code> to allow more overlap, or lower it to spare a loaded cluster.</p>"},{"location":"recipes/#put-a-custom-application-api-behind-one-method","title":"Put a custom application API behind one method","text":"<p>If scripts repeatedly compose the same raw HTTP calls, give the operation a name. The custom API guide builds <code>ml.rest.tasks.list()</code> for the raw response and <code>ml.tasks.open_titles()</code> for parsed Python on a shared connection, with sync and async versions. This keeps transport details out of application scripts while retaining access to the raw response when needed.</p>"},{"location":"releasing/","title":"Release process","text":"<p>Releases are tag-driven and maintained on a single integration branch, <code>main</code>. Use short-lived <code>feature/mlclient-NNN</code> branches and squash-merge reviewed PRs. There is no <code>develop</code> branch. Create a maintenance branch such as <code>release/1.x</code> only when an older major actually needs a supported patch.</p>"},{"location":"releasing/#prepare-review-then-tag","title":"Prepare, review, then tag","text":"<p>Maintainers prepare the version in a pull request and publish it after review. Use Poetry 2.2.0 and an authenticated GitHub CLI (<code>gh</code>):</p> <pre><code>git switch main\ngit pull --ff-only\npoetry install --with docs\nmake release-minor\n</code></pre> <p><code>release-patch</code>, <code>release-minor</code> and <code>release-major</code> use Poetry's version arithmetic. They require a clean <code>main</code> equal to <code>origin/main</code>, create a <code>feature/release-X.Y.Z</code> branch, synchronize versions, commit only version files, push that branch and open a PR using authenticated <code>gh</code>. They never commit or push a version change directly to <code>main</code>.</p> <p>For a candidate, specify the exact version; a generic prerelease increment does not necessarily produce the intended major or <code>rc</code> phase:</p> <pre><code>make release-pre VERSION=1.0.0rc1\n</code></pre> <p>Poetry remains the version tool. The pinned project plugin <code>poetry-bumpversion</code> synchronizes <code>mlclient/_version.py</code>; Poetry updates <code>pyproject.toml</code> itself. README uses version-independent links. The helper checks that the exported version matches the package version before committing or tagging. A prepared 1.x candidate is marked Beta in package metadata; a stable 1.x version is marked Production/Stable. Installing dependencies with Poetry 2.2.0 makes the required plugin available.</p> <p>After required checks pass, squash-merge the version PR. Then:</p> <pre><code>git switch main\ngit pull --ff-only\nmake release-tag\n</code></pre> <p>This pushes an annotated tag matching both version files. It refuses a dirty checkout, an unmerged local commit, a feature branch or an existing tag. Tags are not moved. The target deliberately does not publish from your machine.</p>"},{"location":"releasing/#what-ci-does","title":"What CI does","text":"<p><code>.github/workflows/release.yml</code> validates the tag syntax, version consistency and that its commit is reachable from <code>origin/main</code>. It then runs the reusable lint, Python 3.10\u20133.14 unit/100%-coverage matrix and live integration workflows.</p> <p>Only after those gates succeed does CI build documentation, a wheel and an sdist. It installs the wheel in a clean environment outside the checkout, verifies the import/version and CLI entry point, and checks installed dependencies. The same uploaded distributions are passed to PyPI publishing and attached to the GitHub Release; publishing does not rebuild them.</p> <p>PyPI uses OIDC Trusted Publishing with the <code>pypi</code> environment. No stored API token is needed. GitHub release notes are generated from merged PRs and grouped by <code>breaking</code>, <code>feature</code>/<code>enhancement</code>, <code>fix</code>/<code>bug</code>, and <code>docs</code>/<code>documentation</code> labels. Conventional Commits are not required. Review important migration notes before announcing a release; generated categories do not explain compatibility for you.</p> <p>After publication, the reusable documentation workflow publishes the tagged version. Stable versions update <code>latest</code>; <code>a</code>, <code>b</code> and <code>rc</code> versions get their own docs entry without replacing stable <code>latest</code>. Main-branch pushes still publish <code>dev</code>. A TestPyPI run does not publish a GitHub Release or release docs.</p>"},{"location":"releasing/#testpypi-and-recovery","title":"TestPyPI and recovery","text":"<p>A manual run of <code>release.yml</code> always targets TestPyPI. Select an existing numeric release or prerelease tag, not a branch:</p> <pre><code>gh workflow run release.yml --ref 1.0.0rc1\n</code></pre> <p>Run this for a tag already present in the repository. It does not turn the tag push into a dry run: pushing a release tag normally triggers production PyPI.</p> <p>If tests fail, fix the source through a PR and prepare a new version. Do not move the old tag. If registry publication succeeded but a later job failed, use Re-run failed jobs so the successful PyPI upload is not repeated. PyPI versions are immutable. A Pages failure can be retried independently.</p>"},{"location":"stability/","title":"API stability and compatibility","text":""},{"location":"stability/#the-10-contract","title":"The 1.0 contract","text":"<p>Starting with MLClient 1.0.0, stable public Python APIs and CLI commands follow Semantic Versioning:</p> Version change Meaning Major A breaking public API or CLI change, including removed/renamed symbols, incompatible signatures or changed default behavior Minor A backward-compatible feature, such as a new method, optional parameter or command Patch A backward-compatible fix, documentation correction or internal refactor <p>The contract covers public exports from <code>mlclient</code> and public APIs documented in the reference. Names or modules beginning with <code>_</code> are implementation details. The explicitly experimental jobs API is an exception described below. An import that merely happens to expose a third-party helper is not a supported extension point; use documented imports.</p>"},{"location":"stability/#deprecation","title":"Deprecation","text":"<p>A stable feature continues to work while deprecated. Its warning is a Python <code>DeprecationWarning</code> naming the replacement and intended removal version, and its documentation and release notes explain how to migrate. Removal or an incompatible change happens in a major release, not a minor or patch release. Python may hide <code>DeprecationWarning</code> by default; applications can enable it with <code>python -Wd</code> during testing.</p> <p>Dropping a Python version is treated as a minor release and announced in release notes. This platform-support exception is separate from the API/CLI contract.</p>"},{"location":"stability/#experimental-jobs","title":"Experimental jobs","text":"<p>Everything in <code>mlclient.jobs</code>, including builders and report models, remains experimental. It may change incompatibly in a minor release while the API is being redesigned. It is retained for existing users and remains in the generated reference, but is not a stable workflow recommended by the user guide.</p> <p>Constructing <code>ReadDocumentsJob</code> or <code>WriteDocumentsJob</code> logs a warning on that job's module logger. Importing the package does not emit a warning. Python logging configuration controls where that warning is displayed. Prefer <code>MLClient.documents</code> or <code>AsyncMLClient.documents</code> for stable document operations.</p>"},{"location":"stability/#supported-and-tested-versions","title":"Supported and tested versions","text":"<p>The unit-test CI matrix covers Python 3.10, 3.11, 3.12, 3.13 and 3.14. The live integration setup currently uses MarkLogic 11.2.0. Coverage on that server does not establish that every endpoint and authentication combination works on every MarkLogic release; check the server's endpoint documentation for version-specific requirements.</p>"},{"location":"stability/#release-candidates","title":"Release candidates","text":"<p>Versions use bare numeric tags, such as <code>1.0.0</code>, without a <code>v</code> prefix. Candidates use PEP 440 suffixes such as <code>1.0.0rc1</code> or <code>1.0.0b1</code>:</p> <pre><code>pip install --pre mlclient\n</code></pre> <p>Normal installation selects stable releases when available. A candidate is for validation before a stable release; consult its release notes before upgrading. See GitHub Releases for changes, and the release process for maintainer instructions.</p>"},{"location":"reference/","title":"API reference","text":"<p>Start with MLClient, AsyncMLClient, or MLClientManager. Browse the namespaces for configuration, models and lower-level APIs.</p>"},{"location":"reference/SUMMARY/","title":"SUMMARY","text":"<ul> <li>Overview</li> <li>mlclient<ul> <li>AsyncMLClient</li> <li>MLClient</li> <li>MLClientManager</li> </ul> </li> <li>mlclient.clients<ul> <li>ApiClient</li> <li>AsyncApiClient</li> <li>AsyncHttpClient</li> <li>HttpClient</li> </ul> </li> <li>mlclient.env<ul> <li>MLEnvironment</li> <li>MLServerConfig</li> <li>find_mlclient_directory</li> <li>find_mlclient_environment</li> </ul> </li> <li>mlclient.http<ul> <li>HTTPConfig</li> </ul> </li> <li>mlclient.auth<ul> <li>AuthConfig</li> <li>KerberosAuth</li> <li>MarkLogicCloudAuth</li> <li>OAuthBearerAuth</li> </ul> </li> <li>mlclient.connection<ul> <li>CloudConfig</li> <li>SSLConfig</li> </ul> </li> <li>mlclient.models<ul> <li>BinaryDocument</li> <li>Category</li> <li>Document</li> <li>DocumentType</li> <li>DocumentsBodyPart</li> <li>DocumentsBodyPartType</li> <li>DocumentsDisposition</li> <li>Extract</li> <li>JSONDocument</li> <li>LogType</li> <li>MarkLogicVersion</li> <li>Metadata</li> <li>MetadataDocument</li> <li>Mimetype</li> <li>Mimetypes</li> <li>Permission</li> <li>Repair</li> <li>TextDocument</li> <li>XMLDocument</li> </ul> </li> <li>mlclient.api<ul> <li>AdminApi</li> <li>AsyncAdminApi</li> <li>AsyncDatabasesApi</li> <li>AsyncDocumentsApi</li> <li>AsyncEvalApi</li> <li>AsyncForestsApi</li> <li>AsyncGroupsApi</li> <li>AsyncHostsApi</li> <li>AsyncLogsApi</li> <li>AsyncManageApi</li> <li>AsyncRestApi</li> <li>AsyncRolesApi</li> <li>AsyncServersApi</li> <li>AsyncTransactionsApi</li> <li>AsyncUsersApi</li> <li>DatabasesApi</li> <li>DocumentsApi</li> <li>EvalApi</li> <li>ForestsApi</li> <li>GroupsApi</li> <li>HostsApi</li> <li>LogsApi</li> <li>ManageApi</li> <li>RestApi</li> <li>RolesApi</li> <li>ServersApi</li> <li>TransactionsApi</li> <li>UsersApi</li> </ul> </li> <li>mlclient.calls<ul> <li>ApiCall</li> <li>DatabaseDeleteCall</li> <li>DatabaseGetCall</li> <li>DatabasePostCall</li> <li>DatabasePropertiesGetCall</li> <li>DatabasePropertiesPutCall</li> <li>DatabasesGetCall</li> <li>DatabasesPostCall</li> <li>DocumentsDeleteCall</li> <li>DocumentsGetCall</li> <li>DocumentsPostCall</li> <li>EvalCall</li> <li>ForestDeleteCall</li> <li>ForestGetCall</li> <li>ForestPostCall</li> <li>ForestPropertiesGetCall</li> <li>ForestPropertiesPutCall</li> <li>ForestsGetCall</li> <li>ForestsPostCall</li> <li>ForestsPutCall</li> <li>GroupPropertiesGetCall</li> <li>GroupPropertiesPutCall</li> <li>HostsGetCall</li> <li>LogsCall</li> <li>RoleDeleteCall</li> <li>RoleGetCall</li> <li>RolePropertiesGetCall</li> <li>RolePropertiesPutCall</li> <li>RolesGetCall</li> <li>RolesPostCall</li> <li>ServerConfigGetCall</li> <li>ServerDeleteCall</li> <li>ServerGetCall</li> <li>ServerPropertiesGetCall</li> <li>ServerPropertiesPutCall</li> <li>ServersGetCall</li> <li>ServersPostCall</li> <li>TimestampGetCall</li> <li>TransactionGetCall</li> <li>TransactionPostCall</li> <li>TransactionsPostCall</li> <li>UserDeleteCall</li> <li>UserGetCall</li> <li>UserPropertiesGetCall</li> <li>UserPropertiesPutCall</li> <li>UsersGetCall</li> <li>UsersPostCall</li> </ul> </li> <li>mlclient.services<ul> <li>AsyncDocumentsService</li> <li>AsyncEvalService</li> <li>AsyncLogsService</li> <li>AsyncTransactionService</li> <li>DocumentsService</li> <li>EvalService</li> <li>LogLevelService</li> <li>LogsService</li> <li>TraceEvents</li> <li>TraceEventsService</li> <li>TransactionService</li> <li>async_open_transaction</li> <li>open_transaction</li> </ul> </li> <li>mlclient.responses<ul> <li>MLResponseParser</li> </ul> </li> <li>mlclient.multipart<ul> <li>MultipartPart</li> <li>decode_multipart_mixed</li> <li>encode_multipart_mixed</li> </ul> </li> <li>mlclient.io<ul> <li>DocumentsLoader</li> <li>DocumentsWriter</li> </ul> </li> <li>mlclient.exceptions<ul> <li>ConfigError</li> <li>EnvironmentFileExistsError</li> <li>InvalidLogTypeError</li> <li>InvalidMetadataError</li> <li>MLClientDirectoryNotFoundError</li> <li>MLClientEnvironmentNotFoundError</li> <li>MarkLogicError</li> <li>NoRestServerConfiguredError</li> <li>NoSuchAppServerError</li> <li>NotARestServerError</li> <li>ResourceNotFoundError</li> <li>UnsupportedFileExtensionError</li> <li>UnsupportedFormatError</li> <li>WrongParametersError</li> </ul> </li> <li>mlclient.logging<ul> <li>setup_logger</li> </ul> </li> <li>mlclient.jobs<ul> <li>DocumentJobReport</li> <li>DocumentReport</li> <li>DocumentStatus</li> <li>DocumentStatusDetails</li> <li>ReadDocumentsJob</li> <li>WriteDocumentsJob</li> </ul> </li> </ul>"},{"location":"reference/mlclient/","title":"<code>mlclient</code>","text":""},{"location":"reference/mlclient/#mlclient","title":"mlclient","text":"<p>Python clients for MarkLogic: direct connections and environment management.</p>"},{"location":"reference/mlclient/#mlclient.__version__","title":"__version__  <code>module-attribute</code>","text":"<pre><code>__version__ = '0.4.1'\n</code></pre>"},{"location":"reference/mlclient/AsyncMLClient/","title":"AsyncMLClient","text":"<pre><code>from mlclient import AsyncMLClient\n</code></pre>"},{"location":"reference/mlclient/AsyncMLClient/#mlclient.AsyncMLClient","title":"mlclient.AsyncMLClient","text":"<pre><code>AsyncMLClient(\n    protocol=UNSET,\n    host: str = \"localhost\",\n    port=UNSET,\n    auth: AuthParam = UNSET,\n    username: str = \"admin\",\n    password: str = \"admin\",\n    ssl: SSLConfig | None = None,\n    cloud: CloudConfig | None = None,\n    retry: Retry | int | None = None,\n    limits: Limits | int | None = None,\n    timeout=UNSET,\n    *,\n    config: HTTPConfig | None = None,\n    manage_config: HTTPConfig | None = None,\n    admin_config: HTTPConfig | None = None,\n    health_config: HTTPConfig | None = None,\n)\n</code></pre> <p>Async entry point for MarkLogic interaction.</p> <p>Provides layered access (all methods are async):</p> <ul> <li><code>ml.http.get(\"/endpoint\")</code> -- raw HTTP</li> <li><code>ml.rest.eval.post(xquery=\"...\")</code> -- mid-level REST API (<code>/v1/*</code>)</li> <li><code>ml.manage.databases.get_list()</code> -- mid-level Management API</li> <li><code>ml.admin.get_timestamp()</code> -- mid-level Admin API (<code>/admin/v1/*</code>)</li> <li><code>ml.healthcheck()</code> -- HEAD <code>/</code> on the HealthCheck server (port 7997)</li> <li><code>ml.rest.call(SomeApiCall())</code> -- advanced: custom Call objects</li> <li><code>ml.parser.parse(resp)</code> -- manual parsing of raw responses</li> <li><code>ml.documents.read(\"/doc.json\")</code> -- higher-level, parsed results</li> <li><code>ml.eval.xquery(\"1+1\")</code> -- higher-level, parsed results</li> <li><code>await ml.version()</code> -- complete MarkLogic version with four numeric parts</li> <li><code>ml.transaction(database=...)</code> -- open a scoped transaction (context manager)</li> </ul> <p>Initialize AsyncMLClient instance.</p> <p>The connection parameters describe the primary connection. The Manage (8002), Admin (8001) and HealthCheck (7997) connections are derived from it by default; pass <code>manage_config</code> / <code>admin_config</code> / <code>health_config</code> only to point them at a different host, credentials or port. The derived HealthCheck connection targets port 7997 with no authentication.</p> <p>Parameters:</p> Name Type Description Default <code>protocol</code> <code>str</code> <p>A protocol used for HTTP requests (http / https)</p> <code>\"http\"</code> <code>host</code> <code>str</code> <p>A host name</p> <code>\"localhost\"</code> <code>port</code> <code>int</code> <p>An App Service port</p> <code>8000</code> <code>auth</code> <code>str | Auth | AuthConfig | None</code> <p>An authentication method: a string shortcut (\"basic\", \"digest\", \"digestbasic\", \"certificate\", \"kerberos\"), an AuthConfig, a custom httpx.Auth, or None</p> <code>\"digest\"</code> <code>username</code> <code>str</code> <p>A username</p> <code>\"admin\"</code> <code>password</code> <code>str</code> <p>A password</p> <code>\"admin\"</code> <code>ssl</code> <code>SSLConfig | None</code> <p>SSL/TLS configuration; a client certificate forces HTTPS and defaults the auth method to \"certificate\"</p> <code>None</code> <code>cloud</code> <code>CloudConfig | None</code> <p>MarkLogic Cloud configuration; forces HTTPS on port 443 and handles authentication via the API key</p> <code>None</code> <code>retry</code> <code>Retry | int | None</code> <p>A retry strategy; an int is shorthand for Retry(total=n)</p> <code>Retry(total=5, backoff_factor=0.5)</code> <code>limits</code> <code>Limits | int | None</code> <p>Connection-pool limits; None defers to httpx's own default; an int is shorthand for httpx.Limits(max_connections=n)</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>The request timeout. Unset uses DEFAULT_TIMEOUT (connect=5, read=60, write=60, pool=5 seconds). None disables every HTTP timeout. A number sets all four components to that many seconds. An httpx.Timeout fully overrides them without merging. The derived HealthCheck connection always uses _HEALTH_TIMEOUT, independent of this value</p> <code>unset</code> <code>config</code> <code>HTTPConfig | None</code> <p>An already-resolved primary configuration; when given, the connection parameters above are ignored</p> <code>None</code> <code>manage_config</code> <code>HTTPConfig | None</code> <p>An already-resolved Manage configuration; when given, it is used instead of deriving the Manage connection from the primary</p> <code>None</code> <code>admin_config</code> <code>HTTPConfig | None</code> <p>An already-resolved Admin configuration; when given, it is used instead of deriving the Admin connection from the primary</p> <code>None</code> <code>health_config</code> <code>HTTPConfig | None</code> <p>An already-resolved HealthCheck configuration; when given, it is used instead of deriving the HealthCheck connection (port 7997, no auth, no retries, _HEALTH_TIMEOUT) from the primary. Retry and timeout are resolved independently: an unspecified retry defaults to no retries and an unspecified timeout to _HEALTH_TIMEOUT, while an explicitly supplied retry strategy or timeout (including None to disable it) is preserved</p> <code>None</code> Source code in <code>mlclient/_client.py</code> <pre><code>def __init__(\n    self,\n    protocol=UNSET,\n    host: str = \"localhost\",\n    port=UNSET,\n    auth: AuthParam = UNSET,\n    username: str = \"admin\",\n    password: str = \"admin\",\n    ssl: SSLConfig | None = None,\n    cloud: CloudConfig | None = None,\n    retry: Retry | int | None = None,\n    limits: Limits | int | None = None,\n    timeout=UNSET,\n    *,\n    config: HTTPConfig | None = None,\n    manage_config: HTTPConfig | None = None,\n    admin_config: HTTPConfig | None = None,\n    health_config: HTTPConfig | None = None,\n):\n    \"\"\"Initialize AsyncMLClient instance.\n\n    The connection parameters describe the primary connection. The Manage\n    (8002), Admin (8001) and HealthCheck (7997) connections are derived from it\n    by default; pass ``manage_config`` / ``admin_config`` / ``health_config``\n    only to point them at a different host, credentials or port. The derived\n    HealthCheck connection targets port 7997 with no authentication.\n\n    Parameters\n    ----------\n    protocol : str, default \"http\"\n        A protocol used for HTTP requests (http / https)\n    host : str, default \"localhost\"\n        A host name\n    port : int, default 8000\n        An App Service port\n    auth : str | httpx.Auth | AuthConfig | None, default \"digest\"\n        An authentication method: a string shortcut (\"basic\", \"digest\",\n        \"digestbasic\", \"certificate\", \"kerberos\"), an AuthConfig, a custom\n        httpx.Auth, or None\n    username : str, default \"admin\"\n        A username\n    password : str, default \"admin\"\n        A password\n    ssl : SSLConfig | None, default None\n        SSL/TLS configuration; a client certificate forces HTTPS and\n        defaults the auth method to \"certificate\"\n    cloud : CloudConfig | None, default None\n        MarkLogic Cloud configuration; forces HTTPS on port 443 and handles\n        authentication via the API key\n    retry : Retry | int | None, default Retry(total=5, backoff_factor=0.5)\n        A retry strategy; an int is shorthand for Retry(total=n)\n    limits : httpx.Limits | int | None, default None\n        Connection-pool limits; None defers to httpx's own default; an int\n        is shorthand for httpx.Limits(max_connections=n)\n    timeout : httpx.Timeout | float | None, default unset\n        The request timeout. Unset uses DEFAULT_TIMEOUT (connect=5, read=60,\n        write=60, pool=5 seconds). None disables every HTTP timeout. A number\n        sets all four components to that many seconds. An httpx.Timeout fully\n        overrides them without merging. The derived HealthCheck connection\n        always uses _HEALTH_TIMEOUT, independent of this value\n    config : HTTPConfig | None, default None\n        An already-resolved primary configuration; when given, the\n        connection parameters above are ignored\n    manage_config : HTTPConfig | None, default None\n        An already-resolved Manage configuration; when given, it is used\n        instead of deriving the Manage connection from the primary\n    admin_config : HTTPConfig | None, default None\n        An already-resolved Admin configuration; when given, it is used\n        instead of deriving the Admin connection from the primary\n    health_config : HTTPConfig | None, default None\n        An already-resolved HealthCheck configuration; when given, it is used\n        instead of deriving the HealthCheck connection (port 7997, no auth,\n        no retries, _HEALTH_TIMEOUT) from the primary. Retry and timeout are\n        resolved independently: an unspecified retry defaults to no retries\n        and an unspecified timeout to _HEALTH_TIMEOUT, while an explicitly\n        supplied retry strategy or timeout (including None to disable it) is\n        preserved\n    \"\"\"\n    self._http = AsyncHttpClient(\n        protocol=protocol,\n        host=host,\n        port=port,\n        auth=auth,\n        username=username,\n        password=password,\n        ssl=ssl,\n        cloud=cloud,\n        retry=retry,\n        limits=limits,\n        timeout=timeout,\n        config=config,\n    )\n    self._manage_http = self._secondary_http(\n        manage_config or self._http.config.clone(port=MARKLOGIC_MANAGE_PORT),\n    )\n    self._admin_http = self._secondary_http(\n        admin_config or self._http.config.clone(port=MARKLOGIC_ADMIN_PORT),\n    )\n    self._health_http = self._secondary_http(\n        health_config.with_health_defaults()\n        if health_config\n        else self._http.config.clone(\n            port=MARKLOGIC_HEALTHCHECK_PORT,\n            auth=None,\n            retry=NO_RETRY_STRATEGY,\n            timeout=_HEALTH_TIMEOUT,\n        ),\n    )\n</code></pre>"},{"location":"reference/mlclient/AsyncMLClient/#mlclient.AsyncMLClient.admin","title":"admin  <code>cached</code> <code>property</code>","text":"<pre><code>admin: AsyncAdminApi\n</code></pre> <p>Administration API on the configured Admin connection.</p> <p>Uses <code>admin_config</code> when supplied, otherwise the derived Admin connection (port 8001 by default). The wrapper is cached per client; its session opens on the first request, not when this property is read. Access this property normally; asynchronous methods run when awaited.</p> <p>Returns:</p> Type Description <code>AsyncAdminApi</code> <p>The connection-bound API or service object.</p>"},{"location":"reference/mlclient/AsyncMLClient/#mlclient.AsyncMLClient.documents","title":"documents  <code>cached</code> <code>property</code>","text":"<pre><code>documents: AsyncDocumentsService\n</code></pre> <p>Read, write and delete documents as Python models.</p> <p>Uses the primary REST connection and returns parsed documents or metadata. The service is created once per client without network I/O. Each operation executes its own requests; document results are not cached. Access this property normally; asynchronous methods run when awaited.</p> <p>Returns:</p> Type Description <code>AsyncDocumentsService</code> <p>The connection-bound API or service object.</p>"},{"location":"reference/mlclient/AsyncMLClient/#mlclient.AsyncMLClient.eval","title":"eval  <code>cached</code> <code>property</code>","text":"<pre><code>eval: AsyncEvalService\n</code></pre> <p>Evaluate XQuery or JavaScript and parse the returned values.</p> <p>Uses the primary REST connection. The service is cached per client, not the query results: every evaluation sends a new request. Accessing this property alone performs no network I/O. Access this property normally; asynchronous methods run when awaited.</p> <p>Returns:</p> Type Description <code>AsyncEvalService</code> <p>The connection-bound API or service object.</p>"},{"location":"reference/mlclient/AsyncMLClient/#mlclient.AsyncMLClient.http","title":"http  <code>property</code>","text":"<pre><code>http: AsyncHttpClient\n</code></pre> <p>Raw HTTP access through the primary connection.</p> <p>Uses the main connection settings and returns raw httpx responses. Accessing this property sends no request; request methods open the session on demand. The client context manager owns its lifetime. Access this property normally; asynchronous methods run when awaited.</p> <p>Returns:</p> Type Description <code>AsyncHttpClient</code> <p>The connection-bound API or service object.</p>"},{"location":"reference/mlclient/AsyncMLClient/#mlclient.AsyncMLClient.manage","title":"manage  <code>cached</code> <code>property</code>","text":"<pre><code>manage: AsyncManageApi\n</code></pre> <p>Management API on the configured Manage connection.</p> <p>Uses <code>manage_config</code> when supplied, otherwise the derived Manage connection (port 8002 by default). The wrapper is cached per client; its session opens on the first request, not when this property is read. Access this property normally; asynchronous methods run when awaited.</p> <p>Returns:</p> Type Description <code>AsyncManageApi</code> <p>The connection-bound API or service object.</p>"},{"location":"reference/mlclient/AsyncMLClient/#mlclient.AsyncMLClient.parser","title":"parser  <code>property</code>","text":"<pre><code>parser: type[MLResponseParser]\n</code></pre> <p>Parse raw MarkLogic HTTP responses explicitly.</p> <p>Returns the parser class, not a parser instance. Call <code>ml.parser.parse(response)</code> for a response from the raw API. Parsing is local and performs no network requests.</p> <p>Returns:</p> Type Description <code>type[MLResponseParser]</code> <p>The parser class.</p>"},{"location":"reference/mlclient/AsyncMLClient/#mlclient.AsyncMLClient.rest","title":"rest  <code>cached</code> <code>property</code>","text":"<pre><code>rest: AsyncRestApi\n</code></pre> <p>Client REST API on the primary connection (<code>/v1/*</code>).</p> <p>Requires a REST-enabled App Server. The wrapper is created once per client on first access; accessing it sends no request. Endpoint methods execute fresh requests and return raw httpx responses. Access this property normally; asynchronous methods run when awaited.</p> <p>Returns:</p> Type Description <code>AsyncRestApi</code> <p>The connection-bound API or service object.</p>"},{"location":"reference/mlclient/AsyncMLClient/#mlclient.AsyncMLClient.connect","title":"connect  <code>async</code>","text":"<pre><code>connect()\n</code></pre> <p>Open the primary local HTTP session.</p> <p>This allocates transport resources; it does not probe connectivity or authenticate with the server. Auxiliary sessions open on their first request. Prefer the client context manager for automatic cleanup.</p> Source code in <code>mlclient/_client.py</code> <pre><code>async def connect(self):\n    \"\"\"Open the primary local HTTP session.\n\n    This allocates transport resources; it does not probe connectivity or\n    authenticate with the server. Auxiliary sessions open on their first\n    request. Prefer the client context manager for automatic cleanup.\n    \"\"\"\n    await self._http.connect()\n</code></pre>"},{"location":"reference/mlclient/AsyncMLClient/#mlclient.AsyncMLClient.disconnect","title":"disconnect  <code>async</code>","text":"<pre><code>disconnect()\n</code></pre> <p>Close the primary and auxiliary sessions owned by this client.</p> <p>Shared HTTP clients are closed once. Cached API/service wrappers are retained. Subsequent operations may open sessions again; use an async context manager to give their lifetime an explicit scope.</p> Source code in <code>mlclient/_client.py</code> <pre><code>async def disconnect(self):\n    \"\"\"Close the primary and auxiliary sessions owned by this client.\n\n    Shared HTTP clients are closed once. Cached API/service wrappers are\n    retained. Subsequent operations may open sessions again; use an async\n    context manager to give their lifetime an explicit scope.\n    \"\"\"\n    for http in {\n        self._http,\n        self._manage_http,\n        self._admin_http,\n        self._health_http,\n    }:\n        await http.disconnect()\n</code></pre>"},{"location":"reference/mlclient/AsyncMLClient/#mlclient.AsyncMLClient.healthcheck","title":"healthcheck  <code>async</code>","text":"<pre><code>healthcheck(*, timeout=UNSET) -&gt; bool\n</code></pre> <p>Report whether the HealthCheck app server (port 7997 by default) is healthy.</p> <p>Sends a HEAD request to <code>/</code> on the HealthCheck connection.</p> <p>Parameters:</p> Name Type Description Default <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this probe. Unset uses the HealthCheck connection's timeout (five seconds per component by default). None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them.</p> <code>unset</code> <p>Returns:</p> Type Description <code>bool</code> <p>True when the server answered 2xx, False when it answered 5xx</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> <code>HTTPStatusError</code> <p>If the server answered with a 4xx status, which signals a misdirected request (the HealthCheck server takes no auth) rather than an unhealthy server</p> Source code in <code>mlclient/_client.py</code> <pre><code>async def healthcheck(self, *, timeout=UNSET) -&gt; bool:\n    \"\"\"Report whether the HealthCheck app server (port 7997 by default) is healthy.\n\n    Sends a HEAD request to ``/`` on the HealthCheck connection.\n\n    Parameters\n    ----------\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this probe. Unset uses the HealthCheck\n        connection's timeout (five seconds per component by default). None\n        disables every HTTP timeout; a number sets all four components to\n        that many seconds; an httpx.Timeout overrides them.\n\n    Returns\n    -------\n    bool\n        True when the server answered 2xx, False when it answered 5xx\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    httpx.HTTPStatusError\n        If the server answered with a 4xx status, which signals a\n        misdirected request (the HealthCheck server takes no auth) rather\n        than an unhealthy server\n    \"\"\"\n    return _healthy_or_raise(await self._health_http.head(\"/\", timeout=timeout))\n</code></pre>"},{"location":"reference/mlclient/AsyncMLClient/#mlclient.AsyncMLClient.is_connected","title":"is_connected","text":"<pre><code>is_connected() -&gt; bool\n</code></pre> <p>Return a connection status.</p> <p>Returns:</p> Type Description <code>bool</code> <p>True if the primary local HTTP session is open; otherwise False. This does not probe the server or describe auxiliary session state. Use healthcheck() to probe the server.</p> Source code in <code>mlclient/_client.py</code> <pre><code>def is_connected(self) -&gt; bool:\n    \"\"\"Return a connection status.\n\n    Returns\n    -------\n    bool\n        True if the primary local HTTP session is open; otherwise False.\n        This does not probe the server or describe auxiliary session state.\n        Use healthcheck() to probe the server.\n    \"\"\"\n    return self._http.is_connected()\n</code></pre>"},{"location":"reference/mlclient/AsyncMLClient/#mlclient.AsyncMLClient.transaction","title":"transaction  <code>async</code>","text":"<pre><code>transaction(\n    *,\n    name: str | None = None,\n    time_limit: int | None = None,\n    database: str | None = None,\n    timeout=UNSET,\n) -&gt; AsyncTransactionService\n</code></pre> <p>Open a multi-statement transaction and return a service scoped to it.</p> <p>Use as an async context manager to commit on a clean exit and roll back on error:</p> <p>async with await ml.transaction(database=\"my-db\") as txn:  # doctest: +SKIP ...     await ml.eval.xquery(\"...\", **txn)</p> <p><code>timeout</code> bounds only the HTTP request that opens the transaction: unset uses the client's configured timeout, None disables every HTTP timeout, a number sets all four components to that many seconds, and an httpx.Timeout overrides them. It is unrelated to <code>time_limit</code>, the server-side lifetime of the transaction. Subsequent operations (status/commit/rollback and content ops run with <code>**txn</code>) accept their own <code>timeout</code>.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str | None</code> <p>Optional server-side label; it is not the transaction identifier.</p> <code>None</code> <code>time_limit</code> <code>int | None</code> <p>Server-side transaction lifetime in seconds. None uses the server default; this is independent of the HTTP timeout.</p> <code>None</code> <code>database</code> <code>str | None</code> <p>Content database name or id. None uses the REST server's database.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>HTTP timeout for opening the transaction only. Unset inherits the connection setting; None disables the HTTP timeout.</p> <code>unset</code> <p>Returns:</p> Type Description <code>AsyncTransactionService</code> <p>The newly opened transaction. Use its context manager to finish it; otherwise commit or roll it back explicitly. <code>**txn</code> forwards the transaction id and its database to content operations.</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> <code>MarkLogicError</code> <p>If MarkLogic rejects opening the transaction.</p> Source code in <code>mlclient/_client.py</code> <pre><code>async def transaction(\n    self,\n    *,\n    name: str | None = None,\n    time_limit: int | None = None,\n    database: str | None = None,\n    timeout=UNSET,\n) -&gt; AsyncTransactionService:\n    \"\"\"Open a multi-statement transaction and return a service scoped to it.\n\n    Use as an async context manager to commit on a clean exit and roll back\n    on error:\n\n    &gt;&gt;&gt; async with await ml.transaction(database=\"my-db\") as txn:  # doctest: +SKIP\n    ...     await ml.eval.xquery(\"...\", **txn)\n\n    ``timeout`` bounds only the HTTP request that opens the transaction:\n    unset uses the client's configured timeout, None disables every HTTP\n    timeout, a number sets all four components to that many seconds, and an\n    httpx.Timeout overrides them. It is unrelated to ``time_limit``, the\n    server-side lifetime of the transaction. Subsequent operations\n    (status/commit/rollback and content ops run with ``**txn``) accept their\n    own ``timeout``.\n\n    Parameters\n    ----------\n    name : str | None, default None\n        Optional server-side label; it is not the transaction identifier.\n    time_limit : int | None, default None\n        Server-side transaction lifetime in seconds. None uses the server\n        default; this is independent of the HTTP timeout.\n    database : str | None, default None\n        Content database name or id. None uses the REST server's database.\n    timeout : httpx.Timeout | float | None, default unset\n        HTTP timeout for opening the transaction only. Unset inherits the\n        connection setting; None disables the HTTP timeout.\n\n    Returns\n    -------\n    AsyncTransactionService\n        The newly opened transaction. Use its context manager to finish it;\n        otherwise commit or roll it back explicitly. ``**txn`` forwards\n        the transaction id and its database to content operations.\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    MarkLogicError\n        If MarkLogic rejects opening the transaction.\n    \"\"\"\n    return await async_open_transaction(\n        AsyncApiClient(self._http),\n        name=name,\n        time_limit=time_limit,\n        database=database,\n        timeout=timeout,\n    )\n</code></pre>"},{"location":"reference/mlclient/AsyncMLClient/#mlclient.AsyncMLClient.version","title":"version  <code>async</code>","text":"<pre><code>version() -&gt; MarkLogicVersion\n</code></pre> <p>Complete MarkLogic version with four unpackable numeric parts.</p> <p>Each <code>await ml.version()</code> performs a new lookup; no version value is cached by the asynchronous client.</p> <p>Resolved from <code>xdmp:version()</code>. When the connecting user lacks the eval privilege the query fails; the Manage and Admin server-config endpoints are then tried in turn. If every source fails the eval error is re-raised. A connection error propagates from the eval attempt -- auxiliary servers are not queried for eval transport failures. Unavailable or malformed fallback responses are skipped.</p> <p>Returns:</p> Type Description <code>MarkLogicVersion</code> <p>The full server version. <code>parts</code> always contains four components, with None for missing components; <code>str()</code> preserves the full version.</p> <p>Raises:</p> Type Description <code>MarkLogicError</code> <p>If MarkLogic returns an error and no fallback source succeeds</p> <code>RequestError</code> <p>If the eval request fails at the HTTP transport level</p> <code>ValueError</code> <p>If the eval result is not a recognizable version string</p> Source code in <code>mlclient/_client.py</code> <pre><code>async def version(self) -&gt; MarkLogicVersion:\n    \"\"\"Complete MarkLogic version with four unpackable numeric parts.\n\n    Each ``await ml.version()`` performs a new lookup; no version value\n    is cached by the asynchronous client.\n\n    Resolved from ``xdmp:version()``. When the connecting user lacks the\n    eval privilege the query fails; the Manage and Admin server-config\n    endpoints are then tried in turn. If every source fails the eval error\n    is re-raised. A connection error propagates from the eval attempt --\n    auxiliary servers are not queried for eval transport failures.\n    Unavailable or malformed fallback responses are skipped.\n\n    Returns\n    -------\n    MarkLogicVersion\n        The full server version. ``parts`` always contains four components,\n        with None for missing components; ``str()`` preserves the full version.\n\n    Raises\n    ------\n    MarkLogicError\n        If MarkLogic returns an error and no fallback source succeeds\n    RequestError\n        If the eval request fails at the HTTP transport level\n    ValueError\n        If the eval result is not a recognizable version string\n    \"\"\"\n    try:\n        return MarkLogicVersion(await self.eval.xquery(\"xdmp:version()\"))\n    except MarkLogicError:\n        version = await self._version_from_secondaries()\n        if version is not None:\n            return version\n        raise\n</code></pre>"},{"location":"reference/mlclient/AsyncMLClient/#mlclient.AsyncMLClient.wait_for_restart","title":"wait_for_restart  <code>async</code>","text":"<pre><code>wait_for_restart(\n    response: Response | None = None,\n    *,\n    timeout: float = 30.0,\n    poll_interval: float = 0.25,\n    retry: Retry | None = None,\n) -&gt; None\n</code></pre> <p>Wait for MarkLogic readiness after a restart-signaling response.</p> <p>Parameters:</p> Name Type Description Default <code>response</code> <code>Response | None</code> <p>Response from an operation that may have initiated a restart.</p> <code>None</code> <code>timeout</code> <code>float</code> <p>Maximum number of seconds to wait for readiness.</p> <code>30.0</code> <code>poll_interval</code> <code>float</code> <p>Delay between readiness probes.</p> <code>0.25</code> <code>retry</code> <code>Retry | None</code> <p>Retry strategy for readiness probes.</p> <code>None</code> Source code in <code>mlclient/_client.py</code> <pre><code>async def wait_for_restart(\n    self,\n    response: Response | None = None,\n    *,\n    timeout: float = 30.0,\n    poll_interval: float = 0.25,\n    retry: Retry | None = None,\n) -&gt; None:\n    \"\"\"Wait for MarkLogic readiness after a restart-signaling response.\n\n    Parameters\n    ----------\n    response : Response | None\n        Response from an operation that may have initiated a restart.\n    timeout : float\n        Maximum number of seconds to wait for readiness.\n    poll_interval : float\n        Delay between readiness probes.\n    retry : Retry | None\n        Retry strategy for readiness probes.\n    \"\"\"\n    waiter = self._get_restart_waiter()\n    await waiter.async_wait_for_restart_completion(\n        response,\n        timeout=timeout,\n        poll_interval=poll_interval,\n        retry=retry or _RESTART_RETRY_STRATEGY,\n    )\n</code></pre>"},{"location":"reference/mlclient/MLClient/","title":"MLClient","text":"<pre><code>from mlclient import MLClient\n</code></pre>"},{"location":"reference/mlclient/MLClient/#mlclient.MLClient","title":"mlclient.MLClient","text":"<pre><code>MLClient(\n    protocol=UNSET,\n    host: str = \"localhost\",\n    port=UNSET,\n    auth: AuthParam = UNSET,\n    username: str = \"admin\",\n    password: str = \"admin\",\n    ssl: SSLConfig | None = None,\n    cloud: CloudConfig | None = None,\n    retry: Retry | int | None = None,\n    limits: Limits | int | None = None,\n    timeout=UNSET,\n    *,\n    config: HTTPConfig | None = None,\n    manage_config: HTTPConfig | None = None,\n    admin_config: HTTPConfig | None = None,\n    health_config: HTTPConfig | None = None,\n)\n</code></pre> <p>Main entry point for MarkLogic interaction.</p> <p>Provides layered access:</p> <ul> <li><code>ml.http.get(\"/endpoint\")</code> -- raw HTTP</li> <li><code>ml.rest.eval.post(xquery=\"...\")</code> -- mid-level REST API (<code>/v1/*</code>)</li> <li><code>ml.manage.databases.get_list()</code> -- mid-level Management API (<code>/manage/v2/*</code>)</li> <li><code>ml.admin.get_timestamp()</code> -- mid-level Admin API (<code>/admin/v1/*</code>)</li> <li><code>ml.healthcheck()</code> -- HEAD <code>/</code> on the HealthCheck server (port 7997)</li> <li><code>ml.rest.call(SomeApiCall())</code> -- advanced: custom Call objects</li> <li><code>ml.parser.parse(resp)</code> -- manual parsing of raw responses</li> <li><code>ml.documents.read(\"/doc.json\")</code> -- higher-level, parsed results</li> <li><code>ml.eval.xquery(\"1+1\")</code> -- higher-level, parsed results</li> <li><code>ml.version</code> -- complete MarkLogic version with four numeric parts</li> <li><code>ml.transaction(database=...)</code> -- open a scoped transaction (context manager)</li> </ul> <p>Examples:</p> <p>Low-level (raw HTTP) - returns raw multipart response:</p> <pre><code>&gt;&gt;&gt; from mlclient import MLClient\n&gt;&gt;&gt; config = {\n...     \"host\": \"localhost\",\n...     \"port\": 8000,\n...     \"username\": \"admin\",\n...     \"password\": \"admin\",\n... }\n&gt;&gt;&gt; with MLClient(**config) as ml:\n...     resp = ml.http.post(\n...         \"/v1/eval\",\n...         \"xquery=xdmp:database()\",\n...         headers={\"Content-Type\": \"application/x-www-form-urlencoded\"},\n...     )\n...     resp.status_code\n200\n</code></pre> <p>Mid-level REST API (<code>/v1/*</code>) - returns httpx.Response:</p> <pre><code>&gt;&gt;&gt; with MLClient(**config) as ml:\n...     resp = ml.rest.eval.post(\n...         xquery=\"xdmp:database() =&gt; xdmp:database-name()\",\n...     )\n...     resp.status_code\n200\n</code></pre> <p>Mid-level Management API (<code>/manage/v2/*</code>) - returns httpx.Response:</p> <pre><code>&gt;&gt;&gt; with MLClient(**config) as ml:\n...     resp = ml.manage.databases.get_list()\n...     resp.status_code\n200\n</code></pre> <p>Mid-level Admin API (<code>/admin/v1/*</code>) - returns httpx.Response:</p> <pre><code>&gt;&gt;&gt; with MLClient(**config) as ml:\n...     resp = ml.admin.get_timestamp()\n...     resp.status_code\n200\n</code></pre> <p>Response parsing:</p> <pre><code>&gt;&gt;&gt; from mlclient import MLClient\n&gt;&gt;&gt; with MLClient(**config) as ml:\n...     resp = ml.rest.eval.post(\n...         xquery=\"xdmp:database() =&gt; xdmp:database-name()\",\n...     )\n...     parsed = ml.parser.parse(resp)\n...     print(parsed)\nApp-Services\n</code></pre> <p>Higher-level (services) - returns parsed Python objects:</p> <pre><code>&gt;&gt;&gt; with MLClient(**config) as ml:\n...     result = ml.eval.xquery(\n...         \"xdmp:database() =&gt; xdmp:database-name()\",\n...     )\n...     print(result)\nApp-Services\n</code></pre> <p>Initialize MLClient instance.</p> <p>The connection parameters describe the primary connection. The Manage (8002), Admin (8001) and HealthCheck (7997) connections are derived from it by default; pass <code>manage_config</code> / <code>admin_config</code> / <code>health_config</code> only to point them at a different host, credentials or port. The derived HealthCheck connection targets port 7997 with no authentication.</p> <p>Parameters:</p> Name Type Description Default <code>protocol</code> <code>str</code> <p>A protocol used for HTTP requests (http / https)</p> <code>\"http\"</code> <code>host</code> <code>str</code> <p>A host name</p> <code>\"localhost\"</code> <code>port</code> <code>int</code> <p>An App Service port</p> <code>8000</code> <code>auth</code> <code>str | Auth | AuthConfig | None</code> <p>An authentication method: a string shortcut (\"basic\", \"digest\", \"digestbasic\", \"certificate\", \"kerberos\"), an AuthConfig, a custom httpx.Auth, or None</p> <code>\"digest\"</code> <code>username</code> <code>str</code> <p>A username</p> <code>\"admin\"</code> <code>password</code> <code>str</code> <p>A password</p> <code>\"admin\"</code> <code>ssl</code> <code>SSLConfig | None</code> <p>SSL/TLS configuration; a client certificate forces HTTPS and defaults the auth method to \"certificate\"</p> <code>None</code> <code>cloud</code> <code>CloudConfig | None</code> <p>MarkLogic Cloud configuration; forces HTTPS on port 443 and handles authentication via the API key</p> <code>None</code> <code>retry</code> <code>Retry | int | None</code> <p>A retry strategy; an int is shorthand for Retry(total=n)</p> <code>Retry(total=5, backoff_factor=0.5)</code> <code>limits</code> <code>Limits | int | None</code> <p>Connection-pool limits; None defers to httpx's own default; an int is shorthand for httpx.Limits(max_connections=n)</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>The request timeout. Unset uses DEFAULT_TIMEOUT (connect=5, read=60, write=60, pool=5 seconds). None disables every HTTP timeout. A number sets all four components to that many seconds. An httpx.Timeout fully overrides them without merging. The derived HealthCheck connection always uses _HEALTH_TIMEOUT, independent of this value</p> <code>unset</code> <code>config</code> <code>HTTPConfig | None</code> <p>An already-resolved primary configuration; when given, the connection parameters above are ignored</p> <code>None</code> <code>manage_config</code> <code>HTTPConfig | None</code> <p>An already-resolved Manage configuration; when given, it is used instead of deriving the Manage connection from the primary</p> <code>None</code> <code>admin_config</code> <code>HTTPConfig | None</code> <p>An already-resolved Admin configuration; when given, it is used instead of deriving the Admin connection from the primary</p> <code>None</code> <code>health_config</code> <code>HTTPConfig | None</code> <p>An already-resolved HealthCheck configuration; when given, it is used instead of deriving the HealthCheck connection (port 7997, no auth, no retries, _HEALTH_TIMEOUT) from the primary. Retry and timeout are resolved independently: an unspecified retry defaults to no retries and an unspecified timeout to _HEALTH_TIMEOUT, while an explicitly supplied retry strategy or timeout (including None to disable it) is preserved</p> <code>None</code> Source code in <code>mlclient/_client.py</code> <pre><code>def __init__(\n    self,\n    protocol=UNSET,\n    host: str = \"localhost\",\n    port=UNSET,\n    auth: AuthParam = UNSET,\n    username: str = \"admin\",\n    password: str = \"admin\",\n    ssl: SSLConfig | None = None,\n    cloud: CloudConfig | None = None,\n    retry: Retry | int | None = None,\n    limits: Limits | int | None = None,\n    timeout=UNSET,\n    *,\n    config: HTTPConfig | None = None,\n    manage_config: HTTPConfig | None = None,\n    admin_config: HTTPConfig | None = None,\n    health_config: HTTPConfig | None = None,\n):\n    \"\"\"Initialize MLClient instance.\n\n    The connection parameters describe the primary connection. The Manage\n    (8002), Admin (8001) and HealthCheck (7997) connections are derived from it\n    by default; pass ``manage_config`` / ``admin_config`` / ``health_config``\n    only to point them at a different host, credentials or port. The derived\n    HealthCheck connection targets port 7997 with no authentication.\n\n    Parameters\n    ----------\n    protocol : str, default \"http\"\n        A protocol used for HTTP requests (http / https)\n    host : str, default \"localhost\"\n        A host name\n    port : int, default 8000\n        An App Service port\n    auth : str | httpx.Auth | AuthConfig | None, default \"digest\"\n        An authentication method: a string shortcut (\"basic\", \"digest\",\n        \"digestbasic\", \"certificate\", \"kerberos\"), an AuthConfig, a custom\n        httpx.Auth, or None\n    username : str, default \"admin\"\n        A username\n    password : str, default \"admin\"\n        A password\n    ssl : SSLConfig | None, default None\n        SSL/TLS configuration; a client certificate forces HTTPS and\n        defaults the auth method to \"certificate\"\n    cloud : CloudConfig | None, default None\n        MarkLogic Cloud configuration; forces HTTPS on port 443 and handles\n        authentication via the API key\n    retry : Retry | int | None, default Retry(total=5, backoff_factor=0.5)\n        A retry strategy; an int is shorthand for Retry(total=n)\n    limits : httpx.Limits | int | None, default None\n        Connection-pool limits; None defers to httpx's own default; an int\n        is shorthand for httpx.Limits(max_connections=n)\n    timeout : httpx.Timeout | float | None, default unset\n        The request timeout. Unset uses DEFAULT_TIMEOUT (connect=5, read=60,\n        write=60, pool=5 seconds). None disables every HTTP timeout. A number\n        sets all four components to that many seconds. An httpx.Timeout fully\n        overrides them without merging. The derived HealthCheck connection\n        always uses _HEALTH_TIMEOUT, independent of this value\n    config : HTTPConfig | None, default None\n        An already-resolved primary configuration; when given, the\n        connection parameters above are ignored\n    manage_config : HTTPConfig | None, default None\n        An already-resolved Manage configuration; when given, it is used\n        instead of deriving the Manage connection from the primary\n    admin_config : HTTPConfig | None, default None\n        An already-resolved Admin configuration; when given, it is used\n        instead of deriving the Admin connection from the primary\n    health_config : HTTPConfig | None, default None\n        An already-resolved HealthCheck configuration; when given, it is used\n        instead of deriving the HealthCheck connection (port 7997, no auth,\n        no retries, _HEALTH_TIMEOUT) from the primary. Retry and timeout are\n        resolved independently: an unspecified retry defaults to no retries\n        and an unspecified timeout to _HEALTH_TIMEOUT, while an explicitly\n        supplied retry strategy or timeout (including None to disable it) is\n        preserved\n    \"\"\"\n    self._http = HttpClient(\n        protocol=protocol,\n        host=host,\n        port=port,\n        auth=auth,\n        username=username,\n        password=password,\n        ssl=ssl,\n        cloud=cloud,\n        retry=retry,\n        limits=limits,\n        timeout=timeout,\n        config=config,\n    )\n    self._manage_http = self._secondary_http(\n        manage_config or self._http.config.clone(port=MARKLOGIC_MANAGE_PORT),\n    )\n    self._admin_http = self._secondary_http(\n        admin_config or self._http.config.clone(port=MARKLOGIC_ADMIN_PORT),\n    )\n    self._health_http = self._secondary_http(\n        health_config.with_health_defaults()\n        if health_config\n        else self._http.config.clone(\n            port=MARKLOGIC_HEALTHCHECK_PORT,\n            auth=None,\n            retry=NO_RETRY_STRATEGY,\n            timeout=_HEALTH_TIMEOUT,\n        ),\n    )\n</code></pre>"},{"location":"reference/mlclient/MLClient/#mlclient.MLClient.admin","title":"admin  <code>cached</code> <code>property</code>","text":"<pre><code>admin: AdminApi\n</code></pre> <p>Administration API on the configured Admin connection.</p> <p>Uses <code>admin_config</code> when supplied, otherwise the derived Admin connection (port 8001 by default). The wrapper is cached per client; its session opens on the first request, not when this property is read.</p> <p>Returns:</p> Type Description <code>AdminApi</code> <p>The connection-bound API or service object.</p>"},{"location":"reference/mlclient/MLClient/#mlclient.MLClient.documents","title":"documents  <code>cached</code> <code>property</code>","text":"<pre><code>documents: DocumentsService\n</code></pre> <p>Read, write and delete documents as Python models.</p> <p>Uses the primary REST connection and returns parsed documents or metadata. The service is created once per client without network I/O. Each operation executes its own requests; document results are not cached.</p> <p>Returns:</p> Type Description <code>DocumentsService</code> <p>The connection-bound API or service object.</p>"},{"location":"reference/mlclient/MLClient/#mlclient.MLClient.eval","title":"eval  <code>cached</code> <code>property</code>","text":"<pre><code>eval: EvalService\n</code></pre> <p>Evaluate XQuery or JavaScript and parse the returned values.</p> <p>Uses the primary REST connection. The service is cached per client, not the query results: every evaluation sends a new request. Accessing this property alone performs no network I/O.</p> <p>Returns:</p> Type Description <code>EvalService</code> <p>The connection-bound API or service object.</p>"},{"location":"reference/mlclient/MLClient/#mlclient.MLClient.http","title":"http  <code>property</code>","text":"<pre><code>http: HttpClient\n</code></pre> <p>Raw HTTP access through the primary connection.</p> <p>Uses the main connection settings and returns raw httpx responses. Accessing this property sends no request; request methods open the session on demand. The client context manager owns its lifetime.</p> <p>Returns:</p> Type Description <code>HttpClient</code> <p>The connection-bound API or service object.</p>"},{"location":"reference/mlclient/MLClient/#mlclient.MLClient.manage","title":"manage  <code>cached</code> <code>property</code>","text":"<pre><code>manage: ManageApi\n</code></pre> <p>Management API on the configured Manage connection.</p> <p>Uses <code>manage_config</code> when supplied, otherwise the derived Manage connection (port 8002 by default). The wrapper is cached per client; its session opens on the first request, not when this property is read.</p> <p>Returns:</p> Type Description <code>ManageApi</code> <p>The connection-bound API or service object.</p>"},{"location":"reference/mlclient/MLClient/#mlclient.MLClient.parser","title":"parser  <code>property</code>","text":"<pre><code>parser: type[MLResponseParser]\n</code></pre> <p>Parse raw MarkLogic HTTP responses explicitly.</p> <p>Returns the parser class, not a parser instance. Call <code>ml.parser.parse(response)</code> for a response from the raw API. Parsing is local and performs no network requests.</p> <p>Returns:</p> Type Description <code>type[MLResponseParser]</code> <p>The parser class.</p>"},{"location":"reference/mlclient/MLClient/#mlclient.MLClient.rest","title":"rest  <code>cached</code> <code>property</code>","text":"<pre><code>rest: RestApi\n</code></pre> <p>Client REST API on the primary connection (<code>/v1/*</code>).</p> <p>Requires a REST-enabled App Server. The wrapper is created once per client on first access; accessing it sends no request. Endpoint methods execute fresh requests and return raw httpx responses.</p> <p>Returns:</p> Type Description <code>RestApi</code> <p>The connection-bound API or service object.</p>"},{"location":"reference/mlclient/MLClient/#mlclient.MLClient.version","title":"version  <code>cached</code> <code>property</code>","text":"<pre><code>version: MarkLogicVersion\n</code></pre> <p>Complete MarkLogic version with four unpackable numeric parts.</p> <p>The first successful access performs network I/O and caches the value on this client instance. Later accesses do not contact the server, even after reconnecting; use a new client for a fresh server-version lookup. Failed lookups are not cached.</p> <p>Resolved from <code>xdmp:version()</code>. When the connecting user lacks the eval privilege the query fails; the Manage and Admin server-config endpoints are then tried in turn. If every source fails the eval error is re-raised. A connection error propagates from the eval attempt -- auxiliary servers are not queried for eval transport failures. Unavailable or malformed fallback responses are skipped.</p> <p>Returns:</p> Type Description <code>MarkLogicVersion</code> <p>The full server version. <code>parts</code> always contains four components, with None for missing components; <code>str()</code> preserves the full version.</p> <p>Raises:</p> Type Description <code>MarkLogicError</code> <p>If MarkLogic returns an error and no fallback source succeeds</p> <code>RequestError</code> <p>If the eval request fails at the HTTP transport level</p> <code>ValueError</code> <p>If the eval result is not a recognizable version string</p>"},{"location":"reference/mlclient/MLClient/#mlclient.MLClient.connect","title":"connect","text":"<pre><code>connect()\n</code></pre> <p>Open the primary local HTTP session.</p> <p>This allocates transport resources; it does not probe connectivity or authenticate with the server. Auxiliary sessions open on their first request. Prefer the client context manager for automatic cleanup.</p> Source code in <code>mlclient/_client.py</code> <pre><code>def connect(self):\n    \"\"\"Open the primary local HTTP session.\n\n    This allocates transport resources; it does not probe connectivity or\n    authenticate with the server. Auxiliary sessions open on their first\n    request. Prefer the client context manager for automatic cleanup.\n    \"\"\"\n    self._http.connect()\n</code></pre>"},{"location":"reference/mlclient/MLClient/#mlclient.MLClient.disconnect","title":"disconnect","text":"<pre><code>disconnect()\n</code></pre> <p>Close the primary and auxiliary sessions owned by this client.</p> <p>Shared HTTP clients are closed once. Cached API/service wrappers are retained; a cached synchronous version value is not refreshed by closing or reopening the transport.</p> Source code in <code>mlclient/_client.py</code> <pre><code>def disconnect(self):\n    \"\"\"Close the primary and auxiliary sessions owned by this client.\n\n    Shared HTTP clients are closed once. Cached API/service wrappers are\n    retained; a cached synchronous version value is not refreshed by closing\n    or reopening the transport.\n    \"\"\"\n    for http in {\n        self._http,\n        self._manage_http,\n        self._admin_http,\n        self._health_http,\n    }:\n        http.disconnect()\n</code></pre>"},{"location":"reference/mlclient/MLClient/#mlclient.MLClient.healthcheck","title":"healthcheck","text":"<pre><code>healthcheck(*, timeout=UNSET) -&gt; bool\n</code></pre> <p>Report whether the HealthCheck app server (port 7997 by default) is healthy.</p> <p>Sends a HEAD request to <code>/</code> on the HealthCheck connection.</p> <p>Parameters:</p> Name Type Description Default <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this probe. Unset uses the HealthCheck connection's timeout (five seconds per component by default). None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them.</p> <code>unset</code> <p>Returns:</p> Type Description <code>bool</code> <p>True when the server answered 2xx, False when it answered 5xx</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> <code>HTTPStatusError</code> <p>If the server answered with a 4xx status, which signals a misdirected request (the HealthCheck server takes no auth) rather than an unhealthy server</p> Source code in <code>mlclient/_client.py</code> <pre><code>def healthcheck(self, *, timeout=UNSET) -&gt; bool:\n    \"\"\"Report whether the HealthCheck app server (port 7997 by default) is healthy.\n\n    Sends a HEAD request to ``/`` on the HealthCheck connection.\n\n    Parameters\n    ----------\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this probe. Unset uses the HealthCheck\n        connection's timeout (five seconds per component by default). None\n        disables every HTTP timeout; a number sets all four components to\n        that many seconds; an httpx.Timeout overrides them.\n\n    Returns\n    -------\n    bool\n        True when the server answered 2xx, False when it answered 5xx\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    httpx.HTTPStatusError\n        If the server answered with a 4xx status, which signals a\n        misdirected request (the HealthCheck server takes no auth) rather\n        than an unhealthy server\n    \"\"\"\n    return _healthy_or_raise(self._health_http.head(\"/\", timeout=timeout))\n</code></pre>"},{"location":"reference/mlclient/MLClient/#mlclient.MLClient.is_connected","title":"is_connected","text":"<pre><code>is_connected() -&gt; bool\n</code></pre> <p>Return a connection status.</p> <p>Returns:</p> Type Description <code>bool</code> <p>True if the primary local HTTP session is open; otherwise False. This does not probe the server or describe auxiliary session state. Use healthcheck() to probe the server.</p> Source code in <code>mlclient/_client.py</code> <pre><code>def is_connected(self) -&gt; bool:\n    \"\"\"Return a connection status.\n\n    Returns\n    -------\n    bool\n        True if the primary local HTTP session is open; otherwise False.\n        This does not probe the server or describe auxiliary session state.\n        Use healthcheck() to probe the server.\n    \"\"\"\n    return self._http.is_connected()\n</code></pre>"},{"location":"reference/mlclient/MLClient/#mlclient.MLClient.transaction","title":"transaction","text":"<pre><code>transaction(\n    *,\n    name: str | None = None,\n    time_limit: int | None = None,\n    database: str | None = None,\n    timeout=UNSET,\n) -&gt; TransactionService\n</code></pre> <p>Open a multi-statement transaction and return a service scoped to it.</p> <p>Use as a context manager to commit on a clean exit and roll back on error:</p> <p>with ml.transaction(database=\"my-db\") as txn:  # doctest: +SKIP ...     ml.eval.xquery(\"...\", **txn)</p> <p><code>timeout</code> bounds only the HTTP request that opens the transaction: unset uses the client's configured timeout, None disables every HTTP timeout, a number sets all four components to that many seconds, and an httpx.Timeout overrides them. It is unrelated to <code>time_limit</code>, the server-side lifetime of the transaction. Subsequent operations (status/commit/rollback and content ops run with <code>**txn</code>) accept their own <code>timeout</code>.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str | None</code> <p>Optional server-side label; it is not the transaction identifier.</p> <code>None</code> <code>time_limit</code> <code>int | None</code> <p>Server-side transaction lifetime in seconds. None uses the server default; this is independent of the HTTP timeout.</p> <code>None</code> <code>database</code> <code>str | None</code> <p>Content database name or id. None uses the REST server's database.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>HTTP timeout for opening the transaction only. Unset inherits the connection setting; None disables the HTTP timeout.</p> <code>unset</code> <p>Returns:</p> Type Description <code>TransactionService</code> <p>The newly opened transaction. Use its context manager to finish it; otherwise commit or roll it back explicitly. <code>**txn</code> forwards the transaction id and its database to content operations.</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> <code>MarkLogicError</code> <p>If MarkLogic rejects opening the transaction.</p> Source code in <code>mlclient/_client.py</code> <pre><code>def transaction(\n    self,\n    *,\n    name: str | None = None,\n    time_limit: int | None = None,\n    database: str | None = None,\n    timeout=UNSET,\n) -&gt; TransactionService:\n    \"\"\"Open a multi-statement transaction and return a service scoped to it.\n\n    Use as a context manager to commit on a clean exit and roll back on error:\n\n    &gt;&gt;&gt; with ml.transaction(database=\"my-db\") as txn:  # doctest: +SKIP\n    ...     ml.eval.xquery(\"...\", **txn)\n\n    ``timeout`` bounds only the HTTP request that opens the transaction:\n    unset uses the client's configured timeout, None disables every HTTP\n    timeout, a number sets all four components to that many seconds, and an\n    httpx.Timeout overrides them. It is unrelated to ``time_limit``, the\n    server-side lifetime of the transaction. Subsequent operations\n    (status/commit/rollback and content ops run with ``**txn``) accept their\n    own ``timeout``.\n\n    Parameters\n    ----------\n    name : str | None, default None\n        Optional server-side label; it is not the transaction identifier.\n    time_limit : int | None, default None\n        Server-side transaction lifetime in seconds. None uses the server\n        default; this is independent of the HTTP timeout.\n    database : str | None, default None\n        Content database name or id. None uses the REST server's database.\n    timeout : httpx.Timeout | float | None, default unset\n        HTTP timeout for opening the transaction only. Unset inherits the\n        connection setting; None disables the HTTP timeout.\n\n    Returns\n    -------\n    TransactionService\n        The newly opened transaction. Use its context manager to finish it;\n        otherwise commit or roll it back explicitly. ``**txn`` forwards\n        the transaction id and its database to content operations.\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    MarkLogicError\n        If MarkLogic rejects opening the transaction.\n    \"\"\"\n    return open_transaction(\n        ApiClient(self._http),\n        name=name,\n        time_limit=time_limit,\n        database=database,\n        timeout=timeout,\n    )\n</code></pre>"},{"location":"reference/mlclient/MLClient/#mlclient.MLClient.wait_for_restart","title":"wait_for_restart","text":"<pre><code>wait_for_restart(\n    response: Response | None = None,\n    *,\n    timeout: float = 30.0,\n    poll_interval: float = 0.25,\n    retry: Retry | None = None,\n) -&gt; None\n</code></pre> <p>Wait for MarkLogic readiness after a restart-signaling response.</p> <p>Parameters:</p> Name Type Description Default <code>response</code> <code>Response | None</code> <p>Response from an operation that may have initiated a restart.</p> <code>None</code> <code>timeout</code> <code>float</code> <p>Maximum number of seconds to wait for readiness.</p> <code>30.0</code> <code>poll_interval</code> <code>float</code> <p>Delay between readiness probes.</p> <code>0.25</code> <code>retry</code> <code>Retry | None</code> <p>Retry strategy for readiness probes.</p> <code>None</code> Source code in <code>mlclient/_client.py</code> <pre><code>def wait_for_restart(\n    self,\n    response: Response | None = None,\n    *,\n    timeout: float = 30.0,\n    poll_interval: float = 0.25,\n    retry: Retry | None = None,\n) -&gt; None:\n    \"\"\"Wait for MarkLogic readiness after a restart-signaling response.\n\n    Parameters\n    ----------\n    response : Response | None\n        Response from an operation that may have initiated a restart.\n    timeout : float\n        Maximum number of seconds to wait for readiness.\n    poll_interval : float\n        Delay between readiness probes.\n    retry : Retry | None\n        Retry strategy for readiness probes.\n    \"\"\"\n    waiter = self._get_restart_waiter()\n    waiter.wait_for_restart_completion(\n        response,\n        timeout=timeout,\n        poll_interval=poll_interval,\n        retry=retry or _RESTART_RETRY_STRATEGY,\n    )\n</code></pre>"},{"location":"reference/mlclient/MLClientManager/","title":"MLClientManager","text":"<pre><code>from mlclient import MLClientManager\n</code></pre>"},{"location":"reference/mlclient/MLClientManager/#mlclient.MLClientManager","title":"mlclient.MLClientManager","text":"<pre><code>MLClientManager(env_name: str, **overrides)\n</code></pre> <p>A high-level class managing MarkLogic clients for a given environment.</p> <p>It combines MLEnvironment and MLClient components to simplify every action to perform on your instance.</p> <p>Load an environment and store HTTP defaults for clients created from it.</p> <p>The environment is loaded immediately. Overrides are applied and validated when a configuration or client is requested; constructing the manager does not open HTTP sessions.</p> <p>Parameters:</p> Name Type Description Default <code>env_name</code> <code>str</code> <p>Environment name selecting .mlclient/mlclient-.yaml. The .mlclient directory is located by searching from the current working directory through its parents. required <code>**overrides</code> <p>HTTPConfig.clone keyword arguments: protocol, host, port, auth, username, password, ssl, cloud, retry, limits and timeout. Applied to every server; per-call overrides take precedence. Retry and timeout are configured only in Python; an explicit strategy or timeout also applies to health. These defaults do not modify the environment file.</p> <code>{}</code> <p>Raises:</p> Type Description <code>MLClientDirectoryNotFoundError</code> <p>If no .mlclient directory is found.</p> <code>MLClientEnvironmentNotFoundError</code> <p>If the named environment file is missing.</p> <code>OSError</code> <p>If the environment file cannot be read.</p> <code>YAMLError</code> <p>If the environment file contains invalid YAML.</p> <code>ValidationError</code> <p>If the environment data fails model validation.</p> Source code in <code>mlclient/_manager.py</code> <pre><code>def __init__(\n    self,\n    env_name: str,\n    **overrides,\n):\n    \"\"\"Load an environment and store HTTP defaults for clients created from it.\n\n    The environment is loaded immediately. Overrides are applied and validated\n    when a configuration or client is requested; constructing the manager does\n    not open HTTP sessions.\n\n    Parameters\n    ----------\n    env_name : str\n        Environment name selecting .mlclient/mlclient-&lt;env_name&gt;.yaml.\n        The .mlclient directory is located by searching from the current\n        working directory through its parents.\n    **overrides\n        HTTPConfig.clone keyword arguments: protocol, host, port, auth,\n        username, password, ssl, cloud, retry, limits and timeout. Applied\n        to every server; per-call overrides take precedence. Retry and\n        timeout are configured only in Python; an explicit strategy or\n        timeout also applies to health. These defaults do not modify the\n        environment file.\n\n    Raises\n    ------\n    MLClientDirectoryNotFoundError\n        If no .mlclient directory is found.\n    MLClientEnvironmentNotFoundError\n        If the named environment file is missing.\n    OSError\n        If the environment file cannot be read.\n    yaml.YAMLError\n        If the environment file contains invalid YAML.\n    pydantic.ValidationError\n        If the environment data fails model validation.\n    \"\"\"\n    self._env_name = env_name\n    self._overrides = overrides\n    self.config = MLEnvironment.load(env_name)\n</code></pre>"},{"location":"reference/mlclient/MLClientManager/#mlclient.MLClientManager.config","title":"config  <code>property</code> <code>writable</code>","text":"<pre><code>config: MLEnvironment\n</code></pre> <p>Return a deep copy of the current environment configuration.</p> <p>Returns:</p> Type Description <code>MLEnvironment</code> <p>An independent copy of the environment model. Mutating it does not change the manager unless it is assigned back through this property. Manager HTTP overrides, including retry, limits and timeout, are not part of this model.</p>"},{"location":"reference/mlclient/MLClientManager/#mlclient.MLClientManager.env_name","title":"env_name  <code>property</code>","text":"<pre><code>env_name: str\n</code></pre> <p>Return the name used to load this manager's environment.</p> <p>Returns:</p> Type Description <code>str</code> <p>The original environment name. Replacing config does not change it.</p>"},{"location":"reference/mlclient/MLClientManager/#mlclient.MLClientManager.get_async_client","title":"get_async_client","text":"<pre><code>get_async_client(\n    app_server_id: str | None = None, **overrides\n) -&gt; AsyncMLClient\n</code></pre> <p>Create an asynchronous MarkLogic client for a configured App Server.</p> <p>The selected server is the primary HTTP endpoint. Selecting health, manage or admin makes its corresponding API share the primary settings and session. Per-call overrides affect only that server; auxiliary APIs for other servers retain their own environment and manager settings.</p> <p>An explicit retry strategy from the manager or this call is preserved. If no strategy is supplied, health uses NO_RETRY_STRATEGY and other servers use DEFAULT_RETRY_STRATEGY. Passing retry=None in this call restores the selected server's default, overriding a manager strategy.</p> <p>Retry and timeout resolve independently. Without an explicit timeout, health uses _HEALTH_TIMEOUT and other servers use DEFAULT_TIMEOUT. A manager timeout applies to every server including health; timeout=None at any level disables every HTTP timeout, while timeout=UNSET in this call inherits the manager value rather than clearing it.</p> <p>Parameters:</p> Name Type Description Default <code>app_server_id</code> <code>str | None</code> <p>A configured App Server identifier, including health, manage or admin. None selects the first REST server in environment order. An explicit identifier does not require the server to be marked as REST.</p> <code>None</code> <code>**overrides</code> <p>HTTPConfig.clone keyword arguments: protocol, host, port, auth, username, password, ssl, cloud, retry, limits and timeout. Values override manager defaults, which override environment settings. Cloud retains its gateway port even when a port override is supplied. Retry, limits and timeout are Python-only HTTP options, not environment YAML settings.</p> <code>{}</code> <p>Returns:</p> Type Description <code>AsyncMLClient</code> <p>A new, disconnected client. Use async with to manage its lifecycle: entering opens the primary session, auxiliary sessions open on first request, and exiting closes all opened sessions. Creating the client leaves the environment and later calls unchanged.</p> <p>Raises:</p> Type Description <code>NoSuchAppServerError</code> <p>If the App Server identifier is not configured.</p> <code>ConfigError</code> <p>If connection and authentication settings are incompatible.</p> <code>TypeError</code> <p>If an override name or authentication descriptor type is unsupported.</p> <code>ValueError</code> <p>If configuration values or authentication parameters are invalid.</p> <code>ImportError</code> <p>If the selected authentication method requires an unavailable optional dependency.</p> <code>NoRestServerConfiguredError</code> <p>If no identifier is supplied and no REST server is configured.</p> Source code in <code>mlclient/_manager.py</code> <pre><code>def get_async_client(\n    self,\n    app_server_id: str | None = None,\n    **overrides,\n) -&gt; AsyncMLClient:\n    \"\"\"Create an asynchronous MarkLogic client for a configured App Server.\n\n    The selected server is the primary HTTP endpoint. Selecting health,\n    manage or admin makes its corresponding API share the primary settings\n    and session. Per-call overrides affect only that server; auxiliary APIs\n    for other servers retain their own environment and manager settings.\n\n    An explicit retry strategy from the manager or this call is preserved.\n    If no strategy is supplied, health uses NO_RETRY_STRATEGY and other\n    servers use DEFAULT_RETRY_STRATEGY. Passing retry=None in this call\n    restores the selected server's default, overriding a manager strategy.\n\n    Retry and timeout resolve independently. Without an explicit timeout,\n    health uses _HEALTH_TIMEOUT and other servers use DEFAULT_TIMEOUT. A\n    manager timeout applies to every server including health; timeout=None\n    at any level disables every HTTP timeout, while timeout=UNSET in this\n    call inherits the manager value rather than clearing it.\n\n    Parameters\n    ----------\n    app_server_id : str | None, default None\n        A configured App Server identifier, including health, manage or admin.\n        None selects the first REST server in environment order. An explicit\n        identifier does not require the server to be marked as REST.\n    **overrides\n        HTTPConfig.clone keyword arguments: protocol, host, port, auth,\n        username, password, ssl, cloud, retry, limits and timeout. Values\n        override manager defaults, which override environment settings.\n        Cloud retains its gateway port even when a port override is\n        supplied. Retry, limits and timeout are Python-only HTTP options,\n        not environment YAML settings.\n\n    Returns\n    -------\n    AsyncMLClient\n        A new, disconnected client. Use async with to manage its lifecycle:\n        entering opens the primary session, auxiliary sessions open on first\n        request, and exiting closes all opened sessions. Creating the client\n        leaves the environment and later calls unchanged.\n\n    Raises\n    ------\n    NoSuchAppServerError\n        If the App Server identifier is not configured.\n    ConfigError\n        If connection and authentication settings are incompatible.\n    TypeError\n        If an override name or authentication descriptor type is unsupported.\n    ValueError\n        If configuration values or authentication parameters are invalid.\n    ImportError\n        If the selected authentication method requires an unavailable\n        optional dependency.\n    NoRestServerConfiguredError\n        If no identifier is supplied and no REST server is configured.\n    \"\"\"\n    return AsyncMLClient(**self._client_configs(app_server_id, **overrides))\n</code></pre>"},{"location":"reference/mlclient/MLClientManager/#mlclient.MLClientManager.get_async_http_client","title":"get_async_http_client","text":"<pre><code>get_async_http_client(\n    app_server_id: str, **overrides\n) -&gt; AsyncHttpClient\n</code></pre> <p>Create a raw asynchronous HTTP client for one configured server.</p> <p>All requests target the selected server; no auxiliary API clients are created. Overrides affect this returned client only.</p> <p>An explicit retry strategy from the manager or this call is preserved. If no strategy is supplied, health uses NO_RETRY_STRATEGY and other servers use DEFAULT_RETRY_STRATEGY. Passing retry=None in this call restores the selected server's default, overriding a manager strategy.</p> <p>Retry and timeout resolve independently. Without an explicit timeout, health uses _HEALTH_TIMEOUT and other servers use DEFAULT_TIMEOUT. A manager timeout applies to every server including health; timeout=None at any level disables every HTTP timeout, while timeout=UNSET in this call inherits the manager value rather than clearing it.</p> <p>Parameters:</p> Name Type Description Default <code>app_server_id</code> <code>str</code> <p>A configured App Server identifier, including health, manage or admin. The identifier is required; the server need not be marked as REST.</p> required <code>**overrides</code> <p>HTTPConfig.clone keyword arguments: protocol, host, port, auth, username, password, ssl, cloud, retry, limits and timeout. Values override manager defaults, which override environment settings. Cloud retains its gateway port even when a port override is supplied. Retry, limits and timeout are Python-only HTTP options, not environment YAML settings.</p> <code>{}</code> <p>Returns:</p> Type Description <code>AsyncHttpClient</code> <p>A new, disconnected HTTP client. Use async with to open a reusable session and close it on exit. Requests outside a connected lifecycle use short-lived sessions. The environment and subsequent calls are unchanged.</p> <p>Raises:</p> Type Description <code>NoSuchAppServerError</code> <p>If the App Server identifier is not configured.</p> <code>ConfigError</code> <p>If connection and authentication settings are incompatible.</p> <code>TypeError</code> <p>If an override name or authentication descriptor type is unsupported.</p> <code>ValueError</code> <p>If configuration values or authentication parameters are invalid.</p> <code>ImportError</code> <p>If the selected authentication method requires an unavailable optional dependency.</p> Source code in <code>mlclient/_manager.py</code> <pre><code>def get_async_http_client(\n    self,\n    app_server_id: str,\n    **overrides,\n) -&gt; AsyncHttpClient:\n    \"\"\"Create a raw asynchronous HTTP client for one configured server.\n\n    All requests target the selected server; no auxiliary API clients are\n    created. Overrides affect this returned client only.\n\n    An explicit retry strategy from the manager or this call is preserved.\n    If no strategy is supplied, health uses NO_RETRY_STRATEGY and other\n    servers use DEFAULT_RETRY_STRATEGY. Passing retry=None in this call\n    restores the selected server's default, overriding a manager strategy.\n\n    Retry and timeout resolve independently. Without an explicit timeout,\n    health uses _HEALTH_TIMEOUT and other servers use DEFAULT_TIMEOUT. A\n    manager timeout applies to every server including health; timeout=None\n    at any level disables every HTTP timeout, while timeout=UNSET in this\n    call inherits the manager value rather than clearing it.\n\n    Parameters\n    ----------\n    app_server_id : str\n        A configured App Server identifier, including health, manage or admin.\n        The identifier is required; the server need not be marked as REST.\n    **overrides\n        HTTPConfig.clone keyword arguments: protocol, host, port, auth,\n        username, password, ssl, cloud, retry, limits and timeout. Values\n        override manager defaults, which override environment settings.\n        Cloud retains its gateway port even when a port override is\n        supplied. Retry, limits and timeout are Python-only HTTP options,\n        not environment YAML settings.\n\n    Returns\n    -------\n    AsyncHttpClient\n        A new, disconnected HTTP client. Use async with to open a reusable\n        session and close it on exit. Requests outside a connected lifecycle\n        use short-lived sessions. The environment and subsequent calls are\n        unchanged.\n\n    Raises\n    ------\n    NoSuchAppServerError\n        If the App Server identifier is not configured.\n    ConfigError\n        If connection and authentication settings are incompatible.\n    TypeError\n        If an override name or authentication descriptor type is unsupported.\n    ValueError\n        If configuration values or authentication parameters are invalid.\n    ImportError\n        If the selected authentication method requires an unavailable\n        optional dependency.\n    \"\"\"\n    return AsyncHttpClient(config=self.get_config(app_server_id, **overrides))\n</code></pre>"},{"location":"reference/mlclient/MLClientManager/#mlclient.MLClientManager.get_client","title":"get_client","text":"<pre><code>get_client(\n    app_server_id: str | None = None, **overrides\n) -&gt; MLClient\n</code></pre> <p>Create a synchronous MarkLogic client for a configured App Server.</p> <p>The selected server is the primary HTTP endpoint. Selecting health, manage or admin makes its corresponding API share the primary settings and session. Per-call overrides affect only that server; auxiliary APIs for other servers retain their own environment and manager settings.</p> <p>An explicit retry strategy from the manager or this call is preserved. If no strategy is supplied, health uses NO_RETRY_STRATEGY and other servers use DEFAULT_RETRY_STRATEGY. Passing retry=None in this call restores the selected server's default, overriding a manager strategy.</p> <p>Retry and timeout resolve independently. Without an explicit timeout, health uses _HEALTH_TIMEOUT and other servers use DEFAULT_TIMEOUT. A manager timeout applies to every server including health; timeout=None at any level disables every HTTP timeout, while timeout=UNSET in this call inherits the manager value rather than clearing it.</p> <p>Parameters:</p> Name Type Description Default <code>app_server_id</code> <code>str | None</code> <p>A configured App Server identifier, including health, manage or admin. None selects the first REST server in environment order. An explicit identifier does not require the server to be marked as REST.</p> <code>None</code> <code>**overrides</code> <p>HTTPConfig.clone keyword arguments: protocol, host, port, auth, username, password, ssl, cloud, retry, limits and timeout. Values override manager defaults, which override environment settings. Cloud retains its gateway port even when a port override is supplied. Retry, limits and timeout are Python-only HTTP options, not environment YAML settings.</p> <code>{}</code> <p>Returns:</p> Type Description <code>MLClient</code> <p>A new, disconnected client. Use with to manage its lifecycle: entering opens the primary session, auxiliary sessions open on first request, and exiting closes all opened sessions. Creating the client leaves the environment and later calls unchanged.</p> <p>Raises:</p> Type Description <code>NoSuchAppServerError</code> <p>If the App Server identifier is not configured.</p> <code>ConfigError</code> <p>If connection and authentication settings are incompatible.</p> <code>TypeError</code> <p>If an override name or authentication descriptor type is unsupported.</p> <code>ValueError</code> <p>If configuration values or authentication parameters are invalid.</p> <code>ImportError</code> <p>If the selected authentication method requires an unavailable optional dependency.</p> <code>NoRestServerConfiguredError</code> <p>If no identifier is supplied and no REST server is configured.</p> Source code in <code>mlclient/_manager.py</code> <pre><code>def get_client(self, app_server_id: str | None = None, **overrides) -&gt; MLClient:\n    \"\"\"Create a synchronous MarkLogic client for a configured App Server.\n\n    The selected server is the primary HTTP endpoint. Selecting health,\n    manage or admin makes its corresponding API share the primary settings\n    and session. Per-call overrides affect only that server; auxiliary APIs\n    for other servers retain their own environment and manager settings.\n\n    An explicit retry strategy from the manager or this call is preserved.\n    If no strategy is supplied, health uses NO_RETRY_STRATEGY and other\n    servers use DEFAULT_RETRY_STRATEGY. Passing retry=None in this call\n    restores the selected server's default, overriding a manager strategy.\n\n    Retry and timeout resolve independently. Without an explicit timeout,\n    health uses _HEALTH_TIMEOUT and other servers use DEFAULT_TIMEOUT. A\n    manager timeout applies to every server including health; timeout=None\n    at any level disables every HTTP timeout, while timeout=UNSET in this\n    call inherits the manager value rather than clearing it.\n\n    Parameters\n    ----------\n    app_server_id : str | None, default None\n        A configured App Server identifier, including health, manage or admin.\n        None selects the first REST server in environment order. An explicit\n        identifier does not require the server to be marked as REST.\n    **overrides\n        HTTPConfig.clone keyword arguments: protocol, host, port, auth,\n        username, password, ssl, cloud, retry, limits and timeout. Values\n        override manager defaults, which override environment settings.\n        Cloud retains its gateway port even when a port override is\n        supplied. Retry, limits and timeout are Python-only HTTP options,\n        not environment YAML settings.\n\n    Returns\n    -------\n    MLClient\n        A new, disconnected client. Use with to manage its lifecycle:\n        entering opens the primary session, auxiliary sessions open on first\n        request, and exiting closes all opened sessions. Creating the client\n        leaves the environment and later calls unchanged.\n\n    Raises\n    ------\n    NoSuchAppServerError\n        If the App Server identifier is not configured.\n    ConfigError\n        If connection and authentication settings are incompatible.\n    TypeError\n        If an override name or authentication descriptor type is unsupported.\n    ValueError\n        If configuration values or authentication parameters are invalid.\n    ImportError\n        If the selected authentication method requires an unavailable\n        optional dependency.\n    NoRestServerConfiguredError\n        If no identifier is supplied and no REST server is configured.\n    \"\"\"\n    return MLClient(**self._client_configs(app_server_id, **overrides))\n</code></pre>"},{"location":"reference/mlclient/MLClientManager/#mlclient.MLClientManager.get_config","title":"get_config","text":"<pre><code>get_config(app_server_id: str, **overrides) -&gt; HTTPConfig\n</code></pre> <p>Resolve HTTP settings for one server without creating a client.</p> <p>An explicit retry strategy from the manager or this call is preserved. If no strategy is supplied, health uses NO_RETRY_STRATEGY and other servers use DEFAULT_RETRY_STRATEGY. Passing retry=None in this call restores the selected server's default, overriding a manager strategy.</p> <p>Retry and timeout resolve independently. Without an explicit timeout, health uses _HEALTH_TIMEOUT and other servers use DEFAULT_TIMEOUT. A manager timeout applies to every server including health; timeout=None at any level disables every HTTP timeout, while timeout=UNSET in this call inherits the manager value rather than clearing it.</p> <p>Parameters:</p> Name Type Description Default <code>app_server_id</code> <code>str</code> <p>A configured App Server identifier, including health, manage or admin. The identifier is required; the server need not be marked as REST.</p> required <code>**overrides</code> <p>HTTPConfig.clone keyword arguments: protocol, host, port, auth, username, password, ssl, cloud, retry, limits and timeout. Values override manager defaults, which override environment settings. Cloud retains its gateway port even when a port override is supplied. Retry, limits and timeout are Python-only HTTP options, not environment YAML settings.</p> <code>{}</code> <p>Returns:</p> Type Description <code>HTTPConfig</code> <p>The resolved server configuration, including the effective retry strategy. No HTTP session is opened; the environment and defaults used by later calls are unchanged.</p> <p>Raises:</p> Type Description <code>NoSuchAppServerError</code> <p>If the App Server identifier is not configured.</p> <code>ConfigError</code> <p>If connection and authentication settings are incompatible.</p> <code>TypeError</code> <p>If an override name or authentication descriptor type is unsupported.</p> <code>ValueError</code> <p>If configuration values or authentication parameters are invalid.</p> <code>ImportError</code> <p>If the selected authentication method requires an unavailable optional dependency.</p> Source code in <code>mlclient/_manager.py</code> <pre><code>def get_config(self, app_server_id: str, **overrides) -&gt; HTTPConfig:\n    \"\"\"Resolve HTTP settings for one server without creating a client.\n\n    An explicit retry strategy from the manager or this call is preserved.\n    If no strategy is supplied, health uses NO_RETRY_STRATEGY and other\n    servers use DEFAULT_RETRY_STRATEGY. Passing retry=None in this call\n    restores the selected server's default, overriding a manager strategy.\n\n    Retry and timeout resolve independently. Without an explicit timeout,\n    health uses _HEALTH_TIMEOUT and other servers use DEFAULT_TIMEOUT. A\n    manager timeout applies to every server including health; timeout=None\n    at any level disables every HTTP timeout, while timeout=UNSET in this\n    call inherits the manager value rather than clearing it.\n\n    Parameters\n    ----------\n    app_server_id : str\n        A configured App Server identifier, including health, manage or admin.\n        The identifier is required; the server need not be marked as REST.\n    **overrides\n        HTTPConfig.clone keyword arguments: protocol, host, port, auth,\n        username, password, ssl, cloud, retry, limits and timeout. Values\n        override manager defaults, which override environment settings.\n        Cloud retains its gateway port even when a port override is\n        supplied. Retry, limits and timeout are Python-only HTTP options,\n        not environment YAML settings.\n\n    Returns\n    -------\n    HTTPConfig\n        The resolved server configuration, including the effective retry\n        strategy. No HTTP session is opened; the environment and defaults\n        used by later calls are unchanged.\n\n    Raises\n    ------\n    NoSuchAppServerError\n        If the App Server identifier is not configured.\n    ConfigError\n        If connection and authentication settings are incompatible.\n    TypeError\n        If an override name or authentication descriptor type is unsupported.\n    ValueError\n        If configuration values or authentication parameters are invalid.\n    ImportError\n        If the selected authentication method requires an unavailable\n        optional dependency.\n    \"\"\"\n    config = self._config.provide_config(app_server_id)\n    overrides = {**self._overrides, **_without_unset(overrides)}\n    if overrides:\n        config = config.clone(**overrides)\n    if app_server_id == \"health\":\n        config = config.with_health_defaults()\n    return config\n</code></pre>"},{"location":"reference/mlclient/MLClientManager/#mlclient.MLClientManager.get_http_client","title":"get_http_client","text":"<pre><code>get_http_client(\n    app_server_id: str, **overrides\n) -&gt; HttpClient\n</code></pre> <p>Create a raw synchronous HTTP client for one configured server.</p> <p>All requests target the selected server; no auxiliary API clients are created. Overrides affect this returned client only.</p> <p>An explicit retry strategy from the manager or this call is preserved. If no strategy is supplied, health uses NO_RETRY_STRATEGY and other servers use DEFAULT_RETRY_STRATEGY. Passing retry=None in this call restores the selected server's default, overriding a manager strategy.</p> <p>Retry and timeout resolve independently. Without an explicit timeout, health uses _HEALTH_TIMEOUT and other servers use DEFAULT_TIMEOUT. A manager timeout applies to every server including health; timeout=None at any level disables every HTTP timeout, while timeout=UNSET in this call inherits the manager value rather than clearing it.</p> <p>Parameters:</p> Name Type Description Default <code>app_server_id</code> <code>str</code> <p>A configured App Server identifier, including health, manage or admin. The identifier is required; the server need not be marked as REST.</p> required <code>**overrides</code> <p>HTTPConfig.clone keyword arguments: protocol, host, port, auth, username, password, ssl, cloud, retry, limits and timeout. Values override manager defaults, which override environment settings. Cloud retains its gateway port even when a port override is supplied. Retry, limits and timeout are Python-only HTTP options, not environment YAML settings.</p> <code>{}</code> <p>Returns:</p> Type Description <code>HttpClient</code> <p>A new, disconnected HTTP client. Use with to open a reusable session and close it on exit. Requests outside a connected lifecycle use short-lived sessions. The environment and subsequent calls are unchanged.</p> <p>Raises:</p> Type Description <code>NoSuchAppServerError</code> <p>If the App Server identifier is not configured.</p> <code>ConfigError</code> <p>If connection and authentication settings are incompatible.</p> <code>TypeError</code> <p>If an override name or authentication descriptor type is unsupported.</p> <code>ValueError</code> <p>If configuration values or authentication parameters are invalid.</p> <code>ImportError</code> <p>If the selected authentication method requires an unavailable optional dependency.</p> Source code in <code>mlclient/_manager.py</code> <pre><code>def get_http_client(self, app_server_id: str, **overrides) -&gt; HttpClient:\n    \"\"\"Create a raw synchronous HTTP client for one configured server.\n\n    All requests target the selected server; no auxiliary API clients are\n    created. Overrides affect this returned client only.\n\n    An explicit retry strategy from the manager or this call is preserved.\n    If no strategy is supplied, health uses NO_RETRY_STRATEGY and other\n    servers use DEFAULT_RETRY_STRATEGY. Passing retry=None in this call\n    restores the selected server's default, overriding a manager strategy.\n\n    Retry and timeout resolve independently. Without an explicit timeout,\n    health uses _HEALTH_TIMEOUT and other servers use DEFAULT_TIMEOUT. A\n    manager timeout applies to every server including health; timeout=None\n    at any level disables every HTTP timeout, while timeout=UNSET in this\n    call inherits the manager value rather than clearing it.\n\n    Parameters\n    ----------\n    app_server_id : str\n        A configured App Server identifier, including health, manage or admin.\n        The identifier is required; the server need not be marked as REST.\n    **overrides\n        HTTPConfig.clone keyword arguments: protocol, host, port, auth,\n        username, password, ssl, cloud, retry, limits and timeout. Values\n        override manager defaults, which override environment settings.\n        Cloud retains its gateway port even when a port override is\n        supplied. Retry, limits and timeout are Python-only HTTP options,\n        not environment YAML settings.\n\n    Returns\n    -------\n    HttpClient\n        A new, disconnected HTTP client. Use with to open a reusable\n        session and close it on exit. Requests outside a connected lifecycle\n        use short-lived sessions. The environment and subsequent calls are\n        unchanged.\n\n    Raises\n    ------\n    NoSuchAppServerError\n        If the App Server identifier is not configured.\n    ConfigError\n        If connection and authentication settings are incompatible.\n    TypeError\n        If an override name or authentication descriptor type is unsupported.\n    ValueError\n        If configuration values or authentication parameters are invalid.\n    ImportError\n        If the selected authentication method requires an unavailable\n        optional dependency.\n    \"\"\"\n    return HttpClient(config=self.get_config(app_server_id, **overrides))\n</code></pre>"},{"location":"reference/mlclient/api/","title":"<code>mlclient.api</code>","text":""},{"location":"reference/mlclient/api/#mlclient.api","title":"mlclient.api","text":"<p>Synchronous and asynchronous wrappers for MarkLogic REST endpoints.</p>"},{"location":"reference/mlclient/api/AdminApi/","title":"AdminApi","text":"<pre><code>from mlclient.api import AdminApi\n</code></pre>"},{"location":"reference/mlclient/api/AdminApi/#mlclient.api.AdminApi","title":"mlclient.api.AdminApi","text":"<pre><code>AdminApi(api: ApiClient)\n</code></pre> <p>Admin API group for /admin/v1/* endpoints.</p> <p>Requires the Admin server (port 8001 by default).</p> Source code in <code>mlclient/api/admin.py</code> <pre><code>def __init__(self, api: ApiClient):\n    self._api = api\n</code></pre>"},{"location":"reference/mlclient/api/AdminApi/#mlclient.api.AdminApi.call","title":"call","text":"<pre><code>call(call_: ApiCall, *, timeout=UNSET) -&gt; Response\n</code></pre> <p>Send a custom ApiCall.</p> <p>Parameters:</p> Name Type Description Default <code>call_</code> <code>ApiCall</code> <p>A specific endpoint call implementation</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/admin.py</code> <pre><code>def call(self, call_: ApiCall, *, timeout=UNSET) -&gt; Response:\n    \"\"\"Send a custom ApiCall.\n\n    Parameters\n    ----------\n    call_ : ApiCall\n        A specific endpoint call implementation\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return self._api.call(call_, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AdminApi/#mlclient.api.AdminApi.get_server_config","title":"get_server_config","text":"<pre><code>get_server_config(*, timeout=UNSET) -&gt; Response\n</code></pre> <p>Retrieve server configuration information for cluster join.</p> <p>Returns the host configuration as XML, suitable for use as input to <code>POST /admin/v1/cluster-config</code> when adding this host to a cluster.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/admin/v1/server-config</p> <p>Parameters:</p> Name Type Description Default <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with <code>application/xml</code> body containing the server configuration</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/admin.py</code> <pre><code>def get_server_config(self, *, timeout=UNSET) -&gt; Response:\n    \"\"\"Retrieve server configuration information for cluster join.\n\n    Returns the host configuration as XML, suitable for use as input to\n    ``POST /admin/v1/cluster-config`` when adding this host to a cluster.\n\n    Documentation: https://docs.marklogic.com/REST/GET/admin/v1/server-config\n\n    Parameters\n    ----------\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with ``application/xml`` body containing\n        the server configuration\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return self._api.call(ServerConfigGetCall(), timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AdminApi/#mlclient.api.AdminApi.get_timestamp","title":"get_timestamp","text":"<pre><code>get_timestamp(*, timeout=UNSET) -&gt; Response\n</code></pre> <p>Verify that MarkLogic Server is up and accepting requests.</p> <p>Returns a plain text timestamp of the last restart. Can be used to detect when a restart triggered by an administrative operation has completed.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/admin/v1/timestamp</p> <p>Parameters:</p> Name Type Description Default <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with <code>text/plain</code> body containing the timestamp</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/admin.py</code> <pre><code>def get_timestamp(self, *, timeout=UNSET) -&gt; Response:\n    \"\"\"Verify that MarkLogic Server is up and accepting requests.\n\n    Returns a plain text timestamp of the last restart. Can be used to\n    detect when a restart triggered by an administrative operation has\n    completed.\n\n    Documentation: https://docs.marklogic.com/REST/GET/admin/v1/timestamp\n\n    Parameters\n    ----------\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with ``text/plain`` body containing the timestamp\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return self._api.call(TimestampGetCall(), timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncAdminApi/","title":"AsyncAdminApi","text":"<pre><code>from mlclient.api import AsyncAdminApi\n</code></pre>"},{"location":"reference/mlclient/api/AsyncAdminApi/#mlclient.api.AsyncAdminApi","title":"mlclient.api.AsyncAdminApi","text":"<pre><code>AsyncAdminApi(api: AsyncApiClient)\n</code></pre> <p>Async Admin API group for /admin/v1/* endpoints.</p> Source code in <code>mlclient/api/admin.py</code> <pre><code>def __init__(self, api: AsyncApiClient):\n    self._api = api\n</code></pre>"},{"location":"reference/mlclient/api/AsyncAdminApi/#mlclient.api.AsyncAdminApi.call","title":"call  <code>async</code>","text":"<pre><code>call(call_: ApiCall, *, timeout=UNSET) -&gt; Response\n</code></pre> <p>Send a custom ApiCall.</p> <p>Parameters:</p> Name Type Description Default <code>call_</code> <code>ApiCall</code> <p>A specific endpoint call implementation</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/admin.py</code> <pre><code>async def call(self, call_: ApiCall, *, timeout=UNSET) -&gt; Response:\n    \"\"\"Send a custom ApiCall.\n\n    Parameters\n    ----------\n    call_ : ApiCall\n        A specific endpoint call implementation\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return await self._api.call(call_, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncAdminApi/#mlclient.api.AsyncAdminApi.get_server_config","title":"get_server_config  <code>async</code>","text":"<pre><code>get_server_config(*, timeout=UNSET) -&gt; Response\n</code></pre> <p>Retrieve server configuration information for cluster join.</p> <p>Returns the host configuration as XML, suitable for use as input to <code>POST /admin/v1/cluster-config</code> when adding this host to a cluster.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/admin/v1/server-config</p> <p>Parameters:</p> Name Type Description Default <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with <code>application/xml</code> body containing the server configuration</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/admin.py</code> <pre><code>async def get_server_config(self, *, timeout=UNSET) -&gt; Response:\n    \"\"\"Retrieve server configuration information for cluster join.\n\n    Returns the host configuration as XML, suitable for use as input to\n    ``POST /admin/v1/cluster-config`` when adding this host to a cluster.\n\n    Documentation: https://docs.marklogic.com/REST/GET/admin/v1/server-config\n\n    Parameters\n    ----------\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with ``application/xml`` body containing\n        the server configuration\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return await self._api.call(ServerConfigGetCall(), timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncAdminApi/#mlclient.api.AsyncAdminApi.get_timestamp","title":"get_timestamp  <code>async</code>","text":"<pre><code>get_timestamp(*, timeout=UNSET) -&gt; Response\n</code></pre> <p>Verify that MarkLogic Server is up and accepting requests.</p> <p>Returns a plain text timestamp of the last restart. Can be used to detect when a restart triggered by an administrative operation has completed.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/admin/v1/timestamp</p> <p>Parameters:</p> Name Type Description Default <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with <code>text/plain</code> body containing the timestamp</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/admin.py</code> <pre><code>async def get_timestamp(self, *, timeout=UNSET) -&gt; Response:\n    \"\"\"Verify that MarkLogic Server is up and accepting requests.\n\n    Returns a plain text timestamp of the last restart. Can be used to\n    detect when a restart triggered by an administrative operation has\n    completed.\n\n    Documentation: https://docs.marklogic.com/REST/GET/admin/v1/timestamp\n\n    Parameters\n    ----------\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with ``text/plain`` body containing the timestamp\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return await self._api.call(TimestampGetCall(), timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncDatabasesApi/","title":"AsyncDatabasesApi","text":"<pre><code>from mlclient.api import AsyncDatabasesApi\n</code></pre>"},{"location":"reference/mlclient/api/AsyncDatabasesApi/#mlclient.api.AsyncDatabasesApi","title":"mlclient.api.AsyncDatabasesApi","text":"<pre><code>AsyncDatabasesApi(api: AsyncApiClient)\n</code></pre> <p>Async mid-level API for <code>/manage/v2/databases</code> endpoints.</p> Source code in <code>mlclient/api/databases.py</code> <pre><code>def __init__(self, api: AsyncApiClient):\n    self._api = api\n</code></pre>"},{"location":"reference/mlclient/api/AsyncDatabasesApi/#mlclient.api.AsyncDatabasesApi.create","title":"create  <code>async</code>","text":"<pre><code>create(body: str | dict, *, timeout=UNSET) -&gt; Response\n</code></pre> <p>Create a new database in the cluster.</p> <p>Documentation: https://docs.marklogic.com/REST/POST/manage/v2/databases</p> <p>Parameters:</p> Name Type Description Default <code>body</code> <code>str | dict</code> <p>A database properties in XML or JSON format.</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/databases.py</code> <pre><code>async def create(\n    self,\n    body: str | dict,\n    *,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Create a new database in the cluster.\n\n    Documentation: https://docs.marklogic.com/REST/POST/manage/v2/databases\n\n    Parameters\n    ----------\n    body : str | dict\n        A database properties in XML or JSON format.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = DatabasesPostCall(body=body)\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncDatabasesApi/#mlclient.api.AsyncDatabasesApi.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(\n    database: str,\n    *,\n    forest_delete: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Delete the specified database from the cluster.</p> <p>Documentation: https://docs.marklogic.com/REST/DELETE/manage/v2/databases/[id-or-name]</p> <p>Parameters:</p> Name Type Description Default <code>database</code> <code>str</code> <p>A database identifier. The database can be identified either by ID or name.</p> required <code>forest_delete</code> <code>str</code> <p>Specifies to delete the forests attached to the database. If unspecified, the forests will not be affected. If \"configuration\" is specified, the forest configuration will be removed but public forest data will remain. If \"data\" is specified, the forest configuration and data will be removed.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/databases.py</code> <pre><code>async def delete(\n    self,\n    database: str,\n    *,\n    forest_delete: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Delete the specified database from the cluster.\n\n    Documentation: https://docs.marklogic.com/REST/DELETE/manage/v2/databases/[id-or-name]\n\n    Parameters\n    ----------\n    database : str\n        A database identifier. The database can be identified either by ID or name.\n    forest_delete : str\n        Specifies to delete the forests attached to the database.\n        If unspecified, the forests will not be affected.\n        If \"configuration\" is specified, the forest configuration will be removed\n        but public forest data will remain.\n        If \"data\" is specified, the forest configuration and data will be removed.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = DatabaseDeleteCall(database=database, forest_delete=forest_delete)\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncDatabasesApi/#mlclient.api.AsyncDatabasesApi.get","title":"get  <code>async</code>","text":"<pre><code>get(\n    database: str,\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve information on the specified database.</p> <p>The database can be identified either by ID or name.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/databases/[id-or-name]</p> <p>Parameters:</p> Name Type Description Default <code>database</code> <code>str</code> <p>A database identifier. The database can be identified either by ID or name.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default). This parameter is not meaningful with view=edit.</p> <code>None</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be: properties-schema, package, describe, config, counts, edit, status, forest-storage, or default.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the database details</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/databases.py</code> <pre><code>async def get(\n    self,\n    database: str,\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve information on the specified database.\n\n    The database can be identified either by ID or name.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/databases/[id-or-name]\n\n    Parameters\n    ----------\n    database : str\n        A database identifier. The database can be identified either by ID or name.\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n        This parameter is not meaningful with view=edit.\n    view : str\n        A specific view of the returned data.\n        Can be: properties-schema, package, describe, config, counts, edit, status,\n        forest-storage, or default.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the database details\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = DatabaseGetCall(database=database, data_format=data_format, view=view)\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncDatabasesApi/#mlclient.api.AsyncDatabasesApi.get_list","title":"get_list  <code>async</code>","text":"<pre><code>get_list(\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve a summary of the databases in the cluster.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/databases</p> <p>Parameters:</p> Name Type Description Default <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default).</p> <code>None</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be schema, properties-schema, metrics, package, describe, or default.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the databases summary</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/databases.py</code> <pre><code>async def get_list(\n    self,\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve a summary of the databases in the cluster.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/databases\n\n    Parameters\n    ----------\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n    view : str\n        A specific view of the returned data.\n        Can be schema, properties-schema, metrics, package, describe, or default.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the databases summary\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = DatabasesGetCall(data_format=data_format, view=view)\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncDatabasesApi/#mlclient.api.AsyncDatabasesApi.get_properties","title":"get_properties  <code>async</code>","text":"<pre><code>get_properties(\n    database: str,\n    *,\n    data_format: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve the modifiable properties of the specified database.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/databases/[id-or-name]/properties</p> <p>Parameters:</p> Name Type Description Default <code>database</code> <code>str</code> <p>A database identifier. The database can be identified either by ID or name.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either json or xml (default). This parameter overrides the Accept header if both are present.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the database properties</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/databases.py</code> <pre><code>async def get_properties(\n    self,\n    database: str,\n    *,\n    data_format: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve the modifiable properties of the specified database.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/databases/[id-or-name]/properties\n\n    Parameters\n    ----------\n    database : str\n        A database identifier. The database can be identified either by ID or name.\n    data_format : str\n        The format of the returned data. Can be either json or xml (default).\n        This parameter overrides the Accept header if both are present.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the database properties\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = DatabasePropertiesGetCall(database=database, data_format=data_format)\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncDatabasesApi/#mlclient.api.AsyncDatabasesApi.post","title":"post  <code>async</code>","text":"<pre><code>post(\n    database: str, body: str | dict, *, timeout=UNSET\n) -&gt; Response\n</code></pre> <p>Clear or configure the specified database.</p> <p>Can be used to clear the contents of the named database and to perform various configuration operations on the database.</p> <p>Documentation: https://docs.marklogic.com/REST/POST/manage/v2/databases/[id-or-name]</p> <p>Parameters:</p> Name Type Description Default <code>database</code> <code>str</code> <p>A database identifier. The database can be identified either by ID or name.</p> required <code>body</code> <code>str | dict</code> <p>A database properties in XML or JSON format.</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/databases.py</code> <pre><code>async def post(\n    self,\n    database: str,\n    body: str | dict,\n    *,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Clear or configure the specified database.\n\n    Can be used to clear the contents of the named database and to perform\n    various configuration operations on the database.\n\n    Documentation: https://docs.marklogic.com/REST/POST/manage/v2/databases/[id-or-name]\n\n    Parameters\n    ----------\n    database : str\n        A database identifier. The database can be identified either by ID or name.\n    body : str | dict\n        A database properties in XML or JSON format.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = DatabasePostCall(database=database, body=body)\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncDatabasesApi/#mlclient.api.AsyncDatabasesApi.put_properties","title":"put_properties  <code>async</code>","text":"<pre><code>put_properties(\n    database: str, body: str | dict, *, timeout=UNSET\n) -&gt; Response\n</code></pre> <p>Modify the properties of the specified database.</p> <p>Documentation: https://docs.marklogic.com/REST/PUT/manage/v2/databases/[id-or-name]/properties</p> <p>Parameters:</p> Name Type Description Default <code>database</code> <code>str</code> <p>A database identifier. The database can be identified either by ID or name.</p> required <code>body</code> <code>str | dict</code> <p>A database properties in XML or JSON format.</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/databases.py</code> <pre><code>async def put_properties(\n    self,\n    database: str,\n    body: str | dict,\n    *,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Modify the properties of the specified database.\n\n    Documentation: https://docs.marklogic.com/REST/PUT/manage/v2/databases/[id-or-name]/properties\n\n    Parameters\n    ----------\n    database : str\n        A database identifier. The database can be identified either by ID or name.\n    body : str | dict\n        A database properties in XML or JSON format.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = DatabasePropertiesPutCall(database=database, body=body)\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncDocumentsApi/","title":"AsyncDocumentsApi","text":"<pre><code>from mlclient.api import AsyncDocumentsApi\n</code></pre>"},{"location":"reference/mlclient/api/AsyncDocumentsApi/#mlclient.api.AsyncDocumentsApi","title":"mlclient.api.AsyncDocumentsApi","text":"<pre><code>AsyncDocumentsApi(api: AsyncApiClient)\n</code></pre> <p>Async mid-level API for <code>/v1/documents</code> endpoints.</p> Source code in <code>mlclient/api/documents.py</code> <pre><code>def __init__(self, api: AsyncApiClient):\n    self._api = api\n</code></pre>"},{"location":"reference/mlclient/api/AsyncDocumentsApi/#mlclient.api.AsyncDocumentsApi.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(\n    uri: str | list,\n    *,\n    database: str | None = None,\n    category: str | list | None = None,\n    txid: str | None = None,\n    temporal_collection: str | None = None,\n    system_time: str | None = None,\n    wipe_temporal: bool | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Remove documents, or reset document metadata.</p> <p>Documentation: https://docs.marklogic.com/REST/DELETE/v1/documents</p> <p>Parameters:</p> Name Type Description Default <code>uri</code> <code>str | list</code> <p>The URI of a document to delete or for which to remove metadata. You can specify multiple documents.</p> required <code>database</code> <code>str</code> <p>Perform this operation on the named content database instead of the default content database associated with the REST API instance. Using an alternative database requires the \"eval-in\" privilege.</p> <code>None</code> <code>category</code> <code>str | list</code> <p>The category of data to remove/reset. Category may be specified multiple times to remove or reset any combination of content and metadata. Valid categories: content (default), metadata, metadata-values, collections, permissions, properties, and quality. Use metadata to reset all metadata.</p> <code>None</code> <code>txid</code> <code>str</code> <p>The transaction identifier of the multi-statement transaction in which to service this request. Use the /transactions service to create and manage multi-statement transactions.</p> <code>None</code> <code>temporal_collection</code> <code>str</code> <p>Specify the name of a temporal collection that contains the document(s) to be deleted. Applies to all documents when deleting more than one.</p> <code>None</code> <code>system_time</code> <code>str</code> <p>Set the system start time for the insertion or update. This time will override the system time set by MarkLogic. Ignored if temporal-collection is not included in the request. Applies to all documents when deleting more than one.</p> <code>None</code> <code>wipe_temporal</code> <code>bool</code> <p>Remove all versions of a temporal document rather than performing a temporal delete. You can only use this parameter when you also specify a temporal-collection parameter.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/documents.py</code> <pre><code>async def delete(\n    self,\n    uri: str | list,\n    *,\n    database: str | None = None,\n    category: str | list | None = None,\n    txid: str | None = None,\n    temporal_collection: str | None = None,\n    system_time: str | None = None,\n    wipe_temporal: bool | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Remove documents, or reset document metadata.\n\n    Documentation: https://docs.marklogic.com/REST/DELETE/v1/documents\n\n    Parameters\n    ----------\n    uri : str | list\n        The URI of a document to delete or for which to remove metadata.\n        You can specify multiple documents.\n    database : str\n        Perform this operation on the named content database instead\n        of the default content database associated with the REST API instance.\n        Using an alternative database requires the \"eval-in\" privilege.\n    category : str | list\n        The category of data to remove/reset.\n        Category may be specified multiple times to remove or reset\n        any combination of content and metadata.\n        Valid categories: content (default), metadata, metadata-values,\n        collections, permissions, properties, and quality.\n        Use metadata to reset all metadata.\n    txid : str\n        The transaction identifier of the multi-statement transaction in which\n        to service this request. Use the /transactions service to create and manage\n        multi-statement transactions.\n    temporal_collection : str\n        Specify the name of a temporal collection that contains the document(s)\n        to be deleted. Applies to all documents when deleting more than one.\n    system_time : str\n        Set the system start time for the insertion or update.\n        This time will override the system time set by MarkLogic.\n        Ignored if temporal-collection is not included in the request.\n        Applies to all documents when deleting more than one.\n    wipe_temporal : bool\n        Remove all versions of a temporal document rather than performing\n        a temporal delete. You can only use this parameter when you also specify\n        a temporal-collection parameter.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = DocumentsDeleteCall(\n        uri=uri,\n        database=database,\n        category=category,\n        txid=txid,\n        temporal_collection=temporal_collection,\n        system_time=system_time,\n        wipe_temporal=wipe_temporal,\n    )\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncDocumentsApi/#mlclient.api.AsyncDocumentsApi.get","title":"get  <code>async</code>","text":"<pre><code>get(\n    uri: str | list,\n    *,\n    database: str | None = None,\n    category: str | list | None = None,\n    data_format: str | None = None,\n    timestamp: str | None = None,\n    transform: str | None = None,\n    transform_params: dict | None = None,\n    txid: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve document content and/or metadata from the database.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/v1/documents</p> <p>Parameters:</p> Name Type Description Default <code>uri</code> <code>str | list</code> <p>One or more URIs for documents in the database. If you specify multiple URIs, the Accept header must be multipart/mixed.</p> required <code>database</code> <code>str</code> <p>Perform this operation on the named content database instead of the default content database associated with the REST API instance. Using an alternative database requires the \"eval-in\" privilege.</p> <code>None</code> <code>category</code> <code>str | list</code> <p>The category of data to fetch about the requested document. Category can be specified multiple times to retrieve any combination of content and metadata. Valid categories: content (default), metadata, metadata-values, collections, permissions, properties, and quality. Use metadata to request all categories except content.</p> <code>None</code> <code>data_format</code> <code>str</code> <p>The expected format of metadata returned in the response. Accepted values: xml or json. This parameter does not affect document content. For metadata, this parameter overrides the MIME type in the Accept header, except when the Accept header is multipart/mixed.</p> <code>None</code> <code>timestamp</code> <code>str</code> <p>A timestamp returned in the ML-Effective-Timestamp header of a previous request. Use this parameter to fetch documents based on the contents of the database at a fixed point-in-time.</p> <code>None</code> <code>transform</code> <code>str</code> <p>Names a content transformation previously installed via the /config/transforms service. The service applies the transformation to all documents prior to constructing the response.</p> <code>None</code> <code>transform_params</code> <code>dict</code> <p>A transform parameter names and values. For example, { \"myparam\": 1 }. Transform parameters are passed to the transform named in the transform parameter.</p> <code>None</code> <code>txid</code> <code>str</code> <p>The transaction identifier of the multi-statement transaction in which to service this request. Use the /transactions service to create and manage multi-statement transactions.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with document content and/or metadata</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/documents.py</code> <pre><code>async def get(\n    self,\n    uri: str | list,\n    *,\n    database: str | None = None,\n    category: str | list | None = None,\n    data_format: str | None = None,\n    timestamp: str | None = None,\n    transform: str | None = None,\n    transform_params: dict | None = None,\n    txid: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve document content and/or metadata from the database.\n\n    Documentation: https://docs.marklogic.com/REST/GET/v1/documents\n\n    Parameters\n    ----------\n    uri : str | list\n        One or more URIs for documents in the database.\n        If you specify multiple URIs, the Accept header must be multipart/mixed.\n    database : str\n        Perform this operation on the named content database instead\n        of the default content database associated with the REST API instance.\n        Using an alternative database requires the \"eval-in\" privilege.\n    category : str | list\n        The category of data to fetch about the requested document.\n        Category can be specified multiple times to retrieve any combination\n        of content and metadata. Valid categories: content (default), metadata,\n        metadata-values, collections, permissions, properties, and quality.\n        Use metadata to request all categories except content.\n    data_format : str\n        The expected format of metadata returned in the response.\n        Accepted values: xml or json.\n        This parameter does not affect document content.\n        For metadata, this parameter overrides the MIME type in the Accept header,\n        except when the Accept header is multipart/mixed.\n    timestamp : str\n        A timestamp returned in the ML-Effective-Timestamp header of a previous\n        request. Use this parameter to fetch documents based on the contents\n        of the database at a fixed point-in-time.\n    transform : str\n        Names a content transformation previously installed via\n        the /config/transforms service. The service applies the transformation\n        to all documents prior to constructing the response.\n    transform_params : dict\n        A transform parameter names and values. For example, { \"myparam\": 1 }.\n        Transform parameters are passed to the transform named in the transform\n        parameter.\n    txid : str\n        The transaction identifier of the multi-statement transaction in which\n        to service this request. Use the /transactions service to create and manage\n        multi-statement transactions.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with document content and/or metadata\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = DocumentsGetCall(\n        uri=uri,\n        database=database,\n        category=category,\n        data_format=data_format,\n        timestamp=timestamp,\n        transform=transform,\n        transform_params=transform_params,\n        txid=txid,\n    )\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncDocumentsApi/#mlclient.api.AsyncDocumentsApi.post","title":"post  <code>async</code>","text":"<pre><code>post(\n    body_parts: list[DocumentsBodyPart],\n    *,\n    database: str | None = None,\n    transform: str | None = None,\n    transform_params: dict | None = None,\n    txid: str | None = None,\n    temporal_collection: str | None = None,\n    system_time: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Insert or update content and/or metadata for multiple documents.</p> <p>Documentation: https://docs.marklogic.com/REST/POST/v1/documents</p> <p>Parameters:</p> Name Type Description Default <code>body_parts</code> <code>list[DocumentsBodyPart]</code> <p>A list of multipart request body parts</p> required <code>database</code> <code>str</code> <p>Perform this operation on the named content database instead of the default content database associated with the REST API instance. Using an alternative database requires the \"eval-in\" privilege.</p> <code>None</code> <code>transform</code> <code>str</code> <p>Names a content transformation previously installed via the /config/transforms service. The service applies the transformation to all documents prior to constructing the response.</p> <code>None</code> <code>transform_params</code> <code>dict</code> <p>A transform parameter names and values. For example, { \"myparam\": 1 }. Transform parameters are passed to the transform named in the transform parameter.</p> <code>None</code> <code>txid</code> <code>str</code> <p>The transaction identifier of the multi-statement transaction in which to service this request. Use the /transactions service to create and manage multi-statement transactions.</p> <code>None</code> <code>temporal_collection</code> <code>str</code> <p>Specify the name of a temporal collection into which the documents are to be inserted.</p> <code>None</code> <code>system_time</code> <code>str</code> <p>Set the system start time for the insertion or update. This time will override the system time set by MarkLogic. Ignored if temporal-collection is not included in the request.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with <code>application/json</code> body containing the write results</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/documents.py</code> <pre><code>async def post(\n    self,\n    body_parts: list[DocumentsBodyPart],\n    *,\n    database: str | None = None,\n    transform: str | None = None,\n    transform_params: dict | None = None,\n    txid: str | None = None,\n    temporal_collection: str | None = None,\n    system_time: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Insert or update content and/or metadata for multiple documents.\n\n    Documentation: https://docs.marklogic.com/REST/POST/v1/documents\n\n    Parameters\n    ----------\n    body_parts : list[DocumentsBodyPart]\n        A list of multipart request body parts\n    database : str\n        Perform this operation on the named content database instead\n        of the default content database associated with the REST API instance.\n        Using an alternative database requires the \"eval-in\" privilege.\n    transform : str\n        Names a content transformation previously installed via\n        the /config/transforms service. The service applies the transformation\n        to all documents prior to constructing the response.\n    transform_params : dict\n        A transform parameter names and values. For example, { \"myparam\": 1 }.\n        Transform parameters are passed to the transform named in the transform\n        parameter.\n    txid : str\n        The transaction identifier of the multi-statement transaction in which\n        to service this request. Use the /transactions service to create and manage\n        multi-statement transactions.\n    temporal_collection : str\n        Specify the name of a temporal collection into which the documents are\n        to be inserted.\n    system_time : str\n        Set the system start time for the insertion or update.\n        This time will override the system time set by MarkLogic.\n        Ignored if temporal-collection is not included in the request.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with ``application/json`` body containing\n        the write results\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = DocumentsPostCall(\n        body_parts=body_parts,\n        database=database,\n        transform=transform,\n        transform_params=transform_params,\n        txid=txid,\n        temporal_collection=temporal_collection,\n        system_time=system_time,\n    )\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncEvalApi/","title":"AsyncEvalApi","text":"<pre><code>from mlclient.api import AsyncEvalApi\n</code></pre>"},{"location":"reference/mlclient/api/AsyncEvalApi/#mlclient.api.AsyncEvalApi","title":"mlclient.api.AsyncEvalApi","text":"<pre><code>AsyncEvalApi(api: AsyncApiClient)\n</code></pre> <p>Async mid-level API for <code>/v1/eval</code> endpoint.</p> Source code in <code>mlclient/api/eval.py</code> <pre><code>def __init__(self, api: AsyncApiClient):\n    self._api = api\n</code></pre>"},{"location":"reference/mlclient/api/AsyncEvalApi/#mlclient.api.AsyncEvalApi.post","title":"post  <code>async</code>","text":"<pre><code>post(\n    *,\n    xquery: str | None = None,\n    javascript: str | None = None,\n    variables: dict | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Evaluate an ad-hoc query expressed using XQuery or server-side JavaScript.</p> <p>Documentation: https://docs.marklogic.com/REST/POST/v1/eval</p> <p>Parameters:</p> Name Type Description Default <code>xquery</code> <code>str</code> <p>The query to evaluate, expressed using XQuery. You must include either this parameter or the javascript parameter, but not both.</p> <code>None</code> <code>javascript</code> <code>str</code> <p>The query to evaluate, expressed using server-side JavaScript. You must include either this parameter or the xquery parameter, but not both.</p> <code>None</code> <code>variables</code> <code>dict</code> <p>External variables to pass to the query during evaluation</p> <code>None</code> <code>database</code> <code>str</code> <p>Perform this operation on the named content database instead of the default content database associated with the REST API instance. The database can be identified by name or by database id.</p> <code>None</code> <code>txid</code> <code>str</code> <p>The transaction identifier of the multi-statement transaction in which to service this request.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with <code>multipart/mixed</code> body containing the evaluation results</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/eval.py</code> <pre><code>async def post(\n    self,\n    *,\n    xquery: str | None = None,\n    javascript: str | None = None,\n    variables: dict | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Evaluate an ad-hoc query expressed using XQuery or server-side JavaScript.\n\n    Documentation: https://docs.marklogic.com/REST/POST/v1/eval\n\n    Parameters\n    ----------\n    xquery : str\n        The query to evaluate, expressed using XQuery.\n        You must include either this parameter or the javascript parameter,\n        but not both.\n    javascript : str\n        The query to evaluate, expressed using server-side JavaScript.\n        You must include either this parameter or the xquery parameter,\n        but not both.\n    variables : dict\n        External variables to pass to the query during evaluation\n    database : str\n        Perform this operation on the named content database\n        instead of the default content database associated with the REST API\n        instance. The database can be identified by name or by database id.\n    txid : str\n        The transaction identifier of the multi-statement transaction\n        in which to service this request.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with ``multipart/mixed`` body containing\n        the evaluation results\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = EvalCall(\n        xquery=xquery,\n        javascript=javascript,\n        variables=variables,\n        database=database,\n        txid=txid,\n    )\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncForestsApi/","title":"AsyncForestsApi","text":"<pre><code>from mlclient.api import AsyncForestsApi\n</code></pre>"},{"location":"reference/mlclient/api/AsyncForestsApi/#mlclient.api.AsyncForestsApi","title":"mlclient.api.AsyncForestsApi","text":"<pre><code>AsyncForestsApi(api: AsyncApiClient)\n</code></pre> <p>Async mid-level API for <code>/manage/v2/forests</code> endpoints.</p> Source code in <code>mlclient/api/forests.py</code> <pre><code>def __init__(self, api: AsyncApiClient):\n    self._api = api\n</code></pre>"},{"location":"reference/mlclient/api/AsyncForestsApi/#mlclient.api.AsyncForestsApi.create","title":"create  <code>async</code>","text":"<pre><code>create(\n    body: str | dict,\n    *,\n    wait_for_forest_to_mount: bool | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Create a new forest, including replicas if specified.</p> <p>If a database id or database is included, attach the new forest(s) to the database.</p> <p>Documentation: https://docs.marklogic.com/REST/POST/manage/v2/forests</p> <p>Parameters:</p> Name Type Description Default <code>body</code> <code>str | dict</code> <p>A forest properties in XML or JSON format.</p> required <code>wait_for_forest_to_mount</code> <code>bool</code> <p>Whether to wait for the new forest to mount before sending a response to this request. Allowed values: true (default) or false.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/forests.py</code> <pre><code>async def create(\n    self,\n    body: str | dict,\n    *,\n    wait_for_forest_to_mount: bool | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Create a new forest, including replicas if specified.\n\n    If a database id or database is included, attach the new forest(s)\n    to the database.\n\n    Documentation: https://docs.marklogic.com/REST/POST/manage/v2/forests\n\n    Parameters\n    ----------\n    body : str | dict\n        A forest properties in XML or JSON format.\n    wait_for_forest_to_mount : bool\n        Whether to wait for the new forest to mount before sending a response\n        to this request. Allowed values: true (default) or false.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = ForestsPostCall(\n        body=body,\n        wait_for_forest_to_mount=wait_for_forest_to_mount,\n    )\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncForestsApi/#mlclient.api.AsyncForestsApi.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(\n    forest: str,\n    *,\n    level: str,\n    replicas: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Delete a forest.</p> <p>Documentation: https://docs.marklogic.com/REST/DELETE/manage/v2/forests/[id-or-name]</p> <p>Parameters:</p> Name Type Description Default <code>forest</code> <code>str</code> <p>A forest identifier. The forest can be identified either by ID or name.</p> required <code>level</code> <code>str</code> <p>The type of state change to initiate. Allowed values: full, config-only. A config-only deletion removes only the forest configuration; the data contained in the forest remains on disk. A full deletion removes both the forest configuration and the data.</p> required <code>replicas</code> <code>str</code> <p>Determines how to process the replicas. Allowed values: detach to detach the replica but keep it; delete to detach and delete the replica.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/forests.py</code> <pre><code>async def delete(\n    self,\n    forest: str,\n    *,\n    level: str,\n    replicas: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Delete a forest.\n\n    Documentation: https://docs.marklogic.com/REST/DELETE/manage/v2/forests/[id-or-name]\n\n    Parameters\n    ----------\n    forest : str\n        A forest identifier. The forest can be identified either by ID or name.\n    level : str\n        The type of state change to initiate. Allowed values: full, config-only.\n        A config-only deletion removes only the forest configuration;\n        the data contained in the forest remains on disk.\n        A full deletion removes both the forest configuration and the data.\n    replicas : str\n        Determines how to process the replicas.\n        Allowed values: detach to detach the replica but keep it; delete to detach\n        and delete the replica.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = ForestDeleteCall(forest=forest, level=level, replicas=replicas)\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncForestsApi/#mlclient.api.AsyncForestsApi.get","title":"get  <code>async</code>","text":"<pre><code>get(\n    forest: str,\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve information about a forest.</p> <p>The forest can be identified either by ID or name.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/forests/[id-or-name]</p> <p>Parameters:</p> Name Type Description Default <code>forest</code> <code>str</code> <p>A forest identifier. The forest can be identified either by ID or name.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default).</p> <code>None</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be properties-schema, config, edit, package, describe, status, xdmp:server-status or default.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the forest details</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/forests.py</code> <pre><code>async def get(\n    self,\n    forest: str,\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve information about a forest.\n\n    The forest can be identified either by ID or name.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/forests/[id-or-name]\n\n    Parameters\n    ----------\n    forest : str\n        A forest identifier. The forest can be identified either by ID or name.\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n    view : str\n        A specific view of the returned data.\n        Can be properties-schema, config, edit, package, describe, status,\n        xdmp:server-status or default.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the forest details\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = ForestGetCall(forest=forest, data_format=data_format, view=view)\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncForestsApi/#mlclient.api.AsyncForestsApi.get_list","title":"get_list  <code>async</code>","text":"<pre><code>get_list(\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    database: str | None = None,\n    group: str | None = None,\n    host: str | None = None,\n    full_refs: bool | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve data about the forests in the cluster.</p> <p>The data returned depends on the view. If no view is specified, this request returns a summary of the forests in the cluster.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/forests</p> <p>Parameters:</p> Name Type Description Default <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default).</p> <code>None</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be either describe, default, status, metrics, schema, storage, or properties-schema.</p> <code>None</code> <code>database</code> <code>str</code> <p>Returns a summary of the forests for the specified database. The database can be identified either by id or name.</p> <code>None</code> <code>group</code> <code>str</code> <p>Returns a summary of the forests for the specified group. The group can be identified either by id or name.</p> <code>None</code> <code>host</code> <code>str</code> <p>Returns a summary of the forests for the specified host. The host can be identified either by id or name.</p> <code>None</code> <code>full_refs</code> <code>bool</code> <p>If set to true, full detail is returned for all relationship references. A value of false (the default) indicates to return detail only for first references.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the forests summary</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/forests.py</code> <pre><code>async def get_list(\n    self,\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    database: str | None = None,\n    group: str | None = None,\n    host: str | None = None,\n    full_refs: bool | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve data about the forests in the cluster.\n\n    The data returned depends on the view. If no view is specified, this\n    request returns a summary of the forests in the cluster.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/forests\n\n    Parameters\n    ----------\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n    view : str\n        A specific view of the returned data.\n        Can be either describe, default, status, metrics, schema, storage,\n        or properties-schema.\n    database : str\n        Returns a summary of the forests for the specified database.\n        The database can be identified either by id or name.\n    group : str\n        Returns a summary of the forests for the specified group.\n        The group can be identified either by id or name.\n    host : str\n        Returns a summary of the forests for the specified host.\n        The host can be identified either by id or name.\n    full_refs : bool\n        If set to true, full detail is returned for all relationship references.\n        A value of false (the default) indicates to return detail only for first\n        references.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the forests summary\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = ForestsGetCall(\n        data_format=data_format,\n        view=view,\n        database=database,\n        group=group,\n        host=host,\n        full_refs=full_refs,\n    )\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncForestsApi/#mlclient.api.AsyncForestsApi.get_properties","title":"get_properties  <code>async</code>","text":"<pre><code>get_properties(\n    forest: str,\n    *,\n    data_format: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve the modifiable properties of a forest.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/forests/[id-or-name]/properties</p> <p>Parameters:</p> Name Type Description Default <code>forest</code> <code>str</code> <p>A forest identifier. The forest can be identified either by ID or name.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either json or xml (default). This parameter overrides the Accept header if both are present.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the forest properties</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/forests.py</code> <pre><code>async def get_properties(\n    self,\n    forest: str,\n    *,\n    data_format: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve the modifiable properties of a forest.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/forests/[id-or-name]/properties\n\n    Parameters\n    ----------\n    forest : str\n        A forest identifier. The forest can be identified either by ID or name.\n    data_format : str\n        The format of the returned data. Can be either json or xml (default).\n        This parameter overrides the Accept header if both are present.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the forest properties\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = ForestPropertiesGetCall(forest=forest, data_format=data_format)\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncForestsApi/#mlclient.api.AsyncForestsApi.post","title":"post  <code>async</code>","text":"<pre><code>post(\n    forest: str, body: str | dict, *, timeout=UNSET\n) -&gt; Response\n</code></pre> <p>Initiate a state change on a forest, such as a merge, restart, or attach.</p> <p>Documentation: https://docs.marklogic.com/REST/POST/manage/v2/forests/[id-or-name]</p> <p>Parameters:</p> Name Type Description Default <code>forest</code> <code>str</code> <p>A forest identifier. The forest can be identified either by ID or name.</p> required <code>body</code> <code>dict</code> <p>A list of properties. Need to include the 'state' property (the type of state change to initiate). Allowed values: clear, merge, restart, attach, detach, retire, employ.</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/forests.py</code> <pre><code>async def post(\n    self,\n    forest: str,\n    body: str | dict,\n    *,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Initiate a state change on a forest, such as a merge, restart, or attach.\n\n    Documentation: https://docs.marklogic.com/REST/POST/manage/v2/forests/[id-or-name]\n\n    Parameters\n    ----------\n    forest : str\n        A forest identifier. The forest can be identified either by ID or name.\n    body : dict\n        A list of properties. Need to include the 'state' property (the type\n        of state change to initiate).\n        Allowed values: clear, merge, restart, attach, detach, retire, employ.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = ForestPostCall(forest=forest, body=body)\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncForestsApi/#mlclient.api.AsyncForestsApi.put","title":"put  <code>async</code>","text":"<pre><code>put(body: str | dict, *, timeout=UNSET) -&gt; Response\n</code></pre> <p>Perform an operation on one or more forests.</p> <p>Such as combining multiple forests into a single new one, or migrating the data in the forests to a new data directory.</p> <p>Documentation: https://docs.marklogic.com/REST/PUT/manage/v2/forests</p> <p>Parameters:</p> Name Type Description Default <code>body</code> <code>str | dict</code> <p>A forest properties in XML or JSON format.</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/forests.py</code> <pre><code>async def put(\n    self,\n    body: str | dict,\n    *,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Perform an operation on one or more forests.\n\n    Such as combining multiple forests into a single new one, or migrating\n    the data in the forests to a new data directory.\n\n    Documentation: https://docs.marklogic.com/REST/PUT/manage/v2/forests\n\n    Parameters\n    ----------\n    body : str | dict\n        A forest properties in XML or JSON format.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = ForestsPutCall(body=body)\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncForestsApi/#mlclient.api.AsyncForestsApi.put_properties","title":"put_properties  <code>async</code>","text":"<pre><code>put_properties(\n    forest: str, body: str | dict, *, timeout=UNSET\n) -&gt; Response\n</code></pre> <p>Modify the configuration of a forest.</p> <p>Documentation: https://docs.marklogic.com/REST/PUT/manage/v2/forests/[id-or-name]/properties</p> <p>Parameters:</p> Name Type Description Default <code>forest</code> <code>str</code> <p>A forest identifier. The forest can be identified either by ID or name.</p> required <code>body</code> <code>str | dict</code> <p>A forest properties in XML or JSON format.</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/forests.py</code> <pre><code>async def put_properties(\n    self,\n    forest: str,\n    body: str | dict,\n    *,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Modify the configuration of a forest.\n\n    Documentation: https://docs.marklogic.com/REST/PUT/manage/v2/forests/[id-or-name]/properties\n\n    Parameters\n    ----------\n    forest : str\n        A forest identifier. The forest can be identified either by ID or name.\n    body : str | dict\n        A forest properties in XML or JSON format.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = ForestPropertiesPutCall(forest=forest, body=body)\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncGroupsApi/","title":"AsyncGroupsApi","text":"<pre><code>from mlclient.api import AsyncGroupsApi\n</code></pre>"},{"location":"reference/mlclient/api/AsyncGroupsApi/#mlclient.api.AsyncGroupsApi","title":"mlclient.api.AsyncGroupsApi","text":"<pre><code>AsyncGroupsApi(api: AsyncApiClient)\n</code></pre> <p>Async mid-level API for <code>/manage/v2/groups</code> endpoints.</p> <p>Initialize the asynchronous group API.</p> <p>Parameters:</p> Name Type Description Default <code>api</code> <code>AsyncApiClient</code> <p>Client connected to the Management server</p> required Source code in <code>mlclient/api/groups.py</code> <pre><code>def __init__(self, api: AsyncApiClient):\n    \"\"\"Initialize the asynchronous group API.\n\n    Parameters\n    ----------\n    api : AsyncApiClient\n        Client connected to the Management server\n    \"\"\"\n    self._api = api\n</code></pre>"},{"location":"reference/mlclient/api/AsyncGroupsApi/#mlclient.api.AsyncGroupsApi.get_properties","title":"get_properties  <code>async</code>","text":"<pre><code>get_properties(\n    group: str,\n    *,\n    data_format: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve the modifiable properties of the specified group.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/groups/[id-or-name]/properties</p> <p>Parameters:</p> Name Type Description Default <code>group</code> <code>str</code> <p>A group identifier. The group can be identified either by ID or name.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default). This parameter overrides the Accept header if both are present.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>Per-request HTTP timeout. Unset uses the client configuration; None disables it; a number sets all four components; an httpx.Timeout sets them independently. Not sent as a query parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the group properties</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP timeout expires after any configured retries</p> Source code in <code>mlclient/api/groups.py</code> <pre><code>async def get_properties(\n    self,\n    group: str,\n    *,\n    data_format: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve the modifiable properties of the specified group.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/groups/[id-or-name]/properties\n\n    Parameters\n    ----------\n    group : str\n        A group identifier. The group can be identified either by ID or name.\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n        This parameter overrides the Accept header if both are present.\n    timeout : httpx.Timeout | float | None, default unset\n        Per-request HTTP timeout. Unset uses the client configuration;\n        None disables it; a number sets all four components; an\n        httpx.Timeout sets them independently. Not sent as a query parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the group properties\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP timeout expires after any configured retries\n    \"\"\"\n    call = GroupPropertiesGetCall(group=group, data_format=data_format)\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncGroupsApi/#mlclient.api.AsyncGroupsApi.put_properties","title":"put_properties  <code>async</code>","text":"<pre><code>put_properties(\n    group: str, body: str | dict, *, timeout=UNSET\n) -&gt; Response\n</code></pre> <p>Modify the properties of the specified group.</p> <p>Documentation: https://docs.marklogic.com/REST/PUT/manage/v2/groups/[id-or-name]/properties</p> <p>Parameters:</p> Name Type Description Default <code>group</code> <code>str</code> <p>A group identifier. The group can be identified either by ID or name.</p> required <code>body</code> <code>str | dict</code> <p>Group properties in XML or JSON format.</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>Per-request HTTP timeout. Unset uses the client configuration; None disables it; a number sets all four components; an httpx.Timeout sets them independently. Not sent as a query parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP timeout expires after any configured retries</p> Source code in <code>mlclient/api/groups.py</code> <pre><code>async def put_properties(\n    self,\n    group: str,\n    body: str | dict,\n    *,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Modify the properties of the specified group.\n\n    Documentation: https://docs.marklogic.com/REST/PUT/manage/v2/groups/[id-or-name]/properties\n\n    Parameters\n    ----------\n    group : str\n        A group identifier. The group can be identified either by ID or name.\n    body : str | dict\n        Group properties in XML or JSON format.\n    timeout : httpx.Timeout | float | None, default unset\n        Per-request HTTP timeout. Unset uses the client configuration;\n        None disables it; a number sets all four components; an\n        httpx.Timeout sets them independently. Not sent as a query parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP timeout expires after any configured retries\n    \"\"\"\n    call = GroupPropertiesPutCall(group=group, body=body)\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncHostsApi/","title":"AsyncHostsApi","text":"<pre><code>from mlclient.api import AsyncHostsApi\n</code></pre>"},{"location":"reference/mlclient/api/AsyncHostsApi/#mlclient.api.AsyncHostsApi","title":"mlclient.api.AsyncHostsApi","text":"<pre><code>AsyncHostsApi(api: AsyncApiClient)\n</code></pre> <p>Async mid-level API for <code>/manage/v2/hosts</code> endpoint.</p> <p>Bind host operations to an asynchronous API client.</p> <p>Parameters:</p> Name Type Description Default <code>api</code> <code>AsyncApiClient</code> <p>The transport used to send requests.</p> required Source code in <code>mlclient/api/hosts.py</code> <pre><code>def __init__(self, api: AsyncApiClient):\n    \"\"\"Bind host operations to an asynchronous API client.\n\n    Parameters\n    ----------\n    api : AsyncApiClient\n        The transport used to send requests.\n    \"\"\"\n    self._api = api\n</code></pre>"},{"location":"reference/mlclient/api/AsyncHostsApi/#mlclient.api.AsyncHostsApi.get_list","title":"get_list  <code>async</code>","text":"<pre><code>get_list(\n    *,\n    data_format: str | None = None,\n    group_id: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve data about the hosts in the cluster.</p> <p>The data returned depends on the setting of the view request parameter. The default view provides a summary of the hosts.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/hosts</p> <p>Parameters:</p> Name Type Description Default <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default).</p> <code>None</code> <code>group_id</code> <code>str</code> <p>Specifies to return only the hosts in the specified group. The group can be identified either by id or name. If not specified, the response includes information about all hosts.</p> <code>None</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be default, status, metrics, schema, properties-schema, or describe. The schema view requires XML format.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the hosts summary</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/hosts.py</code> <pre><code>async def get_list(\n    self,\n    *,\n    data_format: str | None = None,\n    group_id: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve data about the hosts in the cluster.\n\n    The data returned depends on the setting of the view request parameter.\n    The default view provides a summary of the hosts.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/hosts\n\n    Parameters\n    ----------\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n    group_id : str\n        Specifies to return only the hosts in the specified group.\n        The group can be identified either by id or name.\n        If not specified, the response includes information about all hosts.\n    view : str\n        A specific view of the returned data.\n        Can be default, status, metrics, schema, properties-schema, or describe.\n        The schema view requires XML format.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the hosts summary\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = HostsGetCall(data_format=data_format, group_id=group_id, view=view)\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncLogsApi/","title":"AsyncLogsApi","text":"<pre><code>from mlclient.api import AsyncLogsApi\n</code></pre>"},{"location":"reference/mlclient/api/AsyncLogsApi/#mlclient.api.AsyncLogsApi","title":"mlclient.api.AsyncLogsApi","text":"<pre><code>AsyncLogsApi(api: AsyncApiClient)\n</code></pre> <p>Async mid-level API for <code>/manage/v2/logs</code> endpoint.</p> Source code in <code>mlclient/api/logs.py</code> <pre><code>def __init__(self, api: AsyncApiClient):\n    self._api = api\n</code></pre>"},{"location":"reference/mlclient/api/AsyncLogsApi/#mlclient.api.AsyncLogsApi.get","title":"get  <code>async</code>","text":"<pre><code>get(\n    filename: str,\n    *,\n    data_format: str | None = None,\n    host: str | None = None,\n    start_time: str | None = None,\n    end_time: str | None = None,\n    regex: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve the contents of a log file.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/logs</p> <p>Parameters:</p> Name Type Description Default <code>filename</code> <code>str</code> <p>The log file to be returned.</p> required <code>data_format</code> <code>str</code> <p>The format of the data in the log file. The supported formats are xml, json or html.</p> <code>None</code> <code>host</code> <code>str</code> <p>The host from which to return the log data.</p> <code>None</code> <code>start_time</code> <code>str</code> <p>The start time for the log data.</p> <code>None</code> <code>end_time</code> <code>str</code> <p>The end time for the log data.</p> <code>None</code> <code>regex</code> <code>str</code> <p>Filters the log data, based on a regular expression.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response containing the log data</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/logs.py</code> <pre><code>async def get(\n    self,\n    filename: str,\n    *,\n    data_format: str | None = None,\n    host: str | None = None,\n    start_time: str | None = None,\n    end_time: str | None = None,\n    regex: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve the contents of a log file.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/logs\n\n    Parameters\n    ----------\n    filename : str\n        The log file to be returned.\n    data_format : str\n        The format of the data in the log file. The supported formats are xml, json\n        or html.\n    host : str\n        The host from which to return the log data.\n    start_time : str\n        The start time for the log data.\n    end_time : str\n        The end time for the log data.\n    regex : str\n        Filters the log data, based on a regular expression.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response containing the log data\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = LogsCall(\n        filename=filename,\n        data_format=data_format,\n        host=host,\n        start_time=start_time,\n        end_time=end_time,\n        regex=regex,\n    )\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncManageApi/","title":"AsyncManageApi","text":"<pre><code>from mlclient.api import AsyncManageApi\n</code></pre>"},{"location":"reference/mlclient/api/AsyncManageApi/#mlclient.api.AsyncManageApi","title":"mlclient.api.AsyncManageApi","text":"<pre><code>AsyncManageApi(api: AsyncApiClient)\n</code></pre> <p>Async Management API group for /manage/v2/* endpoints.</p> Source code in <code>mlclient/api/manage.py</code> <pre><code>def __init__(self, api: AsyncApiClient):\n    self._api = api\n</code></pre>"},{"location":"reference/mlclient/api/AsyncManageApi/#mlclient.api.AsyncManageApi.databases","title":"databases  <code>cached</code> <code>property</code>","text":"<pre><code>databases: AsyncDatabasesApi\n</code></pre> <p>Access databases operations through this API group's connection.</p> <p>Created once on first access and reused by this group. Reading the property sends no request; calling an endpoint method performs I/O and returns a raw HTTP response. Responses are not cached.</p> <p>Returns:</p> Type Description <code>AsyncDatabasesApi</code> <p>The endpoint wrapper bound to this group's configured connection.</p>"},{"location":"reference/mlclient/api/AsyncManageApi/#mlclient.api.AsyncManageApi.forests","title":"forests  <code>cached</code> <code>property</code>","text":"<pre><code>forests: AsyncForestsApi\n</code></pre> <p>Access forests operations through this API group's connection.</p> <p>Created once on first access and reused by this group. Reading the property sends no request; calling an endpoint method performs I/O and returns a raw HTTP response. Responses are not cached.</p> <p>Returns:</p> Type Description <code>AsyncForestsApi</code> <p>The endpoint wrapper bound to this group's configured connection.</p>"},{"location":"reference/mlclient/api/AsyncManageApi/#mlclient.api.AsyncManageApi.groups","title":"groups  <code>cached</code> <code>property</code>","text":"<pre><code>groups: AsyncGroupsApi\n</code></pre> <p>Access groups operations through this API group's connection.</p> <p>Created once on first access and reused by this group. Reading the property sends no request; calling an endpoint method performs I/O and returns a raw HTTP response. Responses are not cached.</p> <p>Returns:</p> Type Description <code>AsyncGroupsApi</code> <p>The endpoint wrapper bound to this group's configured connection.</p>"},{"location":"reference/mlclient/api/AsyncManageApi/#mlclient.api.AsyncManageApi.hosts","title":"hosts  <code>cached</code> <code>property</code>","text":"<pre><code>hosts: AsyncHostsApi\n</code></pre> <p>Access hosts operations through this API group's connection.</p> <p>Created once on first access and reused by this group. Reading the property sends no request; calling an endpoint method performs I/O and returns a raw HTTP response. Responses are not cached.</p> <p>Returns:</p> Type Description <code>AsyncHostsApi</code> <p>The endpoint wrapper bound to this group's configured connection.</p>"},{"location":"reference/mlclient/api/AsyncManageApi/#mlclient.api.AsyncManageApi.logs","title":"logs  <code>cached</code> <code>property</code>","text":"<pre><code>logs: AsyncLogsApi\n</code></pre> <p>Access logs operations through this API group's connection.</p> <p>Created once on first access and reused by this group. Reading the property sends no request; calling an endpoint method performs I/O and returns a raw HTTP response. Responses are not cached.</p> <p>Returns:</p> Type Description <code>AsyncLogsApi</code> <p>The endpoint wrapper bound to this group's configured connection.</p>"},{"location":"reference/mlclient/api/AsyncManageApi/#mlclient.api.AsyncManageApi.roles","title":"roles  <code>cached</code> <code>property</code>","text":"<pre><code>roles: AsyncRolesApi\n</code></pre> <p>Access roles operations through this API group's connection.</p> <p>Created once on first access and reused by this group. Reading the property sends no request; calling an endpoint method performs I/O and returns a raw HTTP response. Responses are not cached.</p> <p>Returns:</p> Type Description <code>AsyncRolesApi</code> <p>The endpoint wrapper bound to this group's configured connection.</p>"},{"location":"reference/mlclient/api/AsyncManageApi/#mlclient.api.AsyncManageApi.servers","title":"servers  <code>cached</code> <code>property</code>","text":"<pre><code>servers: AsyncServersApi\n</code></pre> <p>Access servers operations through this API group's connection.</p> <p>Created once on first access and reused by this group. Reading the property sends no request; calling an endpoint method performs I/O and returns a raw HTTP response. Responses are not cached.</p> <p>Returns:</p> Type Description <code>AsyncServersApi</code> <p>The endpoint wrapper bound to this group's configured connection.</p>"},{"location":"reference/mlclient/api/AsyncManageApi/#mlclient.api.AsyncManageApi.users","title":"users  <code>cached</code> <code>property</code>","text":"<pre><code>users: AsyncUsersApi\n</code></pre> <p>Access users operations through this API group's connection.</p> <p>Created once on first access and reused by this group. Reading the property sends no request; calling an endpoint method performs I/O and returns a raw HTTP response. Responses are not cached.</p> <p>Returns:</p> Type Description <code>AsyncUsersApi</code> <p>The endpoint wrapper bound to this group's configured connection.</p>"},{"location":"reference/mlclient/api/AsyncManageApi/#mlclient.api.AsyncManageApi.call","title":"call  <code>async</code>","text":"<pre><code>call(call_: ApiCall, *, timeout=UNSET) -&gt; Response\n</code></pre> <p>Send a custom ApiCall.</p> <p>Parameters:</p> Name Type Description Default <code>call_</code> <code>ApiCall</code> <p>A specific endpoint call implementation</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/manage.py</code> <pre><code>async def call(self, call_: ApiCall, *, timeout=UNSET) -&gt; Response:\n    \"\"\"Send a custom ApiCall.\n\n    Parameters\n    ----------\n    call_ : ApiCall\n        A specific endpoint call implementation\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return await self._api.call(call_, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncRestApi/","title":"AsyncRestApi","text":"<pre><code>from mlclient.api import AsyncRestApi\n</code></pre>"},{"location":"reference/mlclient/api/AsyncRestApi/#mlclient.api.AsyncRestApi","title":"mlclient.api.AsyncRestApi","text":"<pre><code>AsyncRestApi(api: AsyncApiClient)\n</code></pre> <p>Async REST API group for /v1/* endpoints (eval, documents, transactions).</p> Source code in <code>mlclient/api/rest.py</code> <pre><code>def __init__(self, api: AsyncApiClient):\n    self._api = api\n</code></pre>"},{"location":"reference/mlclient/api/AsyncRestApi/#mlclient.api.AsyncRestApi.documents","title":"documents  <code>cached</code> <code>property</code>","text":"<pre><code>documents: AsyncDocumentsApi\n</code></pre> <p>Access documents operations through this API group's connection.</p> <p>Created once on first access and reused by this group. Reading the property sends no request; calling an endpoint method performs I/O and returns a raw HTTP response. Responses are not cached.</p> <p>Returns:</p> Type Description <code>AsyncDocumentsApi</code> <p>The endpoint wrapper bound to this group's configured connection.</p>"},{"location":"reference/mlclient/api/AsyncRestApi/#mlclient.api.AsyncRestApi.eval","title":"eval  <code>cached</code> <code>property</code>","text":"<pre><code>eval: AsyncEvalApi\n</code></pre> <p>Access eval operations through this API group's connection.</p> <p>Created once on first access and reused by this group. Reading the property sends no request; calling an endpoint method performs I/O and returns a raw HTTP response. Responses are not cached.</p> <p>Returns:</p> Type Description <code>AsyncEvalApi</code> <p>The endpoint wrapper bound to this group's configured connection.</p>"},{"location":"reference/mlclient/api/AsyncRestApi/#mlclient.api.AsyncRestApi.transactions","title":"transactions  <code>cached</code> <code>property</code>","text":"<pre><code>transactions: AsyncTransactionsApi\n</code></pre> <p>Access transactions operations through this API group's connection.</p> <p>Created once on first access and reused by this group. Reading the property sends no request; calling an endpoint method performs I/O and returns a raw HTTP response. Responses are not cached.</p> <p>Returns:</p> Type Description <code>AsyncTransactionsApi</code> <p>The endpoint wrapper bound to this group's configured connection.</p>"},{"location":"reference/mlclient/api/AsyncRestApi/#mlclient.api.AsyncRestApi.call","title":"call  <code>async</code>","text":"<pre><code>call(call_: ApiCall, *, timeout=UNSET) -&gt; Response\n</code></pre> <p>Send a custom ApiCall.</p> <p>Parameters:</p> Name Type Description Default <code>call_</code> <code>ApiCall</code> <p>A specific endpoint call implementation</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/rest.py</code> <pre><code>async def call(self, call_: ApiCall, *, timeout=UNSET) -&gt; Response:\n    \"\"\"Send a custom ApiCall.\n\n    Parameters\n    ----------\n    call_ : ApiCall\n        A specific endpoint call implementation\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return await self._api.call(call_, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncRolesApi/","title":"AsyncRolesApi","text":"<pre><code>from mlclient.api import AsyncRolesApi\n</code></pre>"},{"location":"reference/mlclient/api/AsyncRolesApi/#mlclient.api.AsyncRolesApi","title":"mlclient.api.AsyncRolesApi","text":"<pre><code>AsyncRolesApi(api: AsyncApiClient)\n</code></pre> <p>Async mid-level API for <code>/manage/v2/roles</code> endpoints.</p> Source code in <code>mlclient/api/roles.py</code> <pre><code>def __init__(self, api: AsyncApiClient):\n    self._api = api\n</code></pre>"},{"location":"reference/mlclient/api/AsyncRolesApi/#mlclient.api.AsyncRolesApi.create","title":"create  <code>async</code>","text":"<pre><code>create(body: str | dict, *, timeout=UNSET) -&gt; Response\n</code></pre> <p>Create a new role in the security database.</p> <p>Documentation: https://docs.marklogic.com/REST/POST/manage/v2/roles</p> <p>Parameters:</p> Name Type Description Default <code>body</code> <code>str | dict</code> <p>A role properties in XML or JSON format.</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/roles.py</code> <pre><code>async def create(\n    self,\n    body: str | dict,\n    *,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Create a new role in the security database.\n\n    Documentation: https://docs.marklogic.com/REST/POST/manage/v2/roles\n\n    Parameters\n    ----------\n    body : str | dict\n        A role properties in XML or JSON format.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = RolesPostCall(body=body)\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncRolesApi/#mlclient.api.AsyncRolesApi.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(role: str, *, timeout=UNSET) -&gt; Response\n</code></pre> <p>Delete the specified role from the security database.</p> <p>Documentation: https://docs.marklogic.com/REST/DELETE/manage/v2/roles/[id-or-name]</p> <p>Parameters:</p> Name Type Description Default <code>role</code> <code>str</code> <p>A role identifier. The role can be identified either by ID or name.</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/roles.py</code> <pre><code>async def delete(\n    self,\n    role: str,\n    *,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Delete the specified role from the security database.\n\n    Documentation: https://docs.marklogic.com/REST/DELETE/manage/v2/roles/[id-or-name]\n\n    Parameters\n    ----------\n    role : str\n        A role identifier. The role can be identified either by ID or name.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = RoleDeleteCall(role=role)\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncRolesApi/#mlclient.api.AsyncRolesApi.get","title":"get  <code>async</code>","text":"<pre><code>get(\n    role: str,\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve the configuration for the specified role.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/roles/[id-or-name]</p> <p>Parameters:</p> Name Type Description Default <code>role</code> <code>str</code> <p>A role identifier. The role can be identified either by ID or name.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default). This parameter is not meaningful with view=edit.</p> <code>None</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be: describe, or default.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the role details</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/roles.py</code> <pre><code>async def get(\n    self,\n    role: str,\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve the configuration for the specified role.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/roles/[id-or-name]\n\n    Parameters\n    ----------\n    role : str\n        A role identifier. The role can be identified either by ID or name.\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n        This parameter is not meaningful with view=edit.\n    view : str\n        A specific view of the returned data. Can be: describe, or default.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the role details\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = RoleGetCall(role=role, data_format=data_format, view=view)\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncRolesApi/#mlclient.api.AsyncRolesApi.get_list","title":"get_list  <code>async</code>","text":"<pre><code>get_list(\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve a summary of the roles in the security database.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/roles</p> <p>Parameters:</p> Name Type Description Default <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default).</p> <code>None</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be: describe, or default.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the roles summary</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/roles.py</code> <pre><code>async def get_list(\n    self,\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve a summary of the roles in the security database.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/roles\n\n    Parameters\n    ----------\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n    view : str\n        A specific view of the returned data. Can be: describe, or default.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the roles summary\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = RolesGetCall(data_format=data_format, view=view)\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncRolesApi/#mlclient.api.AsyncRolesApi.get_properties","title":"get_properties  <code>async</code>","text":"<pre><code>get_properties(\n    role: str,\n    *,\n    data_format: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve the properties of the specified role.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/roles/[id-or-name]/properties</p> <p>Parameters:</p> Name Type Description Default <code>role</code> <code>str</code> <p>A role identifier. The role can be identified either by ID or name.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either json or xml (default). This parameter overrides the Accept header if both are present.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the role properties</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/roles.py</code> <pre><code>async def get_properties(\n    self,\n    role: str,\n    *,\n    data_format: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve the properties of the specified role.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/roles/[id-or-name]/properties\n\n    Parameters\n    ----------\n    role : str\n        A role identifier. The role can be identified either by ID or name.\n    data_format : str\n        The format of the returned data. Can be either json or xml (default).\n        This parameter overrides the Accept header if both are present.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the role properties\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = RolePropertiesGetCall(role=role, data_format=data_format)\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncRolesApi/#mlclient.api.AsyncRolesApi.put_properties","title":"put_properties  <code>async</code>","text":"<pre><code>put_properties(\n    role: str, body: str | dict, *, timeout=UNSET\n) -&gt; Response\n</code></pre> <p>Update the properties for the specified role.</p> <p>Documentation: https://docs.marklogic.com/REST/PUT/manage/v2/roles/[id-or-name]/properties</p> <p>Parameters:</p> Name Type Description Default <code>role</code> <code>str</code> <p>A role identifier. The role can be identified either by ID or name.</p> required <code>body</code> <code>str | dict</code> <p>A role properties in XML or JSON format.</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/roles.py</code> <pre><code>async def put_properties(\n    self,\n    role: str,\n    body: str | dict,\n    *,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Update the properties for the specified role.\n\n    Documentation: https://docs.marklogic.com/REST/PUT/manage/v2/roles/[id-or-name]/properties\n\n    Parameters\n    ----------\n    role : str\n        A role identifier. The role can be identified either by ID or name.\n    body : str | dict\n        A role properties in XML or JSON format.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = RolePropertiesPutCall(role=role, body=body)\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncServersApi/","title":"AsyncServersApi","text":"<pre><code>from mlclient.api import AsyncServersApi\n</code></pre>"},{"location":"reference/mlclient/api/AsyncServersApi/#mlclient.api.AsyncServersApi","title":"mlclient.api.AsyncServersApi","text":"<pre><code>AsyncServersApi(api: AsyncApiClient)\n</code></pre> <p>Async mid-level API for <code>/manage/v2/servers</code> endpoints.</p> Source code in <code>mlclient/api/servers.py</code> <pre><code>def __init__(self, api: AsyncApiClient):\n    self._api = api\n</code></pre>"},{"location":"reference/mlclient/api/AsyncServersApi/#mlclient.api.AsyncServersApi.create","title":"create  <code>async</code>","text":"<pre><code>create(\n    body: str | dict,\n    *,\n    group_id: str | None = None,\n    server_type: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Create a new App Server in the specified group.</p> <p>Documentation: https://docs.marklogic.com/REST/POST/manage/v2/servers</p> <p>Parameters:</p> Name Type Description Default <code>body</code> <code>str | dict</code> <p>A server properties in XML or JSON format.</p> required <code>group_id</code> <code>str</code> <p>The id or name of the group to which the App Server belongs. The group must be specified by this parameter or by the group-name property in the request payload. If it is specified in both places, the values must be the same.</p> <code>None</code> <code>server_type</code> <code>str</code> <p>The type of App Server to create. The App Server type must be specified by this parameter or in the request payload. If it is specified in both places, the values must be the same. The valid types are: http, odbc, xdbc, or webdav.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/servers.py</code> <pre><code>async def create(\n    self,\n    body: str | dict,\n    *,\n    group_id: str | None = None,\n    server_type: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Create a new App Server in the specified group.\n\n    Documentation: https://docs.marklogic.com/REST/POST/manage/v2/servers\n\n    Parameters\n    ----------\n    body : str | dict\n        A server properties in XML or JSON format.\n    group_id : str\n        The id or name of the group to which the App Server belongs.\n        The group must be specified by this parameter or by the group-name property\n        in the request payload. If it is specified in both places, the values\n        must be the same.\n    server_type : str\n        The type of App Server to create.\n        The App Server type must be specified by this parameter or in the request\n        payload. If it is specified in both places, the values must be the same.\n        The valid types are: http, odbc, xdbc, or webdav.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = ServersPostCall(body=body, group_id=group_id, server_type=server_type)\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncServersApi/#mlclient.api.AsyncServersApi.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(\n    server: str, group_id: str, *, timeout=UNSET\n) -&gt; Response\n</code></pre> <p>Delete the specified App Server from the specified group.</p> <p>Documentation: https://docs.marklogic.com/REST/DELETE/manage/v2/servers/[id-or-name]</p> <p>Parameters:</p> Name Type Description Default <code>server</code> <code>str</code> <p>A server identifier. The server can be identified either by ID or name.</p> required <code>group_id</code> <code>str</code> <p>The id or name of the group to which the App Server belongs. This parameter is required.</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/servers.py</code> <pre><code>async def delete(\n    self,\n    server: str,\n    group_id: str,\n    *,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Delete the specified App Server from the specified group.\n\n    Documentation: https://docs.marklogic.com/REST/DELETE/manage/v2/servers/[id-or-name]\n\n    Parameters\n    ----------\n    server : str\n        A server identifier. The server can be identified either by ID or name.\n    group_id : str\n        The id or name of the group to which the App Server belongs.\n        This parameter is required.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = ServerDeleteCall(server=server, group_id=group_id)\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncServersApi/#mlclient.api.AsyncServersApi.get","title":"get  <code>async</code>","text":"<pre><code>get(\n    server: str,\n    group_id: str,\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    host_id: str | None = None,\n    full_refs: bool | None = None,\n    modules: bool | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve data about a specific App Server.</p> <p>The server can be identified either by ID or name. The data returned depends on the value of the view request parameter. The default view is a summary with links to additional data.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/servers/[id-or-name]</p> <p>Parameters:</p> Name Type Description Default <code>server</code> <code>str</code> <p>A server identifier. The server can be identified either by ID or name.</p> required <code>group_id</code> <code>str</code> <p>The id or name of the group to which the App Server belongs. This parameter is required.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default).</p> <code>None</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be properties-schema, config, edit, package, describe, status, xdmp:server-status or default.</p> <code>None</code> <code>host_id</code> <code>str</code> <p>Meaningful only when view=status. Specifies to return the status for the server in the specified host. The host can be identified either by id or name.</p> <code>None</code> <code>full_refs</code> <code>bool</code> <p>If set to true, full detail is returned for all relationship references. A value of false (the default) indicates to return detail only for first references. This parameter is not meaningful with view=package.</p> <code>None</code> <code>modules</code> <code>bool</code> <p>Meaningful only with view=package. Whether to include a manifest of the modules database for the App Server in the results, if one exists. It is an error to request a modules database manifest for an App Server that uses the filesystem for modules. Default: false.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the server details</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/servers.py</code> <pre><code>async def get(\n    self,\n    server: str,\n    group_id: str,\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    host_id: str | None = None,\n    full_refs: bool | None = None,\n    modules: bool | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve data about a specific App Server.\n\n    The server can be identified either by ID or name. The data returned\n    depends on the value of the view request parameter. The default view\n    is a summary with links to additional data.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/servers/[id-or-name]\n\n    Parameters\n    ----------\n    server : str\n        A server identifier. The server can be identified either by ID or name.\n    group_id : str\n        The id or name of the group to which the App Server belongs.\n        This parameter is required.\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n    view : str\n        A specific view of the returned data.\n        Can be properties-schema, config, edit, package, describe, status,\n        xdmp:server-status or default.\n    host_id : str\n        Meaningful only when view=status. Specifies to return the status for\n        the server in the specified host. The host can be identified either by id\n        or name.\n    full_refs : bool\n        If set to true, full detail is returned for all relationship references.\n        A value of false (the default) indicates to return detail only for\n        first references. This parameter is not meaningful with view=package.\n    modules : bool\n        Meaningful only with view=package. Whether to include a manifest\n        of the modules database for the App Server in the results, if one exists.\n        It is an error to request a modules database manifest for an App Server\n        that uses the filesystem for modules. Default: false.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the server details\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = ServerGetCall(\n        server=server,\n        group_id=group_id,\n        data_format=data_format,\n        view=view,\n        host_id=host_id,\n        full_refs=full_refs,\n        modules=modules,\n    )\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncServersApi/#mlclient.api.AsyncServersApi.get_list","title":"get_list  <code>async</code>","text":"<pre><code>get_list(\n    *,\n    data_format: str | None = None,\n    group_id: str | None = None,\n    view: str | None = None,\n    full_refs: bool | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve data about the App Servers in the cluster.</p> <p>The data returned depends on the setting of the view request parameter. The default view provides a summary of the servers.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/servers</p> <p>Parameters:</p> Name Type Description Default <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default).</p> <code>None</code> <code>group_id</code> <code>str</code> <p>Specifies to return only the servers in the specified group. The group can be identified either by id or name. If not specified, the response includes information about all App Servers.</p> <code>None</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be schema, properties-schema, metrics, package, describe, or default.</p> <code>None</code> <code>full_refs</code> <code>bool</code> <p>If set to true, full detail is returned for all relationship references. A value of false (the default) indicates to return detail only for first references. This parameter is not meaningful with view=package.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the servers summary</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/servers.py</code> <pre><code>async def get_list(\n    self,\n    *,\n    data_format: str | None = None,\n    group_id: str | None = None,\n    view: str | None = None,\n    full_refs: bool | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve data about the App Servers in the cluster.\n\n    The data returned depends on the setting of the view request parameter.\n    The default view provides a summary of the servers.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/servers\n\n    Parameters\n    ----------\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n    group_id : str\n        Specifies to return only the servers in the specified group.\n        The group can be identified either by id or name.\n        If not specified, the response includes information about all App Servers.\n    view : str\n        A specific view of the returned data.\n        Can be schema, properties-schema, metrics, package, describe, or default.\n    full_refs : bool\n        If set to true, full detail is returned for all relationship references.\n        A value of false (the default) indicates to return detail only for first\n        references. This parameter is not meaningful with view=package.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the servers summary\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = ServersGetCall(\n        data_format=data_format,\n        group_id=group_id,\n        view=view,\n        full_refs=full_refs,\n    )\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncServersApi/#mlclient.api.AsyncServersApi.get_properties","title":"get_properties  <code>async</code>","text":"<pre><code>get_properties(\n    server: str,\n    group_id: str,\n    *,\n    data_format: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve the modifiable properties of the specified App Server.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/servers/[id-or-name]/properties</p> <p>Parameters:</p> Name Type Description Default <code>server</code> <code>str</code> <p>A server identifier. The server can be identified either by ID or name.</p> required <code>group_id</code> <code>str</code> <p>The id or name of the group to which the App Server belongs. This parameter is required.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either json or xml (default). This parameter overrides the Accept header if both are present.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the server properties</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/servers.py</code> <pre><code>async def get_properties(\n    self,\n    server: str,\n    group_id: str,\n    *,\n    data_format: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve the modifiable properties of the specified App Server.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/servers/[id-or-name]/properties\n\n    Parameters\n    ----------\n    server : str\n        A server identifier. The server can be identified either by ID or name.\n    group_id : str\n        The id or name of the group to which the App Server belongs.\n        This parameter is required.\n    data_format : str\n        The format of the returned data. Can be either json or xml (default).\n        This parameter overrides the Accept header if both are present.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the server properties\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = ServerPropertiesGetCall(\n        server=server,\n        group_id=group_id,\n        data_format=data_format,\n    )\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncServersApi/#mlclient.api.AsyncServersApi.put_properties","title":"put_properties  <code>async</code>","text":"<pre><code>put_properties(\n    server: str,\n    group_id: str,\n    body: str | dict,\n    *,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Modify the properties of the specified App Server.</p> <p>Documentation: https://docs.marklogic.com/REST/PUT/manage/v2/servers/[id-or-name]/properties</p> <p>Parameters:</p> Name Type Description Default <code>server</code> <code>str</code> <p>A server identifier. The server can be identified either by ID or name.</p> required <code>group_id</code> <code>str</code> <p>The id or name of the group to which the App Server belongs. This parameter is required.</p> required <code>body</code> <code>str | dict</code> <p>A server properties in XML or JSON format.</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/servers.py</code> <pre><code>async def put_properties(\n    self,\n    server: str,\n    group_id: str,\n    body: str | dict,\n    *,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Modify the properties of the specified App Server.\n\n    Documentation: https://docs.marklogic.com/REST/PUT/manage/v2/servers/[id-or-name]/properties\n\n    Parameters\n    ----------\n    server : str\n        A server identifier. The server can be identified either by ID or name.\n    group_id : str\n        The id or name of the group to which the App Server belongs.\n        This parameter is required.\n    body : str | dict\n        A server properties in XML or JSON format.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = ServerPropertiesPutCall(server=server, group_id=group_id, body=body)\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncTransactionsApi/","title":"AsyncTransactionsApi","text":"<pre><code>from mlclient.api import AsyncTransactionsApi\n</code></pre>"},{"location":"reference/mlclient/api/AsyncTransactionsApi/#mlclient.api.AsyncTransactionsApi","title":"mlclient.api.AsyncTransactionsApi","text":"<pre><code>AsyncTransactionsApi(api: AsyncApiClient)\n</code></pre> <p>Async mid-level API for <code>/v1/transactions</code> endpoints.</p> Source code in <code>mlclient/api/transactions.py</code> <pre><code>def __init__(self, api: AsyncApiClient):\n    self._api = api\n</code></pre>"},{"location":"reference/mlclient/api/AsyncTransactionsApi/#mlclient.api.AsyncTransactionsApi.create","title":"create  <code>async</code>","text":"<pre><code>create(\n    *,\n    name: str | None = None,\n    time_limit: int | None = None,\n    database: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Create a multi-statement transaction.</p> <p>The response is a 303 redirect whose <code>Location</code> header carries the new transaction id (<code>/v1/transactions/{txid}</code>); the redirect is not followed.</p> <p>Documentation: https://docs.marklogic.com/REST/POST/v1/transactions</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>A name to assign to the transaction.</p> <code>None</code> <code>time_limit</code> <code>int</code> <p>The maximum number of seconds for the transaction to remain open.</p> <code>None</code> <code>database</code> <code>str</code> <p>Evaluate against the named content database instead of the default content database associated with the REST API instance.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with a <code>Location</code> header carrying the transaction id</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/transactions.py</code> <pre><code>async def create(\n    self,\n    *,\n    name: str | None = None,\n    time_limit: int | None = None,\n    database: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Create a multi-statement transaction.\n\n    The response is a 303 redirect whose ``Location`` header carries the new\n    transaction id (``/v1/transactions/{txid}``); the redirect is not followed.\n\n    Documentation: https://docs.marklogic.com/REST/POST/v1/transactions\n\n    Parameters\n    ----------\n    name : str\n        A name to assign to the transaction.\n    time_limit : int\n        The maximum number of seconds for the transaction to remain open.\n    database : str\n        Evaluate against the named content database instead of the default\n        content database associated with the REST API instance.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with a ``Location`` header carrying the transaction id\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = TransactionsPostCall(\n        name=name,\n        time_limit=time_limit,\n        database=database,\n    )\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncTransactionsApi/#mlclient.api.AsyncTransactionsApi.get","title":"get  <code>async</code>","text":"<pre><code>get(\n    txid: str,\n    *,\n    data_format: str | None = None,\n    database: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve the status of the specified transaction.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/v1/transactions/[txid]</p> <p>Parameters:</p> Name Type Description Default <code>txid</code> <code>str</code> <p>A transaction identifier.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either json or xml (default).</p> <code>None</code> <code>database</code> <code>str</code> <p>Evaluate against the named content database instead of the default content database associated with the REST API instance.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the transaction status</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/transactions.py</code> <pre><code>async def get(\n    self,\n    txid: str,\n    *,\n    data_format: str | None = None,\n    database: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve the status of the specified transaction.\n\n    Documentation: https://docs.marklogic.com/REST/GET/v1/transactions/[txid]\n\n    Parameters\n    ----------\n    txid : str\n        A transaction identifier.\n    data_format : str\n        The format of the returned data. Can be either json or xml (default).\n    database : str\n        Evaluate against the named content database instead of the default\n        content database associated with the REST API instance.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the transaction status\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = TransactionGetCall(\n        txid=txid,\n        data_format=data_format,\n        database=database,\n    )\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncTransactionsApi/#mlclient.api.AsyncTransactionsApi.post","title":"post  <code>async</code>","text":"<pre><code>post(\n    txid: str,\n    *,\n    result: str,\n    database: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Commit or roll back the specified transaction.</p> <p>Documentation: https://docs.marklogic.com/REST/POST/v1/transactions/[txid]</p> <p>Parameters:</p> Name Type Description Default <code>txid</code> <code>str</code> <p>A transaction identifier.</p> required <code>result</code> <code>str</code> <p>The disposition of the transaction. Can be either commit or rollback.</p> required <code>database</code> <code>str</code> <p>Evaluate against the named content database instead of the default content database associated with the REST API instance.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/transactions.py</code> <pre><code>async def post(\n    self,\n    txid: str,\n    *,\n    result: str,\n    database: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Commit or roll back the specified transaction.\n\n    Documentation: https://docs.marklogic.com/REST/POST/v1/transactions/[txid]\n\n    Parameters\n    ----------\n    txid : str\n        A transaction identifier.\n    result : str\n        The disposition of the transaction. Can be either commit or rollback.\n    database : str\n        Evaluate against the named content database instead of the default\n        content database associated with the REST API instance.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = TransactionPostCall(\n        txid=txid,\n        result=result,\n        database=database,\n    )\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncUsersApi/","title":"AsyncUsersApi","text":"<pre><code>from mlclient.api import AsyncUsersApi\n</code></pre>"},{"location":"reference/mlclient/api/AsyncUsersApi/#mlclient.api.AsyncUsersApi","title":"mlclient.api.AsyncUsersApi","text":"<pre><code>AsyncUsersApi(api: AsyncApiClient)\n</code></pre> <p>Async mid-level API for <code>/manage/v2/users</code> endpoints.</p> Source code in <code>mlclient/api/users.py</code> <pre><code>def __init__(self, api: AsyncApiClient):\n    self._api = api\n</code></pre>"},{"location":"reference/mlclient/api/AsyncUsersApi/#mlclient.api.AsyncUsersApi.create","title":"create  <code>async</code>","text":"<pre><code>create(body: str | dict, *, timeout=UNSET) -&gt; Response\n</code></pre> <p>Create a new user in the security database.</p> <p>Documentation: https://docs.marklogic.com/REST/POST/manage/v2/users</p> <p>Parameters:</p> Name Type Description Default <code>body</code> <code>str | dict</code> <p>A user properties in XML or JSON format.</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/users.py</code> <pre><code>async def create(\n    self,\n    body: str | dict,\n    *,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Create a new user in the security database.\n\n    Documentation: https://docs.marklogic.com/REST/POST/manage/v2/users\n\n    Parameters\n    ----------\n    body : str | dict\n        A user properties in XML or JSON format.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = UsersPostCall(body=body)\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncUsersApi/#mlclient.api.AsyncUsersApi.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(user: str, *, timeout=UNSET) -&gt; Response\n</code></pre> <p>Delete the specified user from the security database.</p> <p>Documentation: https://docs.marklogic.com/REST/DELETE/manage/v2/users/[id-or-name]</p> <p>Parameters:</p> Name Type Description Default <code>user</code> <code>str</code> <p>A user identifier. The user can be identified either by ID or name.</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/users.py</code> <pre><code>async def delete(\n    self,\n    user: str,\n    *,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Delete the specified user from the security database.\n\n    Documentation: https://docs.marklogic.com/REST/DELETE/manage/v2/users/[id-or-name]\n\n    Parameters\n    ----------\n    user : str\n        A user identifier. The user can be identified either by ID or name.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = UserDeleteCall(user=user)\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncUsersApi/#mlclient.api.AsyncUsersApi.get","title":"get  <code>async</code>","text":"<pre><code>get(\n    user: str,\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve the configuration for the specified user.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/users/[id-or-name]</p> <p>Parameters:</p> Name Type Description Default <code>user</code> <code>str</code> <p>A user identifier. The user can be identified either by ID or name.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default). This parameter is not meaningful with view=edit.</p> <code>None</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be: describe, or default.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the user details</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/users.py</code> <pre><code>async def get(\n    self,\n    user: str,\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve the configuration for the specified user.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/users/[id-or-name]\n\n    Parameters\n    ----------\n    user : str\n        A user identifier. The user can be identified either by ID or name.\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n        This parameter is not meaningful with view=edit.\n    view : str\n        A specific view of the returned data. Can be: describe, or default.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the user details\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = UserGetCall(user=user, data_format=data_format, view=view)\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncUsersApi/#mlclient.api.AsyncUsersApi.get_list","title":"get_list  <code>async</code>","text":"<pre><code>get_list(\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve a summary of the users in the cluster.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/users</p> <p>Parameters:</p> Name Type Description Default <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default).</p> <code>None</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be: describe, or default.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the users summary</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/users.py</code> <pre><code>async def get_list(\n    self,\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve a summary of the users in the cluster.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/users\n\n    Parameters\n    ----------\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n    view : str\n        A specific view of the returned data. Can be: describe, or default.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the users summary\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = UsersGetCall(data_format=data_format, view=view)\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncUsersApi/#mlclient.api.AsyncUsersApi.get_properties","title":"get_properties  <code>async</code>","text":"<pre><code>get_properties(\n    user: str,\n    *,\n    data_format: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve the properties of the specified user.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/users/[id-or-name]/properties</p> <p>Parameters:</p> Name Type Description Default <code>user</code> <code>str</code> <p>A user identifier. The user can be identified either by ID or name.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either json or xml (default). This parameter overrides the Accept header if both are present.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the user properties</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/users.py</code> <pre><code>async def get_properties(\n    self,\n    user: str,\n    *,\n    data_format: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve the properties of the specified user.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/users/[id-or-name]/properties\n\n    Parameters\n    ----------\n    user : str\n        A user identifier. The user can be identified either by ID or name.\n    data_format : str\n        The format of the returned data. Can be either json or xml (default).\n        This parameter overrides the Accept header if both are present.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the user properties\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = UserPropertiesGetCall(user=user, data_format=data_format)\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/AsyncUsersApi/#mlclient.api.AsyncUsersApi.put_properties","title":"put_properties  <code>async</code>","text":"<pre><code>put_properties(\n    user: str, body: str | dict, *, timeout=UNSET\n) -&gt; Response\n</code></pre> <p>Update the properties for the specified user.</p> <p>Documentation: https://docs.marklogic.com/REST/PUT/manage/v2/users/[id-or-name]/properties</p> <p>Parameters:</p> Name Type Description Default <code>user</code> <code>str</code> <p>A user identifier. The user can be identified either by ID or name.</p> required <code>body</code> <code>str | dict</code> <p>A user properties in XML or JSON format.</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/users.py</code> <pre><code>async def put_properties(\n    self,\n    user: str,\n    body: str | dict,\n    *,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Update the properties for the specified user.\n\n    Documentation: https://docs.marklogic.com/REST/PUT/manage/v2/users/[id-or-name]/properties\n\n    Parameters\n    ----------\n    user : str\n        A user identifier. The user can be identified either by ID or name.\n    body : str | dict\n        A user properties in XML or JSON format.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = UserPropertiesPutCall(user=user, body=body)\n    return await self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/DatabasesApi/","title":"DatabasesApi","text":"<pre><code>from mlclient.api import DatabasesApi\n</code></pre>"},{"location":"reference/mlclient/api/DatabasesApi/#mlclient.api.DatabasesApi","title":"mlclient.api.DatabasesApi","text":"<pre><code>DatabasesApi(api: ApiClient)\n</code></pre> <p>Mid-level API for <code>/manage/v2/databases</code> endpoints.</p> <p>Create, read, update, and delete databases in the cluster.</p> <p>Requires the Manage server (port 8002 by default).</p> Source code in <code>mlclient/api/databases.py</code> <pre><code>def __init__(self, api: ApiClient):\n    self._api = api\n</code></pre>"},{"location":"reference/mlclient/api/DatabasesApi/#mlclient.api.DatabasesApi.create","title":"create","text":"<pre><code>create(body: str | dict, *, timeout=UNSET) -&gt; Response\n</code></pre> <p>Create a new database in the cluster.</p> <p>Documentation: https://docs.marklogic.com/REST/POST/manage/v2/databases</p> <p>Parameters:</p> Name Type Description Default <code>body</code> <code>str | dict</code> <p>A database properties in XML or JSON format.</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/databases.py</code> <pre><code>def create(\n    self,\n    body: str | dict,\n    *,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Create a new database in the cluster.\n\n    Documentation: https://docs.marklogic.com/REST/POST/manage/v2/databases\n\n    Parameters\n    ----------\n    body : str | dict\n        A database properties in XML or JSON format.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = DatabasesPostCall(body=body)\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/DatabasesApi/#mlclient.api.DatabasesApi.delete","title":"delete","text":"<pre><code>delete(\n    database: str,\n    *,\n    forest_delete: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Delete the specified database from the cluster.</p> <p>Documentation: https://docs.marklogic.com/REST/DELETE/manage/v2/databases/[id-or-name]</p> <p>Parameters:</p> Name Type Description Default <code>database</code> <code>str</code> <p>A database identifier. The database can be identified either by ID or name.</p> required <code>forest_delete</code> <code>str</code> <p>Specifies to delete the forests attached to the database. If unspecified, the forests will not be affected. If \"configuration\" is specified, the forest configuration will be removed but public forest data will remain. If \"data\" is specified, the forest configuration and data will be removed.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/databases.py</code> <pre><code>def delete(\n    self,\n    database: str,\n    *,\n    forest_delete: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Delete the specified database from the cluster.\n\n    Documentation: https://docs.marklogic.com/REST/DELETE/manage/v2/databases/[id-or-name]\n\n    Parameters\n    ----------\n    database : str\n        A database identifier. The database can be identified either by ID or name.\n    forest_delete : str\n        Specifies to delete the forests attached to the database.\n        If unspecified, the forests will not be affected.\n        If \"configuration\" is specified, the forest configuration will be removed\n        but public forest data will remain.\n        If \"data\" is specified, the forest configuration and data will be removed.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = DatabaseDeleteCall(database=database, forest_delete=forest_delete)\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/DatabasesApi/#mlclient.api.DatabasesApi.get","title":"get","text":"<pre><code>get(\n    database: str,\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve information on the specified database.</p> <p>The database can be identified either by ID or name.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/databases/[id-or-name]</p> <p>Parameters:</p> Name Type Description Default <code>database</code> <code>str</code> <p>A database identifier. The database can be identified either by ID or name.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default). This parameter is not meaningful with view=edit.</p> <code>None</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be: properties-schema, package, describe, config, counts, edit, status, forest-storage, or default.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the database details</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/databases.py</code> <pre><code>def get(\n    self,\n    database: str,\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve information on the specified database.\n\n    The database can be identified either by ID or name.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/databases/[id-or-name]\n\n    Parameters\n    ----------\n    database : str\n        A database identifier. The database can be identified either by ID or name.\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n        This parameter is not meaningful with view=edit.\n    view : str\n        A specific view of the returned data.\n        Can be: properties-schema, package, describe, config, counts, edit, status,\n        forest-storage, or default.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the database details\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = DatabaseGetCall(database=database, data_format=data_format, view=view)\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/DatabasesApi/#mlclient.api.DatabasesApi.get_list","title":"get_list","text":"<pre><code>get_list(\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve a summary of the databases in the cluster.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/databases</p> <p>Parameters:</p> Name Type Description Default <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default).</p> <code>None</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be schema, properties-schema, metrics, package, describe, or default.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the databases summary</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/databases.py</code> <pre><code>def get_list(\n    self,\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve a summary of the databases in the cluster.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/databases\n\n    Parameters\n    ----------\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n    view : str\n        A specific view of the returned data.\n        Can be schema, properties-schema, metrics, package, describe, or default.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the databases summary\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = DatabasesGetCall(data_format=data_format, view=view)\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/DatabasesApi/#mlclient.api.DatabasesApi.get_properties","title":"get_properties","text":"<pre><code>get_properties(\n    database: str,\n    *,\n    data_format: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve the modifiable properties of the specified database.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/databases/[id-or-name]/properties</p> <p>Parameters:</p> Name Type Description Default <code>database</code> <code>str</code> <p>A database identifier. The database can be identified either by ID or name.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either json or xml (default). This parameter overrides the Accept header if both are present.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the database properties</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/databases.py</code> <pre><code>def get_properties(\n    self,\n    database: str,\n    *,\n    data_format: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve the modifiable properties of the specified database.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/databases/[id-or-name]/properties\n\n    Parameters\n    ----------\n    database : str\n        A database identifier. The database can be identified either by ID or name.\n    data_format : str\n        The format of the returned data. Can be either json or xml (default).\n        This parameter overrides the Accept header if both are present.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the database properties\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = DatabasePropertiesGetCall(database=database, data_format=data_format)\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/DatabasesApi/#mlclient.api.DatabasesApi.post","title":"post","text":"<pre><code>post(\n    database: str, body: str | dict, *, timeout=UNSET\n) -&gt; Response\n</code></pre> <p>Clear or configure the specified database.</p> <p>Can be used to clear the contents of the named database and to perform various configuration operations on the database.</p> <p>Documentation: https://docs.marklogic.com/REST/POST/manage/v2/databases/[id-or-name]</p> <p>Parameters:</p> Name Type Description Default <code>database</code> <code>str</code> <p>A database identifier. The database can be identified either by ID or name.</p> required <code>body</code> <code>str | dict</code> <p>A database properties in XML or JSON format.</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/databases.py</code> <pre><code>def post(\n    self,\n    database: str,\n    body: str | dict,\n    *,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Clear or configure the specified database.\n\n    Can be used to clear the contents of the named database and to perform\n    various configuration operations on the database.\n\n    Documentation: https://docs.marklogic.com/REST/POST/manage/v2/databases/[id-or-name]\n\n    Parameters\n    ----------\n    database : str\n        A database identifier. The database can be identified either by ID or name.\n    body : str | dict\n        A database properties in XML or JSON format.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = DatabasePostCall(database=database, body=body)\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/DatabasesApi/#mlclient.api.DatabasesApi.put_properties","title":"put_properties","text":"<pre><code>put_properties(\n    database: str, body: str | dict, *, timeout=UNSET\n) -&gt; Response\n</code></pre> <p>Modify the properties of the specified database.</p> <p>Documentation: https://docs.marklogic.com/REST/PUT/manage/v2/databases/[id-or-name]/properties</p> <p>Parameters:</p> Name Type Description Default <code>database</code> <code>str</code> <p>A database identifier. The database can be identified either by ID or name.</p> required <code>body</code> <code>str | dict</code> <p>A database properties in XML or JSON format.</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/databases.py</code> <pre><code>def put_properties(\n    self,\n    database: str,\n    body: str | dict,\n    *,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Modify the properties of the specified database.\n\n    Documentation: https://docs.marklogic.com/REST/PUT/manage/v2/databases/[id-or-name]/properties\n\n    Parameters\n    ----------\n    database : str\n        A database identifier. The database can be identified either by ID or name.\n    body : str | dict\n        A database properties in XML or JSON format.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = DatabasePropertiesPutCall(database=database, body=body)\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/DocumentsApi/","title":"DocumentsApi","text":"<pre><code>from mlclient.api import DocumentsApi\n</code></pre>"},{"location":"reference/mlclient/api/DocumentsApi/#mlclient.api.DocumentsApi","title":"mlclient.api.DocumentsApi","text":"<pre><code>DocumentsApi(api: ApiClient)\n</code></pre> <p>Mid-level API for <code>/v1/documents</code> endpoints.</p> <p>Create, read, update, and delete document content and metadata.</p> Source code in <code>mlclient/api/documents.py</code> <pre><code>def __init__(self, api: ApiClient):\n    self._api = api\n</code></pre>"},{"location":"reference/mlclient/api/DocumentsApi/#mlclient.api.DocumentsApi.delete","title":"delete","text":"<pre><code>delete(\n    uri: str | list,\n    *,\n    database: str | None = None,\n    category: str | list | None = None,\n    txid: str | None = None,\n    temporal_collection: str | None = None,\n    system_time: str | None = None,\n    wipe_temporal: bool | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Remove documents, or reset document metadata.</p> <p>Documentation: https://docs.marklogic.com/REST/DELETE/v1/documents</p> <p>Parameters:</p> Name Type Description Default <code>uri</code> <code>str | list</code> <p>The URI of a document to delete or for which to remove metadata. You can specify multiple documents.</p> required <code>database</code> <code>str</code> <p>Perform this operation on the named content database instead of the default content database associated with the REST API instance. Using an alternative database requires the \"eval-in\" privilege.</p> <code>None</code> <code>category</code> <code>str | list</code> <p>The category of data to remove/reset. Category may be specified multiple times to remove or reset any combination of content and metadata. Valid categories: content (default), metadata, metadata-values, collections, permissions, properties, and quality. Use metadata to reset all metadata.</p> <code>None</code> <code>txid</code> <code>str</code> <p>The transaction identifier of the multi-statement transaction in which to service this request. Use the /transactions service to create and manage multi-statement transactions.</p> <code>None</code> <code>temporal_collection</code> <code>str</code> <p>Specify the name of a temporal collection that contains the document(s) to be deleted. Applies to all documents when deleting more than one.</p> <code>None</code> <code>system_time</code> <code>str</code> <p>Set the system start time for the insertion or update. This time will override the system time set by MarkLogic. Ignored if temporal-collection is not included in the request. Applies to all documents when deleting more than one.</p> <code>None</code> <code>wipe_temporal</code> <code>bool</code> <p>Remove all versions of a temporal document rather than performing a temporal delete. You can only use this parameter when you also specify a temporal-collection parameter.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/documents.py</code> <pre><code>def delete(\n    self,\n    uri: str | list,\n    *,\n    database: str | None = None,\n    category: str | list | None = None,\n    txid: str | None = None,\n    temporal_collection: str | None = None,\n    system_time: str | None = None,\n    wipe_temporal: bool | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Remove documents, or reset document metadata.\n\n    Documentation: https://docs.marklogic.com/REST/DELETE/v1/documents\n\n    Parameters\n    ----------\n    uri : str | list\n        The URI of a document to delete or for which to remove metadata.\n        You can specify multiple documents.\n    database : str\n        Perform this operation on the named content database instead\n        of the default content database associated with the REST API instance.\n        Using an alternative database requires the \"eval-in\" privilege.\n    category : str | list\n        The category of data to remove/reset.\n        Category may be specified multiple times to remove or reset\n        any combination of content and metadata.\n        Valid categories: content (default), metadata, metadata-values,\n        collections, permissions, properties, and quality.\n        Use metadata to reset all metadata.\n    txid : str\n        The transaction identifier of the multi-statement transaction in which\n        to service this request. Use the /transactions service to create and manage\n        multi-statement transactions.\n    temporal_collection : str\n        Specify the name of a temporal collection that contains the document(s)\n        to be deleted. Applies to all documents when deleting more than one.\n    system_time : str\n        Set the system start time for the insertion or update.\n        This time will override the system time set by MarkLogic.\n        Ignored if temporal-collection is not included in the request.\n        Applies to all documents when deleting more than one.\n    wipe_temporal : bool\n        Remove all versions of a temporal document rather than performing\n        a temporal delete. You can only use this parameter when you also specify\n        a temporal-collection parameter.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = DocumentsDeleteCall(\n        uri=uri,\n        database=database,\n        category=category,\n        txid=txid,\n        temporal_collection=temporal_collection,\n        system_time=system_time,\n        wipe_temporal=wipe_temporal,\n    )\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/DocumentsApi/#mlclient.api.DocumentsApi.get","title":"get","text":"<pre><code>get(\n    uri: str | list,\n    *,\n    database: str | None = None,\n    category: str | list | None = None,\n    data_format: str | None = None,\n    timestamp: str | None = None,\n    transform: str | None = None,\n    transform_params: dict | None = None,\n    txid: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve document content and/or metadata from the database.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/v1/documents</p> <p>Parameters:</p> Name Type Description Default <code>uri</code> <code>str | list</code> <p>One or more URIs for documents in the database. If you specify multiple URIs, the Accept header must be multipart/mixed.</p> required <code>database</code> <code>str</code> <p>Perform this operation on the named content database instead of the default content database associated with the REST API instance. Using an alternative database requires the \"eval-in\" privilege.</p> <code>None</code> <code>category</code> <code>str | list</code> <p>The category of data to fetch about the requested document. Category can be specified multiple times to retrieve any combination of content and metadata. Valid categories: content (default), metadata, metadata-values, collections, permissions, properties, and quality. Use metadata to request all categories except content.</p> <code>None</code> <code>data_format</code> <code>str</code> <p>The expected format of metadata returned in the response. Accepted values: xml or json. This parameter does not affect document content. For metadata, this parameter overrides the MIME type in the Accept header, except when the Accept header is multipart/mixed.</p> <code>None</code> <code>timestamp</code> <code>str</code> <p>A timestamp returned in the ML-Effective-Timestamp header of a previous request. Use this parameter to fetch documents based on the contents of the database at a fixed point-in-time.</p> <code>None</code> <code>transform</code> <code>str</code> <p>Names a content transformation previously installed via the /config/transforms service. The service applies the transformation to all documents prior to constructing the response.</p> <code>None</code> <code>transform_params</code> <code>dict</code> <p>A transform parameter names and values. For example, { \"myparam\": 1 }. Transform parameters are passed to the transform named in the transform parameter.</p> <code>None</code> <code>txid</code> <code>str</code> <p>The transaction identifier of the multi-statement transaction in which to service this request. Use the /transactions service to create and manage multi-statement transactions.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with document content and/or metadata</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/documents.py</code> <pre><code>def get(\n    self,\n    uri: str | list,\n    *,\n    database: str | None = None,\n    category: str | list | None = None,\n    data_format: str | None = None,\n    timestamp: str | None = None,\n    transform: str | None = None,\n    transform_params: dict | None = None,\n    txid: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve document content and/or metadata from the database.\n\n    Documentation: https://docs.marklogic.com/REST/GET/v1/documents\n\n    Parameters\n    ----------\n    uri : str | list\n        One or more URIs for documents in the database.\n        If you specify multiple URIs, the Accept header must be multipart/mixed.\n    database : str\n        Perform this operation on the named content database instead\n        of the default content database associated with the REST API instance.\n        Using an alternative database requires the \"eval-in\" privilege.\n    category : str | list\n        The category of data to fetch about the requested document.\n        Category can be specified multiple times to retrieve any combination\n        of content and metadata. Valid categories: content (default), metadata,\n        metadata-values, collections, permissions, properties, and quality.\n        Use metadata to request all categories except content.\n    data_format : str\n        The expected format of metadata returned in the response.\n        Accepted values: xml or json.\n        This parameter does not affect document content.\n        For metadata, this parameter overrides the MIME type in the Accept header,\n        except when the Accept header is multipart/mixed.\n    timestamp : str\n        A timestamp returned in the ML-Effective-Timestamp header of a previous\n        request. Use this parameter to fetch documents based on the contents\n        of the database at a fixed point-in-time.\n    transform : str\n        Names a content transformation previously installed via\n        the /config/transforms service. The service applies the transformation\n        to all documents prior to constructing the response.\n    transform_params : dict\n        A transform parameter names and values. For example, { \"myparam\": 1 }.\n        Transform parameters are passed to the transform named in the transform\n        parameter.\n    txid : str\n        The transaction identifier of the multi-statement transaction in which\n        to service this request. Use the /transactions service to create and manage\n        multi-statement transactions.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with document content and/or metadata\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = DocumentsGetCall(\n        uri=uri,\n        database=database,\n        category=category,\n        data_format=data_format,\n        timestamp=timestamp,\n        transform=transform,\n        transform_params=transform_params,\n        txid=txid,\n    )\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/DocumentsApi/#mlclient.api.DocumentsApi.post","title":"post","text":"<pre><code>post(\n    body_parts: list[DocumentsBodyPart],\n    *,\n    database: str | None = None,\n    transform: str | None = None,\n    transform_params: dict | None = None,\n    txid: str | None = None,\n    temporal_collection: str | None = None,\n    system_time: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Insert or update content and/or metadata for multiple documents.</p> <p>Documentation: https://docs.marklogic.com/REST/POST/v1/documents</p> <p>Parameters:</p> Name Type Description Default <code>body_parts</code> <code>list[DocumentsBodyPart]</code> <p>A list of multipart request body parts</p> required <code>database</code> <code>str</code> <p>Perform this operation on the named content database instead of the default content database associated with the REST API instance. Using an alternative database requires the \"eval-in\" privilege.</p> <code>None</code> <code>transform</code> <code>str</code> <p>Names a content transformation previously installed via the /config/transforms service. The service applies the transformation to all documents prior to constructing the response.</p> <code>None</code> <code>transform_params</code> <code>dict</code> <p>A transform parameter names and values. For example, { \"myparam\": 1 }. Transform parameters are passed to the transform named in the transform parameter.</p> <code>None</code> <code>txid</code> <code>str</code> <p>The transaction identifier of the multi-statement transaction in which to service this request. Use the /transactions service to create and manage multi-statement transactions.</p> <code>None</code> <code>temporal_collection</code> <code>str</code> <p>Specify the name of a temporal collection into which the documents are to be inserted.</p> <code>None</code> <code>system_time</code> <code>str</code> <p>Set the system start time for the insertion or update. This time will override the system time set by MarkLogic. Ignored if temporal-collection is not included in the request.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with <code>application/json</code> body containing the write results</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/documents.py</code> <pre><code>def post(\n    self,\n    body_parts: list[DocumentsBodyPart],\n    *,\n    database: str | None = None,\n    transform: str | None = None,\n    transform_params: dict | None = None,\n    txid: str | None = None,\n    temporal_collection: str | None = None,\n    system_time: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Insert or update content and/or metadata for multiple documents.\n\n    Documentation: https://docs.marklogic.com/REST/POST/v1/documents\n\n    Parameters\n    ----------\n    body_parts : list[DocumentsBodyPart]\n        A list of multipart request body parts\n    database : str\n        Perform this operation on the named content database instead\n        of the default content database associated with the REST API instance.\n        Using an alternative database requires the \"eval-in\" privilege.\n    transform : str\n        Names a content transformation previously installed via\n        the /config/transforms service. The service applies the transformation\n        to all documents prior to constructing the response.\n    transform_params : dict\n        A transform parameter names and values. For example, { \"myparam\": 1 }.\n        Transform parameters are passed to the transform named in the transform\n        parameter.\n    txid : str\n        The transaction identifier of the multi-statement transaction in which\n        to service this request. Use the /transactions service to create and manage\n        multi-statement transactions.\n    temporal_collection : str\n        Specify the name of a temporal collection into which the documents are\n        to be inserted.\n    system_time : str\n        Set the system start time for the insertion or update.\n        This time will override the system time set by MarkLogic.\n        Ignored if temporal-collection is not included in the request.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with ``application/json`` body containing\n        the write results\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = DocumentsPostCall(\n        body_parts=body_parts,\n        database=database,\n        transform=transform,\n        transform_params=transform_params,\n        txid=txid,\n        temporal_collection=temporal_collection,\n        system_time=system_time,\n    )\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/EvalApi/","title":"EvalApi","text":"<pre><code>from mlclient.api import EvalApi\n</code></pre>"},{"location":"reference/mlclient/api/EvalApi/#mlclient.api.EvalApi","title":"mlclient.api.EvalApi","text":"<pre><code>EvalApi(api: ApiClient)\n</code></pre> <p>Mid-level API for <code>/v1/eval</code> endpoint.</p> <p>Evaluate ad-hoc XQuery or server-side JavaScript queries.</p> Source code in <code>mlclient/api/eval.py</code> <pre><code>def __init__(self, api: ApiClient):\n    self._api = api\n</code></pre>"},{"location":"reference/mlclient/api/EvalApi/#mlclient.api.EvalApi.post","title":"post","text":"<pre><code>post(\n    *,\n    xquery: str | None = None,\n    javascript: str | None = None,\n    variables: dict | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Evaluate an ad-hoc query expressed using XQuery or server-side JavaScript.</p> <p>Documentation: https://docs.marklogic.com/REST/POST/v1/eval</p> <p>Parameters:</p> Name Type Description Default <code>xquery</code> <code>str</code> <p>The query to evaluate, expressed using XQuery. You must include either this parameter or the javascript parameter, but not both.</p> <code>None</code> <code>javascript</code> <code>str</code> <p>The query to evaluate, expressed using server-side JavaScript. You must include either this parameter or the xquery parameter, but not both.</p> <code>None</code> <code>variables</code> <code>dict</code> <p>External variables to pass to the query during evaluation</p> <code>None</code> <code>database</code> <code>str</code> <p>Perform this operation on the named content database instead of the default content database associated with the REST API instance. The database can be identified by name or by database id.</p> <code>None</code> <code>txid</code> <code>str</code> <p>The transaction identifier of the multi-statement transaction in which to service this request.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with <code>multipart/mixed</code> body containing the evaluation results</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/eval.py</code> <pre><code>def post(\n    self,\n    *,\n    xquery: str | None = None,\n    javascript: str | None = None,\n    variables: dict | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Evaluate an ad-hoc query expressed using XQuery or server-side JavaScript.\n\n    Documentation: https://docs.marklogic.com/REST/POST/v1/eval\n\n    Parameters\n    ----------\n    xquery : str\n        The query to evaluate, expressed using XQuery.\n        You must include either this parameter or the javascript parameter,\n        but not both.\n    javascript : str\n        The query to evaluate, expressed using server-side JavaScript.\n        You must include either this parameter or the xquery parameter,\n        but not both.\n    variables : dict\n        External variables to pass to the query during evaluation\n    database : str\n        Perform this operation on the named content database\n        instead of the default content database associated with the REST API\n        instance. The database can be identified by name or by database id.\n    txid : str\n        The transaction identifier of the multi-statement transaction\n        in which to service this request.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with ``multipart/mixed`` body containing\n        the evaluation results\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = EvalCall(\n        xquery=xquery,\n        javascript=javascript,\n        variables=variables,\n        database=database,\n        txid=txid,\n    )\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/ForestsApi/","title":"ForestsApi","text":"<pre><code>from mlclient.api import ForestsApi\n</code></pre>"},{"location":"reference/mlclient/api/ForestsApi/#mlclient.api.ForestsApi","title":"mlclient.api.ForestsApi","text":"<pre><code>ForestsApi(api: ApiClient)\n</code></pre> <p>Mid-level API for <code>/manage/v2/forests</code> endpoints.</p> <p>Create, read, update, and delete forests in the cluster.</p> <p>Requires the Manage server (port 8002 by default).</p> Source code in <code>mlclient/api/forests.py</code> <pre><code>def __init__(self, api: ApiClient):\n    self._api = api\n</code></pre>"},{"location":"reference/mlclient/api/ForestsApi/#mlclient.api.ForestsApi.create","title":"create","text":"<pre><code>create(\n    body: str | dict,\n    *,\n    wait_for_forest_to_mount: bool | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Create a new forest, including replicas if specified.</p> <p>If a database id or database is included, attach the new forest(s) to the database.</p> <p>Documentation: https://docs.marklogic.com/REST/POST/manage/v2/forests</p> <p>Parameters:</p> Name Type Description Default <code>body</code> <code>str | dict</code> <p>A forest properties in XML or JSON format.</p> required <code>wait_for_forest_to_mount</code> <code>bool</code> <p>Whether to wait for the new forest to mount before sending a response to this request. Allowed values: true (default) or false.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/forests.py</code> <pre><code>def create(\n    self,\n    body: str | dict,\n    *,\n    wait_for_forest_to_mount: bool | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Create a new forest, including replicas if specified.\n\n    If a database id or database is included, attach the new forest(s)\n    to the database.\n\n    Documentation: https://docs.marklogic.com/REST/POST/manage/v2/forests\n\n    Parameters\n    ----------\n    body : str | dict\n        A forest properties in XML or JSON format.\n    wait_for_forest_to_mount : bool\n        Whether to wait for the new forest to mount before sending a response\n        to this request. Allowed values: true (default) or false.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = ForestsPostCall(\n        body=body,\n        wait_for_forest_to_mount=wait_for_forest_to_mount,\n    )\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/ForestsApi/#mlclient.api.ForestsApi.delete","title":"delete","text":"<pre><code>delete(\n    forest: str,\n    *,\n    level: str,\n    replicas: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Delete a forest.</p> <p>Documentation: https://docs.marklogic.com/REST/DELETE/manage/v2/forests/[id-or-name]</p> <p>Parameters:</p> Name Type Description Default <code>forest</code> <code>str</code> <p>A forest identifier. The forest can be identified either by ID or name.</p> required <code>level</code> <code>str</code> <p>The type of state change to initiate. Allowed values: full, config-only. A config-only deletion removes only the forest configuration; the data contained in the forest remains on disk. A full deletion removes both the forest configuration and the data.</p> required <code>replicas</code> <code>str</code> <p>Determines how to process the replicas. Allowed values: detach to detach the replica but keep it; delete to detach and delete the replica.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/forests.py</code> <pre><code>def delete(\n    self,\n    forest: str,\n    *,\n    level: str,\n    replicas: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Delete a forest.\n\n    Documentation: https://docs.marklogic.com/REST/DELETE/manage/v2/forests/[id-or-name]\n\n    Parameters\n    ----------\n    forest : str\n        A forest identifier. The forest can be identified either by ID or name.\n    level : str\n        The type of state change to initiate. Allowed values: full, config-only.\n        A config-only deletion removes only the forest configuration;\n        the data contained in the forest remains on disk.\n        A full deletion removes both the forest configuration and the data.\n    replicas : str\n        Determines how to process the replicas.\n        Allowed values: detach to detach the replica but keep it; delete to detach\n        and delete the replica.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = ForestDeleteCall(forest=forest, level=level, replicas=replicas)\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/ForestsApi/#mlclient.api.ForestsApi.get","title":"get","text":"<pre><code>get(\n    forest: str,\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve information about a forest.</p> <p>The forest can be identified either by ID or name.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/forests/[id-or-name]</p> <p>Parameters:</p> Name Type Description Default <code>forest</code> <code>str</code> <p>A forest identifier. The forest can be identified either by ID or name.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default).</p> <code>None</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be properties-schema, config, edit, package, describe, status, xdmp:server-status or default.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the forest details</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/forests.py</code> <pre><code>def get(\n    self,\n    forest: str,\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve information about a forest.\n\n    The forest can be identified either by ID or name.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/forests/[id-or-name]\n\n    Parameters\n    ----------\n    forest : str\n        A forest identifier. The forest can be identified either by ID or name.\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n    view : str\n        A specific view of the returned data.\n        Can be properties-schema, config, edit, package, describe, status,\n        xdmp:server-status or default.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the forest details\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = ForestGetCall(forest=forest, data_format=data_format, view=view)\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/ForestsApi/#mlclient.api.ForestsApi.get_list","title":"get_list","text":"<pre><code>get_list(\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    database: str | None = None,\n    group: str | None = None,\n    host: str | None = None,\n    full_refs: bool | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve data about the forests in the cluster.</p> <p>The data returned depends on the view. If no view is specified, this request returns a summary of the forests in the cluster.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/forests</p> <p>Parameters:</p> Name Type Description Default <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default).</p> <code>None</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be either describe, default, status, metrics, schema, storage, or properties-schema.</p> <code>None</code> <code>database</code> <code>str</code> <p>Returns a summary of the forests for the specified database. The database can be identified either by id or name.</p> <code>None</code> <code>group</code> <code>str</code> <p>Returns a summary of the forests for the specified group. The group can be identified either by id or name.</p> <code>None</code> <code>host</code> <code>str</code> <p>Returns a summary of the forests for the specified host. The host can be identified either by id or name.</p> <code>None</code> <code>full_refs</code> <code>bool</code> <p>If set to true, full detail is returned for all relationship references. A value of false (the default) indicates to return detail only for first references.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the forests summary</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/forests.py</code> <pre><code>def get_list(\n    self,\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    database: str | None = None,\n    group: str | None = None,\n    host: str | None = None,\n    full_refs: bool | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve data about the forests in the cluster.\n\n    The data returned depends on the view. If no view is specified, this\n    request returns a summary of the forests in the cluster.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/forests\n\n    Parameters\n    ----------\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n    view : str\n        A specific view of the returned data.\n        Can be either describe, default, status, metrics, schema, storage,\n        or properties-schema.\n    database : str\n        Returns a summary of the forests for the specified database.\n        The database can be identified either by id or name.\n    group : str\n        Returns a summary of the forests for the specified group.\n        The group can be identified either by id or name.\n    host : str\n        Returns a summary of the forests for the specified host.\n        The host can be identified either by id or name.\n    full_refs : bool\n        If set to true, full detail is returned for all relationship references.\n        A value of false (the default) indicates to return detail only for first\n        references.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the forests summary\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = ForestsGetCall(\n        data_format=data_format,\n        view=view,\n        database=database,\n        group=group,\n        host=host,\n        full_refs=full_refs,\n    )\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/ForestsApi/#mlclient.api.ForestsApi.get_properties","title":"get_properties","text":"<pre><code>get_properties(\n    forest: str,\n    *,\n    data_format: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve the modifiable properties of a forest.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/forests/[id-or-name]/properties</p> <p>Parameters:</p> Name Type Description Default <code>forest</code> <code>str</code> <p>A forest identifier. The forest can be identified either by ID or name.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either json or xml (default). This parameter overrides the Accept header if both are present.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the forest properties</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/forests.py</code> <pre><code>def get_properties(\n    self,\n    forest: str,\n    *,\n    data_format: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve the modifiable properties of a forest.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/forests/[id-or-name]/properties\n\n    Parameters\n    ----------\n    forest : str\n        A forest identifier. The forest can be identified either by ID or name.\n    data_format : str\n        The format of the returned data. Can be either json or xml (default).\n        This parameter overrides the Accept header if both are present.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the forest properties\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = ForestPropertiesGetCall(forest=forest, data_format=data_format)\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/ForestsApi/#mlclient.api.ForestsApi.post","title":"post","text":"<pre><code>post(\n    forest: str, body: str | dict, *, timeout=UNSET\n) -&gt; Response\n</code></pre> <p>Initiate a state change on a forest, such as a merge, restart, or attach.</p> <p>Documentation: https://docs.marklogic.com/REST/POST/manage/v2/forests/[id-or-name]</p> <p>Parameters:</p> Name Type Description Default <code>forest</code> <code>str</code> <p>A forest identifier. The forest can be identified either by ID or name.</p> required <code>body</code> <code>dict</code> <p>A list of properties. Need to include the 'state' property (the type of state change to initiate). Allowed values: clear, merge, restart, attach, detach, retire, employ.</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/forests.py</code> <pre><code>def post(\n    self,\n    forest: str,\n    body: str | dict,\n    *,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Initiate a state change on a forest, such as a merge, restart, or attach.\n\n    Documentation: https://docs.marklogic.com/REST/POST/manage/v2/forests/[id-or-name]\n\n    Parameters\n    ----------\n    forest : str\n        A forest identifier. The forest can be identified either by ID or name.\n    body : dict\n        A list of properties. Need to include the 'state' property (the type\n        of state change to initiate).\n        Allowed values: clear, merge, restart, attach, detach, retire, employ.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = ForestPostCall(forest=forest, body=body)\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/ForestsApi/#mlclient.api.ForestsApi.put","title":"put","text":"<pre><code>put(body: str | dict, *, timeout=UNSET) -&gt; Response\n</code></pre> <p>Perform an operation on one or more forests.</p> <p>Such as combining multiple forests into a single new one, or migrating the data in the forests to a new data directory.</p> <p>Documentation: https://docs.marklogic.com/REST/PUT/manage/v2/forests</p> <p>Parameters:</p> Name Type Description Default <code>body</code> <code>str | dict</code> <p>A forest properties in XML or JSON format.</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/forests.py</code> <pre><code>def put(\n    self,\n    body: str | dict,\n    *,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Perform an operation on one or more forests.\n\n    Such as combining multiple forests into a single new one, or migrating\n    the data in the forests to a new data directory.\n\n    Documentation: https://docs.marklogic.com/REST/PUT/manage/v2/forests\n\n    Parameters\n    ----------\n    body : str | dict\n        A forest properties in XML or JSON format.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = ForestsPutCall(body=body)\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/ForestsApi/#mlclient.api.ForestsApi.put_properties","title":"put_properties","text":"<pre><code>put_properties(\n    forest: str, body: str | dict, *, timeout=UNSET\n) -&gt; Response\n</code></pre> <p>Modify the configuration of a forest.</p> <p>Documentation: https://docs.marklogic.com/REST/PUT/manage/v2/forests/[id-or-name]/properties</p> <p>Parameters:</p> Name Type Description Default <code>forest</code> <code>str</code> <p>A forest identifier. The forest can be identified either by ID or name.</p> required <code>body</code> <code>str | dict</code> <p>A forest properties in XML or JSON format.</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/forests.py</code> <pre><code>def put_properties(\n    self,\n    forest: str,\n    body: str | dict,\n    *,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Modify the configuration of a forest.\n\n    Documentation: https://docs.marklogic.com/REST/PUT/manage/v2/forests/[id-or-name]/properties\n\n    Parameters\n    ----------\n    forest : str\n        A forest identifier. The forest can be identified either by ID or name.\n    body : str | dict\n        A forest properties in XML or JSON format.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = ForestPropertiesPutCall(forest=forest, body=body)\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/GroupsApi/","title":"GroupsApi","text":"<pre><code>from mlclient.api import GroupsApi\n</code></pre>"},{"location":"reference/mlclient/api/GroupsApi/#mlclient.api.GroupsApi","title":"mlclient.api.GroupsApi","text":"<pre><code>GroupsApi(api: ApiClient)\n</code></pre> <p>Mid-level API for <code>/manage/v2/groups</code> endpoints.</p> <p>Read and update the properties of a group in the cluster.</p> <p>Requires the Manage server (port 8002 by default).</p> <p>Initialize the group API.</p> <p>Parameters:</p> Name Type Description Default <code>api</code> <code>ApiClient</code> <p>Client connected to the Management server</p> required Source code in <code>mlclient/api/groups.py</code> <pre><code>def __init__(self, api: ApiClient):\n    \"\"\"Initialize the group API.\n\n    Parameters\n    ----------\n    api : ApiClient\n        Client connected to the Management server\n    \"\"\"\n    self._api = api\n</code></pre>"},{"location":"reference/mlclient/api/GroupsApi/#mlclient.api.GroupsApi.get_properties","title":"get_properties","text":"<pre><code>get_properties(\n    group: str,\n    *,\n    data_format: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve the modifiable properties of the specified group.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/groups/[id-or-name]/properties</p> <p>Parameters:</p> Name Type Description Default <code>group</code> <code>str</code> <p>A group identifier. The group can be identified either by ID or name.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default). This parameter overrides the Accept header if both are present.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>Per-request HTTP timeout. Unset uses the client configuration; None disables it; a number sets all four components; an httpx.Timeout sets them independently. Not sent as a query parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the group properties</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP timeout expires after any configured retries</p> Source code in <code>mlclient/api/groups.py</code> <pre><code>def get_properties(\n    self,\n    group: str,\n    *,\n    data_format: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve the modifiable properties of the specified group.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/groups/[id-or-name]/properties\n\n    Parameters\n    ----------\n    group : str\n        A group identifier. The group can be identified either by ID or name.\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n        This parameter overrides the Accept header if both are present.\n    timeout : httpx.Timeout | float | None, default unset\n        Per-request HTTP timeout. Unset uses the client configuration;\n        None disables it; a number sets all four components; an\n        httpx.Timeout sets them independently. Not sent as a query parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the group properties\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP timeout expires after any configured retries\n    \"\"\"\n    call = GroupPropertiesGetCall(group=group, data_format=data_format)\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/GroupsApi/#mlclient.api.GroupsApi.put_properties","title":"put_properties","text":"<pre><code>put_properties(\n    group: str, body: str | dict, *, timeout=UNSET\n) -&gt; Response\n</code></pre> <p>Modify the properties of the specified group.</p> <p>Documentation: https://docs.marklogic.com/REST/PUT/manage/v2/groups/[id-or-name]/properties</p> <p>Parameters:</p> Name Type Description Default <code>group</code> <code>str</code> <p>A group identifier. The group can be identified either by ID or name.</p> required <code>body</code> <code>str | dict</code> <p>Group properties in XML or JSON format.</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>Per-request HTTP timeout. Unset uses the client configuration; None disables it; a number sets all four components; an httpx.Timeout sets them independently. Not sent as a query parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP timeout expires after any configured retries</p> Source code in <code>mlclient/api/groups.py</code> <pre><code>def put_properties(\n    self,\n    group: str,\n    body: str | dict,\n    *,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Modify the properties of the specified group.\n\n    Documentation: https://docs.marklogic.com/REST/PUT/manage/v2/groups/[id-or-name]/properties\n\n    Parameters\n    ----------\n    group : str\n        A group identifier. The group can be identified either by ID or name.\n    body : str | dict\n        Group properties in XML or JSON format.\n    timeout : httpx.Timeout | float | None, default unset\n        Per-request HTTP timeout. Unset uses the client configuration;\n        None disables it; a number sets all four components; an\n        httpx.Timeout sets them independently. Not sent as a query parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP timeout expires after any configured retries\n    \"\"\"\n    call = GroupPropertiesPutCall(group=group, body=body)\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/HostsApi/","title":"HostsApi","text":"<pre><code>from mlclient.api import HostsApi\n</code></pre>"},{"location":"reference/mlclient/api/HostsApi/#mlclient.api.HostsApi","title":"mlclient.api.HostsApi","text":"<pre><code>HostsApi(api: ApiClient)\n</code></pre> <p>Mid-level API for <code>/manage/v2/hosts</code> endpoint.</p> <p>Retrieve information about the hosts in the cluster.</p> <p>Requires the Manage server (port 8002 by default).</p> <p>Bind host operations to a synchronous API client.</p> <p>Parameters:</p> Name Type Description Default <code>api</code> <code>ApiClient</code> <p>The transport used to send requests.</p> required Source code in <code>mlclient/api/hosts.py</code> <pre><code>def __init__(self, api: ApiClient):\n    \"\"\"Bind host operations to a synchronous API client.\n\n    Parameters\n    ----------\n    api : ApiClient\n        The transport used to send requests.\n    \"\"\"\n    self._api = api\n</code></pre>"},{"location":"reference/mlclient/api/HostsApi/#mlclient.api.HostsApi.get_list","title":"get_list","text":"<pre><code>get_list(\n    *,\n    data_format: str | None = None,\n    group_id: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve data about the hosts in the cluster.</p> <p>The data returned depends on the setting of the view request parameter. The default view provides a summary of the hosts.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/hosts</p> <p>Parameters:</p> Name Type Description Default <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default).</p> <code>None</code> <code>group_id</code> <code>str</code> <p>Specifies to return only the hosts in the specified group. The group can be identified either by id or name. If not specified, the response includes information about all hosts.</p> <code>None</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be default, status, metrics, schema, properties-schema, or describe. The schema view requires XML format.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the hosts summary</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/hosts.py</code> <pre><code>def get_list(\n    self,\n    *,\n    data_format: str | None = None,\n    group_id: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve data about the hosts in the cluster.\n\n    The data returned depends on the setting of the view request parameter.\n    The default view provides a summary of the hosts.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/hosts\n\n    Parameters\n    ----------\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n    group_id : str\n        Specifies to return only the hosts in the specified group.\n        The group can be identified either by id or name.\n        If not specified, the response includes information about all hosts.\n    view : str\n        A specific view of the returned data.\n        Can be default, status, metrics, schema, properties-schema, or describe.\n        The schema view requires XML format.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the hosts summary\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = HostsGetCall(data_format=data_format, group_id=group_id, view=view)\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/LogsApi/","title":"LogsApi","text":"<pre><code>from mlclient.api import LogsApi\n</code></pre>"},{"location":"reference/mlclient/api/LogsApi/#mlclient.api.LogsApi","title":"mlclient.api.LogsApi","text":"<pre><code>LogsApi(api: ApiClient)\n</code></pre> <p>Mid-level API for <code>/manage/v2/logs</code> endpoint.</p> <p>Retrieve log file contents from MarkLogic Server.</p> <p>Requires the Manage server (port 8002 by default).</p> Source code in <code>mlclient/api/logs.py</code> <pre><code>def __init__(self, api: ApiClient):\n    self._api = api\n</code></pre>"},{"location":"reference/mlclient/api/LogsApi/#mlclient.api.LogsApi.get","title":"get","text":"<pre><code>get(\n    filename: str,\n    *,\n    data_format: str | None = None,\n    host: str | None = None,\n    start_time: str | None = None,\n    end_time: str | None = None,\n    regex: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve the contents of a log file.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/logs</p> <p>Parameters:</p> Name Type Description Default <code>filename</code> <code>str</code> <p>The log file to be returned.</p> required <code>data_format</code> <code>str</code> <p>The format of the data in the log file. The supported formats are xml, json or html.</p> <code>None</code> <code>host</code> <code>str</code> <p>The host from which to return the log data.</p> <code>None</code> <code>start_time</code> <code>str</code> <p>The start time for the log data.</p> <code>None</code> <code>end_time</code> <code>str</code> <p>The end time for the log data.</p> <code>None</code> <code>regex</code> <code>str</code> <p>Filters the log data, based on a regular expression.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response containing the log data</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/logs.py</code> <pre><code>def get(\n    self,\n    filename: str,\n    *,\n    data_format: str | None = None,\n    host: str | None = None,\n    start_time: str | None = None,\n    end_time: str | None = None,\n    regex: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve the contents of a log file.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/logs\n\n    Parameters\n    ----------\n    filename : str\n        The log file to be returned.\n    data_format : str\n        The format of the data in the log file. The supported formats are xml, json\n        or html.\n    host : str\n        The host from which to return the log data.\n    start_time : str\n        The start time for the log data.\n    end_time : str\n        The end time for the log data.\n    regex : str\n        Filters the log data, based on a regular expression.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response containing the log data\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = LogsCall(\n        filename=filename,\n        data_format=data_format,\n        host=host,\n        start_time=start_time,\n        end_time=end_time,\n        regex=regex,\n    )\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/ManageApi/","title":"ManageApi","text":"<pre><code>from mlclient.api import ManageApi\n</code></pre>"},{"location":"reference/mlclient/api/ManageApi/#mlclient.api.ManageApi","title":"mlclient.api.ManageApi","text":"<pre><code>ManageApi(api: ApiClient)\n</code></pre> <p>Management API group for /manage/v2/* endpoints.</p> <p>Requires the Manage server (port 8002 by default).</p> Source code in <code>mlclient/api/manage.py</code> <pre><code>def __init__(self, api: ApiClient):\n    self._api = api\n</code></pre>"},{"location":"reference/mlclient/api/ManageApi/#mlclient.api.ManageApi.databases","title":"databases  <code>cached</code> <code>property</code>","text":"<pre><code>databases: DatabasesApi\n</code></pre> <p>Access databases operations through this API group's connection.</p> <p>Created once on first access and reused by this group. Reading the property sends no request; calling an endpoint method performs I/O and returns a raw HTTP response. Responses are not cached.</p> <p>Returns:</p> Type Description <code>DatabasesApi</code> <p>The endpoint wrapper bound to this group's configured connection.</p>"},{"location":"reference/mlclient/api/ManageApi/#mlclient.api.ManageApi.forests","title":"forests  <code>cached</code> <code>property</code>","text":"<pre><code>forests: ForestsApi\n</code></pre> <p>Access forests operations through this API group's connection.</p> <p>Created once on first access and reused by this group. Reading the property sends no request; calling an endpoint method performs I/O and returns a raw HTTP response. Responses are not cached.</p> <p>Returns:</p> Type Description <code>ForestsApi</code> <p>The endpoint wrapper bound to this group's configured connection.</p>"},{"location":"reference/mlclient/api/ManageApi/#mlclient.api.ManageApi.groups","title":"groups  <code>cached</code> <code>property</code>","text":"<pre><code>groups: GroupsApi\n</code></pre> <p>Access groups operations through this API group's connection.</p> <p>Created once on first access and reused by this group. Reading the property sends no request; calling an endpoint method performs I/O and returns a raw HTTP response. Responses are not cached.</p> <p>Returns:</p> Type Description <code>GroupsApi</code> <p>The endpoint wrapper bound to this group's configured connection.</p>"},{"location":"reference/mlclient/api/ManageApi/#mlclient.api.ManageApi.hosts","title":"hosts  <code>cached</code> <code>property</code>","text":"<pre><code>hosts: HostsApi\n</code></pre> <p>Access hosts operations through this API group's connection.</p> <p>Created once on first access and reused by this group. Reading the property sends no request; calling an endpoint method performs I/O and returns a raw HTTP response. Responses are not cached.</p> <p>Returns:</p> Type Description <code>HostsApi</code> <p>The endpoint wrapper bound to this group's configured connection.</p>"},{"location":"reference/mlclient/api/ManageApi/#mlclient.api.ManageApi.logs","title":"logs  <code>cached</code> <code>property</code>","text":"<pre><code>logs: LogsApi\n</code></pre> <p>Access logs operations through this API group's connection.</p> <p>Created once on first access and reused by this group. Reading the property sends no request; calling an endpoint method performs I/O and returns a raw HTTP response. Responses are not cached.</p> <p>Returns:</p> Type Description <code>LogsApi</code> <p>The endpoint wrapper bound to this group's configured connection.</p>"},{"location":"reference/mlclient/api/ManageApi/#mlclient.api.ManageApi.roles","title":"roles  <code>cached</code> <code>property</code>","text":"<pre><code>roles: RolesApi\n</code></pre> <p>Access roles operations through this API group's connection.</p> <p>Created once on first access and reused by this group. Reading the property sends no request; calling an endpoint method performs I/O and returns a raw HTTP response. Responses are not cached.</p> <p>Returns:</p> Type Description <code>RolesApi</code> <p>The endpoint wrapper bound to this group's configured connection.</p>"},{"location":"reference/mlclient/api/ManageApi/#mlclient.api.ManageApi.servers","title":"servers  <code>cached</code> <code>property</code>","text":"<pre><code>servers: ServersApi\n</code></pre> <p>Access servers operations through this API group's connection.</p> <p>Created once on first access and reused by this group. Reading the property sends no request; calling an endpoint method performs I/O and returns a raw HTTP response. Responses are not cached.</p> <p>Returns:</p> Type Description <code>ServersApi</code> <p>The endpoint wrapper bound to this group's configured connection.</p>"},{"location":"reference/mlclient/api/ManageApi/#mlclient.api.ManageApi.users","title":"users  <code>cached</code> <code>property</code>","text":"<pre><code>users: UsersApi\n</code></pre> <p>Access users operations through this API group's connection.</p> <p>Created once on first access and reused by this group. Reading the property sends no request; calling an endpoint method performs I/O and returns a raw HTTP response. Responses are not cached.</p> <p>Returns:</p> Type Description <code>UsersApi</code> <p>The endpoint wrapper bound to this group's configured connection.</p>"},{"location":"reference/mlclient/api/ManageApi/#mlclient.api.ManageApi.call","title":"call","text":"<pre><code>call(call_: ApiCall, *, timeout=UNSET) -&gt; Response\n</code></pre> <p>Send a custom ApiCall.</p> <p>Parameters:</p> Name Type Description Default <code>call_</code> <code>ApiCall</code> <p>A specific endpoint call implementation</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/manage.py</code> <pre><code>def call(self, call_: ApiCall, *, timeout=UNSET) -&gt; Response:\n    \"\"\"Send a custom ApiCall.\n\n    Parameters\n    ----------\n    call_ : ApiCall\n        A specific endpoint call implementation\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return self._api.call(call_, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/RestApi/","title":"RestApi","text":"<pre><code>from mlclient.api import RestApi\n</code></pre>"},{"location":"reference/mlclient/api/RestApi/#mlclient.api.RestApi","title":"mlclient.api.RestApi","text":"<pre><code>RestApi(api: ApiClient)\n</code></pre> <p>REST API group for /v1/* endpoints (eval, documents, transactions).</p> <p>Requires a REST app server.</p> Source code in <code>mlclient/api/rest.py</code> <pre><code>def __init__(self, api: ApiClient):\n    self._api = api\n</code></pre>"},{"location":"reference/mlclient/api/RestApi/#mlclient.api.RestApi.documents","title":"documents  <code>cached</code> <code>property</code>","text":"<pre><code>documents: DocumentsApi\n</code></pre> <p>Access documents operations through this API group's connection.</p> <p>Created once on first access and reused by this group. Reading the property sends no request; calling an endpoint method performs I/O and returns a raw HTTP response. Responses are not cached.</p> <p>Returns:</p> Type Description <code>DocumentsApi</code> <p>The endpoint wrapper bound to this group's configured connection.</p>"},{"location":"reference/mlclient/api/RestApi/#mlclient.api.RestApi.eval","title":"eval  <code>cached</code> <code>property</code>","text":"<pre><code>eval: EvalApi\n</code></pre> <p>Access eval operations through this API group's connection.</p> <p>Created once on first access and reused by this group. Reading the property sends no request; calling an endpoint method performs I/O and returns a raw HTTP response. Responses are not cached.</p> <p>Returns:</p> Type Description <code>EvalApi</code> <p>The endpoint wrapper bound to this group's configured connection.</p>"},{"location":"reference/mlclient/api/RestApi/#mlclient.api.RestApi.transactions","title":"transactions  <code>cached</code> <code>property</code>","text":"<pre><code>transactions: TransactionsApi\n</code></pre> <p>Access transactions operations through this API group's connection.</p> <p>Created once on first access and reused by this group. Reading the property sends no request; calling an endpoint method performs I/O and returns a raw HTTP response. Responses are not cached.</p> <p>Returns:</p> Type Description <code>TransactionsApi</code> <p>The endpoint wrapper bound to this group's configured connection.</p>"},{"location":"reference/mlclient/api/RestApi/#mlclient.api.RestApi.call","title":"call","text":"<pre><code>call(call_: ApiCall, *, timeout=UNSET) -&gt; Response\n</code></pre> <p>Send a custom ApiCall.</p> <p>Parameters:</p> Name Type Description Default <code>call_</code> <code>ApiCall</code> <p>A specific endpoint call implementation</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/rest.py</code> <pre><code>def call(self, call_: ApiCall, *, timeout=UNSET) -&gt; Response:\n    \"\"\"Send a custom ApiCall.\n\n    Parameters\n    ----------\n    call_ : ApiCall\n        A specific endpoint call implementation\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return self._api.call(call_, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/RolesApi/","title":"RolesApi","text":"<pre><code>from mlclient.api import RolesApi\n</code></pre>"},{"location":"reference/mlclient/api/RolesApi/#mlclient.api.RolesApi","title":"mlclient.api.RolesApi","text":"<pre><code>RolesApi(api: ApiClient)\n</code></pre> <p>Mid-level API for <code>/manage/v2/roles</code> endpoints.</p> <p>Create, read, update, and delete roles in the security database.</p> <p>Requires the Manage server (port 8002 by default).</p> Source code in <code>mlclient/api/roles.py</code> <pre><code>def __init__(self, api: ApiClient):\n    self._api = api\n</code></pre>"},{"location":"reference/mlclient/api/RolesApi/#mlclient.api.RolesApi.create","title":"create","text":"<pre><code>create(body: str | dict, *, timeout=UNSET) -&gt; Response\n</code></pre> <p>Create a new role in the security database.</p> <p>Documentation: https://docs.marklogic.com/REST/POST/manage/v2/roles</p> <p>Parameters:</p> Name Type Description Default <code>body</code> <code>str | dict</code> <p>A role properties in XML or JSON format.</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/roles.py</code> <pre><code>def create(\n    self,\n    body: str | dict,\n    *,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Create a new role in the security database.\n\n    Documentation: https://docs.marklogic.com/REST/POST/manage/v2/roles\n\n    Parameters\n    ----------\n    body : str | dict\n        A role properties in XML or JSON format.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = RolesPostCall(body=body)\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/RolesApi/#mlclient.api.RolesApi.delete","title":"delete","text":"<pre><code>delete(role: str, *, timeout=UNSET) -&gt; Response\n</code></pre> <p>Delete the specified role from the security database.</p> <p>Documentation: https://docs.marklogic.com/REST/DELETE/manage/v2/roles/[id-or-name]</p> <p>Parameters:</p> Name Type Description Default <code>role</code> <code>str</code> <p>A role identifier. The role can be identified either by ID or name.</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/roles.py</code> <pre><code>def delete(\n    self,\n    role: str,\n    *,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Delete the specified role from the security database.\n\n    Documentation: https://docs.marklogic.com/REST/DELETE/manage/v2/roles/[id-or-name]\n\n    Parameters\n    ----------\n    role : str\n        A role identifier. The role can be identified either by ID or name.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = RoleDeleteCall(role=role)\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/RolesApi/#mlclient.api.RolesApi.get","title":"get","text":"<pre><code>get(\n    role: str,\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve the configuration for the specified role.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/roles/[id-or-name]</p> <p>Parameters:</p> Name Type Description Default <code>role</code> <code>str</code> <p>A role identifier. The role can be identified either by ID or name.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default). This parameter is not meaningful with view=edit.</p> <code>None</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be: describe, or default.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the role details</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/roles.py</code> <pre><code>def get(\n    self,\n    role: str,\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve the configuration for the specified role.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/roles/[id-or-name]\n\n    Parameters\n    ----------\n    role : str\n        A role identifier. The role can be identified either by ID or name.\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n        This parameter is not meaningful with view=edit.\n    view : str\n        A specific view of the returned data. Can be: describe, or default.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the role details\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = RoleGetCall(role=role, data_format=data_format, view=view)\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/RolesApi/#mlclient.api.RolesApi.get_list","title":"get_list","text":"<pre><code>get_list(\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve a summary of the roles in the security database.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/roles</p> <p>Parameters:</p> Name Type Description Default <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default).</p> <code>None</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be: describe, or default.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the roles summary</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/roles.py</code> <pre><code>def get_list(\n    self,\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve a summary of the roles in the security database.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/roles\n\n    Parameters\n    ----------\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n    view : str\n        A specific view of the returned data. Can be: describe, or default.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the roles summary\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = RolesGetCall(data_format=data_format, view=view)\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/RolesApi/#mlclient.api.RolesApi.get_properties","title":"get_properties","text":"<pre><code>get_properties(\n    role: str,\n    *,\n    data_format: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve the properties of the specified role.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/roles/[id-or-name]/properties</p> <p>Parameters:</p> Name Type Description Default <code>role</code> <code>str</code> <p>A role identifier. The role can be identified either by ID or name.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either json or xml (default). This parameter overrides the Accept header if both are present.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the role properties</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/roles.py</code> <pre><code>def get_properties(\n    self,\n    role: str,\n    *,\n    data_format: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve the properties of the specified role.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/roles/[id-or-name]/properties\n\n    Parameters\n    ----------\n    role : str\n        A role identifier. The role can be identified either by ID or name.\n    data_format : str\n        The format of the returned data. Can be either json or xml (default).\n        This parameter overrides the Accept header if both are present.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the role properties\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = RolePropertiesGetCall(role=role, data_format=data_format)\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/RolesApi/#mlclient.api.RolesApi.put_properties","title":"put_properties","text":"<pre><code>put_properties(\n    role: str, body: str | dict, *, timeout=UNSET\n) -&gt; Response\n</code></pre> <p>Update the properties for the specified role.</p> <p>Documentation: https://docs.marklogic.com/REST/PUT/manage/v2/roles/[id-or-name]/properties</p> <p>Parameters:</p> Name Type Description Default <code>role</code> <code>str</code> <p>A role identifier. The role can be identified either by ID or name.</p> required <code>body</code> <code>str | dict</code> <p>A role properties in XML or JSON format.</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/roles.py</code> <pre><code>def put_properties(\n    self,\n    role: str,\n    body: str | dict,\n    *,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Update the properties for the specified role.\n\n    Documentation: https://docs.marklogic.com/REST/PUT/manage/v2/roles/[id-or-name]/properties\n\n    Parameters\n    ----------\n    role : str\n        A role identifier. The role can be identified either by ID or name.\n    body : str | dict\n        A role properties in XML or JSON format.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = RolePropertiesPutCall(role=role, body=body)\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/ServersApi/","title":"ServersApi","text":"<pre><code>from mlclient.api import ServersApi\n</code></pre>"},{"location":"reference/mlclient/api/ServersApi/#mlclient.api.ServersApi","title":"mlclient.api.ServersApi","text":"<pre><code>ServersApi(api: ApiClient)\n</code></pre> <p>Mid-level API for <code>/manage/v2/servers</code> endpoints.</p> <p>Create, read, update, and delete App Servers in the cluster.</p> <p>Requires the Manage server (port 8002 by default).</p> Source code in <code>mlclient/api/servers.py</code> <pre><code>def __init__(self, api: ApiClient):\n    self._api = api\n</code></pre>"},{"location":"reference/mlclient/api/ServersApi/#mlclient.api.ServersApi.create","title":"create","text":"<pre><code>create(\n    body: str | dict,\n    *,\n    group_id: str | None = None,\n    server_type: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Create a new App Server in the specified group.</p> <p>Documentation: https://docs.marklogic.com/REST/POST/manage/v2/servers</p> <p>Parameters:</p> Name Type Description Default <code>body</code> <code>str | dict</code> <p>A server properties in XML or JSON format.</p> required <code>group_id</code> <code>str</code> <p>The id or name of the group to which the App Server belongs. The group must be specified by this parameter or by the group-name property in the request payload. If it is specified in both places, the values must be the same.</p> <code>None</code> <code>server_type</code> <code>str</code> <p>The type of App Server to create. The App Server type must be specified by this parameter or in the request payload. If it is specified in both places, the values must be the same. The valid types are: http, odbc, xdbc, or webdav.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/servers.py</code> <pre><code>def create(\n    self,\n    body: str | dict,\n    *,\n    group_id: str | None = None,\n    server_type: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Create a new App Server in the specified group.\n\n    Documentation: https://docs.marklogic.com/REST/POST/manage/v2/servers\n\n    Parameters\n    ----------\n    body : str | dict\n        A server properties in XML or JSON format.\n    group_id : str\n        The id or name of the group to which the App Server belongs.\n        The group must be specified by this parameter or by the group-name property\n        in the request payload. If it is specified in both places, the values\n        must be the same.\n    server_type : str\n        The type of App Server to create.\n        The App Server type must be specified by this parameter or in the request\n        payload. If it is specified in both places, the values must be the same.\n        The valid types are: http, odbc, xdbc, or webdav.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = ServersPostCall(body=body, group_id=group_id, server_type=server_type)\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/ServersApi/#mlclient.api.ServersApi.delete","title":"delete","text":"<pre><code>delete(\n    server: str, group_id: str, *, timeout=UNSET\n) -&gt; Response\n</code></pre> <p>Delete the specified App Server from the specified group.</p> <p>Documentation: https://docs.marklogic.com/REST/DELETE/manage/v2/servers/[id-or-name]</p> <p>Parameters:</p> Name Type Description Default <code>server</code> <code>str</code> <p>A server identifier. The server can be identified either by ID or name.</p> required <code>group_id</code> <code>str</code> <p>The id or name of the group to which the App Server belongs. This parameter is required.</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/servers.py</code> <pre><code>def delete(\n    self,\n    server: str,\n    group_id: str,\n    *,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Delete the specified App Server from the specified group.\n\n    Documentation: https://docs.marklogic.com/REST/DELETE/manage/v2/servers/[id-or-name]\n\n    Parameters\n    ----------\n    server : str\n        A server identifier. The server can be identified either by ID or name.\n    group_id : str\n        The id or name of the group to which the App Server belongs.\n        This parameter is required.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = ServerDeleteCall(server=server, group_id=group_id)\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/ServersApi/#mlclient.api.ServersApi.get","title":"get","text":"<pre><code>get(\n    server: str,\n    group_id: str,\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    host_id: str | None = None,\n    full_refs: bool | None = None,\n    modules: bool | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve data about a specific App Server.</p> <p>The server can be identified either by ID or name. The data returned depends on the value of the view request parameter. The default view is a summary with links to additional data.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/servers/[id-or-name]</p> <p>Parameters:</p> Name Type Description Default <code>server</code> <code>str</code> <p>A server identifier. The server can be identified either by ID or name.</p> required <code>group_id</code> <code>str</code> <p>The id or name of the group to which the App Server belongs. This parameter is required.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default).</p> <code>None</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be properties-schema, config, edit, package, describe, status, xdmp:server-status or default.</p> <code>None</code> <code>host_id</code> <code>str</code> <p>Meaningful only when view=status. Specifies to return the status for the server in the specified host. The host can be identified either by id or name.</p> <code>None</code> <code>full_refs</code> <code>bool</code> <p>If set to true, full detail is returned for all relationship references. A value of false (the default) indicates to return detail only for first references. This parameter is not meaningful with view=package.</p> <code>None</code> <code>modules</code> <code>bool</code> <p>Meaningful only with view=package. Whether to include a manifest of the modules database for the App Server in the results, if one exists. It is an error to request a modules database manifest for an App Server that uses the filesystem for modules. Default: false.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the server details</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/servers.py</code> <pre><code>def get(\n    self,\n    server: str,\n    group_id: str,\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    host_id: str | None = None,\n    full_refs: bool | None = None,\n    modules: bool | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve data about a specific App Server.\n\n    The server can be identified either by ID or name. The data returned\n    depends on the value of the view request parameter. The default view\n    is a summary with links to additional data.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/servers/[id-or-name]\n\n    Parameters\n    ----------\n    server : str\n        A server identifier. The server can be identified either by ID or name.\n    group_id : str\n        The id or name of the group to which the App Server belongs.\n        This parameter is required.\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n    view : str\n        A specific view of the returned data.\n        Can be properties-schema, config, edit, package, describe, status,\n        xdmp:server-status or default.\n    host_id : str\n        Meaningful only when view=status. Specifies to return the status for\n        the server in the specified host. The host can be identified either by id\n        or name.\n    full_refs : bool\n        If set to true, full detail is returned for all relationship references.\n        A value of false (the default) indicates to return detail only for\n        first references. This parameter is not meaningful with view=package.\n    modules : bool\n        Meaningful only with view=package. Whether to include a manifest\n        of the modules database for the App Server in the results, if one exists.\n        It is an error to request a modules database manifest for an App Server\n        that uses the filesystem for modules. Default: false.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the server details\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = ServerGetCall(\n        server=server,\n        group_id=group_id,\n        data_format=data_format,\n        view=view,\n        host_id=host_id,\n        full_refs=full_refs,\n        modules=modules,\n    )\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/ServersApi/#mlclient.api.ServersApi.get_list","title":"get_list","text":"<pre><code>get_list(\n    *,\n    data_format: str | None = None,\n    group_id: str | None = None,\n    view: str | None = None,\n    full_refs: bool | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve data about the App Servers in the cluster.</p> <p>The data returned depends on the setting of the view request parameter. The default view provides a summary of the servers.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/servers</p> <p>Parameters:</p> Name Type Description Default <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default).</p> <code>None</code> <code>group_id</code> <code>str</code> <p>Specifies to return only the servers in the specified group. The group can be identified either by id or name. If not specified, the response includes information about all App Servers.</p> <code>None</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be schema, properties-schema, metrics, package, describe, or default.</p> <code>None</code> <code>full_refs</code> <code>bool</code> <p>If set to true, full detail is returned for all relationship references. A value of false (the default) indicates to return detail only for first references. This parameter is not meaningful with view=package.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the servers summary</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/servers.py</code> <pre><code>def get_list(\n    self,\n    *,\n    data_format: str | None = None,\n    group_id: str | None = None,\n    view: str | None = None,\n    full_refs: bool | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve data about the App Servers in the cluster.\n\n    The data returned depends on the setting of the view request parameter.\n    The default view provides a summary of the servers.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/servers\n\n    Parameters\n    ----------\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n    group_id : str\n        Specifies to return only the servers in the specified group.\n        The group can be identified either by id or name.\n        If not specified, the response includes information about all App Servers.\n    view : str\n        A specific view of the returned data.\n        Can be schema, properties-schema, metrics, package, describe, or default.\n    full_refs : bool\n        If set to true, full detail is returned for all relationship references.\n        A value of false (the default) indicates to return detail only for first\n        references. This parameter is not meaningful with view=package.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the servers summary\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = ServersGetCall(\n        data_format=data_format,\n        group_id=group_id,\n        view=view,\n        full_refs=full_refs,\n    )\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/ServersApi/#mlclient.api.ServersApi.get_properties","title":"get_properties","text":"<pre><code>get_properties(\n    server: str,\n    group_id: str,\n    *,\n    data_format: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve the modifiable properties of the specified App Server.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/servers/[id-or-name]/properties</p> <p>Parameters:</p> Name Type Description Default <code>server</code> <code>str</code> <p>A server identifier. The server can be identified either by ID or name.</p> required <code>group_id</code> <code>str</code> <p>The id or name of the group to which the App Server belongs. This parameter is required.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either json or xml (default). This parameter overrides the Accept header if both are present.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the server properties</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/servers.py</code> <pre><code>def get_properties(\n    self,\n    server: str,\n    group_id: str,\n    *,\n    data_format: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve the modifiable properties of the specified App Server.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/servers/[id-or-name]/properties\n\n    Parameters\n    ----------\n    server : str\n        A server identifier. The server can be identified either by ID or name.\n    group_id : str\n        The id or name of the group to which the App Server belongs.\n        This parameter is required.\n    data_format : str\n        The format of the returned data. Can be either json or xml (default).\n        This parameter overrides the Accept header if both are present.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the server properties\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = ServerPropertiesGetCall(\n        server=server,\n        group_id=group_id,\n        data_format=data_format,\n    )\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/ServersApi/#mlclient.api.ServersApi.put_properties","title":"put_properties","text":"<pre><code>put_properties(\n    server: str,\n    group_id: str,\n    body: str | dict,\n    *,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Modify the properties of the specified App Server.</p> <p>Documentation: https://docs.marklogic.com/REST/PUT/manage/v2/servers/[id-or-name]/properties</p> <p>Parameters:</p> Name Type Description Default <code>server</code> <code>str</code> <p>A server identifier. The server can be identified either by ID or name.</p> required <code>group_id</code> <code>str</code> <p>The id or name of the group to which the App Server belongs. This parameter is required.</p> required <code>body</code> <code>str | dict</code> <p>A server properties in XML or JSON format.</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/servers.py</code> <pre><code>def put_properties(\n    self,\n    server: str,\n    group_id: str,\n    body: str | dict,\n    *,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Modify the properties of the specified App Server.\n\n    Documentation: https://docs.marklogic.com/REST/PUT/manage/v2/servers/[id-or-name]/properties\n\n    Parameters\n    ----------\n    server : str\n        A server identifier. The server can be identified either by ID or name.\n    group_id : str\n        The id or name of the group to which the App Server belongs.\n        This parameter is required.\n    body : str | dict\n        A server properties in XML or JSON format.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = ServerPropertiesPutCall(server=server, group_id=group_id, body=body)\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/TransactionsApi/","title":"TransactionsApi","text":"<pre><code>from mlclient.api import TransactionsApi\n</code></pre>"},{"location":"reference/mlclient/api/TransactionsApi/#mlclient.api.TransactionsApi","title":"mlclient.api.TransactionsApi","text":"<pre><code>TransactionsApi(api: ApiClient)\n</code></pre> <p>Mid-level API for <code>/v1/transactions</code> endpoints.</p> <p>Create, inspect, and commit or roll back multi-statement transactions.</p> <p>Requires a REST app server.</p> Source code in <code>mlclient/api/transactions.py</code> <pre><code>def __init__(self, api: ApiClient):\n    self._api = api\n</code></pre>"},{"location":"reference/mlclient/api/TransactionsApi/#mlclient.api.TransactionsApi.create","title":"create","text":"<pre><code>create(\n    *,\n    name: str | None = None,\n    time_limit: int | None = None,\n    database: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Create a multi-statement transaction.</p> <p>The response is a 303 redirect whose <code>Location</code> header carries the new transaction id (<code>/v1/transactions/{txid}</code>); the redirect is not followed.</p> <p>Documentation: https://docs.marklogic.com/REST/POST/v1/transactions</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>A name to assign to the transaction.</p> <code>None</code> <code>time_limit</code> <code>int</code> <p>The maximum number of seconds for the transaction to remain open.</p> <code>None</code> <code>database</code> <code>str</code> <p>Evaluate against the named content database instead of the default content database associated with the REST API instance.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with a <code>Location</code> header carrying the transaction id</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/transactions.py</code> <pre><code>def create(\n    self,\n    *,\n    name: str | None = None,\n    time_limit: int | None = None,\n    database: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Create a multi-statement transaction.\n\n    The response is a 303 redirect whose ``Location`` header carries the new\n    transaction id (``/v1/transactions/{txid}``); the redirect is not followed.\n\n    Documentation: https://docs.marklogic.com/REST/POST/v1/transactions\n\n    Parameters\n    ----------\n    name : str\n        A name to assign to the transaction.\n    time_limit : int\n        The maximum number of seconds for the transaction to remain open.\n    database : str\n        Evaluate against the named content database instead of the default\n        content database associated with the REST API instance.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with a ``Location`` header carrying the transaction id\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = TransactionsPostCall(\n        name=name,\n        time_limit=time_limit,\n        database=database,\n    )\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/TransactionsApi/#mlclient.api.TransactionsApi.get","title":"get","text":"<pre><code>get(\n    txid: str,\n    *,\n    data_format: str | None = None,\n    database: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve the status of the specified transaction.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/v1/transactions/[txid]</p> <p>Parameters:</p> Name Type Description Default <code>txid</code> <code>str</code> <p>A transaction identifier.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either json or xml (default).</p> <code>None</code> <code>database</code> <code>str</code> <p>Evaluate against the named content database instead of the default content database associated with the REST API instance.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the transaction status</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/transactions.py</code> <pre><code>def get(\n    self,\n    txid: str,\n    *,\n    data_format: str | None = None,\n    database: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve the status of the specified transaction.\n\n    Documentation: https://docs.marklogic.com/REST/GET/v1/transactions/[txid]\n\n    Parameters\n    ----------\n    txid : str\n        A transaction identifier.\n    data_format : str\n        The format of the returned data. Can be either json or xml (default).\n    database : str\n        Evaluate against the named content database instead of the default\n        content database associated with the REST API instance.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the transaction status\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = TransactionGetCall(\n        txid=txid,\n        data_format=data_format,\n        database=database,\n    )\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/TransactionsApi/#mlclient.api.TransactionsApi.post","title":"post","text":"<pre><code>post(\n    txid: str,\n    *,\n    result: str,\n    database: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Commit or roll back the specified transaction.</p> <p>Documentation: https://docs.marklogic.com/REST/POST/v1/transactions/[txid]</p> <p>Parameters:</p> Name Type Description Default <code>txid</code> <code>str</code> <p>A transaction identifier.</p> required <code>result</code> <code>str</code> <p>The disposition of the transaction. Can be either commit or rollback.</p> required <code>database</code> <code>str</code> <p>Evaluate against the named content database instead of the default content database associated with the REST API instance.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/transactions.py</code> <pre><code>def post(\n    self,\n    txid: str,\n    *,\n    result: str,\n    database: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Commit or roll back the specified transaction.\n\n    Documentation: https://docs.marklogic.com/REST/POST/v1/transactions/[txid]\n\n    Parameters\n    ----------\n    txid : str\n        A transaction identifier.\n    result : str\n        The disposition of the transaction. Can be either commit or rollback.\n    database : str\n        Evaluate against the named content database instead of the default\n        content database associated with the REST API instance.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = TransactionPostCall(\n        txid=txid,\n        result=result,\n        database=database,\n    )\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/UsersApi/","title":"UsersApi","text":"<pre><code>from mlclient.api import UsersApi\n</code></pre>"},{"location":"reference/mlclient/api/UsersApi/#mlclient.api.UsersApi","title":"mlclient.api.UsersApi","text":"<pre><code>UsersApi(api: ApiClient)\n</code></pre> <p>Mid-level API for <code>/manage/v2/users</code> endpoints.</p> <p>Create, read, update, and delete users in the security database.</p> <p>Requires the Manage server (port 8002 by default).</p> Source code in <code>mlclient/api/users.py</code> <pre><code>def __init__(self, api: ApiClient):\n    self._api = api\n</code></pre>"},{"location":"reference/mlclient/api/UsersApi/#mlclient.api.UsersApi.create","title":"create","text":"<pre><code>create(body: str | dict, *, timeout=UNSET) -&gt; Response\n</code></pre> <p>Create a new user in the security database.</p> <p>Documentation: https://docs.marklogic.com/REST/POST/manage/v2/users</p> <p>Parameters:</p> Name Type Description Default <code>body</code> <code>str | dict</code> <p>A user properties in XML or JSON format.</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/users.py</code> <pre><code>def create(\n    self,\n    body: str | dict,\n    *,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Create a new user in the security database.\n\n    Documentation: https://docs.marklogic.com/REST/POST/manage/v2/users\n\n    Parameters\n    ----------\n    body : str | dict\n        A user properties in XML or JSON format.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = UsersPostCall(body=body)\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/UsersApi/#mlclient.api.UsersApi.delete","title":"delete","text":"<pre><code>delete(user: str, *, timeout=UNSET) -&gt; Response\n</code></pre> <p>Delete the specified user from the security database.</p> <p>Documentation: https://docs.marklogic.com/REST/DELETE/manage/v2/users/[id-or-name]</p> <p>Parameters:</p> Name Type Description Default <code>user</code> <code>str</code> <p>A user identifier. The user can be identified either by ID or name.</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/users.py</code> <pre><code>def delete(\n    self,\n    user: str,\n    *,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Delete the specified user from the security database.\n\n    Documentation: https://docs.marklogic.com/REST/DELETE/manage/v2/users/[id-or-name]\n\n    Parameters\n    ----------\n    user : str\n        A user identifier. The user can be identified either by ID or name.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = UserDeleteCall(user=user)\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/UsersApi/#mlclient.api.UsersApi.get","title":"get","text":"<pre><code>get(\n    user: str,\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve the configuration for the specified user.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/users/[id-or-name]</p> <p>Parameters:</p> Name Type Description Default <code>user</code> <code>str</code> <p>A user identifier. The user can be identified either by ID or name.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default). This parameter is not meaningful with view=edit.</p> <code>None</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be: describe, or default.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the user details</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/users.py</code> <pre><code>def get(\n    self,\n    user: str,\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve the configuration for the specified user.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/users/[id-or-name]\n\n    Parameters\n    ----------\n    user : str\n        A user identifier. The user can be identified either by ID or name.\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n        This parameter is not meaningful with view=edit.\n    view : str\n        A specific view of the returned data. Can be: describe, or default.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the user details\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = UserGetCall(user=user, data_format=data_format, view=view)\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/UsersApi/#mlclient.api.UsersApi.get_list","title":"get_list","text":"<pre><code>get_list(\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve a summary of the users in the cluster.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/users</p> <p>Parameters:</p> Name Type Description Default <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default).</p> <code>None</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be: describe, or default.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the users summary</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/users.py</code> <pre><code>def get_list(\n    self,\n    *,\n    data_format: str | None = None,\n    view: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve a summary of the users in the cluster.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/users\n\n    Parameters\n    ----------\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n    view : str\n        A specific view of the returned data. Can be: describe, or default.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the users summary\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = UsersGetCall(data_format=data_format, view=view)\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/UsersApi/#mlclient.api.UsersApi.get_properties","title":"get_properties","text":"<pre><code>get_properties(\n    user: str,\n    *,\n    data_format: str | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Retrieve the properties of the specified user.</p> <p>Documentation: https://docs.marklogic.com/REST/GET/manage/v2/users/[id-or-name]/properties</p> <p>Parameters:</p> Name Type Description Default <code>user</code> <code>str</code> <p>A user identifier. The user can be identified either by ID or name.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either json or xml (default). This parameter overrides the Accept header if both are present.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response with the user properties</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/users.py</code> <pre><code>def get_properties(\n    self,\n    user: str,\n    *,\n    data_format: str | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Retrieve the properties of the specified user.\n\n    Documentation: https://docs.marklogic.com/REST/GET/manage/v2/users/[id-or-name]/properties\n\n    Parameters\n    ----------\n    user : str\n        A user identifier. The user can be identified either by ID or name.\n    data_format : str\n        The format of the returned data. Can be either json or xml (default).\n        This parameter overrides the Accept header if both are present.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response with the user properties\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = UserPropertiesGetCall(user=user, data_format=data_format)\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/api/UsersApi/#mlclient.api.UsersApi.put_properties","title":"put_properties","text":"<pre><code>put_properties(\n    user: str, body: str | dict, *, timeout=UNSET\n) -&gt; Response\n</code></pre> <p>Update the properties for the specified user.</p> <p>Documentation: https://docs.marklogic.com/REST/PUT/manage/v2/users/[id-or-name]/properties</p> <p>Parameters:</p> Name Type Description Default <code>user</code> <code>str</code> <p>A user identifier. The user can be identified either by ID or name.</p> required <code>body</code> <code>str | dict</code> <p>A user properties in XML or JSON format.</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/api/users.py</code> <pre><code>def put_properties(\n    self,\n    user: str,\n    body: str | dict,\n    *,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Update the properties for the specified user.\n\n    Documentation: https://docs.marklogic.com/REST/PUT/manage/v2/users/[id-or-name]/properties\n\n    Parameters\n    ----------\n    user : str\n        A user identifier. The user can be identified either by ID or name.\n    body : str | dict\n        A user properties in XML or JSON format.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them. It is an execution option, never sent as a request parameter.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    call = UserPropertiesPutCall(user=user, body=body)\n    return self._api.call(call, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/auth/","title":"<code>mlclient.auth</code>","text":""},{"location":"reference/mlclient/auth/#mlclient.auth","title":"mlclient.auth","text":"<p>The Authentication module.</p> <p>This module models the application layer of a MarkLogic connection: how the client proves its identity, independent of the transport.</p> <p>Classes:</p> <pre><code>* AuthConfig\n    Data a specific auth mechanism needs beyond the shared user identity.\n* OAuthBearerAuth\n    OAuth 2.0 Bearer token authentication.\n* KerberosAuth\n    Kerberos/SPNEGO (Negotiate) authentication over pyspnego.\n* MarkLogicCloudAuth\n    MarkLogic Cloud API key exchanged for a Bearer token, with refresh.\n</code></pre> <p><code>username</code> / <code>password</code> model the MarkLogic user identity. That identity is shared across the credential methods and independent of the wire mechanism - the same user can be presented as basic or digest by flipping <code>auth</code> without touching the credential - so it lives on the client (and at the root of a YAML environment, inherited per server) rather than on any one method.</p> <p><code>auth</code> selects the wire mechanism. Most mechanisms are selectable by name alone: the credential ones (<code>\"basic\"</code>, <code>\"digest\"</code>, <code>\"digestbasic\"</code>) draw on the shared identity, while <code>\"certificate\"</code> and <code>\"kerberos\"</code> draw their identity from elsewhere (the TLS client certificate, the ambient ticket cache) and need no extra data.</p> <p><code>AuthConfig</code> carries the data a specific mechanism needs beyond the shared identity: OAuth's token (which has no default, so <code>AuthConfig</code> is the only way to select OAuth) or a Kerberos SPN override. It is not a credential store - basic/digest credentials never pass through it.</p>"},{"location":"reference/mlclient/auth/#mlclient.auth.AuthParam","title":"AuthParam  <code>module-attribute</code>","text":"<pre><code>AuthParam = Union[str, httpx.Auth, 'AuthConfig', None]\n</code></pre>"},{"location":"reference/mlclient/auth/AuthConfig/","title":"AuthConfig","text":"<pre><code>from mlclient.auth import AuthConfig\n</code></pre>"},{"location":"reference/mlclient/auth/AuthConfig/#mlclient.auth.AuthConfig","title":"mlclient.auth.AuthConfig  <code>pydantic-model</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Data a specific auth mechanism needs beyond the shared user identity.</p> <p>Holds OAuth's token or a Kerberos SPN override (<code>service</code> / <code>hostname</code>). It is not a credential store: basic/digest identity comes from the client's <code>username</code> / <code>password</code>, never from here.</p> <p><code>method</code> selects the mechanism this config configures: <code>\"oauth\"</code>, <code>\"kerberos\"</code> or <code>\"certificate\"</code>. The remaining fields apply to a single mechanism and are ignored by the others:</p> <ul> <li><code>token</code> -- the OAuth Bearer token; used only for <code>\"oauth\"</code>.</li> <li><code>service</code> -- the service part of the Kerberos SPN <code>service/hostname</code>;   used only for <code>\"kerberos\"</code>. MarkLogic registers its principal under the   <code>\"HTTP\"</code> service, so the default fits every standard deployment; override   it only when the SPN was registered under a different service name.</li> <li><code>hostname</code> -- the hostname part of the Kerberos SPN; used only for   <code>\"kerberos\"</code>, defaulting to the request host when omitted.</li> </ul> Show JSON schema: <pre><code>{\n  \"description\": \"Data a specific auth mechanism needs beyond the shared user identity.\\n\\nHolds OAuth's token or a Kerberos SPN override (``service`` / ``hostname``).\\nIt is not a credential store: basic/digest identity comes from the client's\\n``username`` / ``password``, never from here.\\n\\n``method`` selects the mechanism this config configures: ``\\\"oauth\\\"``,\\n``\\\"kerberos\\\"`` or ``\\\"certificate\\\"``. The remaining fields apply to a single\\nmechanism and are ignored by the others:\\n\\n- ``token`` -- the OAuth Bearer token; used only for ``\\\"oauth\\\"``.\\n- ``service`` -- the service part of the Kerberos SPN ``service/hostname``;\\n  used only for ``\\\"kerberos\\\"``. MarkLogic registers its principal under the\\n  ``\\\"HTTP\\\"`` service, so the default fits every standard deployment; override\\n  it only when the SPN was registered under a different service name.\\n- ``hostname`` -- the hostname part of the Kerberos SPN; used only for\\n  ``\\\"kerberos\\\"``, defaulting to the request host when omitted.\",\n  \"properties\": {\n    \"method\": {\n      \"title\": \"Method\",\n      \"type\": \"string\"\n    },\n    \"token\": {\n      \"anyOf\": [\n        {\n          \"type\": \"string\"\n        },\n        {\n          \"type\": \"null\"\n        }\n      ],\n      \"default\": null,\n      \"title\": \"Token\"\n    },\n    \"service\": {\n      \"default\": \"HTTP\",\n      \"title\": \"Service\",\n      \"type\": \"string\"\n    },\n    \"hostname\": {\n      \"anyOf\": [\n        {\n          \"type\": \"string\"\n        },\n        {\n          \"type\": \"null\"\n        }\n      ],\n      \"default\": null,\n      \"title\": \"Hostname\"\n    }\n  },\n  \"required\": [\n    \"method\"\n  ],\n  \"title\": \"AuthConfig\",\n  \"type\": \"object\"\n}\n</code></pre> <p>Fields:</p> <ul> <li> <code>method</code>                 (<code>str</code>)             </li> <li> <code>token</code>                 (<code>Optional[str]</code>)             </li> <li> <code>service</code>                 (<code>str</code>)             </li> <li> <code>hostname</code>                 (<code>Optional[str]</code>)             </li> </ul>"},{"location":"reference/mlclient/auth/KerberosAuth/","title":"KerberosAuth","text":"<pre><code>from mlclient.auth import KerberosAuth\n</code></pre>"},{"location":"reference/mlclient/auth/KerberosAuth/#mlclient.auth.KerberosAuth","title":"mlclient.auth.KerberosAuth","text":"<pre><code>KerberosAuth(\n    service: str = \"HTTP\", hostname: str | None = None\n)\n</code></pre> <p>               Bases: <code>Auth</code></p> <p>Kerberos/SPNEGO (Negotiate) authentication over pyspnego.</p> <p>On a <code>401</code> carrying <code>WWW-Authenticate: Negotiate</code> the client generates a SPNEGO token for the <code>service/hostname</code> SPN and retries with an <code>Authorization: Negotiate</code> header. The server's final token is fed back to complete mutual authentication.</p> <p>Credentials come from the ambient Kerberos cache (the ticket-granting ticket obtained via <code>kinit</code>); no username or password is sent. <code>hostname</code> is taken from the request when not overridden, so the same handler serves the main, manage and admin ports of one host.</p> <p>The token exchange is local CPU work with no network I/O, so one flow serves both the sync and async clients.</p> <p>Initialize KerberosAuth.</p> <p>Parameters:</p> Name Type Description Default <code>service</code> <code>str</code> <p>The service part of the SPN (<code>service/hostname</code>).</p> <code>\"HTTP\"</code> <code>hostname</code> <code>str | None</code> <p>The principal part of the SPN; the request host is used when None.</p> <code>None</code> Source code in <code>mlclient/auth.py</code> <pre><code>def __init__(\n    self,\n    service: str = \"HTTP\",\n    hostname: str | None = None,\n):\n    \"\"\"Initialize KerberosAuth.\n\n    Parameters\n    ----------\n    service : str, default \"HTTP\"\n        The service part of the SPN (``service/hostname``).\n    hostname : str | None, default None\n        The principal part of the SPN; the request host is used when None.\n    \"\"\"\n    self._service = service\n    self._hostname = hostname\n</code></pre>"},{"location":"reference/mlclient/auth/KerberosAuth/#mlclient.auth.KerberosAuth.auth_flow","title":"auth_flow","text":"<pre><code>auth_flow(request: Request)\n</code></pre> <p>Negotiate authentication, retrying once with a SPNEGO token.</p> Source code in <code>mlclient/auth.py</code> <pre><code>def auth_flow(\n    self,\n    request: httpx.Request,\n):\n    \"\"\"Negotiate authentication, retrying once with a SPNEGO token.\"\"\"\n    response = yield request\n    if not self._needs_negotiation(response):\n        return\n    context = self._new_context(request.url.host)\n    request.headers[\"Authorization\"] = self._initial_header(context)\n    response = yield request\n    self._verify_mutual_auth(context, response)\n</code></pre>"},{"location":"reference/mlclient/auth/MarkLogicCloudAuth/","title":"MarkLogicCloudAuth","text":"<pre><code>from mlclient.auth import MarkLogicCloudAuth\n</code></pre>"},{"location":"reference/mlclient/auth/MarkLogicCloudAuth/#mlclient.auth.MarkLogicCloudAuth","title":"mlclient.auth.MarkLogicCloudAuth","text":"<pre><code>MarkLogicCloudAuth(\n    base_url: str,\n    api_key: str,\n    token_duration: int = 0,\n    verify: _TokenVerify = True,\n)\n</code></pre> <p>               Bases: <code>Auth</code></p> <p>MarkLogic Cloud API key exchanged for a Bearer token, with refresh.</p> <p>The API key is posted to <code>{base_url}/token</code> to obtain a short-lived access token, which is then sent as a Bearer token. A 401 triggers a single token refresh and retry. Both sync and async request flows are supported.</p> <p>Initialize MarkLogicCloudAuth.</p> <p>Parameters:</p> Name Type Description Default <code>base_url</code> <code>str</code> <p>The Cloud instance base URL (scheme and host) used to reach /token.</p> required <code>api_key</code> <code>str</code> <p>The MarkLogic Cloud API key.</p> required <code>token_duration</code> <code>int</code> <p>Token lifetime in seconds; 0 uses the server default.</p> <code>0</code> <code>verify</code> <code>bool | str | SSLContext</code> <p>Server certificate verification for the token exchange, matching the connection's own SSL settings.</p> <code>True</code> Source code in <code>mlclient/auth.py</code> <pre><code>def __init__(\n    self,\n    base_url: str,\n    api_key: str,\n    token_duration: int = 0,\n    verify: _TokenVerify = True,\n):\n    \"\"\"Initialize MarkLogicCloudAuth.\n\n    Parameters\n    ----------\n    base_url : str\n        The Cloud instance base URL (scheme and host) used to reach /token.\n    api_key : str\n        The MarkLogic Cloud API key.\n    token_duration : int, default 0\n        Token lifetime in seconds; 0 uses the server default.\n    verify : bool | str | ssl.SSLContext, default True\n        Server certificate verification for the token exchange, matching the\n        connection's own SSL settings.\n    \"\"\"\n    self._token_endpoint = urljoin(base_url, \"/token\")\n    self._api_key = api_key\n    self._token_duration = token_duration\n    self._verify = verify\n    self._token: str | None = None\n</code></pre>"},{"location":"reference/mlclient/auth/MarkLogicCloudAuth/#mlclient.auth.MarkLogicCloudAuth.async_auth_flow","title":"async_auth_flow  <code>async</code>","text":"<pre><code>async_auth_flow(request: Request)\n</code></pre> <p>Attach a Bearer token, fetching/refreshing it asynchronously.</p> Source code in <code>mlclient/auth.py</code> <pre><code>async def async_auth_flow(\n    self,\n    request: httpx.Request,\n):\n    \"\"\"Attach a Bearer token, fetching/refreshing it asynchronously.\"\"\"\n    if self._token is None:\n        self._token = await self._async_fetch_token()\n    response = yield self._with_token(request)\n    if response.status_code == httpx.codes.UNAUTHORIZED:\n        self._token = await self._async_fetch_token()\n        yield self._with_token(request)\n</code></pre>"},{"location":"reference/mlclient/auth/MarkLogicCloudAuth/#mlclient.auth.MarkLogicCloudAuth.sync_auth_flow","title":"sync_auth_flow","text":"<pre><code>sync_auth_flow(request: Request)\n</code></pre> <p>Attach a Bearer token, fetching/refreshing it synchronously.</p> Source code in <code>mlclient/auth.py</code> <pre><code>def sync_auth_flow(\n    self,\n    request: httpx.Request,\n):\n    \"\"\"Attach a Bearer token, fetching/refreshing it synchronously.\"\"\"\n    if self._token is None:\n        self._token = self._fetch_token()\n    response = yield self._with_token(request)\n    if response.status_code == httpx.codes.UNAUTHORIZED:\n        self._token = self._fetch_token()\n        yield self._with_token(request)\n</code></pre>"},{"location":"reference/mlclient/auth/OAuthBearerAuth/","title":"OAuthBearerAuth","text":"<pre><code>from mlclient.auth import OAuthBearerAuth\n</code></pre>"},{"location":"reference/mlclient/auth/OAuthBearerAuth/#mlclient.auth.OAuthBearerAuth","title":"mlclient.auth.OAuthBearerAuth","text":"<pre><code>OAuthBearerAuth(token: str)\n</code></pre> <p>               Bases: <code>Auth</code></p> <p>OAuth 2.0 Bearer token authentication.</p> <p>Initialize OAuthBearerAuth with a pre-acquired Bearer token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>A pre-acquired OAuth 2.0 Bearer token.</p> required Source code in <code>mlclient/auth.py</code> <pre><code>def __init__(\n    self,\n    token: str,\n):\n    \"\"\"Initialize OAuthBearerAuth with a pre-acquired Bearer token.\n\n    Parameters\n    ----------\n    token : str\n        A pre-acquired OAuth 2.0 Bearer token.\n    \"\"\"\n    self._token = token\n</code></pre>"},{"location":"reference/mlclient/auth/OAuthBearerAuth/#mlclient.auth.OAuthBearerAuth.auth_flow","title":"auth_flow","text":"<pre><code>auth_flow(request: Request)\n</code></pre> <p>Attach the Bearer token to the request.</p> Source code in <code>mlclient/auth.py</code> <pre><code>def auth_flow(\n    self,\n    request: httpx.Request,\n):\n    \"\"\"Attach the Bearer token to the request.\"\"\"\n    request.headers[\"Authorization\"] = f\"Bearer {self._token}\"\n    yield request\n</code></pre>"},{"location":"reference/mlclient/calls/","title":"<code>mlclient.calls</code>","text":""},{"location":"reference/mlclient/calls/#mlclient.calls","title":"mlclient.calls","text":"<p>Public calls for MLClient.</p>"},{"location":"reference/mlclient/calls/ApiCall/","title":"ApiCall","text":"<pre><code>from mlclient.calls import ApiCall\n</code></pre>"},{"location":"reference/mlclient/calls/ApiCall/#mlclient.calls.ApiCall","title":"mlclient.calls.ApiCall","text":"<pre><code>ApiCall(\n    method: str = constants.METHOD_GET,\n    params: dict | None = None,\n    headers: dict | None = None,\n    body: str | dict | bytes | None = None,\n    accept: str | None = None,\n    content_type: str | None = None,\n)\n</code></pre> <p>An abstract class representing a single request to a MarkLogic API endpoint.</p> <p>Initialize ApiCall implementation instance.</p> <p>Parameters:</p> Name Type Description Default <code>method</code> <code>str</code> <p>a request method</p> <code>METHOD_GET</code> <code>params</code> <code>dict</code> <p>request parameters</p> <code>None</code> <code>headers</code> <code>dict</code> <p>request headers</p> <code>None</code> <code>body</code> <code>str | dict | bytes</code> <p>a request body</p> <code>None</code> <code>accept</code> <code>str</code> <p>an Accept header value</p> <code>None</code> <code>content_type</code> <code>str</code> <p>a Content-Type header value</p> <code>None</code> Source code in <code>mlclient/calls/base.py</code> <pre><code>def __init__(\n    self,\n    method: str = constants.METHOD_GET,\n    params: dict | None = None,\n    headers: dict | None = None,\n    body: str | dict | bytes | None = None,\n    accept: str | None = None,\n    content_type: str | None = None,\n):\n    \"\"\"Initialize ApiCall implementation instance.\n\n    Parameters\n    ----------\n    method : str\n        a request method\n    params : dict\n        request parameters\n    headers : dict\n        request headers\n    body : str | dict | bytes\n        a request body\n    accept : str\n        an Accept header value\n    content_type : str\n        a Content-Type header value\n    \"\"\"\n    self.method = method\n    self.params = params\n    self.headers = headers\n    self.body = body\n    if accept:\n        self.add_header(constants.HEADER_NAME_ACCEPT, accept)\n    if content_type:\n        self.add_header(constants.HEADER_NAME_CONTENT_TYPE, content_type)\n</code></pre>"},{"location":"reference/mlclient/calls/ApiCall/#mlclient.calls.ApiCall.body","title":"body  <code>property</code> <code>writable</code>","text":"<pre><code>body: str | dict | bytes | None\n</code></pre> <p>A request body.</p> <p>Returns:</p> Type Description <code>str | dict</code> <p>a request body</p>"},{"location":"reference/mlclient/calls/ApiCall/#mlclient.calls.ApiCall.endpoint","title":"endpoint  <code>abstractmethod</code> <code>property</code>","text":"<pre><code>endpoint: str\n</code></pre> <p>An endpoint of the ApiCall.</p> <p>Returns:</p> Type Description <code>str</code> <p>An endpoint</p>"},{"location":"reference/mlclient/calls/ApiCall/#mlclient.calls.ApiCall.headers","title":"headers  <code>property</code> <code>writable</code>","text":"<pre><code>headers: dict\n</code></pre> <p>Request headers.</p> <p>Returns:</p> Type Description <code>dict</code> <p>request headers</p>"},{"location":"reference/mlclient/calls/ApiCall/#mlclient.calls.ApiCall.method","title":"method  <code>property</code> <code>writable</code>","text":"<pre><code>method: str\n</code></pre> <p>A request method.</p> <p>Returns:</p> Type Description <code>str</code> <p>a request method</p>"},{"location":"reference/mlclient/calls/ApiCall/#mlclient.calls.ApiCall.params","title":"params  <code>property</code> <code>writable</code>","text":"<pre><code>params: dict\n</code></pre> <p>Request parameters.</p> <p>Returns:</p> Type Description <code>dict</code> <p>request parameters</p>"},{"location":"reference/mlclient/calls/ApiCall/#mlclient.calls.ApiCall.add_header","title":"add_header","text":"<pre><code>add_header(header_name: str, header_value: Any)\n</code></pre> <p>Put a request header if it's name and value exist.</p> <p>Parameters:</p> Name Type Description Default <code>header_name</code> <code>str</code> <p>a request header name</p> required <code>header_value</code> <code>Any</code> <p>a request header value</p> required Source code in <code>mlclient/calls/base.py</code> <pre><code>def add_header(\n    self,\n    header_name: str,\n    header_value: Any,\n):\n    \"\"\"Put a request header if it's name and value exist.\n\n    Parameters\n    ----------\n    header_name : str\n        a request header name\n    header_value : Any\n        a request header value\n    \"\"\"\n    if header_name and header_value:\n        self._headers[header_name] = header_value\n</code></pre>"},{"location":"reference/mlclient/calls/ApiCall/#mlclient.calls.ApiCall.add_param","title":"add_param","text":"<pre><code>add_param(param_name: str, param_value: Any)\n</code></pre> <p>Put a request parameter if it's name and value exist.</p> <p>Parameters:</p> Name Type Description Default <code>param_name</code> <code>str</code> <p>a request parameter name</p> required <code>param_value</code> <code>Any</code> <p>a request parameter value</p> required Source code in <code>mlclient/calls/base.py</code> <pre><code>def add_param(\n    self,\n    param_name: str,\n    param_value: Any,\n):\n    \"\"\"Put a request parameter if it's name and value exist.\n\n    Parameters\n    ----------\n    param_name : str\n        a request parameter name\n    param_value : Any\n        a request parameter value\n    \"\"\"\n    if param_name and param_value:\n        self._params[param_name] = param_value\n</code></pre>"},{"location":"reference/mlclient/calls/DatabaseDeleteCall/","title":"DatabaseDeleteCall","text":"<pre><code>from mlclient.calls import DatabaseDeleteCall\n</code></pre>"},{"location":"reference/mlclient/calls/DatabaseDeleteCall/#mlclient.calls.DatabaseDeleteCall","title":"mlclient.calls.DatabaseDeleteCall","text":"<pre><code>DatabaseDeleteCall(\n    database: str, forest_delete: str | None = None\n)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A DELETE request to remove a database from a cluster.</p> <p>An ApiCall implementation representing a single DELETE request to the /manage/v2/databases/{id|name} endpoint.</p> <p>This resource address deletes the named database from the cluster. The database can be identified either by id or name. Documentation of the REST Resource API: https://docs.marklogic.com/REST/DELETE/manage/v2/databases/[id-or-name]</p> <p>Initialize DatabaseDeleteCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>database</code> <code>str</code> <p>A database identifier. The database can be identified either by ID or name.</p> required <code>forest_delete</code> <code>str</code> <p>Specifies to delete the forests attached to the database. If unspecified, the forests will not be affected. If \"configuration\" is specified, the forest configuration will be removed but public forest data will remain. If \"data\" is specified, the forest configuration and data will be removed.</p> <code>None</code> Source code in <code>mlclient/calls/databases.py</code> <pre><code>def __init__(\n    self,\n    database: str,\n    forest_delete: str | None = None,\n):\n    \"\"\"Initialize DatabaseDeleteCall instance.\n\n    Parameters\n    ----------\n    database : str\n        A database identifier. The database can be identified either by ID or name.\n    forest_delete : str\n        Specifies to delete the forests attached to the database.\n        If unspecified, the forests will not be affected.\n        If \"configuration\" is specified, the forest configuration will be removed\n        but public forest data will remain.\n        If \"data\" is specified, the forest configuration and data will be removed.\n    \"\"\"\n    self._validate_params(forest_delete)\n    super().__init__(method=constants.METHOD_DELETE)\n    self.add_param(self._FOREST_DELETE_PARAM, forest_delete)\n    self._database = database\n</code></pre>"},{"location":"reference/mlclient/calls/DatabaseDeleteCall/#mlclient.calls.DatabaseDeleteCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Database call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Database call endpoint</p>"},{"location":"reference/mlclient/calls/DatabaseGetCall/","title":"DatabaseGetCall","text":"<pre><code>from mlclient.calls import DatabaseGetCall\n</code></pre>"},{"location":"reference/mlclient/calls/DatabaseGetCall/#mlclient.calls.DatabaseGetCall","title":"mlclient.calls.DatabaseGetCall","text":"<pre><code>DatabaseGetCall(\n    database: str,\n    data_format: str = \"xml\",\n    view: str = \"default\",\n)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A GET request to get database details.</p> <p>An ApiCall implementation representing a single GET request to the /manage/v2/databases/{id|name} endpoint.</p> <p>This resource address returns information on the specified database. The database can be identified either by ID or name. Documentation of the REST Resource API: https://docs.marklogic.com/REST/GET/manage/v2/databases/[id-or-name]</p> <p>Initialize DatabaseGetCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>database</code> <code>str</code> <p>A database identifier. The database can be identified either by ID or name.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default). This parameter is not meaningful with view=edit.</p> <code>'xml'</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be: properties-schema, package, describe, config, counts, edit, status, forest-storage, or default.</p> <code>'default'</code> Source code in <code>mlclient/calls/databases.py</code> <pre><code>def __init__(\n    self,\n    database: str,\n    data_format: str = \"xml\",\n    view: str = \"default\",\n):\n    \"\"\"Initialize DatabaseGetCall instance.\n\n    Parameters\n    ----------\n    database : str\n        A database identifier. The database can be identified either by ID or name.\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n        This parameter is not meaningful with view=edit.\n    view : str\n        A specific view of the returned data.\n        Can be: properties-schema, package, describe, config, counts, edit, status,\n        forest-storage, or default.\n    \"\"\"\n    data_format = data_format if data_format is not None else \"xml\"\n    view = view if view is not None else \"default\"\n    self._validate_params(data_format, view)\n\n    super().__init__(\n        method=\"GET\",\n        accept=utils.get_accept_header_for_format(data_format),\n    )\n    self._database = database\n    self.add_param(self._FORMAT_PARAM, data_format)\n    self.add_param(self._VIEW_PARAM, view)\n</code></pre>"},{"location":"reference/mlclient/calls/DatabaseGetCall/#mlclient.calls.DatabaseGetCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Database call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Database call endpoint</p>"},{"location":"reference/mlclient/calls/DatabasePostCall/","title":"DatabasePostCall","text":"<pre><code>from mlclient.calls import DatabasePostCall\n</code></pre>"},{"location":"reference/mlclient/calls/DatabasePostCall/#mlclient.calls.DatabasePostCall","title":"mlclient.calls.DatabasePostCall","text":"<pre><code>DatabasePostCall(database: str, body: str | dict)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A POST request to manage a database.</p> <p>An ApiCall implementation representing a single POST request to the /manage/v2/databases/{id|name} endpoint.</p> <p>This resource address can be used to clear the contents of the named database and to perform various configuration operations on the database. The database can be identified either by id or name. Documentation of the REST Resource API: https://docs.marklogic.com/REST/POST/manage/v2/databases/[id-or-name]</p> <p>Initialize DatabasePostCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>database</code> <code>str</code> <p>A database identifier. The database can be identified either by ID or name.</p> required <code>body</code> <code>str | dict</code> <p>A database properties in XML or JSON format.</p> required Source code in <code>mlclient/calls/databases.py</code> <pre><code>def __init__(\n    self,\n    database: str,\n    body: str | dict,\n):\n    \"\"\"Initialize DatabasePostCall instance.\n\n    Parameters\n    ----------\n    database : str\n        A database identifier. The database can be identified either by ID or name.\n    body : str | dict\n        A database properties in XML or JSON format.\n    \"\"\"\n    self._validate_params(body)\n    content_type = utils.get_content_type_header_for_data(body)\n    if content_type == constants.HEADER_JSON and isinstance(body, str):\n        body = json.loads(body)\n    super().__init__(method=\"POST\", content_type=content_type, body=body)\n    self._database = database\n</code></pre>"},{"location":"reference/mlclient/calls/DatabasePostCall/#mlclient.calls.DatabasePostCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Database call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Database call endpoint</p>"},{"location":"reference/mlclient/calls/DatabasePropertiesGetCall/","title":"DatabasePropertiesGetCall","text":"<pre><code>from mlclient.calls import DatabasePropertiesGetCall\n</code></pre>"},{"location":"reference/mlclient/calls/DatabasePropertiesGetCall/#mlclient.calls.DatabasePropertiesGetCall","title":"mlclient.calls.DatabasePropertiesGetCall","text":"<pre><code>DatabasePropertiesGetCall(\n    database: str, data_format: str = \"xml\"\n)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A GET request to get a database properties.</p> <p>An ApiCall implementation representing a single GET request to the /manage/v2/databases/{id|name}/properties endpoint.</p> <p>This resource address returns the modifiable properties of the named database. The database can be identified either by ID or name. Documentation of the REST Resource API: https://docs.marklogic.com/REST/GET/manage/v2/databases/[id-or-name]/properties</p> <p>Initialize DatabasePropertiesGetCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>database</code> <code>str</code> <p>A database identifier. The database can be identified either by ID or name.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either json or xml (default). This parameter overrides the Accept header if both are present.</p> <code>'xml'</code> Source code in <code>mlclient/calls/databases.py</code> <pre><code>def __init__(\n    self,\n    database: str,\n    data_format: str = \"xml\",\n):\n    \"\"\"Initialize DatabasePropertiesGetCall instance.\n\n    Parameters\n    ----------\n    database : str\n        A database identifier. The database can be identified either by ID or name.\n    data_format : str\n        The format of the returned data. Can be either json or xml (default).\n        This parameter overrides the Accept header if both are present.\n    \"\"\"\n    data_format = data_format if data_format is not None else \"xml\"\n    self._validate_params(data_format)\n\n    super().__init__(\n        method=\"GET\",\n        accept=utils.get_accept_header_for_format(data_format),\n    )\n    self._database = database\n    self.add_param(self._FORMAT_PARAM, data_format)\n</code></pre>"},{"location":"reference/mlclient/calls/DatabasePropertiesGetCall/#mlclient.calls.DatabasePropertiesGetCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Database Properties call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Database Properties call endpoint</p>"},{"location":"reference/mlclient/calls/DatabasePropertiesPutCall/","title":"DatabasePropertiesPutCall","text":"<pre><code>from mlclient.calls import DatabasePropertiesPutCall\n</code></pre>"},{"location":"reference/mlclient/calls/DatabasePropertiesPutCall/#mlclient.calls.DatabasePropertiesPutCall","title":"mlclient.calls.DatabasePropertiesPutCall","text":"<pre><code>DatabasePropertiesPutCall(database: str, body: str | dict)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A PUT request to modify database properties.</p> <p>An ApiCall implementation representing a single PUT request to the /manage/v2/databases/{id|name}/properties endpoint.</p> <p>This resource address modifies the properties of the named database. The list of modifiable properties can be returned by the GET version of this endpoint. The database can be identified either by id or name. Documentation of the REST Resource API: https://docs.marklogic.com/REST/PUT/manage/v2/databases/[id-or-name]/properties</p> <p>Initialize DatabasePropertiesPutCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>database</code> <code>str</code> <p>A database identifier. The database can be identified either by ID or name.</p> required <code>body</code> <code>str | dict</code> <p>A database properties in XML or JSON format.</p> required Source code in <code>mlclient/calls/databases.py</code> <pre><code>def __init__(\n    self,\n    database: str,\n    body: str | dict,\n):\n    \"\"\"Initialize DatabasePropertiesPutCall instance.\n\n    Parameters\n    ----------\n    database : str\n        A database identifier. The database can be identified either by ID or name.\n    body : str | dict\n        A database properties in XML or JSON format.\n    \"\"\"\n    self._validate_params(body)\n    content_type = utils.get_content_type_header_for_data(body)\n    if content_type == constants.HEADER_JSON and isinstance(body, str):\n        body = json.loads(body)\n    super().__init__(method=\"PUT\", content_type=content_type, body=body)\n    self._database = database\n</code></pre>"},{"location":"reference/mlclient/calls/DatabasePropertiesPutCall/#mlclient.calls.DatabasePropertiesPutCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Database Properties call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Database Properties call endpoint</p>"},{"location":"reference/mlclient/calls/DatabasesGetCall/","title":"DatabasesGetCall","text":"<pre><code>from mlclient.calls import DatabasesGetCall\n</code></pre>"},{"location":"reference/mlclient/calls/DatabasesGetCall/#mlclient.calls.DatabasesGetCall","title":"mlclient.calls.DatabasesGetCall","text":"<pre><code>DatabasesGetCall(\n    data_format: str = \"xml\", view: str = \"default\"\n)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A GET request to get databases summary.</p> <p>An ApiCall implementation representing a single GET request to the /manage/v2/databases endpoint.</p> <p>This resource address returns a summary of the databases in the cluster. Documentation of the REST Resource API: https://docs.marklogic.com/REST/GET/manage/v2/databases</p> <p>Initialize DatabasesGetCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default).</p> <code>'xml'</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be schema, properties-schema, metrics, package, describe, or default.</p> <code>'default'</code> Source code in <code>mlclient/calls/databases.py</code> <pre><code>def __init__(\n    self,\n    data_format: str = \"xml\",\n    view: str = \"default\",\n):\n    \"\"\"Initialize DatabasesGetCall instance.\n\n    Parameters\n    ----------\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n    view : str\n        A specific view of the returned data.\n        Can be schema, properties-schema, metrics, package, describe, or default.\n    \"\"\"\n    data_format = data_format if data_format is not None else \"xml\"\n    view = view if view is not None else \"default\"\n    self._validate_params(data_format, view)\n\n    super().__init__(\n        method=\"GET\",\n        accept=utils.get_accept_header_for_format(data_format),\n    )\n    self.add_param(self._FORMAT_PARAM, data_format)\n    self.add_param(self._VIEW_PARAM, view)\n</code></pre>"},{"location":"reference/mlclient/calls/DatabasesGetCall/#mlclient.calls.DatabasesGetCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Databases call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Databases call endpoint</p>"},{"location":"reference/mlclient/calls/DatabasesPostCall/","title":"DatabasesPostCall","text":"<pre><code>from mlclient.calls import DatabasesPostCall\n</code></pre>"},{"location":"reference/mlclient/calls/DatabasesPostCall/#mlclient.calls.DatabasesPostCall","title":"mlclient.calls.DatabasesPostCall","text":"<pre><code>DatabasesPostCall(body: str | dict)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A POST request to create a new database.</p> <p>An ApiCall implementation representing a single POST request to the /manage/v2/databases endpoint.</p> <p>This resource address creates a new database in the cluster. Documentation of the REST Resource API: https://docs.marklogic.com/REST/POST/manage/v2/databases</p> <p>Initialize DatabasesPostCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>body</code> <code>str | dict</code> <p>A database properties in XML or JSON format.</p> required Source code in <code>mlclient/calls/databases.py</code> <pre><code>def __init__(\n    self,\n    body: str | dict,\n):\n    \"\"\"Initialize DatabasesPostCall instance.\n\n    Parameters\n    ----------\n    body : str | dict\n        A database properties in XML or JSON format.\n    \"\"\"\n    self._validate_params(body)\n    content_type = utils.get_content_type_header_for_data(body)\n    if content_type == constants.HEADER_JSON and isinstance(body, str):\n        body = json.loads(body)\n    super().__init__(method=\"POST\", content_type=content_type, body=body)\n</code></pre>"},{"location":"reference/mlclient/calls/DatabasesPostCall/#mlclient.calls.DatabasesPostCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Databases call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Databases call endpoint</p>"},{"location":"reference/mlclient/calls/DocumentsDeleteCall/","title":"DocumentsDeleteCall","text":"<pre><code>from mlclient.calls import DocumentsDeleteCall\n</code></pre>"},{"location":"reference/mlclient/calls/DocumentsDeleteCall/#mlclient.calls.DocumentsDeleteCall","title":"mlclient.calls.DocumentsDeleteCall","text":"<pre><code>DocumentsDeleteCall(\n    uri: str | list,\n    database: str | None = None,\n    category: str | list | None = None,\n    txid: str | None = None,\n    temporal_collection: str | None = None,\n    system_time: str | None = None,\n    wipe_temporal: bool | None = None,\n)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A DELETE request to remove documents, or reset document metadata.</p> <p>An ApiCall implementation representing a single DELETE request to the /v1/documents endpoint.</p> <p>Remove documents, or reset document metadata. Documentation of the REST Resource API: https://docs.marklogic.com/REST/DELETE/v1/documents</p> <p>Initialize DocumentsDeleteCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>uri</code> <code>str | list</code> <p>The URI of a document to delete or for which to remove metadata. You can specify multiple documents.</p> required <code>database</code> <code>str</code> <p>Perform this operation on the named content database instead of the default content database associated with the REST API instance. Using an alternative database requires the \"eval-in\" privilege.</p> <code>None</code> <code>category</code> <code>str | list</code> <p>The category of data to remove/reset. Category may be specified multiple times to remove or reset any combination of content and metadata. Valid categories: content (default), metadata, metadata-values, collections, permissions, properties, and quality. Use metadata to reset all metadata.</p> <code>None</code> <code>txid</code> <code>str</code> <p>The transaction identifier of the multi-statement transaction in which to service this request. Use the /transactions service to create and manage multi-statement transactions.</p> <code>None</code> <code>temporal_collection</code> <code>str</code> <p>Specify the name of a temporal collection that contains the document(s) to be deleted. Applies to all documents when deleting more than one.</p> <code>None</code> <code>system_time</code> <code>str</code> <p>Set the system start time for the insertion or update. This time will override the system time set by MarkLogic. Ignored if temporal-collection is not included in the request. Applies to all documents when deleting more than one.</p> <code>None</code> <code>wipe_temporal</code> <code>bool</code> <p>Remove all versions of a temporal document rather than performing a temporal delete. You can only use this parameter when you also specify a temporal-collection parameter.</p> <code>None</code> Source code in <code>mlclient/calls/documents.py</code> <pre><code>def __init__(\n    self,\n    uri: str | list,\n    database: str | None = None,\n    category: str | list | None = None,\n    txid: str | None = None,\n    temporal_collection: str | None = None,\n    system_time: str | None = None,\n    wipe_temporal: bool | None = None,\n):\n    \"\"\"Initialize DocumentsDeleteCall instance.\n\n    Parameters\n    ----------\n    uri : str | list\n        The URI of a document to delete or for which to remove metadata.\n        You can specify multiple documents.\n    database : str\n        Perform this operation on the named content database instead\n        of the default content database associated with the REST API instance.\n        Using an alternative database requires the \"eval-in\" privilege.\n    category : str | list\n        The category of data to remove/reset.\n        Category may be specified multiple times to remove or reset\n        any combination of content and metadata.\n        Valid categories: content (default), metadata, metadata-values,\n        collections, permissions, properties, and quality.\n        Use metadata to reset all metadata.\n    txid : str\n        The transaction identifier of the multi-statement transaction in which\n        to service this request. Use the /transactions service to create and manage\n        multi-statement transactions.\n    temporal_collection : str\n        Specify the name of a temporal collection that contains the document(s)\n        to be deleted. Applies to all documents when deleting more than one.\n    system_time : str\n        Set the system start time for the insertion or update.\n        This time will override the system time set by MarkLogic.\n        Ignored if temporal-collection is not included in the request.\n        Applies to all documents when deleting more than one.\n    wipe_temporal : bool\n        Remove all versions of a temporal document rather than performing\n        a temporal delete. You can only use this parameter when you also specify\n        a temporal-collection parameter.\n    \"\"\"\n    self._validate_params(category)\n\n    super().__init__(method=\"DELETE\")\n    self.add_param(self._URI_PARAM, uri)\n    self.add_param(self._DATABASE_PARAM, database)\n    self.add_param(self._CATEGORY_PARAM, category)\n    self.add_param(self._TXID_PARAM, txid)\n    self.add_param(self._TEMPORAL_COLLECTION_PARAM, temporal_collection)\n    self.add_param(self._SYSTEM_TIME_PARAM, system_time)\n    if wipe_temporal is True:\n        self.add_param(self._RESULT_PARAM, \"wiped\")\n</code></pre>"},{"location":"reference/mlclient/calls/DocumentsDeleteCall/#mlclient.calls.DocumentsDeleteCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Documents call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Documents call endpoint</p>"},{"location":"reference/mlclient/calls/DocumentsGetCall/","title":"DocumentsGetCall","text":"<pre><code>from mlclient.calls import DocumentsGetCall\n</code></pre>"},{"location":"reference/mlclient/calls/DocumentsGetCall/#mlclient.calls.DocumentsGetCall","title":"mlclient.calls.DocumentsGetCall","text":"<pre><code>DocumentsGetCall(\n    uri: str | list,\n    database: str | None = None,\n    category: str | list | None = None,\n    data_format: str | None = None,\n    timestamp: str | None = None,\n    transform: str | None = None,\n    transform_params: dict | None = None,\n    txid: str | None = None,\n)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A GET request to retrieve documents' content or metadata.</p> <p>An ApiCall implementation representing a single GET request to the /v1/documents endpoint.</p> <p>Retrieve document content and/or metadata from the database. Documentation of the REST Resource API: https://docs.marklogic.com/REST/GET/v1/documents</p> <p>Initialize DocumentsGetCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>uri</code> <code>str | list</code> <p>One or more URIs for documents in the database. If you specify multiple URIs, the Accept header must be multipart/mixed.</p> required <code>database</code> <code>str</code> <p>Perform this operation on the named content database instead of the default content database associated with the REST API instance. Using an alternative database requires the \"eval-in\" privilege.</p> <code>None</code> <code>category</code> <code>str | list</code> <p>The category of data to fetch about the requested document. Category can be specified multiple times to retrieve any combination of content and metadata. Valid categories: content (default), metadata, metadata-values, collections, permissions, properties, and quality. Use metadata to request all categories except content.</p> <code>None</code> <code>data_format</code> <code>str</code> <p>The expected format of metadata returned in the response. Accepted values: xml or json. This parameter does not affect document content. For metadata, this parameter overrides the MIME type in the Accept header, except when the Accept header is multipart/mixed.</p> <code>None</code> <code>timestamp</code> <code>str</code> <p>A timestamp returned in the ML-Effective-Timestamp header of a previous request. Use this parameter to fetch documents based on the contents of the database at a fixed point-in-time.</p> <code>None</code> <code>transform</code> <code>str</code> <p>Names a content transformation previously installed via the /config/transforms service. The service applies the transformation to all documents prior to constructing the response.</p> <code>None</code> <code>transform_params</code> <code>dict</code> <p>A transform parameter names and values. For example, { \"myparam\": 1 }. Transform parameters are passed to the transform named in the transform parameter.</p> <code>None</code> <code>txid</code> <code>str</code> <p>The transaction identifier of the multi-statement transaction in which to service this request. Use the /transactions service to create and manage multi-statement transactions.</p> <code>None</code> Source code in <code>mlclient/calls/documents.py</code> <pre><code>def __init__(\n    self,\n    uri: str | list,\n    database: str | None = None,\n    category: str | list | None = None,\n    data_format: str | None = None,\n    timestamp: str | None = None,\n    transform: str | None = None,\n    transform_params: dict | None = None,\n    txid: str | None = None,\n):\n    \"\"\"Initialize DocumentsGetCall instance.\n\n    Parameters\n    ----------\n    uri : str | list\n        One or more URIs for documents in the database.\n        If you specify multiple URIs, the Accept header must be multipart/mixed.\n    database : str\n        Perform this operation on the named content database instead\n        of the default content database associated with the REST API instance.\n        Using an alternative database requires the \"eval-in\" privilege.\n    category : str | list\n        The category of data to fetch about the requested document.\n        Category can be specified multiple times to retrieve any combination\n        of content and metadata. Valid categories: content (default), metadata,\n        metadata-values, collections, permissions, properties, and quality.\n        Use metadata to request all categories except content.\n    data_format : str\n        The expected format of metadata returned in the response.\n        Accepted values: xml or json.\n        This parameter does not affect document content.\n        For metadata, this parameter overrides the MIME type in the Accept header,\n        except when the Accept header is multipart/mixed.\n    timestamp : str\n        A timestamp returned in the ML-Effective-Timestamp header of a previous\n        request. Use this parameter to fetch documents based on the contents\n        of the database at a fixed point-in-time.\n    transform : str\n        Names a content transformation previously installed via\n        the /config/transforms service. The service applies the transformation\n        to all documents prior to constructing the response.\n    transform_params : dict\n        A transform parameter names and values. For example, { \"myparam\": 1 }.\n        Transform parameters are passed to the transform named in the transform\n        parameter.\n    txid : str\n        The transaction identifier of the multi-statement transaction in which\n        to service this request. Use the /transactions service to create and manage\n        multi-statement transactions.\n    \"\"\"\n    self._validate_params(category, data_format)\n\n    super().__init__(method=\"GET\")\n    accept_header = self._get_accept_header(uri, category, data_format)\n    self.add_header(constants.HEADER_NAME_ACCEPT, accept_header)\n    self.add_param(self._URI_PARAM, uri)\n    self.add_param(self._DATABASE_PARAM, database)\n    self.add_param(self._CATEGORY_PARAM, category)\n    self.add_param(self._FORMAT_PARAM, data_format)\n    self.add_param(self._TIMESTAMP_PARAM, timestamp)\n    self.add_param(self._TRANSFORM_PARAM, transform)\n    self.add_param(self._TXID_PARAM, txid)\n    if transform_params:\n        for trans_param_name, value in transform_params.items():\n            param = self._TRANS_PARAM_PREFIX + trans_param_name\n            self.add_param(param, value)\n</code></pre>"},{"location":"reference/mlclient/calls/DocumentsGetCall/#mlclient.calls.DocumentsGetCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Documents call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Documents call endpoint</p>"},{"location":"reference/mlclient/calls/DocumentsPostCall/","title":"DocumentsPostCall","text":"<pre><code>from mlclient.calls import DocumentsPostCall\n</code></pre>"},{"location":"reference/mlclient/calls/DocumentsPostCall/#mlclient.calls.DocumentsPostCall","title":"mlclient.calls.DocumentsPostCall","text":"<pre><code>DocumentsPostCall(\n    body_parts: list[DocumentsBodyPart],\n    database: str | None = None,\n    transform: str | None = None,\n    transform_params: dict | None = None,\n    txid: str | None = None,\n    temporal_collection: str | None = None,\n    system_time: str | None = None,\n)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A POST request to insert or update documents' content or metadata.</p> <p>An ApiCall implementation representing a single POST request to the /v1/documents endpoint.</p> <p>Insert or update content and/or metadata for multiple documents in a single request. Documentation of the REST Resource API: https://docs.marklogic.com/REST/POST/v1/documents</p> <p>Initialize DocumentsPostCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>body_parts</code> <code>list[DocumentsBodyPart]</code> <p>A list of multipart request body parts</p> required <code>database</code> <code>str</code> <p>Perform this operation on the named content database instead of the default content database associated with the REST API instance. Using an alternative database requires the \"eval-in\" privilege.</p> <code>None</code> <code>transform</code> <code>str</code> <p>Names a content transformation previously installed via the /config/transforms service. The service applies the transformation to all documents prior to constructing the response.</p> <code>None</code> <code>transform_params</code> <code>dict</code> <p>A transform parameter names and values. For example, { \"myparam\": 1 }. Transform parameters are passed to the transform named in the transform parameter.</p> <code>None</code> <code>txid</code> <code>str</code> <p>The transaction identifier of the multi-statement transaction in which to service this request. Use the /transactions service to create and manage multi-statement transactions.</p> <code>None</code> <code>temporal_collection</code> <code>str</code> <p>Specify the name of a temporal collection into which the documents are to be inserted.</p> <code>None</code> <code>system_time</code> <code>str</code> <p>Set the system start time for the insertion or update. This time will override the system time set by MarkLogic. Ignored if temporal-collection is not included in the request.</p> <code>None</code> Source code in <code>mlclient/calls/documents.py</code> <pre><code>def __init__(\n    self,\n    body_parts: list[DocumentsBodyPart],\n    database: str | None = None,\n    transform: str | None = None,\n    transform_params: dict | None = None,\n    txid: str | None = None,\n    temporal_collection: str | None = None,\n    system_time: str | None = None,\n):\n    \"\"\"Initialize DocumentsPostCall instance.\n\n    Parameters\n    ----------\n    body_parts : list[DocumentsBodyPart]\n        A list of multipart request body parts\n    database : str\n        Perform this operation on the named content database instead\n        of the default content database associated with the REST API instance.\n        Using an alternative database requires the \"eval-in\" privilege.\n    transform : str\n        Names a content transformation previously installed via\n        the /config/transforms service. The service applies the transformation\n        to all documents prior to constructing the response.\n    transform_params : dict\n        A transform parameter names and values. For example, { \"myparam\": 1 }.\n        Transform parameters are passed to the transform named in the transform\n        parameter.\n    txid : str\n        The transaction identifier of the multi-statement transaction in which\n        to service this request. Use the /transactions service to create and manage\n        multi-statement transactions.\n    temporal_collection : str\n        Specify the name of a temporal collection into which the documents are\n        to be inserted.\n    system_time : str\n        Set the system start time for the insertion or update.\n        This time will override the system time set by MarkLogic.\n        Ignored if temporal-collection is not included in the request.\n    \"\"\"\n    self._validate_params(body_parts)\n\n    super().__init__(method=\"POST\")\n    self.add_header(constants.HEADER_NAME_ACCEPT, constants.HEADER_JSON)\n    self.add_param(self._DATABASE_PARAM, database)\n    self.add_param(self._TRANSFORM_PARAM, transform)\n    self.add_param(self._TXID_PARAM, txid)\n    self.add_param(self._TEMPORAL_COLLECTION_PARAM, temporal_collection)\n    self.add_param(self._SYSTEM_TIME_PARAM, system_time)\n    if transform_params:\n        for trans_param_name, value in transform_params.items():\n            param = self._TRANS_PARAM_PREFIX + trans_param_name\n            self.add_param(param, value)\n    body, content_type = self._build_body(body_parts)\n    self.add_header(constants.HEADER_NAME_CONTENT_TYPE, content_type)\n    self.body = body\n</code></pre>"},{"location":"reference/mlclient/calls/DocumentsPostCall/#mlclient.calls.DocumentsPostCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Documents call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Documents call endpoint</p>"},{"location":"reference/mlclient/calls/EvalCall/","title":"EvalCall","text":"<pre><code>from mlclient.calls import EvalCall\n</code></pre>"},{"location":"reference/mlclient/calls/EvalCall/#mlclient.calls.EvalCall","title":"mlclient.calls.EvalCall","text":"<pre><code>EvalCall(\n    xquery: str | None = None,\n    javascript: str | None = None,\n    variables: dict | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A POST request to evaluate an ad-hoc query.</p> <p>An ApiCall implementation representing a single request to the /v1/eval endpoint.</p> <p>Evaluate an ad-hoc query expressed using XQuery or server-side JavaScript. Documentation of the REST Resource API: https://docs.marklogic.com/REST/POST/v1/eval</p> <p>Initialize EvalCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>xquery</code> <code>str</code> <p>The query to evaluate, expressed using XQuery. You must include either this parameter or the javascript parameter, but not both.</p> <code>None</code> <code>javascript</code> <code>str</code> <p>The query to evaluate, expressed using server-side JavaScript. You must include either this parameter or the xquery parameter, but not both.</p> <code>None</code> <code>variables</code> <code>dict | None</code> <p>External variables to pass to the query during evaluation</p> <code>None</code> <code>database</code> <code>str | None</code> <p>Perform this operation on the named content database instead of the default content database associated with the REST API instance. The database can be identified by name or by database id.</p> <code>None</code> <code>txid</code> <code>str | None</code> <p>The transaction identifier of the multi-statement transaction in which to service this request.</p> <code>None</code> Source code in <code>mlclient/calls/eval.py</code> <pre><code>def __init__(\n    self,\n    xquery: str | None = None,\n    javascript: str | None = None,\n    variables: dict | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n):\n    \"\"\"Initialize EvalCall instance.\n\n    Parameters\n    ----------\n    xquery : str\n        The query to evaluate, expressed using XQuery.\n        You must include either this parameter or the javascript parameter,\n        but not both.\n    javascript : str\n        The query to evaluate, expressed using server-side JavaScript.\n        You must include either this parameter or the xquery parameter,\n        but not both.\n    variables\n        External variables to pass to the query during evaluation\n    database\n        Perform this operation on the named content database\n        instead of the default content database associated with the REST API\n        instance. The database can be identified by name or by database id.\n    txid\n        The transaction identifier of the multi-statement transaction\n        in which to service this request.\n    \"\"\"\n    self._validate_params(xquery, javascript)\n\n    super().__init__(\n        method=constants.METHOD_POST,\n        accept=constants.HEADER_MULTIPART_MIXED,\n        content_type=constants.HEADER_X_WWW_FORM_URLENCODED,\n    )\n    self.add_param(self._DATABASE_PARAM, database)\n    self.add_param(self._TXID_PARAM, txid)\n    self.body = self._build_body(xquery, javascript, variables)\n</code></pre>"},{"location":"reference/mlclient/calls/EvalCall/#mlclient.calls.EvalCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Eval call.</p> <p>Returns:</p> Type Description <code>str</code> <p>An Eval call endpoint</p>"},{"location":"reference/mlclient/calls/ForestDeleteCall/","title":"ForestDeleteCall","text":"<pre><code>from mlclient.calls import ForestDeleteCall\n</code></pre>"},{"location":"reference/mlclient/calls/ForestDeleteCall/#mlclient.calls.ForestDeleteCall","title":"mlclient.calls.ForestDeleteCall","text":"<pre><code>ForestDeleteCall(\n    forest: str, level: str, replicas: str | None = None\n)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A DELETE request to remove a forest.</p> <p>An ApiCall implementation representing a single DELETE request to the /manage/v2/forests/{id|name} endpoint.</p> <p>Delete a forest. Documentation of the REST Resource API: https://docs.marklogic.com/REST/DELETE/manage/v2/forests/[id-or-name]</p> <p>Initialize ForestDeleteCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>forest</code> <code>str</code> <p>A forest identifier. The forest can be identified either by ID or name.</p> required <code>level</code> <code>str</code> <p>The type of state change to initiate. Allowed values: full, config-only. A config-only deletion removes only the forest configuration; the data contained in the forest remains on disk. A full deletion removes both the forest configuration and the data.</p> required <code>replicas</code> <code>str</code> <p>Determines how to process the replicas. Allowed values: detach to detach the replica but keep it; delete to detach and delete the replica.</p> <code>None</code> Source code in <code>mlclient/calls/forests.py</code> <pre><code>def __init__(\n    self,\n    forest: str,\n    level: str,\n    replicas: str | None = None,\n):\n    \"\"\"Initialize ForestDeleteCall instance.\n\n    Parameters\n    ----------\n    forest : str\n        A forest identifier. The forest can be identified either by ID or name.\n    level : str\n        The type of state change to initiate. Allowed values: full, config-only.\n        A config-only deletion removes only the forest configuration;\n        the data contained in the forest remains on disk.\n        A full deletion removes both the forest configuration and the data.\n    replicas : str\n        Determines how to process the replicas.\n        Allowed values: detach to detach the replica but keep it; delete to detach\n        and delete the replica.\n    \"\"\"\n    self._validate_params(level, replicas)\n    super().__init__(method=constants.METHOD_DELETE)\n    self.add_param(self._LEVEL_PARAM, level)\n    self.add_param(self._REPLICAS_PARAM, replicas)\n    self._forest = forest\n</code></pre>"},{"location":"reference/mlclient/calls/ForestDeleteCall/#mlclient.calls.ForestDeleteCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Forest call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Forest call endpoint</p>"},{"location":"reference/mlclient/calls/ForestGetCall/","title":"ForestGetCall","text":"<pre><code>from mlclient.calls import ForestGetCall\n</code></pre>"},{"location":"reference/mlclient/calls/ForestGetCall/#mlclient.calls.ForestGetCall","title":"mlclient.calls.ForestGetCall","text":"<pre><code>ForestGetCall(\n    forest: str,\n    data_format: str = \"xml\",\n    view: str = \"default\",\n)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A GET request to get a forest details.</p> <p>An ApiCall implementation representing a single GET request to the /manage/v2/forests/{id|name} endpoint.</p> <p>Retrieve information about a forest. The forest can be identified either by id or name. Documentation of the REST Resource API: https://docs.marklogic.com/REST/GET/manage/v2/forests/[id-or-name]</p> <p>Initialize ForestGetCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>forest</code> <code>str</code> <p>A forest identifier. The forest can be identified either by ID or name.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default).</p> <code>'xml'</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be properties-schema, config, edit, package, describe, status, xdmp:server-status or default.</p> <code>'default'</code> Source code in <code>mlclient/calls/forests.py</code> <pre><code>def __init__(\n    self,\n    forest: str,\n    data_format: str = \"xml\",\n    view: str = \"default\",\n):\n    \"\"\"Initialize ForestGetCall instance.\n\n    Parameters\n    ----------\n    forest : str\n        A forest identifier. The forest can be identified either by ID or name.\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n    view : str\n        A specific view of the returned data.\n        Can be properties-schema, config, edit, package, describe, status,\n        xdmp:server-status or default.\n    \"\"\"\n    data_format = data_format if data_format is not None else \"xml\"\n    view = view if view is not None else \"default\"\n    self._validate_params(data_format, view)\n\n    super().__init__(\n        method=\"GET\",\n        accept=utils.get_accept_header_for_format(data_format),\n    )\n    self._forest = forest\n    self.add_param(self._FORMAT_PARAM, data_format)\n    self.add_param(self._VIEW_PARAM, view)\n</code></pre>"},{"location":"reference/mlclient/calls/ForestGetCall/#mlclient.calls.ForestGetCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Forest call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Forest call endpoint</p>"},{"location":"reference/mlclient/calls/ForestPostCall/","title":"ForestPostCall","text":"<pre><code>from mlclient.calls import ForestPostCall\n</code></pre>"},{"location":"reference/mlclient/calls/ForestPostCall/#mlclient.calls.ForestPostCall","title":"mlclient.calls.ForestPostCall","text":"<pre><code>ForestPostCall(forest: str, body: dict)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A POST request to change a forest's state.</p> <p>An ApiCall implementation representing a single POST request to the /manage/v2/forests/{id|name} endpoint.</p> <p>Initiate a state change on a forest, such as a merge, restart, or attach. Documentation of the REST Resource API: https://docs.marklogic.com/REST/POST/manage/v2/forests/[id-or-name]</p> <p>Initialize ForestPostCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>forest</code> <code>str</code> <p>A forest identifier. The forest can be identified either by ID or name.</p> required <code>body</code> <code>dict</code> <p>A list of properties. Need to include the 'state' property (the type of state change to initiate). Allowed values: clear, merge, restart, attach, detach, retire, employ.</p> required Source code in <code>mlclient/calls/forests.py</code> <pre><code>def __init__(\n    self,\n    forest: str,\n    body: dict,\n):\n    \"\"\"Initialize ForestPostCall instance.\n\n    Parameters\n    ----------\n    forest : str\n        A forest identifier. The forest can be identified either by ID or name.\n    body : dict\n        A list of properties. Need to include the 'state' property (the type\n        of state change to initiate).\n        Allowed values: clear, merge, restart, attach, detach, retire, employ.\n    \"\"\"\n    self._validate_params(body.get(self._STATE_PARAM))\n    super().__init__(\n        method=\"POST\",\n        content_type=constants.HEADER_X_WWW_FORM_URLENCODED,\n        body=body,\n    )\n    self._forest = forest\n</code></pre>"},{"location":"reference/mlclient/calls/ForestPostCall/#mlclient.calls.ForestPostCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Forests call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Forests call endpoint</p>"},{"location":"reference/mlclient/calls/ForestPropertiesGetCall/","title":"ForestPropertiesGetCall","text":"<pre><code>from mlclient.calls import ForestPropertiesGetCall\n</code></pre>"},{"location":"reference/mlclient/calls/ForestPropertiesGetCall/#mlclient.calls.ForestPropertiesGetCall","title":"mlclient.calls.ForestPropertiesGetCall","text":"<pre><code>ForestPropertiesGetCall(\n    forest: str, data_format: str = \"xml\"\n)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A GET request to get forest properties.</p> <p>An ApiCall implementation representing a single GET request to the /manage/v2/forests/{id|name}/properties endpoint.</p> <p>Retrieve the current state of modifiable properties of the forest identified by {id|name}. Documentation of the REST Resource API: https://docs.marklogic.com/REST/GET/manage/v2/forests/[id-or-name]/properties</p> <p>Initialize ForestPropertiesGetCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>forest</code> <code>str</code> <p>A forest identifier. The forest can be identified either by ID or name.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either json or xml (default). This parameter overrides the Accept header if both are present.</p> <code>'xml'</code> Source code in <code>mlclient/calls/forests.py</code> <pre><code>def __init__(\n    self,\n    forest: str,\n    data_format: str = \"xml\",\n):\n    \"\"\"Initialize ForestPropertiesGetCall instance.\n\n    Parameters\n    ----------\n    forest : str\n        A forest identifier. The forest can be identified either by ID or name.\n    data_format : str\n        The format of the returned data. Can be either json or xml (default).\n        This parameter overrides the Accept header if both are present.\n    \"\"\"\n    data_format = data_format if data_format is not None else \"xml\"\n    self._validate_params(data_format)\n\n    super().__init__(\n        method=\"GET\",\n        accept=utils.get_accept_header_for_format(data_format),\n    )\n    self._forest = forest\n    self.add_param(self._FORMAT_PARAM, data_format)\n</code></pre>"},{"location":"reference/mlclient/calls/ForestPropertiesGetCall/#mlclient.calls.ForestPropertiesGetCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Forest Properties call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Forest Properties call endpoint</p>"},{"location":"reference/mlclient/calls/ForestPropertiesPutCall/","title":"ForestPropertiesPutCall","text":"<pre><code>from mlclient.calls import ForestPropertiesPutCall\n</code></pre>"},{"location":"reference/mlclient/calls/ForestPropertiesPutCall/#mlclient.calls.ForestPropertiesPutCall","title":"mlclient.calls.ForestPropertiesPutCall","text":"<pre><code>ForestPropertiesPutCall(forest: str, body: str | dict)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A PUT request to modify forest properties.</p> <p>An ApiCall implementation representing a single PUT request to the /manage/v2/forests/{id|name}/properties endpoint.</p> <p>Modify the configuration of the forest identified by {id|name}. Documentation of the REST Resource API: https://docs.marklogic.com/REST/PUT/manage/v2/forests/[id-or-name]/properties</p> <p>Initialize ForestPropertiesPutCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>forest</code> <code>str</code> <p>A forest identifier. The forest can be identified either by ID or name.</p> required <code>body</code> <code>str | dict</code> <p>A forest properties in XML or JSON format.</p> required Source code in <code>mlclient/calls/forests.py</code> <pre><code>def __init__(\n    self,\n    forest: str,\n    body: str | dict,\n):\n    \"\"\"Initialize ForestPropertiesPutCall instance.\n\n    Parameters\n    ----------\n    forest : str\n        A forest identifier. The forest can be identified either by ID or name.\n    body : str | dict\n        A forest properties in XML or JSON format.\n    \"\"\"\n    self._validate_params(body)\n    content_type = utils.get_content_type_header_for_data(body)\n    if content_type == constants.HEADER_JSON and isinstance(body, str):\n        body = json.loads(body)\n    super().__init__(method=\"PUT\", content_type=content_type, body=body)\n    self._forest = forest\n</code></pre>"},{"location":"reference/mlclient/calls/ForestPropertiesPutCall/#mlclient.calls.ForestPropertiesPutCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Forest Properties call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Forest Properties call endpoint</p>"},{"location":"reference/mlclient/calls/ForestsGetCall/","title":"ForestsGetCall","text":"<pre><code>from mlclient.calls import ForestsGetCall\n</code></pre>"},{"location":"reference/mlclient/calls/ForestsGetCall/#mlclient.calls.ForestsGetCall","title":"mlclient.calls.ForestsGetCall","text":"<pre><code>ForestsGetCall(\n    data_format: str = \"xml\",\n    view: str = \"default\",\n    database: str | None = None,\n    group: str | None = None,\n    host: str | None = None,\n    full_refs: bool | None = None,\n)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A GET request to get forests summary.</p> <p>An ApiCall implementation representing a single GET request to the /manage/v2/forests endpoint.</p> <p>This resource address returns data about the forests in the cluster. The data returned depends on the view. If no view is specified, this request returns a summary of the forests in the cluster. Documentation of the REST Resource API: https://docs.marklogic.com/REST/GET/manage/v2/forests</p> <p>Initialize ForestsGetCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default).</p> <code>'xml'</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be either describe, default, status, metrics, schema, storage, or properties-schema.</p> <code>'default'</code> <code>database</code> <code>str</code> <p>Returns a summary of the forests for the specified database. The database can be identified either by id or name.</p> <code>None</code> <code>group</code> <code>str</code> <p>Returns a summary of the forests for the specified group. The group can be identified either by id or name.</p> <code>None</code> <code>host</code> <code>str</code> <p>Returns a summary of the forests for the specified host. The host can be identified either by id or name.</p> <code>None</code> <code>full_refs</code> <code>bool</code> <p>If set to true, full detail is returned for all relationship references. A value of false (the default) indicates to return detail only for first references.</p> <code>None</code> Source code in <code>mlclient/calls/forests.py</code> <pre><code>def __init__(\n    self,\n    data_format: str = \"xml\",\n    view: str = \"default\",\n    database: str | None = None,\n    group: str | None = None,\n    host: str | None = None,\n    full_refs: bool | None = None,\n):\n    \"\"\"Initialize ForestsGetCall instance.\n\n    Parameters\n    ----------\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n    view : str\n        A specific view of the returned data.\n        Can be either describe, default, status, metrics, schema, storage,\n        or properties-schema.\n    database : str\n        Returns a summary of the forests for the specified database.\n        The database can be identified either by id or name.\n    group : str\n        Returns a summary of the forests for the specified group.\n        The group can be identified either by id or name.\n    host : str\n        Returns a summary of the forests for the specified host.\n        The host can be identified either by id or name.\n    full_refs : bool\n        If set to true, full detail is returned for all relationship references.\n        A value of false (the default) indicates to return detail only for first\n        references.\n    \"\"\"\n    data_format = data_format if data_format is not None else \"xml\"\n    view = view if view is not None else \"default\"\n    self._validate_params(data_format, view)\n\n    super().__init__(\n        method=\"GET\",\n        accept=utils.get_accept_header_for_format(data_format),\n    )\n    if full_refs is not None:\n        full_refs = str(full_refs).lower()\n    self.add_param(self._FORMAT_PARAM, data_format)\n    self.add_param(self._VIEW_PARAM, view)\n    self.add_param(self._DATABASE_ID_PARAM, database)\n    self.add_param(self._GROUP_ID_PARAM, group)\n    self.add_param(self._HOST_ID_PARAM, host)\n    self.add_param(self._FULL_REFS_PARAM, full_refs)\n</code></pre>"},{"location":"reference/mlclient/calls/ForestsGetCall/#mlclient.calls.ForestsGetCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Forests call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Forests call endpoint</p>"},{"location":"reference/mlclient/calls/ForestsPostCall/","title":"ForestsPostCall","text":"<pre><code>from mlclient.calls import ForestsPostCall\n</code></pre>"},{"location":"reference/mlclient/calls/ForestsPostCall/#mlclient.calls.ForestsPostCall","title":"mlclient.calls.ForestsPostCall","text":"<pre><code>ForestsPostCall(\n    body: str | dict,\n    wait_for_forest_to_mount: bool | None = None,\n)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A POST request to create a new forest.</p> <p>An ApiCall implementation representing a single POST request to the /manage/v2/forests endpoint.</p> <p>Create a new forest, including replicas if specified. If a database id or database is included, attach the new forest(s) to the database. Documentation of the REST Resource API: https://docs.marklogic.com/REST/POST/manage/v2/forests</p> <p>Initialize ForestsPostCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>body</code> <code>str | dict</code> <p>A forest properties in XML or JSON format.</p> required <code>wait_for_forest_to_mount</code> <code>bool</code> <p>Whether to wait for the new forest to mount before sending a response to this request. Allowed values: true (default) or false.</p> <code>None</code> Source code in <code>mlclient/calls/forests.py</code> <pre><code>def __init__(\n    self,\n    body: str | dict,\n    wait_for_forest_to_mount: bool | None = None,\n):\n    \"\"\"Initialize ForestsPostCall instance.\n\n    Parameters\n    ----------\n    body : str | dict\n        A forest properties in XML or JSON format.\n    wait_for_forest_to_mount : bool\n        Whether to wait for the new forest to mount before sending a response\n        to this request. Allowed values: true (default) or false.\n    \"\"\"\n    self._validate_params(body)\n    content_type = utils.get_content_type_header_for_data(body)\n    if content_type == constants.HEADER_JSON and isinstance(body, str):\n        body = json.loads(body)\n    super().__init__(method=\"POST\", content_type=content_type, body=body)\n    if wait_for_forest_to_mount is not None:\n        wait_for_forest_to_mount = str(wait_for_forest_to_mount).lower()\n    self.add_param(self._WAIT_FOR_FOREST_TO_MOUNT_PARAM, wait_for_forest_to_mount)\n</code></pre>"},{"location":"reference/mlclient/calls/ForestsPostCall/#mlclient.calls.ForestsPostCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Forests call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Forests call endpoint</p>"},{"location":"reference/mlclient/calls/ForestsPutCall/","title":"ForestsPutCall","text":"<pre><code>from mlclient.calls import ForestsPutCall\n</code></pre>"},{"location":"reference/mlclient/calls/ForestsPutCall/#mlclient.calls.ForestsPutCall","title":"mlclient.calls.ForestsPutCall","text":"<pre><code>ForestsPutCall(body: str | dict)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A PUT request to perform an operation on forests.</p> <p>An ApiCall implementation representing a single PUT request to the /manage/v2/forests endpoint.</p> <p>Perform an operation on one or more forests, such as combining multiple forests into a single new one, or migrating the data in the forests to a new data directory. Documentation of the REST Resource API: https://docs.marklogic.com/REST/PUT/manage/v2/forests</p> <p>Initialize ForestsPutCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>body</code> <code>str | dict</code> <p>A forest properties in XML or JSON format.</p> required Source code in <code>mlclient/calls/forests.py</code> <pre><code>def __init__(\n    self,\n    body: str | dict,\n):\n    \"\"\"Initialize ForestsPutCall instance.\n\n    Parameters\n    ----------\n    body : str | dict\n        A forest properties in XML or JSON format.\n    \"\"\"\n    self._validate_params(body)\n    content_type = utils.get_content_type_header_for_data(body)\n    if content_type == constants.HEADER_JSON and isinstance(body, str):\n        body = json.loads(body)\n    super().__init__(method=\"PUT\", content_type=content_type, body=body)\n</code></pre>"},{"location":"reference/mlclient/calls/ForestsPutCall/#mlclient.calls.ForestsPutCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Forests call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Forests call endpoint</p>"},{"location":"reference/mlclient/calls/GroupPropertiesGetCall/","title":"GroupPropertiesGetCall","text":"<pre><code>from mlclient.calls import GroupPropertiesGetCall\n</code></pre>"},{"location":"reference/mlclient/calls/GroupPropertiesGetCall/#mlclient.calls.GroupPropertiesGetCall","title":"mlclient.calls.GroupPropertiesGetCall","text":"<pre><code>GroupPropertiesGetCall(\n    group: str, data_format: str = \"xml\"\n)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A GET request to get group properties.</p> <p>An ApiCall implementation representing a single GET request to the /manage/v2/groups/{id|name}/properties endpoint.</p> <p>This resource address returns the current state of modifiable properties of the specified group. Documentation of the REST Resource API: https://docs.marklogic.com/REST/GET/manage/v2/groups/[id-or-name]/properties</p> <p>Initialize GroupPropertiesGetCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>group</code> <code>str</code> <p>A group identifier. The group can be identified either by ID or name.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be html, json or xml (default). This parameter overrides the Accept header if both are present.</p> <code>'xml'</code> Source code in <code>mlclient/calls/groups.py</code> <pre><code>def __init__(\n    self,\n    group: str,\n    data_format: str = \"xml\",\n):\n    \"\"\"Initialize GroupPropertiesGetCall instance.\n\n    Parameters\n    ----------\n    group : str\n        A group identifier. The group can be identified either by ID or name.\n    data_format : str\n        The format of the returned data. Can be html, json or xml (default).\n        This parameter overrides the Accept header if both are present.\n    \"\"\"\n    data_format = data_format if data_format is not None else \"xml\"\n    self._validate_params(data_format)\n\n    super().__init__(\n        method=\"GET\",\n        accept=utils.get_accept_header_for_format(data_format),\n    )\n    self._group = group\n    self.add_param(self._FORMAT_PARAM, data_format)\n</code></pre>"},{"location":"reference/mlclient/calls/GroupPropertiesGetCall/#mlclient.calls.GroupPropertiesGetCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Group Properties call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Group Properties call endpoint</p>"},{"location":"reference/mlclient/calls/GroupPropertiesPutCall/","title":"GroupPropertiesPutCall","text":"<pre><code>from mlclient.calls import GroupPropertiesPutCall\n</code></pre>"},{"location":"reference/mlclient/calls/GroupPropertiesPutCall/#mlclient.calls.GroupPropertiesPutCall","title":"mlclient.calls.GroupPropertiesPutCall","text":"<pre><code>GroupPropertiesPutCall(group: str, body: str | dict)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A PUT request to modify group properties.</p> <p>An ApiCall implementation representing a single PUT request to the /manage/v2/groups/{id|name}/properties endpoint.</p> <p>Initiate a properties change on the specified group. Documentation of the REST Resource API: https://docs.marklogic.com/REST/PUT/manage/v2/groups/[id-or-name]/properties</p> <p>Initialize GroupPropertiesPutCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>group</code> <code>str</code> <p>A group identifier. The group can be identified either by ID or name.</p> required <code>body</code> <code>str | dict</code> <p>Group properties in XML or JSON format.</p> required Source code in <code>mlclient/calls/groups.py</code> <pre><code>def __init__(\n    self,\n    group: str,\n    body: str | dict,\n):\n    \"\"\"Initialize GroupPropertiesPutCall instance.\n\n    Parameters\n    ----------\n    group : str\n        A group identifier. The group can be identified either by ID or name.\n    body : str | dict\n        Group properties in XML or JSON format.\n    \"\"\"\n    self._validate_params(body)\n    content_type = utils.get_content_type_header_for_data(body)\n    if content_type == constants.HEADER_JSON and isinstance(body, str):\n        body = json.loads(body)\n    super().__init__(method=\"PUT\", content_type=content_type, body=body)\n    self._group = group\n</code></pre>"},{"location":"reference/mlclient/calls/GroupPropertiesPutCall/#mlclient.calls.GroupPropertiesPutCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Group Properties call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Group Properties call endpoint</p>"},{"location":"reference/mlclient/calls/HostsGetCall/","title":"HostsGetCall","text":"<pre><code>from mlclient.calls import HostsGetCall\n</code></pre>"},{"location":"reference/mlclient/calls/HostsGetCall/#mlclient.calls.HostsGetCall","title":"mlclient.calls.HostsGetCall","text":"<pre><code>HostsGetCall(\n    data_format: str = \"xml\",\n    group_id: str | None = None,\n    view: str = \"default\",\n)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A GET request to get hosts summary.</p> <p>An ApiCall implementation representing a single GET request to the /manage/v2/hosts endpoint.</p> <p>This resource address returns data about the hosts in the cluster. The data returned depends on the setting of the view request parameter. The default view provides a summary of the hosts. Documentation of the REST Resource API: https://docs.marklogic.com/REST/GET/manage/v2/hosts</p> <p>Initialize HostsGetCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default).</p> <code>'xml'</code> <code>group_id</code> <code>str</code> <p>Specifies to return only the hosts in the specified group. The group can be identified either by id or name. If not specified, the response includes information about all hosts.</p> <code>None</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be default, status, metrics, schema, properties-schema, or describe. The schema view requires XML format.</p> <code>'default'</code> Source code in <code>mlclient/calls/hosts.py</code> <pre><code>def __init__(\n    self,\n    data_format: str = \"xml\",\n    group_id: str | None = None,\n    view: str = \"default\",\n):\n    \"\"\"Initialize HostsGetCall instance.\n\n    Parameters\n    ----------\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n    group_id : str\n        Specifies to return only the hosts in the specified group.\n        The group can be identified either by id or name.\n        If not specified, the response includes information about all hosts.\n    view : str\n        A specific view of the returned data.\n        Can be default, status, metrics, schema, properties-schema, or describe.\n        The schema view requires XML format.\n    \"\"\"\n    data_format = data_format if data_format is not None else \"xml\"\n    view = view if view is not None else \"default\"\n    self._validate_params(data_format, view)\n\n    super().__init__(\n        method=\"GET\",\n        accept=utils.get_accept_header_for_format(data_format),\n    )\n    self.add_param(self._FORMAT_PARAM, data_format)\n    self.add_param(self._GROUP_ID_PARAM, group_id)\n    self.add_param(self._VIEW_PARAM, view)\n</code></pre>"},{"location":"reference/mlclient/calls/HostsGetCall/#mlclient.calls.HostsGetCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Hosts call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Hosts call endpoint</p>"},{"location":"reference/mlclient/calls/LogsCall/","title":"LogsCall","text":"<pre><code>from mlclient.calls import LogsCall\n</code></pre>"},{"location":"reference/mlclient/calls/LogsCall/#mlclient.calls.LogsCall","title":"mlclient.calls.LogsCall","text":"<pre><code>LogsCall(\n    filename: str,\n    data_format: str = \"html\",\n    host: str | None = None,\n    start_time: str | None = None,\n    end_time: str | None = None,\n    regex: str | None = None,\n)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A GET request to retrieve logs.</p> <p>An ApiCall implementation representing a single GET request to the /manage/v2/logs endpoint.</p> <p>Returns the content of server log files. Documentation of the REST Resource API: https://docs.marklogic.com/REST/GET/manage/v2/logs</p> <p>Initialize LogsCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>filename</code> <code>str</code> <p>The log file to be returned.</p> required <code>data_format</code> <code>str</code> <p>The format of the data in the log file. The supported formats are xml, json or html.</p> <code>'html'</code> <code>host</code> <code>str</code> <p>The host from which to return the log data.</p> <code>None</code> <code>start_time</code> <code>str</code> <p>The start time for the log data.</p> <code>None</code> <code>end_time</code> <code>str</code> <p>The end time for the log data.</p> <code>None</code> <code>regex</code> <code>str</code> <p>Filters the log data, based on a regular expression.</p> <code>None</code> Source code in <code>mlclient/calls/logs.py</code> <pre><code>def __init__(\n    self,\n    filename: str,\n    data_format: str = \"html\",\n    host: str | None = None,\n    start_time: str | None = None,\n    end_time: str | None = None,\n    regex: str | None = None,\n):\n    \"\"\"Initialize LogsCall instance.\n\n    Parameters\n    ----------\n    filename : str\n        The log file to be returned.\n    data_format : str\n        The format of the data in the log file. The supported formats are xml, json\n        or html.\n    host : str\n        The host from which to return the log data.\n    start_time : str\n        The start time for the log data.\n    end_time : str\n        The end time for the log data.\n    regex : str\n        Filters the log data, based on a regular expression.\n    \"\"\"\n    data_format = data_format if data_format is not None else \"html\"\n    self._validate_params(data_format, start_time, end_time)\n\n    super().__init__(accept=utils.get_accept_header_for_format(data_format))\n    self.add_param(self._FORMAT_PARAM, data_format)\n    self.add_param(self._FILENAME_PARAM, filename)\n    self.add_param(self._HOST_PARAM, host)\n    self.add_param(self._START_PARAM, self._reformat_datetime_param(start_time))\n    self.add_param(self._END_PARAM, self._reformat_datetime_param(end_time))\n    self.add_param(self._REGEX_PARAM, regex)\n</code></pre>"},{"location":"reference/mlclient/calls/LogsCall/#mlclient.calls.LogsCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Logs call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Logs call endpoint</p>"},{"location":"reference/mlclient/calls/RoleDeleteCall/","title":"RoleDeleteCall","text":"<pre><code>from mlclient.calls import RoleDeleteCall\n</code></pre>"},{"location":"reference/mlclient/calls/RoleDeleteCall/#mlclient.calls.RoleDeleteCall","title":"mlclient.calls.RoleDeleteCall","text":"<pre><code>RoleDeleteCall(role: str)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A DELETE request to remove a role.</p> <p>An ApiCall implementation representing a single DELETE request to the /manage/v2/roles/{id|name} endpoint.</p> <p>This resource address deletes the named role from the named security database. Documentation of the REST Resource API: https://docs.marklogic.com/REST/DELETE/manage/v2/roles/[id-or-name]</p> <p>Initialize RoleDeleteCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>role</code> <code>str</code> <p>A role identifier. The role can be identified either by ID or name.</p> required Source code in <code>mlclient/calls/roles.py</code> <pre><code>def __init__(\n    self,\n    role: str,\n):\n    \"\"\"Initialize RoleDeleteCall instance.\n\n    Parameters\n    ----------\n    role : str\n        A role identifier. The role can be identified either by ID or name.\n    \"\"\"\n    super().__init__(method=constants.METHOD_DELETE)\n    self._role = role\n</code></pre>"},{"location":"reference/mlclient/calls/RoleDeleteCall/#mlclient.calls.RoleDeleteCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Role call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Role call endpoint</p>"},{"location":"reference/mlclient/calls/RoleGetCall/","title":"RoleGetCall","text":"<pre><code>from mlclient.calls import RoleGetCall\n</code></pre>"},{"location":"reference/mlclient/calls/RoleGetCall/#mlclient.calls.RoleGetCall","title":"mlclient.calls.RoleGetCall","text":"<pre><code>RoleGetCall(\n    role: str,\n    data_format: str = \"xml\",\n    view: str = \"default\",\n)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A GET request to get a role details.</p> <p>An ApiCall implementation representing a single GET request to the /manage/v2/roles/{id|name} endpoint.</p> <p>This resource address returns the configuration for the specified role. Documentation of the REST Resource API: https://docs.marklogic.com/REST/GET/manage/v2/roles/[id-or-name]</p> <p>Initialize RoleGetCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>role</code> <code>str</code> <p>A role identifier. The role can be identified either by ID or name.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default). This parameter is not meaningful with view=edit.</p> <code>'xml'</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be: describe, or default.</p> <code>'default'</code> Source code in <code>mlclient/calls/roles.py</code> <pre><code>def __init__(\n    self,\n    role: str,\n    data_format: str = \"xml\",\n    view: str = \"default\",\n):\n    \"\"\"Initialize RoleGetCall instance.\n\n    Parameters\n    ----------\n    role : str\n        A role identifier. The role can be identified either by ID or name.\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n        This parameter is not meaningful with view=edit.\n    view : str\n        A specific view of the returned data. Can be: describe, or default.\n    \"\"\"\n    data_format = data_format if data_format is not None else \"xml\"\n    view = view if view is not None else \"default\"\n    self._validate_params(data_format, view)\n\n    super().__init__(\n        method=\"GET\",\n        accept=utils.get_accept_header_for_format(data_format),\n    )\n    self._role = role\n    self.add_param(self._FORMAT_PARAM, data_format)\n    self.add_param(self._VIEW_PARAM, view)\n</code></pre>"},{"location":"reference/mlclient/calls/RoleGetCall/#mlclient.calls.RoleGetCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Role call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Role call endpoint</p>"},{"location":"reference/mlclient/calls/RolePropertiesGetCall/","title":"RolePropertiesGetCall","text":"<pre><code>from mlclient.calls import RolePropertiesGetCall\n</code></pre>"},{"location":"reference/mlclient/calls/RolePropertiesGetCall/#mlclient.calls.RolePropertiesGetCall","title":"mlclient.calls.RolePropertiesGetCall","text":"<pre><code>RolePropertiesGetCall(role: str, data_format: str = 'xml')\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A GET request to get role properties.</p> <p>An ApiCall implementation representing a single GET request to the /manage/v2/roles/{id|name}/properties endpoint.</p> <p>This resource address returns the properties of the specified role. Documentation of the REST Resource API: https://docs.marklogic.com/REST/GET/manage/v2/roles/[id-or-name]/properties</p> <p>Initialize RolePropertiesGetCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>role</code> <code>str</code> <p>A role identifier. The role can be identified either by ID or name.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either json or xml (default). This parameter overrides the Accept header if both are present.</p> <code>'xml'</code> Source code in <code>mlclient/calls/roles.py</code> <pre><code>def __init__(\n    self,\n    role: str,\n    data_format: str = \"xml\",\n):\n    \"\"\"Initialize RolePropertiesGetCall instance.\n\n    Parameters\n    ----------\n    role : str\n        A role identifier. The role can be identified either by ID or name.\n    data_format : str\n        The format of the returned data. Can be either json or xml (default).\n        This parameter overrides the Accept header if both are present.\n    \"\"\"\n    data_format = data_format if data_format is not None else \"xml\"\n    self._validate_params(data_format)\n\n    super().__init__(\n        method=\"GET\",\n        accept=utils.get_accept_header_for_format(data_format),\n    )\n    self._role = role\n    self.add_param(self._FORMAT_PARAM, data_format)\n</code></pre>"},{"location":"reference/mlclient/calls/RolePropertiesGetCall/#mlclient.calls.RolePropertiesGetCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Role Properties call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Role Properties call endpoint</p>"},{"location":"reference/mlclient/calls/RolePropertiesPutCall/","title":"RolePropertiesPutCall","text":"<pre><code>from mlclient.calls import RolePropertiesPutCall\n</code></pre>"},{"location":"reference/mlclient/calls/RolePropertiesPutCall/#mlclient.calls.RolePropertiesPutCall","title":"mlclient.calls.RolePropertiesPutCall","text":"<pre><code>RolePropertiesPutCall(role: str, body: str | dict)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A PUT request to modify role properties.</p> <p>An ApiCall implementation representing a single PUT request to the /manage/v2/roles/{id|name}/properties endpoint.</p> <p>This resource address can be used to update the properties for the specified role. Documentation of the REST Resource API: https://docs.marklogic.com/REST/PUT/manage/v2/roles/[id-or-name]/properties</p> <p>Initialize RolePropertiesPutCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>role</code> <code>str</code> <p>A role identifier. The role can be identified either by ID or name.</p> required <code>body</code> <code>str | dict</code> <p>A role properties in XML or JSON format.</p> required Source code in <code>mlclient/calls/roles.py</code> <pre><code>def __init__(\n    self,\n    role: str,\n    body: str | dict,\n):\n    \"\"\"Initialize RolePropertiesPutCall instance.\n\n    Parameters\n    ----------\n    role : str\n        A role identifier. The role can be identified either by ID or name.\n    body : str | dict\n        A role properties in XML or JSON format.\n    \"\"\"\n    self._validate_params(body)\n    content_type = utils.get_content_type_header_for_data(body)\n    if content_type == constants.HEADER_JSON and isinstance(body, str):\n        body = json.loads(body)\n    super().__init__(method=\"PUT\", content_type=content_type, body=body)\n    self._role = role\n</code></pre>"},{"location":"reference/mlclient/calls/RolePropertiesPutCall/#mlclient.calls.RolePropertiesPutCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Role Properties call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Role Properties call endpoint</p>"},{"location":"reference/mlclient/calls/RolesGetCall/","title":"RolesGetCall","text":"<pre><code>from mlclient.calls import RolesGetCall\n</code></pre>"},{"location":"reference/mlclient/calls/RolesGetCall/#mlclient.calls.RolesGetCall","title":"mlclient.calls.RolesGetCall","text":"<pre><code>RolesGetCall(\n    data_format: str = \"xml\", view: str = \"default\"\n)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A GET request to get roles summary.</p> <p>An ApiCall implementation representing a single GET request to the /manage/v2/roles endpoint.</p> <p>This resource address returns a summary of the roles in the security database. Documentation of the REST Resource API: https://docs.marklogic.com/REST/GET/manage/v2/roles</p> <p>Initialize RolesGetCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default).</p> <code>'xml'</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be: describe, or default.</p> <code>'default'</code> Source code in <code>mlclient/calls/roles.py</code> <pre><code>def __init__(\n    self,\n    data_format: str = \"xml\",\n    view: str = \"default\",\n):\n    \"\"\"Initialize RolesGetCall instance.\n\n    Parameters\n    ----------\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n    view : str\n        A specific view of the returned data. Can be: describe, or default.\n    \"\"\"\n    data_format = data_format if data_format is not None else \"xml\"\n    view = view if view is not None else \"default\"\n    self._validate_params(data_format, view)\n\n    super().__init__(\n        method=\"GET\",\n        accept=utils.get_accept_header_for_format(data_format),\n    )\n    self.add_param(self._FORMAT_PARAM, data_format)\n    self.add_param(self._VIEW_PARAM, view)\n</code></pre>"},{"location":"reference/mlclient/calls/RolesGetCall/#mlclient.calls.RolesGetCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Roles call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Roles call endpoint</p>"},{"location":"reference/mlclient/calls/RolesPostCall/","title":"RolesPostCall","text":"<pre><code>from mlclient.calls import RolesPostCall\n</code></pre>"},{"location":"reference/mlclient/calls/RolesPostCall/#mlclient.calls.RolesPostCall","title":"mlclient.calls.RolesPostCall","text":"<pre><code>RolesPostCall(body: str | dict)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A POST request to create a new role.</p> <p>An ApiCall implementation representing a single POST request to the /manage/v2/roles endpoint.</p> <p>This resource address creates a new role in the security database. Documentation of the REST Resource API: https://docs.marklogic.com/REST/POST/manage/v2/roles</p> <p>Initialize RolesPostCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>body</code> <code>str | dict</code> <p>A role properties in XML or JSON format.</p> required Source code in <code>mlclient/calls/roles.py</code> <pre><code>def __init__(\n    self,\n    body: str | dict,\n):\n    \"\"\"Initialize RolesPostCall instance.\n\n    Parameters\n    ----------\n    body : str | dict\n        A role properties in XML or JSON format.\n    \"\"\"\n    self._validate_params(body)\n    content_type = utils.get_content_type_header_for_data(body)\n    if content_type == constants.HEADER_JSON and isinstance(body, str):\n        body = json.loads(body)\n    super().__init__(method=\"POST\", content_type=content_type, body=body)\n</code></pre>"},{"location":"reference/mlclient/calls/RolesPostCall/#mlclient.calls.RolesPostCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Roles call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Roles call endpoint</p>"},{"location":"reference/mlclient/calls/ServerConfigGetCall/","title":"ServerConfigGetCall","text":"<pre><code>from mlclient.calls import ServerConfigGetCall\n</code></pre>"},{"location":"reference/mlclient/calls/ServerConfigGetCall/#mlclient.calls.ServerConfigGetCall","title":"mlclient.calls.ServerConfigGetCall","text":"<pre><code>ServerConfigGetCall(\n    method: str = constants.METHOD_GET,\n    params: dict | None = None,\n    headers: dict | None = None,\n    body: str | dict | bytes | None = None,\n    accept: str | None = None,\n    content_type: str | None = None,\n)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A GET request to retrieve server configuration for cluster join.</p> <p>An ApiCall implementation representing a single GET request to the /admin/v1/server-config endpoint.</p> <p>Retrieve MarkLogic Server configuration information, suitable for use in joining a cluster. Upon success, MarkLogic Server returns status code 200 (OK) and the response body contains the requested information, expressed as XML. If license key installation and basic initialization have not yet been done, the response payload will include a timestamp, but all other elements will be empty.</p> <p>This request must be directed to the Admin Interface on port 8001.</p> <p>This method is intended for use in the context of other REST Management API methods during cluster configuration, such as using the data returned by this request as input to a POST request to /admin/v1/cluster-config.</p> <p>Documentation of the REST Resource API: https://docs.marklogic.com/REST/GET/admin/v1/server-config</p> Source code in <code>mlclient/calls/base.py</code> <pre><code>def __init__(\n    self,\n    method: str = constants.METHOD_GET,\n    params: dict | None = None,\n    headers: dict | None = None,\n    body: str | dict | bytes | None = None,\n    accept: str | None = None,\n    content_type: str | None = None,\n):\n    \"\"\"Initialize ApiCall implementation instance.\n\n    Parameters\n    ----------\n    method : str\n        a request method\n    params : dict\n        request parameters\n    headers : dict\n        request headers\n    body : str | dict | bytes\n        a request body\n    accept : str\n        an Accept header value\n    content_type : str\n        a Content-Type header value\n    \"\"\"\n    self.method = method\n    self.params = params\n    self.headers = headers\n    self.body = body\n    if accept:\n        self.add_header(constants.HEADER_NAME_ACCEPT, accept)\n    if content_type:\n        self.add_header(constants.HEADER_NAME_CONTENT_TYPE, content_type)\n</code></pre>"},{"location":"reference/mlclient/calls/ServerConfigGetCall/#mlclient.calls.ServerConfigGetCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint: str\n</code></pre> <p>Return the endpoint.</p>"},{"location":"reference/mlclient/calls/ServerDeleteCall/","title":"ServerDeleteCall","text":"<pre><code>from mlclient.calls import ServerDeleteCall\n</code></pre>"},{"location":"reference/mlclient/calls/ServerDeleteCall/#mlclient.calls.ServerDeleteCall","title":"mlclient.calls.ServerDeleteCall","text":"<pre><code>ServerDeleteCall(server: str, group_id: str)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A DELETE request to remove an app server.</p> <p>An ApiCall implementation representing a single DELETE request to the /manage/v2/servers/{id|name} endpoint.</p> <p>This resource address deletes the specified App Server from the specified group. Documentation of the REST Resource API: https://docs.marklogic.com/REST/DELETE/manage/v2/servers/[id-or-name]</p> <p>Initialize ServerDeleteCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>server</code> <code>str</code> <p>A server identifier. The server can be identified either by ID or name.</p> required <code>group_id</code> <code>str</code> <p>The id or name of the group to which the App Server belongs. This parameter is required.</p> required Source code in <code>mlclient/calls/servers.py</code> <pre><code>def __init__(\n    self,\n    server: str,\n    group_id: str,\n):\n    \"\"\"Initialize ServerDeleteCall instance.\n\n    Parameters\n    ----------\n    server : str\n        A server identifier. The server can be identified either by ID or name.\n    group_id : str\n        The id or name of the group to which the App Server belongs.\n        This parameter is required.\n    \"\"\"\n    super().__init__(method=constants.METHOD_DELETE)\n    self.add_param(self._GROUP_ID_PARAM, group_id)\n    self._server = server\n</code></pre>"},{"location":"reference/mlclient/calls/ServerDeleteCall/#mlclient.calls.ServerDeleteCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Server call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Server call endpoint</p>"},{"location":"reference/mlclient/calls/ServerGetCall/","title":"ServerGetCall","text":"<pre><code>from mlclient.calls import ServerGetCall\n</code></pre>"},{"location":"reference/mlclient/calls/ServerGetCall/#mlclient.calls.ServerGetCall","title":"mlclient.calls.ServerGetCall","text":"<pre><code>ServerGetCall(\n    server: str,\n    group_id: str,\n    data_format: str = \"xml\",\n    view: str = \"default\",\n    host_id: str | None = None,\n    full_refs: bool | None = None,\n    modules: bool | None = None,\n)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A GET request to get app server details.</p> <p>An ApiCall implementation representing a single GET request to the /manage/v2/servers/{id|name} endpoint.</p> <p>This resource address returns data about a specific App Server. The server can be identified either by id or name. The data returned depends on the value of the view request parameter. The default view is a summary with links to additional data. Documentation of the REST Resource API: https://docs.marklogic.com/REST/GET/manage/v2/servers/[id-or-name]</p> <p>Initialize ServerGetCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>server</code> <code>str</code> <p>A server identifier. The server can be identified either by ID or name.</p> required <code>group_id</code> <code>str</code> <p>The id or name of the group to which the App Server belongs. This parameter is required.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default).</p> <code>'xml'</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be properties-schema, config, edit, package, describe, status, xdmp:server-status or default.</p> <code>'default'</code> <code>host_id</code> <code>str</code> <p>Meaningful only when view=status. Specifies to return the status for the server in the specified host. The host can be identified either by id or name.</p> <code>None</code> <code>full_refs</code> <code>bool</code> <p>If set to true, full detail is returned for all relationship references. A value of false (the default) indicates to return detail only for first references. This parameter is not meaningful with view=package.</p> <code>None</code> <code>modules</code> <code>bool</code> <p>Meaningful only with view=package. Whether to include a manifest of the modules database for the App Server in the results, if one exists. It is an error to request a modules database manifest for an App Server that uses the filesystem for modules. Default: false.</p> <code>None</code> Source code in <code>mlclient/calls/servers.py</code> <pre><code>def __init__(\n    self,\n    server: str,\n    group_id: str,\n    data_format: str = \"xml\",\n    view: str = \"default\",\n    host_id: str | None = None,\n    full_refs: bool | None = None,\n    modules: bool | None = None,\n):\n    \"\"\"Initialize ServerGetCall instance.\n\n    Parameters\n    ----------\n    server : str\n        A server identifier. The server can be identified either by ID or name.\n    group_id : str\n        The id or name of the group to which the App Server belongs.\n        This parameter is required.\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n    view : str\n        A specific view of the returned data.\n        Can be properties-schema, config, edit, package, describe, status,\n        xdmp:server-status or default.\n    host_id : str\n        Meaningful only when view=status. Specifies to return the status for\n        the server in the specified host. The host can be identified either by id\n        or name.\n    full_refs : bool\n        If set to true, full detail is returned for all relationship references.\n        A value of false (the default) indicates to return detail only for\n        first references. This parameter is not meaningful with view=package.\n    modules : bool\n        Meaningful only with view=package. Whether to include a manifest\n        of the modules database for the App Server in the results, if one exists.\n        It is an error to request a modules database manifest for an App Server\n        that uses the filesystem for modules. Default: false.\n    \"\"\"\n    data_format = data_format if data_format is not None else \"xml\"\n    view = view if view is not None else \"default\"\n    self._validate_params(data_format, view)\n\n    super().__init__(\n        method=\"GET\",\n        accept=utils.get_accept_header_for_format(data_format),\n    )\n    if full_refs is not None:\n        full_refs = str(full_refs).lower()\n    if modules is not None:\n        modules = str(modules).lower()\n    self._server = server\n    self.add_param(self._GROUP_ID_PARAM, group_id)\n    self.add_param(self._FORMAT_PARAM, data_format)\n    self.add_param(self._VIEW_PARAM, view)\n    self.add_param(self._HOST_ID_PARAM, host_id)\n    self.add_param(self._FULL_REFS_PARAM, full_refs)\n    self.add_param(self._MODULES_PARAM, modules)\n</code></pre>"},{"location":"reference/mlclient/calls/ServerGetCall/#mlclient.calls.ServerGetCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Server call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Server call endpoint</p>"},{"location":"reference/mlclient/calls/ServerPropertiesGetCall/","title":"ServerPropertiesGetCall","text":"<pre><code>from mlclient.calls import ServerPropertiesGetCall\n</code></pre>"},{"location":"reference/mlclient/calls/ServerPropertiesGetCall/#mlclient.calls.ServerPropertiesGetCall","title":"mlclient.calls.ServerPropertiesGetCall","text":"<pre><code>ServerPropertiesGetCall(\n    server: str, group_id: str, data_format: str = \"xml\"\n)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A GET request to get app server properties.</p> <p>An ApiCall implementation representing a single GET request to the /manage/v2/servers/{id|name}/properties endpoint.</p> <p>This resource address returns the current state of modifiable properties of the specified App Server. Documentation of the REST Resource API: https://docs.marklogic.com/REST/GET/manage/v2/servers/[id-or-name]/properties</p> <p>Initialize ServerPropertiesGetCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>server</code> <code>str</code> <p>A server identifier. The server can be identified either by ID or name.</p> required <code>group_id</code> <code>str</code> <p>The id or name of the group to which the App Server belongs. This parameter is required.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either json or xml (default). This parameter overrides the Accept header if both are present.</p> <code>'xml'</code> Source code in <code>mlclient/calls/servers.py</code> <pre><code>def __init__(\n    self,\n    server: str,\n    group_id: str,\n    data_format: str = \"xml\",\n):\n    \"\"\"Initialize ServerPropertiesGetCall instance.\n\n    Parameters\n    ----------\n    server : str\n        A server identifier. The server can be identified either by ID or name.\n    group_id : str\n        The id or name of the group to which the App Server belongs.\n        This parameter is required.\n    data_format : str\n        The format of the returned data. Can be either json or xml (default).\n        This parameter overrides the Accept header if both are present.\n    \"\"\"\n    data_format = data_format if data_format is not None else \"xml\"\n    self._validate_params(data_format)\n\n    super().__init__(\n        method=\"GET\",\n        accept=utils.get_accept_header_for_format(data_format),\n    )\n    self._server = server\n    self.add_param(self._GROUP_ID_PARAM, group_id)\n    self.add_param(self._FORMAT_PARAM, data_format)\n</code></pre>"},{"location":"reference/mlclient/calls/ServerPropertiesGetCall/#mlclient.calls.ServerPropertiesGetCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Server Properties call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Server Properties call endpoint</p>"},{"location":"reference/mlclient/calls/ServerPropertiesPutCall/","title":"ServerPropertiesPutCall","text":"<pre><code>from mlclient.calls import ServerPropertiesPutCall\n</code></pre>"},{"location":"reference/mlclient/calls/ServerPropertiesPutCall/#mlclient.calls.ServerPropertiesPutCall","title":"mlclient.calls.ServerPropertiesPutCall","text":"<pre><code>ServerPropertiesPutCall(\n    server: str, group_id: str, body: str | dict\n)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A PUT request to modify app server properties.</p> <p>An ApiCall implementation representing a single PUT request to the /manage/v2/servers/{id|name}/properties endpoint.</p> <p>Initiate a properties change on the specified App Server. Documentation of the REST Resource API: https://docs.marklogic.com/REST/PUT/manage/v2/servers/[id-or-name]/properties</p> <p>Initialize ServerPropertiesPutCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>server</code> <code>str</code> <p>A server identifier. The server can be identified either by ID or name.</p> required <code>group_id</code> <code>str</code> <p>The id or name of the group to which the App Server belongs. This parameter is required.</p> required <code>body</code> <code>str | dict</code> <p>A server properties in XML or JSON format.</p> required Source code in <code>mlclient/calls/servers.py</code> <pre><code>def __init__(\n    self,\n    server: str,\n    group_id: str,\n    body: str | dict,\n):\n    \"\"\"Initialize ServerPropertiesPutCall instance.\n\n    Parameters\n    ----------\n    server : str\n        A server identifier. The server can be identified either by ID or name.\n    group_id : str\n        The id or name of the group to which the App Server belongs.\n        This parameter is required.\n    body : str | dict\n        A server properties in XML or JSON format.\n    \"\"\"\n    self._validate_params(body)\n    content_type = utils.get_content_type_header_for_data(body)\n    if content_type == constants.HEADER_JSON and isinstance(body, str):\n        body = json.loads(body)\n    super().__init__(method=\"PUT\", content_type=content_type, body=body)\n    self._server = server\n    self.add_param(self._GROUP_ID_PARAM, group_id)\n</code></pre>"},{"location":"reference/mlclient/calls/ServerPropertiesPutCall/#mlclient.calls.ServerPropertiesPutCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Server Properties call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Server Properties call endpoint</p>"},{"location":"reference/mlclient/calls/ServersGetCall/","title":"ServersGetCall","text":"<pre><code>from mlclient.calls import ServersGetCall\n</code></pre>"},{"location":"reference/mlclient/calls/ServersGetCall/#mlclient.calls.ServersGetCall","title":"mlclient.calls.ServersGetCall","text":"<pre><code>ServersGetCall(\n    data_format: str = \"xml\",\n    group_id: str | None = None,\n    view: str = \"default\",\n    full_refs: bool | None = None,\n)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A GET request to get app servers summary.</p> <p>An ApiCall implementation representing a single GET request to the /manage/v2/servers endpoint.</p> <p>This resource address returns data about the App Servers in the cluster. The data returned depends on the setting of the view request parameter. The default view provides a summary of the servers. Documentation of the REST Resource API: https://docs.marklogic.com/REST/GET/manage/v2/servers</p> <p>Initialize ServersGetCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default).</p> <code>'xml'</code> <code>group_id</code> <code>str</code> <p>Specifies to return only the servers in the specified group. The group can be identified either by id or name. If not specified, the response includes information about all App Servers.</p> <code>None</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be schema, properties-schema, metrics, package, describe, or default.</p> <code>'default'</code> <code>full_refs</code> <code>bool</code> <p>If set to true, full detail is returned for all relationship references. A value of false (the default) indicates to return detail only for first references. This parameter is not meaningful with view=package.</p> <code>None</code> Source code in <code>mlclient/calls/servers.py</code> <pre><code>def __init__(\n    self,\n    data_format: str = \"xml\",\n    group_id: str | None = None,\n    view: str = \"default\",\n    full_refs: bool | None = None,\n):\n    \"\"\"Initialize ServersGetCall instance.\n\n    Parameters\n    ----------\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n    group_id : str\n        Specifies to return only the servers in the specified group.\n        The group can be identified either by id or name.\n        If not specified, the response includes information about all App Servers.\n    view : str\n        A specific view of the returned data.\n        Can be schema, properties-schema, metrics, package, describe, or default.\n    full_refs : bool\n        If set to true, full detail is returned for all relationship references.\n        A value of false (the default) indicates to return detail only for first\n        references. This parameter is not meaningful with view=package.\n    \"\"\"\n    data_format = data_format if data_format is not None else \"xml\"\n    view = view if view is not None else \"default\"\n    self._validate_params(data_format, view)\n\n    super().__init__(\n        method=\"GET\",\n        accept=utils.get_accept_header_for_format(data_format),\n    )\n    if full_refs is not None:\n        full_refs = str(full_refs).lower()\n    self.add_param(self._FORMAT_PARAM, data_format)\n    self.add_param(self._GROUP_ID_PARAM, group_id)\n    self.add_param(self._VIEW_PARAM, view)\n    self.add_param(self._FULL_REFS_PARAM, full_refs)\n</code></pre>"},{"location":"reference/mlclient/calls/ServersGetCall/#mlclient.calls.ServersGetCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Servers call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Servers call endpoint</p>"},{"location":"reference/mlclient/calls/ServersPostCall/","title":"ServersPostCall","text":"<pre><code>from mlclient.calls import ServersPostCall\n</code></pre>"},{"location":"reference/mlclient/calls/ServersPostCall/#mlclient.calls.ServersPostCall","title":"mlclient.calls.ServersPostCall","text":"<pre><code>ServersPostCall(\n    body: str | dict,\n    group_id: str | None = None,\n    server_type: str | None = None,\n)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A POST request to create a new app server.</p> <p>An ApiCall implementation representing a single POST request to the /manage/v2/servers endpoint.</p> <p>This resource address is used to create a new App Server in the specified group. Documentation of the REST Resource API: https://docs.marklogic.com/REST/POST/manage/v2/servers</p> <p>Initialize ServersPostCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>body</code> <code>str | dict</code> <p>A server properties in XML or JSON format.</p> required <code>group_id</code> <code>str</code> <p>The id or name of the group to which the App Server belongs. The group must be specified by this parameter or by the group-name property in the request payload. If it is specified in both places, the values must be the same.</p> <code>None</code> <code>server_type</code> <code>str</code> <p>The type of App Server to create. The App Server type must be specified by this parameter or in the request payload. If it is specified in both places, the values must be the same. The valid types are: http, odbc, xdbc, or webdav.</p> <code>None</code> Source code in <code>mlclient/calls/servers.py</code> <pre><code>def __init__(\n    self,\n    body: str | dict,\n    group_id: str | None = None,\n    server_type: str | None = None,\n):\n    \"\"\"Initialize ServersPostCall instance.\n\n    Parameters\n    ----------\n    body : str | dict\n        A server properties in XML or JSON format.\n    group_id : str\n        The id or name of the group to which the App Server belongs.\n        The group must be specified by this parameter or by the group-name property\n        in the request payload. If it is specified in both places, the values\n        must be the same.\n    server_type : str\n        The type of App Server to create.\n        The App Server type must be specified by this parameter or in the request\n        payload. If it is specified in both places, the values must be the same.\n        The valid types are: http, odbc, xdbc, or webdav.\n    \"\"\"\n    self._validate_params(server_type, body)\n    content_type = utils.get_content_type_header_for_data(body)\n    if content_type == constants.HEADER_JSON and isinstance(body, str):\n        body = json.loads(body)\n    super().__init__(method=\"POST\", content_type=content_type, body=body)\n    self.add_param(self._GROUP_ID_PARAM, group_id)\n    self.add_param(self._SERVER_TYPE_PARAM, server_type)\n</code></pre>"},{"location":"reference/mlclient/calls/ServersPostCall/#mlclient.calls.ServersPostCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Servers call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Servers call endpoint</p>"},{"location":"reference/mlclient/calls/TimestampGetCall/","title":"TimestampGetCall","text":"<pre><code>from mlclient.calls import TimestampGetCall\n</code></pre>"},{"location":"reference/mlclient/calls/TimestampGetCall/#mlclient.calls.TimestampGetCall","title":"mlclient.calls.TimestampGetCall","text":"<pre><code>TimestampGetCall(\n    method: str = constants.METHOD_GET,\n    params: dict | None = None,\n    headers: dict | None = None,\n    body: str | dict | bytes | None = None,\n    accept: str | None = None,\n    content_type: str | None = None,\n)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A GET request to verify that MarkLogic Server is up and accepting requests.</p> <p>An ApiCall implementation representing a single GET request to the /admin/v1/timestamp endpoint.</p> <p>Verify that MarkLogic Server is up and accepting requests. If MarkLogic Server is available, this request returns response code 200 (OK) and the response body contains a plain text timestamp that can be used with the timestamp returned by asynchronous admin and manage requests to confirm that a restart occurred and has completed.</p> <p>This request must be directed to the Admin Interface on port 8001.</p> <p>Management REST API requests that cause a restart usually return a reference to this service in the response Location header and/or the data in the response body. You can use this reference to test whether the restart has successfully completed.</p> <p>To test for a restart, call GET /admin/v1/timestamp prior to the operation that can cause a restart, then call it again afterwards, and compare the two timestamps.</p> <p>Documentation of the REST Resource API: https://docs.marklogic.com/REST/GET/admin/v1/timestamp</p> Source code in <code>mlclient/calls/base.py</code> <pre><code>def __init__(\n    self,\n    method: str = constants.METHOD_GET,\n    params: dict | None = None,\n    headers: dict | None = None,\n    body: str | dict | bytes | None = None,\n    accept: str | None = None,\n    content_type: str | None = None,\n):\n    \"\"\"Initialize ApiCall implementation instance.\n\n    Parameters\n    ----------\n    method : str\n        a request method\n    params : dict\n        request parameters\n    headers : dict\n        request headers\n    body : str | dict | bytes\n        a request body\n    accept : str\n        an Accept header value\n    content_type : str\n        a Content-Type header value\n    \"\"\"\n    self.method = method\n    self.params = params\n    self.headers = headers\n    self.body = body\n    if accept:\n        self.add_header(constants.HEADER_NAME_ACCEPT, accept)\n    if content_type:\n        self.add_header(constants.HEADER_NAME_CONTENT_TYPE, content_type)\n</code></pre>"},{"location":"reference/mlclient/calls/TimestampGetCall/#mlclient.calls.TimestampGetCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint: str\n</code></pre> <p>Return the endpoint.</p>"},{"location":"reference/mlclient/calls/TransactionGetCall/","title":"TransactionGetCall","text":"<pre><code>from mlclient.calls import TransactionGetCall\n</code></pre>"},{"location":"reference/mlclient/calls/TransactionGetCall/#mlclient.calls.TransactionGetCall","title":"mlclient.calls.TransactionGetCall","text":"<pre><code>TransactionGetCall(\n    txid: str,\n    data_format: str = \"xml\",\n    database: str | None = None,\n)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A GET request to get a transaction status.</p> <p>An ApiCall implementation representing a single GET request to the /v1/transactions/{txid} endpoint.</p> <p>Retrieve status information for the transaction whose id matches the txid given in the request URI. Documentation of the REST Resource API: https://docs.marklogic.com/REST/GET/v1/transactions/[txid]</p> <p>Initialize TransactionGetCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>txid</code> <code>str</code> <p>A transaction identifier, as returned by TransactionsPostCall.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either json or xml (default). This parameter overrides the Accept header if both are present.</p> <code>'xml'</code> <code>database</code> <code>str</code> <p>Perform this operation on the named content database instead of the default content database associated with the REST API instance. The database can be identified by name or by database id.</p> <code>None</code> Source code in <code>mlclient/calls/transactions.py</code> <pre><code>def __init__(\n    self,\n    txid: str,\n    data_format: str = \"xml\",\n    database: str | None = None,\n):\n    \"\"\"Initialize TransactionGetCall instance.\n\n    Parameters\n    ----------\n    txid : str\n        A transaction identifier, as returned by TransactionsPostCall.\n    data_format : str\n        The format of the returned data. Can be either json or xml (default).\n        This parameter overrides the Accept header if both are present.\n    database : str\n        Perform this operation on the named content database\n        instead of the default content database associated with the REST API\n        instance. The database can be identified by name or by database id.\n    \"\"\"\n    data_format = data_format if data_format is not None else \"xml\"\n    self._validate_params(data_format)\n\n    super().__init__(\n        method=constants.METHOD_GET,\n        accept=utils.get_accept_header_for_format(data_format),\n    )\n    self._txid = txid\n    self.add_param(self._FORMAT_PARAM, data_format)\n    self.add_param(self._DATABASE_PARAM, database)\n</code></pre>"},{"location":"reference/mlclient/calls/TransactionGetCall/#mlclient.calls.TransactionGetCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Transaction call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Transaction call endpoint</p>"},{"location":"reference/mlclient/calls/TransactionPostCall/","title":"TransactionPostCall","text":"<pre><code>from mlclient.calls import TransactionPostCall\n</code></pre>"},{"location":"reference/mlclient/calls/TransactionPostCall/#mlclient.calls.TransactionPostCall","title":"mlclient.calls.TransactionPostCall","text":"<pre><code>TransactionPostCall(\n    txid: str, result: str, database: str | None = None\n)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A POST request to commit or roll back a transaction.</p> <p>An ApiCall implementation representing a single POST request to the /v1/transactions/{txid} endpoint.</p> <p>Commit or roll back the transaction whose id matches the txid given in the request URI. Documentation of the REST Resource API: https://docs.marklogic.com/REST/POST/v1/transactions/[txid]</p> <p>Initialize TransactionPostCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>txid</code> <code>str</code> <p>A transaction identifier, as returned by TransactionsPostCall.</p> required <code>result</code> <code>str</code> <p>The desired outcome of the transaction. Allowed values: commit, rollback.</p> required <code>database</code> <code>str</code> <p>Perform this operation on the named content database instead of the default content database associated with the REST API instance. The database can be identified by name or by database id.</p> <code>None</code> Source code in <code>mlclient/calls/transactions.py</code> <pre><code>def __init__(\n    self,\n    txid: str,\n    result: str,\n    database: str | None = None,\n):\n    \"\"\"Initialize TransactionPostCall instance.\n\n    Parameters\n    ----------\n    txid : str\n        A transaction identifier, as returned by TransactionsPostCall.\n    result : str\n        The desired outcome of the transaction. Allowed values: commit, rollback.\n    database : str\n        Perform this operation on the named content database\n        instead of the default content database associated with the REST API\n        instance. The database can be identified by name or by database id.\n    \"\"\"\n    self._validate_params(result)\n    super().__init__(\n        method=constants.METHOD_POST,\n        content_type=constants.HEADER_PLAIN_TEXT,\n    )\n    self._txid = txid\n    self.add_param(self._RESULT_PARAM, result)\n    self.add_param(self._DATABASE_PARAM, database)\n</code></pre>"},{"location":"reference/mlclient/calls/TransactionPostCall/#mlclient.calls.TransactionPostCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Transaction call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Transaction call endpoint</p>"},{"location":"reference/mlclient/calls/TransactionsPostCall/","title":"TransactionsPostCall","text":"<pre><code>from mlclient.calls import TransactionsPostCall\n</code></pre>"},{"location":"reference/mlclient/calls/TransactionsPostCall/#mlclient.calls.TransactionsPostCall","title":"mlclient.calls.TransactionsPostCall","text":"<pre><code>TransactionsPostCall(\n    name: str | None = None,\n    time_limit: int | None = None,\n    database: str | None = None,\n)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A POST request to create a multi-statement transaction.</p> <p>An ApiCall implementation representing a single POST request to the /v1/transactions endpoint.</p> <p>Create a multi-statement transaction. The resulting transaction id may be used in the txid request parameter of subsequent requests to force evaluation to take place in the context of the created transaction. Documentation of the REST Resource API: https://docs.marklogic.com/REST/POST/v1/transactions</p> <p>Initialize TransactionsPostCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>A symbolic name for the transaction. Default: client-txn.</p> <code>None</code> <code>time_limit</code> <code>int</code> <p>The transaction time limit to apply to this transaction, in seconds. If the transaction is not committed or rolled back within this time limit, the transaction is automatically rolled back.</p> <code>None</code> <code>database</code> <code>str</code> <p>Perform this operation on the named content database instead of the default content database associated with the REST API instance. The database can be identified by name or by database id.</p> <code>None</code> Source code in <code>mlclient/calls/transactions.py</code> <pre><code>def __init__(\n    self,\n    name: str | None = None,\n    time_limit: int | None = None,\n    database: str | None = None,\n):\n    \"\"\"Initialize TransactionsPostCall instance.\n\n    Parameters\n    ----------\n    name : str\n        A symbolic name for the transaction. Default: client-txn.\n    time_limit : int\n        The transaction time limit to apply to this transaction, in seconds.\n        If the transaction is not committed or rolled back within this time limit,\n        the transaction is automatically rolled back.\n    database : str\n        Perform this operation on the named content database\n        instead of the default content database associated with the REST API\n        instance. The database can be identified by name or by database id.\n    \"\"\"\n    super().__init__(\n        method=constants.METHOD_POST,\n        content_type=constants.HEADER_PLAIN_TEXT,\n    )\n    self.add_param(self._NAME_PARAM, name)\n    self.add_param(self._TIME_LIMIT_PARAM, time_limit)\n    self.add_param(self._DATABASE_PARAM, database)\n</code></pre>"},{"location":"reference/mlclient/calls/TransactionsPostCall/#mlclient.calls.TransactionsPostCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Transactions call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Transactions call endpoint</p>"},{"location":"reference/mlclient/calls/UserDeleteCall/","title":"UserDeleteCall","text":"<pre><code>from mlclient.calls import UserDeleteCall\n</code></pre>"},{"location":"reference/mlclient/calls/UserDeleteCall/#mlclient.calls.UserDeleteCall","title":"mlclient.calls.UserDeleteCall","text":"<pre><code>UserDeleteCall(user: str)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A DELETE request to remove a user.</p> <p>An ApiCall implementation representing a single DELETE request to the /manage/v2/users/{id|name} endpoint.</p> <p>This resource address deletes the named user from the named security database. Documentation of the REST Resource API: https://docs.marklogic.com/REST/DELETE/manage/v2/users/[id-or-name]</p> <p>Initialize UserDeleteCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>user</code> <code>str</code> <p>A user identifier. The user can be identified either by ID or name.</p> required Source code in <code>mlclient/calls/users.py</code> <pre><code>def __init__(\n    self,\n    user: str,\n):\n    \"\"\"Initialize UserDeleteCall instance.\n\n    Parameters\n    ----------\n    user : str\n        A user identifier. The user can be identified either by ID or name.\n    \"\"\"\n    super().__init__(method=constants.METHOD_DELETE)\n    self._user = user\n</code></pre>"},{"location":"reference/mlclient/calls/UserDeleteCall/#mlclient.calls.UserDeleteCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the User call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A User call endpoint</p>"},{"location":"reference/mlclient/calls/UserGetCall/","title":"UserGetCall","text":"<pre><code>from mlclient.calls import UserGetCall\n</code></pre>"},{"location":"reference/mlclient/calls/UserGetCall/#mlclient.calls.UserGetCall","title":"mlclient.calls.UserGetCall","text":"<pre><code>UserGetCall(\n    user: str,\n    data_format: str = \"xml\",\n    view: str = \"default\",\n)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A GET request to get user details.</p> <p>An ApiCall implementation representing a single GET request to the /manage/v2/users/{id|name} endpoint.</p> <p>This resource address returns the configuration for the specified user. Documentation of the REST Resource API: https://docs.marklogic.com/REST/GET/manage/v2/users/[id-or-name]</p> <p>Initialize UserGetCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>user</code> <code>str</code> <p>A user identifier. The user can be identified either by ID or name.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default). This parameter is not meaningful with view=edit.</p> <code>'xml'</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be: describe, or default.</p> <code>'default'</code> Source code in <code>mlclient/calls/users.py</code> <pre><code>def __init__(\n    self,\n    user: str,\n    data_format: str = \"xml\",\n    view: str = \"default\",\n):\n    \"\"\"Initialize UserGetCall instance.\n\n    Parameters\n    ----------\n    user : str\n        A user identifier. The user can be identified either by ID or name.\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n        This parameter is not meaningful with view=edit.\n    view : str\n        A specific view of the returned data. Can be: describe, or default.\n    \"\"\"\n    data_format = data_format if data_format is not None else \"xml\"\n    view = view if view is not None else \"default\"\n    self._validate_params(data_format, view)\n\n    super().__init__(\n        method=\"GET\",\n        accept=utils.get_accept_header_for_format(data_format),\n    )\n    self._user = user\n    self.add_param(self._FORMAT_PARAM, data_format)\n    self.add_param(self._VIEW_PARAM, view)\n</code></pre>"},{"location":"reference/mlclient/calls/UserGetCall/#mlclient.calls.UserGetCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the User call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A User call endpoint</p>"},{"location":"reference/mlclient/calls/UserPropertiesGetCall/","title":"UserPropertiesGetCall","text":"<pre><code>from mlclient.calls import UserPropertiesGetCall\n</code></pre>"},{"location":"reference/mlclient/calls/UserPropertiesGetCall/#mlclient.calls.UserPropertiesGetCall","title":"mlclient.calls.UserPropertiesGetCall","text":"<pre><code>UserPropertiesGetCall(user: str, data_format: str = 'xml')\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A GET request to get user properties.</p> <p>An ApiCall implementation representing a single GET request to the /manage/v2/users/{id|name}/properties endpoint.</p> <p>This resource address returns the properties of the specified user. Documentation of the REST Resource API: https://docs.marklogic.com/REST/GET/manage/v2/users/[id-or-name]/properties</p> <p>Initialize UserPropertiesGetCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>user</code> <code>str</code> <p>A user identifier. The user can be identified either by ID or name.</p> required <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either json or xml (default). This parameter overrides the Accept header if both are present.</p> <code>'xml'</code> Source code in <code>mlclient/calls/users.py</code> <pre><code>def __init__(\n    self,\n    user: str,\n    data_format: str = \"xml\",\n):\n    \"\"\"Initialize UserPropertiesGetCall instance.\n\n    Parameters\n    ----------\n    user : str\n        A user identifier. The user can be identified either by ID or name.\n    data_format : str\n        The format of the returned data. Can be either json or xml (default).\n        This parameter overrides the Accept header if both are present.\n    \"\"\"\n    data_format = data_format if data_format is not None else \"xml\"\n    self._validate_params(data_format)\n\n    super().__init__(\n        method=\"GET\",\n        accept=utils.get_accept_header_for_format(data_format),\n    )\n    self._user = user\n    self.add_param(self._FORMAT_PARAM, data_format)\n</code></pre>"},{"location":"reference/mlclient/calls/UserPropertiesGetCall/#mlclient.calls.UserPropertiesGetCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the User Properties call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A User Properties call endpoint</p>"},{"location":"reference/mlclient/calls/UserPropertiesPutCall/","title":"UserPropertiesPutCall","text":"<pre><code>from mlclient.calls import UserPropertiesPutCall\n</code></pre>"},{"location":"reference/mlclient/calls/UserPropertiesPutCall/#mlclient.calls.UserPropertiesPutCall","title":"mlclient.calls.UserPropertiesPutCall","text":"<pre><code>UserPropertiesPutCall(user: str, body: str | dict)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A PUT request to modify user properties.</p> <p>An ApiCall implementation representing a single PUT request to the /manage/v2/users/{id|name}/properties endpoint.</p> <p>This resource address can be used to update the properties for the specified user. Documentation of the REST Resource API: https://docs.marklogic.com/REST/PUT/manage/v2/users/[id-or-name]/properties</p> <p>Initialize UserPropertiesPutCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>user</code> <code>str</code> <p>A user identifier. The user can be identified either by ID or name.</p> required <code>body</code> <code>str | dict</code> <p>A user properties in XML or JSON format.</p> required Source code in <code>mlclient/calls/users.py</code> <pre><code>def __init__(\n    self,\n    user: str,\n    body: str | dict,\n):\n    \"\"\"Initialize UserPropertiesPutCall instance.\n\n    Parameters\n    ----------\n    user : str\n        A user identifier. The user can be identified either by ID or name.\n    body : str | dict\n        A user properties in XML or JSON format.\n    \"\"\"\n    self._validate_params(body)\n    content_type = utils.get_content_type_header_for_data(body)\n    if content_type == constants.HEADER_JSON and isinstance(body, str):\n        body = json.loads(body)\n    super().__init__(method=\"PUT\", content_type=content_type, body=body)\n    self._user = user\n</code></pre>"},{"location":"reference/mlclient/calls/UserPropertiesPutCall/#mlclient.calls.UserPropertiesPutCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the User Properties call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A User Properties call endpoint</p>"},{"location":"reference/mlclient/calls/UsersGetCall/","title":"UsersGetCall","text":"<pre><code>from mlclient.calls import UsersGetCall\n</code></pre>"},{"location":"reference/mlclient/calls/UsersGetCall/#mlclient.calls.UsersGetCall","title":"mlclient.calls.UsersGetCall","text":"<pre><code>UsersGetCall(\n    data_format: str = \"xml\", view: str = \"default\"\n)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A GET request to get users summary.</p> <p>An ApiCall implementation representing a single GET request to the /manage/v2/users endpoint.</p> <p>This resource address returns a summary of the users in the cluster. Documentation of the REST Resource API: https://docs.marklogic.com/REST/GET/manage/v2/users</p> <p>Initialize UsersGetCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>data_format</code> <code>str</code> <p>The format of the returned data. Can be either html, json, or xml (default).</p> <code>'xml'</code> <code>view</code> <code>str</code> <p>A specific view of the returned data. Can be: describe, or default.</p> <code>'default'</code> Source code in <code>mlclient/calls/users.py</code> <pre><code>def __init__(\n    self,\n    data_format: str = \"xml\",\n    view: str = \"default\",\n):\n    \"\"\"Initialize UsersGetCall instance.\n\n    Parameters\n    ----------\n    data_format : str\n        The format of the returned data. Can be either html, json, or xml (default).\n    view : str\n        A specific view of the returned data. Can be: describe, or default.\n    \"\"\"\n    data_format = data_format if data_format is not None else \"xml\"\n    view = view if view is not None else \"default\"\n    self._validate_params(data_format, view)\n\n    super().__init__(\n        method=\"GET\",\n        accept=utils.get_accept_header_for_format(data_format),\n    )\n    self.add_param(self._FORMAT_PARAM, data_format)\n    self.add_param(self._VIEW_PARAM, view)\n</code></pre>"},{"location":"reference/mlclient/calls/UsersGetCall/#mlclient.calls.UsersGetCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Users call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Users call endpoint</p>"},{"location":"reference/mlclient/calls/UsersPostCall/","title":"UsersPostCall","text":"<pre><code>from mlclient.calls import UsersPostCall\n</code></pre>"},{"location":"reference/mlclient/calls/UsersPostCall/#mlclient.calls.UsersPostCall","title":"mlclient.calls.UsersPostCall","text":"<pre><code>UsersPostCall(body: str | dict)\n</code></pre> <p>               Bases: <code>ApiCall</code></p> <p>A POST request to create a new user.</p> <p>An ApiCall implementation representing a single POST request to the /manage/v2/users endpoint.</p> <p>This resource address creates a new user in the security database. Documentation of the REST Resource API: https://docs.marklogic.com/REST/POST/manage/v2/users</p> <p>Initialize UsersPostCall instance.</p> <p>Parameters:</p> Name Type Description Default <code>body</code> <code>str | dict</code> <p>A user properties in XML or JSON format.</p> required Source code in <code>mlclient/calls/users.py</code> <pre><code>def __init__(\n    self,\n    body: str | dict,\n):\n    \"\"\"Initialize UsersPostCall instance.\n\n    Parameters\n    ----------\n    body : str | dict\n        A user properties in XML or JSON format.\n    \"\"\"\n    self._validate_params(body)\n    content_type = utils.get_content_type_header_for_data(body)\n    if content_type == constants.HEADER_JSON and isinstance(body, str):\n        body = json.loads(body)\n    super().__init__(method=\"POST\", content_type=content_type, body=body)\n</code></pre>"},{"location":"reference/mlclient/calls/UsersPostCall/#mlclient.calls.UsersPostCall.endpoint","title":"endpoint  <code>property</code>","text":"<pre><code>endpoint\n</code></pre> <p>An endpoint for the Users call.</p> <p>Returns:</p> Type Description <code>str</code> <p>A Users call endpoint</p>"},{"location":"reference/mlclient/clients/","title":"<code>mlclient.clients</code>","text":""},{"location":"reference/mlclient/clients/#mlclient.clients","title":"mlclient.clients","text":"<p>HTTP transport and execution of MarkLogic API calls.</p>"},{"location":"reference/mlclient/clients/ApiClient/","title":"ApiClient","text":"<pre><code>from mlclient.clients import ApiClient\n</code></pre>"},{"location":"reference/mlclient/clients/ApiClient/#mlclient.clients.ApiClient","title":"mlclient.clients.ApiClient","text":"<pre><code>ApiClient(http: HttpClient)\n</code></pre> <p>Mid-level client providing call() for ApiCall objects.</p> Source code in <code>mlclient/clients/api.py</code> <pre><code>def __init__(self, http: HttpClient):\n    self._http = http\n</code></pre>"},{"location":"reference/mlclient/clients/ApiClient/#mlclient.clients.ApiClient.call","title":"call","text":"<pre><code>call(call_: ApiCall, *, timeout=UNSET) -&gt; Response\n</code></pre> <p>Send a request using an ApiCall object.</p> <p>Parameters:</p> Name Type Description Default <code>call_</code> <code>ApiCall</code> <p>A specific endpoint call implementation</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout applied to this call only. Unset uses the client's configured timeout. None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter, header or body.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/clients/api.py</code> <pre><code>def call(self, call_: ApiCall, *, timeout=UNSET) -&gt; Response:\n    \"\"\"Send a request using an ApiCall object.\n\n    Parameters\n    ----------\n    call_ : ApiCall\n        A specific endpoint call implementation\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout applied to this call only. Unset uses the\n        client's configured timeout. None disables every HTTP timeout; a\n        number sets all four components to that many seconds; an\n        httpx.Timeout overrides them. It is an execution option, never sent\n        as a request parameter, header or body.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return self._http.request(\n        method=call_.method,\n        endpoint=call_.endpoint,\n        params=call_.params,\n        headers=call_.headers,\n        body=call_.body,\n        timeout=timeout,\n    )\n</code></pre>"},{"location":"reference/mlclient/clients/AsyncApiClient/","title":"AsyncApiClient","text":"<pre><code>from mlclient.clients import AsyncApiClient\n</code></pre>"},{"location":"reference/mlclient/clients/AsyncApiClient/#mlclient.clients.AsyncApiClient","title":"mlclient.clients.AsyncApiClient","text":"<pre><code>AsyncApiClient(http: AsyncHttpClient)\n</code></pre> <p>Async mid-level client providing call() for ApiCall objects.</p> Source code in <code>mlclient/clients/api.py</code> <pre><code>def __init__(self, http: AsyncHttpClient):\n    self._http = http\n</code></pre>"},{"location":"reference/mlclient/clients/AsyncApiClient/#mlclient.clients.AsyncApiClient.call","title":"call  <code>async</code>","text":"<pre><code>call(call_: ApiCall, *, timeout=UNSET) -&gt; Response\n</code></pre> <p>Send a request using an ApiCall object.</p> <p>Parameters:</p> Name Type Description Default <code>call_</code> <code>ApiCall</code> <p>A specific endpoint call implementation</p> required <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout applied to this call only. Unset uses the client's configured timeout. None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It is an execution option, never sent as a request parameter, header or body.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/clients/api.py</code> <pre><code>async def call(self, call_: ApiCall, *, timeout=UNSET) -&gt; Response:\n    \"\"\"Send a request using an ApiCall object.\n\n    Parameters\n    ----------\n    call_ : ApiCall\n        A specific endpoint call implementation\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout applied to this call only. Unset uses the\n        client's configured timeout. None disables every HTTP timeout; a\n        number sets all four components to that many seconds; an\n        httpx.Timeout overrides them. It is an execution option, never sent\n        as a request parameter, header or body.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return await self._http.request(\n        method=call_.method,\n        endpoint=call_.endpoint,\n        params=call_.params,\n        headers=call_.headers,\n        body=call_.body,\n        timeout=timeout,\n    )\n</code></pre>"},{"location":"reference/mlclient/clients/AsyncHttpClient/","title":"AsyncHttpClient","text":"<pre><code>from mlclient.clients import AsyncHttpClient\n</code></pre>"},{"location":"reference/mlclient/clients/AsyncHttpClient/#mlclient.clients.AsyncHttpClient","title":"mlclient.clients.AsyncHttpClient","text":"<pre><code>AsyncHttpClient(**kwargs)\n</code></pre> <p>               Bases: <code>_HttpClientBase</code></p> <p>An async low-level class used to send HTTP requests to a MarkLogic instance.</p> <p>Attributes:</p> Name Type Description <code>config</code> <code>HTTPConfig</code> <p>the resolved connection and authentication configuration</p> <code>base_url</code> <code>str</code> <p>a base url built based on the protocol, the host name and the port</p> <p>Initialize AsyncHttpClient instance.</p> <p>Parameters:</p> Name Type Description Default <code>protocol</code> <code>str</code> <p>A protocol used for HTTP requests (http / https)</p> <code>\"http\"</code> <code>host</code> <code>str</code> <p>A host name</p> <code>\"localhost\"</code> <code>port</code> <code>int</code> <p>An App Service port</p> <code>8000</code> <code>auth</code> <code>str | Auth | AuthConfig | None</code> <p>An authentication method</p> <code>\"digest\"</code> <code>username</code> <code>str</code> <p>A username</p> <code>\"admin\"</code> <code>password</code> <code>str</code> <p>A password</p> <code>\"admin\"</code> <code>ssl</code> <code>SSLConfig | None</code> <p>SSL/TLS configuration</p> <code>None</code> <code>cloud</code> <code>CloudConfig | None</code> <p>MarkLogic Cloud configuration</p> <code>None</code> <code>retry</code> <code>Retry | None</code> <p>A retry strategy</p> <code>Retry(total=5, backoff_factor=0.5)</code> <code>limits</code> <code>Limits | None</code> <p>Connection-pool limits; None defers to httpx's own default</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A request timeout. Unset uses DEFAULT_TIMEOUT (connect=5, read=60, write=60, pool=5 seconds). None disables every HTTP timeout. A number sets all four components to that many seconds. An httpx.Timeout fully overrides the timeout. The config argument, when given, takes precedence over this argument.</p> <code>unset</code> Source code in <code>mlclient/clients/http.py</code> <pre><code>def __init__(self, **kwargs):\n    \"\"\"Initialize AsyncHttpClient instance.\n\n    Parameters\n    ----------\n    protocol : str, default \"http\"\n        A protocol used for HTTP requests (http / https)\n    host : str, default \"localhost\"\n        A host name\n    port : int, default 8000\n        An App Service port\n    auth : str | httpx.Auth | AuthConfig | None, default \"digest\"\n        An authentication method\n    username : str, default \"admin\"\n        A username\n    password : str, default \"admin\"\n        A password\n    ssl : SSLConfig | None, default None\n        SSL/TLS configuration\n    cloud : CloudConfig | None, default None\n        MarkLogic Cloud configuration\n    retry : Retry | None, default Retry(total=5, backoff_factor=0.5)\n        A retry strategy\n    limits : httpx.Limits | None, default None\n        Connection-pool limits; None defers to httpx's own default\n    timeout : httpx.Timeout | float | None, default unset\n        A request timeout. Unset uses DEFAULT_TIMEOUT (connect=5, read=60,\n        write=60, pool=5 seconds). None disables every HTTP timeout. A\n        number sets all four components to that many seconds. An\n        httpx.Timeout fully overrides the timeout. The config argument, when\n        given, takes precedence over this argument.\n    \"\"\"\n    super().__init__(**kwargs)\n</code></pre>"},{"location":"reference/mlclient/clients/AsyncHttpClient/#mlclient.clients.AsyncHttpClient.connect","title":"connect  <code>async</code>","text":"<pre><code>connect()\n</code></pre> <p>Start an async HTTP session unless already connected.</p> Source code in <code>mlclient/clients/http.py</code> <pre><code>async def connect(self):\n    \"\"\"Start an async HTTP session unless already connected.\"\"\"\n    if self.is_connected():\n        return\n    logger.debug(\"Initiating a connection with %s\", self.base_url)\n    transport = AsyncHTTPTransport(**self.config.transport_options())\n    self._client = AsyncClient(\n        transport=RetryTransport(transport=transport, retry=self.config.retry),\n        timeout=self.config.timeout,\n    )\n</code></pre>"},{"location":"reference/mlclient/clients/AsyncHttpClient/#mlclient.clients.AsyncHttpClient.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(\n    endpoint: str,\n    *,\n    params: dict | None = None,\n    headers: dict | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Send an async DELETE request.</p> <p>Parameters:</p> Name Type Description Default <code>endpoint</code> <code>str</code> <p>A REST endpoint to call</p> required <code>params</code> <code>dict | None</code> <p>Request parameters</p> <code>None</code> <code>headers</code> <code>dict | None</code> <p>Request headers</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout. Unset uses the client's configured timeout. None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It applies only to this request and does not change the client's configuration.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/clients/http.py</code> <pre><code>async def delete(\n    self,\n    endpoint: str,\n    *,\n    params: dict | None = None,\n    headers: dict | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Send an async DELETE request.\n\n    Parameters\n    ----------\n    endpoint : str\n        A REST endpoint to call\n    params : dict | None\n        Request parameters\n    headers : dict | None\n        Request headers\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout. Unset uses the client's configured timeout.\n        None disables every HTTP timeout; a number sets all four components\n        to that many seconds; an httpx.Timeout overrides them. It applies\n        only to this request and does not change the client's configuration.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return await self.request(\n        \"DELETE\",\n        endpoint,\n        params=params,\n        headers=headers,\n        timeout=timeout,\n    )\n</code></pre>"},{"location":"reference/mlclient/clients/AsyncHttpClient/#mlclient.clients.AsyncHttpClient.disconnect","title":"disconnect  <code>async</code>","text":"<pre><code>disconnect()\n</code></pre> <p>Close an async HTTP session.</p> Source code in <code>mlclient/clients/http.py</code> <pre><code>async def disconnect(self):\n    \"\"\"Close an async HTTP session.\"\"\"\n    if self._client:\n        logger.debug(\"Closing a connection\")\n        await self._client.aclose()\n        self._client = None\n</code></pre>"},{"location":"reference/mlclient/clients/AsyncHttpClient/#mlclient.clients.AsyncHttpClient.get","title":"get  <code>async</code>","text":"<pre><code>get(\n    endpoint: str,\n    *,\n    params: dict | None = None,\n    headers: dict | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Send an async GET request.</p> <p>Parameters:</p> Name Type Description Default <code>endpoint</code> <code>str</code> <p>A REST endpoint to call</p> required <code>params</code> <code>dict | None</code> <p>Request parameters</p> <code>None</code> <code>headers</code> <code>dict | None</code> <p>Request headers</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout. Unset uses the client's configured timeout. None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It applies only to this request and does not change the client's configuration.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/clients/http.py</code> <pre><code>async def get(\n    self,\n    endpoint: str,\n    *,\n    params: dict | None = None,\n    headers: dict | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Send an async GET request.\n\n    Parameters\n    ----------\n    endpoint : str\n        A REST endpoint to call\n    params : dict | None\n        Request parameters\n    headers : dict | None\n        Request headers\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout. Unset uses the client's configured timeout.\n        None disables every HTTP timeout; a number sets all four components\n        to that many seconds; an httpx.Timeout overrides them. It applies\n        only to this request and does not change the client's configuration.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return await self.request(\n        \"GET\",\n        endpoint,\n        params=params,\n        headers=headers,\n        timeout=timeout,\n    )\n</code></pre>"},{"location":"reference/mlclient/clients/AsyncHttpClient/#mlclient.clients.AsyncHttpClient.head","title":"head  <code>async</code>","text":"<pre><code>head(\n    endpoint: str,\n    *,\n    params: dict | None = None,\n    headers: dict | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Send an async HEAD request.</p> <p>Parameters:</p> Name Type Description Default <code>endpoint</code> <code>str</code> <p>A REST endpoint to call</p> required <code>params</code> <code>dict | None</code> <p>Request parameters</p> <code>None</code> <code>headers</code> <code>dict | None</code> <p>Request headers</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout. Unset uses the client's configured timeout. None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It applies only to this request and does not change the client's configuration.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/clients/http.py</code> <pre><code>async def head(\n    self,\n    endpoint: str,\n    *,\n    params: dict | None = None,\n    headers: dict | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Send an async HEAD request.\n\n    Parameters\n    ----------\n    endpoint : str\n        A REST endpoint to call\n    params : dict | None\n        Request parameters\n    headers : dict | None\n        Request headers\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout. Unset uses the client's configured timeout.\n        None disables every HTTP timeout; a number sets all four components\n        to that many seconds; an httpx.Timeout overrides them. It applies\n        only to this request and does not change the client's configuration.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return await self.request(\n        \"HEAD\",\n        endpoint,\n        params=params,\n        headers=headers,\n        timeout=timeout,\n    )\n</code></pre>"},{"location":"reference/mlclient/clients/AsyncHttpClient/#mlclient.clients.AsyncHttpClient.is_connected","title":"is_connected","text":"<pre><code>is_connected() -&gt; bool\n</code></pre> <p>Return a connection status.</p> <p>Returns:</p> Type Description <code>bool</code> <p>True if the client has started a connection; otherwise False</p> Source code in <code>mlclient/clients/http.py</code> <pre><code>def is_connected(self) -&gt; bool:\n    \"\"\"Return a connection status.\n\n    Returns\n    -------\n    bool\n        True if the client has started a connection; otherwise False\n    \"\"\"\n    return self._client is not None\n</code></pre>"},{"location":"reference/mlclient/clients/AsyncHttpClient/#mlclient.clients.AsyncHttpClient.post","title":"post  <code>async</code>","text":"<pre><code>post(\n    endpoint: str,\n    body: str | bytes | dict | None = None,\n    *,\n    params: dict | None = None,\n    headers: dict | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Send an async POST request.</p> <p>Parameters:</p> Name Type Description Default <code>endpoint</code> <code>str</code> <p>A REST endpoint to call</p> required <code>body</code> <code>str | bytes | dict | None</code> <p>A request body</p> <code>None</code> <code>params</code> <code>dict | None</code> <p>Request parameters</p> <code>None</code> <code>headers</code> <code>dict | None</code> <p>Request headers</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout. Unset uses the client's configured timeout. None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It applies only to this request and does not change the client's configuration.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/clients/http.py</code> <pre><code>async def post(\n    self,\n    endpoint: str,\n    body: str | bytes | dict | None = None,\n    *,\n    params: dict | None = None,\n    headers: dict | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Send an async POST request.\n\n    Parameters\n    ----------\n    endpoint : str\n        A REST endpoint to call\n    body : str | bytes | dict | None\n        A request body\n    params : dict | None\n        Request parameters\n    headers : dict | None\n        Request headers\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout. Unset uses the client's configured timeout.\n        None disables every HTTP timeout; a number sets all four components\n        to that many seconds; an httpx.Timeout overrides them. It applies\n        only to this request and does not change the client's configuration.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return await self.request(\n        \"POST\",\n        endpoint,\n        body,\n        params=params,\n        headers=headers,\n        timeout=timeout,\n    )\n</code></pre>"},{"location":"reference/mlclient/clients/AsyncHttpClient/#mlclient.clients.AsyncHttpClient.put","title":"put  <code>async</code>","text":"<pre><code>put(\n    endpoint: str,\n    body: str | bytes | dict | None = None,\n    *,\n    params: dict | None = None,\n    headers: dict | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Send an async PUT request.</p> <p>Parameters:</p> Name Type Description Default <code>endpoint</code> <code>str</code> <p>A REST endpoint to call</p> required <code>body</code> <code>str | bytes | dict | None</code> <p>A request body</p> <code>None</code> <code>params</code> <code>dict | None</code> <p>Request parameters</p> <code>None</code> <code>headers</code> <code>dict | None</code> <p>Request headers</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout. Unset uses the client's configured timeout. None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It applies only to this request and does not change the client's configuration.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/clients/http.py</code> <pre><code>async def put(\n    self,\n    endpoint: str,\n    body: str | bytes | dict | None = None,\n    *,\n    params: dict | None = None,\n    headers: dict | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Send an async PUT request.\n\n    Parameters\n    ----------\n    endpoint : str\n        A REST endpoint to call\n    body : str | bytes | dict | None\n        A request body\n    params : dict | None\n        Request parameters\n    headers : dict | None\n        Request headers\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout. Unset uses the client's configured timeout.\n        None disables every HTTP timeout; a number sets all four components\n        to that many seconds; an httpx.Timeout overrides them. It applies\n        only to this request and does not change the client's configuration.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return await self.request(\n        \"PUT\",\n        endpoint,\n        body,\n        params=params,\n        headers=headers,\n        timeout=timeout,\n    )\n</code></pre>"},{"location":"reference/mlclient/clients/AsyncHttpClient/#mlclient.clients.AsyncHttpClient.request","title":"request  <code>async</code>","text":"<pre><code>request(\n    method: str,\n    endpoint: str,\n    body: str | bytes | dict | None = None,\n    *,\n    params: dict | None = None,\n    headers: dict | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Send an async HTTP request.</p> <p>Parameters:</p> Name Type Description Default <code>method</code> <code>str</code> <p>An HTTP request method</p> required <code>endpoint</code> <code>str</code> <p>A REST endpoint to call</p> required <code>body</code> <code>str | bytes | dict | None</code> <p>A request body</p> <code>None</code> <code>params</code> <code>dict | None</code> <p>Request parameters</p> <code>None</code> <code>headers</code> <code>dict | None</code> <p>Request headers</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout. Unset uses the client's configured timeout. None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It applies only to this request and does not change the client's configuration.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/clients/http.py</code> <pre><code>async def request(\n    self,\n    method: str,\n    endpoint: str,\n    body: str | bytes | dict | None = None,\n    *,\n    params: dict | None = None,\n    headers: dict | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Send an async HTTP request.\n\n    Parameters\n    ----------\n    method : str\n        An HTTP request method\n    endpoint : str\n        A REST endpoint to call\n    body : str | bytes | dict | None\n        A request body\n    params : dict | None\n        Request parameters\n    headers : dict | None\n        Request headers\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout. Unset uses the client's configured timeout.\n        None disables every HTTP timeout; a number sets all four components\n        to that many seconds; an httpx.Timeout overrides them. It applies\n        only to this request and does not change the client's configuration.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    request = self._prepare_request(params, headers, body)\n    if timeout is not UNSET:\n        request[\"timeout\"] = timeout\n    resp = await self._send_request(method, endpoint, request)\n    self._log_response(method, endpoint, resp)\n    return resp\n</code></pre>"},{"location":"reference/mlclient/clients/HttpClient/","title":"HttpClient","text":"<pre><code>from mlclient.clients import HttpClient\n</code></pre>"},{"location":"reference/mlclient/clients/HttpClient/#mlclient.clients.HttpClient","title":"mlclient.clients.HttpClient","text":"<pre><code>HttpClient(**kwargs)\n</code></pre> <p>               Bases: <code>_HttpClientBase</code></p> <p>A low-level class used to send HTTP requests to a MarkLogic instance.</p> <p>Attributes:</p> Name Type Description <code>config</code> <code>HTTPConfig</code> <p>the resolved connection and authentication configuration</p> <code>base_url</code> <code>str</code> <p>a base url built based on the protocol, the host name and the port</p> <p>Initialize HttpClient instance.</p> <p>Parameters:</p> Name Type Description Default <code>protocol</code> <code>str</code> <p>A protocol used for HTTP requests (http / https)</p> <code>\"http\"</code> <code>host</code> <code>str</code> <p>A host name</p> <code>\"localhost\"</code> <code>port</code> <code>int</code> <p>An App Service port</p> <code>8000</code> <code>auth</code> <code>str | Auth | AuthConfig | None</code> <p>An authentication method</p> <code>\"digest\"</code> <code>username</code> <code>str</code> <p>A username</p> <code>\"admin\"</code> <code>password</code> <code>str</code> <p>A password</p> <code>\"admin\"</code> <code>ssl</code> <code>SSLConfig | None</code> <p>SSL/TLS configuration</p> <code>None</code> <code>cloud</code> <code>CloudConfig | None</code> <p>MarkLogic Cloud configuration</p> <code>None</code> <code>retry</code> <code>Retry | None</code> <p>A retry strategy</p> <code>Retry(total=5, backoff_factor=0.5)</code> <code>limits</code> <code>Limits | None</code> <p>Connection-pool limits; None defers to httpx's own default</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A request timeout. Unset uses DEFAULT_TIMEOUT (connect=5, read=60, write=60, pool=5 seconds). None disables every HTTP timeout. A number sets all four components to that many seconds. An httpx.Timeout fully overrides the timeout. The config argument, when given, takes precedence over this argument.</p> <code>unset</code> Source code in <code>mlclient/clients/http.py</code> <pre><code>def __init__(self, **kwargs):\n    \"\"\"Initialize HttpClient instance.\n\n    Parameters\n    ----------\n    protocol : str, default \"http\"\n        A protocol used for HTTP requests (http / https)\n    host : str, default \"localhost\"\n        A host name\n    port : int, default 8000\n        An App Service port\n    auth : str | httpx.Auth | AuthConfig | None, default \"digest\"\n        An authentication method\n    username : str, default \"admin\"\n        A username\n    password : str, default \"admin\"\n        A password\n    ssl : SSLConfig | None, default None\n        SSL/TLS configuration\n    cloud : CloudConfig | None, default None\n        MarkLogic Cloud configuration\n    retry : Retry | None, default Retry(total=5, backoff_factor=0.5)\n        A retry strategy\n    limits : httpx.Limits | None, default None\n        Connection-pool limits; None defers to httpx's own default\n    timeout : httpx.Timeout | float | None, default unset\n        A request timeout. Unset uses DEFAULT_TIMEOUT (connect=5, read=60,\n        write=60, pool=5 seconds). None disables every HTTP timeout. A\n        number sets all four components to that many seconds. An\n        httpx.Timeout fully overrides the timeout. The config argument, when\n        given, takes precedence over this argument.\n    \"\"\"\n    super().__init__(**kwargs)\n</code></pre>"},{"location":"reference/mlclient/clients/HttpClient/#mlclient.clients.HttpClient.connect","title":"connect","text":"<pre><code>connect()\n</code></pre> <p>Start an HTTP session unless already connected.</p> Source code in <code>mlclient/clients/http.py</code> <pre><code>def connect(self):\n    \"\"\"Start an HTTP session unless already connected.\"\"\"\n    if self.is_connected():\n        return\n    logger.debug(\"Initiating a connection with %s\", self.base_url)\n    transport = HTTPTransport(**self.config.transport_options())\n    self._client = Client(\n        transport=RetryTransport(transport=transport, retry=self.config.retry),\n        timeout=self.config.timeout,\n    )\n</code></pre>"},{"location":"reference/mlclient/clients/HttpClient/#mlclient.clients.HttpClient.delete","title":"delete","text":"<pre><code>delete(\n    endpoint: str,\n    *,\n    params: dict | None = None,\n    headers: dict | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Send a DELETE request.</p> <p>Parameters:</p> Name Type Description Default <code>endpoint</code> <code>str</code> <p>A REST endpoint to call</p> required <code>params</code> <code>dict | None</code> <p>Request parameters</p> <code>None</code> <code>headers</code> <code>dict | None</code> <p>Request headers</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout. Unset uses the client's configured timeout. None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It applies only to this request and does not change the client's configuration.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/clients/http.py</code> <pre><code>def delete(\n    self,\n    endpoint: str,\n    *,\n    params: dict | None = None,\n    headers: dict | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Send a DELETE request.\n\n    Parameters\n    ----------\n    endpoint : str\n        A REST endpoint to call\n    params : dict | None\n        Request parameters\n    headers : dict | None\n        Request headers\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout. Unset uses the client's configured timeout.\n        None disables every HTTP timeout; a number sets all four components\n        to that many seconds; an httpx.Timeout overrides them. It applies\n        only to this request and does not change the client's configuration.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return self.request(\n        \"DELETE\",\n        endpoint,\n        params=params,\n        headers=headers,\n        timeout=timeout,\n    )\n</code></pre>"},{"location":"reference/mlclient/clients/HttpClient/#mlclient.clients.HttpClient.disconnect","title":"disconnect","text":"<pre><code>disconnect()\n</code></pre> <p>Close an HTTP session.</p> Source code in <code>mlclient/clients/http.py</code> <pre><code>def disconnect(self):\n    \"\"\"Close an HTTP session.\"\"\"\n    if self._client:\n        logger.debug(\"Closing a connection\")\n        self._client.close()\n        self._client = None\n</code></pre>"},{"location":"reference/mlclient/clients/HttpClient/#mlclient.clients.HttpClient.get","title":"get","text":"<pre><code>get(\n    endpoint: str,\n    *,\n    params: dict | None = None,\n    headers: dict | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Send a GET request.</p> <p>Parameters:</p> Name Type Description Default <code>endpoint</code> <code>str</code> <p>A REST endpoint to call</p> required <code>params</code> <code>dict | None</code> <p>Request parameters</p> <code>None</code> <code>headers</code> <code>dict | None</code> <p>Request headers</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout. Unset uses the client's configured timeout. None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It applies only to this request and does not change the client's configuration.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/clients/http.py</code> <pre><code>def get(\n    self,\n    endpoint: str,\n    *,\n    params: dict | None = None,\n    headers: dict | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Send a GET request.\n\n    Parameters\n    ----------\n    endpoint : str\n        A REST endpoint to call\n    params : dict | None\n        Request parameters\n    headers : dict | None\n        Request headers\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout. Unset uses the client's configured timeout.\n        None disables every HTTP timeout; a number sets all four components\n        to that many seconds; an httpx.Timeout overrides them. It applies\n        only to this request and does not change the client's configuration.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return self.request(\n        \"GET\",\n        endpoint,\n        params=params,\n        headers=headers,\n        timeout=timeout,\n    )\n</code></pre>"},{"location":"reference/mlclient/clients/HttpClient/#mlclient.clients.HttpClient.head","title":"head","text":"<pre><code>head(\n    endpoint: str,\n    *,\n    params: dict | None = None,\n    headers: dict | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Send a HEAD request.</p> <p>Parameters:</p> Name Type Description Default <code>endpoint</code> <code>str</code> <p>A REST endpoint to call</p> required <code>params</code> <code>dict | None</code> <p>Request parameters</p> <code>None</code> <code>headers</code> <code>dict | None</code> <p>Request headers</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout. Unset uses the client's configured timeout. None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It applies only to this request and does not change the client's configuration.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/clients/http.py</code> <pre><code>def head(\n    self,\n    endpoint: str,\n    *,\n    params: dict | None = None,\n    headers: dict | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Send a HEAD request.\n\n    Parameters\n    ----------\n    endpoint : str\n        A REST endpoint to call\n    params : dict | None\n        Request parameters\n    headers : dict | None\n        Request headers\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout. Unset uses the client's configured timeout.\n        None disables every HTTP timeout; a number sets all four components\n        to that many seconds; an httpx.Timeout overrides them. It applies\n        only to this request and does not change the client's configuration.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return self.request(\n        \"HEAD\",\n        endpoint,\n        params=params,\n        headers=headers,\n        timeout=timeout,\n    )\n</code></pre>"},{"location":"reference/mlclient/clients/HttpClient/#mlclient.clients.HttpClient.is_connected","title":"is_connected","text":"<pre><code>is_connected() -&gt; bool\n</code></pre> <p>Return a connection status.</p> <p>Returns:</p> Type Description <code>bool</code> <p>True if the client has started a connection; otherwise False</p> Source code in <code>mlclient/clients/http.py</code> <pre><code>def is_connected(self) -&gt; bool:\n    \"\"\"Return a connection status.\n\n    Returns\n    -------\n    bool\n        True if the client has started a connection; otherwise False\n    \"\"\"\n    return self._client is not None\n</code></pre>"},{"location":"reference/mlclient/clients/HttpClient/#mlclient.clients.HttpClient.post","title":"post","text":"<pre><code>post(\n    endpoint: str,\n    body: str | bytes | dict | None = None,\n    *,\n    params: dict | None = None,\n    headers: dict | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Send a POST request.</p> <p>Parameters:</p> Name Type Description Default <code>endpoint</code> <code>str</code> <p>A REST endpoint to call</p> required <code>body</code> <code>str | bytes | dict | None</code> <p>A request body</p> <code>None</code> <code>params</code> <code>dict | None</code> <p>Request parameters</p> <code>None</code> <code>headers</code> <code>dict | None</code> <p>Request headers</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout. Unset uses the client's configured timeout. None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It applies only to this request and does not change the client's configuration.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/clients/http.py</code> <pre><code>def post(\n    self,\n    endpoint: str,\n    body: str | bytes | dict | None = None,\n    *,\n    params: dict | None = None,\n    headers: dict | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Send a POST request.\n\n    Parameters\n    ----------\n    endpoint : str\n        A REST endpoint to call\n    body : str | bytes | dict | None\n        A request body\n    params : dict | None\n        Request parameters\n    headers : dict | None\n        Request headers\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout. Unset uses the client's configured timeout.\n        None disables every HTTP timeout; a number sets all four components\n        to that many seconds; an httpx.Timeout overrides them. It applies\n        only to this request and does not change the client's configuration.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return self.request(\n        \"POST\",\n        endpoint,\n        body,\n        params=params,\n        headers=headers,\n        timeout=timeout,\n    )\n</code></pre>"},{"location":"reference/mlclient/clients/HttpClient/#mlclient.clients.HttpClient.put","title":"put","text":"<pre><code>put(\n    endpoint: str,\n    body: str | bytes | dict | None = None,\n    *,\n    params: dict | None = None,\n    headers: dict | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Send a PUT request.</p> <p>Parameters:</p> Name Type Description Default <code>endpoint</code> <code>str</code> <p>A REST endpoint to call</p> required <code>body</code> <code>str | bytes | dict | None</code> <p>A request body</p> <code>None</code> <code>params</code> <code>dict | None</code> <p>Request parameters</p> <code>None</code> <code>headers</code> <code>dict | None</code> <p>Request headers</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout. Unset uses the client's configured timeout. None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It applies only to this request and does not change the client's configuration.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/clients/http.py</code> <pre><code>def put(\n    self,\n    endpoint: str,\n    body: str | bytes | dict | None = None,\n    *,\n    params: dict | None = None,\n    headers: dict | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Send a PUT request.\n\n    Parameters\n    ----------\n    endpoint : str\n        A REST endpoint to call\n    body : str | bytes | dict | None\n        A request body\n    params : dict | None\n        Request parameters\n    headers : dict | None\n        Request headers\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout. Unset uses the client's configured timeout.\n        None disables every HTTP timeout; a number sets all four components\n        to that many seconds; an httpx.Timeout overrides them. It applies\n        only to this request and does not change the client's configuration.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return self.request(\n        \"PUT\",\n        endpoint,\n        body,\n        params=params,\n        headers=headers,\n        timeout=timeout,\n    )\n</code></pre>"},{"location":"reference/mlclient/clients/HttpClient/#mlclient.clients.HttpClient.request","title":"request","text":"<pre><code>request(\n    method: str,\n    endpoint: str,\n    body: str | bytes | dict | None = None,\n    *,\n    params: dict | None = None,\n    headers: dict | None = None,\n    timeout=UNSET,\n) -&gt; Response\n</code></pre> <p>Send an HTTP request.</p> <p>Parameters:</p> Name Type Description Default <code>method</code> <code>str</code> <p>An HTTP request method</p> required <code>endpoint</code> <code>str</code> <p>A REST endpoint to call</p> required <code>body</code> <code>str | bytes | dict | None</code> <p>A request body</p> <code>None</code> <code>params</code> <code>dict | None</code> <p>Request parameters</p> <code>None</code> <code>headers</code> <code>dict | None</code> <p>Request headers</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request timeout. Unset uses the client's configured timeout. None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It applies only to this request and does not change the client's configuration.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Response</code> <p>An HTTP response</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/clients/http.py</code> <pre><code>def request(\n    self,\n    method: str,\n    endpoint: str,\n    body: str | bytes | dict | None = None,\n    *,\n    params: dict | None = None,\n    headers: dict | None = None,\n    timeout=UNSET,\n) -&gt; Response:\n    \"\"\"Send an HTTP request.\n\n    Parameters\n    ----------\n    method : str\n        An HTTP request method\n    endpoint : str\n        A REST endpoint to call\n    body : str | bytes | dict | None\n        A request body\n    params : dict | None\n        Request parameters\n    headers : dict | None\n        Request headers\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request timeout. Unset uses the client's configured timeout.\n        None disables every HTTP timeout; a number sets all four components\n        to that many seconds; an httpx.Timeout overrides them. It applies\n        only to this request and does not change the client's configuration.\n\n    Returns\n    -------\n    Response\n        An HTTP response\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    request = self._prepare_request(params, headers, body)\n    if timeout is not UNSET:\n        request[\"timeout\"] = timeout\n    resp = self._send_request(method, endpoint, request)\n    self._log_response(method, endpoint, resp)\n    return resp\n</code></pre>"},{"location":"reference/mlclient/connection/","title":"<code>mlclient.connection</code>","text":""},{"location":"reference/mlclient/connection/#mlclient.connection","title":"mlclient.connection","text":"<p>Connection settings and standard MarkLogic server ports.</p> <p>Use SSLConfig for TLS and client certificates, or CloudConfig for a MarkLogic Cloud gateway. HTTPConfig combines these settings with authentication and request defaults. Connection normalization is an implementation detail.</p>"},{"location":"reference/mlclient/connection/#mlclient.connection.MARKLOGIC_ADMIN_PORT","title":"MARKLOGIC_ADMIN_PORT  <code>module-attribute</code>","text":"<pre><code>MARKLOGIC_ADMIN_PORT = 8001\n</code></pre>"},{"location":"reference/mlclient/connection/#mlclient.connection.MARKLOGIC_APP_SERVICES_PORT","title":"MARKLOGIC_APP_SERVICES_PORT  <code>module-attribute</code>","text":"<pre><code>MARKLOGIC_APP_SERVICES_PORT = 8000\n</code></pre>"},{"location":"reference/mlclient/connection/#mlclient.connection.MARKLOGIC_HEALTHCHECK_PORT","title":"MARKLOGIC_HEALTHCHECK_PORT  <code>module-attribute</code>","text":"<pre><code>MARKLOGIC_HEALTHCHECK_PORT = 7997\n</code></pre>"},{"location":"reference/mlclient/connection/#mlclient.connection.MARKLOGIC_MANAGE_PORT","title":"MARKLOGIC_MANAGE_PORT  <code>module-attribute</code>","text":"<pre><code>MARKLOGIC_MANAGE_PORT = 8002\n</code></pre>"},{"location":"reference/mlclient/connection/CloudConfig/","title":"CloudConfig","text":"<pre><code>from mlclient.connection import CloudConfig\n</code></pre>"},{"location":"reference/mlclient/connection/CloudConfig/#mlclient.connection.CloudConfig","title":"mlclient.connection.CloudConfig  <code>pydantic-model</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>MarkLogic Cloud connection configuration.</p> Show JSON schema: <pre><code>{\n  \"description\": \"MarkLogic Cloud connection configuration.\",\n  \"properties\": {\n    \"api-key\": {\n      \"title\": \"Api-Key\",\n      \"type\": \"string\"\n    },\n    \"base-path\": {\n      \"title\": \"Base-Path\",\n      \"type\": \"string\"\n    },\n    \"token-duration\": {\n      \"default\": 0,\n      \"title\": \"Token-Duration\",\n      \"type\": \"integer\"\n    }\n  },\n  \"required\": [\n    \"api-key\",\n    \"base-path\"\n  ],\n  \"title\": \"CloudConfig\",\n  \"type\": \"object\"\n}\n</code></pre> <p>Config:</p> <ul> <li><code>populate_by_name</code>: <code>True</code></li> </ul> <p>Fields:</p> <ul> <li> <code>api_key</code>                 (<code>str</code>)             </li> <li> <code>base_path</code>                 (<code>str</code>)             </li> <li> <code>token_duration</code>                 (<code>int</code>)             </li> </ul>"},{"location":"reference/mlclient/connection/SSLConfig/","title":"SSLConfig","text":"<pre><code>from mlclient.connection import SSLConfig\n</code></pre>"},{"location":"reference/mlclient/connection/SSLConfig/#mlclient.connection.SSLConfig","title":"mlclient.connection.SSLConfig  <code>pydantic-model</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>SSL/TLS connection configuration.</p> Show JSON schema: <pre><code>{\n  \"description\": \"SSL/TLS connection configuration.\",\n  \"properties\": {\n    \"verify\": {\n      \"anyOf\": [\n        {\n          \"type\": \"boolean\"\n        },\n        {\n          \"type\": \"string\"\n        }\n      ],\n      \"default\": true,\n      \"title\": \"Verify\"\n    },\n    \"cert_file\": {\n      \"anyOf\": [\n        {\n          \"type\": \"string\"\n        },\n        {\n          \"type\": \"null\"\n        }\n      ],\n      \"default\": null,\n      \"title\": \"Cert File\"\n    },\n    \"key_file\": {\n      \"anyOf\": [\n        {\n          \"type\": \"string\"\n        },\n        {\n          \"type\": \"null\"\n        }\n      ],\n      \"default\": null,\n      \"title\": \"Key File\"\n    },\n    \"key_password\": {\n      \"anyOf\": [\n        {\n          \"type\": \"string\"\n        },\n        {\n          \"type\": \"null\"\n        }\n      ],\n      \"default\": null,\n      \"title\": \"Key Password\"\n    }\n  },\n  \"title\": \"SSLConfig\",\n  \"type\": \"object\"\n}\n</code></pre> <p>Fields:</p> <ul> <li> <code>verify</code>                 (<code>Union[bool, str]</code>)             </li> <li> <code>cert_file</code>                 (<code>Optional[str]</code>)             </li> <li> <code>key_file</code>                 (<code>Optional[str]</code>)             </li> <li> <code>key_password</code>                 (<code>Optional[str]</code>)             </li> </ul>"},{"location":"reference/mlclient/connection/SSLConfig/#mlclient.connection.SSLConfig.has_client_cert","title":"has_client_cert  <code>property</code>","text":"<pre><code>has_client_cert: bool\n</code></pre> <p>Whether a client certificate is configured (mutual TLS).</p>"},{"location":"reference/mlclient/env/","title":"<code>mlclient.env</code>","text":""},{"location":"reference/mlclient/env/#mlclient.env","title":"mlclient.env","text":"<p>The ML Environment module.</p> <p>This module contains an API for MarkLogic environment configuration. It exports the following classes:</p> <pre><code>* MLEnvironment\n    A class representing a MarkLogic configuration environment.\n* MLServerConfig\n    A class representing MarkLogic App Server configuration.\n</code></pre> <p>It exports the following functions:</p> <pre><code>* find_mlclient_environment\n    Locate a named environment's configuration file in the nearest .mlclient.\n* find_mlclient_directory\n    Locate the nearest .mlclient directory at a path or in an ancestor.\n</code></pre>"},{"location":"reference/mlclient/env/MLEnvironment/","title":"MLEnvironment","text":"<pre><code>from mlclient.env import MLEnvironment\n</code></pre>"},{"location":"reference/mlclient/env/MLEnvironment/#mlclient.env.MLEnvironment","title":"mlclient.env.MLEnvironment  <code>pydantic-model</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>A class representing a MarkLogic configuration environment.</p> <p>Connection and authentication settings configured here act as defaults for every app server and may be overridden per server. The App Services, Manage, Admin and Health servers always exist; a user entry sharing one of their ids overrides it.</p> Show JSON schema: <pre><code>{\n  \"$defs\": {\n    \"AuthConfig\": {\n      \"description\": \"Data a specific auth mechanism needs beyond the shared user identity.\\n\\nHolds OAuth's token or a Kerberos SPN override (``service`` / ``hostname``).\\nIt is not a credential store: basic/digest identity comes from the client's\\n``username`` / ``password``, never from here.\\n\\n``method`` selects the mechanism this config configures: ``\\\"oauth\\\"``,\\n``\\\"kerberos\\\"`` or ``\\\"certificate\\\"``. The remaining fields apply to a single\\nmechanism and are ignored by the others:\\n\\n- ``token`` -- the OAuth Bearer token; used only for ``\\\"oauth\\\"``.\\n- ``service`` -- the service part of the Kerberos SPN ``service/hostname``;\\n  used only for ``\\\"kerberos\\\"``. MarkLogic registers its principal under the\\n  ``\\\"HTTP\\\"`` service, so the default fits every standard deployment; override\\n  it only when the SPN was registered under a different service name.\\n- ``hostname`` -- the hostname part of the Kerberos SPN; used only for\\n  ``\\\"kerberos\\\"``, defaulting to the request host when omitted.\",\n      \"properties\": {\n        \"method\": {\n          \"title\": \"Method\",\n          \"type\": \"string\"\n        },\n        \"token\": {\n          \"anyOf\": [\n            {\n              \"type\": \"string\"\n            },\n            {\n              \"type\": \"null\"\n            }\n          ],\n          \"default\": null,\n          \"title\": \"Token\"\n        },\n        \"service\": {\n          \"default\": \"HTTP\",\n          \"title\": \"Service\",\n          \"type\": \"string\"\n        },\n        \"hostname\": {\n          \"anyOf\": [\n            {\n              \"type\": \"string\"\n            },\n            {\n              \"type\": \"null\"\n            }\n          ],\n          \"default\": null,\n          \"title\": \"Hostname\"\n        }\n      },\n      \"required\": [\n        \"method\"\n      ],\n      \"title\": \"AuthConfig\",\n      \"type\": \"object\"\n    },\n    \"CloudConfig\": {\n      \"description\": \"MarkLogic Cloud connection configuration.\",\n      \"properties\": {\n        \"api-key\": {\n          \"title\": \"Api-Key\",\n          \"type\": \"string\"\n        },\n        \"base-path\": {\n          \"title\": \"Base-Path\",\n          \"type\": \"string\"\n        },\n        \"token-duration\": {\n          \"default\": 0,\n          \"title\": \"Token-Duration\",\n          \"type\": \"integer\"\n        }\n      },\n      \"required\": [\n        \"api-key\",\n        \"base-path\"\n      ],\n      \"title\": \"CloudConfig\",\n      \"type\": \"object\"\n    },\n    \"MLServerConfig\": {\n      \"description\": \"A class representing MarkLogic App Server configuration.\\n\\nConnection and authentication settings default to ``None`` so an unset\\nfield inherits the root-level value from MLEnvironment.\",\n      \"properties\": {\n        \"id\": {\n          \"description\": \"A unique identifier of the App Server\",\n          \"title\": \"Id\",\n          \"type\": \"string\"\n        },\n        \"port\": {\n          \"anyOf\": [\n            {\n              \"type\": \"integer\"\n            },\n            {\n              \"type\": \"null\"\n            }\n          ],\n          \"default\": null,\n          \"description\": \"A port number; None uses the connection's default port\",\n          \"title\": \"Port\"\n        },\n        \"protocol\": {\n          \"anyOf\": [\n            {\n              \"type\": \"string\"\n            },\n            {\n              \"type\": \"null\"\n            }\n          ],\n          \"default\": null,\n          \"description\": \"An HTTP protocol; None inherits from root\",\n          \"title\": \"Protocol\"\n        },\n        \"auth\": {\n          \"anyOf\": [\n            {\n              \"type\": \"string\"\n            },\n            {\n              \"$ref\": \"#/$defs/AuthConfig\"\n            },\n            {\n              \"type\": \"null\"\n            }\n          ],\n          \"default\": null,\n          \"description\": \"An authentication method; omitted inherits from root\",\n          \"title\": \"Auth\"\n        },\n        \"username\": {\n          \"anyOf\": [\n            {\n              \"type\": \"string\"\n            },\n            {\n              \"type\": \"null\"\n            }\n          ],\n          \"default\": null,\n          \"description\": \"A username; None inherits from root\",\n          \"title\": \"Username\"\n        },\n        \"password\": {\n          \"anyOf\": [\n            {\n              \"type\": \"string\"\n            },\n            {\n              \"type\": \"null\"\n            }\n          ],\n          \"default\": null,\n          \"description\": \"A password; None inherits from root\",\n          \"title\": \"Password\"\n        },\n        \"ssl\": {\n          \"anyOf\": [\n            {\n              \"$ref\": \"#/$defs/SSLConfig\"\n            },\n            {\n              \"type\": \"null\"\n            }\n          ],\n          \"default\": null,\n          \"description\": \"SSL/TLS configuration; None inherits from root\"\n        },\n        \"rest\": {\n          \"default\": false,\n          \"description\": \"A flag informing if the App-Server is a REST server\",\n          \"title\": \"Rest\",\n          \"type\": \"boolean\"\n        }\n      },\n      \"required\": [\n        \"id\"\n      ],\n      \"title\": \"MLServerConfig\",\n      \"type\": \"object\"\n    },\n    \"SSLConfig\": {\n      \"description\": \"SSL/TLS connection configuration.\",\n      \"properties\": {\n        \"verify\": {\n          \"anyOf\": [\n            {\n              \"type\": \"boolean\"\n            },\n            {\n              \"type\": \"string\"\n            }\n          ],\n          \"default\": true,\n          \"title\": \"Verify\"\n        },\n        \"cert_file\": {\n          \"anyOf\": [\n            {\n              \"type\": \"string\"\n            },\n            {\n              \"type\": \"null\"\n            }\n          ],\n          \"default\": null,\n          \"title\": \"Cert File\"\n        },\n        \"key_file\": {\n          \"anyOf\": [\n            {\n              \"type\": \"string\"\n            },\n            {\n              \"type\": \"null\"\n            }\n          ],\n          \"default\": null,\n          \"title\": \"Key File\"\n        },\n        \"key_password\": {\n          \"anyOf\": [\n            {\n              \"type\": \"string\"\n            },\n            {\n              \"type\": \"null\"\n            }\n          ],\n          \"default\": null,\n          \"title\": \"Key Password\"\n        }\n      },\n      \"title\": \"SSLConfig\",\n      \"type\": \"object\"\n    }\n  },\n  \"description\": \"A class representing a MarkLogic configuration environment.\\n\\nConnection and authentication settings configured here act as defaults for\\nevery app server and may be overridden per server. The App Services, Manage,\\nAdmin and Health servers always exist; a user entry sharing one of their ids\\noverrides it.\",\n  \"properties\": {\n    \"app-name\": {\n      \"anyOf\": [\n        {\n          \"type\": \"string\"\n        },\n        {\n          \"type\": \"null\"\n        }\n      ],\n      \"default\": null,\n      \"description\": \"An application name; a label used to scope discovery when set\",\n      \"title\": \"App-Name\"\n    },\n    \"protocol\": {\n      \"default\": \"http\",\n      \"description\": \"An HTTP protocol\",\n      \"title\": \"Protocol\",\n      \"type\": \"string\"\n    },\n    \"host\": {\n      \"default\": \"localhost\",\n      \"description\": \"A hostname\",\n      \"title\": \"Host\",\n      \"type\": \"string\"\n    },\n    \"username\": {\n      \"default\": \"admin\",\n      \"description\": \"An username\",\n      \"title\": \"Username\",\n      \"type\": \"string\"\n    },\n    \"password\": {\n      \"default\": \"admin\",\n      \"description\": \"A password\",\n      \"title\": \"Password\",\n      \"type\": \"string\"\n    },\n    \"auth\": {\n      \"anyOf\": [\n        {\n          \"type\": \"string\"\n        },\n        {\n          \"$ref\": \"#/$defs/AuthConfig\"\n        },\n        {\n          \"type\": \"null\"\n        }\n      ],\n      \"default\": \"digest\",\n      \"description\": \"An authentication method\",\n      \"title\": \"Auth\"\n    },\n    \"ssl\": {\n      \"anyOf\": [\n        {\n          \"$ref\": \"#/$defs/SSLConfig\"\n        },\n        {\n          \"type\": \"null\"\n        }\n      ],\n      \"default\": null,\n      \"description\": \"SSL/TLS configuration\"\n    },\n    \"cloud\": {\n      \"anyOf\": [\n        {\n          \"$ref\": \"#/$defs/CloudConfig\"\n        },\n        {\n          \"type\": \"null\"\n        }\n      ],\n      \"default\": null,\n      \"description\": \"MarkLogic Cloud configuration\"\n    },\n    \"app-servers\": {\n      \"description\": \"App Servers configurations' list\",\n      \"items\": {\n        \"$ref\": \"#/$defs/MLServerConfig\"\n      },\n      \"title\": \"App-Servers\",\n      \"type\": \"array\"\n    }\n  },\n  \"title\": \"MLEnvironment\",\n  \"type\": \"object\"\n}\n</code></pre> <p>Fields:</p> <ul> <li> <code>app_name</code>                 (<code>Optional[str]</code>)             </li> <li> <code>protocol</code>                 (<code>str</code>)             </li> <li> <code>host</code>                 (<code>str</code>)             </li> <li> <code>username</code>                 (<code>str</code>)             </li> <li> <code>password</code>                 (<code>str</code>)             </li> <li> <code>auth</code>                 (<code>Auth</code>)             </li> <li> <code>ssl</code>                 (<code>Optional[SSLConfig]</code>)             </li> <li> <code>cloud</code>                 (<code>Optional[CloudConfig]</code>)             </li> <li> <code>app_servers</code>                 (<code>list[MLServerConfig]</code>)             </li> </ul> <p>Validators:</p> <ul> <li> <code>_ensure_default_app_servers</code> </li> </ul>"},{"location":"reference/mlclient/env/MLEnvironment/#mlclient.env.MLEnvironment.app_name","title":"app_name  <code>pydantic-field</code>","text":"<pre><code>app_name: Optional[str] = None\n</code></pre> <p>An application name; a label used to scope discovery when set</p>"},{"location":"reference/mlclient/env/MLEnvironment/#mlclient.env.MLEnvironment.app_server_ids","title":"app_server_ids  <code>property</code>","text":"<pre><code>app_server_ids: list[str]\n</code></pre> <p>App server identifiers.</p>"},{"location":"reference/mlclient/env/MLEnvironment/#mlclient.env.MLEnvironment.app_servers","title":"app_servers  <code>pydantic-field</code>","text":"<pre><code>app_servers: list[MLServerConfig]\n</code></pre> <p>App Servers configurations' list</p>"},{"location":"reference/mlclient/env/MLEnvironment/#mlclient.env.MLEnvironment.auth","title":"auth  <code>pydantic-field</code>","text":"<pre><code>auth: Auth = 'digest'\n</code></pre> <p>An authentication method</p>"},{"location":"reference/mlclient/env/MLEnvironment/#mlclient.env.MLEnvironment.cloud","title":"cloud  <code>pydantic-field</code>","text":"<pre><code>cloud: Optional[CloudConfig] = None\n</code></pre> <p>MarkLogic Cloud configuration</p>"},{"location":"reference/mlclient/env/MLEnvironment/#mlclient.env.MLEnvironment.host","title":"host  <code>pydantic-field</code>","text":"<pre><code>host: str = 'localhost'\n</code></pre> <p>A hostname</p>"},{"location":"reference/mlclient/env/MLEnvironment/#mlclient.env.MLEnvironment.password","title":"password  <code>pydantic-field</code>","text":"<pre><code>password: str = 'admin'\n</code></pre> <p>A password</p>"},{"location":"reference/mlclient/env/MLEnvironment/#mlclient.env.MLEnvironment.protocol","title":"protocol  <code>pydantic-field</code>","text":"<pre><code>protocol: str = 'http'\n</code></pre> <p>An HTTP protocol</p>"},{"location":"reference/mlclient/env/MLEnvironment/#mlclient.env.MLEnvironment.rest_servers","title":"rest_servers  <code>property</code>","text":"<pre><code>rest_servers: list[str]\n</code></pre> <p>REST servers identifiers.</p>"},{"location":"reference/mlclient/env/MLEnvironment/#mlclient.env.MLEnvironment.ssl","title":"ssl  <code>pydantic-field</code>","text":"<pre><code>ssl: Optional[SSLConfig] = None\n</code></pre> <p>SSL/TLS configuration</p>"},{"location":"reference/mlclient/env/MLEnvironment/#mlclient.env.MLEnvironment.username","title":"username  <code>pydantic-field</code>","text":"<pre><code>username: str = 'admin'\n</code></pre> <p>An username</p>"},{"location":"reference/mlclient/env/MLEnvironment/#mlclient.env.MLEnvironment.load","title":"load  <code>classmethod</code>","text":"<pre><code>load(env_name: str) -&gt; MLEnvironment\n</code></pre> <p>Instantiate MLEnvironment from a named environment.</p> <p>This method looks for a configuration file in the .mlclient directory. An environment configuration needs to match a file name pattern to be recognized: mlclient-.yaml. <p>Parameters:</p> Name Type Description Default <code>env_name</code> <code>str</code> <p>An environment name</p> required <p>Returns:</p> Type Description <code>MLEnvironment</code> <p>An MLEnvironment instance</p> <p>Raises:</p> Type Description <code>MLClientDirectoryNotFoundError</code> <p>If .mlclient directory has not been found</p> <code>MLClientEnvironmentNotFoundError</code> <p>If there's no .mlclient/mlclient-.yaml file Source code in <code>mlclient/env.py</code> <pre><code>@classmethod\ndef load(\n    cls,\n    env_name: str,\n) -&gt; MLEnvironment:\n    \"\"\"Instantiate MLEnvironment from a named environment.\n\n    This method looks for a configuration file in the .mlclient directory.\n    An environment configuration needs to match a file name pattern\n    to be recognized: mlclient-&lt;env-name&gt;.yaml.\n\n    Parameters\n    ----------\n    env_name : str\n        An environment name\n\n    Returns\n    -------\n    MLEnvironment\n        An MLEnvironment instance\n\n    Raises\n    ------\n    MLClientDirectoryNotFoundError\n        If .mlclient directory has not been found\n    MLClientEnvironmentNotFoundError\n        If there's no .mlclient/mlclient-&lt;env_name&gt;.yaml file\n    \"\"\"\n    logger.debug(\n        \"Loading MLClient configuration for the environment: [%s]\",\n        env_name,\n    )\n    env_file_path = find_mlclient_environment(env_name)\n    return cls.load_file(env_file_path.as_posix())\n</code></pre>"},{"location":"reference/mlclient/env/MLEnvironment/#mlclient.env.MLEnvironment.load_file","title":"load_file  <code>classmethod</code>","text":"<pre><code>load_file(file_path: str) -&gt; MLEnvironment\n</code></pre> <p>Instantiate MLEnvironment from a file.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str</code> <p>A source configuration file</p> required <p>Returns:</p> Type Description <code>MLEnvironment</code> <p>An MLEnvironment instance</p> Source code in <code>mlclient/env.py</code> <pre><code>@classmethod\ndef load_file(\n    cls,\n    file_path: str,\n) -&gt; MLEnvironment:\n    \"\"\"Instantiate MLEnvironment from a file.\n\n    Parameters\n    ----------\n    file_path : str\n        A source configuration file\n\n    Returns\n    -------\n    MLEnvironment\n        An MLEnvironment instance\n    \"\"\"\n    logger.info(\"Loading MLClient configuration from the file: [%s]\", file_path)\n    source_config = cls._get_source_config(file_path)\n    return MLEnvironment(**source_config)\n</code></pre>"},{"location":"reference/mlclient/env/MLEnvironment/#mlclient.env.MLEnvironment.provide_config","title":"provide_config","text":"<pre><code>provide_config(app_server_id: str) -&gt; HTTPConfig\n</code></pre> <p>Provide a resolved connection configuration for an App Server.</p> <p>The root-level connection and auth defaults are merged with the app server's overrides and resolved into an HTTPConfig ready to hand to a client via its <code>config</code> parameter.</p> <p>Parameters:</p> Name Type Description Default <code>app_server_id</code> <code>str</code> <p>A unique identifier of the App Server</p> required <p>Returns:</p> Type Description <code>HTTPConfig</code> <p>A resolved configuration for a client initialization</p> Source code in <code>mlclient/env.py</code> <pre><code>def provide_config(\n    self,\n    app_server_id: str,\n) -&gt; HTTPConfig:\n    \"\"\"Provide a resolved connection configuration for an App Server.\n\n    The root-level connection and auth defaults are merged with the app\n    server's overrides and resolved into an HTTPConfig ready to hand to a\n    client via its ``config`` parameter.\n\n    Parameters\n    ----------\n    app_server_id : str\n        A unique identifier of the App Server\n\n    Returns\n    -------\n    HTTPConfig\n        A resolved configuration for a client initialization\n    \"\"\"\n    logger.debug(\"Getting configuration for the [%s] app server\", app_server_id)\n    ml_config = self._root_config()\n    app_server = self._find_app_server(app_server_id)\n    app_server_config = self._app_server_overrides(app_server)\n    merged = {**ml_config, **app_server_config}\n    if app_server.ssl is not None:\n        merged[\"ssl\"] = self._merge_ssl(self.ssl, app_server.ssl)\n    return HTTPConfig.resolve(**merged)\n</code></pre>"},{"location":"reference/mlclient/env/MLServerConfig/","title":"MLServerConfig","text":"<pre><code>from mlclient.env import MLServerConfig\n</code></pre>"},{"location":"reference/mlclient/env/MLServerConfig/#mlclient.env.MLServerConfig","title":"mlclient.env.MLServerConfig  <code>pydantic-model</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>A class representing MarkLogic App Server configuration.</p> <p>Connection and authentication settings default to <code>None</code> so an unset field inherits the root-level value from MLEnvironment.</p> Show JSON schema: <pre><code>{\n  \"$defs\": {\n    \"AuthConfig\": {\n      \"description\": \"Data a specific auth mechanism needs beyond the shared user identity.\\n\\nHolds OAuth's token or a Kerberos SPN override (``service`` / ``hostname``).\\nIt is not a credential store: basic/digest identity comes from the client's\\n``username`` / ``password``, never from here.\\n\\n``method`` selects the mechanism this config configures: ``\\\"oauth\\\"``,\\n``\\\"kerberos\\\"`` or ``\\\"certificate\\\"``. The remaining fields apply to a single\\nmechanism and are ignored by the others:\\n\\n- ``token`` -- the OAuth Bearer token; used only for ``\\\"oauth\\\"``.\\n- ``service`` -- the service part of the Kerberos SPN ``service/hostname``;\\n  used only for ``\\\"kerberos\\\"``. MarkLogic registers its principal under the\\n  ``\\\"HTTP\\\"`` service, so the default fits every standard deployment; override\\n  it only when the SPN was registered under a different service name.\\n- ``hostname`` -- the hostname part of the Kerberos SPN; used only for\\n  ``\\\"kerberos\\\"``, defaulting to the request host when omitted.\",\n      \"properties\": {\n        \"method\": {\n          \"title\": \"Method\",\n          \"type\": \"string\"\n        },\n        \"token\": {\n          \"anyOf\": [\n            {\n              \"type\": \"string\"\n            },\n            {\n              \"type\": \"null\"\n            }\n          ],\n          \"default\": null,\n          \"title\": \"Token\"\n        },\n        \"service\": {\n          \"default\": \"HTTP\",\n          \"title\": \"Service\",\n          \"type\": \"string\"\n        },\n        \"hostname\": {\n          \"anyOf\": [\n            {\n              \"type\": \"string\"\n            },\n            {\n              \"type\": \"null\"\n            }\n          ],\n          \"default\": null,\n          \"title\": \"Hostname\"\n        }\n      },\n      \"required\": [\n        \"method\"\n      ],\n      \"title\": \"AuthConfig\",\n      \"type\": \"object\"\n    },\n    \"SSLConfig\": {\n      \"description\": \"SSL/TLS connection configuration.\",\n      \"properties\": {\n        \"verify\": {\n          \"anyOf\": [\n            {\n              \"type\": \"boolean\"\n            },\n            {\n              \"type\": \"string\"\n            }\n          ],\n          \"default\": true,\n          \"title\": \"Verify\"\n        },\n        \"cert_file\": {\n          \"anyOf\": [\n            {\n              \"type\": \"string\"\n            },\n            {\n              \"type\": \"null\"\n            }\n          ],\n          \"default\": null,\n          \"title\": \"Cert File\"\n        },\n        \"key_file\": {\n          \"anyOf\": [\n            {\n              \"type\": \"string\"\n            },\n            {\n              \"type\": \"null\"\n            }\n          ],\n          \"default\": null,\n          \"title\": \"Key File\"\n        },\n        \"key_password\": {\n          \"anyOf\": [\n            {\n              \"type\": \"string\"\n            },\n            {\n              \"type\": \"null\"\n            }\n          ],\n          \"default\": null,\n          \"title\": \"Key Password\"\n        }\n      },\n      \"title\": \"SSLConfig\",\n      \"type\": \"object\"\n    }\n  },\n  \"description\": \"A class representing MarkLogic App Server configuration.\\n\\nConnection and authentication settings default to ``None`` so an unset\\nfield inherits the root-level value from MLEnvironment.\",\n  \"properties\": {\n    \"id\": {\n      \"description\": \"A unique identifier of the App Server\",\n      \"title\": \"Id\",\n      \"type\": \"string\"\n    },\n    \"port\": {\n      \"anyOf\": [\n        {\n          \"type\": \"integer\"\n        },\n        {\n          \"type\": \"null\"\n        }\n      ],\n      \"default\": null,\n      \"description\": \"A port number; None uses the connection's default port\",\n      \"title\": \"Port\"\n    },\n    \"protocol\": {\n      \"anyOf\": [\n        {\n          \"type\": \"string\"\n        },\n        {\n          \"type\": \"null\"\n        }\n      ],\n      \"default\": null,\n      \"description\": \"An HTTP protocol; None inherits from root\",\n      \"title\": \"Protocol\"\n    },\n    \"auth\": {\n      \"anyOf\": [\n        {\n          \"type\": \"string\"\n        },\n        {\n          \"$ref\": \"#/$defs/AuthConfig\"\n        },\n        {\n          \"type\": \"null\"\n        }\n      ],\n      \"default\": null,\n      \"description\": \"An authentication method; omitted inherits from root\",\n      \"title\": \"Auth\"\n    },\n    \"username\": {\n      \"anyOf\": [\n        {\n          \"type\": \"string\"\n        },\n        {\n          \"type\": \"null\"\n        }\n      ],\n      \"default\": null,\n      \"description\": \"A username; None inherits from root\",\n      \"title\": \"Username\"\n    },\n    \"password\": {\n      \"anyOf\": [\n        {\n          \"type\": \"string\"\n        },\n        {\n          \"type\": \"null\"\n        }\n      ],\n      \"default\": null,\n      \"description\": \"A password; None inherits from root\",\n      \"title\": \"Password\"\n    },\n    \"ssl\": {\n      \"anyOf\": [\n        {\n          \"$ref\": \"#/$defs/SSLConfig\"\n        },\n        {\n          \"type\": \"null\"\n        }\n      ],\n      \"default\": null,\n      \"description\": \"SSL/TLS configuration; None inherits from root\"\n    },\n    \"rest\": {\n      \"default\": false,\n      \"description\": \"A flag informing if the App-Server is a REST server\",\n      \"title\": \"Rest\",\n      \"type\": \"boolean\"\n    }\n  },\n  \"required\": [\n    \"id\"\n  ],\n  \"title\": \"MLServerConfig\",\n  \"type\": \"object\"\n}\n</code></pre> <p>Fields:</p> <ul> <li> <code>identifier</code>                 (<code>str</code>)             </li> <li> <code>port</code>                 (<code>Optional[int]</code>)             </li> <li> <code>protocol</code>                 (<code>Optional[str]</code>)             </li> <li> <code>auth</code>                 (<code>Auth</code>)             </li> <li> <code>username</code>                 (<code>Optional[str]</code>)             </li> <li> <code>password</code>                 (<code>Optional[str]</code>)             </li> <li> <code>ssl</code>                 (<code>Optional[SSLConfig]</code>)             </li> <li> <code>rest</code>                 (<code>bool</code>)             </li> </ul>"},{"location":"reference/mlclient/env/MLServerConfig/#mlclient.env.MLServerConfig.auth","title":"auth  <code>pydantic-field</code>","text":"<pre><code>auth: Auth = None\n</code></pre> <p>An authentication method; omitted inherits from root</p>"},{"location":"reference/mlclient/env/MLServerConfig/#mlclient.env.MLServerConfig.identifier","title":"identifier  <code>pydantic-field</code>","text":"<pre><code>identifier: str\n</code></pre> <p>A unique identifier of the App Server</p>"},{"location":"reference/mlclient/env/MLServerConfig/#mlclient.env.MLServerConfig.password","title":"password  <code>pydantic-field</code>","text":"<pre><code>password: Optional[str] = None\n</code></pre> <p>A password; None inherits from root</p>"},{"location":"reference/mlclient/env/MLServerConfig/#mlclient.env.MLServerConfig.port","title":"port  <code>pydantic-field</code>","text":"<pre><code>port: Optional[int] = None\n</code></pre> <p>A port number; None uses the connection's default port</p>"},{"location":"reference/mlclient/env/MLServerConfig/#mlclient.env.MLServerConfig.protocol","title":"protocol  <code>pydantic-field</code>","text":"<pre><code>protocol: Optional[str] = None\n</code></pre> <p>An HTTP protocol; None inherits from root</p>"},{"location":"reference/mlclient/env/MLServerConfig/#mlclient.env.MLServerConfig.rest","title":"rest  <code>pydantic-field</code>","text":"<pre><code>rest: bool = False\n</code></pre> <p>A flag informing if the App-Server is a REST server</p>"},{"location":"reference/mlclient/env/MLServerConfig/#mlclient.env.MLServerConfig.ssl","title":"ssl  <code>pydantic-field</code>","text":"<pre><code>ssl: Optional[SSLConfig] = None\n</code></pre> <p>SSL/TLS configuration; None inherits from root</p>"},{"location":"reference/mlclient/env/MLServerConfig/#mlclient.env.MLServerConfig.username","title":"username  <code>pydantic-field</code>","text":"<pre><code>username: Optional[str] = None\n</code></pre> <p>A username; None inherits from root</p>"},{"location":"reference/mlclient/env/find_mlclient_directory/","title":"find_mlclient_directory","text":"<pre><code>from mlclient.env import find_mlclient_directory\n</code></pre>"},{"location":"reference/mlclient/env/find_mlclient_directory/#mlclient.env.find_mlclient_directory","title":"mlclient.env.find_mlclient_directory","text":"<pre><code>find_mlclient_directory(path: Path) -&gt; Path\n</code></pre> <p>Return the nearest .mlclient directory at path or in an ancestor.</p> <p>Searches path and, failing there, each parent up to the root.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>Path</code> <p>A path to start the search from</p> required <p>Returns:</p> Type Description <code>Path</code> <p>The located .mlclient directory</p> <p>Raises:</p> Type Description <code>MLClientDirectoryNotFoundError</code> <p>If no .mlclient directory exists at path or any of its parents</p> Source code in <code>mlclient/env.py</code> <pre><code>def find_mlclient_directory(\n    path: Path,\n) -&gt; Path:\n    \"\"\"Return the nearest .mlclient directory at path or in an ancestor.\n\n    Searches path and, failing there, each parent up to the root.\n\n    Parameters\n    ----------\n    path : Path\n        A path to start the search from\n\n    Returns\n    -------\n    Path\n        The located .mlclient directory\n\n    Raises\n    ------\n    MLClientDirectoryNotFoundError\n        If no .mlclient directory exists at path or any of its parents\n    \"\"\"\n    if path.as_posix() in (\".\", \"/\"):\n        msg = (\n            f\"{constants.ML_CLIENT_DIR} directory has not been found in any of \"\n            f\"parent directories!\"\n        )\n        raise MLClientDirectoryNotFoundError(msg)\n    mlclient_dir = next(\n        (child for child in path.glob(constants.ML_CLIENT_DIR) if child.is_dir()),\n        None,\n    )\n    if mlclient_dir:\n        logger.debug(\"MLClient configuration home directory found: [%s]\", mlclient_dir)\n        return mlclient_dir\n    return find_mlclient_directory(path.parent)\n</code></pre>"},{"location":"reference/mlclient/env/find_mlclient_environment/","title":"find_mlclient_environment","text":"<pre><code>from mlclient.env import find_mlclient_environment\n</code></pre>"},{"location":"reference/mlclient/env/find_mlclient_environment/#mlclient.env.find_mlclient_environment","title":"mlclient.env.find_mlclient_environment","text":"<pre><code>find_mlclient_environment(env_name: str) -&gt; Path\n</code></pre> <p>Return the configuration file path for a named MLClient environment.</p> <p>Searches the nearest .mlclient directory (see find_mlclient_directory) for a file matching the mlclient-.yaml pattern. <p>Parameters:</p> Name Type Description Default <code>env_name</code> <code>str</code> <p>An MLClient environment name</p> required <p>Returns:</p> Type Description <code>Path</code> <p>The environment's configuration file path</p> <p>Raises:</p> Type Description <code>MLClientDirectoryNotFoundError</code> <p>If no .mlclient directory has been found</p> <code>MLClientEnvironmentNotFoundError</code> <p>If the .mlclient directory has no mlclient-.yaml file Source code in <code>mlclient/env.py</code> <pre><code>def find_mlclient_environment(\n    env_name: str,\n) -&gt; Path:\n    \"\"\"Return the configuration file path for a named MLClient environment.\n\n    Searches the nearest .mlclient directory (see find_mlclient_directory) for a\n    file matching the mlclient-&lt;env_name&gt;.yaml pattern.\n\n    Parameters\n    ----------\n    env_name : str\n        An MLClient environment name\n\n    Returns\n    -------\n    Path\n        The environment's configuration file path\n\n    Raises\n    ------\n    MLClientDirectoryNotFoundError\n        If no .mlclient directory has been found\n    MLClientEnvironmentNotFoundError\n        If the .mlclient directory has no mlclient-&lt;env_name&gt;.yaml file\n    \"\"\"\n    ml_client_dir = find_mlclient_directory(Path.cwd())\n    env_file_name = f\"mlclient-{env_name}.yaml\"\n    env_file_path = next(ml_client_dir.glob(env_file_name), None)\n    if not env_file_path:\n        msg = (\n            f\"MLClient's environment configuration has not been found for [{env_name}]!\"\n        )\n        raise MLClientEnvironmentNotFoundError(msg)\n    logger.debug(\"MLClient configuration file found: [%s]\", env_file_name)\n    return env_file_path\n</code></pre>"},{"location":"reference/mlclient/exceptions/","title":"<code>mlclient.exceptions</code>","text":""},{"location":"reference/mlclient/exceptions/#mlclient.exceptions","title":"mlclient.exceptions","text":"<p>The ML Client Exceptions module.</p> <p>It contains all custom exceptions related to ML Client:     * WrongParametersError         A custom Exception class for wrong parameters.     * UnsupportedFormatError         A custom Exception class for an unsupported format.     * MLClientDirectoryNotFoundError         A custom Exception class for a non-existing MLClient configuration directory.     * MLClientEnvironmentNotFoundError         A custom Exception class for a non-existing MLClient environment.     * NoSuchAppServerError         A custom Exception class for a non-existing app server configuration.     * NotARestServerError         A custom Exception class for a regular App-Server used as a REST Server.     * NoRestServerConfiguredError         A custom Exception class for no REST Server configured when required.     * MarkLogicError         A custom Exception class representing MarkLogic errors.     * UnsupportedFileExtensionError         A custom Exception class for an unsupported file extension.     * InvalidMetadataError         A custom Exception class for invalid metadata XML.     * ResourceNotFoundError         A custom Exception class for a not found resource.     * ConfigError         A custom Exception class for invalid connection/authentication config.</p>"},{"location":"reference/mlclient/exceptions/ConfigError/","title":"ConfigError","text":"<pre><code>from mlclient.exceptions import ConfigError\n</code></pre>"},{"location":"reference/mlclient/exceptions/ConfigError/#mlclient.exceptions.ConfigError","title":"mlclient.exceptions.ConfigError","text":"<p>               Bases: <code>Exception</code></p> <p>A custom Exception class for invalid connection/authentication config.</p> <p>Raised at client creation time when connection and authentication settings form a combination MarkLogic cannot support (e.g. certificate auth without a client certificate, or a Cloud connection mixed with explicit auth).</p>"},{"location":"reference/mlclient/exceptions/EnvironmentFileExistsError/","title":"EnvironmentFileExistsError","text":"<pre><code>from mlclient.exceptions import EnvironmentFileExistsError\n</code></pre>"},{"location":"reference/mlclient/exceptions/EnvironmentFileExistsError/#mlclient.exceptions.EnvironmentFileExistsError","title":"mlclient.exceptions.EnvironmentFileExistsError","text":"<pre><code>EnvironmentFileExistsError(file_path: str)\n</code></pre> <p>               Bases: <code>Exception</code></p> <p>A custom Exception class for an already existing environment file.</p> <p>Raised when scaffolding an environment configuration that would overwrite an existing file without the force flag.</p> <p>Initialize EnvironmentFileExistsError exception with details.</p> <p>Extends Exception constructor with a custom message.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str</code> <p>A path of the existing configuration file</p> required Source code in <code>mlclient/exceptions.py</code> <pre><code>def __init__(\n    self,\n    file_path: str,\n):\n    \"\"\"Initialize EnvironmentFileExistsError exception with details.\n\n    Extends Exception constructor with a custom message.\n\n    Parameters\n    ----------\n    file_path : str\n        A path of the existing configuration file\n    \"\"\"\n    super().__init__(\n        f\"Configuration file already exists: [{file_path}]. Use --force to \"\n        f\"overwrite.\",\n    )\n</code></pre>"},{"location":"reference/mlclient/exceptions/InvalidLogTypeError/","title":"InvalidLogTypeError","text":"<pre><code>from mlclient.exceptions import InvalidLogTypeError\n</code></pre>"},{"location":"reference/mlclient/exceptions/InvalidLogTypeError/#mlclient.exceptions.InvalidLogTypeError","title":"mlclient.exceptions.InvalidLogTypeError","text":"<p>               Bases: <code>Exception</code></p> <p>A custom Exception class for an invalid log type.</p> <p>Raised while getting LogType enum for a value different from error, access and request.</p>"},{"location":"reference/mlclient/exceptions/InvalidMetadataError/","title":"InvalidMetadataError","text":"<pre><code>from mlclient.exceptions import InvalidMetadataError\n</code></pre>"},{"location":"reference/mlclient/exceptions/InvalidMetadataError/#mlclient.exceptions.InvalidMetadataError","title":"mlclient.exceptions.InvalidMetadataError","text":"<p>               Bases: <code>Exception</code></p> <p>A custom Exception class for invalid metadata XML.</p> <p>Raised when parsing a metadata XML file with unexpected elements or namespaces.</p>"},{"location":"reference/mlclient/exceptions/MLClientDirectoryNotFoundError/","title":"MLClientDirectoryNotFoundError","text":"<pre><code>from mlclient.exceptions import MLClientDirectoryNotFoundError\n</code></pre>"},{"location":"reference/mlclient/exceptions/MLClientDirectoryNotFoundError/#mlclient.exceptions.MLClientDirectoryNotFoundError","title":"mlclient.exceptions.MLClientDirectoryNotFoundError","text":"<p>               Bases: <code>Exception</code></p> <p>A custom Exception class for a non-existing MLClient configuration directory.</p> <p>Raised when loading an MLEnvironment.</p>"},{"location":"reference/mlclient/exceptions/MLClientEnvironmentNotFoundError/","title":"MLClientEnvironmentNotFoundError","text":"<pre><code>from mlclient.exceptions import MLClientEnvironmentNotFoundError\n</code></pre>"},{"location":"reference/mlclient/exceptions/MLClientEnvironmentNotFoundError/#mlclient.exceptions.MLClientEnvironmentNotFoundError","title":"mlclient.exceptions.MLClientEnvironmentNotFoundError","text":"<p>               Bases: <code>Exception</code></p> <p>A custom Exception class for a non-existing MLClient environment.</p> <p>Raised when loading an MLEnvironment.</p>"},{"location":"reference/mlclient/exceptions/MarkLogicError/","title":"MarkLogicError","text":"<pre><code>from mlclient.exceptions import MarkLogicError\n</code></pre>"},{"location":"reference/mlclient/exceptions/MarkLogicError/#mlclient.exceptions.MarkLogicError","title":"mlclient.exceptions.MarkLogicError","text":"<pre><code>MarkLogicError(error: dict | str)\n</code></pre> <p>               Bases: <code>Exception</code></p> <p>A custom Exception class representing MarkLogic errors.</p> <p>Raised whenever an ML server returns an error.</p> <p>Initialize MarkLogicError instance.</p> <p>Parameters:</p> Name Type Description Default <code>error</code> <code>dict | str</code> <p>An error response object or a raw error message</p> required Source code in <code>mlclient/exceptions.py</code> <pre><code>def __init__(\n    self,\n    error: dict | str,\n):\n    \"\"\"Initialize MarkLogicError instance.\n\n    Parameters\n    ----------\n    error : dict | str\n        An error response object or a raw error message\n    \"\"\"\n    if isinstance(error, dict):\n        error = error.get(\"errorResponse\", error)\n        status_code = error.get(\"statusCode\")\n        status = error.get(\"status\")\n        msg_code = error.get(\"messageCode\")\n        msg = error.get(\"message\")\n        if msg_code:\n            error_msg = f\"[{status_code} {status}] ({msg_code}) {msg}\"\n        else:\n            error_msg = f\"[{status_code} {status}] {msg}\"\n    else:\n        error_msg = error\n    super().__init__(error_msg)\n</code></pre>"},{"location":"reference/mlclient/exceptions/NoRestServerConfiguredError/","title":"NoRestServerConfiguredError","text":"<pre><code>from mlclient.exceptions import NoRestServerConfiguredError\n</code></pre>"},{"location":"reference/mlclient/exceptions/NoRestServerConfiguredError/#mlclient.exceptions.NoRestServerConfiguredError","title":"mlclient.exceptions.NoRestServerConfiguredError","text":"<p>               Bases: <code>Exception</code></p> <p>A custom Exception class for no REST Server configured when required.</p> <p>Raised while getting an app server config when no REST server is configured.</p>"},{"location":"reference/mlclient/exceptions/NoSuchAppServerError/","title":"NoSuchAppServerError","text":"<pre><code>from mlclient.exceptions import NoSuchAppServerError\n</code></pre>"},{"location":"reference/mlclient/exceptions/NoSuchAppServerError/#mlclient.exceptions.NoSuchAppServerError","title":"mlclient.exceptions.NoSuchAppServerError","text":"<p>               Bases: <code>Exception</code></p> <p>A custom Exception class for a non-existing app server configuration.</p> <p>Raised when getting an app server config from an MLEnvironment instance.</p>"},{"location":"reference/mlclient/exceptions/NotARestServerError/","title":"NotARestServerError","text":"<pre><code>from mlclient.exceptions import NotARestServerError\n</code></pre>"},{"location":"reference/mlclient/exceptions/NotARestServerError/#mlclient.exceptions.NotARestServerError","title":"mlclient.exceptions.NotARestServerError","text":"<p>               Bases: <code>Exception</code></p> <p>A custom Exception class for a regular App-Server used as a REST Server.</p> <p>Raised while getting an app server config not being a REST server.</p>"},{"location":"reference/mlclient/exceptions/ResourceNotFoundError/","title":"ResourceNotFoundError","text":"<pre><code>from mlclient.exceptions import ResourceNotFoundError\n</code></pre>"},{"location":"reference/mlclient/exceptions/ResourceNotFoundError/#mlclient.exceptions.ResourceNotFoundError","title":"mlclient.exceptions.ResourceNotFoundError","text":"<pre><code>ResourceNotFoundError(resource_name: str)\n</code></pre> <p>               Bases: <code>Exception</code></p> <p>A custom Exception class for a not found resource.</p> <p>Raised while attempting to get a resource that does not exist.</p> <p>Initialize ResourceNotFoundError exception with details.</p> <p>Extends Exception constructor with a custom message.</p> <p>Parameters:</p> Name Type Description Default <code>resource_name</code> <code>str</code> <p>A resource name</p> required Source code in <code>mlclient/exceptions.py</code> <pre><code>def __init__(\n    self,\n    resource_name: str,\n):\n    \"\"\"Initialize ResourceNotFoundError exception with details.\n\n    Extends Exception constructor with a custom message.\n\n    Parameters\n    ----------\n    resource_name : str\n        A resource name\n    \"\"\"\n    super().__init__(f\"No such resource: [{resource_name}]\")\n</code></pre>"},{"location":"reference/mlclient/exceptions/UnsupportedFileExtensionError/","title":"UnsupportedFileExtensionError","text":"<pre><code>from mlclient.exceptions import UnsupportedFileExtensionError\n</code></pre>"},{"location":"reference/mlclient/exceptions/UnsupportedFileExtensionError/#mlclient.exceptions.UnsupportedFileExtensionError","title":"mlclient.exceptions.UnsupportedFileExtensionError","text":"<p>               Bases: <code>Exception</code></p> <p>A custom Exception class for an unsupported file extension.</p> <p>Raised while evaluating code from file with unsupported extension.</p>"},{"location":"reference/mlclient/exceptions/UnsupportedFormatError/","title":"UnsupportedFormatError","text":"<pre><code>from mlclient.exceptions import UnsupportedFormatError\n</code></pre>"},{"location":"reference/mlclient/exceptions/UnsupportedFormatError/#mlclient.exceptions.UnsupportedFormatError","title":"mlclient.exceptions.UnsupportedFormatError","text":"<p>               Bases: <code>Exception</code></p> <p>A custom Exception class for an unsupported format.</p> <p>Raised when getting an Accept header for a format.</p>"},{"location":"reference/mlclient/exceptions/WrongParametersError/","title":"WrongParametersError","text":"<pre><code>from mlclient.exceptions import WrongParametersError\n</code></pre>"},{"location":"reference/mlclient/exceptions/WrongParametersError/#mlclient.exceptions.WrongParametersError","title":"mlclient.exceptions.WrongParametersError","text":"<p>               Bases: <code>Exception</code></p> <p>A custom Exception class for wrong parameters.</p> <p>Raised when attempting to call a REST Resource with incorrect parameters.</p>"},{"location":"reference/mlclient/http/","title":"<code>mlclient.http</code>","text":""},{"location":"reference/mlclient/http/#mlclient.http","title":"mlclient.http","text":"<p>The HTTP Configuration module.</p> <p>This module exposes a single facade over the connection and authentication layers: :class:<code>HTTPConfig</code>. A client resolves one HTTPConfig up front and then carries it as the sole source of communication details - the base URL, the SSL verification setting, the URL builder and the ready httpx auth handler.</p> <p>HTTPConfig sits above the two focused layers it composes and does not replace them:</p> <pre><code>* ``mlclient.connection`` decides the transport mode (HTTP, HTTPS, mutual\n  TLS or Cloud) and validates connection/auth combinations.\n* ``mlclient.auth`` turns an auth descriptor into an httpx.Auth handler.\n</code></pre> <p>The facade owns the order those steps run in, so a client no longer wires the connection and auth functions together by hand.</p>"},{"location":"reference/mlclient/http/#mlclient.http.DEFAULT_RETRY_STRATEGY","title":"DEFAULT_RETRY_STRATEGY  <code>module-attribute</code>","text":"<pre><code>DEFAULT_RETRY_STRATEGY = Retry(total=5, backoff_factor=0.5)\n</code></pre>"},{"location":"reference/mlclient/http/#mlclient.http.DEFAULT_TIMEOUT","title":"DEFAULT_TIMEOUT  <code>module-attribute</code>","text":"<pre><code>DEFAULT_TIMEOUT = httpx.Timeout(\n    connect=5.0, read=60.0, write=60.0, pool=5.0\n)\n</code></pre>"},{"location":"reference/mlclient/http/#mlclient.http.NO_RETRY_STRATEGY","title":"NO_RETRY_STRATEGY  <code>module-attribute</code>","text":"<pre><code>NO_RETRY_STRATEGY = Retry(total=0)\n</code></pre>"},{"location":"reference/mlclient/http/#mlclient.http.UNSET","title":"UNSET  <code>module-attribute</code>","text":"<pre><code>UNSET = _Unset()\n</code></pre>"},{"location":"reference/mlclient/http/HTTPConfig/","title":"HTTPConfig","text":"<pre><code>from mlclient.http import HTTPConfig\n</code></pre>"},{"location":"reference/mlclient/http/HTTPConfig/#mlclient.http.HTTPConfig","title":"mlclient.http.HTTPConfig","text":"<pre><code>HTTPConfig(\n    connection: _ConnectionMode,\n    host: str,\n    auth_method: AuthParam,\n    auth: Auth | None,\n    username: str,\n    password: str,\n    retry: Retry | int | None,\n    limits: Limits | int | None,\n    timeout,\n)\n</code></pre> <p>Resolved connection and authentication details for a MarkLogic client.</p> <p>The single point a client reads its communication details from. It pairs the resolved transport mode with the ready httpx auth handler, so callers ask for <code>base_url</code>, <code>transport_verify()</code> or <code>auth</code> rather than reconstructing them from loose parameters.</p> <p><code>auth</code> is the built httpx.Auth handler applied to each request. The descriptor it was built from (a shortcut string, an AuthConfig, a custom handler or None) is retained internally so an independent variant can be derived via :meth:<code>clone</code>.</p> <p>Initialize HTTPConfig from already-resolved parts.</p> <p>Prefer :meth:<code>resolve</code>, which runs the connection and auth layers to produce these parts. This initializer only stores them.</p> Source code in <code>mlclient/http.py</code> <pre><code>def __init__(\n    self,\n    connection: _ConnectionMode,\n    host: str,\n    auth_method: AuthParam,\n    auth: httpx.Auth | None,\n    username: str,\n    password: str,\n    retry: Retry | int | None,\n    limits: httpx.Limits | int | None,\n    timeout,\n):\n    \"\"\"Initialize HTTPConfig from already-resolved parts.\n\n    Prefer :meth:`resolve`, which runs the connection and auth layers to\n    produce these parts. This initializer only stores them.\n    \"\"\"\n    self._connection = connection\n    self._host = host\n    self._auth_method = auth_method\n    self._auth = auth\n    self._username = username\n    self._password = password\n    self._retry = _normalize_retry(retry)\n    self._limits = _normalize_limits(limits)\n    self._timeout = _normalize_timeout(timeout)\n</code></pre>"},{"location":"reference/mlclient/http/HTTPConfig/#mlclient.http.HTTPConfig.auth","title":"auth  <code>property</code>","text":"<pre><code>auth: Auth | None\n</code></pre> <p>The httpx auth handler applied to each request, or None.</p>"},{"location":"reference/mlclient/http/HTTPConfig/#mlclient.http.HTTPConfig.auth_method","title":"auth_method  <code>property</code>","text":"<pre><code>auth_method: AuthParam\n</code></pre> <p>The authentication descriptor used to build the HTTP handler.</p>"},{"location":"reference/mlclient/http/HTTPConfig/#mlclient.http.HTTPConfig.base_path","title":"base_path  <code>property</code>","text":"<pre><code>base_path: str | None\n</code></pre> <p>The URL prefix prepended to every endpoint (MarkLogic Cloud).</p>"},{"location":"reference/mlclient/http/HTTPConfig/#mlclient.http.HTTPConfig.base_url","title":"base_url  <code>property</code>","text":"<pre><code>base_url: str\n</code></pre> <p>The base URL built from protocol, host and port.</p>"},{"location":"reference/mlclient/http/HTTPConfig/#mlclient.http.HTTPConfig.cloud","title":"cloud  <code>property</code>","text":"<pre><code>cloud: CloudConfig | None\n</code></pre> <p>The MarkLogic Cloud configuration, if any.</p>"},{"location":"reference/mlclient/http/HTTPConfig/#mlclient.http.HTTPConfig.connection","title":"connection  <code>property</code>","text":"<pre><code>connection: _ConnectionMode\n</code></pre> <p>The resolved transport mode.</p>"},{"location":"reference/mlclient/http/HTTPConfig/#mlclient.http.HTTPConfig.has_explicit_retry","title":"has_explicit_retry  <code>property</code>","text":"<pre><code>has_explicit_retry: bool\n</code></pre> <p>Whether a retry strategy was supplied rather than left to the default.</p>"},{"location":"reference/mlclient/http/HTTPConfig/#mlclient.http.HTTPConfig.has_explicit_timeout","title":"has_explicit_timeout  <code>property</code>","text":"<pre><code>has_explicit_timeout: bool\n</code></pre> <p>Whether a timeout was supplied rather than left to the default.</p>"},{"location":"reference/mlclient/http/HTTPConfig/#mlclient.http.HTTPConfig.host","title":"host  <code>property</code>","text":"<pre><code>host: str\n</code></pre> <p>The host name.</p>"},{"location":"reference/mlclient/http/HTTPConfig/#mlclient.http.HTTPConfig.limits","title":"limits  <code>property</code>","text":"<pre><code>limits: Limits | None\n</code></pre> <p>A copy of the pool limits, or None to defer to httpx's default.</p> <p>Use clone(limits=...) to change settings; mutating this copy has no effect on the configuration or an existing session.</p>"},{"location":"reference/mlclient/http/HTTPConfig/#mlclient.http.HTTPConfig.password","title":"password  <code>property</code>","text":"<pre><code>password: str\n</code></pre> <p>The password for credential-based methods.</p>"},{"location":"reference/mlclient/http/HTTPConfig/#mlclient.http.HTTPConfig.port","title":"port  <code>property</code>","text":"<pre><code>port: int\n</code></pre> <p>The connection port.</p>"},{"location":"reference/mlclient/http/HTTPConfig/#mlclient.http.HTTPConfig.protocol","title":"protocol  <code>property</code>","text":"<pre><code>protocol: str\n</code></pre> <p>The connection protocol (http / https).</p>"},{"location":"reference/mlclient/http/HTTPConfig/#mlclient.http.HTTPConfig.retry","title":"retry  <code>property</code>","text":"<pre><code>retry: Retry\n</code></pre> <p>The retry strategy for transport creation.</p>"},{"location":"reference/mlclient/http/HTTPConfig/#mlclient.http.HTTPConfig.ssl","title":"ssl  <code>property</code>","text":"<pre><code>ssl: SSLConfig | None\n</code></pre> <p>The SSL/TLS configuration, if any.</p>"},{"location":"reference/mlclient/http/HTTPConfig/#mlclient.http.HTTPConfig.timeout","title":"timeout  <code>property</code>","text":"<pre><code>timeout: Timeout\n</code></pre> <p>A copy of the effective timeout, defaulting to DEFAULT_TIMEOUT.</p> <p>A timeout of <code>httpx.Timeout(None)</code> (all components None) means every HTTP timeout is disabled; it is still an explicit value, distinct from the unset case that resolves to DEFAULT_TIMEOUT. Use clone(timeout=...) to change settings; mutating this copy does not affect the configuration.</p>"},{"location":"reference/mlclient/http/HTTPConfig/#mlclient.http.HTTPConfig.username","title":"username  <code>property</code>","text":"<pre><code>username: str\n</code></pre> <p>The username for credential-based methods.</p>"},{"location":"reference/mlclient/http/HTTPConfig/#mlclient.http.HTTPConfig.build_url","title":"build_url","text":"<pre><code>build_url(endpoint: str) -&gt; str\n</code></pre> <p>Build a full request URL, applying the Cloud base path if present.</p> Source code in <code>mlclient/http.py</code> <pre><code>def build_url(self, endpoint: str) -&gt; str:\n    \"\"\"Build a full request URL, applying the Cloud base path if present.\"\"\"\n    base_path = self.base_path\n    if base_path:\n        endpoint = f\"/{base_path.strip('/')}/{endpoint.lstrip('/')}\"\n    return self.base_url + endpoint\n</code></pre>"},{"location":"reference/mlclient/http/HTTPConfig/#mlclient.http.HTTPConfig.can_share_session","title":"can_share_session","text":"<pre><code>can_share_session(other: HTTPConfig) -&gt; bool\n</code></pre> <p>Whether two configurations can safely use the same HTTP session.</p> <p>Compare connection and credential values, and require the same retry strategy object and, for custom authentication, the same handler. Custom strategies and handlers may carry behavior beyond their fields. Explicit pool limits are compared by value. Timeout is compared by its effective connect/read/write/ pool components, so equivalent values share a session even when built separately, while any component difference (including disabled versus bounded) blocks sharing.</p> Source code in <code>mlclient/http.py</code> <pre><code>def can_share_session(self, other: HTTPConfig) -&gt; bool:\n    \"\"\"Whether two configurations can safely use the same HTTP session.\n\n    Compare connection and credential values, and require the same retry\n    strategy object and, for custom authentication, the same handler.\n    Custom strategies and handlers may carry behavior beyond their fields.\n    Explicit pool limits are compared by value. Timeout is compared by its\n    effective connect/read/write/\n    pool components, so equivalent values share a session even when built\n    separately, while any component difference (including disabled versus\n    bounded) blocks sharing.\n    \"\"\"\n    same_auth = (\n        self.auth_method is other.auth_method\n        if isinstance(self.auth_method, httpx.Auth)\n        or isinstance(other.auth_method, httpx.Auth)\n        else self.auth_method == other.auth_method\n    )\n    return (\n        self.connection == other.connection\n        and self.host == other.host\n        and self.username == other.username\n        and self.password == other.password\n        and same_auth\n        and self.retry is other.retry\n        and self.limits == other.limits\n        and self.timeout == other.timeout\n    )\n</code></pre>"},{"location":"reference/mlclient/http/HTTPConfig/#mlclient.http.HTTPConfig.clone","title":"clone","text":"<pre><code>clone(**overrides) -&gt; HTTPConfig\n</code></pre> <p>Return an independent config, overriding the given fields.</p> <p>Accepts any resolve() parameter (<code>port</code>, <code>host</code>, <code>auth</code>, ...); unspecified fields keep this config's values. The result is produced by a fresh resolve(). Built-in auth handlers are rebuilt; custom handlers and retry strategies retain their identity.</p> <p>Fixed-port siblings (Admin on 8001, Manage on 8002) use <code>config.clone(port=...)</code>. Cloud routes every tier through its single port-443 connection, so Cloud ignores port overrides. Other overrides still produce a fresh config, including a changed retry strategy.</p> Source code in <code>mlclient/http.py</code> <pre><code>def clone(self, **overrides) -&gt; HTTPConfig:\n    \"\"\"Return an independent config, overriding the given fields.\n\n    Accepts any resolve() parameter (``port``, ``host``, ``auth``, ...);\n    unspecified fields keep this config's values. The result is produced by\n    a fresh resolve(). Built-in auth handlers are rebuilt; custom handlers\n    and retry strategies retain their identity.\n\n    Fixed-port siblings (Admin on 8001, Manage on 8002) use\n    ``config.clone(port=...)``. Cloud routes every tier through its single\n    port-443 connection, so Cloud ignores port overrides. Other overrides\n    still produce a fresh config, including a changed retry strategy.\n    \"\"\"\n    if self._connection.is_cloud:\n        overrides.pop(\"port\", None)\n        if not overrides:\n            return self\n    base = {\n        \"protocol\": self.protocol,\n        \"host\": self._host,\n        \"port\": self.port,\n        \"auth\": self._auth_method,\n        \"username\": self._username,\n        \"password\": self._password,\n        \"ssl\": self._connection.ssl,\n        \"cloud\": self._connection.cloud,\n        \"retry\": self._retry,\n        \"limits\": self._limits,\n        \"timeout\": self._timeout,\n    }\n    return self.resolve(**{**base, **overrides})\n</code></pre>"},{"location":"reference/mlclient/http/HTTPConfig/#mlclient.http.HTTPConfig.resolve","title":"resolve  <code>classmethod</code>","text":"<pre><code>resolve(\n    *,\n    protocol=UNSET,\n    host: str = \"localhost\",\n    port=UNSET,\n    auth: AuthParam = UNSET,\n    username: str = \"admin\",\n    password: str = \"admin\",\n    ssl: SSLConfig | None = None,\n    cloud: CloudConfig | None = None,\n    retry: Retry | int | None = None,\n    limits: Limits | int | None = None,\n    timeout=UNSET,\n) -&gt; HTTPConfig\n</code></pre> <p>Resolve connection and auth parameters into an HTTPConfig.</p> <p>Runs the connection layer (transport mode, defaulting, validation) and the auth layer (handler construction) in order, raising ConfigError on an unsupported connection/auth combination.</p> <p>Parameters:</p> Name Type Description Default <code>protocol</code> <code>str | UNSET</code> <p>The requested protocol (http / https), or UNSET to auto-resolve.</p> <code>UNSET</code> <code>host</code> <code>str</code> <p>A host name.</p> <code>\"localhost\"</code> <code>port</code> <code>int | UNSET</code> <p>The requested port, or UNSET to auto-resolve.</p> <code>UNSET</code> <code>auth</code> <code>str | Auth | AuthConfig | None | UNSET</code> <p>The auth descriptor, or UNSET to apply the connection default.</p> <code>UNSET</code> <code>username</code> <code>str</code> <p>A username for credential-based methods.</p> <code>\"admin\"</code> <code>password</code> <code>str</code> <p>A password for credential-based methods.</p> <code>\"admin\"</code> <code>ssl</code> <code>SSLConfig | None</code> <p>SSL/TLS configuration.</p> <code>None</code> <code>cloud</code> <code>CloudConfig | None</code> <p>MarkLogic Cloud configuration.</p> <code>None</code> <code>retry</code> <code>Retry | int | None</code> <p>The retry strategy for transport creation. None leaves the strategy unspecified, using DEFAULT_RETRY_STRATEGY for ordinary requests. An int is shorthand for the retry count, Retry(total=n); 0 disables retries.</p> <code>DEFAULT_RETRY_STRATEGY</code> <code>limits</code> <code>Limits | int | None</code> <p>The connection-pool limits for transport creation. None leaves the limits unset, deferring to httpx's own default. An int is shorthand for the concurrency cap, httpx.Limits(max_connections=n).</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None | UNSET</code> <p>The request timeout. UNSET leaves it unspecified, using DEFAULT_TIMEOUT. None disables every HTTP timeout. A number sets all four components (connect, read, write, pool) to that many seconds. An httpx.Timeout fully overrides the timeout without merging.</p> <code>UNSET</code> <p>Returns:</p> Type Description <code>HTTPConfig</code> <p>The resolved configuration.</p> Source code in <code>mlclient/http.py</code> <pre><code>@classmethod\ndef resolve(\n    cls,\n    *,\n    protocol=UNSET,\n    host: str = \"localhost\",\n    port=UNSET,\n    auth: AuthParam = UNSET,\n    username: str = \"admin\",\n    password: str = \"admin\",\n    ssl: SSLConfig | None = None,\n    cloud: CloudConfig | None = None,\n    retry: Retry | int | None = None,\n    limits: httpx.Limits | int | None = None,\n    timeout=UNSET,\n) -&gt; HTTPConfig:\n    \"\"\"Resolve connection and auth parameters into an HTTPConfig.\n\n    Runs the connection layer (transport mode, defaulting, validation) and\n    the auth layer (handler construction) in order, raising ConfigError on\n    an unsupported connection/auth combination.\n\n    Parameters\n    ----------\n    protocol : str | UNSET\n        The requested protocol (http / https), or UNSET to auto-resolve.\n    host : str, default \"localhost\"\n        A host name.\n    port : int | UNSET\n        The requested port, or UNSET to auto-resolve.\n    auth : str | httpx.Auth | AuthConfig | None | UNSET\n        The auth descriptor, or UNSET to apply the connection default.\n    username : str, default \"admin\"\n        A username for credential-based methods.\n    password : str, default \"admin\"\n        A password for credential-based methods.\n    ssl : SSLConfig | None, default None\n        SSL/TLS configuration.\n    cloud : CloudConfig | None, default None\n        MarkLogic Cloud configuration.\n    retry : Retry | int | None, default DEFAULT_RETRY_STRATEGY\n        The retry strategy for transport creation. None leaves the strategy\n        unspecified, using DEFAULT_RETRY_STRATEGY for ordinary requests. An\n        int is shorthand for the retry count, Retry(total=n); 0 disables\n        retries.\n    limits : httpx.Limits | int | None, default None\n        The connection-pool limits for transport creation. None leaves the\n        limits unset, deferring to httpx's own default. An int is shorthand\n        for the concurrency cap, httpx.Limits(max_connections=n).\n    timeout : httpx.Timeout | float | None | UNSET, default UNSET\n        The request timeout. UNSET leaves it unspecified, using\n        DEFAULT_TIMEOUT. None disables every HTTP timeout. A number sets all\n        four components (connect, read, write, pool) to that many seconds.\n        An httpx.Timeout fully overrides the timeout without merging.\n\n    Returns\n    -------\n    HTTPConfig\n        The resolved configuration.\n    \"\"\"\n    connection = _resolve_connection(protocol, port, ssl, cloud)\n    auth_method = _default_auth(auth, connection)\n    _validate_config(connection, auth_method)\n    base_url = cls._build_base_url(connection.protocol, host, connection.port)\n    auth = _build_connection_auth(\n        connection,\n        auth_method,\n        username,\n        password,\n        base_url,\n    )\n    return cls(\n        connection,\n        host,\n        auth_method,\n        auth,\n        username,\n        password,\n        retry,\n        limits,\n        timeout,\n    )\n</code></pre>"},{"location":"reference/mlclient/http/HTTPConfig/#mlclient.http.HTTPConfig.transport_options","title":"transport_options","text":"<pre><code>transport_options() -&gt; dict\n</code></pre> <p>Keyword arguments for an httpx transport.</p> <p>Always carries <code>verify</code>; carries <code>limits</code> only when explicitly set, so an unset value defers to httpx's own default.</p> Source code in <code>mlclient/http.py</code> <pre><code>def transport_options(self) -&gt; dict:\n    \"\"\"Keyword arguments for an httpx transport.\n\n    Always carries ``verify``; carries ``limits`` only when explicitly set,\n    so an unset value defers to httpx's own default.\n    \"\"\"\n    options = {\"verify\": self.transport_verify()}\n    if self._limits is not None:\n        options[\"limits\"] = self.limits\n    return options\n</code></pre>"},{"location":"reference/mlclient/http/HTTPConfig/#mlclient.http.HTTPConfig.transport_verify","title":"transport_verify","text":"<pre><code>transport_verify() -&gt; ssl.SSLContext | bool\n</code></pre> <p>Return the SSL verification setting for transport creation.</p> Source code in <code>mlclient/http.py</code> <pre><code>def transport_verify(self) -&gt; ssl.SSLContext | bool:\n    \"\"\"Return the SSL verification setting for transport creation.\"\"\"\n    return _transport_verify(self._connection)\n</code></pre>"},{"location":"reference/mlclient/http/HTTPConfig/#mlclient.http.HTTPConfig.with_health_defaults","title":"with_health_defaults","text":"<pre><code>with_health_defaults() -&gt; HTTPConfig\n</code></pre> <p>Fill unspecified retry and timeout settings for a HealthCheck client.</p> <p>Retry defaults to NO_RETRY_STRATEGY and timeout to _HEALTH_TIMEOUT. Each setting is resolved independently. Explicit values, including timeout=None, are preserved; host, auth, TLS and limits are unchanged.</p> <p>Returns:</p> Type Description <code>HTTPConfig</code> <p>A cloned configuration if a default needs applying, otherwise this configuration. The source is not modified and no session is opened.</p> Source code in <code>mlclient/http.py</code> <pre><code>def with_health_defaults(self) -&gt; HTTPConfig:\n    \"\"\"Fill unspecified retry and timeout settings for a HealthCheck client.\n\n    Retry defaults to NO_RETRY_STRATEGY and timeout to _HEALTH_TIMEOUT.\n    Each setting is resolved independently. Explicit values, including\n    timeout=None, are preserved; host, auth, TLS and limits are unchanged.\n\n    Returns\n    -------\n    HTTPConfig\n        A cloned configuration if a default needs applying, otherwise this\n        configuration. The source is not modified and no session is opened.\n    \"\"\"\n    overrides = {}\n    if not self.has_explicit_retry:\n        overrides[\"retry\"] = NO_RETRY_STRATEGY\n    if not self.has_explicit_timeout:\n        overrides[\"timeout\"] = _HEALTH_TIMEOUT\n    return self.clone(**overrides) if overrides else self\n</code></pre>"},{"location":"reference/mlclient/io/","title":"<code>mlclient.io</code>","text":""},{"location":"reference/mlclient/io/#mlclient.io","title":"mlclient.io","text":"<p>Load and write documents on the local filesystem.</p>"},{"location":"reference/mlclient/io/DocumentsLoader/","title":"DocumentsLoader","text":"<pre><code>from mlclient.io import DocumentsLoader\n</code></pre>"},{"location":"reference/mlclient/io/DocumentsLoader/#mlclient.io.DocumentsLoader","title":"mlclient.io.DocumentsLoader","text":"<p>A class parsing files into Documents.</p>"},{"location":"reference/mlclient/io/DocumentsLoader/#mlclient.io.DocumentsLoader.load","title":"load  <code>classmethod</code>","text":"<pre><code>load(\n    path: str, uri_prefix: str = \"\"\n) -&gt; Generator[Document]\n</code></pre> <p>Load documents from files under a path.</p> <p>When the path points to a file - yields a single Document with URI set to the file name. Otherwise, yields documents with URIs without the input path at the beginning. Both options can be customized with the uri_prefix parameter. File bytes are stored as-is on the resulting Document; structural parsing (XML, JSON) happens lazily on first <code>.content</code> access. Metadata is identified for a file at the same level with .metadata.json or .metadata.xml suffix.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>A path to a directory or a single file.</p> required <code>uri_prefix</code> <code>str</code> <p>URIs prefix to apply</p> <code>\"\"</code> <p>Returns:</p> Type Description <code>Generator[Document]</code> <p>A generator of Document instances</p> Source code in <code>mlclient/io/loader.py</code> <pre><code>@classmethod\ndef load(\n    cls,\n    path: str,\n    uri_prefix: str = \"\",\n) -&gt; Generator[Document]:\n    \"\"\"Load documents from files under a path.\n\n    When the path points to a file - yields a single Document with URI set to\n    the file name. Otherwise, yields documents with URIs without the input path\n    at the beginning. Both options can be customized with the uri_prefix\n    parameter.\n    File bytes are stored as-is on the resulting Document; structural parsing\n    (XML, JSON) happens lazily on first ``.content`` access.\n    Metadata is identified for a file at the same level with .metadata.json or\n    .metadata.xml suffix.\n\n    Parameters\n    ----------\n    path : str\n        A path to a directory or a single file.\n    uri_prefix : str, default \"\"\n        URIs prefix to apply\n\n    Returns\n    -------\n    Generator[Document]\n        A generator of Document instances\n    \"\"\"\n    if Path(path).is_file():\n        file_path = path\n        path = Path(path)\n        uri = file_path.replace(str(path.parent), uri_prefix)\n        yield cls.load_document(file_path, uri)\n    else:\n        logger.debug(\"Loading documents from [%s] directory\", path)\n        for dir_path, _, file_names in os.walk(path):\n            for file_name in file_names:\n                if file_name.endswith(cls._METADATA_SUFFIXES):\n                    continue\n\n                file_path = str(Path(dir_path) / file_name)\n                uri = file_path.replace(path, uri_prefix)\n                yield cls.load_document(file_path, uri)\n</code></pre>"},{"location":"reference/mlclient/io/DocumentsLoader/#mlclient.io.DocumentsLoader.load_document","title":"load_document  <code>classmethod</code>","text":"<pre><code>load_document(\n    path: str, uri: str | None = None\n) -&gt; Document\n</code></pre> <p>Load a document from a file.</p> <p>By default, returns a Document without URI. It can be customized with the uri parameter. File bytes are stored as-is on the resulting Document; structural parsing (XML, JSON) happens lazily on first <code>.content</code> access. Metadata is identified for a file at the same level with .metadata.json or .metadata.xml suffix.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>A file path</p> required <code>uri</code> <code>str | None</code> <p>URI to set for a document.</p> <code>None</code> <p>Returns:</p> Type Description <code>Document</code> <p>A Document instance</p> Source code in <code>mlclient/io/loader.py</code> <pre><code>@classmethod\ndef load_document(\n    cls,\n    path: str,\n    uri: str | None = None,\n) -&gt; Document:\n    \"\"\"Load a document from a file.\n\n    By default, returns a Document without URI. It can be customized with\n    the uri parameter.\n    File bytes are stored as-is on the resulting Document; structural parsing\n    (XML, JSON) happens lazily on first ``.content`` access.\n    Metadata is identified for a file at the same level with .metadata.json or\n    .metadata.xml suffix.\n\n    Parameters\n    ----------\n    path : str\n        A file path\n    uri : str | None, default None\n        URI to set for a document.\n\n    Returns\n    -------\n    Document\n        A Document instance\n    \"\"\"\n    doc_type = Mimetypes.get_doc_type(path)\n    with Path(path).open(\"rb\") as file:\n        content = file.read()\n    metadata = cls._load_metadata(path)\n\n    return Document.create(\n        content=content,\n        doc_type=doc_type,\n        uri=uri,\n        metadata=metadata,\n    )\n</code></pre>"},{"location":"reference/mlclient/io/DocumentsWriter/","title":"DocumentsWriter","text":"<pre><code>from mlclient.io import DocumentsWriter\n</code></pre>"},{"location":"reference/mlclient/io/DocumentsWriter/#mlclient.io.DocumentsWriter","title":"mlclient.io.DocumentsWriter","text":"<p>A class serializing Documents into files.</p>"},{"location":"reference/mlclient/io/DocumentsWriter/#mlclient.io.DocumentsWriter.write","title":"write  <code>async</code> <code>classmethod</code>","text":"<pre><code>write(\n    documents: Iterable[Document], output_path: str\n) -&gt; None\n</code></pre> <p>Write documents to files under a path.</p> <p>Each document is written to <code>output_path</code> joined with its URI. When a document carries metadata, a <code>.metadata.json</code> or <code>.metadata.xml</code> sidecar is written alongside it - the suffix matches the format of the metadata payload so a round-trip through DocumentsLoader preserves it.</p> <p>Parameters:</p> Name Type Description Default <code>documents</code> <code>Iterable[Document]</code> <p>The documents to write.</p> required <code>output_path</code> <code>str</code> <p>A path to the output directory.</p> required Source code in <code>mlclient/io/writer.py</code> <pre><code>@classmethod\nasync def write(\n    cls,\n    documents: Iterable[Document],\n    output_path: str,\n) -&gt; None:\n    \"\"\"Write documents to files under a path.\n\n    Each document is written to ``output_path`` joined with its URI. When a\n    document carries metadata, a ``.metadata.json`` or ``.metadata.xml``\n    sidecar is written alongside it - the suffix matches the format of the\n    metadata payload so a round-trip through DocumentsLoader preserves it.\n\n    Parameters\n    ----------\n    documents : Iterable[Document]\n        The documents to write.\n    output_path : str\n        A path to the output directory.\n    \"\"\"\n    for document in documents:\n        await cls.write_document(document, output_path)\n</code></pre>"},{"location":"reference/mlclient/io/DocumentsWriter/#mlclient.io.DocumentsWriter.write_document","title":"write_document  <code>async</code> <code>classmethod</code>","text":"<pre><code>write_document(\n    document: Document, output_path: str\n) -&gt; None\n</code></pre> <p>Write a single document to a file under a path.</p> <p>For content-bearing documents writes the content body and, when present, a metadata sidecar. For metadata-only documents (<code>MetadataDocument</code>, whose <code>content_bytes</code> is None) writes the sidecar without a content file.</p> <p>Parameters:</p> Name Type Description Default <code>document</code> <code>Document</code> <p>The document to write.</p> required <code>output_path</code> <code>str</code> <p>A path to the output directory.</p> required Source code in <code>mlclient/io/writer.py</code> <pre><code>@classmethod\nasync def write_document(\n    cls,\n    document: Document,\n    output_path: str,\n) -&gt; None:\n    \"\"\"Write a single document to a file under a path.\n\n    For content-bearing documents writes the content body and, when\n    present, a metadata sidecar. For metadata-only documents\n    (``MetadataDocument``, whose ``content_bytes`` is None) writes the\n    sidecar without a content file.\n\n    Parameters\n    ----------\n    document : Document\n        The document to write.\n    output_path : str\n        A path to the output directory.\n    \"\"\"\n    doc_path = Path(output_path) / document.uri[1:]\n    doc_path.parent.mkdir(parents=True, exist_ok=True)\n\n    if document.content_bytes is not None:\n        logger.fine(\"Writing data into file [%s]\", doc_path)\n        async with aiofiles.open(doc_path, mode=\"wb\") as file:\n            await file.write(document.content_bytes)\n\n    if document.metadata is not None:\n        await cls._write_metadata(document.metadata, doc_path)\n</code></pre>"},{"location":"reference/mlclient/jobs/","title":"<code>mlclient.jobs</code>","text":"<p>Experimental API</p> <p>This API is experimental and outside the stable API contract. It may change incompatibly in minor releases. </p>"},{"location":"reference/mlclient/jobs/#mlclient.jobs","title":"mlclient.jobs","text":"<p>Experimental document jobs and their progress reports.</p> <p>Use the documents service for stable document operations.</p>"},{"location":"reference/mlclient/jobs/DocumentJobReport/","title":"DocumentJobReport","text":"<p>Experimental API</p> <p>This API is experimental and outside the stable API contract. It may change incompatibly in minor releases. </p> <pre><code>from mlclient.jobs import DocumentJobReport\n</code></pre>"},{"location":"reference/mlclient/jobs/DocumentJobReport/#mlclient.jobs.DocumentJobReport","title":"mlclient.jobs.DocumentJobReport","text":"<pre><code>DocumentJobReport()\n</code></pre> <p>A class representing documents job report.</p> <p>Initialize DocumentJobReport instance.</p> Source code in <code>mlclient/jobs/documents.py</code> <pre><code>def __init__(\n    self,\n):\n    \"\"\"Initialize DocumentJobReport instance.\"\"\"\n    self._doc_reports: dict[str, DocumentReport] = {}\n</code></pre>"},{"location":"reference/mlclient/jobs/DocumentJobReport/#mlclient.jobs.DocumentJobReport.completed","title":"completed  <code>property</code>","text":"<pre><code>completed: int\n</code></pre> <p>Return number of completed documents.</p>"},{"location":"reference/mlclient/jobs/DocumentJobReport/#mlclient.jobs.DocumentJobReport.failed","title":"failed  <code>property</code>","text":"<pre><code>failed: int\n</code></pre> <p>Return number of completed documents that failed.</p>"},{"location":"reference/mlclient/jobs/DocumentJobReport/#mlclient.jobs.DocumentJobReport.failed_docs","title":"failed_docs  <code>property</code>","text":"<pre><code>failed_docs: list[str]\n</code></pre> <p>Return completed documents' URIs that failed.</p>"},{"location":"reference/mlclient/jobs/DocumentJobReport/#mlclient.jobs.DocumentJobReport.full","title":"full  <code>property</code>","text":"<pre><code>full: dict[str, DocumentReport]\n</code></pre> <p>Return full documents job report.</p>"},{"location":"reference/mlclient/jobs/DocumentJobReport/#mlclient.jobs.DocumentJobReport.pending","title":"pending  <code>property</code>","text":"<pre><code>pending: int\n</code></pre> <p>Return number of pending documents.</p>"},{"location":"reference/mlclient/jobs/DocumentJobReport/#mlclient.jobs.DocumentJobReport.pending_docs","title":"pending_docs  <code>property</code>","text":"<pre><code>pending_docs: list[str]\n</code></pre> <p>Return pending documents' URIs.</p>"},{"location":"reference/mlclient/jobs/DocumentJobReport/#mlclient.jobs.DocumentJobReport.successful","title":"successful  <code>property</code>","text":"<pre><code>successful: int\n</code></pre> <p>Return number of successfully completed documents.</p>"},{"location":"reference/mlclient/jobs/DocumentJobReport/#mlclient.jobs.DocumentJobReport.successful_docs","title":"successful_docs  <code>property</code>","text":"<pre><code>successful_docs: list[str]\n</code></pre> <p>Return successfully completed documents' URIs.</p>"},{"location":"reference/mlclient/jobs/DocumentJobReport/#mlclient.jobs.DocumentJobReport.add_doc_report","title":"add_doc_report","text":"<pre><code>add_doc_report(report: DocumentReport)\n</code></pre> <p>Add a document report.</p> Source code in <code>mlclient/jobs/documents.py</code> <pre><code>def add_doc_report(\n    self,\n    report: DocumentReport,\n):\n    \"\"\"Add a document report.\"\"\"\n    self._doc_reports[report.uri] = report\n</code></pre>"},{"location":"reference/mlclient/jobs/DocumentJobReport/#mlclient.jobs.DocumentJobReport.add_failed_doc","title":"add_failed_doc","text":"<pre><code>add_failed_doc(uri: str, err: Exception)\n</code></pre> <p>Add a failed document report.</p> Source code in <code>mlclient/jobs/documents.py</code> <pre><code>def add_failed_doc(\n    self,\n    uri: str,\n    err: Exception,\n):\n    \"\"\"Add a failed document report.\"\"\"\n    self.add_doc_report(\n        DocumentReport(\n            uri=uri,\n            status=DocumentStatus.failure,\n            details=DocumentStatusDetails(\n                error=err.__class__,\n                message=str(err),\n            ),\n        ),\n    )\n</code></pre>"},{"location":"reference/mlclient/jobs/DocumentJobReport/#mlclient.jobs.DocumentJobReport.add_failed_docs","title":"add_failed_docs","text":"<pre><code>add_failed_docs(uris: list[str], err: Exception)\n</code></pre> <p>Add failed documents' reports.</p> Source code in <code>mlclient/jobs/documents.py</code> <pre><code>def add_failed_docs(\n    self,\n    uris: list[str],\n    err: Exception,\n):\n    \"\"\"Add failed documents' reports.\"\"\"\n    for uri in uris:\n        self.add_failed_doc(uri, err)\n</code></pre>"},{"location":"reference/mlclient/jobs/DocumentJobReport/#mlclient.jobs.DocumentJobReport.add_pending_doc","title":"add_pending_doc","text":"<pre><code>add_pending_doc(uri: str)\n</code></pre> <p>Add a pending document report.</p> Source code in <code>mlclient/jobs/documents.py</code> <pre><code>def add_pending_doc(\n    self,\n    uri: str,\n):\n    \"\"\"Add a pending document report.\"\"\"\n    self.add_doc_report(DocumentReport(uri=uri, status=DocumentStatus.pending))\n</code></pre>"},{"location":"reference/mlclient/jobs/DocumentJobReport/#mlclient.jobs.DocumentJobReport.add_pending_docs","title":"add_pending_docs","text":"<pre><code>add_pending_docs(uris: list[str])\n</code></pre> <p>Add pending documents' reports.</p> Source code in <code>mlclient/jobs/documents.py</code> <pre><code>def add_pending_docs(\n    self,\n    uris: list[str],\n):\n    \"\"\"Add pending documents' reports.\"\"\"\n    for uri in uris:\n        self.add_pending_doc(uri)\n</code></pre>"},{"location":"reference/mlclient/jobs/DocumentJobReport/#mlclient.jobs.DocumentJobReport.add_successful_doc","title":"add_successful_doc","text":"<pre><code>add_successful_doc(uri: str)\n</code></pre> <p>Add a successfully completed document report.</p> Source code in <code>mlclient/jobs/documents.py</code> <pre><code>def add_successful_doc(\n    self,\n    uri: str,\n):\n    \"\"\"Add a successfully completed document report.\"\"\"\n    self.add_doc_report(DocumentReport(uri=uri, status=DocumentStatus.success))\n</code></pre>"},{"location":"reference/mlclient/jobs/DocumentJobReport/#mlclient.jobs.DocumentJobReport.add_successful_docs","title":"add_successful_docs","text":"<pre><code>add_successful_docs(uris: list[str])\n</code></pre> <p>Add successfully completed documents' reports.</p> Source code in <code>mlclient/jobs/documents.py</code> <pre><code>def add_successful_docs(\n    self,\n    uris: list[str],\n):\n    \"\"\"Add successfully completed documents' reports.\"\"\"\n    for uri in uris:\n        self.add_successful_doc(uri)\n</code></pre>"},{"location":"reference/mlclient/jobs/DocumentJobReport/#mlclient.jobs.DocumentJobReport.get_doc_report","title":"get_doc_report","text":"<pre><code>get_doc_report(uri: str)\n</code></pre> <p>Return a document report.</p> Source code in <code>mlclient/jobs/documents.py</code> <pre><code>def get_doc_report(\n    self,\n    uri: str,\n):\n    \"\"\"Return a document report.\"\"\"\n    return self._doc_reports.get(uri)\n</code></pre>"},{"location":"reference/mlclient/jobs/DocumentReport/","title":"DocumentReport","text":"<p>Experimental API</p> <p>This API is experimental and outside the stable API contract. It may change incompatibly in minor releases. </p> <pre><code>from mlclient.jobs import DocumentReport\n</code></pre>"},{"location":"reference/mlclient/jobs/DocumentReport/#mlclient.jobs.DocumentReport","title":"mlclient.jobs.DocumentReport  <code>pydantic-model</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>A class representing a document's report.</p> <p>Fields:</p> <ul> <li> <code>uri</code>                 (<code>str</code>)             </li> <li> <code>status</code>                 (<code>DocumentStatus</code>)             </li> <li> <code>details</code>                 (<code>DocumentStatusDetails</code>)             </li> </ul>"},{"location":"reference/mlclient/jobs/DocumentStatus/","title":"DocumentStatus","text":"<p>Experimental API</p> <p>This API is experimental and outside the stable API contract. It may change incompatibly in minor releases. </p> <pre><code>from mlclient.jobs import DocumentStatus\n</code></pre>"},{"location":"reference/mlclient/jobs/DocumentStatus/#mlclient.jobs.DocumentStatus","title":"mlclient.jobs.DocumentStatus","text":"<p>               Bases: <code>Enum</code></p> <p>A document's status enum.</p>"},{"location":"reference/mlclient/jobs/DocumentStatusDetails/","title":"DocumentStatusDetails","text":"<p>Experimental API</p> <p>This API is experimental and outside the stable API contract. It may change incompatibly in minor releases. </p> <pre><code>from mlclient.jobs import DocumentStatusDetails\n</code></pre>"},{"location":"reference/mlclient/jobs/DocumentStatusDetails/#mlclient.jobs.DocumentStatusDetails","title":"mlclient.jobs.DocumentStatusDetails  <code>pydantic-model</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>A class representing a document's status details.</p> <p>Fields:</p> <ul> <li> <code>error</code>                 (<code>type</code>)             </li> <li> <code>message</code>                 (<code>str</code>)             </li> </ul>"},{"location":"reference/mlclient/jobs/ReadDocumentsJob/","title":"ReadDocumentsJob","text":"<p>Experimental API</p> <p>This API is experimental and outside the stable API contract. It may change incompatibly in minor releases. </p> <pre><code>from mlclient.jobs import ReadDocumentsJob\n</code></pre>"},{"location":"reference/mlclient/jobs/ReadDocumentsJob/#mlclient.jobs.ReadDocumentsJob","title":"mlclient.jobs.ReadDocumentsJob","text":"<pre><code>ReadDocumentsJob(\n    concurrency: int | None = None, batch_size: int = 400\n)\n</code></pre> <p>An async job reading documents from a MarkLogic database.</p> <p>Uses asyncio with a concurrency-limited semaphore to send URI batches in parallel via AsyncMLClient.</p> <p>Recommended settings based on benchmarks (10000 documents):     concurrency: 8-24 (default: 16)     batch_size: 300-1000 (default: 400) Higher concurrency combined with larger batch sizes yields the best read throughput, with improvements of 30-40% over synchronous execution.</p> <p>Initialize ReadDocumentsJob instance.</p> <p>Parameters:</p> Name Type Description Default <code>concurrency</code> <code>int | None</code> <p>Maximum number of concurrent batch requests (default: 16)</p> <code>None</code> <code>batch_size</code> <code>int</code> <p>A number of URIs in a single batch</p> <code>400</code> Source code in <code>mlclient/jobs/documents.py</code> <pre><code>def __init__(\n    self,\n    concurrency: int | None = None,\n    batch_size: int = 400,\n):\n    \"\"\"Initialize ReadDocumentsJob instance.\n\n    Parameters\n    ----------\n    concurrency : int | None, default None\n        Maximum number of concurrent batch requests (default: 16)\n    batch_size : int, default 400\n        A number of URIs in a single batch\n    \"\"\"\n    self._concurrency: int = concurrency or 16\n    self._batch_size: int = batch_size\n    self._config: dict = {}\n    self._database: str | None = None\n    self._uris: list[str] = []\n    self._categories: list[str] = [\"content\"]\n    self._fs_output_path: Path | None = None\n    self._documents: list[Document] = []\n    self._report = DocumentJobReport()\n</code></pre>"},{"location":"reference/mlclient/jobs/ReadDocumentsJob/#mlclient.jobs.ReadDocumentsJob.documents","title":"documents  <code>property</code>","text":"<pre><code>documents: list[Document]\n</code></pre> <p>Return all read documents from the job.</p>"},{"location":"reference/mlclient/jobs/ReadDocumentsJob/#mlclient.jobs.ReadDocumentsJob.report","title":"report  <code>property</code>","text":"<pre><code>report: DocumentJobReport\n</code></pre> <p>A status of the job.</p>"},{"location":"reference/mlclient/jobs/ReadDocumentsJob/#mlclient.jobs.ReadDocumentsJob.run","title":"run  <code>async</code>","text":"<pre><code>run() -&gt; DocumentJobReport\n</code></pre> <p>Execute the job and return a report when complete.</p> Source code in <code>mlclient/jobs/documents.py</code> <pre><code>async def run(self) -&gt; DocumentJobReport:\n    \"\"\"Execute the job and return a report when complete.\"\"\"\n    for uri in self._uris:\n        self._report.add_pending_doc(uri)\n\n    batches = [\n        self._uris[i : i + self._batch_size]\n        for i in range(0, len(self._uris), self._batch_size)\n    ]\n\n    sem = asyncio.Semaphore(self._concurrency)\n    async with AsyncMLClient(**self._config) as ml:\n        await asyncio.gather(\n            *(self._send_batch(sem, batch, ml) for batch in batches),\n        )\n\n    if self._fs_output_path is not None:\n        await self._save_documents()\n\n    return copy(self._report)\n</code></pre>"},{"location":"reference/mlclient/jobs/ReadDocumentsJob/#mlclient.jobs.ReadDocumentsJob.run_sync","title":"run_sync","text":"<pre><code>run_sync() -&gt; DocumentJobReport\n</code></pre> <p>Execute the job synchronously.</p> <p>Wrapper around asyncio.run(self.run()). Cannot be called from within a running event loop.</p> Source code in <code>mlclient/jobs/documents.py</code> <pre><code>def run_sync(self) -&gt; DocumentJobReport:\n    \"\"\"Execute the job synchronously.\n\n    Wrapper around asyncio.run(self.run()). Cannot be called from within\n    a running event loop.\n    \"\"\"\n    return asyncio.run(self.run())\n</code></pre>"},{"location":"reference/mlclient/jobs/ReadDocumentsJob/#mlclient.jobs.ReadDocumentsJob.with_client_config","title":"with_client_config","text":"<pre><code>with_client_config(**config)\n</code></pre> <p>Set AsyncMLClient configuration.</p> Source code in <code>mlclient/jobs/documents.py</code> <pre><code>def with_client_config(self, **config):\n    \"\"\"Set AsyncMLClient configuration.\"\"\"\n    self._config = config\n</code></pre>"},{"location":"reference/mlclient/jobs/ReadDocumentsJob/#mlclient.jobs.ReadDocumentsJob.with_database","title":"with_database","text":"<pre><code>with_database(database: str)\n</code></pre> <p>Set a database name.</p> Source code in <code>mlclient/jobs/documents.py</code> <pre><code>def with_database(self, database: str):\n    \"\"\"Set a database name.\"\"\"\n    self._database = database\n</code></pre>"},{"location":"reference/mlclient/jobs/ReadDocumentsJob/#mlclient.jobs.ReadDocumentsJob.with_filesystem_output","title":"with_filesystem_output","text":"<pre><code>with_filesystem_output(output_path: str)\n</code></pre> <p>Set filesystem output directory to save documents inside.</p> Source code in <code>mlclient/jobs/documents.py</code> <pre><code>def with_filesystem_output(self, output_path: str):\n    \"\"\"Set filesystem output directory to save documents inside.\"\"\"\n    self._fs_output_path = Path(output_path).resolve().absolute()\n</code></pre>"},{"location":"reference/mlclient/jobs/ReadDocumentsJob/#mlclient.jobs.ReadDocumentsJob.with_metadata","title":"with_metadata","text":"<pre><code>with_metadata(*args: Category | str)\n</code></pre> <p>Add metadata category/ies to retrieve from a MarkLogic server.</p> Source code in <code>mlclient/jobs/documents.py</code> <pre><code>def with_metadata(self, *args: Category | str):\n    \"\"\"Add metadata category/ies to retrieve from a MarkLogic server.\"\"\"\n    if len(args) == 0:\n        self._categories.append(\"metadata\")\n    else:\n        self._categories.extend(\n            c.value if isinstance(c, Category) else c for c in args\n        )\n</code></pre>"},{"location":"reference/mlclient/jobs/ReadDocumentsJob/#mlclient.jobs.ReadDocumentsJob.with_uris_input","title":"with_uris_input","text":"<pre><code>with_uris_input(uris: Iterable[str])\n</code></pre> <p>Add URIs to the job's input.</p> Source code in <code>mlclient/jobs/documents.py</code> <pre><code>def with_uris_input(self, uris: Iterable[str]):\n    \"\"\"Add URIs to the job's input.\"\"\"\n    self._uris.extend(uris)\n</code></pre>"},{"location":"reference/mlclient/jobs/WriteDocumentsJob/","title":"WriteDocumentsJob","text":"<p>Experimental API</p> <p>This API is experimental and outside the stable API contract. It may change incompatibly in minor releases. </p> <pre><code>from mlclient.jobs import WriteDocumentsJob\n</code></pre>"},{"location":"reference/mlclient/jobs/WriteDocumentsJob/#mlclient.jobs.WriteDocumentsJob","title":"mlclient.jobs.WriteDocumentsJob","text":"<pre><code>WriteDocumentsJob(\n    concurrency: int | None = None, batch_size: int = 100\n)\n</code></pre> <p>An async job writing documents into a MarkLogic database.</p> <p>Uses asyncio with a concurrency-limited semaphore to send document batches in parallel via AsyncMLClient.</p> <p>Recommended settings based on benchmarks (1000 documents):     concurrency: 4-12 (default: 8)     batch_size: 50-200 (default: 100)</p> <p>Initialize WriteDocumentsJob instance.</p> <p>Parameters:</p> Name Type Description Default <code>concurrency</code> <code>int | None</code> <p>Maximum number of concurrent batch requests (default: 8)</p> <code>None</code> <code>batch_size</code> <code>int</code> <p>A number of documents in a single batch</p> <code>100</code> Source code in <code>mlclient/jobs/documents.py</code> <pre><code>def __init__(\n    self,\n    concurrency: int | None = None,\n    batch_size: int = 100,\n):\n    \"\"\"Initialize WriteDocumentsJob instance.\n\n    Parameters\n    ----------\n    concurrency : int | None, default None\n        Maximum number of concurrent batch requests (default: 8)\n    batch_size : int, default 100\n        A number of documents in a single batch\n    \"\"\"\n    self._concurrency: int = concurrency or 8\n    self._batch_size: int = batch_size\n    self._config: dict = {}\n    self._database: str | None = None\n    self._documents: list[Document] = []\n    self._report = DocumentJobReport()\n</code></pre>"},{"location":"reference/mlclient/jobs/WriteDocumentsJob/#mlclient.jobs.WriteDocumentsJob.report","title":"report  <code>property</code>","text":"<pre><code>report: DocumentJobReport\n</code></pre> <p>A status of the job.</p>"},{"location":"reference/mlclient/jobs/WriteDocumentsJob/#mlclient.jobs.WriteDocumentsJob.run","title":"run  <code>async</code>","text":"<pre><code>run() -&gt; DocumentJobReport\n</code></pre> <p>Execute the job and return a report when complete.</p> Source code in <code>mlclient/jobs/documents.py</code> <pre><code>async def run(self) -&gt; DocumentJobReport:\n    \"\"\"Execute the job and return a report when complete.\"\"\"\n    for doc in self._documents:\n        self._report.add_pending_doc(doc.uri)\n\n    batches = [\n        self._documents[i : i + self._batch_size]\n        for i in range(0, len(self._documents), self._batch_size)\n    ]\n\n    sem = asyncio.Semaphore(self._concurrency)\n    async with AsyncMLClient(**self._config) as ml:\n        await asyncio.gather(\n            *(self._send_batch(sem, batch, ml) for batch in batches),\n        )\n\n    return copy(self._report)\n</code></pre>"},{"location":"reference/mlclient/jobs/WriteDocumentsJob/#mlclient.jobs.WriteDocumentsJob.run_sync","title":"run_sync","text":"<pre><code>run_sync() -&gt; DocumentJobReport\n</code></pre> <p>Execute the job synchronously.</p> <p>Wrapper around asyncio.run(self.run()). Cannot be called from within a running event loop.</p> Source code in <code>mlclient/jobs/documents.py</code> <pre><code>def run_sync(self) -&gt; DocumentJobReport:\n    \"\"\"Execute the job synchronously.\n\n    Wrapper around asyncio.run(self.run()). Cannot be called from within\n    a running event loop.\n    \"\"\"\n    return asyncio.run(self.run())\n</code></pre>"},{"location":"reference/mlclient/jobs/WriteDocumentsJob/#mlclient.jobs.WriteDocumentsJob.with_client_config","title":"with_client_config","text":"<pre><code>with_client_config(**config)\n</code></pre> <p>Set AsyncMLClient configuration.</p> Source code in <code>mlclient/jobs/documents.py</code> <pre><code>def with_client_config(self, **config):\n    \"\"\"Set AsyncMLClient configuration.\"\"\"\n    self._config = config\n</code></pre>"},{"location":"reference/mlclient/jobs/WriteDocumentsJob/#mlclient.jobs.WriteDocumentsJob.with_database","title":"with_database","text":"<pre><code>with_database(database: str)\n</code></pre> <p>Set a database name.</p> Source code in <code>mlclient/jobs/documents.py</code> <pre><code>def with_database(self, database: str):\n    \"\"\"Set a database name.\"\"\"\n    self._database = database\n</code></pre>"},{"location":"reference/mlclient/jobs/WriteDocumentsJob/#mlclient.jobs.WriteDocumentsJob.with_documents_input","title":"with_documents_input","text":"<pre><code>with_documents_input(documents: Iterable[Document])\n</code></pre> <p>Add Documents to the job's input.</p> Source code in <code>mlclient/jobs/documents.py</code> <pre><code>def with_documents_input(self, documents: Iterable[Document]):\n    \"\"\"Add Documents to the job's input.\"\"\"\n    self._documents.extend(documents)\n</code></pre>"},{"location":"reference/mlclient/jobs/WriteDocumentsJob/#mlclient.jobs.WriteDocumentsJob.with_filesystem_input","title":"with_filesystem_input","text":"<pre><code>with_filesystem_input(path: str, uri_prefix: str = '')\n</code></pre> <p>Load files and add parsed Documents to the job's input.</p> Source code in <code>mlclient/jobs/documents.py</code> <pre><code>def with_filesystem_input(self, path: str, uri_prefix: str = \"\"):\n    \"\"\"Load files and add parsed Documents to the job's input.\"\"\"\n    self._documents.extend(DocumentsLoader.load(path, uri_prefix))\n</code></pre>"},{"location":"reference/mlclient/logging/","title":"<code>mlclient.logging</code>","text":""},{"location":"reference/mlclient/logging/#mlclient.logging","title":"mlclient.logging","text":"<p>Opt-in logging configuration for MLClient applications.</p>"},{"location":"reference/mlclient/logging/setup_logger/","title":"setup_logger","text":"<pre><code>from mlclient.logging import setup_logger\n</code></pre>"},{"location":"reference/mlclient/logging/setup_logger/#mlclient.logging.setup_logger","title":"mlclient.logging.setup_logger","text":"<pre><code>setup_logger()\n</code></pre> <p>Set up MLClient logging configuration.</p> Source code in <code>mlclient/logging.py</code> <pre><code>def setup_logger():\n    \"\"\"Set up MLClient logging configuration.\"\"\"\n    with utils.get_resource(\"logging.yaml\") as config_file:\n        config = yaml.safe_load(config_file.read())\n        logging.config.dictConfig(config)\n</code></pre>"},{"location":"reference/mlclient/models/","title":"<code>mlclient.models</code>","text":""},{"location":"reference/mlclient/models/#mlclient.models","title":"mlclient.models","text":"<p>Documents, metadata, request options and server result models.</p>"},{"location":"reference/mlclient/models/BinaryDocument/","title":"BinaryDocument","text":"<pre><code>from mlclient.models import BinaryDocument\n</code></pre>"},{"location":"reference/mlclient/models/BinaryDocument/#mlclient.models.BinaryDocument","title":"mlclient.models.BinaryDocument","text":"<pre><code>BinaryDocument(\n    content: bytes,\n    uri: str | None = None,\n    metadata: Metadata | bytes | str | None = None,\n    temporal_collection: str | None = None,\n)\n</code></pre> <p>               Bases: <code>Document</code></p> <p>A Document implementation representing a single MarkLogic BINARY document.</p> <p>This implementation stores content in bytes format.</p> Source code in <code>mlclient/models/documents.py</code> <pre><code>def __init__(\n    self,\n    content: bytes,\n    uri: str | None = None,\n    metadata: Metadata | bytes | str | None = None,\n    temporal_collection: str | None = None,\n):\n    if content is None:\n        msg = (\n            \"BinaryDocument requires content; \"\n            \"use Document.metadata_update() for metadata-only updates\"\n        )\n        raise TypeError(msg)\n    super().__init__(uri, DocumentType.BINARY, metadata, temporal_collection)\n    self._content_bytes = content\n</code></pre>"},{"location":"reference/mlclient/models/BinaryDocument/#mlclient.models.BinaryDocument.content","title":"content  <code>property</code>","text":"<pre><code>content: bytes\n</code></pre> <p>A document content.</p> <p>Returns:</p> Type Description <code>bytes</code> <p>The binary content.</p>"},{"location":"reference/mlclient/models/BinaryDocument/#mlclient.models.BinaryDocument.content_bytes","title":"content_bytes  <code>property</code>","text":"<pre><code>content_bytes: bytes\n</code></pre> <p>A document content as bytes.</p> <p>Returns:</p> Type Description <code>bytes</code> <p>The binary content.</p>"},{"location":"reference/mlclient/models/BinaryDocument/#mlclient.models.BinaryDocument.content_string","title":"content_string  <code>property</code>","text":"<pre><code>content_string: None\n</code></pre> <p>A document content as a string.</p> <p>Returns:</p> Type Description <code>None</code> <p>Always None; binary content has no meaningful string representation.</p>"},{"location":"reference/mlclient/models/Category/","title":"Category","text":"<pre><code>from mlclient.models import Category\n</code></pre>"},{"location":"reference/mlclient/models/Category/#mlclient.models.Category","title":"mlclient.models.Category","text":"<p>               Bases: <code>Enum</code></p> <p>An enumeration class representing data categories.</p>"},{"location":"reference/mlclient/models/Document/","title":"Document","text":"<pre><code>from mlclient.models import Document\n</code></pre>"},{"location":"reference/mlclient/models/Document/#mlclient.models.Document","title":"mlclient.models.Document","text":"<pre><code>Document(\n    uri: str | None = None,\n    doc_type: DocumentType | None = DocumentType.XML,\n    metadata: Metadata | bytes | str | None = None,\n    temporal_collection: str | None = None,\n)\n</code></pre> <p>An abstract class representing a single MarkLogic document.</p> <p>Initialize Document instance.</p> <p>Parameters:</p> Name Type Description Default <code>uri</code> <code>str</code> <p>A document URI</p> <code>None</code> <code>doc_type</code> <code>DocumentType | None</code> <p>A document type</p> <code>XML</code> <code>metadata</code> <code>Metadata</code> <p>A document metadata</p> <code>None</code> <code>temporal_collection</code> <code>str | None</code> <p>The temporal collection</p> <code>None</code> Source code in <code>mlclient/models/documents.py</code> <pre><code>def __init__(\n    self,\n    uri: str | None = None,\n    doc_type: DocumentType | None = DocumentType.XML,\n    metadata: Metadata | bytes | str | None = None,\n    temporal_collection: str | None = None,\n):\n    \"\"\"Initialize Document instance.\n\n    Parameters\n    ----------\n    uri : str\n        A document URI\n    doc_type : DocumentType | None\n        A document type\n    metadata : Metadata\n        A document metadata\n    temporal_collection : str | None\n        The temporal collection\n    \"\"\"\n    self._uri = self._get_non_blank_uri(uri)\n    self._doc_type = doc_type\n    if isinstance(metadata, (bytes, str)):\n        metadata = Metadata(raw=metadata)\n    self._metadata = metadata\n    self._temporal_collection = temporal_collection\n</code></pre>"},{"location":"reference/mlclient/models/Document/#mlclient.models.Document.content","title":"content  <code>abstractmethod</code> <code>property</code>","text":"<pre><code>content: Any\n</code></pre> <p>A document content.</p> <p>Typed content-bearing implementations narrow this to a non-<code>None</code> return type (<code>ElemTree.ElementTree</code>, <code>dict</code>, <code>str</code>, <code>bytes</code>). <code>MetadataDocument</code> carries no content body and returns <code>None</code>.</p> <p>Returns:</p> Type Description <code>Any</code> <p>A document's content, or <code>None</code> for metadata-only documents.</p>"},{"location":"reference/mlclient/models/Document/#mlclient.models.Document.content_bytes","title":"content_bytes  <code>abstractmethod</code> <code>property</code>","text":"<pre><code>content_bytes: bytes | None\n</code></pre> <p>A document content bytes.</p> <p>Typed content-bearing documents (<code>XMLDocument</code>, <code>JSONDocument</code>, <code>TextDocument</code>, <code>BinaryDocument</code>) always return <code>bytes</code> and narrow this return type accordingly. <code>MetadataDocument</code> carries no content body and returns <code>None</code>; callers that operate on a generic <code>Document</code> must handle that case before writing to a bytes sink.</p> <p>Returns:</p> Type Description <code>bytes | None</code> <p>A document's content bytes, or <code>None</code> for metadata-only documents.</p>"},{"location":"reference/mlclient/models/Document/#mlclient.models.Document.content_string","title":"content_string  <code>abstractmethod</code> <code>property</code>","text":"<pre><code>content_string: str | None\n</code></pre> <p>A document content as a string.</p> <p><code>XMLDocument</code>, <code>JSONDocument</code> and <code>TextDocument</code> always return <code>str</code> and narrow this return type accordingly. <code>BinaryDocument</code> and <code>MetadataDocument</code> return <code>None</code> - binary content has no meaningful string representation and metadata-only documents carry no content body.</p> <p>Returns:</p> Type Description <code>str | None</code> <p>A document's content as a string, or <code>None</code> for binary or metadata-only documents.</p>"},{"location":"reference/mlclient/models/Document/#mlclient.models.Document.doc_type","title":"doc_type  <code>property</code>","text":"<pre><code>doc_type: DocumentType | None\n</code></pre> <p>A document type.</p>"},{"location":"reference/mlclient/models/Document/#mlclient.models.Document.metadata","title":"metadata  <code>property</code>","text":"<pre><code>metadata: Metadata | bytes | str\n</code></pre> <p>A document metadata.</p>"},{"location":"reference/mlclient/models/Document/#mlclient.models.Document.temporal_collection","title":"temporal_collection  <code>property</code>","text":"<pre><code>temporal_collection: str | None\n</code></pre> <p>The temporal collection.</p>"},{"location":"reference/mlclient/models/Document/#mlclient.models.Document.uri","title":"uri  <code>property</code>","text":"<pre><code>uri: str\n</code></pre> <p>A document URI.</p>"},{"location":"reference/mlclient/models/Document/#mlclient.models.Document.binary","title":"binary  <code>classmethod</code>","text":"<pre><code>binary(\n    uri: str | None = None,\n    content: bytes | None = None,\n    *,\n    metadata: Metadata | bytes | str | None = None,\n    temporal_collection: str | None = None,\n) -&gt; BinaryDocument\n</code></pre> <p>Instantiate a BinaryDocument.</p> <p>Parameters:</p> Name Type Description Default <code>uri</code> <code>str | None</code> <p>A document URI.</p> <code>None</code> <code>content</code> <code>bytes</code> <p>Binary content. Required; <code>None</code> raises <code>TypeError</code>.</p> <code>None</code> <code>metadata</code> <code>Metadata | bytes | str | None</code> <p>A document metadata.</p> <code>None</code> <code>temporal_collection</code> <code>str | None</code> <p>The temporal collection.</p> <code>None</code> <p>Returns:</p> Type Description <code>BinaryDocument</code> Source code in <code>mlclient/models/documents.py</code> <pre><code>@classmethod\ndef binary(\n    cls,\n    uri: str | None = None,\n    content: bytes | None = None,\n    *,\n    metadata: Metadata | bytes | str | None = None,\n    temporal_collection: str | None = None,\n) -&gt; BinaryDocument:\n    \"\"\"Instantiate a BinaryDocument.\n\n    Parameters\n    ----------\n    uri : str | None, default None\n        A document URI.\n    content : bytes\n        Binary content. Required; ``None`` raises ``TypeError``.\n    metadata : Metadata | bytes | str | None, default None\n        A document metadata.\n    temporal_collection : str | None, default None\n        The temporal collection.\n\n    Returns\n    -------\n    BinaryDocument\n    \"\"\"\n    return BinaryDocument(\n        content=content,\n        uri=uri,\n        metadata=metadata,\n        temporal_collection=temporal_collection,\n    )\n</code></pre>"},{"location":"reference/mlclient/models/Document/#mlclient.models.Document.create","title":"create  <code>classmethod</code>","text":"<pre><code>create(\n    uri: str | None = None,\n    content: Element | dict | str | bytes | None = None,\n    *,\n    doc_type: DocumentType | str | None = None,\n    metadata: Metadata | bytes | str | None = None,\n    temporal_collection: str | None = None,\n) -&gt; Document\n</code></pre> <p>Instantiate a typed Document, inferring type from content and URI.</p> <p>The factory methods (<code>create</code>, <code>xml</code>, <code>json</code>, <code>text</code>, <code>binary</code>, <code>metadata_update</code>) take <code>uri</code> first - they read as \"create a document at this URI with this content\". The concrete constructors (<code>XMLDocument</code> etc.) take <code>content</code> first, since content is their only required argument and a URI-less document is valid (e.g. one loaded from a file, with its URI assigned on write).</p> <p>Type inference order: 1. Explicit <code>doc_type</code> parameter. 2. <code>ElemTree.Element</code> content -&gt; XML. 3. <code>dict</code> content -&gt; JSON. 4. <code>str</code> or <code>bytes</code> content + <code>uri</code> -&gt; <code>Mimetypes.get_doc_type(uri)</code>    (fallback: BINARY when the extension is unrecognised). 5. <code>str</code> content, no URI -&gt; TEXT. 6. <code>bytes</code> content, no URI -&gt; BINARY.</p> <p>Parameters:</p> Name Type Description Default <code>uri</code> <code>str | None</code> <p>A document URI.</p> <code>None</code> <code>content</code> <code>Element | dict | str | bytes | None</code> <p>A document content.</p> <code>None</code> <code>doc_type</code> <code>DocumentType | str | None</code> <p>A document type override.</p> <code>None</code> <code>metadata</code> <code>Metadata | bytes | str | None</code> <p>A document metadata.</p> <code>None</code> <code>temporal_collection</code> <code>str | None</code> <p>The temporal collection.</p> <code>None</code> <p>Returns:</p> Type Description <code>Document</code> <p>A typed Document subclass instance.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If both <code>content</code> and <code>doc_type</code> are None (use <code>Document.metadata_update()</code> for metadata-only updates).</p> Source code in <code>mlclient/models/documents.py</code> <pre><code>@classmethod\ndef create(\n    cls,\n    uri: str | None = None,\n    content: ElemTree.Element | dict | str | bytes | None = None,\n    *,\n    doc_type: DocumentType | str | None = None,\n    metadata: Metadata | bytes | str | None = None,\n    temporal_collection: str | None = None,\n) -&gt; Document:\n    \"\"\"Instantiate a typed Document, inferring type from content and URI.\n\n    The factory methods (``create``, ``xml``, ``json``, ``text``,\n    ``binary``, ``metadata_update``) take ``uri`` first - they read as\n    \"create a document at this URI with this content\". The concrete\n    constructors (``XMLDocument`` etc.) take ``content`` first, since\n    content is their only required argument and a URI-less document is\n    valid (e.g. one loaded from a file, with its URI assigned on write).\n\n    Type inference order:\n    1. Explicit ``doc_type`` parameter.\n    2. ``ElemTree.Element`` content -&gt; XML.\n    3. ``dict`` content -&gt; JSON.\n    4. ``str`` or ``bytes`` content + ``uri`` -&gt; ``Mimetypes.get_doc_type(uri)``\n       (fallback: BINARY when the extension is unrecognised).\n    5. ``str`` content, no URI -&gt; TEXT.\n    6. ``bytes`` content, no URI -&gt; BINARY.\n\n    Parameters\n    ----------\n    uri : str | None, default None\n        A document URI.\n    content : ElemTree.Element | dict | str | bytes | None, default None\n        A document content.\n    doc_type : DocumentType | str | None, default None\n        A document type override.\n    metadata : Metadata | bytes | str | None, default None\n        A document metadata.\n    temporal_collection : str | None, default None\n        The temporal collection.\n\n    Returns\n    -------\n    Document\n        A typed Document subclass instance.\n\n    Raises\n    ------\n    TypeError\n        If both ``content`` and ``doc_type`` are None (use\n        ``Document.metadata_update()`` for metadata-only updates).\n    \"\"\"\n    if isinstance(doc_type, str):\n        doc_type = DocumentType(doc_type)\n\n    if content is None and doc_type is None:\n        msg = (\n            \"Cannot infer document type: provide content, doc_type, \"\n            \"or use Document.metadata_update() for metadata-only updates\"\n        )\n        raise TypeError(msg)\n\n    resolved_type = _resolve_doc_type(content, doc_type, uri)\n    impl = _TYPED_IMPLS[resolved_type]\n    return impl(\n        content=content,\n        uri=uri,\n        metadata=metadata,\n        temporal_collection=temporal_collection,\n    )\n</code></pre>"},{"location":"reference/mlclient/models/Document/#mlclient.models.Document.json","title":"json  <code>classmethod</code>","text":"<pre><code>json(\n    uri: str | None = None,\n    content: dict | str | bytes | None = None,\n    *,\n    metadata: Metadata | bytes | str | None = None,\n    temporal_collection: str | None = None,\n) -&gt; JSONDocument\n</code></pre> <p>Instantiate a JSONDocument.</p> <p>Accepts <code>dict</code>, <code>str</code>, or <code>bytes</code> content. String and bytes are stored as-is; parsing to dict is deferred to the first <code>.content</code> access.</p> <p>Parameters:</p> Name Type Description Default <code>uri</code> <code>str | None</code> <p>A document URI.</p> <code>None</code> <code>content</code> <code>dict | str | bytes</code> <p>JSON content. Required; <code>None</code> raises <code>TypeError</code>.</p> <code>None</code> <code>metadata</code> <code>Metadata | bytes | str | None</code> <p>A document metadata.</p> <code>None</code> <code>temporal_collection</code> <code>str | None</code> <p>The temporal collection.</p> <code>None</code> <p>Returns:</p> Type Description <code>JSONDocument</code> Source code in <code>mlclient/models/documents.py</code> <pre><code>@classmethod\ndef json(\n    cls,\n    uri: str | None = None,\n    content: dict | str | bytes | None = None,\n    *,\n    metadata: Metadata | bytes | str | None = None,\n    temporal_collection: str | None = None,\n) -&gt; JSONDocument:\n    \"\"\"Instantiate a JSONDocument.\n\n    Accepts ``dict``, ``str``, or ``bytes`` content.\n    String and bytes are stored as-is; parsing to dict is deferred to the\n    first ``.content`` access.\n\n    Parameters\n    ----------\n    uri : str | None, default None\n        A document URI.\n    content : dict | str | bytes\n        JSON content. Required; ``None`` raises ``TypeError``.\n    metadata : Metadata | bytes | str | None, default None\n        A document metadata.\n    temporal_collection : str | None, default None\n        The temporal collection.\n\n    Returns\n    -------\n    JSONDocument\n    \"\"\"\n    return JSONDocument(\n        content=content,\n        uri=uri,\n        metadata=metadata,\n        temporal_collection=temporal_collection,\n    )\n</code></pre>"},{"location":"reference/mlclient/models/Document/#mlclient.models.Document.metadata_update","title":"metadata_update  <code>classmethod</code>","text":"<pre><code>metadata_update(\n    uri: str,\n    metadata: Metadata | bytes | str,\n    *,\n    temporal_collection: str | None = None,\n) -&gt; MetadataDocument\n</code></pre> <p>Instantiate a MetadataDocument for a metadata-only update.</p> <p>Updates the metadata of an existing MarkLogic document without touching its content body.</p> <p>Parameters:</p> Name Type Description Default <code>uri</code> <code>str</code> <p>A document URI.</p> required <code>metadata</code> <code>Metadata | bytes | str</code> <p>Document metadata to apply.</p> required <code>temporal_collection</code> <code>str | None</code> <p>The temporal collection.</p> <code>None</code> <p>Returns:</p> Type Description <code>MetadataDocument</code> <p>Raises:</p> Type Description <code>TypeError</code> <p>If uri is blank or metadata is None.</p> Source code in <code>mlclient/models/documents.py</code> <pre><code>@classmethod\ndef metadata_update(\n    cls,\n    uri: str,\n    metadata: Metadata | bytes | str,\n    *,\n    temporal_collection: str | None = None,\n) -&gt; MetadataDocument:\n    \"\"\"Instantiate a MetadataDocument for a metadata-only update.\n\n    Updates the metadata of an existing MarkLogic document without\n    touching its content body.\n\n    Parameters\n    ----------\n    uri : str\n        A document URI.\n    metadata : Metadata | bytes | str\n        Document metadata to apply.\n    temporal_collection : str | None, default None\n        The temporal collection.\n\n    Returns\n    -------\n    MetadataDocument\n\n    Raises\n    ------\n    TypeError\n        If uri is blank or metadata is None.\n    \"\"\"\n    if not uri or (isinstance(uri, str) and not uri.strip()):\n        msg = \"uri is required for Document.metadata_update()\"\n        raise TypeError(msg)\n    if metadata is None:\n        msg = \"metadata is required for Document.metadata_update()\"\n        raise TypeError(msg)\n    return MetadataDocument(\n        uri=uri,\n        metadata=metadata,\n        temporal_collection=temporal_collection,\n    )\n</code></pre>"},{"location":"reference/mlclient/models/Document/#mlclient.models.Document.text","title":"text  <code>classmethod</code>","text":"<pre><code>text(\n    uri: str | None = None,\n    content: str | bytes | None = None,\n    *,\n    metadata: Metadata | bytes | str | None = None,\n    temporal_collection: str | None = None,\n) -&gt; TextDocument\n</code></pre> <p>Instantiate a TextDocument.</p> <p>Parameters:</p> Name Type Description Default <code>uri</code> <code>str | None</code> <p>A document URI.</p> <code>None</code> <code>content</code> <code>str | bytes</code> <p>Text content. Required; <code>None</code> raises <code>TypeError</code>.</p> <code>None</code> <code>metadata</code> <code>Metadata | bytes | str | None</code> <p>A document metadata.</p> <code>None</code> <code>temporal_collection</code> <code>str | None</code> <p>The temporal collection.</p> <code>None</code> <p>Returns:</p> Type Description <code>TextDocument</code> Source code in <code>mlclient/models/documents.py</code> <pre><code>@classmethod\ndef text(\n    cls,\n    uri: str | None = None,\n    content: str | bytes | None = None,\n    *,\n    metadata: Metadata | bytes | str | None = None,\n    temporal_collection: str | None = None,\n) -&gt; TextDocument:\n    \"\"\"Instantiate a TextDocument.\n\n    Parameters\n    ----------\n    uri : str | None, default None\n        A document URI.\n    content : str | bytes\n        Text content. Required; ``None`` raises ``TypeError``.\n    metadata : Metadata | bytes | str | None, default None\n        A document metadata.\n    temporal_collection : str | None, default None\n        The temporal collection.\n\n    Returns\n    -------\n    TextDocument\n    \"\"\"\n    return TextDocument(\n        content=content,\n        uri=uri,\n        metadata=metadata,\n        temporal_collection=temporal_collection,\n    )\n</code></pre>"},{"location":"reference/mlclient/models/Document/#mlclient.models.Document.xml","title":"xml  <code>classmethod</code>","text":"<pre><code>xml(\n    uri: str | None = None,\n    content: Element | str | bytes | None = None,\n    *,\n    metadata: Metadata | bytes | str | None = None,\n    temporal_collection: str | None = None,\n) -&gt; XMLDocument\n</code></pre> <p>Instantiate an XMLDocument.</p> <p>Accepts <code>ElemTree.Element</code>, <code>str</code>, or <code>bytes</code> content. String and bytes are stored as-is; parsing to ElementTree is deferred to the first <code>.content</code> access.</p> <p>Parameters:</p> Name Type Description Default <code>uri</code> <code>str | None</code> <p>A document URI.</p> <code>None</code> <code>content</code> <code>Element | str | bytes</code> <p>XML content. Required; <code>None</code> raises <code>TypeError</code>.</p> <code>None</code> <code>metadata</code> <code>Metadata | bytes | str | None</code> <p>A document metadata.</p> <code>None</code> <code>temporal_collection</code> <code>str | None</code> <p>The temporal collection.</p> <code>None</code> <p>Returns:</p> Type Description <code>XMLDocument</code> Source code in <code>mlclient/models/documents.py</code> <pre><code>@classmethod\ndef xml(\n    cls,\n    uri: str | None = None,\n    content: ElemTree.Element | str | bytes | None = None,\n    *,\n    metadata: Metadata | bytes | str | None = None,\n    temporal_collection: str | None = None,\n) -&gt; XMLDocument:\n    \"\"\"Instantiate an XMLDocument.\n\n    Accepts ``ElemTree.Element``, ``str``, or ``bytes`` content.\n    String and bytes are stored as-is; parsing to ElementTree is deferred\n    to the first ``.content`` access.\n\n    Parameters\n    ----------\n    uri : str | None, default None\n        A document URI.\n    content : ElemTree.Element | str | bytes\n        XML content. Required; ``None`` raises ``TypeError``.\n    metadata : Metadata | bytes | str | None, default None\n        A document metadata.\n    temporal_collection : str | None, default None\n        The temporal collection.\n\n    Returns\n    -------\n    XMLDocument\n    \"\"\"\n    return XMLDocument(\n        content=content,\n        uri=uri,\n        metadata=metadata,\n        temporal_collection=temporal_collection,\n    )\n</code></pre>"},{"location":"reference/mlclient/models/DocumentType/","title":"DocumentType","text":"<pre><code>from mlclient.models import DocumentType\n</code></pre>"},{"location":"reference/mlclient/models/DocumentType/#mlclient.models.DocumentType","title":"mlclient.models.DocumentType","text":"<p>               Bases: <code>Enum</code></p> <p>An enumeration class representing document types.</p>"},{"location":"reference/mlclient/models/DocumentsBodyPart/","title":"DocumentsBodyPart","text":"<pre><code>from mlclient.models import DocumentsBodyPart\n</code></pre>"},{"location":"reference/mlclient/models/DocumentsBodyPart/#mlclient.models.DocumentsBodyPart","title":"mlclient.models.DocumentsBodyPart  <code>pydantic-model</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>A class representing /v1/documents body part.</p> Show JSON schema: <pre><code>{\n  \"$defs\": {\n    \"Category\": {\n      \"description\": \"An enumeration class representing data categories.\",\n      \"enum\": [\n        \"content\",\n        \"metadata\",\n        \"metadata-values\",\n        \"collections\",\n        \"permissions\",\n        \"properties\",\n        \"quality\"\n      ],\n      \"title\": \"Category\",\n      \"type\": \"string\"\n    },\n    \"DocumentType\": {\n      \"description\": \"An enumeration class representing document types.\",\n      \"enum\": [\n        \"xml\",\n        \"json\",\n        \"binary\",\n        \"text\"\n      ],\n      \"title\": \"DocumentType\",\n      \"type\": \"string\"\n    },\n    \"DocumentsBodyPartType\": {\n      \"description\": \"An enumeration class representing /v1/documents body part types.\",\n      \"enum\": [\n        \"inline\",\n        \"attachment\"\n      ],\n      \"title\": \"DocumentsBodyPartType\",\n      \"type\": \"string\"\n    },\n    \"DocumentsDisposition\": {\n      \"description\": \"A class representing /v1/documents body part Content-Disposition header.\",\n      \"properties\": {\n        \"type\": {\n          \"$ref\": \"#/$defs/DocumentsBodyPartType\",\n          \"description\": \"The content type indication (inline or attachment).\"\n        },\n        \"category\": {\n          \"anyOf\": [\n            {\n              \"$ref\": \"#/$defs/Category\"\n            },\n            {\n              \"items\": {\n                \"$ref\": \"#/$defs/Category\"\n              },\n              \"type\": \"array\"\n            },\n            {\n              \"type\": \"null\"\n            }\n          ],\n          \"default\": null,\n          \"description\": \"The category of data.\",\n          \"title\": \"Category\"\n        },\n        \"repair\": {\n          \"anyOf\": [\n            {\n              \"$ref\": \"#/$defs/Repair\"\n            },\n            {\n              \"type\": \"null\"\n            }\n          ],\n          \"default\": null,\n          \"description\": \"For an XML content part, the level of XML repair to perform. Allowed values: full (default) or none. Use full to request the server to repair malformed input XML. Use none to request the server to reject malformed input XML. If repair results in multiple root nodes, the update is rejected.\"\n        },\n        \"uri\": {\n          \"anyOf\": [\n            {\n              \"type\": \"string\"\n            },\n            {\n              \"type\": \"null\"\n            }\n          ],\n          \"default\": null,\n          \"description\": \"Specifies an explicit document URI. Use extension to have MarkLogic Server generate a URI instead. For a given part, filename and extension are mutually exclusive.\",\n          \"title\": \"Uri\"\n        },\n        \"extension\": {\n          \"anyOf\": [\n            {\n              \"type\": \"string\"\n            },\n            {\n              \"type\": \"null\"\n            }\n          ],\n          \"default\": null,\n          \"description\": \"Specifies a URI extension to use when the document URI is generated by MarkLogic Server. The generated URI will end with '.' plus this extension. For a given part, filename and extension are mutually exclusive.\",\n          \"title\": \"Extension\"\n        },\n        \"directory\": {\n          \"anyOf\": [\n            {\n              \"type\": \"string\"\n            },\n            {\n              \"type\": \"null\"\n            }\n          ],\n          \"default\": null,\n          \"description\": \"Specifies a directory prefix to use when the document URI is generated by MarkLogic Server. The directory prefix must end with '/'. If the part header includes a directory parameter, it must also include an extension parameter. For a given part, filename and directory are mutually exclusive.\",\n          \"title\": \"Directory\"\n        },\n        \"extract\": {\n          \"anyOf\": [\n            {\n              \"$ref\": \"#/$defs/Extract\"\n            },\n            {\n              \"type\": \"null\"\n            }\n          ],\n          \"default\": null,\n          \"description\": \"For a binary content part, whether or not to extract metadata, and whether to store the extracted metadata as document propertiesor in a separate XHTML document. Allowed values: properties or document.\"\n        },\n        \"version_id\": {\n          \"anyOf\": [\n            {\n              \"type\": \"integer\"\n            },\n            {\n              \"type\": \"null\"\n            }\n          ],\n          \"default\": null,\n          \"description\": \"When optimistic locking is enabled by setting the REST instance configuration property update-policy to version-required or version-optional, reject this request if the current version of this document does not match the version in versionId. Only applicable to content parts. Ignored if optimistic locking is not enabled. This option is equivalent to supplying a version id through the If-Match header of a single document update.\",\n          \"title\": \"Version Id\"\n        },\n        \"temporal_document\": {\n          \"anyOf\": [\n            {\n              \"type\": \"string\"\n            },\n            {\n              \"type\": \"null\"\n            }\n          ],\n          \"default\": null,\n          \"description\": \"The 'logical' document URI in the temporal collection specified using the temporal-collection request parameter. You can only use this parameter if the request also includes the temporal-collection parameter.\",\n          \"title\": \"Temporal Document\"\n        },\n        \"format\": {\n          \"anyOf\": [\n            {\n              \"$ref\": \"#/$defs/DocumentType\"\n            },\n            {\n              \"type\": \"null\"\n            }\n          ],\n          \"default\": null,\n          \"description\": \"The content format (xml, json, text or binary)\"\n        }\n      },\n      \"required\": [\n        \"type\"\n      ],\n      \"title\": \"DocumentsDisposition\",\n      \"type\": \"object\"\n    },\n    \"Extract\": {\n      \"description\": \"An enumeration class representing metadata extract types.\",\n      \"enum\": [\n        \"properties\",\n        \"document\"\n      ],\n      \"title\": \"Extract\",\n      \"type\": \"string\"\n    },\n    \"Repair\": {\n      \"description\": \"An enumeration class representing repair levels.\",\n      \"enum\": [\n        \"none\",\n        \"full\"\n      ],\n      \"title\": \"Repair\",\n      \"type\": \"string\"\n    }\n  },\n  \"description\": \"A class representing /v1/documents body part.\",\n  \"properties\": {\n    \"content-type\": {\n      \"default\": \"application/json\",\n      \"title\": \"Content-Type\",\n      \"type\": \"string\"\n    },\n    \"content-disposition\": {\n      \"$ref\": \"#/$defs/DocumentsDisposition\"\n    },\n    \"content\": {\n      \"anyOf\": [\n        {\n          \"type\": \"string\"\n        },\n        {\n          \"format\": \"binary\",\n          \"type\": \"string\"\n        },\n        {\n          \"additionalProperties\": true,\n          \"type\": \"object\"\n        }\n      ],\n      \"title\": \"Content\"\n    }\n  },\n  \"required\": [\n    \"content-disposition\",\n    \"content\"\n  ],\n  \"title\": \"DocumentsBodyPart\",\n  \"type\": \"object\"\n}\n</code></pre> <p>Fields:</p> <ul> <li> <code>content_type</code>                 (<code>str</code>)             </li> <li> <code>disposition</code>                 (<code>DocumentsDisposition</code>)             </li> <li> <code>content</code>                 (<code>Union[str, bytes, dict]</code>)             </li> </ul> <p>Validators:</p> <ul> <li> <code>parse_disposition</code>                 \u2192                   <code>disposition</code> </li> </ul>"},{"location":"reference/mlclient/models/DocumentsBodyPart/#mlclient.models.DocumentsBodyPart.parse_disposition","title":"parse_disposition  <code>pydantic-validator</code>","text":"<pre><code>parse_disposition(\n    value: str | dict,\n) -&gt; DocumentsDisposition\n</code></pre> <p>Parse a Content-Disposition header.</p> Source code in <code>mlclient/models/document_parts.py</code> <pre><code>@field_validator(\"disposition\", mode=\"before\")\n@classmethod\ndef parse_disposition(\n    cls,\n    value: str | dict,\n) -&gt; DocumentsDisposition:\n    \"\"\"Parse a Content-Disposition header.\"\"\"\n    if isinstance(value, dict):\n        return DocumentsDisposition(**value)\n    return DocumentsDisposition.from_header(value)\n</code></pre>"},{"location":"reference/mlclient/models/DocumentsBodyPartType/","title":"DocumentsBodyPartType","text":"<pre><code>from mlclient.models import DocumentsBodyPartType\n</code></pre>"},{"location":"reference/mlclient/models/DocumentsBodyPartType/#mlclient.models.DocumentsBodyPartType","title":"mlclient.models.DocumentsBodyPartType","text":"<p>               Bases: <code>Enum</code></p> <p>An enumeration class representing /v1/documents body part types.</p>"},{"location":"reference/mlclient/models/DocumentsDisposition/","title":"DocumentsDisposition","text":"<pre><code>from mlclient.models import DocumentsDisposition\n</code></pre>"},{"location":"reference/mlclient/models/DocumentsDisposition/#mlclient.models.DocumentsDisposition","title":"mlclient.models.DocumentsDisposition  <code>pydantic-model</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>A class representing /v1/documents body part Content-Disposition header.</p> Show JSON schema: <pre><code>{\n  \"$defs\": {\n    \"Category\": {\n      \"description\": \"An enumeration class representing data categories.\",\n      \"enum\": [\n        \"content\",\n        \"metadata\",\n        \"metadata-values\",\n        \"collections\",\n        \"permissions\",\n        \"properties\",\n        \"quality\"\n      ],\n      \"title\": \"Category\",\n      \"type\": \"string\"\n    },\n    \"DocumentType\": {\n      \"description\": \"An enumeration class representing document types.\",\n      \"enum\": [\n        \"xml\",\n        \"json\",\n        \"binary\",\n        \"text\"\n      ],\n      \"title\": \"DocumentType\",\n      \"type\": \"string\"\n    },\n    \"DocumentsBodyPartType\": {\n      \"description\": \"An enumeration class representing /v1/documents body part types.\",\n      \"enum\": [\n        \"inline\",\n        \"attachment\"\n      ],\n      \"title\": \"DocumentsBodyPartType\",\n      \"type\": \"string\"\n    },\n    \"Extract\": {\n      \"description\": \"An enumeration class representing metadata extract types.\",\n      \"enum\": [\n        \"properties\",\n        \"document\"\n      ],\n      \"title\": \"Extract\",\n      \"type\": \"string\"\n    },\n    \"Repair\": {\n      \"description\": \"An enumeration class representing repair levels.\",\n      \"enum\": [\n        \"none\",\n        \"full\"\n      ],\n      \"title\": \"Repair\",\n      \"type\": \"string\"\n    }\n  },\n  \"description\": \"A class representing /v1/documents body part Content-Disposition header.\",\n  \"properties\": {\n    \"type\": {\n      \"$ref\": \"#/$defs/DocumentsBodyPartType\",\n      \"description\": \"The content type indication (inline or attachment).\"\n    },\n    \"category\": {\n      \"anyOf\": [\n        {\n          \"$ref\": \"#/$defs/Category\"\n        },\n        {\n          \"items\": {\n            \"$ref\": \"#/$defs/Category\"\n          },\n          \"type\": \"array\"\n        },\n        {\n          \"type\": \"null\"\n        }\n      ],\n      \"default\": null,\n      \"description\": \"The category of data.\",\n      \"title\": \"Category\"\n    },\n    \"repair\": {\n      \"anyOf\": [\n        {\n          \"$ref\": \"#/$defs/Repair\"\n        },\n        {\n          \"type\": \"null\"\n        }\n      ],\n      \"default\": null,\n      \"description\": \"For an XML content part, the level of XML repair to perform. Allowed values: full (default) or none. Use full to request the server to repair malformed input XML. Use none to request the server to reject malformed input XML. If repair results in multiple root nodes, the update is rejected.\"\n    },\n    \"uri\": {\n      \"anyOf\": [\n        {\n          \"type\": \"string\"\n        },\n        {\n          \"type\": \"null\"\n        }\n      ],\n      \"default\": null,\n      \"description\": \"Specifies an explicit document URI. Use extension to have MarkLogic Server generate a URI instead. For a given part, filename and extension are mutually exclusive.\",\n      \"title\": \"Uri\"\n    },\n    \"extension\": {\n      \"anyOf\": [\n        {\n          \"type\": \"string\"\n        },\n        {\n          \"type\": \"null\"\n        }\n      ],\n      \"default\": null,\n      \"description\": \"Specifies a URI extension to use when the document URI is generated by MarkLogic Server. The generated URI will end with '.' plus this extension. For a given part, filename and extension are mutually exclusive.\",\n      \"title\": \"Extension\"\n    },\n    \"directory\": {\n      \"anyOf\": [\n        {\n          \"type\": \"string\"\n        },\n        {\n          \"type\": \"null\"\n        }\n      ],\n      \"default\": null,\n      \"description\": \"Specifies a directory prefix to use when the document URI is generated by MarkLogic Server. The directory prefix must end with '/'. If the part header includes a directory parameter, it must also include an extension parameter. For a given part, filename and directory are mutually exclusive.\",\n      \"title\": \"Directory\"\n    },\n    \"extract\": {\n      \"anyOf\": [\n        {\n          \"$ref\": \"#/$defs/Extract\"\n        },\n        {\n          \"type\": \"null\"\n        }\n      ],\n      \"default\": null,\n      \"description\": \"For a binary content part, whether or not to extract metadata, and whether to store the extracted metadata as document propertiesor in a separate XHTML document. Allowed values: properties or document.\"\n    },\n    \"version_id\": {\n      \"anyOf\": [\n        {\n          \"type\": \"integer\"\n        },\n        {\n          \"type\": \"null\"\n        }\n      ],\n      \"default\": null,\n      \"description\": \"When optimistic locking is enabled by setting the REST instance configuration property update-policy to version-required or version-optional, reject this request if the current version of this document does not match the version in versionId. Only applicable to content parts. Ignored if optimistic locking is not enabled. This option is equivalent to supplying a version id through the If-Match header of a single document update.\",\n      \"title\": \"Version Id\"\n    },\n    \"temporal_document\": {\n      \"anyOf\": [\n        {\n          \"type\": \"string\"\n        },\n        {\n          \"type\": \"null\"\n        }\n      ],\n      \"default\": null,\n      \"description\": \"The 'logical' document URI in the temporal collection specified using the temporal-collection request parameter. You can only use this parameter if the request also includes the temporal-collection parameter.\",\n      \"title\": \"Temporal Document\"\n    },\n    \"format\": {\n      \"anyOf\": [\n        {\n          \"$ref\": \"#/$defs/DocumentType\"\n        },\n        {\n          \"type\": \"null\"\n        }\n      ],\n      \"default\": null,\n      \"description\": \"The content format (xml, json, text or binary)\"\n    }\n  },\n  \"required\": [\n    \"type\"\n  ],\n  \"title\": \"DocumentsDisposition\",\n  \"type\": \"object\"\n}\n</code></pre> <p>Config:</p> <ul> <li><code>default</code>: <code>{'populate_by_name': True}</code></li> </ul> <p>Fields:</p> <ul> <li> <code>type_</code>                 (<code>DocumentsBodyPartType</code>)             </li> <li> <code>category</code>                 (<code>Optional[Union[Category, list[Category]]]</code>)             </li> <li> <code>repair</code>                 (<code>Optional[Repair]</code>)             </li> <li> <code>filename</code>                 (<code>Optional[str]</code>)             </li> <li> <code>extension</code>                 (<code>Optional[str]</code>)             </li> <li> <code>directory</code>                 (<code>Optional[str]</code>)             </li> <li> <code>extract</code>                 (<code>Optional[Extract]</code>)             </li> <li> <code>version_id</code>                 (<code>Optional[int]</code>)             </li> <li> <code>temporal_document</code>                 (<code>Optional[str]</code>)             </li> <li> <code>format_</code>                 (<code>Optional[DocumentType]</code>)             </li> </ul>"},{"location":"reference/mlclient/models/DocumentsDisposition/#mlclient.models.DocumentsDisposition.category","title":"category  <code>pydantic-field</code>","text":"<pre><code>category: Optional[Union[Category, list[Category]]] = None\n</code></pre> <p>The category of data.</p>"},{"location":"reference/mlclient/models/DocumentsDisposition/#mlclient.models.DocumentsDisposition.directory","title":"directory  <code>pydantic-field</code>","text":"<pre><code>directory: Optional[str] = None\n</code></pre> <p>Specifies a directory prefix to use when the document URI is generated by MarkLogic Server. The directory prefix must end with '/'. If the part header includes a directory parameter, it must also include an extension parameter. For a given part, filename and directory are mutually exclusive.</p>"},{"location":"reference/mlclient/models/DocumentsDisposition/#mlclient.models.DocumentsDisposition.extension","title":"extension  <code>pydantic-field</code>","text":"<pre><code>extension: Optional[str] = None\n</code></pre> <p>Specifies a URI extension to use when the document URI is generated by MarkLogic Server. The generated URI will end with '.' plus this extension. For a given part, filename and extension are mutually exclusive.</p>"},{"location":"reference/mlclient/models/DocumentsDisposition/#mlclient.models.DocumentsDisposition.extract","title":"extract  <code>pydantic-field</code>","text":"<pre><code>extract: Optional[Extract] = None\n</code></pre> <p>For a binary content part, whether or not to extract metadata, and whether to store the extracted metadata as document propertiesor in a separate XHTML document. Allowed values: properties or document.</p>"},{"location":"reference/mlclient/models/DocumentsDisposition/#mlclient.models.DocumentsDisposition.filename","title":"filename  <code>pydantic-field</code>","text":"<pre><code>filename: Optional[str] = None\n</code></pre> <p>Specifies an explicit document URI. Use extension to have MarkLogic Server generate a URI instead. For a given part, filename and extension are mutually exclusive.</p>"},{"location":"reference/mlclient/models/DocumentsDisposition/#mlclient.models.DocumentsDisposition.format_","title":"format_  <code>pydantic-field</code>","text":"<pre><code>format_: Optional[DocumentType] = None\n</code></pre> <p>The content format (xml, json, text or binary)</p>"},{"location":"reference/mlclient/models/DocumentsDisposition/#mlclient.models.DocumentsDisposition.is_attachment","title":"is_attachment  <code>property</code>","text":"<pre><code>is_attachment: bool\n</code></pre> <p>Whether this disposition is an attachment (document-specific part).</p>"},{"location":"reference/mlclient/models/DocumentsDisposition/#mlclient.models.DocumentsDisposition.is_inline","title":"is_inline  <code>property</code>","text":"<pre><code>is_inline: bool\n</code></pre> <p>Whether this disposition is inline (default metadata part).</p>"},{"location":"reference/mlclient/models/DocumentsDisposition/#mlclient.models.DocumentsDisposition.repair","title":"repair  <code>pydantic-field</code>","text":"<pre><code>repair: Optional[Repair] = None\n</code></pre> <p>For an XML content part, the level of XML repair to perform. Allowed values: full (default) or none. Use full to request the server to repair malformed input XML. Use none to request the server to reject malformed input XML. If repair results in multiple root nodes, the update is rejected.</p>"},{"location":"reference/mlclient/models/DocumentsDisposition/#mlclient.models.DocumentsDisposition.temporal_document","title":"temporal_document  <code>pydantic-field</code>","text":"<pre><code>temporal_document: Optional[str] = None\n</code></pre> <p>The 'logical' document URI in the temporal collection specified using the temporal-collection request parameter. You can only use this parameter if the request also includes the temporal-collection parameter.</p>"},{"location":"reference/mlclient/models/DocumentsDisposition/#mlclient.models.DocumentsDisposition.type_","title":"type_  <code>pydantic-field</code>","text":"<pre><code>type_: DocumentsBodyPartType\n</code></pre> <p>The content type indication (inline or attachment).</p>"},{"location":"reference/mlclient/models/DocumentsDisposition/#mlclient.models.DocumentsDisposition.version_id","title":"version_id  <code>pydantic-field</code>","text":"<pre><code>version_id: Optional[int] = None\n</code></pre> <p>When optimistic locking is enabled by setting the REST instance configuration property update-policy to version-required or version-optional, reject this request if the current version of this document does not match the version in versionId. Only applicable to content parts. Ignored if optimistic locking is not enabled. This option is equivalent to supplying a version id through the If-Match header of a single document update.</p>"},{"location":"reference/mlclient/models/DocumentsDisposition/#mlclient.models.DocumentsDisposition.from_header","title":"from_header  <code>classmethod</code>","text":"<pre><code>from_header(header: str) -&gt; DocumentsDisposition\n</code></pre> <p>Parse a raw Content-Disposition header string.</p> <p>Parameters:</p> Name Type Description Default <code>header</code> <code>str</code> <p>A raw Content-Disposition header value</p> required <p>Returns:</p> Type Description <code>DocumentsDisposition</code> <p>A parsed DocumentsDisposition instance</p> Source code in <code>mlclient/models/document_parts.py</code> <pre><code>@classmethod\ndef from_header(cls, header: str) -&gt; DocumentsDisposition:\n    \"\"\"Parse a raw Content-Disposition header string.\n\n    Parameters\n    ----------\n    header : str\n        A raw Content-Disposition header value\n\n    Returns\n    -------\n    DocumentsDisposition\n        A parsed DocumentsDisposition instance\n    \"\"\"\n    m = _DispositionMapping\n    disp_dict = {}\n    for disp in header.split(m.DISP_SEP):\n        key, value = cls._parse_header_part(disp)\n        curr_value = disp_dict.get(key)\n        if curr_value is None:\n            disp_dict[key] = value\n        elif not isinstance(curr_value, list):\n            disp_dict[key] = [curr_value, value]\n        else:\n            curr_value.append(value)\n    return cls(**disp_dict)\n</code></pre>"},{"location":"reference/mlclient/models/DocumentsDisposition/#mlclient.models.DocumentsDisposition.to_header","title":"to_header","text":"<pre><code>to_header() -&gt; str\n</code></pre> <p>Serialize this instance to a raw Content-Disposition header string.</p> <p>Returns:</p> Type Description <code>str</code> <p>A raw Content-Disposition header value</p> Source code in <code>mlclient/models/document_parts.py</code> <pre><code>def to_header(self) -&gt; str:\n    \"\"\"Serialize this instance to a raw Content-Disposition header string.\n\n    Returns\n    -------\n    str\n        A raw Content-Disposition header value\n    \"\"\"\n    m = _DispositionMapping\n    parts = [self._serialize_field(key) for key in m.FIELD_ORDER]\n    return m.DISP_SEP.join([p for p in parts if p is not None])\n</code></pre>"},{"location":"reference/mlclient/models/Extract/","title":"Extract","text":"<pre><code>from mlclient.models import Extract\n</code></pre>"},{"location":"reference/mlclient/models/Extract/#mlclient.models.Extract","title":"mlclient.models.Extract","text":"<p>               Bases: <code>Enum</code></p> <p>An enumeration class representing metadata extract types.</p>"},{"location":"reference/mlclient/models/JSONDocument/","title":"JSONDocument","text":"<pre><code>from mlclient.models import JSONDocument\n</code></pre>"},{"location":"reference/mlclient/models/JSONDocument/#mlclient.models.JSONDocument","title":"mlclient.models.JSONDocument","text":"<pre><code>JSONDocument(\n    content: dict | str | bytes,\n    uri: str | None = None,\n    metadata: Metadata | bytes | str | None = None,\n    temporal_collection: str | None = None,\n)\n</code></pre> <p>               Bases: <code>Document</code></p> <p>A Document implementation representing a single MarkLogic JSON document.</p> <p>Accepts <code>dict</code>, <code>str</code>, or <code>bytes</code> content. String and bytes are stored as-is; parsing to <code>dict</code> is deferred to the first <code>.content</code> access.</p> Source code in <code>mlclient/models/documents.py</code> <pre><code>def __init__(\n    self,\n    content: dict | str | bytes,\n    uri: str | None = None,\n    metadata: Metadata | bytes | str | None = None,\n    temporal_collection: str | None = None,\n):\n    if content is None:\n        msg = (\n            \"JSONDocument requires content; \"\n            \"use Document.metadata_update() for metadata-only updates\"\n        )\n        raise TypeError(msg)\n    super().__init__(uri, DocumentType.JSON, metadata, temporal_collection)\n    self._parsed: dict | None = content if isinstance(content, dict) else None\n    self._content_bytes: bytes | None = (\n        content if isinstance(content, bytes) else None\n    )\n    self._content_string: str | None = content if isinstance(content, str) else None\n</code></pre>"},{"location":"reference/mlclient/models/JSONDocument/#mlclient.models.JSONDocument.content","title":"content  <code>property</code>","text":"<pre><code>content: dict\n</code></pre> <p>A parsed dict representation of JSON content.</p> <p>Parsing from the raw string/bytes form is deferred to the first access and cached for subsequent calls.</p> <p>Returns:</p> Type Description <code>dict</code> <p>The parsed JSON content as a dictionary.</p>"},{"location":"reference/mlclient/models/JSONDocument/#mlclient.models.JSONDocument.content_bytes","title":"content_bytes  <code>property</code>","text":"<pre><code>content_bytes: bytes\n</code></pre> <p>A UTF-8 encoded representation of the JSON content.</p> <p>Computed once on first access and cached for subsequent calls.</p> <p>Returns:</p> Type Description <code>bytes</code> <p>The content encoded as UTF-8 bytes.</p>"},{"location":"reference/mlclient/models/JSONDocument/#mlclient.models.JSONDocument.content_string","title":"content_string  <code>property</code>","text":"<pre><code>content_string: str\n</code></pre> <p>A string representation of the JSON content.</p> <p>Computed once on first access and cached for subsequent calls.</p> <p>Returns:</p> Type Description <code>str</code> <p>The content as a string.</p>"},{"location":"reference/mlclient/models/JSONDocument/#mlclient.models.JSONDocument.invalidate","title":"invalidate","text":"<pre><code>invalidate() -&gt; JSONDocument\n</code></pre> <p>Drop cached bytes/string serializations of the parsed dict.</p> <p>Use this when the parsed <code>.content</code> has been mutated externally so that subsequent <code>.content_bytes</code> / <code>.content_string</code> calls re-serialize from the parsed form instead of returning stale cached output. Has no effect when the document has not been parsed yet.</p> <p>Returns:</p> Type Description <code>JSONDocument</code> <p><code>self</code>, so the call can be chained directly into a write.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; doc.content[\"name\"] = \"Smith\"\n&gt;&gt;&gt; ml.documents.write(doc.invalidate())\n</code></pre> Source code in <code>mlclient/models/documents.py</code> <pre><code>def invalidate(self) -&gt; JSONDocument:\n    \"\"\"Drop cached bytes/string serializations of the parsed dict.\n\n    Use this when the parsed ``.content`` has been mutated externally so\n    that subsequent ``.content_bytes`` / ``.content_string`` calls\n    re-serialize from the parsed form instead of returning stale cached\n    output. Has no effect when the document has not been parsed yet.\n\n    Returns\n    -------\n    JSONDocument\n        ``self``, so the call can be chained directly into a write.\n\n    Examples\n    --------\n    &gt;&gt;&gt; doc.content[\"name\"] = \"Smith\"\n    &gt;&gt;&gt; ml.documents.write(doc.invalidate())\n    \"\"\"\n    if self._parsed is not None:\n        self._content_bytes = None\n        self._content_string = None\n    return self\n</code></pre>"},{"location":"reference/mlclient/models/LogType/","title":"LogType","text":"<pre><code>from mlclient.models import LogType\n</code></pre>"},{"location":"reference/mlclient/models/LogType/#mlclient.models.LogType","title":"mlclient.models.LogType","text":"<p>               Bases: <code>Enum</code></p> <p>An enumeration class representing MarkLogic log types.</p>"},{"location":"reference/mlclient/models/LogType/#mlclient.models.LogType.get","title":"get  <code>staticmethod</code>","text":"<pre><code>get(logs_type: str) -&gt; LogType\n</code></pre> <p>Get a specific LogType enum for a string value.</p> Source code in <code>mlclient/models/types.py</code> <pre><code>@staticmethod\ndef get(\n    logs_type: str,\n) -&gt; LogType:\n    \"\"\"Get a specific LogType enum for a string value.\"\"\"\n    if logs_type.lower() == \"error\":\n        return LogType.ERROR\n    if logs_type.lower() == \"access\":\n        return LogType.ACCESS\n    if logs_type.lower() == \"request\":\n        return LogType.REQUEST\n    if logs_type.lower() == \"audit\":\n        return LogType.AUDIT\n    msg = \"Invalid log type! Allowed values are: error, access, request.\"\n    raise InvalidLogTypeError(msg)\n</code></pre>"},{"location":"reference/mlclient/models/MarkLogicVersion/","title":"MarkLogicVersion","text":"<pre><code>from mlclient.models import MarkLogicVersion\n</code></pre>"},{"location":"reference/mlclient/models/MarkLogicVersion/#mlclient.models.MarkLogicVersion","title":"mlclient.models.MarkLogicVersion  <code>dataclass</code>","text":"<pre><code>MarkLogicVersion(raw: str)\n</code></pre> <p>An immutable server version with four numeric parts.</p> <p>Parameters:</p> Name Type Description Default <code>raw</code> <code>str</code> <p>Dotted or legacy hyphenated version, e.g. <code>12.0.1</code> or <code>10.0-9.5</code>. Surrounding whitespace is removed; separators and suffixes are preserved.</p> required <p>Attributes:</p> Name Type Description <code>parts</code> <code>tuple[int, int, int | None, int | None]</code> <p>Numeric components in their original order. Missing components are None. Iterating or unpacking the object yields these same four components.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the input is not a version with two to four numeric components, optionally followed by a textual suffix</p>"},{"location":"reference/mlclient/models/Metadata/","title":"Metadata","text":"<pre><code>from mlclient.models import Metadata\n</code></pre>"},{"location":"reference/mlclient/models/Metadata/#mlclient.models.Metadata","title":"mlclient.models.Metadata","text":"<pre><code>Metadata(\n    collections: list | None = None,\n    permissions: list | None = None,\n    properties: dict | None = None,\n    quality: int = 0,\n    metadata_values: dict | None = None,\n    *,\n    raw: bytes | str | None = None,\n)\n</code></pre> <p>A class representing MarkLogic's document metadata.</p> <p>Initialize Metadata instance.</p> <p>Parameters:</p> Name Type Description Default <code>collections</code> <code>list | None</code> <p>Document collections' list</p> <code>None</code> <code>permissions</code> <code>list | None</code> <p>Document permissions' list</p> <code>None</code> <code>properties</code> <code>dict | None</code> <p>Document's properties</p> <code>None</code> <code>quality</code> <code>int</code> <p>Document's quality</p> <code>0</code> <code>metadata_values</code> <code>dict | None</code> <p>Document's metadata values</p> <code>None</code> <code>raw</code> <code>bytes | str | None</code> <p>Raw metadata payload. When set, parsing is deferred until a field accessor is called. Format is auto-detected: a leading <code>&lt;</code> (after whitespace) is treated as XML, anything else as JSON. Once parsed the raw form is discarded and <code>to_json_string</code> / <code>to_xml_string</code> re-serialize from the parsed state.</p> <code>None</code> Source code in <code>mlclient/models/documents.py</code> <pre><code>def __init__(\n    self,\n    collections: list | None = None,\n    permissions: list | None = None,\n    properties: dict | None = None,\n    quality: int = 0,\n    metadata_values: dict | None = None,\n    *,\n    raw: bytes | str | None = None,\n):\n    \"\"\"Initialize Metadata instance.\n\n    Parameters\n    ----------\n    collections : list | None\n        Document collections' list\n    permissions : list | None\n        Document permissions' list\n    properties : dict | None\n        Document's properties\n    quality : int\n        Document's quality\n    metadata_values : dict | None\n        Document's metadata values\n    raw : bytes | str | None\n        Raw metadata payload. When set, parsing is deferred until a field\n        accessor is called. Format is auto-detected: a leading ``&lt;`` (after\n        whitespace) is treated as XML, anything else as JSON. Once parsed\n        the raw form is discarded and ``to_json_string`` / ``to_xml_string``\n        re-serialize from the parsed state.\n    \"\"\"\n    self._raw: bytes | str | None = raw\n    if raw is not None:\n        self._collections: list | None = None\n        self._permissions: list | None = None\n        self._properties: dict | None = None\n        self._quality: int | None = None\n        self._metadata_values: dict | None = None\n    else:\n        self._collections = list(set(collections)) if collections else []\n        self._permissions = self._get_clean_permissions(permissions)\n        self._properties = self._get_clean_dict(properties)\n        self._quality = quality\n        self._metadata_values = self._get_clean_dict(metadata_values)\n</code></pre>"},{"location":"reference/mlclient/models/Metadata/#mlclient.models.Metadata.add_collection","title":"add_collection","text":"<pre><code>add_collection(collection: str) -&gt; bool\n</code></pre> <p>Assign a new collection to document.</p> <p>Parameters:</p> Name Type Description Default <code>collection</code> <code>str</code> <p>A document's new collection</p> required <p>Returns:</p> Name Type Description <code>allow</code> <code>bool</code> <p>True if the collection is non-blank value, and it does not appear already in document's collections. Otherwise, False.</p> Source code in <code>mlclient/models/documents.py</code> <pre><code>def add_collection(\n    self,\n    collection: str,\n) -&gt; bool:\n    \"\"\"Assign a new collection to document.\n\n    Parameters\n    ----------\n    collection : str\n        A document's new collection\n\n    Returns\n    -------\n    allow : bool\n        True if the collection is non-blank value, and it does not appear already\n        in document's collections. Otherwise, False.\n    \"\"\"\n    allow = (\n        collection is not None\n        and not re.search(\"^\\\\s*$\", collection)\n        and collection not in self.collections()\n    )\n    if allow:\n        self._collections.append(collection)\n    return allow\n</code></pre>"},{"location":"reference/mlclient/models/Metadata/#mlclient.models.Metadata.add_permission","title":"add_permission","text":"<pre><code>add_permission(role_name: str, capability: str) -&gt; bool\n</code></pre> <p>Assign a new permission to document.</p> <p>Parameters:</p> Name Type Description Default <code>role_name</code> <code>str</code> <p>a permission's role name</p> required <code>capability</code> <code>str</code> <p>a permission's capability</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if there's no such capability assigned to this role already, and it is a correct one. Otherwise, False.</p> Source code in <code>mlclient/models/documents.py</code> <pre><code>def add_permission(\n    self,\n    role_name: str,\n    capability: str,\n) -&gt; bool:\n    \"\"\"Assign a new permission to document.\n\n    Parameters\n    ----------\n    role_name : str\n        a permission's role name\n    capability : str\n        a permission's capability\n\n    Returns\n    -------\n    bool\n        True if there's no such capability assigned to this role already, and it is\n        a correct one. Otherwise, False.\n    \"\"\"\n    allow = role_name is not None and capability is not None\n    if allow:\n        self._ensure_parsed()\n        permission = self._get_permission_for_role(self._permissions, role_name)\n        if permission is not None:\n            return permission.add_capability(capability)\n\n        self._permissions.append(Permission(role_name, {capability}))\n        return True\n    return allow\n</code></pre>"},{"location":"reference/mlclient/models/Metadata/#mlclient.models.Metadata.collections","title":"collections","text":"<pre><code>collections() -&gt; list\n</code></pre> <p>Return document's collections.</p> Source code in <code>mlclient/models/documents.py</code> <pre><code>def collections(\n    self,\n) -&gt; list:\n    \"\"\"Return document's collections.\"\"\"\n    self._ensure_parsed()\n    return self._collections.copy()\n</code></pre>"},{"location":"reference/mlclient/models/Metadata/#mlclient.models.Metadata.from_file","title":"from_file  <code>classmethod</code>","text":"<pre><code>from_file(file_path: str) -&gt; Metadata\n</code></pre> <p>Initialize a Metadata instance from a file.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str</code> <p>A metadata file path</p> required <p>Returns:</p> Type Description <code>Metadata</code> <p>A Metadata instance</p> Source code in <code>mlclient/models/documents.py</code> <pre><code>@classmethod\ndef from_file(\n    cls,\n    file_path: str,\n) -&gt; Metadata:\n    \"\"\"Initialize a Metadata instance from a file.\n\n    Parameters\n    ----------\n    file_path : str\n        A metadata file path\n\n    Returns\n    -------\n    Metadata\n        A Metadata instance\n    \"\"\"\n    file_path = Path(file_path)\n    with file_path.open() as file:\n        if file_path.suffix == \".json\":\n            return cls._from_json_file(file)\n        return cls._from_xml_file(file)\n</code></pre>"},{"location":"reference/mlclient/models/Metadata/#mlclient.models.Metadata.metadata_values","title":"metadata_values","text":"<pre><code>metadata_values() -&gt; dict\n</code></pre> <p>Return document's metadata values.</p> Source code in <code>mlclient/models/documents.py</code> <pre><code>def metadata_values(\n    self,\n) -&gt; dict:\n    \"\"\"Return document's metadata values.\"\"\"\n    self._ensure_parsed()\n    return self._metadata_values.copy()\n</code></pre>"},{"location":"reference/mlclient/models/Metadata/#mlclient.models.Metadata.permissions","title":"permissions","text":"<pre><code>permissions() -&gt; list\n</code></pre> <p>Return document's permissions.</p> Source code in <code>mlclient/models/documents.py</code> <pre><code>def permissions(\n    self,\n) -&gt; list:\n    \"\"\"Return document's permissions.\"\"\"\n    self._ensure_parsed()\n    return [copy.copy(perm) for perm in self._permissions]\n</code></pre>"},{"location":"reference/mlclient/models/Metadata/#mlclient.models.Metadata.properties","title":"properties","text":"<pre><code>properties() -&gt; dict\n</code></pre> <p>Return document's properties.</p> Source code in <code>mlclient/models/documents.py</code> <pre><code>def properties(\n    self,\n) -&gt; dict:\n    \"\"\"Return document's properties.\"\"\"\n    self._ensure_parsed()\n    return self._properties.copy()\n</code></pre>"},{"location":"reference/mlclient/models/Metadata/#mlclient.models.Metadata.put_metadata_value","title":"put_metadata_value","text":"<pre><code>put_metadata_value(name: str, value: str)\n</code></pre> <p>Assign a new metadata value to document.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>A metadata name</p> required <code>value</code> <code>str</code> <p>A metadata value</p> required Source code in <code>mlclient/models/documents.py</code> <pre><code>def put_metadata_value(\n    self,\n    name: str,\n    value: str,\n):\n    \"\"\"Assign a new metadata value to document.\n\n    Parameters\n    ----------\n    name : str\n        A metadata name\n    value : str\n        A metadata value\n    \"\"\"\n    if name and value:\n        self._ensure_parsed()\n        self._metadata_values[name] = value\n</code></pre>"},{"location":"reference/mlclient/models/Metadata/#mlclient.models.Metadata.put_property","title":"put_property","text":"<pre><code>put_property(name: str, value: str)\n</code></pre> <p>Assign a new property to document.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>A property name</p> required <code>value</code> <code>str</code> <p>A property value</p> required Source code in <code>mlclient/models/documents.py</code> <pre><code>def put_property(\n    self,\n    name: str,\n    value: str,\n):\n    \"\"\"Assign a new property to document.\n\n    Parameters\n    ----------\n    name : str\n        A property name\n    value : str\n        A property value\n    \"\"\"\n    if name and value:\n        self._ensure_parsed()\n        self._properties[name] = value\n</code></pre>"},{"location":"reference/mlclient/models/Metadata/#mlclient.models.Metadata.quality","title":"quality","text":"<pre><code>quality() -&gt; int\n</code></pre> <p>Return document's quality.</p> Source code in <code>mlclient/models/documents.py</code> <pre><code>def quality(\n    self,\n) -&gt; int:\n    \"\"\"Return document's quality.\"\"\"\n    self._ensure_parsed()\n    return self._quality\n</code></pre>"},{"location":"reference/mlclient/models/Metadata/#mlclient.models.Metadata.raw","title":"raw","text":"<pre><code>raw() -&gt; bytes | str | None\n</code></pre> <p>Return the raw metadata payload, or None if not available.</p> <p>Available when the instance was created via <code>Metadata(raw=...)</code> and no field has been read or modified yet. Returns the payload in the original type (<code>bytes</code> or <code>str</code>) so consumers can route it to bytes-oriented sinks (file writes, multipart bodies) without an unnecessary decode/encode round-trip.</p> <p>Once a field accessor is called, the raw payload is discarded and <code>raw()</code> returns None; use <code>to_json_string()</code> / <code>to_xml_string()</code> instead.</p> <p>Returns:</p> Type Description <code>bytes | str | None</code> <p>The raw payload in its original type, or None if the metadata was built from fields or has already been parsed.</p> Source code in <code>mlclient/models/documents.py</code> <pre><code>def raw(\n    self,\n) -&gt; bytes | str | None:\n    \"\"\"Return the raw metadata payload, or None if not available.\n\n    Available when the instance was created via ``Metadata(raw=...)`` and\n    no field has been read or modified yet. Returns the payload in the\n    original type (``bytes`` or ``str``) so consumers can route it to\n    bytes-oriented sinks (file writes, multipart bodies) without an\n    unnecessary decode/encode round-trip.\n\n    Once a field accessor is called, the raw payload is discarded and\n    ``raw()`` returns None; use ``to_json_string()`` / ``to_xml_string()``\n    instead.\n\n    Returns\n    -------\n    bytes | str | None\n        The raw payload in its original type, or None if the metadata was\n        built from fields or has already been parsed.\n    \"\"\"\n    return self._raw\n</code></pre>"},{"location":"reference/mlclient/models/Metadata/#mlclient.models.Metadata.remove_collection","title":"remove_collection","text":"<pre><code>remove_collection(collection: str) -&gt; bool\n</code></pre> <p>Remove a collection from document.</p> <p>Parameters:</p> Name Type Description Default <code>collection</code> <code>str</code> <p>A document's collection</p> required <p>Returns:</p> Name Type Description <code>allow</code> <code>bool</code> <p>True if the collection is assigned to the document. Otherwise, False.</p> Source code in <code>mlclient/models/documents.py</code> <pre><code>def remove_collection(\n    self,\n    collection: str,\n) -&gt; bool:\n    \"\"\"Remove a collection from document.\n\n    Parameters\n    ----------\n    collection : str\n        A document's collection\n\n    Returns\n    -------\n    allow : bool\n        True if the collection is assigned to the document. Otherwise, False.\n    \"\"\"\n    allow = collection is not None and collection in self.collections()\n    if allow:\n        self._collections.remove(collection)\n    return allow\n</code></pre>"},{"location":"reference/mlclient/models/Metadata/#mlclient.models.Metadata.remove_metadata_value","title":"remove_metadata_value","text":"<pre><code>remove_metadata_value(name: str) -&gt; bool\n</code></pre> <p>Remove a metadata value from document.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>A metadata name</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the document has a metadata with such name. Otherwise, False.</p> Source code in <code>mlclient/models/documents.py</code> <pre><code>def remove_metadata_value(\n    self,\n    name: str,\n) -&gt; bool:\n    \"\"\"Remove a metadata value from document.\n\n    Parameters\n    ----------\n    name : str\n        A metadata name\n\n    Returns\n    -------\n    bool\n        True if the document has a metadata with such name. Otherwise, False.\n    \"\"\"\n    self._ensure_parsed()\n    return self._metadata_values.pop(name, None) is not None\n</code></pre>"},{"location":"reference/mlclient/models/Metadata/#mlclient.models.Metadata.remove_permission","title":"remove_permission","text":"<pre><code>remove_permission(role_name: str, capability: str) -&gt; bool\n</code></pre> <p>Remove a permission from document.</p> <p>Parameters:</p> Name Type Description Default <code>role_name</code> <code>str</code> <p>A permission's role name</p> required <code>capability</code> <code>str</code> <p>A permission's capability</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the capability is assigned to the role for a document. Otherwise, False.</p> Source code in <code>mlclient/models/documents.py</code> <pre><code>def remove_permission(\n    self,\n    role_name: str,\n    capability: str,\n) -&gt; bool:\n    \"\"\"Remove a permission from document.\n\n    Parameters\n    ----------\n    role_name : str\n        A permission's role name\n    capability : str\n        A permission's capability\n\n    Returns\n    -------\n    bool\n        True if the capability is assigned to the role for a document.\n        Otherwise, False.\n    \"\"\"\n    allow = role_name is not None and capability is not None\n    if allow:\n        self._ensure_parsed()\n        permission = self._get_permission_for_role(self._permissions, role_name)\n        allow = permission is not None\n        if allow:\n            success = permission.remove_capability(capability)\n            if len(permission.capabilities()) == 0:\n                self._permissions.remove(permission)\n            return success\n        return allow\n    return allow\n</code></pre>"},{"location":"reference/mlclient/models/Metadata/#mlclient.models.Metadata.remove_property","title":"remove_property","text":"<pre><code>remove_property(name: str) -&gt; bool\n</code></pre> <p>Remove a property from document.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>A property name</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the document has a property with such name. Otherwise, False.</p> Source code in <code>mlclient/models/documents.py</code> <pre><code>def remove_property(\n    self,\n    name: str,\n) -&gt; bool:\n    \"\"\"Remove a property from document.\n\n    Parameters\n    ----------\n    name : str\n        A property name\n\n    Returns\n    -------\n    bool\n        True if the document has a property with such name. Otherwise, False.\n    \"\"\"\n    self._ensure_parsed()\n    return self._properties.pop(name, None) is not None\n</code></pre>"},{"location":"reference/mlclient/models/Metadata/#mlclient.models.Metadata.set_quality","title":"set_quality","text":"<pre><code>set_quality(quality: int) -&gt; bool\n</code></pre> <p>Set document's quality.</p> <p>Parameters:</p> Name Type Description Default <code>quality</code> <code>int</code> <p>A document's new quality</p> required <p>Returns:</p> Name Type Description <code>allow</code> <code>bool</code> <p>True if value provided is an integer. Otherwise, False.</p> Source code in <code>mlclient/models/documents.py</code> <pre><code>def set_quality(\n    self,\n    quality: int,\n) -&gt; bool:\n    \"\"\"Set document's quality.\n\n    Parameters\n    ----------\n    quality : int\n        A document's new quality\n\n    Returns\n    -------\n    allow : bool\n        True if value provided is an integer. Otherwise, False.\n    \"\"\"\n    allow = isinstance(quality, int)\n    if allow:\n        self._ensure_parsed()\n        self._quality = quality\n    return allow\n</code></pre>"},{"location":"reference/mlclient/models/Metadata/#mlclient.models.Metadata.to_json","title":"to_json","text":"<pre><code>to_json() -&gt; dict\n</code></pre> <p>Return a JSON representation of the Metadata instance.</p> Source code in <code>mlclient/models/documents.py</code> <pre><code>def to_json(\n    self,\n) -&gt; dict:\n    \"\"\"Return a JSON representation of the Metadata instance.\"\"\"\n    self._ensure_parsed()\n    return {\n        self._COLLECTIONS_KEY: self.collections(),\n        self._PERMISSIONS_KEY: [p.to_json() for p in self._permissions],\n        self._PROPERTIES_KEY: self.properties(),\n        self._QUALITY_KEY: self.quality(),\n        self._METADATA_VALUES_KEY: self._metadata_values,\n    }\n</code></pre>"},{"location":"reference/mlclient/models/Metadata/#mlclient.models.Metadata.to_json_string","title":"to_json_string","text":"<pre><code>to_json_string(indent: int | None = None) -&gt; str\n</code></pre> <p>Return a stringified JSON representation of the Metadata instance.</p> <p>When the instance was created from a raw JSON payload and no field has been read or modified yet, the original payload is returned without re-serialization.</p> <p>Parameters:</p> Name Type Description Default <code>indent</code> <code>int | None</code> <p>A number of spaces per indent level</p> <code>None</code> <p>Returns:</p> Type Description <code>str</code> <p>Metadata in a stringified JSON representation</p> Source code in <code>mlclient/models/documents.py</code> <pre><code>def to_json_string(\n    self,\n    indent: int | None = None,\n) -&gt; str:\n    \"\"\"Return a stringified JSON representation of the Metadata instance.\n\n    When the instance was created from a raw JSON payload and no field has\n    been read or modified yet, the original payload is returned without\n    re-serialization.\n\n    Parameters\n    ----------\n    indent : int | None\n        A number of spaces per indent level\n\n    Returns\n    -------\n    str\n        Metadata in a stringified JSON representation\n    \"\"\"\n    if indent is None and self._raw is not None:\n        source = (\n            self._raw.decode(\"utf-8\") if isinstance(self._raw, bytes) else self._raw\n        )\n        stripped = source.lstrip()\n        if stripped.startswith((\"{\", \"[\")):\n            return source\n    return json.dumps(self.to_json(), indent=indent)\n</code></pre>"},{"location":"reference/mlclient/models/Metadata/#mlclient.models.Metadata.to_xml","title":"to_xml","text":"<pre><code>to_xml() -&gt; ElemTree.ElementTree\n</code></pre> <p>Return an XML representation of the Metadata instance.</p> Source code in <code>mlclient/models/documents.py</code> <pre><code>def to_xml(\n    self,\n) -&gt; ElemTree.ElementTree:\n    \"\"\"Return an XML representation of the Metadata instance.\"\"\"\n    self._ensure_parsed()\n    attrs = {self._RAPI_NS_PREFIX: self._RAPI_NS_URI}\n    root = ElemTree.Element(self._METADATA_TAG, attrib=attrs)\n\n    self._to_xml_collections(root)\n    self._to_xml_permissions(root)\n    self._to_xml_properties(root)\n    self._to_xml_quality(root)\n    self._to_xml_metadata_values(root)\n    return ElemTree.ElementTree(root)\n</code></pre>"},{"location":"reference/mlclient/models/Metadata/#mlclient.models.Metadata.to_xml_string","title":"to_xml_string","text":"<pre><code>to_xml_string(indent: int | None = None) -&gt; str\n</code></pre> <p>Return a stringified XML representation of the Metadata instance.</p> <p>When the instance was created from a raw XML payload and no field has been read or modified yet, the original payload is returned without re-serialization.</p> <p>Parameters:</p> Name Type Description Default <code>indent</code> <code>int | None</code> <p>A number of spaces per indent level</p> <code>None</code> <p>Returns:</p> Type Description <code>str</code> <p>Metadata in a stringified XML representation</p> Source code in <code>mlclient/models/documents.py</code> <pre><code>def to_xml_string(\n    self,\n    indent: int | None = None,\n) -&gt; str:\n    \"\"\"Return a stringified XML representation of the Metadata instance.\n\n    When the instance was created from a raw XML payload and no field has\n    been read or modified yet, the original payload is returned without\n    re-serialization.\n\n    Parameters\n    ----------\n    indent : int | None\n        A number of spaces per indent level\n\n    Returns\n    -------\n    str\n        Metadata in a stringified XML representation\n    \"\"\"\n    if indent is None and self._raw is not None:\n        source = (\n            self._raw.decode(\"utf-8\") if isinstance(self._raw, bytes) else self._raw\n        )\n        if source.lstrip().startswith(\"&lt;\"):\n            return source\n    metadata_xml = self.to_xml().getroot()\n    if indent is None:\n        metadata_str = ElemTree.tostring(\n            metadata_xml,\n            encoding=\"utf-8\",\n            method=\"xml\",\n            xml_declaration=True,\n        )\n    else:\n        metadata_xml_string = ElemTree.tostring(metadata_xml)\n        metadata_xml_minidom = minidom.parseString(metadata_xml_string)\n        metadata_str = metadata_xml_minidom.toprettyxml(\n            indent=\" \" * indent,\n            encoding=\"utf-8\",\n        )\n    return _normalize_xml_declaration(metadata_str).decode(\"ascii\")\n</code></pre>"},{"location":"reference/mlclient/models/MetadataDocument/","title":"MetadataDocument","text":"<pre><code>from mlclient.models import MetadataDocument\n</code></pre>"},{"location":"reference/mlclient/models/MetadataDocument/#mlclient.models.MetadataDocument","title":"mlclient.models.MetadataDocument","text":"<pre><code>MetadataDocument(\n    uri: str,\n    metadata: Metadata | bytes | str,\n    temporal_collection: str | None = None,\n)\n</code></pre> <p>               Bases: <code>Document</code></p> <p>A Document implementation for metadata-only updates.</p> <p>Does not store document content. Use <code>Document.metadata_update()</code> to create instances rather than calling the constructor directly.</p> Source code in <code>mlclient/models/documents.py</code> <pre><code>def __init__(\n    self,\n    uri: str,\n    metadata: Metadata | bytes | str,\n    temporal_collection: str | None = None,\n):\n    super().__init__(\n        uri,\n        doc_type=None,\n        metadata=metadata,\n        temporal_collection=temporal_collection,\n    )\n</code></pre>"},{"location":"reference/mlclient/models/MetadataDocument/#mlclient.models.MetadataDocument.content","title":"content  <code>property</code>","text":"<pre><code>content: None\n</code></pre> <p>A document content.</p> <p>Returns:</p> Type Description <code>None</code> <p>Always None; metadata-only documents have no content.</p>"},{"location":"reference/mlclient/models/MetadataDocument/#mlclient.models.MetadataDocument.content_bytes","title":"content_bytes  <code>property</code>","text":"<pre><code>content_bytes: None\n</code></pre> <p>A document content as bytes.</p> <p>Returns:</p> Type Description <code>None</code> <p>Always None; metadata-only documents have no content.</p>"},{"location":"reference/mlclient/models/MetadataDocument/#mlclient.models.MetadataDocument.content_string","title":"content_string  <code>property</code>","text":"<pre><code>content_string: None\n</code></pre> <p>A document content as a string.</p> <p>Returns:</p> Type Description <code>None</code> <p>Always None; metadata-only documents have no content.</p>"},{"location":"reference/mlclient/models/Mimetype/","title":"Mimetype","text":"<pre><code>from mlclient.models import Mimetype\n</code></pre>"},{"location":"reference/mlclient/models/Mimetype/#mlclient.models.Mimetype","title":"mlclient.models.Mimetype  <code>pydantic-model</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>A class representing a mime type.</p> Show JSON schema: <pre><code>{\n  \"$defs\": {\n    \"DocumentType\": {\n      \"description\": \"An enumeration class representing document types.\",\n      \"enum\": [\n        \"xml\",\n        \"json\",\n        \"binary\",\n        \"text\"\n      ],\n      \"title\": \"DocumentType\",\n      \"type\": \"string\"\n    }\n  },\n  \"description\": \"A class representing a mime type.\",\n  \"properties\": {\n    \"mime-type\": {\n      \"title\": \"Mime-Type\",\n      \"type\": \"string\"\n    },\n    \"extensions\": {\n      \"items\": {\n        \"type\": \"string\"\n      },\n      \"title\": \"Extensions\",\n      \"type\": \"array\"\n    },\n    \"doc-type\": {\n      \"$ref\": \"#/$defs/DocumentType\"\n    }\n  },\n  \"required\": [\n    \"mime-type\",\n    \"extensions\",\n    \"doc-type\"\n  ],\n  \"title\": \"Mimetype\",\n  \"type\": \"object\"\n}\n</code></pre> <p>Fields:</p> <ul> <li> <code>mime_type</code>                 (<code>str</code>)             </li> <li> <code>extensions</code>                 (<code>list[str]</code>)             </li> <li> <code>document_type</code>                 (<code>DocumentType</code>)             </li> </ul>"},{"location":"reference/mlclient/models/Mimetypes/","title":"Mimetypes","text":"<pre><code>from mlclient.models import Mimetypes\n</code></pre>"},{"location":"reference/mlclient/models/Mimetypes/#mlclient.models.Mimetypes","title":"mlclient.models.Mimetypes","text":"<p>The Mimetypes utilities provider.</p> <p>This class is not meant to be instantiated. It provides a set of useful static methods related to mimetypes in MarkLogic.</p>"},{"location":"reference/mlclient/models/Mimetypes/#mlclient.models.Mimetypes.get_doc_type","title":"get_doc_type  <code>classmethod</code>","text":"<pre><code>get_doc_type(uri_or_mimetype: str) -&gt; DocumentType\n</code></pre> <p>Return a document type for a specific file or mime type.</p> <p>Parameters:</p> Name Type Description Default <code>uri_or_mimetype</code> <code>str</code> <p>A document uri or mimetype to fetch a document type for</p> required <p>Returns:</p> Type Description <code>DocumentType</code> <p>A document type for a specific file or mime type</p> Source code in <code>mlclient/models/mimetypes.py</code> <pre><code>@classmethod\ndef get_doc_type(\n    cls,\n    uri_or_mimetype: str,\n) -&gt; DocumentType:\n    \"\"\"Return a document type for a specific file or mime type.\n\n    Parameters\n    ----------\n    uri_or_mimetype : str\n        A document uri or mimetype to fetch a document type for\n\n    Returns\n    -------\n    DocumentType\n        A document type for a specific file or mime type\n    \"\"\"\n    cls._init_mimetypes()\n    for mimetype in cls._MIMETYPES:\n        if uri_or_mimetype.startswith(\n            mimetype.mime_type,\n        ) or uri_or_mimetype.endswith(cls._dotted_extensions(mimetype)):\n            return mimetype.document_type\n    return DocumentType.BINARY\n</code></pre>"},{"location":"reference/mlclient/models/Mimetypes/#mlclient.models.Mimetypes.get_mimetype","title":"get_mimetype  <code>classmethod</code>","text":"<pre><code>get_mimetype(uri: str) -&gt; str | None\n</code></pre> <p>Return a mimetype linked to a specific extension.</p> <p>Parameters:</p> Name Type Description Default <code>uri</code> <code>str</code> <p>A document uri, path or filename to find a mimetype for</p> required <p>Returns:</p> Type Description <code>str</code> <p>A mimetype linked to a specific extension</p> Source code in <code>mlclient/models/mimetypes.py</code> <pre><code>@classmethod\ndef get_mimetype(\n    cls,\n    uri: str,\n) -&gt; str | None:\n    \"\"\"Return a mimetype linked to a specific extension.\n\n    Parameters\n    ----------\n    uri : str\n        A document uri, path or filename to find a mimetype for\n\n    Returns\n    -------\n    str\n        A mimetype linked to a specific extension\n    \"\"\"\n    cls._init_mimetypes()\n    gen = (\n        mimetype.mime_type\n        for mimetype in cls._MIMETYPES\n        if uri.endswith(cls._dotted_extensions(mimetype))\n    )\n    return next(gen, None)\n</code></pre>"},{"location":"reference/mlclient/models/Mimetypes/#mlclient.models.Mimetypes.get_mimetypes","title":"get_mimetypes  <code>classmethod</code>","text":"<pre><code>get_mimetypes(doc_type: DocumentType) -&gt; tuple[str]\n</code></pre> <p>Return all known mimetypes linked to a specific document type.</p> <p>Parameters:</p> Name Type Description Default <code>doc_type</code> <code>DocumentType</code> <p>A document type to find mimetypes for</p> required <p>Returns:</p> Type Description <code>tuple[str]</code> <p>A tuple of mimetypes linked to a specific document type</p> Source code in <code>mlclient/models/mimetypes.py</code> <pre><code>@classmethod\ndef get_mimetypes(\n    cls,\n    doc_type: DocumentType,\n) -&gt; tuple[str]:\n    \"\"\"Return all known mimetypes linked to a specific document type.\n\n    Parameters\n    ----------\n    doc_type : DocumentType\n        A document type to find mimetypes for\n\n    Returns\n    -------\n    tuple[str]\n        A tuple of mimetypes linked to a specific document type\n    \"\"\"\n    cls._init_doc_type_mimetypes(doc_type)\n    return tuple(\n        mimetype.mime_type for mimetype in cls._DOC_TYPE_MIMETYPES[doc_type]\n    )\n</code></pre>"},{"location":"reference/mlclient/models/Permission/","title":"Permission","text":"<pre><code>from mlclient.models import Permission\n</code></pre>"},{"location":"reference/mlclient/models/Permission/#mlclient.models.Permission","title":"mlclient.models.Permission","text":"<pre><code>Permission(role_name: str, capabilities: set)\n</code></pre> <p>A class representing MarkLogic's document permission.</p> <p>Initialize a Permission instance.</p> <p>Parameters:</p> Name Type Description Default <code>role_name</code> <code>str</code> <p>A role name</p> required <code>capabilities</code> <code>set</code> <p>Capabilities set</p> required Source code in <code>mlclient/models/documents.py</code> <pre><code>def __init__(\n    self,\n    role_name: str,\n    capabilities: set,\n):\n    \"\"\"Initialize a Permission instance.\n\n    Parameters\n    ----------\n    role_name : str\n        A role name\n    capabilities : set\n        Capabilities set\n    \"\"\"\n    self._role_name = role_name\n    self._capabilities = {cap for cap in capabilities if cap in self._CAPABILITIES}\n</code></pre>"},{"location":"reference/mlclient/models/Permission/#mlclient.models.Permission.add_capability","title":"add_capability","text":"<pre><code>add_capability(capability: str) -&gt; bool\n</code></pre> <p>Assign a new capability to the role.</p> <p>Parameters:</p> Name Type Description Default <code>capability</code> <code>str</code> <p>a permission's capability</p> required <p>Returns:</p> Name Type Description <code>allow</code> <code>bool</code> <p>True if there's no such capability assigned to this role already, and it is a correct one. Otherwise, False.</p> Source code in <code>mlclient/models/documents.py</code> <pre><code>def add_capability(\n    self,\n    capability: str,\n) -&gt; bool:\n    \"\"\"Assign a new capability to the role.\n\n    Parameters\n    ----------\n    capability : str\n        a permission's capability\n\n    Returns\n    -------\n    allow : bool\n        True if there's no such capability assigned to this role already, and it is\n        a correct one. Otherwise, False.\n    \"\"\"\n    allow = (\n        capability is not None\n        and capability in self._CAPABILITIES\n        and capability not in self.capabilities()\n    )\n    if allow:\n        self._capabilities.add(capability)\n    return allow\n</code></pre>"},{"location":"reference/mlclient/models/Permission/#mlclient.models.Permission.capabilities","title":"capabilities","text":"<pre><code>capabilities() -&gt; set\n</code></pre> <p>Return permission's capabilities.</p> Source code in <code>mlclient/models/documents.py</code> <pre><code>def capabilities(\n    self,\n) -&gt; set:\n    \"\"\"Return permission's capabilities.\"\"\"\n    return self._capabilities.copy()\n</code></pre>"},{"location":"reference/mlclient/models/Permission/#mlclient.models.Permission.remove_capability","title":"remove_capability","text":"<pre><code>remove_capability(capability: str) -&gt; bool\n</code></pre> <p>Remove a capability from the role.</p> <p>Parameters:</p> Name Type Description Default <code>capability</code> <code>str</code> <p>a permission's capability</p> required <p>Returns:</p> Name Type Description <code>allow</code> <code>bool</code> <p>True if the capability is assigned to the role. Otherwise, False.</p> Source code in <code>mlclient/models/documents.py</code> <pre><code>def remove_capability(\n    self,\n    capability: str,\n) -&gt; bool:\n    \"\"\"Remove a capability from the role.\n\n    Parameters\n    ----------\n    capability : str\n        a permission's capability\n\n    Returns\n    -------\n    allow : bool\n        True if the capability is assigned to the role.\n        Otherwise, False.\n    \"\"\"\n    allow = capability is not None and capability in self.capabilities()\n    if allow:\n        self._capabilities.remove(capability)\n    return allow\n</code></pre>"},{"location":"reference/mlclient/models/Permission/#mlclient.models.Permission.role_name","title":"role_name","text":"<pre><code>role_name() -&gt; str\n</code></pre> <p>Return permission's role name.</p> Source code in <code>mlclient/models/documents.py</code> <pre><code>def role_name(\n    self,\n) -&gt; str:\n    \"\"\"Return permission's role name.\"\"\"\n    return self._role_name\n</code></pre>"},{"location":"reference/mlclient/models/Permission/#mlclient.models.Permission.to_json","title":"to_json","text":"<pre><code>to_json() -&gt; dict\n</code></pre> <p>Return a JSON representation of the Permission instance.</p> Source code in <code>mlclient/models/documents.py</code> <pre><code>def to_json(\n    self,\n) -&gt; dict:\n    \"\"\"Return a JSON representation of the Permission instance.\"\"\"\n    return {\n        \"role-name\": self.role_name(),\n        \"capabilities\": list(self.capabilities()),\n    }\n</code></pre>"},{"location":"reference/mlclient/models/Repair/","title":"Repair","text":"<pre><code>from mlclient.models import Repair\n</code></pre>"},{"location":"reference/mlclient/models/Repair/#mlclient.models.Repair","title":"mlclient.models.Repair","text":"<p>               Bases: <code>Enum</code></p> <p>An enumeration class representing repair levels.</p>"},{"location":"reference/mlclient/models/TextDocument/","title":"TextDocument","text":"<pre><code>from mlclient.models import TextDocument\n</code></pre>"},{"location":"reference/mlclient/models/TextDocument/#mlclient.models.TextDocument","title":"mlclient.models.TextDocument","text":"<pre><code>TextDocument(\n    content: str | bytes,\n    uri: str | None = None,\n    metadata: Metadata | bytes | str | None = None,\n    temporal_collection: str | None = None,\n)\n</code></pre> <p>               Bases: <code>Document</code></p> <p>A Document implementation representing a single MarkLogic TEXT document.</p> <p>This implementation stores content in a string format.</p> Source code in <code>mlclient/models/documents.py</code> <pre><code>def __init__(\n    self,\n    content: str | bytes,\n    uri: str | None = None,\n    metadata: Metadata | bytes | str | None = None,\n    temporal_collection: str | None = None,\n):\n    if content is None:\n        msg = (\n            \"TextDocument requires content; \"\n            \"use Document.metadata_update() for metadata-only updates\"\n        )\n        raise TypeError(msg)\n    super().__init__(uri, DocumentType.TEXT, metadata, temporal_collection)\n    self._content_string: str | None = content if isinstance(content, str) else None\n    self._content_bytes: bytes | None = (\n        content if isinstance(content, bytes) else None\n    )\n</code></pre>"},{"location":"reference/mlclient/models/TextDocument/#mlclient.models.TextDocument.content","title":"content  <code>property</code>","text":"<pre><code>content: str\n</code></pre> <p>A document content.</p> <p>Returns:</p> Type Description <code>str</code> <p>The text content.</p>"},{"location":"reference/mlclient/models/TextDocument/#mlclient.models.TextDocument.content_bytes","title":"content_bytes  <code>property</code>","text":"<pre><code>content_bytes: bytes\n</code></pre> <p>A document content as bytes.</p> <p>Returns:</p> Type Description <code>bytes</code> <p>The text content encoded as UTF-8.</p>"},{"location":"reference/mlclient/models/TextDocument/#mlclient.models.TextDocument.content_string","title":"content_string  <code>property</code>","text":"<pre><code>content_string: str\n</code></pre> <p>A document content as a string.</p> <p>Returns:</p> Type Description <code>str</code> <p>The text content.</p>"},{"location":"reference/mlclient/models/XMLDocument/","title":"XMLDocument","text":"<pre><code>from mlclient.models import XMLDocument\n</code></pre>"},{"location":"reference/mlclient/models/XMLDocument/#mlclient.models.XMLDocument","title":"mlclient.models.XMLDocument","text":"<pre><code>XMLDocument(\n    content: ElementTree | Element | str | bytes,\n    uri: str | None = None,\n    metadata: Metadata | bytes | str | None = None,\n    temporal_collection: str | None = None,\n)\n</code></pre> <p>               Bases: <code>Document</code></p> <p>A Document implementation representing a single MarkLogic XML document.</p> <p>Accepts <code>ElemTree.Element</code>, <code>str</code>, or <code>bytes</code> content. String and bytes are stored as-is; parsing to <code>ElementTree</code> is deferred to the first <code>.content</code> access.</p> Source code in <code>mlclient/models/documents.py</code> <pre><code>def __init__(\n    self,\n    content: ElemTree.ElementTree | ElemTree.Element | str | bytes,\n    uri: str | None = None,\n    metadata: Metadata | bytes | str | None = None,\n    temporal_collection: str | None = None,\n):\n    if content is None:\n        msg = (\n            \"XMLDocument requires content; \"\n            \"use Document.metadata_update() for metadata-only updates\"\n        )\n        raise TypeError(msg)\n    super().__init__(uri, DocumentType.XML, metadata, temporal_collection)\n    if isinstance(content, ElemTree.Element):\n        self._parsed: ElemTree.ElementTree | None = ElemTree.ElementTree(content)\n    elif isinstance(content, ElemTree.ElementTree):\n        self._parsed = content\n    else:\n        self._parsed = None\n    self._content_bytes: bytes | None = (\n        content if isinstance(content, bytes) else None\n    )\n    self._content_string: str | None = content if isinstance(content, str) else None\n</code></pre>"},{"location":"reference/mlclient/models/XMLDocument/#mlclient.models.XMLDocument.content","title":"content  <code>property</code>","text":"<pre><code>content: ElementTree\n</code></pre> <p>A parsed ElementTree representation of the XML content.</p> <p>Parsing from the raw string/bytes form is deferred to the first access and cached for subsequent calls.</p> <p>Returns:</p> Type Description <code>ElementTree</code> <p>The parsed XML content as an ElementTree.</p>"},{"location":"reference/mlclient/models/XMLDocument/#mlclient.models.XMLDocument.content_bytes","title":"content_bytes  <code>property</code>","text":"<pre><code>content_bytes: bytes\n</code></pre> <p>A UTF-8 encoded representation of the XML content with declaration.</p> <p>Computed once on first access and cached for subsequent calls.</p> <p>Returns:</p> Type Description <code>bytes</code> <p>The content encoded as UTF-8 bytes.</p>"},{"location":"reference/mlclient/models/XMLDocument/#mlclient.models.XMLDocument.content_string","title":"content_string  <code>property</code>","text":"<pre><code>content_string: str\n</code></pre> <p>A string representation of the XML content.</p> <p>Computed once on first access and cached for subsequent calls.</p> <p>Returns:</p> Type Description <code>str</code> <p>The content as a string.</p>"},{"location":"reference/mlclient/models/XMLDocument/#mlclient.models.XMLDocument.invalidate","title":"invalidate","text":"<pre><code>invalidate() -&gt; XMLDocument\n</code></pre> <p>Drop cached bytes/string serializations of the parsed ElementTree.</p> <p>Use this when the parsed <code>.content</code> has been mutated externally so that subsequent <code>.content_bytes</code> / <code>.content_string</code> calls re-serialize from the parsed form instead of returning stale cached output. Has no effect when the document has not been parsed yet.</p> <p>Returns:</p> Type Description <code>XMLDocument</code> <p><code>self</code>, so the call can be chained directly into a write.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; doc.content.find(\"name\").text = \"Smith\"\n&gt;&gt;&gt; ml.documents.write(doc.invalidate())\n</code></pre> Source code in <code>mlclient/models/documents.py</code> <pre><code>def invalidate(self) -&gt; XMLDocument:\n    \"\"\"Drop cached bytes/string serializations of the parsed ElementTree.\n\n    Use this when the parsed ``.content`` has been mutated externally so\n    that subsequent ``.content_bytes`` / ``.content_string`` calls\n    re-serialize from the parsed form instead of returning stale cached\n    output. Has no effect when the document has not been parsed yet.\n\n    Returns\n    -------\n    XMLDocument\n        ``self``, so the call can be chained directly into a write.\n\n    Examples\n    --------\n    &gt;&gt;&gt; doc.content.find(\"name\").text = \"Smith\"\n    &gt;&gt;&gt; ml.documents.write(doc.invalidate())\n    \"\"\"\n    if self._parsed is not None:\n        self._content_bytes = None\n        self._content_string = None\n    return self\n</code></pre>"},{"location":"reference/mlclient/models/XMLDocument/#mlclient.models.XMLDocument.xpath","title":"xpath","text":"<pre><code>xpath(expr: str, **namespaces: str) -&gt; list\n</code></pre> <p>Run an XPath expression against the document content.</p> <p>Parameters:</p> Name Type Description Default <code>expr</code> <code>str</code> <p>XPath expression.</p> required <code>**namespaces</code> <code>str</code> <p>Namespace prefix-to-URI mappings used in <code>expr</code>.</p> <code>{}</code> <p>Returns:</p> Type Description <code>list</code> <p>Matching elements.</p> Source code in <code>mlclient/models/documents.py</code> <pre><code>def xpath(self, expr: str, **namespaces: str) -&gt; list:\n    \"\"\"Run an XPath expression against the document content.\n\n    Parameters\n    ----------\n    expr : str\n        XPath expression.\n    **namespaces : str\n        Namespace prefix-to-URI mappings used in ``expr``.\n\n    Returns\n    -------\n    list\n        Matching elements.\n    \"\"\"\n    return self.content.findall(expr, namespaces or None)\n</code></pre>"},{"location":"reference/mlclient/multipart/","title":"<code>mlclient.multipart</code>","text":""},{"location":"reference/mlclient/multipart/#mlclient.multipart","title":"mlclient.multipart","text":"<p>The Multipart/Mixed module.</p> <p>It provides functions and a data class for building and parsing multipart/mixed HTTP messages:     * MultipartPart         A single part of a multipart/mixed message.     * encode_multipart_mixed         Encode parts into a multipart/mixed body.     * decode_multipart_mixed         Parse a multipart/mixed body into parts.</p>"},{"location":"reference/mlclient/multipart/MultipartPart/","title":"MultipartPart","text":"<pre><code>from mlclient.multipart import MultipartPart\n</code></pre>"},{"location":"reference/mlclient/multipart/MultipartPart/#mlclient.multipart.MultipartPart","title":"mlclient.multipart.MultipartPart  <code>dataclass</code>","text":"<pre><code>MultipartPart(headers: dict[str, str], content: bytes)\n</code></pre> <p>A single part of a multipart/mixed message.</p>"},{"location":"reference/mlclient/multipart/MultipartPart/#mlclient.multipart.MultipartPart.text","title":"text  <code>property</code>","text":"<pre><code>text: str\n</code></pre> <p>Decode content to string using the charset from Content-Type.</p>"},{"location":"reference/mlclient/multipart/decode_multipart_mixed/","title":"decode_multipart_mixed","text":"<pre><code>from mlclient.multipart import decode_multipart_mixed\n</code></pre>"},{"location":"reference/mlclient/multipart/decode_multipart_mixed/#mlclient.multipart.decode_multipart_mixed","title":"mlclient.multipart.decode_multipart_mixed","text":"<pre><code>decode_multipart_mixed(\n    content: bytes, content_type: str\n) -&gt; list[MultipartPart]\n</code></pre> <p>Parse a multipart/mixed body into parts.</p> <p>Parameters:</p> Name Type Description Default <code>content</code> <code>bytes</code> <p>The raw multipart body</p> required <code>content_type</code> <code>str</code> <p>The Content-Type header value containing the boundary</p> required <p>Returns:</p> Type Description <code>list[MultipartPart]</code> <p>The parsed parts</p> Source code in <code>mlclient/multipart.py</code> <pre><code>def decode_multipart_mixed(\n    content: bytes,\n    content_type: str,\n) -&gt; list[MultipartPart]:\n    \"\"\"Parse a multipart/mixed body into parts.\n\n    Parameters\n    ----------\n    content : bytes\n        The raw multipart body\n    content_type : str\n        The Content-Type header value containing the boundary\n\n    Returns\n    -------\n    list[MultipartPart]\n        The parsed parts\n    \"\"\"\n    boundary = _extract_boundary(content_type)\n    delimiter = f\"\\r\\n--{boundary}\".encode()\n    pieces = content.split(delimiter)\n    first = f\"--{boundary}\\r\\n\".encode()\n    if pieces[0].startswith(first):\n        pieces[0] = pieces[0][len(first) :]\n    else:\n        pieces.pop(0)\n    # Last piece is \"--\\r\\n\" (closing marker)\n    pieces.pop()\n    parts = []\n    for raw in pieces:\n        chunk = raw[2:] if raw.startswith(b\"\\r\\n\") else raw\n        header_block, _, body = chunk.partition(b\"\\r\\n\\r\\n\")\n        headers = {}\n        for line in header_block.split(b\"\\r\\n\"):\n            name, _, value = line.partition(b\": \")\n            headers[name.decode()] = value.decode()\n        parts.append(MultipartPart(headers=headers, content=body))\n    return parts\n</code></pre>"},{"location":"reference/mlclient/multipart/encode_multipart_mixed/","title":"encode_multipart_mixed","text":"<pre><code>from mlclient.multipart import encode_multipart_mixed\n</code></pre>"},{"location":"reference/mlclient/multipart/encode_multipart_mixed/#mlclient.multipart.encode_multipart_mixed","title":"mlclient.multipart.encode_multipart_mixed","text":"<pre><code>encode_multipart_mixed(\n    parts: list[MultipartPart], boundary: str | None = None\n) -&gt; tuple[bytes, str]\n</code></pre> <p>Encode parts into a multipart/mixed body.</p> <p>Parameters:</p> Name Type Description Default <code>parts</code> <code>list[MultipartPart]</code> <p>The parts to encode</p> required <code>boundary</code> <code>str | None</code> <p>An optional boundary string; generated if not provided</p> <code>None</code> <p>Returns:</p> Type Description <code>tuple[bytes, str]</code> <p>A tuple of (body_bytes, content_type_header)</p> Source code in <code>mlclient/multipart.py</code> <pre><code>def encode_multipart_mixed(\n    parts: list[MultipartPart],\n    boundary: str | None = None,\n) -&gt; tuple[bytes, str]:\n    \"\"\"Encode parts into a multipart/mixed body.\n\n    Parameters\n    ----------\n    parts : list[MultipartPart]\n        The parts to encode\n    boundary : str | None\n        An optional boundary string; generated if not provided\n\n    Returns\n    -------\n    tuple[bytes, str]\n        A tuple of (body_bytes, content_type_header)\n    \"\"\"\n    if boundary is None:\n        boundary = uuid.uuid4().hex\n    segments = []\n    for part in parts:\n        segment = f\"--{boundary}\\r\\n\".encode()\n        for name, value in part.headers.items():\n            segment += f\"{name}: {value}\\r\\n\".encode()\n        segment += b\"\\r\\n\"\n        segment += part.content\n        segment += b\"\\r\\n\"\n        segments.append(segment)\n    body = b\"\".join(segments) + f\"--{boundary}--\\r\\n\".encode()\n    content_type = f\"multipart/mixed; boundary={boundary}\"\n    return body, content_type\n</code></pre>"},{"location":"reference/mlclient/responses/","title":"<code>mlclient.responses</code>","text":""},{"location":"reference/mlclient/responses/#mlclient.responses","title":"mlclient.responses","text":"<p>The ML Response Parser module.</p> <p>It exports 1 class:     * MLResponseParser         A MarkLogic HTTP response parser.</p>"},{"location":"reference/mlclient/responses/MLResponseParser/","title":"MLResponseParser","text":"<pre><code>from mlclient.responses import MLResponseParser\n</code></pre>"},{"location":"reference/mlclient/responses/MLResponseParser/#mlclient.responses.MLResponseParser","title":"mlclient.responses.MLResponseParser","text":"<p>A MarkLogic HTTP response parser.</p> <p>MarkLogic returns responses with multipart/mixed content. This class allows to get all returned parts as python representations. They are parsed depending on content type of corresponding part.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from mlclient import MLClient, MLResponseParser\n&gt;&gt;&gt; config = {\n...     \"host\": \"localhost\",\n...     \"port\": 8002,\n...     \"username\": \"admin\",\n...     \"password\": \"admin\",\n...     \"auth\": \"digest\",\n... }\n&gt;&gt;&gt; with MLClient(**config) as ml:\n...     resp = ml.rest.eval.post(xquery=\"xdmp:database() =&gt; xdmp:database-name()\")\n...     print(\"Raw:\", resp.text)\n...     print(\"Parsed:\", MLResponseParser.parse(resp))\n...\nRaw:\n--6a5df7d535c71968\nContent-Type: text/plain\nX-Primitive: string\nApp-Services\n--6a5df7d535c71968--\nParsed: App-Services\n</code></pre>"},{"location":"reference/mlclient/responses/MLResponseParser/#mlclient.responses.MLResponseParser.parse","title":"parse  <code>classmethod</code>","text":"<pre><code>parse(\n    response: Response, output_type: type | None = None\n) -&gt; (\n    bytes\n    | str\n    | int\n    | float\n    | bool\n    | dict\n    | ElemTree.ElementTree\n    | ElemTree.Element\n    | list\n)\n</code></pre> <p>Parse MarkLogic HTTP Response.</p> <p>Parameters:</p> Name Type Description Default <code>response</code> <code>Response</code> <p>An HTTP response taken from MarkLogic instance</p> required <code>output_type</code> <code>type | None</code> <p>A raw output type (supported: str, bytes)</p> <code>None</code> <p>Returns:</p> Type Description <code>bytes | str | int | float | bool | dict |</code> <code>ElemTree.ElementTree | ElemTree.Element |</code> <code>list</code> <p>A parsed response body</p> Source code in <code>mlclient/responses.py</code> <pre><code>@classmethod\ndef parse(\n    cls,\n    response: Response,\n    output_type: type | None = None,\n) -&gt; (\n    bytes\n    | str\n    | int\n    | float\n    | bool\n    | dict\n    | ElemTree.ElementTree\n    | ElemTree.Element\n    | list\n):\n    \"\"\"Parse MarkLogic HTTP Response.\n\n    Parameters\n    ----------\n    response : Response\n        An HTTP response taken from MarkLogic instance\n    output_type : type | None , default None\n        A raw output type (supported: str, bytes)\n\n    Returns\n    -------\n    bytes | str | int | float | bool | dict |\n    ElemTree.ElementTree | ElemTree.Element |\n    list\n        A parsed response body\n    \"\"\"\n    logger.debug(\"Attempt to parse a response\")\n    if response.is_success and int(response.headers.get(\"Content-Length\", -1)) == 0:\n        logger.fine(\"No content to parse\")\n        return []\n\n    if output_type is str:\n        return cls._parse_text(response)\n    if output_type is bytes:\n        return cls._parse_bytes(response)\n\n    return cls._parse(response)\n</code></pre>"},{"location":"reference/mlclient/responses/MLResponseParser/#mlclient.responses.MLResponseParser.parse_with_headers","title":"parse_with_headers  <code>classmethod</code>","text":"<pre><code>parse_with_headers(\n    response: Response, output_type: type | None = None\n) -&gt; tuple | list[tuple]\n</code></pre> <p>Parse MarkLogic HTTP Response and get headers.</p> <p>Parameters:</p> Name Type Description Default <code>response</code> <code>Response</code> <p>An HTTP response taken from MarkLogic instance</p> required <code>output_type</code> <code>type | None</code> <p>A raw output type (supported: str, bytes)</p> <code>None</code> <p>Returns:</p> Type Description <code>tuple</code> <p>A parsed response body with headers</p> Source code in <code>mlclient/responses.py</code> <pre><code>@classmethod\ndef parse_with_headers(\n    cls,\n    response: Response,\n    output_type: type | None = None,\n) -&gt; tuple | list[tuple]:\n    \"\"\"Parse MarkLogic HTTP Response and get headers.\n\n    Parameters\n    ----------\n    response : Response\n        An HTTP response taken from MarkLogic instance\n    output_type : type | None , default None\n        A raw output type (supported: str, bytes)\n\n    Returns\n    -------\n    tuple\n        A parsed response body with headers\n    \"\"\"\n    logger.debug(\"Attempt to parse a response\")\n    if response.is_success and int(response.headers.get(\"Content-Length\", -1)) == 0:\n        logger.fine(\"No content to parse\")\n        return response.headers, []\n\n    if output_type is str:\n        return cls._parse_text(response, with_headers=True)\n    if output_type is bytes:\n        return cls._parse_bytes(response, with_headers=True)\n\n    return cls._parse(response, with_headers=True)\n</code></pre>"},{"location":"reference/mlclient/responses/MLResponseParser/#mlclient.responses.MLResponseParser.raise_for_status","title":"raise_for_status  <code>classmethod</code>","text":"<pre><code>raise_for_status(response: Response) -&gt; None\n</code></pre> <p>Raise a MarkLogic error when a response reports failure.</p> <p>A success response returns without parsing the body. A non-success response is parsed: when MarkLogic described the error a MarkLogicError is raised, otherwise the call defers to httpx so a bodyless gateway failure still surfaces as an HTTPStatusError.</p> <p>Parameters:</p> Name Type Description Default <code>response</code> <code>Response</code> <p>An HTTP response taken from MarkLogic instance</p> required <p>Raises:</p> Type Description <code>MarkLogicError</code> <p>If MarkLogic answered a non-success status with an error it described</p> <code>HTTPStatusError</code> <p>If a non-success status carried no MarkLogic error body</p> Source code in <code>mlclient/responses.py</code> <pre><code>@classmethod\ndef raise_for_status(\n    cls,\n    response: Response,\n) -&gt; None:\n    \"\"\"Raise a MarkLogic error when a response reports failure.\n\n    A success response returns without parsing the body. A non-success\n    response is parsed: when MarkLogic described the error a MarkLogicError\n    is raised, otherwise the call defers to httpx so a bodyless gateway\n    failure still surfaces as an HTTPStatusError.\n\n    Parameters\n    ----------\n    response : Response\n        An HTTP response taken from MarkLogic instance\n\n    Raises\n    ------\n    MarkLogicError\n        If MarkLogic answered a non-success status with an error it described\n    HTTPStatusError\n        If a non-success status carried no MarkLogic error body\n    \"\"\"\n    if response.is_success:\n        return\n    error = cls._parse(response)\n    if error:\n        raise MarkLogicError(error)\n    response.raise_for_status()\n</code></pre>"},{"location":"reference/mlclient/services/","title":"<code>mlclient.services</code>","text":""},{"location":"reference/mlclient/services/#mlclient.services","title":"mlclient.services","text":"<p>Public services for MLClient.</p>"},{"location":"reference/mlclient/services/AsyncDocumentsService/","title":"AsyncDocumentsService","text":"<pre><code>from mlclient.services import AsyncDocumentsService\n</code></pre>"},{"location":"reference/mlclient/services/AsyncDocumentsService/#mlclient.services.AsyncDocumentsService","title":"mlclient.services.AsyncDocumentsService","text":"<pre><code>AsyncDocumentsService(rest: AsyncRestApi)\n</code></pre> <p>Async higher-level service for /v1/documents CRUD operations.</p> Source code in <code>mlclient/services/documents.py</code> <pre><code>def __init__(self, rest: AsyncRestApi):\n    self._rest = rest\n</code></pre>"},{"location":"reference/mlclient/services/AsyncDocumentsService/#mlclient.services.AsyncDocumentsService.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(\n    uris: str | list[str] | tuple[str] | set[str],\n    *,\n    category: Category\n    | str\n    | list[Category | str]\n    | None = None,\n    database: str | None = None,\n    temporal_collection: str | None = None,\n    wipe_temporal: bool | None = None,\n    txid: str | None = None,\n    timeout=UNSET,\n)\n</code></pre> <p>Delete document(s) content or metadata in a MarkLogic database.</p> <p>URIs are transparently split into batches whose combined query string stays below the httpx URL length limit; each batch is a separate HTTP request.</p> <p>Parameters:</p> Name Type Description Default <code>uris</code> <code>str | list[str] | tuple[str] | set[str]</code> <p>The URI of a document to delete.</p> required <code>category</code> <code>Category | str | list[Category | str] | None</code> <p>The category of data to remove/reset.</p> <code>None</code> <code>database</code> <code>str | None</code> <p>Perform this operation on the named content database.</p> <code>None</code> <code>temporal_collection</code> <code>str | None</code> <p>Temporal collection name.</p> <code>None</code> <code>wipe_temporal</code> <code>bool | None</code> <p>Remove all versions of a temporal document.</p> <code>None</code> <code>txid</code> <code>str | None</code> <p>Perform this operation within the named multi-statement transaction.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. When the operation issues several requests it applies to each of them independently, not as a shared budget, and is not persisted.</p> <code>unset</code> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> <code>MarkLogicError</code> <p>If MarkLogic returns an error</p> Source code in <code>mlclient/services/documents.py</code> <pre><code>async def delete(\n    self,\n    uris: str | list[str] | tuple[str] | set[str],\n    *,\n    category: Category | str | list[Category | str] | None = None,\n    database: str | None = None,\n    temporal_collection: str | None = None,\n    wipe_temporal: bool | None = None,\n    txid: str | None = None,\n    timeout=UNSET,\n):\n    \"\"\"Delete document(s) content or metadata in a MarkLogic database.\n\n    URIs are transparently split into batches whose combined query string\n    stays below the httpx URL length limit; each batch is a separate\n    HTTP request.\n\n    Parameters\n    ----------\n    uris : str | list[str] | tuple[str] | set[str]\n        The URI of a document to delete.\n    category : Category | str | list[Category | str] | None, default None\n        The category of data to remove/reset.\n    database : str | None, default None\n        Perform this operation on the named content database.\n    temporal_collection : str | None, default None\n        Temporal collection name.\n    wipe_temporal : bool | None, default None\n        Remove all versions of a temporal document.\n    txid : str | None, default None\n        Perform this operation within the named multi-statement transaction.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout. Unset uses the client's configured\n        timeout; None disables every HTTP timeout; a number sets all four\n        components to that many seconds; an httpx.Timeout overrides them.\n        When the operation issues several requests it applies to each of\n        them independently, not as a shared budget, and is not persisted.\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    MarkLogicError\n        If MarkLogic returns an error\n    \"\"\"\n    category = _normalize_category(category)\n    for batch in _batched_uris(uris):\n        resp = await self._rest.documents.delete(\n            batch,\n            category=category,\n            database=database,\n            temporal_collection=temporal_collection,\n            wipe_temporal=wipe_temporal,\n            txid=txid,\n            timeout=timeout,\n        )\n        MLResponseParser.raise_for_status(resp)\n</code></pre>"},{"location":"reference/mlclient/services/AsyncDocumentsService/#mlclient.services.AsyncDocumentsService.read","title":"read  <code>async</code>","text":"<pre><code>read(\n    uris: str | list[str] | tuple[str] | set[str],\n    *,\n    category: Category\n    | str\n    | list[Category | str]\n    | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    timeout=UNSET,\n) -&gt; Document | dict[str, Document]\n</code></pre> <p>Return document(s) content or metadata from a MarkLogic database.</p> <p>When uris is a string it returns a single Document instance. Otherwise, result is a dict mapping URI to Document.</p> <p>Parameters:</p> Name Type Description Default <code>uris</code> <code>str | list[str] | tuple[str] | set[str]</code> <p>One or more URIs for documents in the database.</p> required <code>category</code> <code>Category | str | list[Category | str] | None</code> <p>The category of data to fetch about the requested document.</p> <code>None</code> <code>database</code> <code>str | None</code> <p>Perform this operation on the named content database.</p> <code>None</code> <code>txid</code> <code>str | None</code> <p>Perform this operation within the named multi-statement transaction.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. When the operation issues several requests it applies to each of them independently, not as a shared budget, and is not persisted.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Document | dict[str, Document]</code> <p>A single document when uris is a string, otherwise a dict keyed by URI.</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> <code>MarkLogicError</code> <p>If MarkLogic returns an error</p> Source code in <code>mlclient/services/documents.py</code> <pre><code>async def read(\n    self,\n    uris: str | list[str] | tuple[str] | set[str],\n    *,\n    category: Category | str | list[Category | str] | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    timeout=UNSET,\n) -&gt; Document | dict[str, Document]:\n    \"\"\"Return document(s) content or metadata from a MarkLogic database.\n\n    When uris is a string it returns a single Document instance. Otherwise,\n    result is a dict mapping URI to Document.\n\n    Parameters\n    ----------\n    uris : str | list[str] | tuple[str] | set[str]\n        One or more URIs for documents in the database.\n    category : Category | str | list[Category | str] | None, default None\n        The category of data to fetch about the requested document.\n    database : str | None, default None\n        Perform this operation on the named content database.\n    txid : str | None, default None\n        Perform this operation within the named multi-statement transaction.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout. Unset uses the client's configured\n        timeout; None disables every HTTP timeout; a number sets all four\n        components to that many seconds; an httpx.Timeout overrides them.\n        When the operation issues several requests it applies to each of\n        them independently, not as a shared budget, and is not persisted.\n\n    Returns\n    -------\n    Document | dict[str, Document]\n        A single document when uris is a string, otherwise a dict keyed by URI.\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    MarkLogicError\n        If MarkLogic returns an error\n    \"\"\"\n    stream = self.read_stream(\n        uris,\n        category=category,\n        database=database,\n        txid=txid,\n        timeout=timeout,\n    )\n    if isinstance(uris, str):\n        return await stream.__anext__()\n    return {doc.uri: doc async for doc in stream}\n</code></pre>"},{"location":"reference/mlclient/services/AsyncDocumentsService/#mlclient.services.AsyncDocumentsService.read_stream","title":"read_stream  <code>async</code>","text":"<pre><code>read_stream(\n    uris: str | list[str] | tuple[str] | set[str],\n    *,\n    category: Category\n    | str\n    | list[Category | str]\n    | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    timeout=UNSET,\n) -&gt; AsyncIterator[Document]\n</code></pre> <p>Return document(s) as an iterator, suitable for batch processing.</p> <p>Unlike read(), does not materialize results into a dict. URIs are transparently split into batches whose combined query string stays below the httpx URL length limit; each batch is a separate HTTP request.</p> <p>Parameters:</p> Name Type Description Default <code>uris</code> <code>str | list[str] | tuple[str] | set[str]</code> <p>One or more URIs for documents in the database.</p> required <code>category</code> <code>Category | str | list[Category | str] | None</code> <p>The category of data to fetch about the requested document.</p> <code>None</code> <code>database</code> <code>str | None</code> <p>Perform this operation on the named content database.</p> <code>None</code> <code>txid</code> <code>str | None</code> <p>Perform this operation within the named multi-statement transaction.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. When the operation issues several requests it applies to each of them independently, not as a shared budget, and is not persisted.</p> <code>unset</code> <p>Returns:</p> Type Description <code>AsyncIterator[Document]</code> <p>Documents from the database.</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> <code>MarkLogicError</code> <p>If MarkLogic returns an error</p> Source code in <code>mlclient/services/documents.py</code> <pre><code>async def read_stream(\n    self,\n    uris: str | list[str] | tuple[str] | set[str],\n    *,\n    category: Category | str | list[Category | str] | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    timeout=UNSET,\n) -&gt; AsyncIterator[Document]:\n    \"\"\"Return document(s) as an iterator, suitable for batch processing.\n\n    Unlike read(), does not materialize results into a dict. URIs are\n    transparently split into batches whose combined query string stays below\n    the httpx URL length limit; each batch is a separate HTTP request.\n\n    Parameters\n    ----------\n    uris : str | list[str] | tuple[str] | set[str]\n        One or more URIs for documents in the database.\n    category : Category | str | list[Category | str] | None, default None\n        The category of data to fetch about the requested document.\n    database : str | None, default None\n        Perform this operation on the named content database.\n    txid : str | None, default None\n        Perform this operation within the named multi-statement transaction.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout. Unset uses the client's configured\n        timeout; None disables every HTTP timeout; a number sets all four\n        components to that many seconds; an httpx.Timeout overrides them.\n        When the operation issues several requests it applies to each of\n        them independently, not as a shared budget, and is not persisted.\n\n    Returns\n    -------\n    AsyncIterator[Document]\n        Documents from the database.\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    MarkLogicError\n        If MarkLogic returns an error\n    \"\"\"\n    category = _normalize_category(category)\n    for batch in _batched_uris(uris):\n        resp = await self._rest.documents.get(\n            batch,\n            category=category,\n            database=database,\n            data_format=\"json\",\n            txid=txid,\n            timeout=timeout,\n        )\n        MLResponseParser.raise_for_status(resp)\n        for doc in _DocumentsReader.parse(resp, batch, category):\n            yield doc\n</code></pre>"},{"location":"reference/mlclient/services/AsyncDocumentsService/#mlclient.services.AsyncDocumentsService.write","title":"write  <code>async</code>","text":"<pre><code>write(\n    data: Document | Metadata | list[Document | Metadata],\n    *,\n    database: str | None = None,\n    temporal_collection: str | None = None,\n    txid: str | None = None,\n    timeout=UNSET,\n) -&gt; dict\n</code></pre> <p>Write (create or update) document(s) content or metadata.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>Document | Metadata | list[Document | Metadata]</code> <p>One or more document or default metadata.</p> required <code>database</code> <code>str | None</code> <p>Perform this operation on the named content database.</p> <code>None</code> <code>temporal_collection</code> <code>str | None</code> <p>Temporal collection name.</p> <code>None</code> <code>txid</code> <code>str | None</code> <p>Perform this operation within the named multi-statement transaction.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. When the operation issues several requests it applies to each of them independently, not as a shared budget, and is not persisted.</p> <code>unset</code> <p>Returns:</p> Type Description <code>dict</code> <p>An origin response from a MarkLogic server.</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> <code>MarkLogicError</code> <p>If MarkLogic returns an error</p> Source code in <code>mlclient/services/documents.py</code> <pre><code>async def write(\n    self,\n    data: Document | Metadata | list[Document | Metadata],\n    *,\n    database: str | None = None,\n    temporal_collection: str | None = None,\n    txid: str | None = None,\n    timeout=UNSET,\n) -&gt; dict:\n    \"\"\"Write (create or update) document(s) content or metadata.\n\n    Parameters\n    ----------\n    data : Document | Metadata | list[Document | Metadata]\n        One or more document or default metadata.\n    database : str | None, default None\n        Perform this operation on the named content database.\n    temporal_collection : str | None, default None\n        Temporal collection name.\n    txid : str | None, default None\n        Perform this operation within the named multi-statement transaction.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout. Unset uses the client's configured\n        timeout; None disables every HTTP timeout; a number sets all four\n        components to that many seconds; an httpx.Timeout overrides them.\n        When the operation issues several requests it applies to each of\n        them independently, not as a shared budget, and is not persisted.\n\n    Returns\n    -------\n    dict\n        An origin response from a MarkLogic server.\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    MarkLogicError\n        If MarkLogic returns an error\n    \"\"\"\n    body_parts = _DocumentsSender.parse(data)\n    resp = await self._rest.documents.post(\n        body_parts,\n        database=database,\n        temporal_collection=temporal_collection,\n        txid=txid,\n        timeout=timeout,\n    )\n    MLResponseParser.raise_for_status(resp)\n    return MLResponseParser.parse(resp)\n</code></pre>"},{"location":"reference/mlclient/services/AsyncEvalService/","title":"AsyncEvalService","text":"<pre><code>from mlclient.services import AsyncEvalService\n</code></pre>"},{"location":"reference/mlclient/services/AsyncEvalService/#mlclient.services.AsyncEvalService","title":"mlclient.services.AsyncEvalService","text":"<pre><code>AsyncEvalService(rest: AsyncRestApi)\n</code></pre> <p>Async higher-level service for /v1/eval endpoint.</p> Source code in <code>mlclient/services/eval.py</code> <pre><code>def __init__(self, rest: AsyncRestApi):\n    self._rest = rest\n</code></pre>"},{"location":"reference/mlclient/services/AsyncEvalService/#mlclient.services.AsyncEvalService.execute","title":"execute  <code>async</code>","text":"<pre><code>execute(\n    *,\n    file: str | None = None,\n    xq: str | None = None,\n    js: str | None = None,\n    variables: dict | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    output_type: type | None = None,\n    timeout=UNSET,\n    **kwargs,\n)\n</code></pre> <p>Evaluate code in a MarkLogic server (general-purpose).</p> <p>Dynamically resolves the code type from the provided parameters. For explicit, typed calls prefer xquery(), javascript(), or file().</p> <p>Parameters:</p> Name Type Description Default <code>file</code> <code>str | None</code> <p>A file path of a code to evaluate</p> <code>None</code> <code>xq</code> <code>str | None</code> <p>A raw XQuery code to evaluate</p> <code>None</code> <code>js</code> <code>str | None</code> <p>A raw JavaScript code to evaluate</p> <code>None</code> <code>variables</code> <code>dict | None</code> <p>External variables to pass to the query during evaluation</p> <code>None</code> <code>database</code> <code>str | None</code> <p>Content database name or id</p> <code>None</code> <code>txid</code> <code>str | None</code> <p>Transaction identifier</p> <code>None</code> <code>output_type</code> <code>type | None</code> <p>A raw output type (supported: str, bytes)</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout for this evaluation. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It bounds the HTTP request only and is never sent as a query variable - to pass a query variable literally named \"timeout\" use variables={\"timeout\": ...}.</p> <code>unset</code> <code>kwargs</code> <code>dict</code> <p>Key value arguments used as variables</p> <code>{}</code> <p>Returns:</p> Type Description <code>Parsed evaluation result</code> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> <code>MarkLogicError</code> <p>If MarkLogic returns an error</p> Source code in <code>mlclient/services/eval.py</code> <pre><code>async def execute(\n    self,\n    *,\n    file: str | None = None,\n    xq: str | None = None,\n    js: str | None = None,\n    variables: dict | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    output_type: type | None = None,\n    timeout=UNSET,\n    **kwargs,\n):\n    \"\"\"Evaluate code in a MarkLogic server (general-purpose).\n\n    Dynamically resolves the code type from the provided parameters.\n    For explicit, typed calls prefer xquery(), javascript(), or file().\n\n    Parameters\n    ----------\n    file : str | None, default None\n        A file path of a code to evaluate\n    xq : str | None, default None\n        A raw XQuery code to evaluate\n    js : str | None, default None\n        A raw JavaScript code to evaluate\n    variables : dict | None, default None\n        External variables to pass to the query during evaluation\n    database : str | None, default None\n        Content database name or id\n    txid : str | None, default None\n        Transaction identifier\n    output_type : type | None, default None\n        A raw output type (supported: str, bytes)\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout for this evaluation. Unset uses the\n        client's configured timeout; None disables every HTTP timeout; a\n        number sets all four components to that many seconds; an\n        httpx.Timeout overrides them. It bounds the HTTP request only and is\n        never sent as a query variable - to pass a query variable literally\n        named \"timeout\" use variables={\"timeout\": ...}.\n    kwargs : dict\n        Key value arguments used as variables\n\n    Returns\n    -------\n    Parsed evaluation result\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    MarkLogicError\n        If MarkLogic returns an error\n    \"\"\"\n    return await self._eval(\n        file=file,\n        xq=xq,\n        js=js,\n        variables=variables,\n        database=database,\n        txid=txid,\n        output_type=output_type,\n        timeout=timeout,\n        **kwargs,\n    )\n</code></pre>"},{"location":"reference/mlclient/services/AsyncEvalService/#mlclient.services.AsyncEvalService.file","title":"file  <code>async</code>","text":"<pre><code>file(\n    path: str,\n    *,\n    variables: dict | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    output_type: type | None = None,\n    timeout=UNSET,\n    **kwargs,\n) -&gt; (\n    bytes\n    | str\n    | int\n    | float\n    | bool\n    | dict\n    | ElemTree.ElementTree\n    | ElemTree.Element\n    | list\n)\n</code></pre> <p>Evaluate code from a file in MarkLogic (auto-detect language).</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>File path to the code to evaluate</p> required <code>variables</code> <code>dict | None</code> <p>External variables</p> <code>None</code> <code>database</code> <code>str | None</code> <p>Content database name or id</p> <code>None</code> <code>txid</code> <code>str | None</code> <p>Transaction identifier</p> <code>None</code> <code>output_type</code> <code>type | None</code> <p>A raw output type (supported: str, bytes)</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout for this evaluation. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It bounds the HTTP request only and is never sent as a query variable - to pass a query variable literally named \"timeout\" use variables={\"timeout\": ...}.</p> <code>unset</code> <code>kwargs</code> <code>dict</code> <p>Key value arguments used as variables</p> <code>{}</code> <p>Returns:</p> Type Description <code>Parsed evaluation result</code> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/services/eval.py</code> <pre><code>async def file(\n    self,\n    path: str,\n    *,\n    variables: dict | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    output_type: type | None = None,\n    timeout=UNSET,\n    **kwargs,\n) -&gt; (\n    bytes\n    | str\n    | int\n    | float\n    | bool\n    | dict\n    | ElemTree.ElementTree\n    | ElemTree.Element\n    | list\n):\n    \"\"\"Evaluate code from a file in MarkLogic (auto-detect language).\n\n    Parameters\n    ----------\n    path : str\n        File path to the code to evaluate\n    variables : dict | None, default None\n        External variables\n    database : str | None, default None\n        Content database name or id\n    txid : str | None, default None\n        Transaction identifier\n    output_type : type | None, default None\n        A raw output type (supported: str, bytes)\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout for this evaluation. Unset uses the\n        client's configured timeout; None disables every HTTP timeout; a\n        number sets all four components to that many seconds; an\n        httpx.Timeout overrides them. It bounds the HTTP request only and is\n        never sent as a query variable - to pass a query variable literally\n        named \"timeout\" use variables={\"timeout\": ...}.\n    kwargs : dict\n        Key value arguments used as variables\n\n    Returns\n    -------\n    Parsed evaluation result\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return await self._eval(\n        file=path,\n        variables=variables,\n        database=database,\n        txid=txid,\n        output_type=output_type,\n        timeout=timeout,\n        **kwargs,\n    )\n</code></pre>"},{"location":"reference/mlclient/services/AsyncEvalService/#mlclient.services.AsyncEvalService.javascript","title":"javascript  <code>async</code>","text":"<pre><code>javascript(\n    code: str,\n    *,\n    variables: dict | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    output_type: type | None = None,\n    timeout=UNSET,\n    **kwargs,\n) -&gt; (\n    bytes\n    | str\n    | int\n    | float\n    | bool\n    | dict\n    | ElemTree.ElementTree\n    | ElemTree.Element\n    | list\n)\n</code></pre> <p>Evaluate JavaScript code in MarkLogic.</p> <p>Parameters:</p> Name Type Description Default <code>code</code> <code>str</code> <p>Raw JavaScript code to evaluate</p> required <code>variables</code> <code>dict | None</code> <p>External variables</p> <code>None</code> <code>database</code> <code>str | None</code> <p>Content database name or id</p> <code>None</code> <code>txid</code> <code>str | None</code> <p>Transaction identifier</p> <code>None</code> <code>output_type</code> <code>type | None</code> <p>A raw output type (supported: str, bytes)</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout for this evaluation. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It bounds the HTTP request only and is never sent as a query variable - to pass a query variable literally named \"timeout\" use variables={\"timeout\": ...}.</p> <code>unset</code> <code>kwargs</code> <code>dict</code> <p>Key value arguments used as variables</p> <code>{}</code> <p>Returns:</p> Type Description <code>Parsed evaluation result</code> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/services/eval.py</code> <pre><code>async def javascript(\n    self,\n    code: str,\n    *,\n    variables: dict | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    output_type: type | None = None,\n    timeout=UNSET,\n    **kwargs,\n) -&gt; (\n    bytes\n    | str\n    | int\n    | float\n    | bool\n    | dict\n    | ElemTree.ElementTree\n    | ElemTree.Element\n    | list\n):\n    \"\"\"Evaluate JavaScript code in MarkLogic.\n\n    Parameters\n    ----------\n    code : str\n        Raw JavaScript code to evaluate\n    variables : dict | None, default None\n        External variables\n    database : str | None, default None\n        Content database name or id\n    txid : str | None, default None\n        Transaction identifier\n    output_type : type | None, default None\n        A raw output type (supported: str, bytes)\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout for this evaluation. Unset uses the\n        client's configured timeout; None disables every HTTP timeout; a\n        number sets all four components to that many seconds; an\n        httpx.Timeout overrides them. It bounds the HTTP request only and is\n        never sent as a query variable - to pass a query variable literally\n        named \"timeout\" use variables={\"timeout\": ...}.\n    kwargs : dict\n        Key value arguments used as variables\n\n    Returns\n    -------\n    Parsed evaluation result\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return await self._eval(\n        js=code,\n        variables=variables,\n        database=database,\n        txid=txid,\n        output_type=output_type,\n        timeout=timeout,\n        **kwargs,\n    )\n</code></pre>"},{"location":"reference/mlclient/services/AsyncEvalService/#mlclient.services.AsyncEvalService.js","title":"js  <code>async</code>","text":"<pre><code>js(\n    code: str,\n    *,\n    variables: dict | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    output_type: type | None = None,\n    timeout=UNSET,\n    **kwargs,\n) -&gt; (\n    bytes\n    | str\n    | int\n    | float\n    | bool\n    | dict\n    | ElemTree.ElementTree\n    | ElemTree.Element\n    | list\n)\n</code></pre> <p>Evaluate JavaScript code in MarkLogic.</p> <p>Parameters:</p> Name Type Description Default <code>code</code> <code>str</code> <p>Raw JavaScript code to evaluate</p> required <code>variables</code> <code>dict | None</code> <p>External variables</p> <code>None</code> <code>database</code> <code>str | None</code> <p>Content database name or id</p> <code>None</code> <code>txid</code> <code>str | None</code> <p>Transaction identifier</p> <code>None</code> <code>output_type</code> <code>type | None</code> <p>A raw output type (supported: str, bytes)</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout for this evaluation. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It bounds the HTTP request only and is never sent as a query variable - to pass a query variable literally named \"timeout\" use variables={\"timeout\": ...}.</p> <code>unset</code> <code>kwargs</code> <code>dict</code> <p>Key value arguments used as variables</p> <code>{}</code> <p>Returns:</p> Type Description <code>Parsed evaluation result</code> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/services/eval.py</code> <pre><code>async def js(\n    self,\n    code: str,\n    *,\n    variables: dict | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    output_type: type | None = None,\n    timeout=UNSET,\n    **kwargs,\n) -&gt; (\n    bytes\n    | str\n    | int\n    | float\n    | bool\n    | dict\n    | ElemTree.ElementTree\n    | ElemTree.Element\n    | list\n):\n    \"\"\"Evaluate JavaScript code in MarkLogic.\n\n    Parameters\n    ----------\n    code : str\n        Raw JavaScript code to evaluate\n    variables : dict | None, default None\n        External variables\n    database : str | None, default None\n        Content database name or id\n    txid : str | None, default None\n        Transaction identifier\n    output_type : type | None, default None\n        A raw output type (supported: str, bytes)\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout for this evaluation. Unset uses the\n        client's configured timeout; None disables every HTTP timeout; a\n        number sets all four components to that many seconds; an\n        httpx.Timeout overrides them. It bounds the HTTP request only and is\n        never sent as a query variable - to pass a query variable literally\n        named \"timeout\" use variables={\"timeout\": ...}.\n    kwargs : dict\n        Key value arguments used as variables\n\n    Returns\n    -------\n    Parsed evaluation result\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return await self.javascript(\n        code,\n        variables=variables,\n        database=database,\n        txid=txid,\n        output_type=output_type,\n        timeout=timeout,\n        **kwargs,\n    )\n</code></pre>"},{"location":"reference/mlclient/services/AsyncEvalService/#mlclient.services.AsyncEvalService.xquery","title":"xquery  <code>async</code>","text":"<pre><code>xquery(\n    code: str,\n    *,\n    variables: dict | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    output_type: type | None = None,\n    timeout=UNSET,\n    **kwargs,\n) -&gt; (\n    bytes\n    | str\n    | int\n    | float\n    | bool\n    | dict\n    | ElemTree.ElementTree\n    | ElemTree.Element\n    | list\n)\n</code></pre> <p>Evaluate XQuery code in MarkLogic.</p> <p>Parameters:</p> Name Type Description Default <code>code</code> <code>str</code> <p>Raw XQuery code to evaluate</p> required <code>variables</code> <code>dict | None</code> <p>External variables</p> <code>None</code> <code>database</code> <code>str | None</code> <p>Content database name or id</p> <code>None</code> <code>txid</code> <code>str | None</code> <p>Transaction identifier</p> <code>None</code> <code>output_type</code> <code>type | None</code> <p>A raw output type (supported: str, bytes)</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout for this evaluation. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It bounds the HTTP request only and is never sent as a query variable - to pass a query variable literally named \"timeout\" use variables={\"timeout\": ...}.</p> <code>unset</code> <code>kwargs</code> <code>dict</code> <p>Key value arguments used as variables</p> <code>{}</code> <p>Returns:</p> Type Description <code>Parsed evaluation result</code> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/services/eval.py</code> <pre><code>async def xquery(\n    self,\n    code: str,\n    *,\n    variables: dict | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    output_type: type | None = None,\n    timeout=UNSET,\n    **kwargs,\n) -&gt; (\n    bytes\n    | str\n    | int\n    | float\n    | bool\n    | dict\n    | ElemTree.ElementTree\n    | ElemTree.Element\n    | list\n):\n    \"\"\"Evaluate XQuery code in MarkLogic.\n\n    Parameters\n    ----------\n    code : str\n        Raw XQuery code to evaluate\n    variables : dict | None, default None\n        External variables\n    database : str | None, default None\n        Content database name or id\n    txid : str | None, default None\n        Transaction identifier\n    output_type : type | None, default None\n        A raw output type (supported: str, bytes)\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout for this evaluation. Unset uses the\n        client's configured timeout; None disables every HTTP timeout; a\n        number sets all four components to that many seconds; an\n        httpx.Timeout overrides them. It bounds the HTTP request only and is\n        never sent as a query variable - to pass a query variable literally\n        named \"timeout\" use variables={\"timeout\": ...}.\n    kwargs : dict\n        Key value arguments used as variables\n\n    Returns\n    -------\n    Parsed evaluation result\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return await self._eval(\n        xq=code,\n        variables=variables,\n        database=database,\n        txid=txid,\n        output_type=output_type,\n        timeout=timeout,\n        **kwargs,\n    )\n</code></pre>"},{"location":"reference/mlclient/services/AsyncEvalService/#mlclient.services.AsyncEvalService.xqy","title":"xqy  <code>async</code>","text":"<pre><code>xqy(\n    code: str,\n    *,\n    variables: dict | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    output_type: type | None = None,\n    timeout=UNSET,\n    **kwargs,\n) -&gt; (\n    bytes\n    | str\n    | int\n    | float\n    | bool\n    | dict\n    | ElemTree.ElementTree\n    | ElemTree.Element\n    | list\n)\n</code></pre> <p>Evaluate XQuery code in MarkLogic.</p> <p>Parameters:</p> Name Type Description Default <code>code</code> <code>str</code> <p>Raw XQuery code to evaluate</p> required <code>variables</code> <code>dict | None</code> <p>External variables</p> <code>None</code> <code>database</code> <code>str | None</code> <p>Content database name or id</p> <code>None</code> <code>txid</code> <code>str | None</code> <p>Transaction identifier</p> <code>None</code> <code>output_type</code> <code>type | None</code> <p>A raw output type (supported: str, bytes)</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout for this evaluation. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It bounds the HTTP request only and is never sent as a query variable - to pass a query variable literally named \"timeout\" use variables={\"timeout\": ...}.</p> <code>unset</code> <code>kwargs</code> <code>dict</code> <p>Key value arguments used as variables</p> <code>{}</code> <p>Returns:</p> Type Description <code>Parsed evaluation result</code> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/services/eval.py</code> <pre><code>async def xqy(\n    self,\n    code: str,\n    *,\n    variables: dict | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    output_type: type | None = None,\n    timeout=UNSET,\n    **kwargs,\n) -&gt; (\n    bytes\n    | str\n    | int\n    | float\n    | bool\n    | dict\n    | ElemTree.ElementTree\n    | ElemTree.Element\n    | list\n):\n    \"\"\"Evaluate XQuery code in MarkLogic.\n\n    Parameters\n    ----------\n    code : str\n        Raw XQuery code to evaluate\n    variables : dict | None, default None\n        External variables\n    database : str | None, default None\n        Content database name or id\n    txid : str | None, default None\n        Transaction identifier\n    output_type : type | None, default None\n        A raw output type (supported: str, bytes)\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout for this evaluation. Unset uses the\n        client's configured timeout; None disables every HTTP timeout; a\n        number sets all four components to that many seconds; an\n        httpx.Timeout overrides them. It bounds the HTTP request only and is\n        never sent as a query variable - to pass a query variable literally\n        named \"timeout\" use variables={\"timeout\": ...}.\n    kwargs : dict\n        Key value arguments used as variables\n\n    Returns\n    -------\n    Parsed evaluation result\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return await self.xquery(\n        code,\n        variables=variables,\n        database=database,\n        txid=txid,\n        output_type=output_type,\n        timeout=timeout,\n        **kwargs,\n    )\n</code></pre>"},{"location":"reference/mlclient/services/AsyncLogsService/","title":"AsyncLogsService","text":"<pre><code>from mlclient.services import AsyncLogsService\n</code></pre>"},{"location":"reference/mlclient/services/AsyncLogsService/#mlclient.services.AsyncLogsService","title":"mlclient.services.AsyncLogsService","text":"<pre><code>AsyncLogsService(manage: AsyncManageApi)\n</code></pre> <p>               Bases: <code>LogsService</code></p> <p>Async higher-level service for /manage/v2/logs endpoint.</p> Source code in <code>mlclient/services/logs.py</code> <pre><code>def __init__(self, manage: AsyncManageApi):\n    self._manage = manage\n</code></pre>"},{"location":"reference/mlclient/services/AsyncLogsService/#mlclient.services.AsyncLogsService.get","title":"get  <code>async</code>","text":"<pre><code>get(\n    app_server: int | str | None = None,\n    log_type: LogType | str = LogType.ERROR,\n    *,\n    start_time: str | None = None,\n    end_time: str | None = None,\n    regex: str | None = None,\n    host: str | None = None,\n    timeout=UNSET,\n) -&gt; Iterator[dict]\n</code></pre> <p>Return logs from a MarkLogic server.</p> <p>Parameters:</p> Name Type Description Default <code>app_server</code> <code>int | str | None</code> <p>An app server (port) with logs to retrieve</p> <code>None</code> <code>log_type</code> <code>LogType | str</code> <p>A log type (enum or string: \"error\", \"access\", \"request\", \"audit\")</p> <code>LogType.ERROR</code> <code>start_time</code> <code>str | None</code> <p>A start time to search error logs</p> <code>None</code> <code>end_time</code> <code>str | None</code> <p>An end time to search error logs</p> <code>None</code> <code>regex</code> <code>str | None</code> <p>A regex to search error logs</p> <code>None</code> <code>host</code> <code>str | None</code> <p>A host name with logs to retrieve</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Iterator[dict]</code> <p>A log details generator.</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> <code>MarkLogicError</code> <p>If MarkLogic returns an error</p> Source code in <code>mlclient/services/logs.py</code> <pre><code>async def get(  # type: ignore[override]\n    self,\n    app_server: int | str | None = None,\n    log_type: LogType | str = LogType.ERROR,\n    *,\n    start_time: str | None = None,\n    end_time: str | None = None,\n    regex: str | None = None,\n    host: str | None = None,\n    timeout=UNSET,\n) -&gt; Iterator[dict]:\n    \"\"\"Return logs from a MarkLogic server.\n\n    Parameters\n    ----------\n    app_server : int | str | None, default None\n        An app server (port) with logs to retrieve\n    log_type : LogType | str, default LogType.ERROR\n        A log type (enum or string: \"error\", \"access\", \"request\", \"audit\")\n    start_time : str | None, default None\n        A start time to search error logs\n    end_time : str | None, default None\n        An end time to search error logs\n    regex : str | None, default None\n        A regex to search error logs\n    host : str | None, default None\n        A host name with logs to retrieve\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them.\n\n    Returns\n    -------\n    Iterator[dict]\n        A log details generator.\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    MarkLogicError\n        If MarkLogic returns an error\n    \"\"\"\n    if isinstance(log_type, str):\n        log_type = LogType.get(log_type)\n    is_error = log_type == LogType.ERROR\n    resp = await self._manage.logs.get(\n        self._filename(app_server, log_type),\n        data_format=\"json\",\n        host=host,\n        start_time=start_time if is_error else None,\n        end_time=end_time if is_error else None,\n        regex=regex if is_error else None,\n        timeout=timeout,\n    )\n    MLResponseParser.raise_for_status(resp)\n    return self._parse_logs(log_type, resp.json())\n</code></pre>"},{"location":"reference/mlclient/services/AsyncLogsService/#mlclient.services.AsyncLogsService.list","title":"list  <code>async</code>","text":"<pre><code>list(host: str | None = None, *, timeout=UNSET) -&gt; dict\n</code></pre> <p>Return a logs list from a MarkLogic server.</p> <p>Parameters:</p> Name Type Description Default <code>host</code> <code>str | None</code> <p>A host name with log files to retrieve</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them.</p> <code>unset</code> <p>Returns:</p> Type Description <code>dict</code> <p>A parsed list of log files in the MarkLogic server</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> <code>MarkLogicError</code> <p>If MarkLogic returns an error</p> Source code in <code>mlclient/services/logs.py</code> <pre><code>async def list(  # type: ignore[override]\n    self,\n    host: str | None = None,\n    *,\n    timeout=UNSET,\n) -&gt; dict:\n    \"\"\"Return a logs list from a MarkLogic server.\n\n    Parameters\n    ----------\n    host : str | None, default None\n        A host name with log files to retrieve\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them.\n\n    Returns\n    -------\n    dict\n        A parsed list of log files in the MarkLogic server\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    MarkLogicError\n        If MarkLogic returns an error\n    \"\"\"\n    resp = await self._manage.logs.get(\n        filename=None,\n        data_format=\"json\",\n        host=host,\n        timeout=timeout,\n    )\n    resp_body = resp.json()\n    if \"errorResponse\" in resp_body:\n        raise MarkLogicError(resp_body[\"errorResponse\"])\n\n    return self._parse_logs_list(resp_body)\n</code></pre>"},{"location":"reference/mlclient/services/AsyncTransactionService/","title":"AsyncTransactionService","text":"<pre><code>from mlclient.services import AsyncTransactionService\n</code></pre>"},{"location":"reference/mlclient/services/AsyncTransactionService/#mlclient.services.AsyncTransactionService","title":"mlclient.services.AsyncTransactionService","text":"<pre><code>AsyncTransactionService(\n    api: AsyncTransactionsApi,\n    txid: str,\n    database: str | None = None,\n)\n</code></pre> <p>Async per-transaction service; see TransactionService.</p> Source code in <code>mlclient/services/transactions.py</code> <pre><code>def __init__(\n    self,\n    api: AsyncTransactionsApi,\n    txid: str,\n    database: str | None = None,\n):\n    self._api = api\n    self._txid = txid\n    self._database = database\n</code></pre>"},{"location":"reference/mlclient/services/AsyncTransactionService/#mlclient.services.AsyncTransactionService.database","title":"database  <code>property</code>","text":"<pre><code>database: str | None\n</code></pre> <p>The content database the transaction was opened against.</p>"},{"location":"reference/mlclient/services/AsyncTransactionService/#mlclient.services.AsyncTransactionService.id","title":"id  <code>property</code>","text":"<pre><code>id: str\n</code></pre> <p>The server-assigned transaction id.</p>"},{"location":"reference/mlclient/services/AsyncTransactionService/#mlclient.services.AsyncTransactionService.commit","title":"commit  <code>async</code>","text":"<pre><code>commit(*, timeout=UNSET) -&gt; None\n</code></pre> <p>Commit the transaction.</p> <p>Parameters:</p> Name Type Description Default <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout for the commit request. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. This bounds the HTTP request only and is unrelated to the transaction's server-side time_limit.</p> <code>unset</code> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> <code>MarkLogicError</code> <p>If MarkLogic returns an error</p> Source code in <code>mlclient/services/transactions.py</code> <pre><code>async def commit(self, *, timeout=UNSET) -&gt; None:\n    \"\"\"Commit the transaction.\n\n    Parameters\n    ----------\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout for the commit request. Unset uses the\n        client's configured timeout; None disables every HTTP timeout; a\n        number sets all four components to that many seconds; an\n        httpx.Timeout overrides them. This bounds the HTTP request only and\n        is unrelated to the transaction's server-side time_limit.\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    MarkLogicError\n        If MarkLogic returns an error\n    \"\"\"\n    await self._finish(\"commit\", timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/services/AsyncTransactionService/#mlclient.services.AsyncTransactionService.keys","title":"keys","text":"<pre><code>keys() -&gt; Iterable[str]\n</code></pre> <p>Return the keys exposed for <code>**</code> unpacking (txid, database if set).</p> Source code in <code>mlclient/services/transactions.py</code> <pre><code>def keys(self) -&gt; Iterable[str]:\n    \"\"\"Return the keys exposed for ``**`` unpacking (txid, database if set).\"\"\"\n    if self._database is None:\n        return (\"txid\",)\n    return (\"txid\", \"database\")\n</code></pre>"},{"location":"reference/mlclient/services/AsyncTransactionService/#mlclient.services.AsyncTransactionService.rollback","title":"rollback  <code>async</code>","text":"<pre><code>rollback(*, timeout=UNSET) -&gt; None\n</code></pre> <p>Roll back the transaction.</p> <p>Parameters:</p> Name Type Description Default <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout for the rollback request. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. This bounds the HTTP request only and is unrelated to the transaction's server-side time_limit.</p> <code>unset</code> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> <code>MarkLogicError</code> <p>If MarkLogic returns an error</p> Source code in <code>mlclient/services/transactions.py</code> <pre><code>async def rollback(self, *, timeout=UNSET) -&gt; None:\n    \"\"\"Roll back the transaction.\n\n    Parameters\n    ----------\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout for the rollback request. Unset uses the\n        client's configured timeout; None disables every HTTP timeout; a\n        number sets all four components to that many seconds; an\n        httpx.Timeout overrides them. This bounds the HTTP request only and\n        is unrelated to the transaction's server-side time_limit.\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    MarkLogicError\n        If MarkLogic returns an error\n    \"\"\"\n    await self._finish(\"rollback\", timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/services/AsyncTransactionService/#mlclient.services.AsyncTransactionService.status","title":"status  <code>async</code>","text":"<pre><code>status(*, data_format: str = 'json', timeout=UNSET)\n</code></pre> <p>Return the status of the transaction.</p> <p>Parameters:</p> Name Type Description Default <code>data_format</code> <code>str</code> <p>The format of the returned status; either json or xml</p> <code>\"json\"</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout for the status request. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. This bounds the HTTP request only and is unrelated to the transaction's server-side time_limit.</p> <code>unset</code> <p>Returns:</p> Type Description <code>The parsed transaction status</code> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> <code>MarkLogicError</code> <p>If MarkLogic returns an error</p> Source code in <code>mlclient/services/transactions.py</code> <pre><code>async def status(self, *, data_format: str = \"json\", timeout=UNSET):\n    \"\"\"Return the status of the transaction.\n\n    Parameters\n    ----------\n    data_format : str, default \"json\"\n        The format of the returned status; either json or xml\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout for the status request. Unset uses the\n        client's configured timeout; None disables every HTTP timeout; a\n        number sets all four components to that many seconds; an\n        httpx.Timeout overrides them. This bounds the HTTP request only and\n        is unrelated to the transaction's server-side time_limit.\n\n    Returns\n    -------\n    The parsed transaction status\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    MarkLogicError\n        If MarkLogic returns an error\n    \"\"\"\n    resp = await self._api.get(\n        self._txid,\n        data_format=data_format,\n        database=self._database,\n        timeout=timeout,\n    )\n    MLResponseParser.raise_for_status(resp)\n    return MLResponseParser.parse(resp)\n</code></pre>"},{"location":"reference/mlclient/services/DocumentsService/","title":"DocumentsService","text":"<pre><code>from mlclient.services import DocumentsService\n</code></pre>"},{"location":"reference/mlclient/services/DocumentsService/#mlclient.services.DocumentsService","title":"mlclient.services.DocumentsService","text":"<pre><code>DocumentsService(rest: RestApi)\n</code></pre> <p>Higher-level service for /v1/documents CRUD operations.</p> Notes <p>MarkLogic's REST API (App-Services, port 8000) and Manage API (port 8002) return different HTTP status codes for the same underlying errors. For example, RESTAPI-NODOCUMENT and XDMP-DOCNOTFOUND are returned as 404 Not Found on the REST API but as 500 Internal Server Error on the Manage API. This is due to different error handler mappings on each port.</p> Source code in <code>mlclient/services/documents.py</code> <pre><code>def __init__(self, rest: RestApi):\n    self._rest = rest\n</code></pre>"},{"location":"reference/mlclient/services/DocumentsService/#mlclient.services.DocumentsService.delete","title":"delete","text":"<pre><code>delete(\n    uris: str | list[str] | tuple[str] | set[str],\n    *,\n    category: Category\n    | str\n    | list[Category | str]\n    | None = None,\n    database: str | None = None,\n    temporal_collection: str | None = None,\n    wipe_temporal: bool | None = None,\n    txid: str | None = None,\n    timeout=UNSET,\n)\n</code></pre> <p>Delete document(s) content or metadata in a MarkLogic database.</p> <p>URIs are transparently split into batches whose combined query string stays below the httpx URL length limit; each batch is a separate HTTP request.</p> <p>Parameters:</p> Name Type Description Default <code>uris</code> <code>str | list[str] | tuple[str] | set[str]</code> <p>The URI of a document to delete.</p> required <code>category</code> <code>Category | str | list[Category | str] | None</code> <p>The category of data to remove/reset.</p> <code>None</code> <code>database</code> <code>str | None</code> <p>Perform this operation on the named content database.</p> <code>None</code> <code>temporal_collection</code> <code>str | None</code> <p>Temporal collection name.</p> <code>None</code> <code>wipe_temporal</code> <code>bool | None</code> <p>Remove all versions of a temporal document.</p> <code>None</code> <code>txid</code> <code>str | None</code> <p>Perform this operation within the named multi-statement transaction.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. When the operation issues several requests it applies to each of them independently, not as a shared budget, and is not persisted.</p> <code>unset</code> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> <code>MarkLogicError</code> <p>If MarkLogic returns an error</p> Source code in <code>mlclient/services/documents.py</code> <pre><code>def delete(\n    self,\n    uris: str | list[str] | tuple[str] | set[str],\n    *,\n    category: Category | str | list[Category | str] | None = None,\n    database: str | None = None,\n    temporal_collection: str | None = None,\n    wipe_temporal: bool | None = None,\n    txid: str | None = None,\n    timeout=UNSET,\n):\n    \"\"\"Delete document(s) content or metadata in a MarkLogic database.\n\n    URIs are transparently split into batches whose combined query string\n    stays below the httpx URL length limit; each batch is a separate\n    HTTP request.\n\n    Parameters\n    ----------\n    uris : str | list[str] | tuple[str] | set[str]\n        The URI of a document to delete.\n    category : Category | str | list[Category | str] | None, default None\n        The category of data to remove/reset.\n    database : str | None, default None\n        Perform this operation on the named content database.\n    temporal_collection : str | None, default None\n        Temporal collection name.\n    wipe_temporal : bool | None, default None\n        Remove all versions of a temporal document.\n    txid : str | None, default None\n        Perform this operation within the named multi-statement transaction.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout. Unset uses the client's configured\n        timeout; None disables every HTTP timeout; a number sets all four\n        components to that many seconds; an httpx.Timeout overrides them.\n        When the operation issues several requests it applies to each of\n        them independently, not as a shared budget, and is not persisted.\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    MarkLogicError\n        If MarkLogic returns an error\n    \"\"\"\n    category = _normalize_category(category)\n    for batch in _batched_uris(uris):\n        resp = self._rest.documents.delete(\n            batch,\n            category=category,\n            database=database,\n            temporal_collection=temporal_collection,\n            wipe_temporal=wipe_temporal,\n            txid=txid,\n            timeout=timeout,\n        )\n        MLResponseParser.raise_for_status(resp)\n</code></pre>"},{"location":"reference/mlclient/services/DocumentsService/#mlclient.services.DocumentsService.read","title":"read","text":"<pre><code>read(\n    uris: str | list[str] | tuple[str] | set[str],\n    *,\n    category: Category\n    | str\n    | list[Category | str]\n    | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    timeout=UNSET,\n) -&gt; Document | dict[str, Document]\n</code></pre> <p>Return document(s) content or metadata from a MarkLogic database.</p> <p>When uris is a string it returns a single Document instance. Otherwise, result is a dict mapping URI to Document.</p> <p>Parameters:</p> Name Type Description Default <code>uris</code> <code>str | list[str] | tuple[str] | set[str]</code> <p>One or more URIs for documents in the database.</p> required <code>category</code> <code>Category | str | list[Category | str] | None</code> <p>The category of data to fetch about the requested document.</p> <code>None</code> <code>database</code> <code>str | None</code> <p>Perform this operation on the named content database.</p> <code>None</code> <code>txid</code> <code>str | None</code> <p>Perform this operation within the named multi-statement transaction.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. When the operation issues several requests it applies to each of them independently, not as a shared budget, and is not persisted.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Document | dict[str, Document]</code> <p>A single document when uris is a string, otherwise a dict keyed by URI.</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> <code>MarkLogicError</code> <p>If MarkLogic returns an error</p> Source code in <code>mlclient/services/documents.py</code> <pre><code>def read(\n    self,\n    uris: str | list[str] | tuple[str] | set[str],\n    *,\n    category: Category | str | list[Category | str] | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    timeout=UNSET,\n) -&gt; Document | dict[str, Document]:\n    \"\"\"Return document(s) content or metadata from a MarkLogic database.\n\n    When uris is a string it returns a single Document instance. Otherwise,\n    result is a dict mapping URI to Document.\n\n    Parameters\n    ----------\n    uris : str | list[str] | tuple[str] | set[str]\n        One or more URIs for documents in the database.\n    category : Category | str | list[Category | str] | None, default None\n        The category of data to fetch about the requested document.\n    database : str | None, default None\n        Perform this operation on the named content database.\n    txid : str | None, default None\n        Perform this operation within the named multi-statement transaction.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout. Unset uses the client's configured\n        timeout; None disables every HTTP timeout; a number sets all four\n        components to that many seconds; an httpx.Timeout overrides them.\n        When the operation issues several requests it applies to each of\n        them independently, not as a shared budget, and is not persisted.\n\n    Returns\n    -------\n    Document | dict[str, Document]\n        A single document when uris is a string, otherwise a dict keyed by URI.\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    MarkLogicError\n        If MarkLogic returns an error\n    \"\"\"\n    docs = self.read_stream(\n        uris,\n        category=category,\n        database=database,\n        txid=txid,\n        timeout=timeout,\n    )\n    return next(docs) if isinstance(uris, str) else {doc.uri: doc for doc in docs}\n</code></pre>"},{"location":"reference/mlclient/services/DocumentsService/#mlclient.services.DocumentsService.read_stream","title":"read_stream","text":"<pre><code>read_stream(\n    uris: str | list[str] | tuple[str] | set[str],\n    *,\n    category: Category\n    | str\n    | list[Category | str]\n    | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    timeout=UNSET,\n) -&gt; Iterator[Document]\n</code></pre> <p>Return document(s) as an iterator, suitable for batch processing.</p> <p>Unlike read(), does not materialize results into a dict. URIs are transparently split into batches whose combined query string stays below the httpx URL length limit; each batch is a separate HTTP request.</p> <p>Parameters:</p> Name Type Description Default <code>uris</code> <code>str | list[str] | tuple[str] | set[str]</code> <p>One or more URIs for documents in the database.</p> required <code>category</code> <code>Category | str | list[Category | str] | None</code> <p>The category of data to fetch about the requested document.</p> <code>None</code> <code>database</code> <code>str | None</code> <p>Perform this operation on the named content database.</p> <code>None</code> <code>txid</code> <code>str | None</code> <p>Perform this operation within the named multi-statement transaction.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. When the operation issues several requests it applies to each of them independently, not as a shared budget, and is not persisted.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Iterator[Document]</code> <p>Documents from the database.</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> <code>MarkLogicError</code> <p>If MarkLogic returns an error</p> Source code in <code>mlclient/services/documents.py</code> <pre><code>def read_stream(\n    self,\n    uris: str | list[str] | tuple[str] | set[str],\n    *,\n    category: Category | str | list[Category | str] | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    timeout=UNSET,\n) -&gt; Iterator[Document]:\n    \"\"\"Return document(s) as an iterator, suitable for batch processing.\n\n    Unlike read(), does not materialize results into a dict. URIs are\n    transparently split into batches whose combined query string stays below\n    the httpx URL length limit; each batch is a separate HTTP request.\n\n    Parameters\n    ----------\n    uris : str | list[str] | tuple[str] | set[str]\n        One or more URIs for documents in the database.\n    category : Category | str | list[Category | str] | None, default None\n        The category of data to fetch about the requested document.\n    database : str | None, default None\n        Perform this operation on the named content database.\n    txid : str | None, default None\n        Perform this operation within the named multi-statement transaction.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout. Unset uses the client's configured\n        timeout; None disables every HTTP timeout; a number sets all four\n        components to that many seconds; an httpx.Timeout overrides them.\n        When the operation issues several requests it applies to each of\n        them independently, not as a shared budget, and is not persisted.\n\n    Returns\n    -------\n    Iterator[Document]\n        Documents from the database.\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    MarkLogicError\n        If MarkLogic returns an error\n    \"\"\"\n    category = _normalize_category(category)\n    for batch in _batched_uris(uris):\n        resp = self._rest.documents.get(\n            batch,\n            category=category,\n            database=database,\n            data_format=\"json\",\n            txid=txid,\n            timeout=timeout,\n        )\n        MLResponseParser.raise_for_status(resp)\n        yield from _DocumentsReader.parse(resp, batch, category)\n</code></pre>"},{"location":"reference/mlclient/services/DocumentsService/#mlclient.services.DocumentsService.write","title":"write","text":"<pre><code>write(\n    data: Document | Metadata | list[Document | Metadata],\n    *,\n    database: str | None = None,\n    temporal_collection: str | None = None,\n    txid: str | None = None,\n    timeout=UNSET,\n) -&gt; dict\n</code></pre> <p>Write (create or update) document(s) content or metadata.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>Document | Metadata | list[Document | Metadata]</code> <p>One or more document or default metadata.</p> required <code>database</code> <code>str | None</code> <p>Perform this operation on the named content database.</p> <code>None</code> <code>temporal_collection</code> <code>str | None</code> <p>Temporal collection name.</p> <code>None</code> <code>txid</code> <code>str | None</code> <p>Perform this operation within the named multi-statement transaction.</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. When the operation issues several requests it applies to each of them independently, not as a shared budget, and is not persisted.</p> <code>unset</code> <p>Returns:</p> Type Description <code>dict</code> <p>An origin response from a MarkLogic server.</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> <code>MarkLogicError</code> <p>If MarkLogic returns an error</p> Source code in <code>mlclient/services/documents.py</code> <pre><code>def write(\n    self,\n    data: Document | Metadata | list[Document | Metadata],\n    *,\n    database: str | None = None,\n    temporal_collection: str | None = None,\n    txid: str | None = None,\n    timeout=UNSET,\n) -&gt; dict:\n    \"\"\"Write (create or update) document(s) content or metadata.\n\n    Parameters\n    ----------\n    data : Document | Metadata | list[Document | Metadata]\n        One or more document or default metadata.\n    database : str | None, default None\n        Perform this operation on the named content database.\n    temporal_collection : str | None, default None\n        Temporal collection name.\n    txid : str | None, default None\n        Perform this operation within the named multi-statement transaction.\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout. Unset uses the client's configured\n        timeout; None disables every HTTP timeout; a number sets all four\n        components to that many seconds; an httpx.Timeout overrides them.\n        When the operation issues several requests it applies to each of\n        them independently, not as a shared budget, and is not persisted.\n\n    Returns\n    -------\n    dict\n        An origin response from a MarkLogic server.\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    MarkLogicError\n        If MarkLogic returns an error\n    \"\"\"\n    body_parts = _DocumentsSender.parse(data)\n    resp = self._rest.documents.post(\n        body_parts,\n        database=database,\n        temporal_collection=temporal_collection,\n        txid=txid,\n        timeout=timeout,\n    )\n    MLResponseParser.raise_for_status(resp)\n    return MLResponseParser.parse(resp)\n</code></pre>"},{"location":"reference/mlclient/services/EvalService/","title":"EvalService","text":"<pre><code>from mlclient.services import EvalService\n</code></pre>"},{"location":"reference/mlclient/services/EvalService/#mlclient.services.EvalService","title":"mlclient.services.EvalService","text":"<pre><code>EvalService(rest: RestApi)\n</code></pre> <p>Higher-level service for /v1/eval endpoint.</p> Source code in <code>mlclient/services/eval.py</code> <pre><code>def __init__(self, rest: RestApi):\n    self._rest = rest\n</code></pre>"},{"location":"reference/mlclient/services/EvalService/#mlclient.services.EvalService.execute","title":"execute","text":"<pre><code>execute(\n    *,\n    file: str | None = None,\n    xq: str | None = None,\n    js: str | None = None,\n    variables: dict | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    output_type: type | None = None,\n    timeout=UNSET,\n    **kwargs,\n)\n</code></pre> <p>Evaluate code in a MarkLogic server (general-purpose).</p> <p>Dynamically resolves the code type from the provided parameters. For explicit, typed calls prefer xquery(), javascript(), or file().</p> <p>Parameters:</p> Name Type Description Default <code>file</code> <code>str | None</code> <p>A file path of a code to evaluate</p> <code>None</code> <code>xq</code> <code>str | None</code> <p>A raw XQuery code to evaluate</p> <code>None</code> <code>js</code> <code>str | None</code> <p>A raw JavaScript code to evaluate</p> <code>None</code> <code>variables</code> <code>dict | None</code> <p>External variables to pass to the query during evaluation</p> <code>None</code> <code>database</code> <code>str | None</code> <p>Content database name or id</p> <code>None</code> <code>txid</code> <code>str | None</code> <p>Transaction identifier</p> <code>None</code> <code>output_type</code> <code>type | None</code> <p>A raw output type (supported: str, bytes)</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout for this evaluation. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It bounds the HTTP request only and is never sent as a query variable - to pass a query variable literally named \"timeout\" use variables={\"timeout\": ...}.</p> <code>unset</code> <code>kwargs</code> <code>dict</code> <p>Key value arguments used as variables</p> <code>{}</code> <p>Returns:</p> Type Description <code>Parsed evaluation result</code> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> <code>MarkLogicError</code> <p>If MarkLogic returns an error</p> Source code in <code>mlclient/services/eval.py</code> <pre><code>def execute(\n    self,\n    *,\n    file: str | None = None,\n    xq: str | None = None,\n    js: str | None = None,\n    variables: dict | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    output_type: type | None = None,\n    timeout=UNSET,\n    **kwargs,\n):\n    \"\"\"Evaluate code in a MarkLogic server (general-purpose).\n\n    Dynamically resolves the code type from the provided parameters.\n    For explicit, typed calls prefer xquery(), javascript(), or file().\n\n    Parameters\n    ----------\n    file : str | None, default None\n        A file path of a code to evaluate\n    xq : str | None, default None\n        A raw XQuery code to evaluate\n    js : str | None, default None\n        A raw JavaScript code to evaluate\n    variables : dict | None, default None\n        External variables to pass to the query during evaluation\n    database : str | None, default None\n        Content database name or id\n    txid : str | None, default None\n        Transaction identifier\n    output_type : type | None, default None\n        A raw output type (supported: str, bytes)\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout for this evaluation. Unset uses the\n        client's configured timeout; None disables every HTTP timeout; a\n        number sets all four components to that many seconds; an\n        httpx.Timeout overrides them. It bounds the HTTP request only and is\n        never sent as a query variable - to pass a query variable literally\n        named \"timeout\" use variables={\"timeout\": ...}.\n    kwargs : dict\n        Key value arguments used as variables\n\n    Returns\n    -------\n    Parsed evaluation result\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    MarkLogicError\n        If MarkLogic returns an error\n    \"\"\"\n    return self._eval(\n        file=file,\n        xq=xq,\n        js=js,\n        variables=variables,\n        database=database,\n        txid=txid,\n        output_type=output_type,\n        timeout=timeout,\n        **kwargs,\n    )\n</code></pre>"},{"location":"reference/mlclient/services/EvalService/#mlclient.services.EvalService.file","title":"file","text":"<pre><code>file(\n    path: str,\n    *,\n    variables: dict | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    output_type: type | None = None,\n    timeout=UNSET,\n    **kwargs,\n) -&gt; (\n    bytes\n    | str\n    | int\n    | float\n    | bool\n    | dict\n    | ElemTree.ElementTree\n    | ElemTree.Element\n    | list\n)\n</code></pre> <p>Evaluate code from a file in MarkLogic (auto-detect language).</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>File path to the code to evaluate</p> required <code>variables</code> <code>dict | None</code> <p>External variables</p> <code>None</code> <code>database</code> <code>str | None</code> <p>Content database name or id</p> <code>None</code> <code>txid</code> <code>str | None</code> <p>Transaction identifier</p> <code>None</code> <code>output_type</code> <code>type | None</code> <p>A raw output type (supported: str, bytes)</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout for this evaluation. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It bounds the HTTP request only and is never sent as a query variable - to pass a query variable literally named \"timeout\" use variables={\"timeout\": ...}.</p> <code>unset</code> <code>kwargs</code> <code>dict</code> <p>Key value arguments used as variables</p> <code>{}</code> <p>Returns:</p> Type Description <code>Parsed evaluation result</code> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/services/eval.py</code> <pre><code>def file(\n    self,\n    path: str,\n    *,\n    variables: dict | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    output_type: type | None = None,\n    timeout=UNSET,\n    **kwargs,\n) -&gt; (\n    bytes\n    | str\n    | int\n    | float\n    | bool\n    | dict\n    | ElemTree.ElementTree\n    | ElemTree.Element\n    | list\n):\n    \"\"\"Evaluate code from a file in MarkLogic (auto-detect language).\n\n    Parameters\n    ----------\n    path : str\n        File path to the code to evaluate\n    variables : dict | None, default None\n        External variables\n    database : str | None, default None\n        Content database name or id\n    txid : str | None, default None\n        Transaction identifier\n    output_type : type | None, default None\n        A raw output type (supported: str, bytes)\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout for this evaluation. Unset uses the\n        client's configured timeout; None disables every HTTP timeout; a\n        number sets all four components to that many seconds; an\n        httpx.Timeout overrides them. It bounds the HTTP request only and is\n        never sent as a query variable - to pass a query variable literally\n        named \"timeout\" use variables={\"timeout\": ...}.\n    kwargs : dict\n        Key value arguments used as variables\n\n    Returns\n    -------\n    Parsed evaluation result\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return self._eval(\n        file=path,\n        variables=variables,\n        database=database,\n        txid=txid,\n        output_type=output_type,\n        timeout=timeout,\n        **kwargs,\n    )\n</code></pre>"},{"location":"reference/mlclient/services/EvalService/#mlclient.services.EvalService.javascript","title":"javascript","text":"<pre><code>javascript(\n    code: str,\n    *,\n    variables: dict | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    output_type: type | None = None,\n    timeout=UNSET,\n    **kwargs,\n) -&gt; (\n    bytes\n    | str\n    | int\n    | float\n    | bool\n    | dict\n    | ElemTree.ElementTree\n    | ElemTree.Element\n    | list\n)\n</code></pre> <p>Evaluate JavaScript code in MarkLogic.</p> <p>Parameters:</p> Name Type Description Default <code>code</code> <code>str</code> <p>Raw JavaScript code to evaluate</p> required <code>variables</code> <code>dict | None</code> <p>External variables</p> <code>None</code> <code>database</code> <code>str | None</code> <p>Content database name or id</p> <code>None</code> <code>txid</code> <code>str | None</code> <p>Transaction identifier</p> <code>None</code> <code>output_type</code> <code>type | None</code> <p>A raw output type (supported: str, bytes)</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout for this evaluation. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It bounds the HTTP request only and is never sent as a query variable - to pass a query variable literally named \"timeout\" use variables={\"timeout\": ...}.</p> <code>unset</code> <code>kwargs</code> <code>dict</code> <p>Key value arguments used as variables</p> <code>{}</code> <p>Returns:</p> Type Description <code>Parsed evaluation result</code> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/services/eval.py</code> <pre><code>def javascript(\n    self,\n    code: str,\n    *,\n    variables: dict | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    output_type: type | None = None,\n    timeout=UNSET,\n    **kwargs,\n) -&gt; (\n    bytes\n    | str\n    | int\n    | float\n    | bool\n    | dict\n    | ElemTree.ElementTree\n    | ElemTree.Element\n    | list\n):\n    \"\"\"Evaluate JavaScript code in MarkLogic.\n\n    Parameters\n    ----------\n    code : str\n        Raw JavaScript code to evaluate\n    variables : dict | None, default None\n        External variables\n    database : str | None, default None\n        Content database name or id\n    txid : str | None, default None\n        Transaction identifier\n    output_type : type | None, default None\n        A raw output type (supported: str, bytes)\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout for this evaluation. Unset uses the\n        client's configured timeout; None disables every HTTP timeout; a\n        number sets all four components to that many seconds; an\n        httpx.Timeout overrides them. It bounds the HTTP request only and is\n        never sent as a query variable - to pass a query variable literally\n        named \"timeout\" use variables={\"timeout\": ...}.\n    kwargs : dict\n        Key value arguments used as variables\n\n    Returns\n    -------\n    Parsed evaluation result\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return self._eval(\n        js=code,\n        variables=variables,\n        database=database,\n        txid=txid,\n        output_type=output_type,\n        timeout=timeout,\n        **kwargs,\n    )\n</code></pre>"},{"location":"reference/mlclient/services/EvalService/#mlclient.services.EvalService.js","title":"js","text":"<pre><code>js(\n    code: str,\n    *,\n    variables: dict | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    output_type: type | None = None,\n    timeout=UNSET,\n    **kwargs,\n) -&gt; (\n    bytes\n    | str\n    | int\n    | float\n    | bool\n    | dict\n    | ElemTree.ElementTree\n    | ElemTree.Element\n    | list\n)\n</code></pre> <p>Evaluate JavaScript code in MarkLogic.</p> <p>Parameters:</p> Name Type Description Default <code>code</code> <code>str</code> <p>Raw JavaScript code to evaluate</p> required <code>variables</code> <code>dict | None</code> <p>External variables</p> <code>None</code> <code>database</code> <code>str | None</code> <p>Content database name or id</p> <code>None</code> <code>txid</code> <code>str | None</code> <p>Transaction identifier</p> <code>None</code> <code>output_type</code> <code>type | None</code> <p>A raw output type (supported: str, bytes)</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout for this evaluation. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It bounds the HTTP request only and is never sent as a query variable - to pass a query variable literally named \"timeout\" use variables={\"timeout\": ...}.</p> <code>unset</code> <code>kwargs</code> <code>dict</code> <p>Key value arguments used as variables</p> <code>{}</code> <p>Returns:</p> Type Description <code>Parsed evaluation result</code> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/services/eval.py</code> <pre><code>def js(\n    self,\n    code: str,\n    *,\n    variables: dict | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    output_type: type | None = None,\n    timeout=UNSET,\n    **kwargs,\n) -&gt; (\n    bytes\n    | str\n    | int\n    | float\n    | bool\n    | dict\n    | ElemTree.ElementTree\n    | ElemTree.Element\n    | list\n):\n    \"\"\"Evaluate JavaScript code in MarkLogic.\n\n    Parameters\n    ----------\n    code : str\n        Raw JavaScript code to evaluate\n    variables : dict | None, default None\n        External variables\n    database : str | None, default None\n        Content database name or id\n    txid : str | None, default None\n        Transaction identifier\n    output_type : type | None, default None\n        A raw output type (supported: str, bytes)\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout for this evaluation. Unset uses the\n        client's configured timeout; None disables every HTTP timeout; a\n        number sets all four components to that many seconds; an\n        httpx.Timeout overrides them. It bounds the HTTP request only and is\n        never sent as a query variable - to pass a query variable literally\n        named \"timeout\" use variables={\"timeout\": ...}.\n    kwargs : dict\n        Key value arguments used as variables\n\n    Returns\n    -------\n    Parsed evaluation result\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return self.javascript(\n        code,\n        variables=variables,\n        database=database,\n        txid=txid,\n        output_type=output_type,\n        timeout=timeout,\n        **kwargs,\n    )\n</code></pre>"},{"location":"reference/mlclient/services/EvalService/#mlclient.services.EvalService.xquery","title":"xquery","text":"<pre><code>xquery(\n    code: str,\n    *,\n    variables: dict | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    output_type: type | None = None,\n    timeout=UNSET,\n    **kwargs,\n) -&gt; (\n    bytes\n    | str\n    | int\n    | float\n    | bool\n    | dict\n    | ElemTree.ElementTree\n    | ElemTree.Element\n    | list\n)\n</code></pre> <p>Evaluate XQuery code in MarkLogic.</p> <p>Parameters:</p> Name Type Description Default <code>code</code> <code>str</code> <p>Raw XQuery code to evaluate</p> required <code>variables</code> <code>dict | None</code> <p>External variables</p> <code>None</code> <code>database</code> <code>str | None</code> <p>Content database name or id</p> <code>None</code> <code>txid</code> <code>str | None</code> <p>Transaction identifier</p> <code>None</code> <code>output_type</code> <code>type | None</code> <p>A raw output type (supported: str, bytes)</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout for this evaluation. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It bounds the HTTP request only and is never sent as a query variable - to pass a query variable literally named \"timeout\" use variables={\"timeout\": ...}.</p> <code>unset</code> <code>kwargs</code> <code>dict</code> <p>Key value arguments used as variables</p> <code>{}</code> <p>Returns:</p> Type Description <code>Parsed evaluation result</code> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/services/eval.py</code> <pre><code>def xquery(\n    self,\n    code: str,\n    *,\n    variables: dict | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    output_type: type | None = None,\n    timeout=UNSET,\n    **kwargs,\n) -&gt; (\n    bytes\n    | str\n    | int\n    | float\n    | bool\n    | dict\n    | ElemTree.ElementTree\n    | ElemTree.Element\n    | list\n):\n    \"\"\"Evaluate XQuery code in MarkLogic.\n\n    Parameters\n    ----------\n    code : str\n        Raw XQuery code to evaluate\n    variables : dict | None, default None\n        External variables\n    database : str | None, default None\n        Content database name or id\n    txid : str | None, default None\n        Transaction identifier\n    output_type : type | None, default None\n        A raw output type (supported: str, bytes)\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout for this evaluation. Unset uses the\n        client's configured timeout; None disables every HTTP timeout; a\n        number sets all four components to that many seconds; an\n        httpx.Timeout overrides them. It bounds the HTTP request only and is\n        never sent as a query variable - to pass a query variable literally\n        named \"timeout\" use variables={\"timeout\": ...}.\n    kwargs : dict\n        Key value arguments used as variables\n\n    Returns\n    -------\n    Parsed evaluation result\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return self._eval(\n        xq=code,\n        variables=variables,\n        database=database,\n        txid=txid,\n        output_type=output_type,\n        timeout=timeout,\n        **kwargs,\n    )\n</code></pre>"},{"location":"reference/mlclient/services/EvalService/#mlclient.services.EvalService.xqy","title":"xqy","text":"<pre><code>xqy(\n    code: str,\n    *,\n    variables: dict | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    output_type: type | None = None,\n    timeout=UNSET,\n    **kwargs,\n) -&gt; (\n    bytes\n    | str\n    | int\n    | float\n    | bool\n    | dict\n    | ElemTree.ElementTree\n    | ElemTree.Element\n    | list\n)\n</code></pre> <p>Evaluate XQuery code in MarkLogic.</p> <p>Parameters:</p> Name Type Description Default <code>code</code> <code>str</code> <p>Raw XQuery code to evaluate</p> required <code>variables</code> <code>dict | None</code> <p>External variables</p> <code>None</code> <code>database</code> <code>str | None</code> <p>Content database name or id</p> <code>None</code> <code>txid</code> <code>str | None</code> <p>Transaction identifier</p> <code>None</code> <code>output_type</code> <code>type | None</code> <p>A raw output type (supported: str, bytes)</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout for this evaluation. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. It bounds the HTTP request only and is never sent as a query variable - to pass a query variable literally named \"timeout\" use variables={\"timeout\": ...}.</p> <code>unset</code> <code>kwargs</code> <code>dict</code> <p>Key value arguments used as variables</p> <code>{}</code> <p>Returns:</p> Type Description <code>Parsed evaluation result</code> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> Source code in <code>mlclient/services/eval.py</code> <pre><code>def xqy(\n    self,\n    code: str,\n    *,\n    variables: dict | None = None,\n    database: str | None = None,\n    txid: str | None = None,\n    output_type: type | None = None,\n    timeout=UNSET,\n    **kwargs,\n) -&gt; (\n    bytes\n    | str\n    | int\n    | float\n    | bool\n    | dict\n    | ElemTree.ElementTree\n    | ElemTree.Element\n    | list\n):\n    \"\"\"Evaluate XQuery code in MarkLogic.\n\n    Parameters\n    ----------\n    code : str\n        Raw XQuery code to evaluate\n    variables : dict | None, default None\n        External variables\n    database : str | None, default None\n        Content database name or id\n    txid : str | None, default None\n        Transaction identifier\n    output_type : type | None, default None\n        A raw output type (supported: str, bytes)\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout for this evaluation. Unset uses the\n        client's configured timeout; None disables every HTTP timeout; a\n        number sets all four components to that many seconds; an\n        httpx.Timeout overrides them. It bounds the HTTP request only and is\n        never sent as a query variable - to pass a query variable literally\n        named \"timeout\" use variables={\"timeout\": ...}.\n    kwargs : dict\n        Key value arguments used as variables\n\n    Returns\n    -------\n    Parsed evaluation result\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    \"\"\"\n    return self.xquery(\n        code,\n        variables=variables,\n        database=database,\n        txid=txid,\n        output_type=output_type,\n        timeout=timeout,\n        **kwargs,\n    )\n</code></pre>"},{"location":"reference/mlclient/services/LogLevelService/","title":"LogLevelService","text":"<pre><code>from mlclient.services import LogLevelService\n</code></pre>"},{"location":"reference/mlclient/services/LogLevelService/#mlclient.services.LogLevelService","title":"mlclient.services.LogLevelService","text":"<pre><code>LogLevelService(rest: RestApi, manage: ManageApi)\n</code></pre> <p>Get or set a group/App-Server log level, eval-first with Manage fallback.</p> <p>Initialize the service with REST and Manage API handles.</p> <p>Parameters:</p> Name Type Description Default <code>rest</code> <code>RestApi</code> <p>Connected REST API used for Admin-module evaluation</p> required <code>manage</code> <code>ManageApi</code> <p>Management API used when eval lacks privileges</p> required Source code in <code>mlclient/services/log_level.py</code> <pre><code>def __init__(\n    self,\n    rest: RestApi,\n    manage: ManageApi,\n):\n    \"\"\"Initialize the service with REST and Manage API handles.\n\n    Parameters\n    ----------\n    rest : RestApi\n        Connected REST API used for Admin-module evaluation\n    manage : ManageApi\n        Management API used when eval lacks privileges\n    \"\"\"\n    self._rest = rest\n    self._manage = manage\n</code></pre>"},{"location":"reference/mlclient/services/LogLevelService/#mlclient.services.LogLevelService.get","title":"get","text":"<pre><code>get(\n    *,\n    group: str = \"Default\",\n    server: str | None = None,\n    log_type: str = \"file\",\n    timeout=UNSET,\n) -&gt; str\n</code></pre> <p>Return the current log level, falling back to Manage on privilege errors.</p> <p>Parameters:</p> Name Type Description Default <code>group</code> <code>str</code> <p>The group whose log level is read, or that owns the App Server.</p> <code>\"Default\"</code> <code>server</code> <code>str | None</code> <p>An App Server name. When omitted the group log level is read.</p> <code>None</code> <code>log_type</code> <code>str</code> <p>Either \"file\" or \"system\". App Servers expose only \"file\".</p> <code>\"file\"</code> <code>timeout</code> <code>Timeout | float | None</code> <p>Per-request HTTP timeout. Unset preserves each client's configured timeout; None disables it; a number applies to all four components; an httpx.Timeout sets them independently. An explicit override also applies to any Manage fallback request, not to the whole operation.</p> <code>unset</code> <p>Returns:</p> Type Description <code>str</code> <p>The current log level.</p> <p>Raises:</p> Type Description <code>WrongParametersError</code> <p>If the requested log type, target combination or level is invalid</p> <code>MarkLogicError</code> <p>If eval fails without a privilege error, or Management fallback fails</p> <code>RequestError</code> <p>If HTTP transport fails; transport errors do not trigger fallback</p> Source code in <code>mlclient/services/log_level.py</code> <pre><code>def get(\n    self,\n    *,\n    group: str = \"Default\",\n    server: str | None = None,\n    log_type: str = \"file\",\n    timeout=UNSET,\n) -&gt; str:\n    \"\"\"Return the current log level, falling back to Manage on privilege errors.\n\n    Parameters\n    ----------\n    group : str, default \"Default\"\n        The group whose log level is read, or that owns the App Server.\n    server : str | None, default None\n        An App Server name. When omitted the group log level is read.\n    log_type : str, default \"file\"\n        Either \"file\" or \"system\". App Servers expose only \"file\".\n    timeout : httpx.Timeout | float | None, default unset\n        Per-request HTTP timeout. Unset preserves each client's configured\n        timeout; None disables it; a number applies to all four components;\n        an httpx.Timeout sets them independently. An explicit override also\n        applies to any Manage fallback request, not to the whole operation.\n\n    Returns\n    -------\n    str\n        The current log level.\n\n    Raises\n    ------\n    WrongParametersError\n        If the requested log type, target combination or level is invalid\n    MarkLogicError\n        If eval fails without a privilege error, or Management fallback fails\n    RequestError\n        If HTTP transport fails; transport errors do not trigger fallback\n    \"\"\"\n    self._validate(server, log_type, level=None)\n    logger.debug(\"Log-level get: attempting eval using the Admin module\")\n    try:\n        resp = self._rest.eval.post(\n            xquery=self._get_code(server, log_type),\n            variables=self._vars(group, server),\n            timeout=timeout,\n        )\n    except RequestError as exc:\n        logger.debug(\"Log-level eval transport failure; no fallback: %s\", exc)\n        raise\n    if resp.is_success:\n        logger.debug(\"Log-level eval succeeded\")\n        return MLResponseParser.parse(resp)\n    _raise_unless_privilege_error(resp)\n    return self._manage_get(group, server, log_type, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/services/LogLevelService/#mlclient.services.LogLevelService.set","title":"set","text":"<pre><code>set(\n    level: str,\n    *,\n    group: str = \"Default\",\n    server: str | None = None,\n    log_type: str = \"file\",\n    timeout=UNSET,\n) -&gt; str\n</code></pre> <p>Change the log level, falling back to Manage on privilege errors.</p> <p>Parameters:</p> Name Type Description Default <code>level</code> <code>str</code> <p>The new log level. Must be one of :data:<code>SUPPORTED_LEVELS</code>.</p> required <code>group</code> <code>str</code> <p>The group whose log level is changed, or that owns the App Server.</p> <code>\"Default\"</code> <code>server</code> <code>str | None</code> <p>An App Server name. When omitted the group log level is changed.</p> <code>None</code> <code>log_type</code> <code>str</code> <p>Either \"file\" or \"system\". App Servers expose only \"file\".</p> <code>\"file\"</code> <code>timeout</code> <code>Timeout | float | None</code> <p>Per-request HTTP timeout. Unset preserves each client's configured timeout; None disables it; a number applies to all four components; an httpx.Timeout sets them independently. An explicit override also applies to any Manage fallback request, not to the whole operation.</p> <code>unset</code> <p>Returns:</p> Type Description <code>str</code> <p>The level that was set.</p> <p>Raises:</p> Type Description <code>WrongParametersError</code> <p>If the requested log type, target combination or level is invalid</p> <code>MarkLogicError</code> <p>If eval fails without a privilege error, or Management fallback fails</p> <code>RequestError</code> <p>If HTTP transport fails; transport errors do not trigger fallback</p> Source code in <code>mlclient/services/log_level.py</code> <pre><code>def set(\n    self,\n    level: str,\n    *,\n    group: str = \"Default\",\n    server: str | None = None,\n    log_type: str = \"file\",\n    timeout=UNSET,\n) -&gt; str:\n    \"\"\"Change the log level, falling back to Manage on privilege errors.\n\n    Parameters\n    ----------\n    level : str\n        The new log level. Must be one of :data:`SUPPORTED_LEVELS`.\n    group : str, default \"Default\"\n        The group whose log level is changed, or that owns the App Server.\n    server : str | None, default None\n        An App Server name. When omitted the group log level is changed.\n    log_type : str, default \"file\"\n        Either \"file\" or \"system\". App Servers expose only \"file\".\n    timeout : httpx.Timeout | float | None, default unset\n        Per-request HTTP timeout. Unset preserves each client's configured\n        timeout; None disables it; a number applies to all four components;\n        an httpx.Timeout sets them independently. An explicit override also\n        applies to any Manage fallback request, not to the whole operation.\n\n    Returns\n    -------\n    str\n        The level that was set.\n\n    Raises\n    ------\n    WrongParametersError\n        If the requested log type, target combination or level is invalid\n    MarkLogicError\n        If eval fails without a privilege error, or Management fallback fails\n    RequestError\n        If HTTP transport fails; transport errors do not trigger fallback\n    \"\"\"\n    self._validate(server, log_type, level=level)\n    logger.debug(\"Log-level set: attempting eval using the Admin module\")\n    try:\n        resp = self._rest.eval.post(\n            xquery=self._set_code(server, log_type),\n            variables=self._vars(group, server, level),\n            timeout=timeout,\n        )\n    except RequestError as exc:\n        logger.debug(\"Log-level eval transport failure; no fallback: %s\", exc)\n        raise\n    if resp.is_success:\n        logger.debug(\"Log-level eval succeeded\")\n        return level\n    _raise_unless_privilege_error(resp)\n    return self._manage_set(group, server, log_type, level, timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/services/LogsService/","title":"LogsService","text":"<pre><code>from mlclient.services import LogsService\n</code></pre>"},{"location":"reference/mlclient/services/LogsService/#mlclient.services.LogsService","title":"mlclient.services.LogsService","text":"<pre><code>LogsService(manage: ManageApi)\n</code></pre> <p>Higher-level service for /manage/v2/logs endpoint.</p> Source code in <code>mlclient/services/logs.py</code> <pre><code>def __init__(self, manage: ManageApi):\n    self._manage = manage\n</code></pre>"},{"location":"reference/mlclient/services/LogsService/#mlclient.services.LogsService.get","title":"get","text":"<pre><code>get(\n    app_server: int | str | None = None,\n    log_type: LogType | str = LogType.ERROR,\n    *,\n    start_time: str | None = None,\n    end_time: str | None = None,\n    regex: str | None = None,\n    host: str | None = None,\n    timeout=UNSET,\n) -&gt; Iterator[dict]\n</code></pre> <p>Return logs from a MarkLogic server.</p> <p>Parameters:</p> Name Type Description Default <code>app_server</code> <code>int | str | None</code> <p>An app server (port) with logs to retrieve</p> <code>None</code> <code>log_type</code> <code>LogType | str</code> <p>A log type (enum or string: \"error\", \"access\", \"request\", \"audit\")</p> <code>LogType.ERROR</code> <code>start_time</code> <code>str | None</code> <p>A start time to search error logs</p> <code>None</code> <code>end_time</code> <code>str | None</code> <p>An end time to search error logs</p> <code>None</code> <code>regex</code> <code>str | None</code> <p>A regex to search error logs</p> <code>None</code> <code>host</code> <code>str | None</code> <p>A host name with logs to retrieve</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them.</p> <code>unset</code> <p>Returns:</p> Type Description <code>Iterator[dict]</code> <p>A log details generator.</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> <code>MarkLogicError</code> <p>If MarkLogic returns an error</p> Source code in <code>mlclient/services/logs.py</code> <pre><code>def get(\n    self,\n    app_server: int | str | None = None,\n    log_type: LogType | str = LogType.ERROR,\n    *,\n    start_time: str | None = None,\n    end_time: str | None = None,\n    regex: str | None = None,\n    host: str | None = None,\n    timeout=UNSET,\n) -&gt; Iterator[dict]:\n    \"\"\"Return logs from a MarkLogic server.\n\n    Parameters\n    ----------\n    app_server : int | str | None, default None\n        An app server (port) with logs to retrieve\n    log_type : LogType | str, default LogType.ERROR\n        A log type (enum or string: \"error\", \"access\", \"request\", \"audit\")\n    start_time : str | None, default None\n        A start time to search error logs\n    end_time : str | None, default None\n        An end time to search error logs\n    regex : str | None, default None\n        A regex to search error logs\n    host : str | None, default None\n        A host name with logs to retrieve\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them.\n\n    Returns\n    -------\n    Iterator[dict]\n        A log details generator.\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    MarkLogicError\n        If MarkLogic returns an error\n    \"\"\"\n    if isinstance(log_type, str):\n        log_type = LogType.get(log_type)\n    is_error = log_type == LogType.ERROR\n    resp = self._manage.logs.get(\n        self._filename(app_server, log_type),\n        data_format=\"json\",\n        host=host,\n        start_time=start_time if is_error else None,\n        end_time=end_time if is_error else None,\n        regex=regex if is_error else None,\n        timeout=timeout,\n    )\n    MLResponseParser.raise_for_status(resp)\n    return self._parse_logs(log_type, resp.json())\n</code></pre>"},{"location":"reference/mlclient/services/LogsService/#mlclient.services.LogsService.list","title":"list","text":"<pre><code>list(host: str | None = None, *, timeout=UNSET) -&gt; dict\n</code></pre> <p>Return a logs list from a MarkLogic server.</p> <p>Parameters:</p> Name Type Description Default <code>host</code> <code>str | None</code> <p>A host name with log files to retrieve</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout for this call. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them.</p> <code>unset</code> <p>Returns:</p> Type Description <code>dict</code> <p>A parsed list of log files in the MarkLogic server</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> <code>MarkLogicError</code> <p>If MarkLogic returns an error</p> Source code in <code>mlclient/services/logs.py</code> <pre><code>def list(\n    self,\n    host: str | None = None,\n    *,\n    timeout=UNSET,\n) -&gt; dict:\n    \"\"\"Return a logs list from a MarkLogic server.\n\n    Parameters\n    ----------\n    host : str | None, default None\n        A host name with log files to retrieve\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout for this call. Unset uses the client's\n        configured timeout; None disables every HTTP timeout; a number sets\n        all four components to that many seconds; an httpx.Timeout overrides\n        them.\n\n    Returns\n    -------\n    dict\n        A parsed list of log files in the MarkLogic server\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    MarkLogicError\n        If MarkLogic returns an error\n    \"\"\"\n    resp = self._manage.logs.get(\n        filename=None,\n        data_format=\"json\",\n        host=host,\n        timeout=timeout,\n    )\n    resp_body = resp.json()\n    if \"errorResponse\" in resp_body:\n        raise MarkLogicError(resp_body[\"errorResponse\"])\n\n    return self._parse_logs_list(resp_body)\n</code></pre>"},{"location":"reference/mlclient/services/TraceEvents/","title":"TraceEvents","text":"<pre><code>from mlclient.services import TraceEvents\n</code></pre>"},{"location":"reference/mlclient/services/TraceEvents/#mlclient.services.TraceEvents","title":"mlclient.services.TraceEvents  <code>dataclass</code>","text":"<pre><code>TraceEvents(activated: bool, events: tuple[str, ...])\n</code></pre> <p>A group's trace-event state.</p> <p>Attributes:</p> Name Type Description <code>activated</code> <code>bool</code> <p>Whether the group's master trace-events switch is on.</p> <code>events</code> <code>tuple[str, ...]</code> <p>The names of the enabled trace events, sorted alphabetically.</p>"},{"location":"reference/mlclient/services/TraceEventsService/","title":"TraceEventsService","text":"<pre><code>from mlclient.services import TraceEventsService\n</code></pre>"},{"location":"reference/mlclient/services/TraceEventsService/#mlclient.services.TraceEventsService","title":"mlclient.services.TraceEventsService","text":"<pre><code>TraceEventsService(rest: RestApi)\n</code></pre> <p>Read the trace-event state of a group by evaluating the Admin module.</p> <p>Initialize the service with a REST API handle.</p> <p>Parameters:</p> Name Type Description Default <code>rest</code> <code>RestApi</code> <p>Connected REST API used for Admin-module evaluation</p> required Source code in <code>mlclient/services/trace_events.py</code> <pre><code>def __init__(\n    self,\n    rest: RestApi,\n):\n    \"\"\"Initialize the service with a REST API handle.\n\n    Parameters\n    ----------\n    rest : RestApi\n        Connected REST API used for Admin-module evaluation\n    \"\"\"\n    self._rest = rest\n</code></pre>"},{"location":"reference/mlclient/services/TraceEventsService/#mlclient.services.TraceEventsService.get","title":"get","text":"<pre><code>get(\n    *, group: str = \"Default\", timeout=UNSET\n) -&gt; TraceEvents\n</code></pre> <p>Return the group's trace-events activation and enabled events.</p> <p>Parameters:</p> Name Type Description Default <code>group</code> <code>str</code> <p>The group whose trace-event state is read.</p> <code>\"Default\"</code> <code>timeout</code> <code>Timeout | float | None</code> <p>Per-request HTTP timeout. Unset preserves the client's configured timeout; None disables it; a number applies to all four components; an httpx.Timeout sets them independently.</p> <code>unset</code> <p>Returns:</p> Type Description <code>TraceEvents</code> <p>The activation flag and the enabled trace-event names.</p> <p>Raises:</p> Type Description <code>RequestError</code> <p>If HTTP transport fails</p> Source code in <code>mlclient/services/trace_events.py</code> <pre><code>def get(\n    self,\n    *,\n    group: str = \"Default\",\n    timeout=UNSET,\n) -&gt; TraceEvents:\n    \"\"\"Return the group's trace-events activation and enabled events.\n\n    Parameters\n    ----------\n    group : str, default \"Default\"\n        The group whose trace-event state is read.\n    timeout : httpx.Timeout | float | None, default unset\n        Per-request HTTP timeout. Unset preserves the client's configured\n        timeout; None disables it; a number applies to all four components;\n        an httpx.Timeout sets them independently.\n\n    Returns\n    -------\n    TraceEvents\n        The activation flag and the enabled trace-event names.\n\n    Raises\n    ------\n    RequestError\n        If HTTP transport fails\n    \"\"\"\n    resp = self._eval(self._get_code(), {\"group\": group}, timeout)\n    state = MLResponseParser.parse(resp)\n    return TraceEvents(\n        activated=state[\"activated\"],\n        events=tuple(sorted(state[\"events\"])),\n    )\n</code></pre>"},{"location":"reference/mlclient/services/TraceEventsService/#mlclient.services.TraceEventsService.set_activated","title":"set_activated","text":"<pre><code>set_activated(\n    *, value: bool, group: str = \"Default\", timeout=UNSET\n) -&gt; TraceEvents\n</code></pre> <p>Turn the group's master trace-events switch on or off.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>bool</code> <p>The activation state to save.</p> required <code>group</code> <code>str</code> <p>The group whose switch is changed.</p> <code>\"Default\"</code> <code>timeout</code> <code>Timeout | float | None</code> <p>Per-request HTTP timeout applied to both the mutation and the follow-up read. Unset preserves the client's configured timeout; None disables it; a number applies to all four components; an httpx.Timeout sets them independently.</p> <code>unset</code> <p>Returns:</p> Type Description <code>TraceEvents</code> <p>The group's trace-event state after the change.</p> <p>Raises:</p> Type Description <code>RequestError</code> <p>If HTTP transport fails</p> Source code in <code>mlclient/services/trace_events.py</code> <pre><code>def set_activated(\n    self,\n    *,\n    value: bool,\n    group: str = \"Default\",\n    timeout=UNSET,\n) -&gt; TraceEvents:\n    \"\"\"Turn the group's master trace-events switch on or off.\n\n    Parameters\n    ----------\n    value : bool\n        The activation state to save.\n    group : str, default \"Default\"\n        The group whose switch is changed.\n    timeout : httpx.Timeout | float | None, default unset\n        Per-request HTTP timeout applied to both the mutation and the\n        follow-up read. Unset preserves the client's configured timeout;\n        None disables it; a number applies to all four components; an\n        httpx.Timeout sets them independently.\n\n    Returns\n    -------\n    TraceEvents\n        The group's trace-event state after the change.\n\n    Raises\n    ------\n    RequestError\n        If HTTP transport fails\n    \"\"\"\n    variables = {\"group\": group, \"value\": \"true\" if value else \"false\"}\n    return self._mutate(self._set_activated_code(), variables, group, timeout)\n</code></pre>"},{"location":"reference/mlclient/services/TraceEventsService/#mlclient.services.TraceEventsService.set_event","title":"set_event","text":"<pre><code>set_event(\n    event: str,\n    *,\n    enabled: bool,\n    group: str = \"Default\",\n    timeout=UNSET,\n) -&gt; TraceEvents\n</code></pre> <p>Add or remove a single trace event from the group's enabled set.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>str</code> <p>The trace-event name to enable or disable.</p> required <code>enabled</code> <code>bool</code> <p>True adds the event to the enabled set; False removes it.</p> required <code>group</code> <code>str</code> <p>The group whose enabled events are changed.</p> <code>\"Default\"</code> <code>timeout</code> <code>Timeout | float | None</code> <p>Per-request HTTP timeout applied to both the mutation and the follow-up read. Unset preserves the client's configured timeout; None disables it; a number applies to all four components; an httpx.Timeout sets them independently.</p> <code>unset</code> <p>Returns:</p> Type Description <code>TraceEvents</code> <p>The group's trace-event state after the change.</p> <p>Raises:</p> Type Description <code>RequestError</code> <p>If HTTP transport fails</p> Source code in <code>mlclient/services/trace_events.py</code> <pre><code>def set_event(\n    self,\n    event: str,\n    *,\n    enabled: bool,\n    group: str = \"Default\",\n    timeout=UNSET,\n) -&gt; TraceEvents:\n    \"\"\"Add or remove a single trace event from the group's enabled set.\n\n    Parameters\n    ----------\n    event : str\n        The trace-event name to enable or disable.\n    enabled : bool\n        True adds the event to the enabled set; False removes it.\n    group : str, default \"Default\"\n        The group whose enabled events are changed.\n    timeout : httpx.Timeout | float | None, default unset\n        Per-request HTTP timeout applied to both the mutation and the\n        follow-up read. Unset preserves the client's configured timeout;\n        None disables it; a number applies to all four components; an\n        httpx.Timeout sets them independently.\n\n    Returns\n    -------\n    TraceEvents\n        The group's trace-event state after the change.\n\n    Raises\n    ------\n    RequestError\n        If HTTP transport fails\n    \"\"\"\n    variables = {\"group\": group, \"event\": event}\n    return self._mutate(self._set_event_code(enabled), variables, group, timeout)\n</code></pre>"},{"location":"reference/mlclient/services/TransactionService/","title":"TransactionService","text":"<pre><code>from mlclient.services import TransactionService\n</code></pre>"},{"location":"reference/mlclient/services/TransactionService/#mlclient.services.TransactionService","title":"mlclient.services.TransactionService","text":"<pre><code>TransactionService(\n    api: TransactionsApi,\n    txid: str,\n    database: str | None = None,\n)\n</code></pre> <p>A single open multi-statement transaction on MarkLogic.</p> <p>Acts as a context manager: commits on a clean exit, rolls back if the block raises. Unpacks (<code>**</code>) into <code>txid</code> and <code>database</code> keyword arguments so it can be spread into content operations (eval, documents) that must run within the transaction and against the same database it was opened on.</p> Source code in <code>mlclient/services/transactions.py</code> <pre><code>def __init__(self, api: TransactionsApi, txid: str, database: str | None = None):\n    self._api = api\n    self._txid = txid\n    self._database = database\n</code></pre>"},{"location":"reference/mlclient/services/TransactionService/#mlclient.services.TransactionService.database","title":"database  <code>property</code>","text":"<pre><code>database: str | None\n</code></pre> <p>The content database the transaction was opened against.</p>"},{"location":"reference/mlclient/services/TransactionService/#mlclient.services.TransactionService.id","title":"id  <code>property</code>","text":"<pre><code>id: str\n</code></pre> <p>The server-assigned transaction id.</p>"},{"location":"reference/mlclient/services/TransactionService/#mlclient.services.TransactionService.commit","title":"commit","text":"<pre><code>commit(*, timeout=UNSET) -&gt; None\n</code></pre> <p>Commit the transaction.</p> <p>Parameters:</p> Name Type Description Default <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout for the commit request. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. This bounds the HTTP request only and is unrelated to the transaction's server-side time_limit.</p> <code>unset</code> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> <code>MarkLogicError</code> <p>If MarkLogic returns an error</p> Source code in <code>mlclient/services/transactions.py</code> <pre><code>def commit(self, *, timeout=UNSET) -&gt; None:\n    \"\"\"Commit the transaction.\n\n    Parameters\n    ----------\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout for the commit request. Unset uses the\n        client's configured timeout; None disables every HTTP timeout; a\n        number sets all four components to that many seconds; an\n        httpx.Timeout overrides them. This bounds the HTTP request only and\n        is unrelated to the transaction's server-side time_limit.\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    MarkLogicError\n        If MarkLogic returns an error\n    \"\"\"\n    self._finish(\"commit\", timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/services/TransactionService/#mlclient.services.TransactionService.keys","title":"keys","text":"<pre><code>keys() -&gt; Iterable[str]\n</code></pre> <p>Return the keys exposed for <code>**</code> unpacking (txid, database if set).</p> Source code in <code>mlclient/services/transactions.py</code> <pre><code>def keys(self) -&gt; Iterable[str]:\n    \"\"\"Return the keys exposed for ``**`` unpacking (txid, database if set).\"\"\"\n    if self._database is None:\n        return (\"txid\",)\n    return (\"txid\", \"database\")\n</code></pre>"},{"location":"reference/mlclient/services/TransactionService/#mlclient.services.TransactionService.rollback","title":"rollback","text":"<pre><code>rollback(*, timeout=UNSET) -&gt; None\n</code></pre> <p>Roll back the transaction.</p> <p>Parameters:</p> Name Type Description Default <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout for the rollback request. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. This bounds the HTTP request only and is unrelated to the transaction's server-side time_limit.</p> <code>unset</code> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> <code>MarkLogicError</code> <p>If MarkLogic returns an error</p> Source code in <code>mlclient/services/transactions.py</code> <pre><code>def rollback(self, *, timeout=UNSET) -&gt; None:\n    \"\"\"Roll back the transaction.\n\n    Parameters\n    ----------\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout for the rollback request. Unset uses the\n        client's configured timeout; None disables every HTTP timeout; a\n        number sets all four components to that many seconds; an\n        httpx.Timeout overrides them. This bounds the HTTP request only and\n        is unrelated to the transaction's server-side time_limit.\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    MarkLogicError\n        If MarkLogic returns an error\n    \"\"\"\n    self._finish(\"rollback\", timeout=timeout)\n</code></pre>"},{"location":"reference/mlclient/services/TransactionService/#mlclient.services.TransactionService.status","title":"status","text":"<pre><code>status(*, data_format: str = 'json', timeout=UNSET)\n</code></pre> <p>Return the status of the transaction.</p> <p>Parameters:</p> Name Type Description Default <code>data_format</code> <code>str</code> <p>The format of the returned status; either json or xml</p> <code>\"json\"</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout for the status request. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. This bounds the HTTP request only and is unrelated to the transaction's server-side time_limit.</p> <code>unset</code> <p>Returns:</p> Type Description <code>The parsed transaction status</code> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> <code>MarkLogicError</code> <p>If MarkLogic returns an error</p> Source code in <code>mlclient/services/transactions.py</code> <pre><code>def status(self, *, data_format: str = \"json\", timeout=UNSET):\n    \"\"\"Return the status of the transaction.\n\n    Parameters\n    ----------\n    data_format : str, default \"json\"\n        The format of the returned status; either json or xml\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout for the status request. Unset uses the\n        client's configured timeout; None disables every HTTP timeout; a\n        number sets all four components to that many seconds; an\n        httpx.Timeout overrides them. This bounds the HTTP request only and\n        is unrelated to the transaction's server-side time_limit.\n\n    Returns\n    -------\n    The parsed transaction status\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    MarkLogicError\n        If MarkLogic returns an error\n    \"\"\"\n    resp = self._api.get(\n        self._txid,\n        data_format=data_format,\n        database=self._database,\n        timeout=timeout,\n    )\n    MLResponseParser.raise_for_status(resp)\n    return MLResponseParser.parse(resp)\n</code></pre>"},{"location":"reference/mlclient/services/async_open_transaction/","title":"async_open_transaction","text":"<pre><code>from mlclient.services import async_open_transaction\n</code></pre>"},{"location":"reference/mlclient/services/async_open_transaction/#mlclient.services.async_open_transaction","title":"mlclient.services.async_open_transaction  <code>async</code>","text":"<pre><code>async_open_transaction(\n    api: AsyncApiClient,\n    *,\n    name: str | None = None,\n    time_limit: int | None = None,\n    database: str | None = None,\n    timeout=UNSET,\n) -&gt; AsyncTransactionService\n</code></pre> <p>Open a multi-statement transaction and return a service scoped to it.</p> <p>Parameters:</p> Name Type Description Default <code>api</code> <code>AsyncApiClient</code> <p>The client used both to open the transaction and by the returned service</p> required <code>name</code> <code>str | None</code> <p>A name to assign to the transaction</p> <code>None</code> <code>time_limit</code> <code>int | None</code> <p>The maximum number of seconds for the transaction to remain open</p> <code>None</code> <code>database</code> <code>str | None</code> <p>Content database name or id to open the transaction against</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout for the open request only. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. This bounds the HTTP request that opens the transaction and is unrelated to time_limit, the server-side lifetime of the transaction itself.</p> <code>unset</code> <p>Returns:</p> Type Description <code>AsyncTransactionService</code> <p>A service scoped to the newly opened transaction</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> <code>MarkLogicError</code> <p>If MarkLogic returns an error</p> Source code in <code>mlclient/services/transactions.py</code> <pre><code>async def async_open_transaction(\n    api: AsyncApiClient,\n    *,\n    name: str | None = None,\n    time_limit: int | None = None,\n    database: str | None = None,\n    timeout=UNSET,\n) -&gt; AsyncTransactionService:\n    \"\"\"Open a multi-statement transaction and return a service scoped to it.\n\n    Parameters\n    ----------\n    api : AsyncApiClient\n        The client used both to open the transaction and by the returned service\n    name : str | None, default None\n        A name to assign to the transaction\n    time_limit : int | None, default None\n        The maximum number of seconds for the transaction to remain open\n    database : str | None, default None\n        Content database name or id to open the transaction against\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout for the open request only. Unset uses the\n        client's configured timeout; None disables every HTTP timeout; a number\n        sets all four components to that many seconds; an httpx.Timeout\n        overrides them. This bounds the HTTP request that opens the transaction\n        and is unrelated to time_limit, the server-side lifetime of the\n        transaction itself.\n\n    Returns\n    -------\n    AsyncTransactionService\n        A service scoped to the newly opened transaction\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    MarkLogicError\n        If MarkLogic returns an error\n    \"\"\"\n    transactions = AsyncTransactionsApi(api)\n    resp = await transactions.create(\n        name=name,\n        time_limit=time_limit,\n        database=database,\n        timeout=timeout,\n    )\n    txid = _txid_or_raise(resp)\n    return AsyncTransactionService(transactions, txid, database)\n</code></pre>"},{"location":"reference/mlclient/services/open_transaction/","title":"open_transaction","text":"<pre><code>from mlclient.services import open_transaction\n</code></pre>"},{"location":"reference/mlclient/services/open_transaction/#mlclient.services.open_transaction","title":"mlclient.services.open_transaction","text":"<pre><code>open_transaction(\n    api: ApiClient,\n    *,\n    name: str | None = None,\n    time_limit: int | None = None,\n    database: str | None = None,\n    timeout=UNSET,\n) -&gt; TransactionService\n</code></pre> <p>Open a multi-statement transaction and return a service scoped to it.</p> <p>Parameters:</p> Name Type Description Default <code>api</code> <code>ApiClient</code> <p>The client used both to open the transaction and by the returned service</p> required <code>name</code> <code>str | None</code> <p>A name to assign to the transaction</p> <code>None</code> <code>time_limit</code> <code>int | None</code> <p>The maximum number of seconds for the transaction to remain open</p> <code>None</code> <code>database</code> <code>str | None</code> <p>Content database name or id to open the transaction against</p> <code>None</code> <code>timeout</code> <code>Timeout | float | None</code> <p>A per-request HTTP timeout for the open request only. Unset uses the client's configured timeout; None disables every HTTP timeout; a number sets all four components to that many seconds; an httpx.Timeout overrides them. This bounds the HTTP request that opens the transaction and is unrelated to time_limit, the server-side lifetime of the transaction itself.</p> <code>unset</code> <p>Returns:</p> Type Description <code>TransactionService</code> <p>A service scoped to the newly opened transaction</p> <p>Raises:</p> Type Description <code>TimeoutException</code> <p>If an HTTP connect, read, write or pool timeout expires after any configured retries are exhausted.</p> <code>MarkLogicError</code> <p>If MarkLogic returns an error</p> Source code in <code>mlclient/services/transactions.py</code> <pre><code>def open_transaction(\n    api: ApiClient,\n    *,\n    name: str | None = None,\n    time_limit: int | None = None,\n    database: str | None = None,\n    timeout=UNSET,\n) -&gt; TransactionService:\n    \"\"\"Open a multi-statement transaction and return a service scoped to it.\n\n    Parameters\n    ----------\n    api : ApiClient\n        The client used both to open the transaction and by the returned service\n    name : str | None, default None\n        A name to assign to the transaction\n    time_limit : int | None, default None\n        The maximum number of seconds for the transaction to remain open\n    database : str | None, default None\n        Content database name or id to open the transaction against\n    timeout : httpx.Timeout | float | None, default unset\n        A per-request HTTP timeout for the open request only. Unset uses the\n        client's configured timeout; None disables every HTTP timeout; a number\n        sets all four components to that many seconds; an httpx.Timeout\n        overrides them. This bounds the HTTP request that opens the transaction\n        and is unrelated to time_limit, the server-side lifetime of the\n        transaction itself.\n\n    Returns\n    -------\n    TransactionService\n        A service scoped to the newly opened transaction\n\n    Raises\n    ------\n    httpx.TimeoutException\n        If an HTTP connect, read, write or pool timeout expires after any\n        configured retries are exhausted.\n    MarkLogicError\n        If MarkLogic returns an error\n    \"\"\"\n    transactions = TransactionsApi(api)\n    resp = transactions.create(\n        name=name,\n        time_limit=time_limit,\n        database=database,\n        timeout=timeout,\n    )\n    txid = _txid_or_raise(resp)\n    return TransactionService(transactions, txid, database)\n</code></pre>"},{"location":"user/cli/","title":"Command line interface","text":"<p>Use <code>ml</code> to inspect an environment, evaluate code, work with logs or send a raw HTTP request. Every command has <code>--help</code>; use <code>-vv</code> for diagnostic logs.</p>"},{"location":"user/cli/#connect-a-project","title":"Connect a project","text":"<p>For a new project, run <code>ml env init</code> and review the generated connection settings. For an ml-gradle project, import the properties already present:</p> <pre><code>ml env init --from-gradle=dev\nml version -e dev\n</code></pre> <p>This merges <code>gradle.properties</code> and <code>gradle-dev.properties</code>. It reads configuration; it does not run Gradle or deploy the project.</p> <p>For a running server, discovery can populate connections from its App Servers:</p> <pre><code>ml env init local --from-host=localhost:8002 --interactive\nml env show local\n</code></pre> <p>Supply the host's credentials when prompted. Discovery uses the Manage API and needs permission to read server configuration. One environment can contain many App Server entries; review the discovered identifiers before using them. See env init for discovery filters and configuration details.</p>"},{"location":"user/cli/#run-everyday-commands","title":"Run everyday commands","text":"<pre><code>ml eval -x '\"Hello World!\"'\nml http get /v1/documents uri=/example.json\nml logs -s 8002\nml log-level\nml health\nml version\n</code></pre> <p>The <code>local</code> environment is the default. Use <code>-e dev</code> for another environment. A connection selector chooses where the request is sent; an operation target chooses what the server acts on. The distinction is explained below and on each command page.</p>"},{"location":"user/cli/#find-a-command","title":"Find a command","text":"Task Command Create or inspect project configuration <code>ml env</code>, with <code>init</code> and <code>show</code> Run XQuery or JavaScript <code>ml eval</code> Send an HTTP request <code>ml http</code> Read server logs <code>ml logs</code> Inspect or change a log level <code>ml log-level</code> Inspect or change diagnostic trace events <code>ml trace-events</code> Check server health, once or continuously <code>ml health</code> Read the MarkLogic version <code>ml version</code> <p>Use <code>ml --help</code> for global options and <code>ml env init --help</code> (or another command) for its arguments. The pages above explain workflows, defaults and examples.</p>"},{"location":"user/cli/#connection-and-target-selection","title":"Connection and target selection","text":"<p><code>http</code>, <code>eval</code>, <code>version</code> and <code>log-level</code> use <code>-c / --connection</code> to select a configured connection identifier or a TCP port. A numeric port changes the default REST connection's port and retains its other settings.</p> <p><code>logs -s / --server</code> selects whose logs to read by an environment identifier or port. <code>log-level -s / --server</code> instead takes the actual App Server name in MarkLogic. It does not select the connection used for the request.</p> <p>Server commands default to the <code>local</code> environment; <code>-e / --environment</code> selects another environment.</p>"},{"location":"user/cli/#global-options","title":"Global options","text":"<p>These options apply to every command. Command-specific flags are described on the corresponding command page.</p>"},{"location":"user/cli/#-help-h","title":"<code>--help</code>, <code>-h</code>","text":"<p>Display help for a command, for example <code>ml env init --help</code>.</p>"},{"location":"user/cli/#-version-v","title":"<code>--version</code>, <code>-V</code>","text":"<p>Print the MLClient package version. Use <code>ml version</code> for the MarkLogic Server version.</p>"},{"location":"user/cli/#-quiet-q","title":"<code>--quiet</code>, <code>-q</code>","text":"<p>Suppress command output.</p>"},{"location":"user/cli/#-verbose-v","title":"<code>--verbose</code>, <code>-v</code>","text":"<p>Increase verbosity; repeat as <code>-vv</code> or <code>-vvv</code> for more detail.</p>"},{"location":"user/cli/#-ansi-and-no-ansi","title":"<code>--ansi</code> and <code>--no-ansi</code>","text":"<p>Force or disable terminal styling. Disabling ANSI also prevents in-place repainting in <code>ml health</code>.</p>"},{"location":"user/cli/#-no-interaction-n","title":"<code>--no-interaction</code>, <code>-n</code>","text":"<p>Do not prompt for input. Supply the required values explicitly when scripting.</p>"},{"location":"user/clients/","title":"Clients","text":"<p>Start with the quickstart to connect and evaluate a query. Use a context manager so connections close even when an operation fails. Choose <code>MLClient</code> for synchronous scripts or <code>AsyncMLClient</code> inside async code. These clients are facades combining services and several APIs. You can also use a single API directly.</p>"},{"location":"user/clients/#imports","title":"Imports","text":"<p>Import the main clients from <code>mlclient</code>:</p> <pre><code>from mlclient import MLClient, AsyncMLClient\n</code></pre> <p>Additional types are grouped by purpose:</p> Module Examples <code>mlclient.env</code> <code>MLEnvironment</code>, <code>MLServerConfig</code> <code>mlclient.http</code> <code>HTTPConfig</code>, <code>UNSET</code>, retry and timeout defaults <code>mlclient.auth</code> <code>AuthConfig</code>, <code>OAuthBearerAuth</code> <code>mlclient.connection</code> <code>SSLConfig</code>, <code>CloudConfig</code> <code>mlclient.models</code> <code>Document</code>, <code>Metadata</code>, <code>Category</code>, <code>MarkLogicVersion</code> <code>mlclient.clients</code> <code>HttpClient</code>, <code>ApiClient</code> and their async counterparts <code>mlclient.api</code> <code>RestApi</code>, <code>DocumentsApi</code> and other endpoint wrappers <code>mlclient.calls</code> <code>ApiCall</code>, <code>EvalCall</code> and other request objects <code>mlclient.services</code> <code>DocumentsService</code>, <code>TransactionService</code> <code>mlclient.responses</code> <code>MLResponseParser</code> <code>mlclient.io</code> <code>DocumentsLoader</code>, <code>DocumentsWriter</code> <code>mlclient.exceptions</code> <code>MarkLogicError</code>, <code>ConfigError</code> <p>The API reference shows the import for every public class and function. Modules starting with <code>_</code> contain internal code. Use <code>httpx.Timeout</code>, <code>httpx.Limits</code>, and <code>httpx_retries.Retry</code> directly when customizing the corresponding HTTP settings.</p>"},{"location":"user/clients/#entry-points-at-a-glance","title":"Entry points at a glance","text":"<p>Each entry point differs on two independent axes: how much it parses for you, and which MarkLogic server it targets. The two sections after this table explain each axis.</p> Entry point Returns Server (port) Reach for it when <code>ml.documents</code>, <code>ml.eval</code> Parsed values and models REST (main) You want Python values without handling HTTP yourself <code>ml.transaction()</code> Transaction context manager REST (main) You group operations to commit or roll back together <code>ml.rest</code> <code>httpx.Response</code> REST (main) Named <code>/v1/*</code> operations where you read the response <code>ml.manage</code> <code>httpx.Response</code> Manage (8002) Management API (<code>/manage/v2/*</code>) operations <code>ml.admin</code> <code>httpx.Response</code> Admin (8001) Admin API (<code>/admin/v1/*</code>) operations <code>ml.http</code> <code>httpx.Response</code> Main port, any path An endpoint without a wrapper, or a custom route <code>ml.parser</code> Parses a <code>httpx.Response</code> - You have a response and want the value a service would give <p><code>ml.transaction()</code> is a method, not a property: opening a transaction performs a request, so it returns a service scoped to that transaction rather than a shared one. See Transactions.</p>"},{"location":"user/clients/#abstraction-parsed-response-or-raw","title":"Abstraction: parsed, response, or raw","text":"<p>The client works at three levels of abstraction. Use the highest one that fits the task; you can mix them in one application.</p> <ul> <li>Services (<code>ml.documents</code>, <code>ml.eval</code>) parse the response into Python values   and document models.</li> <li>Wrappers (<code>ml.rest</code>, <code>ml.manage</code>, <code>ml.admin</code>) build the request for a named   endpoint and return the raw <code>httpx.Response</code>.</li> <li>Raw HTTP (<code>ml.http</code>) sends an arbitrary request on the primary connection.</li> </ul> <p>The three read the same document three different ways:</p> <pre><code>from mlclient import MLClient\n\nwith MLClient() as ml:  # Local development defaults; configure your credentials.\n    document = ml.documents.read(\"/example.json\")\n    rest_response = ml.rest.documents.get(uri=\"/example.json\")\n    http_response = ml.http.get(\"/v1/documents\", params={\"uri\": \"/example.json\"})\n</code></pre> <p><code>rest_response</code> and <code>http_response</code> are both <code>httpx.Response</code> objects, each from a separate request. The wrapper builds the request for you. Only <code>ml.documents.read</code> parses the body into a model. Reach for a service by default, drop to a wrapper or <code>ml.http</code> when you need the raw response, and parse it back when you want the parsed value without the service.</p>"},{"location":"user/clients/#server-tiers-and-ports","title":"Server tiers and ports","text":"<p>Each API targets a server connection. MLClient uses these ports by default:</p> Tier Port Endpoints Client (REST) API 8000/custom <code>/v1/*</code> Admin API 8001 <code>/admin/v1/*</code> Management API 8002 <code>/manage/v2/*</code> <p>Port 8000 is the default App-Services REST server. A custom REST App Server uses its configured port.</p> <p><code>ml.rest</code> uses the primary connection. By default, <code>.manage</code> uses port 8002 and <code>.admin</code> uses port 8001, inheriting the primary connection settings except for the port. Changing the primary port leaves these auxiliary defaults intact:</p> <pre><code>&gt;&gt;&gt; from mlclient import MLClient\n\n# Default port 8000 - .manage uses 8002, .admin uses 8001\n&gt;&gt;&gt; with MLClient() as ml:\n...     resp = ml.manage.databases.get_list()\n...     ts = ml.admin.get_timestamp()\n\n# Custom REST server on port 8040 - .rest uses 8040, .manage/.admin still 8002/8001\n&gt;&gt;&gt; with MLClient(port=8040) as ml:\n...     resp = ml.rest.eval.post(xquery=\"1\")\n...     dbs = ml.manage.databases.get_list()\n</code></pre> <p><code>ml.http</code> is the exception: it sends on the primary connection and the path does not select a port, so target Manage or Admin by connecting to their port.</p> <p>For session lifecycle and overriding Manage or Admin connection settings, see Connection and authentication.</p>"},{"location":"user/clients/#call-a-wrapper-rest-manage-admin","title":"Call a wrapper (REST, Manage, Admin)","text":"<p>Each wrapper exposes a named method per endpoint and returns an <code>httpx.Response</code>, so you read status codes, headers and bodies yourself. Methods take endpoint parameters as keyword arguments; the API reference lists each resource's signature.</p> <pre><code>&gt;&gt;&gt; from mlclient import MLClient\n\n&gt;&gt;&gt; with MLClient() as ml:\n...     query = ml.rest.eval.post(xquery=\"xdmp:database-name(xdmp:database())\")\n...     databases = ml.manage.databases.get_list()\n...     timestamp = ml.admin.get_timestamp()\n</code></pre>"},{"location":"user/clients/#use-one-api-without-the-facade","title":"Use one API without the facade","text":"<p>The individual layers can be used independently. If you only need Admin operations, compose AdminApi with an ApiClient and a HttpClient:</p> <pre><code>from mlclient.api import AdminApi\nfrom mlclient.clients import ApiClient, HttpClient\n\nwith HttpClient(port=8001) as http:\n    admin = AdminApi(ApiClient(http))\n    response = admin.get_timestamp()\n    print(response.text)\n</code></pre> <p><code>HttpClient</code> owns the connection; its context manager closes it. The API wrappers use that connection and do not select a port or create additional clients. <code>RestApi</code> and <code>ManageApi</code> compose in the same way, using their respective server configurations. Async code uses <code>AsyncHttpClient</code>, <code>AsyncApiClient</code> and the corresponding async wrapper, with <code>async with</code> and awaited operations.</p>"},{"location":"user/clients/#send-a-raw-request-mlhttp","title":"Send a raw request (<code>ml.http</code>)","text":"<p><code>ml.http</code> sends arbitrary requests on the primary connection. Strings and bytes are sent as raw content; a <code>dict</code> is JSON-encoded for a JSON content type and submitted as form data otherwise. Header names are case-insensitive.</p> <pre><code>&gt;&gt;&gt; from mlclient import MLClient\n\n# GET with parameters and headers\n&gt;&gt;&gt; with MLClient(port=8002) as ml:\n...     servers = ml.http.get(\n...         \"/manage/v2/servers\",\n...         params={\"format\": \"json\"},\n...         headers={\"custom-header\": \"custom-value\"},\n...     )\n\n# POST a body\n&gt;&gt;&gt; with MLClient() as ml:\n...     result = ml.http.post(\n...         \"/v1/eval\",\n...         {\"xquery\": \"fn:current-dateTime()\"},\n...         params={\"database\": \"Documents\"},\n...         headers={\"Content-Type\": \"application/x-www-form-urlencoded\"},\n...     )\n</code></pre> <p><code>ml.http</code> also has <code>put</code>, <code>delete</code> and a general <code>request</code> method with the same body handling.</p>"},{"location":"user/clients/#transactions","title":"Transactions","text":"<p>Group several operations so they commit or roll back together. <code>ml.transaction()</code> returns a context manager that commits on a clean exit and rolls back if the block raises; spread it with <code>**</code> into each operation to run that operation inside the transaction:</p> <pre><code>&gt;&gt;&gt; from mlclient import MLClient\n&gt;&gt;&gt; from mlclient.models import Document\n\n&gt;&gt;&gt; doc = Document.create(\"/doc-1.xml\", \"&lt;root&gt;data&lt;/root&gt;\")\n&gt;&gt;&gt; with MLClient() as ml:\n...     with ml.transaction() as txn:\n...         ml.documents.write(doc, **txn)\n...         ml.eval.xquery('xdmp:document-insert(\"/doc-2.xml\", &lt;root/&gt;)', **txn)\n</code></pre> <p>See the transactions guide for rollback, reading the id and status, manual commit, and what <code>**txn</code> unpacks to.</p> <p>To drive the lifecycle through the raw wrapper instead, <code>ml.rest.transactions</code> gives full control - create it, take the id from the <code>Location</code> header, thread it through each call, then commit or roll back:</p> <pre><code>&gt;&gt;&gt; with MLClient() as ml:\n...     location = ml.rest.transactions.create().headers[\"Location\"]\n...     txid = location.rsplit(\"/\", 1)[-1]\n...     try:\n...         ml.documents.write(doc, txid=txid)\n...         ml.rest.transactions.post(txid, result=\"commit\")\n...     except Exception:\n...         ml.rest.transactions.post(txid, result=\"rollback\")\n...         raise\n</code></pre>"},{"location":"user/clients/#parse-a-response","title":"Parse a response","text":"<p>Any wrapper or <code>ml.http</code> returns an <code>httpx.Response</code>. To get the parsed Python value a service would give - a <code>dict</code> for JSON, a <code>datetime</code> for <code>xs:dateTime</code>, and so on - hand it to <code>ml.parser</code>:</p> <pre><code>&gt;&gt;&gt; from mlclient import MLClient\n\n&gt;&gt;&gt; with MLClient() as ml:\n...     resp = ml.rest.eval.post(\n...         xquery=\"xdmp:database() =&gt; xdmp:database-name()\",\n...     )\n...     parsed = ml.parser.parse(resp)\n&gt;&gt;&gt; parsed\n'Documents'\n</code></pre>"},{"location":"user/clients/#pick-a-task","title":"Pick a task","text":"<ul> <li>Documents: content types, metadata, reading, writing and deletion.</li> <li>Evaluate code: XQuery, JavaScript and external variables.</li> <li>Transactions: commit or roll back related operations.</li> <li>Services: the parsed higher-level layer, and the off-facade logs and log-level services.</li> <li>Health, versions and administration: inspect the server, versions and restart waits.</li> </ul>"},{"location":"user/clients/#configure-and-extend","title":"Configure and extend","text":"<p>For credentials, TLS, Cloud and authentication choices, read Connection and authentication. For project configuration, and manager factories, read Environments. For transport options, start with HTTP configuration, then timeouts, retries or resource limits.</p> <p>Custom application APIs builds an endpoint wrapper and adds it to your own client without touching MLClient internals.</p> <p>The API reference contains complete signatures and docstrings. The <code>mlclient.jobs</code> package remains experimental; use document services for operations covered by the stability contract.</p>"},{"location":"user/environments/","title":"Environments","text":"<p>An environment stores a project's connection settings in a YAML file. Use the same file from Python and the CLI, without repeating credentials in each script.</p>"},{"location":"user/environments/#create-an-environment","title":"Create an environment","text":"<p>Save this as <code>.mlclient/mlclient-local.yaml</code> in your project:</p> <pre><code>app-name: my-app\nhost: localhost\nprotocol: http\nusername: admin\npassword: admin\nauth: digest\napp-servers:\n  - id: content\n    port: 8100\n    rest: true\n</code></pre> <p>This describes a local MarkLogic server with Digest authentication and one REST connection named <code>content</code> on a custom port. Set the host, credentials and port to match your server. <code>app-name</code> labels the application; <code>id</code> is the name you select the connection by, and <code>rest: true</code> makes it eligible as the default REST connection.</p> <p>Download this example, or use <code>ml env init</code> to create a file interactively.</p>"},{"location":"user/environments/#use-it-from-python","title":"Use it from Python","text":"<p><code>MLClientManager(\"local\")</code> loads the file and creates clients from its settings. Select a connection by the <code>id</code> from the YAML:</p> <pre><code>from mlclient import MLClientManager\n\nmanager = MLClientManager(\"local\")\nwith manager.get_client(\"content\") as ml:\n    print(ml.eval.xquery('\"Hello World!\"'))\n</code></pre> <p>The manager searches for <code>.mlclient</code> in the current directory and its parents, so the same code works from a project subdirectory.</p> <p>Omit the identifier to select the first connection marked <code>rest: true</code> - here, <code>content</code>:</p> <pre><code>with manager.get_client() as ml:\n    print(ml.eval.xquery('\"Hello World!\"'))\n</code></pre> <p>The predefined <code>app-services</code>, <code>manage</code>, <code>admin</code> and <code>health</code> connections are always available even when the file does not list them, so you can select one by id without declaring it:</p> <pre><code>with manager.get_client(\"app-services\") as ml:\n    print(ml.eval.xquery('\"Hello World!\"'))\n</code></pre> <p>Use <code>get_async_client()</code> in async code; see async support. Leaving the client context closes its opened connections.</p>"},{"location":"user/environments/#use-it-from-the-cli","title":"Use it from the CLI","text":"<p>Commands use <code>local</code> by default:</p> <pre><code>ml env show local\nml eval -x '\"Hello World!\"'\nml logs\n</code></pre> <p><code>ml env show</code> displays the file's settings and masks secrets.</p>"},{"location":"user/environments/#add-another-environment","title":"Add another environment","text":"<p>Create <code>.mlclient/mlclient-dev.yaml</code> for a development server and change the settings that differ, such as <code>host</code> and credentials. Each file is independent; <code>dev</code> does not inherit settings from <code>local</code>.</p> <pre><code>manager = MLClientManager(\"dev\")\nwith manager.get_client() as ml:\n    print(ml.eval.xquery('\"Hello World!\"'))\n</code></pre> <p>Select the same environment in the CLI with <code>-e</code>:</p> <pre><code>ml eval -e dev -x '\"Hello World!\"'\n</code></pre>"},{"location":"user/environments/#add-another-connection","title":"Add another connection","text":"<p>A single environment can contain several App Servers. For example, extend the <code>app-servers</code> list with a second REST connection:</p> <pre><code>app-servers:\n  - id: content\n    port: 8100\n    rest: true\n  - id: reporting\n    port: 8110\n    rest: true\n</code></pre> <p>Both use the file's host and credentials. Select <code>reporting</code> in Python or with the CLI's connection option:</p> <pre><code>with MLClientManager(\"local\").get_client(\"reporting\") as ml:\n    print(ml.eval.xquery('\"Hello World!\"'))\n</code></pre> <pre><code>ml eval -c reporting -x '\"Hello World!\"'\n</code></pre> <p>Manage, Admin and Health connections are provided automatically with their default ports; you do not need to list them for a standard local setup.</p>"},{"location":"user/environments/#go-further","title":"Go further","text":"<p>More on environments covers per-server overrides, inheritance, TLS, authentication, Cloud and temporary Python overrides. For supported transport/authentication combinations and their requirements, see connections and authentication.</p> <p>Retry, timeouts and pool limits are Python-only HTTP settings. Configure them through the client or manager as described in HTTP configuration.</p>"},{"location":"user/http-configuration/","title":"HTTP configuration","text":"<p>Use these settings to control HTTP requests and connection pools in Python. They are separate from the environment YAML, which stores server connections and credentials. Start with the manager basics if you want to create clients from a named environment.</p>"},{"location":"user/http-configuration/#set-it-on-a-manager-a-client-or-a-request","title":"Set it on a manager, a client, or a request","text":"<p><code>retry</code>, <code>limits</code> and <code>timeout</code> are HTTP client options, not environment settings: none can be configured in the environment YAML. <code>limits</code> caps the connection pool - <code>max_connections</code> acts as a semaphore over concurrent requests - and <code>timeout</code> bounds each request. You set them at three levels, each overriding the one before it:</p> <ul> <li>Manager - a default for every client the manager creates, across all servers including Health.</li> <li>Client - on <code>MLClient</code> or <code>HTTPConfig</code> when creating a client directly, or as an override to a manager factory (<code>get_client</code>, <code>get_config</code>, ...) for the selected server.</li> <li>Request - <code>timeout</code> on an individual operation, bounding just that call. <code>retry</code> and <code>limits</code> resolve at the client, so they have no per-request form.</li> </ul> <p>There is no YAML value beneath these levels. Set the manager defaults on construction and its factories:</p> <pre><code>&gt;&gt;&gt; import httpx\n&gt;&gt;&gt; from httpx_retries import Retry\n&gt;&gt;&gt; from mlclient import MLClientManager\n&gt;&gt;&gt; from mlclient.http import NO_RETRY_STRATEGY\n&gt;&gt;&gt; mgr = MLClientManager(\n...     \"local\",\n...     retry=Retry(total=2),\n...     limits=httpx.Limits(max_connections=10),\n...     timeout=httpx.Timeout(connect=5.0, read=120.0, write=60.0, pool=5.0),\n... )\n&gt;&gt;&gt; with mgr.get_client(\"content\") as ml:\n...     result = ml.eval.xquery('\"Hello World!\"')\n&gt;&gt;&gt; with mgr.get_client(\"health\", retry=NO_RETRY_STRATEGY) as ml:\n...     healthy = ml.healthcheck()\n</code></pre> <p>A factory override wins over the manager default for that one client; pass <code>timeout</code> to a single operation to bound just that request:</p> <pre><code>&gt;&gt;&gt; with mgr.get_client(\"content\") as ml:\n...     result = ml.eval.xquery('\"Hello World!\"', timeout=5.0)\n</code></pre> <p>The options have different defaults and override semantics. Read timeouts, retries and resource limits for details. In particular, <code>timeout=None</code> disables timeouts, while <code>retry=None</code> restores the server's default retry policy.</p>"},{"location":"user/http-configuration/#async-clients-raw-http-and-resolved-configuration","title":"Async clients, raw HTTP and resolved configuration","text":"<p>The same selection and override rules apply to <code>get_async_client()</code>, <code>get_http_client()</code> and <code>get_async_http_client()</code>. Raw HTTP factories require a server identifier:</p> <pre><code>&gt;&gt;&gt; mgr = MLClientManager(\"local\")\n&gt;&gt;&gt; async with mgr.get_async_client(\"health\") as ml:\n...     healthy = await ml.healthcheck()\n&gt;&gt;&gt; with mgr.get_http_client(\"health\") as http:\n...     response = http.head(\"/\")\n</code></pre> <p>Use <code>get_config()</code> to obtain a resolved <code>HTTPConfig</code> without creating a client or opening a session:</p> <pre><code>&gt;&gt;&gt; config = mgr.get_config(\"content\", port=9100, retry=NO_RETRY_STRATEGY)\n</code></pre> <p>Overrides accept the parameters of <code>HTTPConfig.clone()</code>; unknown names raise <code>TypeError</code>. <code>get_config()</code> applies the same manager defaults and per-call overrides as the client factories.</p>"},{"location":"user/cli/env/","title":"env","text":"<p>An MLClient environment is a YAML file containing the connection settings for a project. The <code>env</code> command group creates and inspects those files; it does not create databases or deploy a MarkLogic application.</p> Command Use it to <code>ml env init</code> Create a template, import ml-gradle properties or discover App Servers from a host <code>ml env show</code> List environments, inspect a configuration or copy a simple setting"},{"location":"user/cli/env/#create-and-inspect-a-configuration","title":"Create and inspect a configuration","text":"<pre><code>ml env init\nml env show local\n</code></pre> <p>Review <code>.mlclient/mlclient-local.yaml</code> and fill in your connection settings before sending requests. Other commands use <code>local</code> by default; <code>-e dev</code> selects <code>.mlclient/mlclient-dev.yaml</code> instead.</p> <p>An environment can name several connections. For the file format, inherited settings and use from Python, see Environments.</p>"},{"location":"user/cli/eval/","title":"eval","text":"<p>Run XQuery or server-side JavaScript and print the result.</p> <pre><code>ml eval -x '\"Hello World!\"'\nml eval -j '\"Hello World!\"'\nml eval ./query.xqy\n</code></pre> <p>Use <code>-d</code> to select a content database, or pass external variables with <code>--var</code>:</p> <pre><code>ml eval ./query.xqy -d Documents\nml eval -x 'declare variable $name external; concat(\"Hello, \", $name)' --var name=Tom\n</code></pre>"},{"location":"user/cli/eval/#arguments","title":"Arguments","text":""},{"location":"user/cli/eval/#code","title":"<code>code</code>","text":"<p>Required. A file path by default; use <code>--xquery</code> or <code>--javascript</code> for inline code. The file extension determines the language when evaluating a file.</p>"},{"location":"user/cli/eval/#options","title":"Options","text":""},{"location":"user/cli/eval/#-xquery-x","title":"<code>--xquery</code>, <code>-x</code>","text":"<p>Treat <code>code</code> as inline XQuery. Cannot be combined with <code>--javascript</code>.</p>"},{"location":"user/cli/eval/#-javascript-j","title":"<code>--javascript</code>, <code>-j</code>","text":"<p>Treat <code>code</code> as inline JavaScript. Cannot be combined with <code>--xquery</code>.</p>"},{"location":"user/cli/eval/#-var","title":"<code>--var</code>","text":"<p>Bind an external variable using <code>name=value</code>. Repeat the option for several variables. Values are supplied as strings; convert them in your query as needed. For an XQuery variable in a namespace, use its expanded name:</p> <pre><code>ml eval -x 'declare namespace local=\"urn:example\";\n  declare variable $local:days external;\n  xs:integer($local:days) + 1' --var '{urn:example}days=5'\n</code></pre>"},{"location":"user/cli/eval/#-database-d","title":"<code>--database</code>, <code>-d</code>","text":"<p>Content database in which to evaluate the code. When omitted, use the REST App Server's default content database. The user must have permission to access it.</p>"},{"location":"user/cli/eval/#-txid-t","title":"<code>--txid</code>, <code>-t</code>","text":"<p>Run within an existing multi-statement transaction. This command does not create, commit or roll back that transaction.</p>"},{"location":"user/cli/eval/#-environment-e","title":"<code>--environment</code>, <code>-e</code>","text":"<p>Environment name. Defaults to <code>local</code>; use <code>-e dev</code> to load <code>.mlclient/mlclient-dev.yaml</code>.</p>"},{"location":"user/cli/eval/#-connection-c","title":"<code>--connection</code>, <code>-c</code>","text":"<p>Configured connection identifier or TCP port (<code>1</code>\u2013<code>65535</code>). Omit it to use the default REST connection. A port changes that connection's port and retains its other settings. For example, <code>-c content</code> selects a named connection and <code>-c 8100</code> uses port 8100.</p> <p>See also global options.</p>"},{"location":"user/cli/health/","title":"health","text":"<p>Check whether the environment's HealthCheck server is healthy.</p> <pre><code>ml health\n</code></pre> <p>Keep checking until Ctrl-C, including while the server is unreachable:</p> <pre><code>ml health --watch\nml health --watch --interval 10 --overwrite --lines 5\n</code></pre> <p>The command uses the environment's <code>health</code> connection and sends <code>HEAD /</code>; it does not require a REST App Server.</p>"},{"location":"user/cli/health/#options","title":"Options","text":""},{"location":"user/cli/health/#-watch-w","title":"<code>--watch</code>, <code>-w</code>","text":"<p>Poll until interrupted with Ctrl-C. Each status includes a local timestamp. Connection errors and timeouts print <code>UNREACHABLE</code>; polling continues and shows recovery on a later successful request.</p>"},{"location":"user/cli/health/#-interval-i","title":"<code>--interval</code>, <code>-i</code>","text":"<p>Seconds between polls. Default: <code>5</code>; allowed range: <code>1</code>\u2013<code>3600</code>. Ignored without <code>--watch</code>.</p>"},{"location":"user/cli/health/#-overwrite-o","title":"<code>--overwrite</code>, <code>-o</code>","text":"<p>Repaint recent statuses in place instead of scrolling. Requires <code>--watch</code>. With <code>--no-ansi</code>, or redirected output without forced ANSI, statuses are appended normally instead of repainting.</p>"},{"location":"user/cli/health/#-lines-l","title":"<code>--lines</code>, <code>-l</code>","text":"<p>Number of statuses retained by <code>--overwrite</code>. Default: <code>3</code>; allowed range: <code>1</code>\u2013<code>100</code>. Ignored without <code>--watch</code>.</p>"},{"location":"user/cli/health/#-environment-e","title":"<code>--environment</code>, <code>-e</code>","text":"<p>Environment name. Defaults to <code>local</code>; use <code>-e dev</code> to load <code>.mlclient/mlclient-dev.yaml</code>.</p>"},{"location":"user/cli/health/#status-and-exit-codes","title":"Status and exit codes","text":"Status Meaning <code>HEALTHY</code> HealthCheck returned a success response <code>UNHEALTHY</code> HealthCheck returned a failure response <code>UNREACHABLE</code> A watch-mode probe could not connect or timed out <p>A single check exits with <code>0</code> for healthy and <code>1</code> for unhealthy. A connection error outside watch mode is reported as an error. Watch mode continues through failures until interrupted.</p> <p>See also global options.</p>"},{"location":"user/cli/http/","title":"http","text":"<p>Send an HTTP request and print its response body. Use lowercase method names; method matching is case-insensitive.</p> <pre><code>ml http get /v1/documents uri=/doc.json\nml http get /v1/documents uri=/doc.json --include --pretty\n</code></pre> <p>Supply headers with <code>name:value</code>, query parameters with <code>name=value</code>, and a body with <code>--body</code>:</p> <pre><code>ml http put /v1/documents uri=/doc.json \\\n  Content-Type:application/json -b '{\"hello\":\"world\"}'\nml http put /v1/documents uri=/doc.xml \\\n  Content-Type:application/xml -b @./doc.xml\nml http delete /v1/documents uri=/doc.xml\n</code></pre> <p>To use the Manage API, select its configured connection explicitly:</p> <pre><code>ml http get /manage/v2/hosts -c manage view=status format=json\n</code></pre>"},{"location":"user/cli/http/#arguments","title":"Arguments","text":""},{"location":"user/cli/http/#method","title":"<code>method</code>","text":"<p>Required. An HTTP method such as <code>get</code>, <code>head</code>, <code>post</code>, <code>put</code>, <code>delete</code> or <code>patch</code>.</p>"},{"location":"user/cli/http/#endpoint","title":"<code>endpoint</code>","text":"<p>Required. An endpoint path such as <code>/v1/documents</code>. It may contain a query string. The path does not select a connection automatically: <code>/manage/v2/*</code> still needs <code>-c manage</code> (or the appropriate port).</p>"},{"location":"user/cli/http/#params","title":"<code>params</code>","text":"<p>Optional, repeatable query parameters and headers. A <code>key=value</code> token adds a query parameter; a <code>key:value</code> token adds a header. Quote tokens containing spaces. Repeated query keys preserve every value. Header names are case-insensitive; the last value wins.</p> <pre><code>ml http get /v1/documents uri=/one.xml uri=/two.xml Accept:application/xml\n</code></pre>"},{"location":"user/cli/http/#options","title":"Options","text":""},{"location":"user/cli/http/#-body-b","title":"<code>--body</code>, <code>-b</code>","text":"<p>Request body as literal text, an existing file path, or an explicit <code>@path</code>. Files are read as bytes, preserving binary content and line endings. An explicit <code>@path</code> must exist and be readable. Text, including JSON, is sent unchanged. Set the appropriate <code>Content-Type</code> header yourself.</p>"},{"location":"user/cli/http/#-include-i","title":"<code>--include</code>, <code>-i</code>","text":"<p>Print the HTTP status line and response headers before the body. A <code>head</code> request always prints them because it has no response body.</p>"},{"location":"user/cli/http/#-pretty-p","title":"<code>--pretty</code>, <code>-p</code>","text":"<p>Format JSON and element-only XML with two-space indentation. Invalid JSON/XML, mixed XML content and <code>xml:space=\"preserve\"</code> are left unchanged.</p>"},{"location":"user/cli/http/#-environment-e","title":"<code>--environment</code>, <code>-e</code>","text":"<p>Environment name. Defaults to <code>local</code>; use <code>-e dev</code> to load <code>.mlclient/mlclient-dev.yaml</code>.</p>"},{"location":"user/cli/http/#-connection-c","title":"<code>--connection</code>, <code>-c</code>","text":"<p>Configured connection identifier or TCP port (<code>1</code>\u2013<code>65535</code>). Omit it to use the default REST connection. A port changes that connection's port and retains its other settings. For example, <code>-c content</code> selects a named connection and <code>-c 8100</code> uses port 8100.</p>"},{"location":"user/cli/http/#responses-and-errors","title":"Responses and errors","text":"<p>Output is decoded response text. Responses are printed before a 4xx/5xx error is reported with a nonzero exit code. Redirect responses are displayed without following them and do not count as HTTP errors. With <code>--include</code>, the status line uses the actual HTTP protocol version.</p> <p>See also global options.</p>"},{"location":"user/cli/log-level/","title":"log-level","text":"<p>Show or change a group or App Server log level.</p> <pre><code>ml log-level\nml log-level debug\n</code></pre> <p>The command shows the group file log level by default. Reading or setting a level prints the same output format:</p> <pre><code>Group: Default\nFile Log Level: info\n</code></pre> <p>Target a group's system log level or an App Server's file log level:</p> <pre><code>ml log-level -t system -g Default\nml log-level -s App-Services\nml log-level warning -s App-Services\n</code></pre>"},{"location":"user/cli/log-level/#arguments","title":"Arguments","text":""},{"location":"user/cli/log-level/#level","title":"<code>level</code>","text":"<p>Optional new log level. Omit it to read the current value. Supported values: <code>finest</code>, <code>finer</code>, <code>fine</code>, <code>debug</code>, <code>config</code>, <code>info</code>, <code>notice</code>, <code>warning</code>, <code>error</code>, <code>critical</code>, <code>alert</code>, <code>emergency</code>.</p>"},{"location":"user/cli/log-level/#options","title":"Options","text":""},{"location":"user/cli/log-level/#-type-t","title":"<code>--type</code>, <code>-t</code>","text":"<p>Log type: <code>file</code> (default) or <code>system</code>. App Servers support only <code>file</code>.</p>"},{"location":"user/cli/log-level/#-group-g","title":"<code>--group</code>, <code>-g</code>","text":"<p>MarkLogic group name. Defaults to <code>Default</code>.</p>"},{"location":"user/cli/log-level/#-server-s","title":"<code>--server</code>, <code>-s</code>","text":"<p>Actual MarkLogic App Server name, such as <code>App-Services</code>. This is the target of the operation, not a connection identifier or port. Omit it to act on the group. May only be combined with <code>--type file</code>.</p>"},{"location":"user/cli/log-level/#-environment-e","title":"<code>--environment</code>, <code>-e</code>","text":"<p>Environment name. Defaults to <code>local</code>; use <code>-e dev</code> to load <code>.mlclient/mlclient-dev.yaml</code>.</p>"},{"location":"user/cli/log-level/#-connection-c","title":"<code>--connection</code>, <code>-c</code>","text":"<p>Configured connection identifier or TCP port (<code>1</code>\u2013<code>65535</code>). Omit it to use the default REST connection. A port changes that connection's port and retains its other settings. For example, <code>-c content</code> selects a named connection and <code>-c 8100</code> uses port 8100.</p>"},{"location":"user/cli/log-level/#permissions-and-diagnostics","title":"Permissions and diagnostics","text":"<p>The command first evaluates Admin module functions through the selected REST connection. Only authorization failures trigger Management REST fallback. Reading App Server properties through that fallback requires <code>manage-user</code>; reading group properties or changing either target requires <code>manage-admin</code> (or equivalent privileges).</p> <p>If both paths refuse access, the error names the required Management role. Transport and other server errors retain their original meaning and do not trigger a Management retry.</p> <p>Use <code>ml log-level -vv</code> to see the eval attempt, any authorization failure, and the Manage fallback result. There is no separate Admin REST fallback.</p> <p>See also global options.</p>"},{"location":"user/cli/logs/","title":"logs","text":"<p>Read MarkLogic log entries or list available log files.</p> <pre><code>ml logs\nml logs -s 8002\nml logs --list\n</code></pre> <p>The first command reads the main <code>ErrorLog.txt</code>; <code>-s 8002</code> selects <code>8002_ErrorLog.txt</code>. Narrow an error log by time or regular expression:</p> <pre><code>ml logs -s 8002 --from 10:00 --to 12:00\nml logs -s 8002 --regex 'Forest M.*'\n</code></pre>"},{"location":"user/cli/logs/#options","title":"Options","text":""},{"location":"user/cli/logs/#-server-s","title":"<code>--server</code>, <code>-s</code>","text":"<p>App Server identifier from the environment or its port. Identifiers resolve to the configured port; unknown identifiers are rejected. <code>TaskServer</code> and <code>0</code> both select Task Server logs. With no selector, read the main server log.</p> <p>This selects whose logs to read, not the connection used for the request.</p> <pre><code>ml logs -s app-services\nml logs -s TaskServer\n</code></pre>"},{"location":"user/cli/logs/#-log-type-l","title":"<code>--log-type</code>, <code>-l</code>","text":"<p>Log type: <code>error</code> (default), <code>access</code> or <code>request</code>.</p> <pre><code>ml logs -s 8002 --log-type access\n</code></pre>"},{"location":"user/cli/logs/#-from-f","title":"<code>--from</code>, <code>-f</code>","text":"<p>Start time for error-log filtering. Accepts a time, date or date and time. Quote values containing spaces.</p> <pre><code>ml logs --from '2026-09-01 10:00'\n</code></pre>"},{"location":"user/cli/logs/#-to-t","title":"<code>--to</code>, <code>-t</code>","text":"<p>End time for error-log filtering, in the same forms as <code>--from</code>.</p> <pre><code>ml logs --from 2026-09-01 --to 2026-09-03\n</code></pre>"},{"location":"user/cli/logs/#-regex-r","title":"<code>--regex</code>, <code>-r</code>","text":"<p>Regular expression used to filter error logs. Quote the expression to keep the shell from interpreting it. <code>--from</code>, <code>--to</code> and <code>--regex</code> apply only to error logs.</p>"},{"location":"user/cli/logs/#-host-h","title":"<code>--host</code>, <code>-H</code>","text":"<p>MarkLogic host from which to retrieve log data. This selects a host within the server operation; it does not replace the environment's connection hostname.</p>"},{"location":"user/cli/logs/#-all-hosts","title":"<code>--all-hosts</code>","text":"<p>Read error logs from every host in the cluster at once, merged into a single timeline by timestamp. The host is shown in parentheses between the log level and the message. The host list comes from <code>/manage/v2/hosts</code>, and each host is queried concurrently. If host discovery or any host read fails, the command fails without printing a partial timeline. Error logs only: <code>--all-hosts</code> with any other log type is rejected. <code>--all-hosts</code> cannot be combined with <code>--host</code> or <code>--list</code>.</p> <p>Reading unfiltered error logs from every host can return a large volume and may time out, so a multi-host read with no <code>--from</code>, <code>--to</code> or <code>--regex</code> prints a warning suggesting you narrow it.</p> <pre><code>ml logs -e dev --all-hosts\nml logs -e dev --all-hosts --from '2026-09-01 10:00' --regex 'Forest M.*'\n</code></pre>"},{"location":"user/cli/logs/#-list","title":"<code>--list</code>","text":"<p>List available log files instead of reading entries. Combine with <code>--server</code> or <code>--host</code> to narrow the list.</p>"},{"location":"user/cli/logs/#-environment-e","title":"<code>--environment</code>, <code>-e</code>","text":"<p>Environment name. Defaults to <code>local</code>; use <code>-e dev</code> to load <code>.mlclient/mlclient-dev.yaml</code>.</p> <p>See also global options.</p>"},{"location":"user/cli/trace-events/","title":"trace-events","text":"<p>Show or change a group's diagnostic trace events: the master \"Trace Events Activated\" switch and the set of enabled trace events shown on the Admin Interface Diagnostics page.</p> <pre><code>ml trace-events\nml trace-events true\n</code></pre> <p>Without a value the command shows the group's state:</p> <pre><code>Group: Default\nTrace Events Activated: true\nOptic Query Parsing: on\nXDMP Deadlock: on\n</code></pre> <p>A bare value sets the master switch:</p> <pre><code>Group: Default\nTrace Events Activated: true\n</code></pre> <p>Show or toggle a single event with <code>--event</code>. With a value it adds the event (truthy) or removes it (falsy); without a value it shows that event's status:</p> <pre><code>ml trace-events --event \"XDMP Deadlock\"\nml trace-events --event \"XDMP Deadlock\" on\nml trace-events --event \"XDMP Deadlock\" off\n</code></pre> <pre><code>Group: Default\nTrace Events Activated: true\nXDMP Deadlock: on\n</code></pre> <p>Choose the switch and prune the enabled events at prompts:</p> <pre><code>ml trace-events -i\n</code></pre> <p>The command evaluates Admin module functions through the selected REST connection and saves the configuration when a value changes it.</p>"},{"location":"user/cli/trace-events/#arguments","title":"Arguments","text":""},{"location":"user/cli/trace-events/#value","title":"<code>value</code>","text":"<p>Optional boolean. On its own it sets Trace Events Activated; with <code>--event</code> it adds the event (truthy) or removes it (falsy). Omit it to read the current state. Accepts <code>true</code>/<code>false</code>, <code>on</code>/<code>off</code>, <code>1</code>/<code>0</code> or <code>yes</code>/<code>no</code>.</p>"},{"location":"user/cli/trace-events/#options","title":"Options","text":""},{"location":"user/cli/trace-events/#-group-g","title":"<code>--group</code>, <code>-g</code>","text":"<p>MarkLogic group name. Defaults to <code>Default</code>.</p>"},{"location":"user/cli/trace-events/#-event","title":"<code>--event</code>","text":"<p>A single trace event name. On its own it shows that event's status; with a value it adds or removes the event.</p>"},{"location":"user/cli/trace-events/#-interactive-i","title":"<code>--interactive</code>, <code>-i</code>","text":"<p>Prompt for the activation switch and the enabled events instead of taking a value. The event prompt lists the currently enabled events, all pre-selected; unselecting one removes it. Adding a new event still uses <code>--event &lt;name&gt; on</code>.</p>"},{"location":"user/cli/trace-events/#-environment-e","title":"<code>--environment</code>, <code>-e</code>","text":"<p>Environment name. Defaults to <code>local</code>; use <code>-e dev</code> to load <code>.mlclient/mlclient-dev.yaml</code>.</p>"},{"location":"user/cli/trace-events/#-connection-c","title":"<code>--connection</code>, <code>-c</code>","text":"<p>Configured connection identifier or TCP port (<code>1</code>\u2013<code>65535</code>). Omit it to use the default REST connection. A port changes that connection's port and retains its other settings. For example, <code>-c content</code> selects a named connection and <code>-c 8100</code> uses port 8100.</p> <p>See also global options.</p>"},{"location":"user/cli/version/","title":"version","text":"<p>Print the complete MarkLogic Server version.</p> <pre><code>ml version\nml version -e dev\nml version -c content\n</code></pre> <p>For example, output may be <code>12.0.1</code> or <code>10.0-9.5</code>. All numeric components, separators and suffixes are preserved. To print the MLClient package version, use the global <code>ml --version</code> option instead.</p>"},{"location":"user/cli/version/#options","title":"Options","text":""},{"location":"user/cli/version/#-environment-e","title":"<code>--environment</code>, <code>-e</code>","text":"<p>Environment name. Defaults to <code>local</code>; use <code>-e dev</code> to load <code>.mlclient/mlclient-dev.yaml</code>.</p>"},{"location":"user/cli/version/#-connection-c","title":"<code>--connection</code>, <code>-c</code>","text":"<p>Configured connection identifier or TCP port (<code>1</code>\u2013<code>65535</code>). Omit it to use the default REST connection. A port changes that connection's port and retains its other settings. For example, <code>-c content</code> selects a named connection and <code>-c 8100</code> uses port 8100.</p>"},{"location":"user/cli/version/#version-lookup","title":"Version lookup","text":"<p>The command evaluates <code>xdmp:version()</code> on the selected REST server. If the user lacks the eval privilege, it tries Manage and then Admin endpoints. Unavailable endpoints and malformed fallback responses are skipped. If none succeeds, the original eval error is reported. An invalid eval version also makes the command fail; a connection error does not trigger an auxiliary-server fallback.</p> <p>See also global options.</p>"},{"location":"user/cli/env/init/","title":"env init","text":"<p>Create a project environment from a template, ml-gradle properties or a running MarkLogic host.</p> <pre><code>ml env init\n</code></pre> <p>With no name, a wizard asks for the environment name and configuration source. To write a commented template directly:</p> <pre><code>ml env init local\n</code></pre> <p>Import a Gradle profile or discover App Servers from a host:</p> <pre><code>ml env init --from-gradle=dev\nml env init local --from-host=localhost:8002 --interactive\n</code></pre> <p>The result is <code>.mlclient/mlclient-&lt;name&gt;.yaml</code> in the current directory. Review it with <code>ml env show</code> before making requests. See Environments for the file format and Python usage.</p>"},{"location":"user/cli/env/init/#arguments","title":"Arguments","text":""},{"location":"user/cli/env/init/#name","title":"<code>name</code>","text":"<p>Optional environment name. With a name and no source option, write a fully commented template. Omit the name to run the wizard. A named Gradle profile can supply the name automatically; see <code>--from-gradle</code> below.</p>"},{"location":"user/cli/env/init/#options","title":"Options","text":""},{"location":"user/cli/env/init/#-interactive-i","title":"<code>--interactive</code>, <code>-i</code>","text":"<p>Run the wizard even when a name is supplied. Host discovery asks only for fields not already provided through options; the password prompt does not echo input.</p>"},{"location":"user/cli/env/init/#-from-gradle","title":"<code>--from-gradle</code>","text":"<p>Read ml-gradle properties. Accepts an environment name or a <code>.properties</code> file path. Cannot be combined with <code>--from-host</code>.</p> <p>A profile name merges <code>gradle-&lt;name&gt;.properties</code> over <code>gradle.properties</code> and also names the MLClient environment:</p> <pre><code>ml env init --from-gradle=dev\nml env init my-dev --from-gradle=dev\n</code></pre> <p>A file path is read on its own, without an overlay merge. Supply a name or the command prompts for one:</p> <pre><code>ml env init dev --from-gradle=./gradle-dev.properties\n</code></pre> <p>Passing <code>--from-gradle</code> without a value prompts for the name and selector. An unknown profile or missing file is reported before prompting, with available profiles or files to help identify the correct source.</p>"},{"location":"user/cli/env/init/#-from-host","title":"<code>--from-host</code>","text":"<p>Discover App Servers through a host's Manage API. Accepts <code>host</code>, <code>host:port</code> or <code>protocol://host[:port]</code>.</p> <pre><code>ml env init dev --from-host=ml.example.com:8002 --interactive\n</code></pre> <p>When both a name and this option are supplied, discovery is non-interactive unless <code>--interactive</code> is passed. Unspecified values default to HTTP, <code>localhost</code>, port <code>8002</code>, username/password <code>admin</code>/<code>admin</code>, and Digest auth. An option without a value uses these defaults; omitting the name starts prompts.</p> <p>For HTTPS discovery, certificate verification is disabled and the generated YAML contains <code>ssl: {verify: false}</code>. Set <code>ssl.verify</code> to a trusted CA bundle in the result when verification is required.</p> <p>The target URL is printed before discovery. The generated file omits unchanged predefined servers and includes a comment listing their defaults.</p>"},{"location":"user/cli/env/init/#-app-name","title":"<code>--app-name</code>","text":"<p>Application label. For host discovery, also restricts results to matching App Server names. Without it, all discovered servers are retained and the label is left commented out.</p> <pre><code>ml env init dev --from-host=ml.example.com --app-name=my-app --interactive\n</code></pre>"},{"location":"user/cli/env/init/#-username-u","title":"<code>--username</code>, <code>-u</code>","text":"<p>Username for host discovery. Defaults to <code>admin</code> in non-interactive mode.</p>"},{"location":"user/cli/env/init/#-password-p","title":"<code>--password</code>, <code>-p</code>","text":"<p>Password for host discovery. Defaults to <code>admin</code> in non-interactive mode. An explicit empty value (<code>--password=''</code>) is preserved. At an interactive prompt, Enter accepts the displayed default.</p>"},{"location":"user/cli/env/init/#-auth-a","title":"<code>--auth</code>, <code>-a</code>","text":"<p>Host-discovery authentication: <code>basic</code>, <code>digest</code> (default) or <code>digestbasic</code>. Other values are rejected.</p>"},{"location":"user/cli/env/init/#-global-g","title":"<code>--global</code>, <code>-g</code>","text":"<p>Write to <code>.mlclient</code> in your home directory instead of the current directory.</p>"},{"location":"user/cli/env/init/#-force-f","title":"<code>--force</code>, <code>-f</code>","text":"<p>Overwrite an existing environment file. Without this flag, an existing file is left untouched and the command reports an error.</p> <p>See also global options.</p>"},{"location":"user/cli/env/init/#discovery-and-connection-settings","title":"Discovery and connection settings","text":"<p>For <code>localhost</code> and IP addresses, each server's own protocol and authentication are recorded when they differ from the root configuration. Other hostnames are treated as load-balanced connections: servers inherit the root protocol and authentication rather than their direct listener settings.</p> <p>Unsupported <code>saml</code> authentication is skipped with a warning. An <code>oauth</code> scheme is retained; add its bearer token before loading the environment. Review the output for the access path your application will actually use.</p>"},{"location":"user/cli/env/show/","title":"env show","text":"<p>List environments, inspect their settings or copy one value.</p> <pre><code>ml env show\nml env show local\nml env show local host\n</code></pre> <p>The command reads the nearest <code>.mlclient</code> directory in the current directory or its parents. Values come from the configuration file. Secrets are masked in the formatted output unless <code>--secrets</code> is passed.</p>"},{"location":"user/cli/env/show/#arguments","title":"Arguments","text":""},{"location":"user/cli/env/show/#name","title":"<code>name</code>","text":"<p>Environment name. Omit it to list available names, one per line. For example, <code>local</code> selects <code>mlclient-local.yaml</code>.</p>"},{"location":"user/cli/env/show/#setting","title":"<code>setting</code>","text":"<p>Optional root setting or App Server identifier. A root setting prints its value; an App Server renders a table of that server's settings.</p> <pre><code>ml env show local host\nml env show local app-services\n</code></pre> <p>Predefined servers (<code>app-services</code>, <code>manage</code>, <code>admin</code>, <code>health</code>) can be inspected even when the file does not list them; in that case their defaults are shown.</p>"},{"location":"user/cli/env/show/#options","title":"Options","text":""},{"location":"user/cli/env/show/#-global-g","title":"<code>--global</code>, <code>-g</code>","text":"<p>Read <code>.mlclient</code> in your home directory instead of searching the project.</p>"},{"location":"user/cli/env/show/#-raw","title":"<code>--raw</code>","text":"<p>Print the configuration file verbatim, including secret values. No formatting, masking or YAML validation is applied.</p> <pre><code>ml env show local --raw\n</code></pre>"},{"location":"user/cli/env/show/#-secrets-s","title":"<code>--secrets</code>, <code>-s</code>","text":"<p>Reveal secrets in formatted output. Without this option, passwords and API keys are masked, including values inside mappings and lists.</p>"},{"location":"user/cli/env/show/#-copy-c","title":"<code>--copy</code>, <code>-c</code>","text":"<p>Copy a simple setting while also printing it:</p> <pre><code>ml env show local host --copy\nml env show local password -c\n</code></pre> <p>Text, numbers and booleans are supported. The clipboard receives the original value, including an unmasked password even without <code>--secrets</code>. Terminal output remains masked. A successful copy is followed by Copied to clipboard. in green. Copied text has no formatting or trailing newline; booleans use <code>true</code> or <code>false</code>.</p> <p>For a whole environment, environment list, mapping, list, null, App Server table or <code>--raw</code> output, the command displays the result and then warns that copying was skipped. A missing clipboard utility or session also produces a warning without failing the command.</p> <p>Clipboard support uses <code>xclip</code> on Linux/X11, <code>wl-copy</code> on Wayland, <code>pbcopy</code> on macOS or <code>clip</code> on Windows.</p> <p>See also global options.</p>"},{"location":"user/cli/env/show/#invalid-configuration","title":"Invalid configuration","text":"<p>An empty file renders as an empty environment. Otherwise, YAML must contain a mapping. <code>app-servers</code> may be omitted, null or a list of mappings with non-empty <code>id</code> values. Errors name the file without printing its contents.</p>"},{"location":"user/python/async/","title":"Async support","text":"<p>Use <code>AsyncMLClient</code> in asynchronous applications. Its configuration matches <code>MLClient</code>; request operations are awaited.</p> <pre><code>import asyncio\nfrom mlclient import AsyncMLClient\n\n\nasync def main():\n    async with AsyncMLClient() as ml:\n        result = await ml.eval.xquery('\"Hello World!\"')\n        print(result)\n\n\nasyncio.run(main())\n</code></pre> <p>Inside an existing event loop, await your function instead of calling <code>asyncio.run()</code> again.</p>"},{"location":"user/python/async/#reuse-the-client","title":"Reuse the client","text":"<p>Keep one client open for related operations so its connections can be reused. Exit the <code>async with</code> block to close all opened connections, including auxiliary Manage, Admin and Health connections.</p> <pre><code>async with AsyncMLClient() as ml:\n    documents = await ml.documents.read([\"/first.json\", \"/second.json\"])\n</code></pre>"},{"location":"user/python/async/#run-independent-requests-concurrently","title":"Run independent requests concurrently","text":"<p>Use <code>asyncio.gather()</code> for independent evaluations, such as counts in different database contexts:</p> <pre><code>async with AsyncMLClient() as ml:\n    documents_count, modules_count = await asyncio.gather(\n        ml.eval.xquery(\"fn:count(fn:collection())\", database=\"Documents\"),\n        ml.eval.xquery(\"fn:count(fn:collection())\", database=\"Modules\"),\n    )\n</code></pre> <p>For more databases, bound concurrency as shown in the concurrent eval recipe. For multiple documents in one database, use the bulk read shown above. Pool limits control active connections, not how many tasks you create.</p>"},{"location":"user/python/async/#use-an-environment","title":"Use an environment","text":"<pre><code>from mlclient import MLClientManager\n\nasync with MLClientManager(\"local\").get_async_client() as ml:\n    result = await ml.eval.xquery('\"Hello World!\"')\n</code></pre> <p>The manager applies the same configuration overrides to sync and async clients. Raw API wrappers are available as <code>ml.rest</code>, <code>ml.manage</code> and <code>ml.admin</code>; their request methods are awaited too.</p>"},{"location":"user/python/async/#api-differences","title":"API differences","text":"<p>Most operations mirror the synchronous API. Two lifecycle differences are worth calling out:</p> <ul> <li>Use <code>await ml.version()</code> to resolve the server version; the synchronous client   exposes a cached <code>ml.version</code> property.</li> <li>Opening a transaction performs I/O, so use <code>async with await ml.transaction()</code>.</li> </ul> <p>See transactions and the generated AsyncMLClient reference for complete signatures. Check the reference when choosing a service: the standalone <code>LogLevelService</code> currently has a synchronous interface.</p>"},{"location":"user/python/connections/","title":"Connection and authentication","text":"<p>MLClient models two independent concerns separately:</p> <ul> <li>Connection -- the transport: HTTP, HTTPS, mutual TLS, or MarkLogic Cloud. Chosen from <code>protocol</code>, <code>ssl</code> (SSLConfig ), and <code>cloud</code> (CloudConfig ).</li> <li>Authentication -- how the client proves its identity, chosen from <code>auth</code> with credentials supplied via <code>username</code> / <code>password</code>.</li> </ul> <p>Both are validated when the client is constructed, so an unsupported combination raises ConfigError immediately rather than failing on the first request.</p>"},{"location":"user/python/connections/#connection-modes","title":"Connection modes","text":"Mode How to select it Protocol / port HTTP default <code>http</code> / <code>8000</code> HTTPS <code>protocol=\"https\"</code> <code>https</code> Mutual TLS <code>ssl=SSLConfig(cert_file=..., key_file=...)</code> <code>https</code> MarkLogic Cloud <code>cloud=CloudConfig(api_key=..., base_path=...)</code> <code>https</code> / <code>443</code> <p>A client certificate implies HTTPS, so the protocol is inferred. MarkLogic Cloud forces HTTPS on port 443 and routes every API tier through the same gateway using the configured <code>base_path</code>. HTTP sessions are shared only when their complete configurations match; health checks normally use a separate session because their retry strategy differs. Passing a conflicting <code>protocol</code> or <code>port</code> raises ConfigError.</p> <pre><code>&gt;&gt;&gt; from mlclient import MLClient\n&gt;&gt;&gt; from mlclient.connection import SSLConfig, CloudConfig\n\n# Plain HTTP (default)\n&gt;&gt;&gt; ml = MLClient(host=\"ml.example.com\", port=8000)\n\n# HTTPS with server-certificate verification\n&gt;&gt;&gt; ml = MLClient(protocol=\"https\", host=\"ml.example.com\", port=8443)\n\n# HTTPS verified against a custom CA bundle\n&gt;&gt;&gt; ml = MLClient(\n...     protocol=\"https\",\n...     host=\"ml.example.com\",\n...     port=8443,\n...     ssl=SSLConfig(verify=\"/etc/ssl/corp-ca.pem\"),\n... )\n\n# Mutual TLS - the client certificate forces HTTPS\n&gt;&gt;&gt; ml = MLClient(\n...     host=\"ml.example.com\",\n...     port=8443,\n...     ssl=SSLConfig(cert_file=\"/client.pem\", key_file=\"/client-key.pem\"),\n... )\n\n# Mutual TLS with an encrypted client key - key_password decrypts it\n&gt;&gt;&gt; ml = MLClient(\n...     host=\"ml.example.com\",\n...     port=8443,\n...     ssl=SSLConfig(\n...         cert_file=\"/client.pem\",\n...         key_file=\"/client-key.pem\",\n...         key_password=\"my-key-passphrase\",\n...     ),\n... )\n\n# MarkLogic Cloud\n&gt;&gt;&gt; ml = MLClient(\n...     host=\"my-org.marklogic.cloud\",\n...     cloud=CloudConfig(api_key=\"my-api-key\", base_path=\"/ml/my-instance\"),\n... )\n</code></pre>"},{"location":"user/python/connections/#authentication-methods","title":"Authentication methods","text":"<p>The <code>auth</code> parameter accepts a string shortcut, an AuthConfig for methods that need more than a username and password, a custom Auth, or <code>None</code> for application-level auth. Credentials always come from <code>username</code> / <code>password</code> -- never from <code>AuthConfig</code>.</p> <code>auth</code> value Method Credentials <code>\"digest\"</code> (default) HTTP digest <code>username</code> / <code>password</code> <code>\"basic\"</code> HTTP basic <code>username</code> / <code>password</code> <code>\"digestbasic\"</code> HTTP digest <code>username</code> / <code>password</code> <code>\"certificate\"</code> Client certificate <code>ssl</code> client cert <code>\"kerberos\"</code> Kerberos / SPNEGO ambient ticket cache <code>AuthConfig(method=\"oauth\", token=...)</code> OAuth 2.0 Bearer pre-acquired token <code>AuthConfig(method=\"kerberos\", ...)</code> Kerberos / SPNEGO ambient ticket cache, custom SPN <code>None</code> application-level none a custom Auth instance custom supplied by the handler <p><code>\"certificate\"</code> and <code>\"kerberos\"</code> are accepted as plain strings because they need no extra data: certificate identity comes from the <code>ssl</code> client cert, and Kerberos defaults to the <code>HTTP</code> service on the request host. Use <code>AuthConfig</code> only to override the Kerberos SPN (<code>service</code> / <code>hostname</code>), or for OAuth, which has no default token.</p> <p>With mutual TLS, <code>auth</code> defaults to <code>\"certificate\"</code>, so it need not be set explicitly. Kerberos relies on the optional <code>pyspnego</code> dependency (<code>pip install mlclient[kerberos]</code>); the import is verified at client creation so a missing dependency fails early.</p> <pre><code>&gt;&gt;&gt; from mlclient import MLClient\n&gt;&gt;&gt; from mlclient.auth import AuthConfig\n&gt;&gt;&gt; from mlclient.connection import SSLConfig\n\n# Digest (default) - explicit here for clarity\n&gt;&gt;&gt; ml = MLClient(auth=\"digest\", username=\"my-user\", password=\"my-password\")\n\n# Basic auth\n&gt;&gt;&gt; ml = MLClient(auth=\"basic\", username=\"my-user\", password=\"my-password\")\n\n# Client certificate - the cert is the identity, and forces HTTPS;\n# auth defaults to \"certificate\", so setting it explicitly is optional\n&gt;&gt;&gt; ml = MLClient(\n...     host=\"ml.example.com\",\n...     port=8443,\n...     ssl=SSLConfig(cert_file=\"/client.pem\", key_file=\"/client-key.pem\"),\n... )\n\n# Double auth - the client cert sets up mutual TLS as transport, but the\n# user identity comes from a digest credential. Set auth explicitly to keep\n# it instead of letting mutual TLS default it to \"certificate\".\n&gt;&gt;&gt; ml = MLClient(\n...     host=\"ml.example.com\",\n...     port=8443,\n...     auth=\"digest\",\n...     username=\"my-user\",\n...     password=\"my-password\",\n...     ssl=SSLConfig(cert_file=\"/client.pem\", key_file=\"/client-key.pem\"),\n... )\n\n# OAuth 2.0 Bearer token\n&gt;&gt;&gt; ml = MLClient(auth=AuthConfig(method=\"oauth\", token=\"jwt-token\"))\n\n# Kerberos / SPNEGO - credentials come from the ambient ticket cache\n&gt;&gt;&gt; ml = MLClient(protocol=\"https\", auth=\"kerberos\")\n\n# Kerberos with a custom SPN - AuthConfig overrides service / hostname\n&gt;&gt;&gt; ml = MLClient(\n...     protocol=\"https\",\n...     auth=AuthConfig(method=\"kerberos\", service=\"HTTP\", hostname=\"ml.example.com\"),\n... )\n\n# Application-level auth - no HTTP auth header is added\n&gt;&gt;&gt; ml = MLClient(auth=None)\n\n# A custom httpx.Auth handler is passed through unchanged\n&gt;&gt;&gt; import httpx\n&gt;&gt;&gt; ml = MLClient(auth=httpx.BasicAuth(\"my-user\", \"my-password\"))\n</code></pre>"},{"location":"user/python/connections/#valid-and-rejected-combinations","title":"Valid and rejected combinations","text":"<p>Validation rejects combinations MarkLogic cannot serve, and warns on ones that are valid but risky (basic auth over plain HTTP logs a cleartext-credentials warning rather than raising).</p> Combination Result Reason HTTP + digest / basic valid default credential auth HTTPS + digest / basic / oauth / kerberos valid credential or token auth over TLS Mutual TLS + certificate valid client certificate proves identity Mutual TLS + explicit <code>auth=\"digest\"</code> valid double auth: certificate plus digest header Cloud + <code>auth=None</code> valid Cloud authenticates via its API key <code>certificate</code> without a client cert rejected certificate auth requires a client cert over HTTPS client cert + <code>protocol=\"http\"</code> rejected mutual TLS requires HTTPS Cloud + any explicit <code>auth</code> rejected Cloud handles authentication internally Cloud + <code>protocol=\"http\"</code> or custom <code>port</code> rejected Cloud forces HTTPS on port 443 <p>A client certificate plays one of two roles. On its own it is the identity: mutual TLS defaults <code>auth</code> to <code>\"certificate\"</code> and no auth header is sent. Set <code>auth</code> to a credential method explicitly and the certificate drops to being just the transport - mutual TLS sets up the TLS channel while a digest or basic header carries the MarkLogic user identity. That second arrangement is double auth.</p> <pre><code>&gt;&gt;&gt; from mlclient import MLClient\n&gt;&gt;&gt; from mlclient.auth import AuthConfig\n&gt;&gt;&gt; from mlclient.connection import SSLConfig, CloudConfig\n&gt;&gt;&gt; from mlclient.exceptions import ConfigError\n\n# Certificate auth without a client certificate\n&gt;&gt;&gt; try:\n...     MLClient(auth=AuthConfig(method=\"certificate\"))\n... except ConfigError as exc:\n...     print(\"rejected\")\nrejected\n\n# A client certificate cannot be used over plain HTTP\n&gt;&gt;&gt; try:\n...     MLClient(\n...         protocol=\"http\",\n...         ssl=SSLConfig(cert_file=\"/client.pem\", key_file=\"/client-key.pem\"),\n...     )\n... except ConfigError as exc:\n...     print(\"rejected\")\nrejected\n\n# A Cloud connection rejects an explicit auth method\n&gt;&gt;&gt; try:\n...     MLClient(\n...         host=\"my-org.marklogic.cloud\",\n...         cloud=CloudConfig(api_key=\"my-api-key\", base_path=\"/ml/x\"),\n...         auth=\"digest\",\n...     )\n... except ConfigError as exc:\n...     print(\"rejected\")\nrejected\n</code></pre>"},{"location":"user/python/connections/#api-tiers-on-separate-connections","title":"API tiers on separate connections","text":"<p>MLClient reflects MarkLogic's three API tiers (REST, Manage, Admin) as separate connections - see Clients for the tiers, their ports and automatic routing. This section covers how those auxiliary connections derive from and share the primary session.</p> <p><code>connect()</code> and entering a context open only the primary HTTP session. Auxiliary sessions open on their first request, not when accessing an API property. <code>disconnect()</code> and context exit close every opened session. Cached API objects can be reused after reconnecting. Outside a connected client's lifecycle, requests use short-lived sessions.</p> <p>An auxiliary API reuses the primary session when its complete configuration matches, including injected configurations on custom ports. Matching includes host, connection mode, credentials, authentication, retry strategy, pool limits and timeout. Custom auth handlers and retry strategies must be the same object. Explicit limits and effective timeouts are compared by value, so independently constructed settings with equal components can share a session. Unset limits continue to defer to HTTPX defaults. Timeout and limits properties return copies. Change settings with <code>config.clone(timeout=..., limits=...)</code>; mutating a returned object does not change the configuration, its clones, presets or an already-open session.</p>"},{"location":"user/python/connections/#manage-or-admin-on-a-different-host-or-credentials","title":"Manage or Admin on a different host or credentials","text":"<p>Derivation only changes the port; it keeps the primary host, protocol, and credentials. When the Manage or Admin tier lives on a different host, needs different credentials, or listens on a non-standard port (for example behind a reverse proxy), pass a fully resolved HTTPConfig as <code>manage_config</code> or <code>admin_config</code>. That tier then uses the given configuration as-is instead of deriving one from the primary connection; the other tier keeps deriving as usual.</p> <pre><code>&gt;&gt;&gt; from mlclient import MLClient\n&gt;&gt;&gt; from mlclient.http import HTTPConfig\n\n# Manage lives behind a proxy on a different host and port;\n# .admin is still derived from the primary connection (localhost:8001)\n&gt;&gt;&gt; manage_config = HTTPConfig.resolve(\n...     host=\"manage-proxy.example.com\",\n...     port=9002,\n...     username=\"manage-user\",\n...     password=\"manage-password\",\n... )\n&gt;&gt;&gt; with MLClient(host=\"ml.example.com\", manage_config=manage_config) as ml:\n...     dbs = ml.manage.databases.get_list()\n...     ts = ml.admin.get_timestamp()\n</code></pre>"},{"location":"user/python/custom-api/","title":"Extend the client for your application","text":"<p>Suppose your MarkLogic HTTP App Server exposes <code>GET /app/tasks?status=open</code> and returns <code>{\"tasks\": [{\"title\": \"Review imports\"}]}</code>. This is an example application contract, not an endpoint installed by MLClient.</p> <p>You can call it immediately with <code>ml.http.get(\"/app/tasks\", params={\"status\": \"open\"})</code>. For repeated use, extend the same two layers MLClient already gives you: add the endpoint to the REST API so <code>ml.rest.tasks</code> returns the raw HTTP response, and add a service so <code>ml.tasks</code> returns parsed Python. The existing public interfaces already support this; no library internals or global registration are needed.</p>"},{"location":"user/python/custom-api/#follow-the-request-through-the-layers","title":"Follow the request through the layers","text":"<ol> <li><code>TasksGetCall</code> describes the path, query and accepted response type.</li> <li><code>TasksApi</code> exposes a named method returning the raw response.</li> <li><code>AppRestApi</code> subclasses <code>RestApi</code>, adding <code>tasks</code> beside the built-in    <code>eval</code>, <code>documents</code> and <code>transactions</code> - so <code>ml.rest.tasks</code> joins them.</li> <li><code>TasksService</code> calls <code>rest.tasks</code>, checks the status and parses the body -    it builds on the API tier, just as the built-in <code>ml.eval</code> service builds on    <code>ml.rest.eval</code>.</li> <li><code>AppClient</code> overrides <code>rest</code> to build <code>AppRestApi</code> and adds <code>tasks</code> as the    parsed service on top of it - both sharing the client's connection and    lifecycle.</li> </ol> <pre><code>\"\"\"A client extension for an application's hypothetical /app/tasks endpoint.\"\"\"\n\nfrom functools import cached_property\n\nfrom mlclient import AsyncMLClient, MLClient\nfrom mlclient.api import AsyncRestApi, RestApi\nfrom mlclient.calls import ApiCall\nfrom mlclient.clients import ApiClient, AsyncApiClient\nfrom mlclient.http import UNSET\n\n\nclass TasksGetCall(ApiCall):\n    \"\"\"Describe a request for application tasks; perform no network I/O.\"\"\"\n\n    def __init__(self, status=\"open\"):\n        super().__init__(params={\"status\": status}, accept=\"application/json\")\n\n    @property\n    def endpoint(self):\n        return \"/app/tasks\"\n\n\nclass TasksApi:\n    \"\"\"Return the application's raw HTTP response, like the built-in REST APIs.\"\"\"\n\n    def __init__(self, api):\n        self._api = api\n\n    def list(self, status=\"open\", *, timeout=UNSET):\n        return self._api.call(TasksGetCall(status), timeout=timeout)\n\n\nclass AppRestApi(RestApi):\n    \"\"\"The built-in REST APIs plus the application's tasks endpoint.\"\"\"\n\n    @cached_property\n    def tasks(self):\n        return TasksApi(self._api)\n\n\nclass TasksService:\n    \"\"\"Turn task responses into parsed Python values.\"\"\"\n\n    def __init__(self, rest):\n        self._rest = rest\n\n    def open_titles(self, *, timeout=UNSET):\n        response = self._rest.tasks.list(timeout=timeout)\n        response.raise_for_status()\n        return [task[\"title\"] for task in response.json()[\"tasks\"]]\n\n\nclass AppClient(MLClient):\n    \"\"\"Expose tasks as raw HTTP on ``.rest.tasks`` and parsed on ``.tasks``.\"\"\"\n\n    @cached_property\n    def rest(self):\n        return AppRestApi(ApiClient(self.http))\n\n    @cached_property\n    def tasks(self):\n        return TasksService(self.rest)\n\n\nclass AsyncTasksApi:\n    \"\"\"Return the application's raw response without blocking the event loop.\"\"\"\n\n    def __init__(self, api):\n        self._api = api\n\n    async def list(self, status=\"open\", *, timeout=UNSET):\n        return await self._api.call(TasksGetCall(status), timeout=timeout)\n\n\nclass AsyncAppRestApi(AsyncRestApi):\n    \"\"\"The built-in async REST APIs plus the application's tasks endpoint.\"\"\"\n\n    @cached_property\n    def tasks(self):\n        return AsyncTasksApi(self._api)\n\n\nclass AsyncTasksService:\n    \"\"\"Turn task responses into parsed Python values without blocking.\"\"\"\n\n    def __init__(self, rest):\n        self._rest = rest\n\n    async def open_titles(self, *, timeout=UNSET):\n        response = await self._rest.tasks.list(timeout=timeout)\n        response.raise_for_status()\n        return [task[\"title\"] for task in response.json()[\"tasks\"]]\n\n\nclass AsyncAppClient(AsyncMLClient):\n    \"\"\"An async client with the same two-layer application operations.\"\"\"\n\n    @cached_property\n    def rest(self):\n        return AsyncAppRestApi(AsyncApiClient(self.http))\n\n    @cached_property\n    def tasks(self):\n        return AsyncTasksService(self.rest)\n</code></pre> <p>Save this as <code>custom_api.py</code> in your application, then use it just like MLClient. The API layer speaks HTTP; the service layer returns Python:</p> <pre><code>from custom_api import AppClient\n\nwith AppClient(port=8100) as ml:\n    response = ml.rest.tasks.list(timeout=5)  # raw httpx.Response\n    response.raise_for_status()\n\n    print(ml.tasks.open_titles(timeout=5))    # parsed list of titles\n</code></pre> <p><code>ml.rest.tasks.list()</code> gives you the status code, headers and body to handle yourself - the same contract as <code>ml.rest.eval</code> or <code>ml.rest.documents</code>. <code>ml.tasks.open_titles()</code> sits above it, doing the parsing and error handling for you. Reach for the service by default; drop to <code>ml.rest.tasks</code> when you need the raw response.</p> <p>Port 8100 must serve your application endpoint. Built-in REST services only work if that App Server also exposes their <code>/v1/*</code> routes. A custom route does not make an arbitrary HTTP server a MarkLogic REST App Server.</p>"},{"location":"user/python/custom-api/#keep-transport-configuration-in-one-place","title":"Keep transport configuration in one place","text":"<p>The extension wraps public <code>self.http</code>, so authentication, TLS, retry, timeout, connection limits and cleanup remain owned by the parent client. A per-request <code>timeout</code> is forwarded as an execution option, not a query parameter. Use a separate configured client when the application lives on another host or needs different credentials.</p> <p>The async version shares the same Call, since a Call performs no I/O. Only the sending and service methods become async. Inside a coroutine:</p> <pre><code>from custom_api import AsyncAppClient\n\nasync def titles(password):\n    async with AsyncAppClient(port=8100, username=\"my-user\", password=password) as ml:\n        return await ml.tasks.open_titles()\n</code></pre> <p>The two layers mirror MLClient itself: the API tier returns HTTP responses and the service tier returns parsed values. See Services for how the built-in services build on the API tiers. The example is exercised against mocked HTTP responses by the documentation-example tests, including timeout forwarding and server failures.</p>"},{"location":"user/python/documents/","title":"Documents","text":"<p><code>ml.documents</code> is the higher-level DocumentsService for <code>/v1/documents</code>. It exposes three operations - <code>read</code>, <code>write</code> (create or update) and <code>delete</code> - that work with typed <code>Document</code> models instead of raw multipart bodies.</p> <p>Every operation accepts one URI or many, an optional <code>database</code>, and a <code>txid</code> to run inside a transaction. <code>read</code> and <code>write</code> also carry metadata; <code>write</code> and <code>delete</code> also handle temporal collections.</p> <p>Before the operations, two models are worth knowing: <code>Document</code> (what <code>read</code> returns and <code>write</code> accepts) and <code>Metadata</code>.</p>"},{"location":"user/python/documents/#the-document-model","title":"The <code>Document</code> model","text":""},{"location":"user/python/documents/#types","title":"Types","text":"<p>A document is one of five typed subclasses, chosen by content:</p> Subclass Content type <code>content_string</code> <code>content_bytes</code> <code>XMLDocument</code> <code>ElementTree</code> / <code>str</code> / <code>bytes</code> yes yes <code>JSONDocument</code> <code>dict</code> / <code>str</code> / <code>bytes</code> yes yes <code>TextDocument</code> <code>str</code> / <code>bytes</code> yes yes <code>BinaryDocument</code> <code>bytes</code> <code>None</code> yes <code>MetadataDocument</code> none (metadata only) <code>None</code> <code>None</code>"},{"location":"user/python/documents/#building-a-document","title":"Building a document","text":"<p><code>Document.create</code> infers the type from the content and the URI extension:</p> <pre><code>&gt;&gt;&gt; from mlclient.models import Document\n\n&gt;&gt;&gt; type(Document.create(\"/doc-1.xml\", \"&lt;root&gt;&lt;child&gt;data&lt;/child&gt;&lt;/root&gt;\")).__name__\n'XMLDocument'\n\n&gt;&gt;&gt; type(Document.create(\"/doc-2.json\", {\"root\": {\"child\": \"data\"}})).__name__\n'JSONDocument'\n</code></pre> <p>The typed factories <code>Document.xml</code>, <code>Document.json</code>, <code>Document.text</code> and <code>Document.binary</code> force a type instead of inferring one - use them when the URI extension would mislead the inference (<code>.log</code> text, <code>.dat</code> binary):</p> <pre><code>&gt;&gt;&gt; type(Document.text(\"/notes/2024-01.log\", \"line one\\nline two\")).__name__\n'TextDocument'\n</code></pre> <p><code>Document.metadata_update</code> builds a <code>MetadataDocument</code> for a metadata-only change - see Update only the metadata.</p>"},{"location":"user/python/documents/#inspecting-a-document","title":"Inspecting a document","text":"<p>Whatever the subclass, the same accessors read it back. <code>content</code> is typed per subclass (<code>dict</code> for JSON, <code>ElementTree</code> for XML); <code>content_string</code> and <code>content_bytes</code> give the serialized forms:</p> <pre><code>&gt;&gt;&gt; doc = Document.create(\"/doc-2.json\", {\"root\": {\"child\": \"data\"}})\n\n&gt;&gt;&gt; doc.uri\n'/doc-2.json'\n\n&gt;&gt;&gt; doc.doc_type\n&lt;DocumentType.JSON: 'json'&gt;\n\n&gt;&gt;&gt; doc.content\n{'root': {'child': 'data'}}\n\n&gt;&gt;&gt; doc.content_string\n'{\"root\": {\"child\": \"data\"}}'\n</code></pre>"},{"location":"user/python/documents/#the-metadata-model","title":"The <code>Metadata</code> model","text":"<p><code>Metadata</code> holds the five categories MarkLogic keeps beside a document: <code>collections</code>, <code>permissions</code>, <code>properties</code>, <code>quality</code> and <code>metadata_values</code>.</p>"},{"location":"user/python/documents/#structured-metadata","title":"Structured metadata","text":"<p>Build it field by field:</p> <pre><code>&gt;&gt;&gt; from mlclient.models import Metadata\n\n&gt;&gt;&gt; metadata = Metadata(collections=[\"some-collection\"], quality=2)\n&gt;&gt;&gt; metadata.to_json()\n{'collections': ['some-collection'], 'permissions': [], 'properties': {}, 'quality': 2, 'metadataValues': {}}\n</code></pre>"},{"location":"user/python/documents/#raw-metadata","title":"Raw metadata","text":"<p>When you already hold a JSON or XML metadata payload, pass it as <code>raw</code> - the format is auto-detected and parsing is deferred until a field is read:</p> <pre><code>&gt;&gt;&gt; metadata = Metadata(raw=b'{\"collections\": [\"some-collection\"]}')\n&gt;&gt;&gt; metadata.collections()\n['some-collection']\n</code></pre> <p><code>Document.create(..., metadata=...)</code> accepts a <code>Metadata</code>, or the raw <code>bytes</code> / <code>str</code> payload directly - see Write with raw metadata.</p>"},{"location":"user/python/documents/#read","title":"Read","text":"<p><code>read</code> returns a single <code>Document</code> for a string URI, or a <code>dict</code> keyed by URI for a collection of URIs.</p>"},{"location":"user/python/documents/#read-a-single-document","title":"Read a single document","text":"<pre><code>&gt;&gt;&gt; from mlclient import MLClient\n\n&gt;&gt;&gt; with MLClient() as ml:\n...     doc = ml.documents.read(\"/doc-1.xml\")\n\n&gt;&gt;&gt; doc.uri\n'/doc-1.xml'\n\n&gt;&gt;&gt; doc.doc_type\n&lt;DocumentType.XML: 'xml'&gt;\n\n&gt;&gt;&gt; doc.content_string\n'&lt;root&gt;&lt;child&gt;data&lt;/child&gt;&lt;/root&gt;'\n</code></pre>"},{"location":"user/python/documents/#read-multiple-documents","title":"Read multiple documents","text":"<p>A list, tuple or set of URIs returns a <code>dict</code> keyed by URI, each value typed from its own content:</p> <pre><code>&gt;&gt;&gt; with MLClient() as ml:\n...     docs = ml.documents.read([\"/doc-1.xml\", \"/doc-2.json\", \"/doc-4.zip\"])\n\n&gt;&gt;&gt; {uri: type(doc).__name__ for uri, doc in docs.items()}\n{'/doc-1.xml': 'XMLDocument', '/doc-2.json': 'JSONDocument', '/doc-4.zip': 'BinaryDocument'}\n</code></pre>"},{"location":"user/python/documents/#read-as-string-or-bytes","title":"Read as string or bytes","text":"<p><code>content_string</code> and <code>content_bytes</code> give the serialized forms of any content-bearing document:</p> <pre><code>&gt;&gt;&gt; with MLClient() as ml:\n...     doc = ml.documents.read(\"/doc-1.xml\")\n\n&gt;&gt;&gt; doc.content_string\n'&lt;root&gt;&lt;child&gt;data&lt;/child&gt;&lt;/root&gt;'\n\n&gt;&gt;&gt; doc.content_bytes\nb'&lt;root&gt;&lt;child&gt;data&lt;/child&gt;&lt;/root&gt;'\n</code></pre>"},{"location":"user/python/documents/#read-with-metadata","title":"Read with metadata","text":"<p>Pass <code>category</code> to fetch metadata. The values are <code>Category</code> enum members (or their string equivalents). <code>[Category.CONTENT, Category.METADATA]</code> returns the content document with its <code>.metadata</code> populated:</p> <pre><code>&gt;&gt;&gt; from mlclient.models import Category\n\n&gt;&gt;&gt; with MLClient() as ml:\n...     doc = ml.documents.read(\n...         \"/doc-1.xml\",\n...         category=[Category.CONTENT, Category.METADATA],\n...     )\n\n&gt;&gt;&gt; doc.content_string\n'&lt;root&gt;&lt;child&gt;data&lt;/child&gt;&lt;/root&gt;'\n\n&gt;&gt;&gt; doc.metadata.to_json()\n{'collections': [], 'permissions': [], 'properties': {}, 'quality': 0, 'metadataValues': {}}\n</code></pre>"},{"location":"user/python/documents/#read-metadata-only","title":"Read metadata only","text":"<p>Dropping <code>Category.CONTENT</code> returns a <code>MetadataDocument</code> - no content body, just the requested metadata categories:</p> <pre><code>&gt;&gt;&gt; with MLClient() as ml:\n...     doc = ml.documents.read(\"/doc-1.xml\", category=[Category.COLLECTIONS])\n\n&gt;&gt;&gt; doc\n&lt;mlclient.models.MetadataDocument object at 0x7f9200929e20&gt;\n\n&gt;&gt;&gt; doc.content_string is None\nTrue\n\n&gt;&gt;&gt; doc.metadata.collections()\n['some-collection']\n</code></pre>"},{"location":"user/python/documents/#stream-a-large-uri-list","title":"Stream a large URI list","text":"<p><code>read</code> materializes every document into a <code>dict</code>. <code>read_stream</code> yields each one as it arrives and transparently splits a long URI list into several requests (each staying under the httpx URL-length limit) - prefer it for large batches:</p> <pre><code>&gt;&gt;&gt; from mlclient import MLClientManager\n\n&gt;&gt;&gt; mgr = MLClientManager(\"local\")\n&gt;&gt;&gt; with mgr.get_client() as ml:\n...     for doc in ml.documents.read_stream([\"/doc-1.xml\", \"/doc-2.json\"]):\n...         print(doc.uri)\n</code></pre>"},{"location":"user/python/documents/#read-from-a-custom-database","title":"Read from a custom database","text":"<pre><code>&gt;&gt;&gt; from mlclient import MLClientManager\n\n&gt;&gt;&gt; mgr = MLClientManager(\"local\")\n&gt;&gt;&gt; with mgr.get_client(\"my-app\") as ml:\n...     doc = ml.documents.read(\"/doc-1.xml\", database=\"Documents\")\n</code></pre>"},{"location":"user/python/documents/#write-create-or-update","title":"Write (create or update)","text":"<p><code>write</code> accepts one <code>Document</code>, one <code>Metadata</code> (default metadata for a batch), or a list mixing both.</p>"},{"location":"user/python/documents/#write-content","title":"Write content","text":"<pre><code>&gt;&gt;&gt; from mlclient.models import Document\n\n&gt;&gt;&gt; doc = Document.create(\"/doc-1.xml\", \"&lt;root&gt;&lt;child&gt;data&lt;/child&gt;&lt;/root&gt;\")\n&gt;&gt;&gt; with MLClient() as ml:\n...     ml.documents.write(doc)\n</code></pre>"},{"location":"user/python/documents/#write-content-with-metadata","title":"Write content with metadata","text":"<p>Attach a <code>Metadata</code> to the document and both parts are written together:</p> <pre><code>&gt;&gt;&gt; from mlclient.models import Document, Metadata\n\n&gt;&gt;&gt; metadata = Metadata(collections=[\"some-collection\"])\n&gt;&gt;&gt; doc = Document.create(\n...     \"/doc-2.json\",\n...     {\"root\": {\"child\": \"data\"}},\n...     metadata=metadata,\n... )\n&gt;&gt;&gt; with MLClient() as ml:\n...     ml.documents.write(doc)\n</code></pre>"},{"location":"user/python/documents/#write-with-raw-metadata","title":"Write with raw metadata","text":"<p><code>metadata</code> also accepts a raw JSON/XML payload as <code>bytes</code> or <code>str</code>, skipping the <code>Metadata</code> model entirely:</p> <pre><code>&gt;&gt;&gt; doc = Document.create(\n...     \"/doc-1.xml\",\n...     \"&lt;root&gt;&lt;child&gt;data&lt;/child&gt;&lt;/root&gt;\",\n...     metadata=b'{\"collections\": [\"some-collection\"]}',\n... )\n&gt;&gt;&gt; with MLClient() as ml:\n...     ml.documents.write(doc)\n</code></pre>"},{"location":"user/python/documents/#update-only-the-metadata","title":"Update only the metadata","text":"<p><code>Document.metadata_update</code> builds a <code>MetadataDocument</code> that rewrites a document's metadata without touching its content body:</p> <pre><code>&gt;&gt;&gt; metadata = Metadata(collections=[\"some-collection\"])\n&gt;&gt;&gt; doc = Document.metadata_update(\"/doc-2.json\", metadata)\n&gt;&gt;&gt; with MLClient() as ml:\n...     ml.documents.write(doc)\n</code></pre> <p>For a read-modify-write that preserves the other categories, see the collection update recipe.</p>"},{"location":"user/python/documents/#write-multiple-documents","title":"Write multiple documents","text":"<pre><code>&gt;&gt;&gt; doc_1 = Document.create(\"/doc-1.xml\", \"&lt;root&gt;&lt;child&gt;data&lt;/child&gt;&lt;/root&gt;\")\n&gt;&gt;&gt; doc_2 = Document.create(\"/doc-2.json\", {\"root\": {\"child\": \"data\"}})\n&gt;&gt;&gt; with MLClient() as ml:\n...     ml.documents.write([doc_1, doc_2])\n</code></pre>"},{"location":"user/python/documents/#write-with-default-metadata-for-a-batch","title":"Write with default metadata for a batch","text":"<p>A bare <code>Metadata</code> in the list is default metadata: it applies to every document after it that carries none of its own.</p> <pre><code>&gt;&gt;&gt; default_metadata = Metadata(collections=[\"some-collection\"])\n&gt;&gt;&gt; doc_1 = Document.create(\"/doc-1.xml\", \"&lt;root&gt;&lt;child&gt;data&lt;/child&gt;&lt;/root&gt;\")\n&gt;&gt;&gt; doc_2 = Document.create(\"/doc-2.json\", {\"root\": {\"child\": \"data\"}})\n&gt;&gt;&gt; with MLClient() as ml:\n...     ml.documents.write([default_metadata, doc_1, doc_2])\n</code></pre>"},{"location":"user/python/documents/#write-to-a-custom-database","title":"Write to a custom database","text":"<pre><code>&gt;&gt;&gt; doc = Document.create(\"/doc-2.json\", {\"root\": {\"child\": \"data\"}})\n&gt;&gt;&gt; with MLClient() as ml:\n...     ml.documents.write(doc, database=\"Documents\")\n</code></pre>"},{"location":"user/python/documents/#delete","title":"Delete","text":""},{"location":"user/python/documents/#delete-one-or-many-documents","title":"Delete one or many documents","text":"<p>A string deletes one document; a list, tuple or set deletes many, split across requests the same way <code>read</code> batches its URIs.</p> <pre><code>&gt;&gt;&gt; with MLClient() as ml:\n...     ml.documents.delete(\"/doc-1.xml\")\n\n&gt;&gt;&gt; with MLClient() as ml:\n...     ml.documents.delete([\"/doc-1.xml\", \"/doc-2.json\", \"/doc-4.zip\"])\n</code></pre>"},{"location":"user/python/documents/#delete-metadata-categories","title":"Delete metadata categories","text":"<p>With <code>category</code>, the document stays but the listed metadata categories are removed or reset to their defaults:</p> <pre><code>&gt;&gt;&gt; from mlclient.models import Category\n\n&gt;&gt;&gt; with MLClient() as ml:\n...     ml.documents.delete(\n...         \"/doc-1.xml\",\n...         category=[Category.PROPERTIES, Category.COLLECTIONS],\n...     )\n</code></pre>"},{"location":"user/python/documents/#delete-from-a-custom-database","title":"Delete from a custom database","text":"<pre><code>&gt;&gt;&gt; with MLClient() as ml:\n...     ml.documents.delete(\"/doc-1.xml\", database=\"Documents\")\n</code></pre>"},{"location":"user/python/documents/#temporal-documents","title":"Temporal documents","text":"<p><code>write</code> and <code>delete</code> take <code>temporal_collection</code> to operate on a bitemporal document.</p>"},{"location":"user/python/documents/#write-into-a-temporal-collection","title":"Write into a temporal collection","text":"<pre><code>&gt;&gt;&gt; doc = Document.create(\"/doc-1.xml\", \"&lt;root&gt;&lt;child&gt;data&lt;/child&gt;&lt;/root&gt;\")\n&gt;&gt;&gt; with MLClient() as ml:\n...     ml.documents.write(doc, temporal_collection=\"temporal-collection\")\n</code></pre>"},{"location":"user/python/documents/#delete-close-a-temporal-document","title":"Delete (close) a temporal document","text":"<p>Deleting within a temporal collection closes the current version - earlier versions remain queryable:</p> <pre><code>&gt;&gt;&gt; with MLClient() as ml:\n...     ml.documents.delete(\"/doc-1.xml\", temporal_collection=\"temporal-collection\")\n</code></pre>"},{"location":"user/python/documents/#wipe-every-version","title":"Wipe every version","text":"<p><code>wipe_temporal=True</code> removes all versions instead of closing the latest:</p> <pre><code>&gt;&gt;&gt; with MLClient() as ml:\n...     ml.documents.delete(\n...         \"/doc-1.xml\",\n...         temporal_collection=\"temporal-collection\",\n...         wipe_temporal=True,\n...     )\n</code></pre>"},{"location":"user/python/documents/#within-a-transaction","title":"Within a transaction","text":"<p><code>read</code>, <code>write</code> and <code>delete</code> accept a <code>txid</code> to run inside an open multi-statement transaction. Pass the id explicitly, or spread a <code>TransactionService</code> with <code>**</code> to carry both the <code>txid</code> and its database. See Transactions for the full lifecycle.</p> <pre><code>&gt;&gt;&gt; doc = Document.create(\"/doc-1.xml\", \"&lt;root&gt;&lt;child&gt;data&lt;/child&gt;&lt;/root&gt;\")\n&gt;&gt;&gt; with MLClient() as ml:\n...     with ml.transaction() as txn:\n...         ml.documents.write(doc, **txn)\n...         ml.documents.read(\"/doc-1.xml\", txid=txn.id)\n</code></pre>"},{"location":"user/python/eval/","title":"Evaluate code","text":"<p><code>ml.eval</code> is the EvalService: it evaluates XQuery or JavaScript on the server and returns the result already parsed into Python - a <code>fn:current-dateTime()</code> comes back as a <code>datetime.datetime</code>, a JSON node as a <code>dict</code>, and so on.</p> <p>The methods:</p> Method Evaluates <code>xquery</code> (<code>xqy</code>) a raw XQuery string <code>javascript</code> (<code>js</code>) a raw JavaScript string <code>file</code> a file, language detected from its suffix <code>execute</code> general-purpose dispatch over <code>xq</code>/<code>js</code>/<code>file</code> <p>Every method accepts <code>variables</code>, a <code>database</code>, a <code>txid</code> to run inside a transaction, and <code>output_type</code> to skip parsing.</p>"},{"location":"user/python/eval/#evaluate-a-string","title":"Evaluate a string","text":""},{"location":"user/python/eval/#xquery","title":"XQuery","text":"<pre><code>&gt;&gt;&gt; from mlclient import MLClient\n\n&gt;&gt;&gt; with MLClient() as ml:\n...     result = ml.eval.xquery(\"fn:current-dateTime()\")\n&gt;&gt;&gt; result\ndatetime.datetime(2024, 2, 22, 11, 38, 32, 709484, tzinfo=datetime.timezone.utc)\n</code></pre>"},{"location":"user/python/eval/#javascript","title":"JavaScript","text":"<pre><code>&gt;&gt;&gt; from mlclient import MLClient\n\n&gt;&gt;&gt; with MLClient() as ml:\n...     result = ml.eval.javascript(\"fn.currentDateTime()\")\n&gt;&gt;&gt; result\ndatetime.datetime(2024, 2, 22, 11, 39, 22, 264102, tzinfo=datetime.timezone.utc)\n</code></pre>"},{"location":"user/python/eval/#evaluate-a-file","title":"Evaluate a file","text":"<p><code>file</code> detects the language from the suffix:</p> <pre><code>&gt;&gt;&gt; from mlclient import MLClient\n\n&gt;&gt;&gt; with MLClient() as ml:\n...     result1 = ml.eval.file(\"./xqy-code-to-eval.xqy\")\n...     result2 = ml.eval.file(\"./js-code-to-eval.js\")\n</code></pre> <p>The recognized suffixes are:</p> Language Suffixes XQuery <code>.xq</code>, <code>.xql</code>, <code>.xqm</code>, <code>.xqu</code>, <code>.xquery</code>, <code>.xqy</code> JavaScript <code>.js</code>, <code>.sjs</code> <p>Any other suffix raises <code>UnsupportedFileExtensionError</code>.</p>"},{"location":"user/python/eval/#pass-variables","title":"Pass variables","text":""},{"location":"user/python/eval/#as-a-dict","title":"As a dict","text":"<pre><code>&gt;&gt;&gt; from mlclient import MLClient\n\n&gt;&gt;&gt; xq = '''\n... declare variable $DAYS external;\n...\n... fn:current-dateTime() - xs:dayTimeDuration(\"P\" || $DAYS || \"D\")'''\n\n&gt;&gt;&gt; with MLClient() as ml:\n...     result = ml.eval.xquery(xq, variables={\"DAYS\": 5})\n&gt;&gt;&gt; result\ndatetime.datetime(2024, 2, 17, 12, 17, 49, 556376, tzinfo=datetime.timezone.utc)\n</code></pre>"},{"location":"user/python/eval/#as-keyword-arguments","title":"As keyword arguments","text":"<p>The same variables can be passed as keyword arguments:</p> <pre><code>&gt;&gt;&gt; from mlclient import MLClient\n\n&gt;&gt;&gt; xq = '''\n... declare variable $DAYS external;\n...\n... fn:current-dateTime() - xs:dayTimeDuration(\"P\" || $DAYS || \"D\")'''\n\n&gt;&gt;&gt; with MLClient() as ml:\n...     result = ml.eval.xquery(xq, DAYS=5)\n&gt;&gt;&gt; result\ndatetime.datetime(2024, 2, 17, 12, 19, 45, 225135, tzinfo=datetime.timezone.utc)\n</code></pre>"},{"location":"user/python/eval/#within-a-namespace","title":"Within a namespace","text":"<p>Name a namespaced variable in Clark notation (<code>{namespace-uri}local-name</code>):</p> <pre><code>&gt;&gt;&gt; from mlclient import MLClient\n\n&gt;&gt;&gt; xq = '''\n... declare variable $local:DAYS external;\n...\n... fn:current-dateTime() - xs:dayTimeDuration(\"P\" || $local:DAYS || \"D\")'''\n\n&gt;&gt;&gt; with MLClient() as ml:\n...     result = ml.eval.xquery(\n...         xq,\n...         variables={\n...             \"{http://www.w3.org/2005/xquery-local-functions}DAYS\": 5,\n...         },\n...     )\n&gt;&gt;&gt; result\ndatetime.datetime(2024, 2, 17, 12, 21, 28, 547853, tzinfo=datetime.timezone.utc)\n</code></pre>"},{"location":"user/python/eval/#get-raw-output","title":"Get raw output","text":"<p>Without <code>output_type</code> the result is parsed into a Python object. Pass <code>str</code> or <code>bytes</code> to receive the server's response verbatim instead:</p> <pre><code>&gt;&gt;&gt; from mlclient import MLClient\n\n&gt;&gt;&gt; with MLClient() as ml:\n...     result = ml.eval.xquery(\"fn:current-dateTime()\", output_type=str)\n&gt;&gt;&gt; result\n'2024-02-22T12:24:40.362014Z'\n</code></pre> <pre><code>&gt;&gt;&gt; from mlclient import MLClient\n\n&gt;&gt;&gt; with MLClient() as ml:\n...     result = ml.eval.xquery(\"fn:current-dateTime()\", output_type=bytes)\n&gt;&gt;&gt; result\nb'2024-02-22T12:24:53.677793Z'\n</code></pre>"},{"location":"user/python/eval/#evaluate-on-a-custom-database","title":"Evaluate on a custom database","text":"<pre><code>&gt;&gt;&gt; from mlclient import MLClient\n\n&gt;&gt;&gt; with MLClient() as ml:\n...     result = ml.eval.xquery(\n...         \"xdmp:database() =&gt; xdmp:database-name()\",\n...         database=\"Documents\",\n...     )\n&gt;&gt;&gt; result\n'Documents'\n</code></pre>"},{"location":"user/python/eval/#within-a-transaction","title":"Within a transaction","text":"<p>Pass a <code>txid</code> to run inside an open multi-statement transaction, or spread a <code>TransactionService</code> with <code>**</code> to carry both the <code>txid</code> and its database. See Transactions for the full lifecycle.</p> <pre><code>&gt;&gt;&gt; from mlclient import MLClient\n\n&gt;&gt;&gt; with MLClient() as ml:\n...     with ml.transaction() as txn:\n...         ml.eval.xquery('xdmp:document-insert(\"/doc-1.xml\", &lt;root/&gt;)', **txn)\n...         count = ml.eval.xquery(\"fn:count(fn:collection())\", txid=txn.id)\n</code></pre>"},{"location":"user/python/health-and-administration/","title":"Health, versions and administration","text":"<p>Check health and version before using administrative operations. The examples below cover readiness checks, version information and restart waits.</p>"},{"location":"user/python/health-and-administration/#health-check","title":"Health check","text":"<p>healthcheck reports whether MarkLogic's HealthCheck app server answers. That server is unauthenticated by design and returns <code>200 OK</code> only while the node is healthy - load balancers treat any other status as unhealthy.</p> <pre><code>&gt;&gt;&gt; from mlclient import MLClient\n\n&gt;&gt;&gt; with MLClient() as ml:\n...     ml.healthcheck()\nTrue\n</code></pre> <p>The probe maps the response to a verdict:</p> <ul> <li><code>2xx</code> -&gt; <code>True</code> (healthy)</li> <li><code>5xx</code> -&gt; <code>False</code> (server up but not ready)</li> <li><code>4xx</code> -&gt; raises HTTPStatusError; a client error means the request was misdirected (for example, aimed at an authenticated server), not that the node is unhealthy</li> </ul> <p>By default, health checks use a configuration derived from the primary configuration, overriding the port to <code>7997</code>, authentication to none, retry to <code>NO_RETRY_STRATEGY</code> and timeout to <code>_HEALTH_TIMEOUT</code> (5 seconds) - the health probe uses <code>_HEALTH_TIMEOUT</code> regardless of the primary client's timeout. These defaults also apply when the primary already targets <code>7997</code>; an identical effective configuration reuses its session. Cloud connections retain their gateway port and Cloud authentication, while health requests still default to no retries. healthcheck accepts a keyword-only <code>timeout</code> to override the probe's default for a single call.</p> <p>Pass a resolved HTTPConfig as <code>health_config</code> to supply the health connection explicitly. Its host, port, authentication and TLS settings are preserved. Retry and timeout resolve independently: when <code>retry</code> was omitted, health requests use <code>NO_RETRY_STRATEGY</code>, and when <code>timeout</code> was omitted they use <code>_HEALTH_TIMEOUT</code>; an explicitly supplied strategy or timeout is preserved, including <code>None</code> (which disables every HTTP timeout). <code>HTTPConfig.clone()</code> retains whether each was explicitly configured; <code>has_explicit_retry</code> and <code>has_explicit_timeout</code> expose that distinction without changing the normal defaults for other requests.</p> <pre><code>&gt;&gt;&gt; from mlclient import MLClient\n&gt;&gt;&gt; from mlclient.http import HTTPConfig\n\n&gt;&gt;&gt; health_config = HTTPConfig.resolve(\n...     host=\"healthcheck.example.com\",\n...     port=7997,\n...     auth=None,\n... )\n&gt;&gt;&gt; with MLClient(host=\"ml.example.com\", health_config=health_config) as ml:\n...     ml.healthcheck()\nTrue\n</code></pre>"},{"location":"user/python/health-and-administration/#server-version","title":"Server version","text":"<p>version returns an immutable MarkLogicVersion, resolved from <code>xdmp:version()</code> and cached after the first access. <code>str(version)</code> preserves the complete server version. <code>parts</code> is always a four-element tuple of numeric components in their original order, padded with <code>None</code> for missing components. Unpacking yields those same four values.</p> <pre><code>&gt;&gt;&gt; from mlclient import MLClient\n\n&gt;&gt;&gt; with MLClient() as ml:\n...     version = ml.version\n&gt;&gt;&gt; str(version)\n'12.0.1'\n&gt;&gt;&gt; version.parts\n(12, 0, 1, None)\n&gt;&gt;&gt; first, second, third, fourth = version\n&gt;&gt;&gt; fourth is None\nTrue\n\n&gt;&gt;&gt; from mlclient.models import MarkLogicVersion\n&gt;&gt;&gt; version = MarkLogicVersion(\"10.0-9.5\")\n&gt;&gt;&gt; str(version)\n'10.0-9.5'\n&gt;&gt;&gt; version.parts\n(10, 0, 9, 5)\n</code></pre> <p>When the connecting user lacks the eval privilege, the query fails and the Manage (<code>/manage/v2/properties</code>) then Admin (<code>/admin/v1/server-config</code>) endpoints are tried in turn. If none succeeds, the eval MarkLogicError is re-raised. A connection error propagates from the eval attempt without trying auxiliary servers. Unavailable endpoints and malformed fallback responses are skipped.</p> <p>On AsyncMLClient the version is an awaitable method rather than a cached property, because resolving it performs I/O:</p> <pre><code>&gt;&gt;&gt; import asyncio\n&gt;&gt;&gt; from mlclient import AsyncMLClient\n\n&gt;&gt;&gt; async def get_version():\n...     async with AsyncMLClient() as ml:\n...         return (await ml.version()).parts\n&gt;&gt;&gt; asyncio.run(get_version())\n(12, 0, 1, None)\n</code></pre> <p>Numeric components have no universal major/minor/patch/hotfix labels because MarkLogic's versioning scheme changed between releases. Textual suffixes are preserved by <code>str(version)</code> and excluded from <code>parts</code>. An invalid eval version raises <code>ValueError</code>.</p>"},{"location":"user/python/health-and-administration/#wait-for-a-restart","title":"Wait for a restart","text":"<p>Some Management and Admin API operations return <code>202 Accepted</code> together with <code>Location: /admin/v1/timestamp</code> and a <code>restart</code> payload body. That payload contains one or more <code>last-startup</code> entries keyed by <code>host-id</code>. This indicates that MarkLogic accepted the request and that callers should verify readiness through the Admin timestamp endpoint on port <code>8001</code> before issuing follow-up administrative requests.</p> <p>The timestamp endpoint is host-specific, not cluster-wide. MarkLogic documentation explicitly notes that if an operation restarts multiple hosts, the caller must iterate through the returned <code>host-id</code> and timestamp pairs and check each host separately. <code>MLClient.wait_for_restart()</code> does that internally: for multi-host restart responses it resolves host ids through <code>GET /manage/v2/hosts</code> and waits for all affected hosts in parallel. The current client host is probed immediately while the host mapping request is in flight, and the remaining host probes are started as soon as the mapping is available. If the current client host is one of the affected hosts, the method still waits for a timestamp newer than that host's own <code>last-startup</code> value before it returns.</p> <p>Use wait_for_restart for this check:</p> <pre><code>&gt;&gt;&gt; from mlclient import MLClient\n\n&gt;&gt;&gt; with MLClient(port=8002) as ml:\n...     resp = ml.http.delete(\n...         \"/manage/v2/servers/TestServer\",\n...         params={\"group-id\": \"Default\"},\n...     )\n...     ml.wait_for_restart(resp)\n</code></pre> <p>If you call wait_for_restart without a response, it performs a single readiness probe using a retry policy tuned for restart windows:</p> <pre><code>&gt;&gt;&gt; from mlclient import MLClient\n\n&gt;&gt;&gt; with MLClient() as ml:\n...     ml.wait_for_restart()\n</code></pre> <p>For multi-host restart responses, the method waits for all affected hosts before it returns.</p>"},{"location":"user/python/limits/","title":"Resource limits","text":"<p>Set connection-pool limits when creating a client:</p> <pre><code>from mlclient import MLClient\n\nwith MLClient(limits=20) as ml:\n    result = ml.eval.xquery('\"Hello World!\"')\n</code></pre> <p>An int is shorthand for the concurrency cap: <code>limits=20</code> means <code>max_connections=20</code>, leaving the other pool settings at their httpx defaults. Use <code>httpx.Limits</code> to set them independently:</p> <pre><code>import httpx\nfrom mlclient import MLClient\n\nlimits = httpx.Limits(max_connections=20, max_keepalive_connections=10)\nwith MLClient(limits=limits) as ml:\n    result = ml.eval.xquery('\"Hello World!\"')\n</code></pre> <p>The same option is accepted by <code>AsyncMLClient</code>, <code>HTTPConfig</code> and manager factories. It is configured in Python, not environment YAML.</p>"},{"location":"user/python/limits/#pool-settings","title":"Pool settings","text":"<ul> <li><code>max_connections</code>: maximum concurrent connections, including active and idle ones.</li> <li><code>max_keepalive_connections</code>: maximum idle connections kept for reuse.</li> <li><code>keepalive_expiry</code>: how long an idle connection can be reused, in seconds.</li> </ul> <p>When <code>limits</code> is omitted or <code>None</code>, MLClient leaves it to HTTPX. With the currently supported HTTPX version, defaults are 100 connections, 20 idle connections and a five-second keep-alive expiry.</p>"},{"location":"user/python/limits/#limits-and-concurrency","title":"Limits and concurrency","text":"<p>A pool limit bounds connections, not the number of tasks created by your application. If all connections are busy, a request waits for one according to its pool timeout. For large workloads, also bound the work you schedule; the concurrent eval recipe shows this for queries against several databases.</p>"},{"location":"user/python/limits/#separate-api-connections","title":"Separate API connections","text":"<p>Limits apply per underlying HTTP transport. REST, Manage, Admin and Health can use separate transports, each with its own pool. Configurations can share a session only when their effective connection settings are compatible, including pool limits and timeout values.</p> <p>Changing an <code>httpx.Limits</code> object after creating a client does not reconfigure that client's pool. Supply the desired limits when creating the client or use <code>HTTPConfig.clone(limits=...)</code> to prepare a new configuration.</p>"},{"location":"user/python/more-on-environments/","title":"More on environments","text":"<p>Start with the environment guide for a basic YAML file and Python usage. This page is the complete environment configuration guide: YAML fields, inheritance, and creating or customizing configuration in Python. The connections and authentication guide describes supported transport/authentication combinations and their requirements.</p>"},{"location":"user/python/more-on-environments/#yaml-fields","title":"YAML fields","text":"<p>An environment file is a mapping. These are all supported root keys; omit a key to use its default. The names below are the YAML names, including hyphens.</p> Key Value Default <code>app-name</code> Application label used to scope discovery <code>null</code> <code>host</code> Hostname or IP address <code>localhost</code> <code>protocol</code> <code>http</code> or <code>https</code> <code>http</code> <code>username</code> Username for credential-based authentication <code>admin</code> <code>password</code> Password for credential-based authentication <code>admin</code> <code>auth</code> Method name, authentication mapping, or <code>null</code> <code>digest</code> <code>ssl</code> TLS mapping described below, or <code>null</code> <code>null</code> <code>cloud</code> Cloud gateway mapping described below, or <code>null</code> <code>null</code> <code>app-servers</code> List of connection mappings The four predefined connections below <p>Each <code>app-servers</code> entry supports these keys:</p> Key Value Default when omitted <code>id</code> Connection identifier used by the CLI and manager Required <code>port</code> Integer port Connection default: 8000, or 443 for Cloud <code>rest</code> Whether the connection is a default REST candidate <code>false</code> <code>protocol</code> <code>http</code> or <code>https</code> Inherit root <code>username</code>, <code>password</code> Credentials Inherit root <code>auth</code> Method name, authentication mapping, or <code>null</code> Inherit root <code>ssl</code> TLS mapping Inherit root <p>Use distinct identifiers. An identifier is a local connection label, not necessarily the App Server's name in MarkLogic. Root <code>host</code> applies to every entry; use Python overrides when a connection needs another host.</p> <p>The environment models currently ignore unknown keys. Only the fields listed here affect configuration; putting <code>timeout</code>, <code>retry</code> or <code>limits</code> in YAML does not configure HTTP behavior. Values are parsed when loading the environment; connection/authentication combinations are validated when resolving a connection.</p>"},{"location":"user/python/more-on-environments/#root-defaults-and-server-overrides","title":"Root defaults and server overrides","text":"<p>Root settings apply to the environment's connections. An App Server entry can override the fields listed below:</p> Setting Root Per server Override behavior <code>host</code> Yes No Use a Python override for a server on another host <code>protocol</code> Yes Yes A non-null server value replaces the root value <code>username</code>, <code>password</code> Yes Yes Non-null server values replace root credentials <code>auth</code> Yes Yes An explicit server value replaces the root method <code>ssl</code> Yes Yes Explicit server fields merge into root SSL settings <code>cloud</code> Yes No Configures the environment's Cloud gateway <code>port</code> No Yes Sets the connection port <code>id</code>, <code>rest</code> No Yes Identify a connection and mark default REST candidates <code>app-name</code> Yes No Labels the application <p>An omitted server <code>auth</code> inherits the root method. <code>auth: app</code> or an explicit <code>auth: null</code> disables HTTP authentication. For other nullable server fields, null inherits the root value; an empty username or password is an explicit value.</p> <p>For example, use one identity for REST and another for Manage:</p> <pre><code>host: ml.example.com\nprotocol: https\nusername: app-user\npassword: app-password\nauth: digest\napp-servers:\n  - id: app-services\n    port: 8100\n    rest: true\n  - id: manage\n    port: 8002\n    username: manage-user\n    password: manage-password\n    auth: basic\n</code></pre> <p>The <code>manage</code> entry inherits HTTPS and the host, while replacing credentials and authentication. These are connection settings; the corresponding server must already be configured to accept them.</p>"},{"location":"user/python/more-on-environments/#predefined-connections","title":"Predefined connections","text":"<p>These connections are added when absent. Declaring a matching <code>id</code> replaces that entry; declaring a new <code>id</code> adds a connection.</p> Identifier Default port Purpose <code>app-services</code> 8000 Default REST App Server <code>manage</code> 8002 Management API <code>admin</code> 8001 Admin API <code>health</code> 7997 HealthCheck, without HTTP authentication <p>A replacement entry uses the normal field defaults: include <code>rest: true</code> when replacing a connection that should remain eligible for default REST selection. The first declared REST connection is the default; an explicit identifier can select any connection, even one with <code>rest: false</code>.</p> <p>For a custom connection with no port, the connection default is 8000 on premises or 443 for Cloud. To use a custom Manage or Admin port, specify it in that entry.</p>"},{"location":"user/python/more-on-environments/#tls-and-client-certificates","title":"TLS and client certificates","text":"<p>The root <code>ssl</code> object sets TLS defaults. Per-server <code>ssl</code> merges only explicitly specified fields, so a client certificate can be added without losing the root CA configuration:</p> <pre><code>host: ml.example.com\nprotocol: https\nusername: app-user\npassword: app-password\nauth: digest\nssl:\n  verify: /certs/server-ca.pem\napp-servers:\n  - id: content\n    port: 8100\n    rest: true\n  - id: secure\n    port: 8200\n    rest: true\n    auth: certificate\n    ssl:\n      cert_file: /certs/client.pem\n      key_file: /certs/client-key.pem\n</code></pre> <p><code>content</code> uses Digest over HTTPS. <code>secure</code> inherits the CA bundle and authenticates with its client certificate. If <code>secure</code> instead inherits or explicitly selects <code>auth: digest</code>, it presents the TLS certificate and uses Digest credentials.</p> SSL key Value Default <code>verify</code> <code>true</code>, <code>false</code>, or a CA-bundle path <code>true</code> <code>cert_file</code> Client certificate path <code>null</code> <code>key_file</code> Separate private-key path; omit if included in the certificate file <code>null</code> <code>key_password</code> Password for an encrypted private key <code>null</code> <p>Use <code>verify: false</code> only when deliberately disabling server certificate verification. An explicitly supplied <code>null</code> for a nullable SSL field clears that inherited field. A server can replace that field while retaining the root's other SSL fields. For certificate formats, validation rules and examples, see connection modes.</p>"},{"location":"user/python/more-on-environments/#authentication-settings","title":"Authentication settings","text":"<p>Use a method name for <code>basic</code>, <code>digest</code>, <code>digestbasic</code>, <code>certificate</code> or <code>kerberos</code>. The YAML-only <code>app</code> alias means no HTTP authentication. Credential methods use the environment or server's <code>username</code> and <code>password</code>.</p> <p>An authentication mapping supports all of these fields:</p> Auth key Value Default <code>method</code> Authentication mechanism Required <code>token</code> Pre-acquired bearer token for <code>oauth</code> <code>null</code> <code>service</code> Kerberos SPN service component <code>HTTP</code> <code>hostname</code> Kerberos SPN hostname override Request host when omitted <p>Basic and Digest credentials belong in <code>username</code> and <code>password</code>, outside this mapping. Additional fields apply only to their corresponding mechanism.</p> <p>Methods requiring additional data use an <code>auth</code> mapping. For an OAuth bearer token:</p> <pre><code>host: ml.example.com\nprotocol: https\nauth:\n  method: oauth\n  token: my-access-token\n</code></pre> <p>For a custom Kerberos service principal:</p> <pre><code>host: ml.example.com\nauth:\n  method: kerberos\n  service: HTTP\n  hostname: kerberos-host.example.com\n</code></pre> <p>An <code>auth</code> mapping replaces the inherited authentication configuration as a whole; its fields are not merged. Kerberos requires the optional dependencies and an available ticket cache. Python clients additionally accept custom <code>httpx.Auth</code> objects, which cannot be represented in YAML. See authentication methods and valid and rejected combinations.</p>"},{"location":"user/python/more-on-environments/#marklogic-cloud","title":"MarkLogic Cloud","text":"<p>Set <code>cloud</code> at the root and omit <code>protocol</code> and <code>auth</code>:</p> <pre><code>app-name: my-app\nhost: my-org.marklogic.cloud\ncloud:\n  api-key: my-api-key\n  base-path: /ml/my-instance\napp-servers:\n  - id: manage\n    port: 443\n  - id: admin\n    port: 443\n  - id: health\n    port: 443\n</code></pre> <p>Cloud uses HTTPS, port 443 and API-key authentication. API tiers share the gateway and route through <code>base-path</code>. In environment YAML, explicitly replace the on-premises ports of the predefined Manage, Admin and Health entries with 443; the default <code>app-services</code> entry already resolves to the Cloud port. | Cloud key | Value | Default | | --- | --- | --- | | <code>api-key</code> | Gateway API key | Required | | <code>base-path</code> | Gateway route prefix for the instance | Required | | <code>token-duration</code> | Requested token duration in seconds; <code>0</code> leaves the gateway default | <code>0</code> |</p> <p>Cloud settings apply to the whole environment. Root credentials, <code>protocol</code>, <code>auth</code> and <code>ssl</code> do not participate in Cloud resolution; the gateway supplies its own authentication and HTTPS settings.</p>"},{"location":"user/python/more-on-environments/#create-an-environment-in-python","title":"Create an environment in Python","text":"<p>Use MLEnvironment to build the same configuration without a YAML file. <code>model_validate()</code> accepts a dictionary with the YAML key names; nested mappings become the corresponding configuration models:</p> <pre><code>from mlclient import MLClient\nfrom mlclient.env import MLEnvironment\n\nenv = MLEnvironment.model_validate({\n    \"app-name\": \"my-app\",\n    \"host\": \"ml.example.com\",\n    \"username\": \"app-user\",\n    \"password\": \"app-password\",\n    \"app-servers\": [{\"id\": \"content\", \"port\": 8100, \"rest\": True}],\n})\n\nwith MLClient(config=env.provide_config(\"content\")) as ml:\n    print(ml.eval.xquery('\"Hello World!\"'))\n</code></pre> <p>The same inheritance rules and predefined connections apply. Model attributes use Python names: <code>app_name</code>, <code>app_servers</code>, and a server's <code>identifier</code>. Input mappings use <code>app-name</code>, <code>app-servers</code>, and <code>id</code>. To obtain a mapping using YAML names, call <code>env.model_dump(by_alias=True)</code>; this includes credentials, so treat the result as configuration containing secrets.</p> <p>A client built from one resolved configuration does not automatically load the other entries from the environment. For explicit auxiliary configurations:</p> <pre><code>with MLClient(\n    config=env.provide_config(\"content\"),\n    manage_config=env.provide_config(\"manage\"),\n    admin_config=env.provide_config(\"admin\"),\n    health_config=env.provide_config(\"health\"),\n) as ml:\n    response = ml.manage.databases.get_list()\n</code></pre> <p>For an existing manager, <code>manager.config</code> returns an independent environment copy. Assign it back to apply changes to subsequently created clients:</p> <pre><code>from mlclient import MLClientManager\nfrom mlclient.env import MLServerConfig\n\nmanager = MLClientManager(\"local\")\nenv = manager.config\nenv.app_servers.append(MLServerConfig(id=\"reporting\", port=8110, rest=True))\nmanager.config = env\n</code></pre> <p>This changes the manager's in-memory configuration, not the YAML file or clients already created. The manager constructor still loads a named environment; use <code>MLEnvironment</code> and explicit client configurations for a file-free setup.</p>"},{"location":"user/python/more-on-environments/#override-settings-in-python","title":"Override settings in Python","text":"<p>Use manager overrides for application-wide defaults and factory overrides for one client:</p> <pre><code>from mlclient import MLClientManager\n\nmanager = MLClientManager(\"local\", host=\"gateway.example.com\")\nwith manager.get_client(\"app-services\", port=9100) as ml:\n    print(ml.eval.xquery('\"Hello World!\"'))\n</code></pre> <p>Precedence is: resolved environment, manager overrides, then factory-call overrides. Manager overrides apply to every server. Factory overrides apply to the selected server and its corresponding API; neither the YAML nor subsequent factory calls are modified.</p> <p>For example, target a Manage server on another host:</p> <pre><code>with manager.get_client(\"manage\", host=\"manage.example.com\", port=9002) as ml:\n    response = ml.manage.databases.get_list()\n</code></pre> <p>Here both <code>ml.http</code> and <code>ml.manage</code> use the selected Manage configuration. Other API connections retain their environment settings and manager-level overrides. Auxiliary sessions open on their first request; context exit closes all opened sessions.</p> <p>Overrides accept the HTTPConfig.clone parameters: <code>protocol</code>, <code>host</code>, <code>port</code>, <code>auth</code>, <code>username</code>, <code>password</code>, <code>ssl</code>, <code>cloud</code>, <code>retry</code>, <code>limits</code> and <code>timeout</code>. Unknown override names raise <code>TypeError</code>.</p> <p><code>retry</code>, <code>limits</code> and <code>timeout</code> have no environment-YAML counterpart. Their inheritance and Health-specific defaults are described in HTTP configuration, retries, timeouts and resource limits.</p>"},{"location":"user/python/more-on-environments/#load-or-resolve-configuration-directly","title":"Load or resolve configuration directly","text":"<p>Use <code>MLEnvironment.load_file()</code> when the path is already known:</p> <pre><code>from mlclient.env import MLEnvironment\n\nenv = MLEnvironment.load_file(\"path/to/mlclient-local.yaml\")\nconfig = env.provide_config(\"app-services\")\n</code></pre> <p><code>provide_config()</code> resolves one server's YAML configuration. To also apply manager/factory overrides and server-kind HTTP defaults without opening a connection, use <code>manager.get_config(\"app-services\", timeout=10)</code>.</p> <p>Pass a resolved configuration to <code>MLClient(config=config)</code> when building a client directly. Use <code>MLClientManager</code> when auxiliary APIs should also use the environment's Manage, Admin and Health settings.</p>"},{"location":"user/python/retries/","title":"Retries","text":"<p>Set retries when creating a client:</p> <pre><code>from mlclient import MLClient\n\nwith MLClient(retry=2) as ml:\n    response = ml.http.get(\"/v1/documents\", params={\"uri\": \"/hello.json\"})\n</code></pre> <p>An int is shorthand for the retry count: <code>retry=2</code> allows up to two retries and <code>retry=0</code> disables them. Use <code>httpx_retries.Retry</code> to also configure backoff and eligibility:</p> <pre><code>from httpx_retries import Retry\nfrom mlclient import MLClient\n\nwith MLClient(retry=Retry(total=2, backoff_factor=0.5)) as ml:\n    response = ml.http.get(\"/v1/documents\", params={\"uri\": \"/hello.json\"})\n</code></pre> <p>Retry settings belong to the Python HTTP configuration, not environment YAML.</p>"},{"location":"user/python/retries/#default-policy","title":"Default policy","text":"<p><code>DEFAULT_RETRY_STRATEGY</code> allows up to five retries with a backoff factor of <code>0.5</code>. Eligibility depends on the method, response status or exception according to <code>httpx-retries</code>; a failed request is not automatically retried just because a retry budget remains.</p> <p>HealthCheck uses <code>NO_RETRY_STRATEGY</code> by default so a probe returns promptly. For an explicit <code>health_config</code>, an omitted retry strategy still resolves to no retries; an explicitly supplied strategy is preserved.</p>"},{"location":"user/python/retries/#disable-retries","title":"Disable retries","text":"<pre><code>from mlclient import MLClient\nfrom mlclient.http import NO_RETRY_STRATEGY\n\nwith MLClient(retry=NO_RETRY_STRATEGY) as ml:\n    response = ml.http.get(\"/v1/documents\", params={\"uri\": \"/hello.json\"})\n</code></pre> <p>Passing <code>retry=None</code> restores the default policy; it does not disable retries.</p>"},{"location":"user/python/retries/#set-a-manager-policy","title":"Set a manager policy","text":"<p>A manager's explicit policy applies to every connection, including Health. Override it when requesting an individual client:</p> <pre><code>from httpx_retries import Retry\nfrom mlclient import MLClientManager\n\nmanager = MLClientManager(\"local\", retry=Retry(total=2))\nwith manager.get_client(\"health\", retry=None) as ml:\n    healthy = ml.healthcheck()\n</code></pre> <p>Here <code>retry=None</code> restores Health's no-retry default. For another server kind, it restores <code>DEFAULT_RETRY_STRATEGY</code>. Per-call overrides do not change later clients created by the manager.</p>"},{"location":"user/python/retries/#retry-timing","title":"Retry timing","text":"<p>Retries and backoff can make an operation take longer than a single request's timeout. An HTTP timeout limits individual transport phases; it is not a deadline for the entire retry sequence.</p>"},{"location":"user/python/services/","title":"Services","text":"<p>The API tiers - <code>ml.rest</code>, <code>ml.manage</code>, <code>ml.admin</code> and <code>ml.http</code> - speak HTTP. You pass endpoint parameters and get back an httpx <code>Response</code>: a status code, headers and a body you read and interpret yourself.</p> <p>Services sit one level above. A service calls those tiers internally, then does the work you would otherwise repeat by hand - it applies sensible defaults, sends the request, checks the status, and parses the body into a Python value: a document model, a <code>dict</code>, a <code>datetime</code>. You work with results, not payloads.</p> <p>Reach for a service whenever one covers what you need. Drop to an API tier only for an endpoint no service wraps, or when you need the raw status and headers. See Clients for the full tier map.</p>"},{"location":"user/python/services/#client-services","title":"Client services","text":"<p>The essential services hang off a connected client, ready to use:</p> Service Access Guide Documents <code>ml.documents</code> Documents Evaluate code <code>ml.eval</code> Evaluate code Transactions <code>ml.transaction()</code> Transactions <pre><code>from mlclient import MLClient\n\nwith MLClient() as ml:\n    document = ml.documents.read(\"/example.json\")\n    result = ml.eval.xquery(\"1 + 1\")\n</code></pre> <p><code>ml.documents</code> and <code>ml.eval</code> are properties returning stateless services. <code>ml.transaction()</code> is a method instead: opening a transaction performs a request, so it returns a TransactionService scoped to that transaction - a context manager that commits on a clean exit, rolls back on error, and unpacks with <code>**</code> into the operations it should cover. See Transactions.</p>"},{"location":"user/python/services/#additional-services","title":"Additional services","text":"<p>Other services are not on the client facade. They cover narrower or more operational needs, so instead of a ready property you construct them yourself and pass the API tier handle they build on. They are the same layer - parsed Python output, defaults applied - just off the curated client surface.</p>"},{"location":"user/python/services/#logs","title":"Logs","text":"<p>LogsService returns parsed log records through the Management API. Construct it from <code>ml.manage</code>:</p> <pre><code>from mlclient import MLClient\nfrom mlclient.services import LogsService\n\nwith MLClient() as ml:\n    logs = LogsService(ml.manage)\n    for record in logs.get(8002, \"error\"):\n        print(record)\n    files = logs.list()\n</code></pre> <p>The Management connection uses its own configuration, normally port 8002. <code>get</code> filters by app server, log type, time range and regex; <code>list</code> returns the available log files. Prefer a time range over refetching a whole file for large logs. Use the logs command when you only need terminal output.</p>"},{"location":"user/python/services/#log-level","title":"Log level","text":"<p>LogLevelService reads or changes a group or App Server log level. It evaluates the Admin module through the REST server first and falls back to the Management API only when the connecting user lacks the eval privilege - so it needs both <code>ml.rest</code> and <code>ml.manage</code>:</p> <pre><code>from mlclient import MLClient\nfrom mlclient.services import LogLevelService\n\nwith MLClient() as ml:\n    levels = LogLevelService(ml.rest, ml.manage)\n    current = levels.get(group=\"Default\", log_type=\"file\")\n    levels.set(current, group=\"Default\", log_type=\"file\")\n</code></pre> <p>Omit <code>server</code> to target a group, or supply an App Server name for its file log level; App Servers have no system log level. Both <code>get</code> and <code>set</code> accept a keyword-only <code>timeout</code>. Use the log-level command for supported levels and the Management permissions each path needs.</p>"},{"location":"user/python/services/#building-your-own","title":"Building your own","text":"<p>To wrap an endpoint no service covers, see Custom application APIs, which adds an endpoint wrapper to your own client without touching MLClient internals.</p>"},{"location":"user/python/timeouts/","title":"Timeouts","text":"<p>Set a timeout when creating a client:</p> <pre><code>from mlclient import MLClient\n\nwith MLClient(timeout=10) as ml:\n    result = ml.eval.xquery('\"Hello World!\"')\n</code></pre> <p>A number sets all four HTTP timeout components to that many seconds. Use <code>httpx.Timeout</code> to configure them independently:</p> <pre><code>import httpx\nfrom mlclient import MLClient\n\ntimeout = httpx.Timeout(connect=5, read=120, write=60, pool=5)\nwith MLClient(timeout=timeout) as ml:\n    result = ml.eval.xquery('\"Hello World!\"')\n</code></pre>"},{"location":"user/python/timeouts/#timeout-components","title":"Timeout components","text":"Component Limits time spent <code>connect</code> Opening a connection <code>read</code> Waiting between response chunks <code>write</code> Waiting between request-body chunks <code>pool</code> Waiting for a connection from the pool <p>Read and write limits apply between chunks, not to the entire body.</p>"},{"location":"user/python/timeouts/#override-one-request","title":"Override one request","text":"<p>Pass <code>timeout</code> to an operation to change it for that request only:</p> <pre><code>with MLClient(timeout=10) as ml:\n    ml.eval.xquery('\"Hello World!\"', timeout=2)\n    ml.documents.read(\"/doc.xml\", timeout=None)\n    ml.healthcheck(timeout=2)\n</code></pre> <p><code>None</code> disables all HTTP timeouts. Omitting the parameter inherits the client setting. An override does not change later calls.</p>"},{"location":"user/python/timeouts/#defaults","title":"Defaults","text":"Connection Connect Read Write Pool REST, Manage, Admin 5 s 60 s 60 s 5 s Health 5 s 5 s 5 s 5 s <p>A directly created <code>MLClient</code> derives Health with <code>_HEALTH_TIMEOUT</code>, independently of the primary timeout. An explicit <code>health_config</code> timeout or request override changes it. A manager's explicit timeout applies to every server, including Health.</p>"},{"location":"user/python/timeouts/#configuration-precedence","title":"Configuration precedence","text":"<p>Timeouts are Python HTTP options, not environment YAML settings. With a manager, values resolve in this order, with each explicit value replacing the previous one:</p> <ol> <li>The selected server kind's default.</li> <li>The manager's timeout.</li> <li>The client-factory call's timeout.</li> <li>The individual request's timeout.</li> </ol> <p>At every level, omit the argument to inherit; pass a number or <code>httpx.Timeout</code> to replace it, or <code>None</code> to disable it. Timeout components are not merged.</p>"},{"location":"user/python/timeouts/#server-execution-limits-and-total-duration","title":"Server execution limits and total duration","text":"<p>An HTTP timeout does not limit how long MarkLogic may execute a query. Server settings such as a transaction's <code>time_limit</code> control that separately. In eval, <code>timeout</code> is a transport option; use <code>variables={\"timeout\": value}</code> if your query itself declares an external variable with that name.</p> <p>Streaming, multiple requests, eligible retries and backoff can make an operation last longer than one HTTP timeout. It is not a total wall-clock deadline.</p> <p>Sessions compare effective timeout components before sharing a connection. Separately constructed but equal timeouts are compatible; a disabled timeout never shares with a bounded one.</p>"},{"location":"user/python/transactions/","title":"Transactions","text":"<p>A multi-statement transaction groups several document or eval operations so they commit or roll back as a unit. Open one with <code>ml.transaction()</code>; the returned <code>TransactionService</code> is a context manager that commits on a clean exit and rolls back if the block raises.</p> <p>Spread it with <code>**</code> into any content operation to run that operation inside the transaction - it unpacks to <code>txid</code> (and <code>database</code>, when the transaction was opened against one).</p>"},{"location":"user/python/transactions/#commit-on-clean-exit","title":"Commit on clean exit","text":"<pre><code>&gt;&gt;&gt; from mlclient import MLClient\n&gt;&gt;&gt; from mlclient.models import Document\n\n&gt;&gt;&gt; doc_1 = Document.create(\"/doc-1.xml\", \"&lt;root&gt;1&lt;/root&gt;\")\n&gt;&gt;&gt; doc_2 = Document.create(\"/doc-2.xml\", \"&lt;root&gt;2&lt;/root&gt;\")\n\n&gt;&gt;&gt; with MLClient() as ml:\n...     with ml.transaction() as txn:\n...         ml.documents.write([doc_1, doc_2], **txn)\n...         ml.eval.xquery('xdmp:document-insert(\"/doc-3.xml\", &lt;root&gt;3&lt;/root&gt;)', **txn)\n...     # all three inserts commit together here, on the clean exit\n</code></pre> <p>Opened against a specific database, <code>**txn</code> carries the database too:</p> <pre><code>&gt;&gt;&gt; with MLClient() as ml:\n...     with ml.transaction(database=\"Documents\") as txn:\n...         ml.documents.write(doc_1, **txn)\n...         ml.documents.read(\"/doc-1.xml\", **txn)\n</code></pre>"},{"location":"user/python/transactions/#roll-back-on-error","title":"Roll back on error","text":"<p>If anything in the block raises, the transaction rolls back on the way out - even an operation that already succeeded never becomes visible:</p> <pre><code>&gt;&gt;&gt; with MLClient() as ml:\n...     with ml.transaction() as txn:\n...         ml.documents.write(doc_1, **txn)\n...         ml.documents.read(\"/missing.xml\", **txn)   # raises RESTAPI-NODOCUMENT\n...     # the read raised, so the block exits with an error and rolls back:\n...     # doc_1 was never written\n</code></pre>"},{"location":"user/python/transactions/#read-transaction-details","title":"Read transaction details","text":"<pre><code>&gt;&gt;&gt; with MLClient() as ml:\n...     with ml.transaction(database=\"Documents\") as txn:\n...         txn.id           # the server-assigned transaction id\n...         txn.database     # the database it was opened against, or None\n...         txn.status()     # {'transaction-status': {...}}\n</code></pre>"},{"location":"user/python/transactions/#manual-commit-and-rollback","title":"Manual commit and rollback","text":"<p>Without a <code>with</code> block you own the lifecycle - commit or roll back yourself:</p> <pre><code>&gt;&gt;&gt; with MLClient() as ml:\n...     txn = ml.transaction()\n...     try:\n...         ml.documents.write(doc_1, **txn)\n...         txn.commit()\n...     except Exception:\n...         txn.rollback()\n...         raise\n</code></pre>"},{"location":"user/python/transactions/#what-mltransaction-returns-and-how-txn-works","title":"What <code>ml.transaction()</code> returns and how <code>**txn</code> works","text":"<p><code>ml.transaction()</code> opens a server-side transaction (a <code>POST /v1/transactions</code> request) and returns a TransactionService bound to its id. It exposes:</p> <ul> <li><code>txn.id</code> - the server-assigned transaction id.</li> <li><code>txn.database</code> - the database it was opened against, or <code>None</code>.</li> <li><code>txn.status()</code> - the raw status document from the server.</li> <li><code>txn.commit()</code> and <code>txn.rollback()</code> - end the transaction manually.</li> <li>context-manager entry and exit - commit on a clean exit, roll back on an error.</li> </ul> <p><code>**txn</code> works because the service is a mapping: it exposes <code>txid</code>, and <code>database</code> only when the transaction was opened against one. Spreading it into a call passes exactly the keyword arguments that bind that call to the transaction. Opened without a database, <code>**txn</code> yields just <code>txid</code>, so it never passes <code>database=None</code> into an operation that has its own default database.</p>"}]}